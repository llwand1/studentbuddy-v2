# GROWTH-CHANNELS — 免费增长渠道台账

> 版本：v0.1.6 | 状态：[活跃] | 更新：2026-09-23 23:1x（**批次 G-1＝C3 收口 ＋ C1 断口补口（v0.2.117，未发版）**：今天读线上数据逮到一条**批次 E 留下的口径漏洞**——Googlebot 当天四轮抓 `/` 与 `/robots.txt`（后者早就声明 `Sitemap:`），**零抓 `/sitemap.xml`、零抓词条页**；再看线上那份 `/` 的字节：1487、`#root` 空、整页**零个静态 `<a>`** ⇒ **批次 E 那「两处入口」全是 React 渲染产物，对不执行 JS 的首波抓取等于没做**（这笔账记在 E 的口径下，不回改 E 的沿革行）。补法＝在 `packages/web/index.html` 手写一条**静态** `<a href="/terms/index.html">`（目录页自己链向十二条 ⇒ 一跳拿到整簇）＋ 一个 `<link rel="sitemap">`，容器带 `hidden`——★ 目标只是**让爬虫发现 URL**，不是给访客看；真正能确定生效的那条路仍然是老板去站长平台提交 sitemap（＝C9，本批把它点名为全表最便宜的待办）。同批收口 C3（README 体验入口＋把应用里那句既有警示一起搬过去），并**销掉本会话 F-3 欠的产物账**（README／`metrics.json`／`metrics.md` 三本 204／2817；同伴 22:54 已代提到 204／2815，见 `CHANGELOG.md` 那半段补记）。⚠️ **本批效果无法自证**：`hidden` 里那条链接 Google 会不会跟、多久跟，仓内没有任何数据能判，判据在站长平台后台）｜前值 v0.1.5（2026-09-23 **批次 F-3＝C2 公开页分享卡落码（v0.2.116，未发版）**：14 页公开页（首页＋目录页＋12 词条页）各有一张**指向自己**的 1200×630 `og:image`，★ 卡片文案逐字取自 `term-corpus` ⇒ 页与卡结构上不可能漂；`twitter:card` 升 `summary_large_image`（不升＝大卡被裁成缩略图，图白做）；★ **PNG 是提交资源**（发版 tar 与 CI 里都没有浏览器），重新生成＝`npm run og:shots`，配套锁是「**图真的在**」（文件存在 ＋ 从 IHDR 字节读出尺寸 ＋ 页面里每个 `/og/` 地址对得上一个文件 ＋ `robots.txt` 放行 `/og/`）。**至此批次 F 三小批全部落码、全部未发版**。⚠️ **一条台账口径提醒**：同仓另一会话把「AI 出题工具化」那批也叫了**批次 G**（v0.2.115），与本表 §2 预留给 C3/C7/C8 的「批次 G」**不是同一件事** ⇒ 以后引用「批次 G」必须带版本号。｜前值 v0.1.4（批次 F-1＝C5 体验号种子落码，v0.2.113 ★ 未发版；同批把老板四项拍板记进台账：C4 只做后端暂不显示／C2 只做公开页的图／OG 用 HTML 卡真机截图／题材与公开词条同源——★ 其中三条已在 F-2／F-3 兑现））｜前值 v0.1.2（2026-09-23 09:54 **批次 E 发版：C1 两轮都在线上，验收七条全绿；同批把「不存在的 `/terms/` 页返 200 壳」与「首轮 37 条命中全是我自己的 curl」两条如实入账**，见 §4 与 `SEO-SPEC.md` §5 第 9 条）｜创建：2026-09-23（老板 09:06 拍板「现在发」＋ 09:1x 口述「**目前这些渠道都要做下去**」）
>
> ★ **为什么先有这份文件**：渠道清单此前**只活在对话里**——一个都没落到仓内任何文档。这是流程漏洞（§0.13：跑掉的决策必须成文），本文件补上。
> ★ 以后**每开一个渠道先在本表成行**，再动手；不在表里的渠道＝没讨论过，别顺手做。

## 0. 三条总口径（凌驾下面所有条目）

1. **「做能力」＝做进产品，不是 AI 代做动作**。发帖、提交导航站、注册站长平台都是**老板本人的账号动作**（AI 不代做、也不代注册）；本表里凡标 `产品侧` 的格子，才是我该写代码的部分。
2. **数字只许放宽口径，不许改标签**（见 `CHANGELOG.md` 2026-09-23 08:56 行的取证结论）：现有设施算不出 UV（GoatCounter 原始 `hits` 表 0 行），所以任何对外挂出的量**只能叫「访问／次」，不能叫「人」**；要叫「人」就必须换成产品内真实动作计数。
3. **度量可信是渠道的前提**：爬虫与探针流量必须可识别（C10），否则任何渠道效果都是假的——★ 已实测到的污染源：UptimeRobot 952 次/5 天、伪装百度 UA 的扫描器、`ntp.msn.cn` 预取 15 次、referer 垃圾词 `ratelzr/afterawy/fenceud4/gatehk1`。

## 1. 渠道总表

| # | 渠道 | 状态 | 产品侧还要做什么 | 老板本人动作 | 验收判据（可证伪） |
|---|------|------|------------------|--------------|--------------------|
| **C1** | **搜索长尾**（词条讲解页） | 🟢 **已上线两轮**：09-23 09:09（12 页中文静态页＋`robots.txt` 半开放＋`sitemap.xml`）→ **09:54 批次 E**（目录页 URL 换正＋两处入口）。★ 发版后七条判据全绿，实录见 `SEO-SPEC.md` §6.1。★★ **09-23 23:1x 读线上数据定性出一条断口**：Googlebot 当天四轮抓 `/` 与 `/robots.txt` 却**零抓 `/sitemap.xml`、零抓词条页**，而线上那份 `/` 只有 1487 字节、整页**零个静态 `<a>`** ⇒ **批次 E 的「两处入口」要执行 JS 才存在，对不跑 JS 的首波抓取等于没做**（`robots` 与 `sitemap` 声明一直都在，缺的是**走得到的路**）。批次 G-1（v0.2.117，**未发版**）为此补了一条静态入口＋`<link rel="sitemap">`；★ 「补了就有人跟」这句话本表**不承担**——判据仍在站长平台后台（C9） | ①② 已上线：目录页一律走 `CATALOG_PATH=/terms/index.html`（★ 实测 `/terms/` 线上是 SPA 壳），入口两处＝落地页页脚（站内同标签，给爬虫走得到）＋ 词条库空态（新标签）；③ 英文侧词条页（批次 H）；④ JSON-LD（要先实测 CSP Report-Only 会不会刷报告）；⑤ ★ 新挂一条待办：**`/terms/` 下错地址不返 404 而返 200 壳**（soft 404），要真 404 得动 Caddy，见 `SEO-SPEC.md` §5 第 9 条 | 百度搜索资源平台验证站点＋主动推送 API 提交；Google Search Console 验证（HTML 文件走 C9 那条通路） | 上线 1～2 天后 grep Caddy 日志 `/terms/*.html` 是否出现过非零，按 UA 分开看（★ **不许用 GoatCounter 判断**，词条页零 JS 它数不到；★ 也不许拿自己的验收 curl 充数）。09:54 首轮实况：**37 条命中全是我自己的 `curl/8.18.0` ⇒ 真实第三方访客仍为 0**，本条判据**尚未通过**；★ 09-23 23:1x 第二轮同口径复核：当日 `/terms` 命中**依旧全是自己的 curl**（本会话当天 87 次取证请求，全部带 `curl/` UA、可 grep 剔），真实第三方访客**仍为 0** |
| **C2** | **社交分享面**（可分享的卡片与链接） | 🟡 **公开页的图已落码、未发版**（批次 F-3，v0.2.116；★ 零迁移零新依赖零运行时行为改动）：14 页（首页＋目录页＋12 词条页）各有**指向自己那张**的 1200×630 卡，`twitter:card` 升到 `summary_large_image`；PNG 是**提交资源**（重新生成＝`npm run og:shots`，不是构建步骤）。⚪ **站内分享 URL 仍未做**（老板 09-23 拍板推迟——它要动 `term_library` 的公开列＝多租户边界） | 每个词条页/学习成果给**唯一可分享 URL** ＋ 完整 `og:*` ＋ 一张不依赖 JS 的卡；★ 公开页这半边已齐，**产品内（登录后）的成果卡还没有** | 把卡发到即刻／小红书／V2EX／微信群 | 分享出去的 URL 在社交平台的预览卡渲染出正确标题与图（★ 发版后 `curl -sI https://11wand.com/og/<slug>.png` 须回 **200 ＋ `content-type: image/png`**——本站有「错地址也返 200 壳」的前科，只看状态码不算过） |
| **C3** | **开源仓库位**（GitHub） | 🟢 已在做（README 是第二门面，GitHub 来源实测 6 次点击）；**批次 G-1 收口了那条入口行**（★ 文案随同伴 22:54 的代提交 `eeb968e` 已在 main，本批对它只剩数字账）：门面条由「免安装，邮箱注册即用」改为「免安装，进首页点『免注册，直接体验』」，其下补一段零凭证通道说明，★★ **并把应用里那句既有警示一起搬了过去**——「公用池**全站共享**、访客彼此可见 ⇒ 别放个人信息」（与 `landing-copy.ts` 的 `DEMO.warn` 同一句；只搬入口不搬警告＝我在制造隐私事故） | ① 体验入口行 ✅ 已做（G-1）；② badge 与 `metrics --tests` 同源这条由 `metrics --check` 常驻守着，本批顺手把三本产物账对齐到 204／2817；③ **尚未做**：GitHub Topics 与 `awesome-*` 清单 PR（老板动作）、以及 README 那条英文访客进中文应用壳的手感（MT-13 判据④仍挂着） | 提 `awesome-*` 清单 PR、开 GitHub Topics | 来源分析里 `github.com/llwand1/...` 的点击数变化（Caddy 日志／GoatCounter referrer） |
| **C4** | **社会证明计数**（原批次 B） | 🟡 **后端已落码、未发版**（批次 F-2，v0.2.114；★ **有迁移 v41**，非「零迁移」）：契约新立 `docs/GROWTH-SPEC.md` v0.1.0，代码＝`server/src/growth/counters.ts` ＋ 只读出口 `GET /api/growth/counters`（★ 老板拍板**公开 GET＋IP 限流 20/时**，不挂 `requireAuth` ⇒ `AUTH-SPEC` §3 白名单同批加一行） | ★★ **计数只写在 HTTP 入口层**（三个采集点全在 `routes/`）⇒ ① 体验号的八条种子走 `saveOneTerm`、一个请求都不过 ⇒ **结构上进不了这份账**，不靠「记得排除」；② 探针在写入口按批次 E 的两个标记（`X-SB-Probe`／`studentbuddy-probe/*` UA）一处判掉 ⇒ `_probe/prod-pulse` 每几分钟一次的 demo-login 不再灌爆数；③ 三个动作都在**成功之后**才记（429／409／400 不算）；★ 单位是「**IP·天**」＝同一来源 IP 同一自然日只算一次，**只许叫「次」不许叫「人」**（§0 第 2 条），库里**不落裸 IP**（`sha256(盐+IP)` 前 16 位，盐由迁移 v41 用 `randomblob` 生成、不读环境变量）；⚠️ **刻意不含「词条写入」**（那是 owner 维度、与 IP 桶不同单位）；★ 老板拍板**暂不显示**：真实数为 0 时页面什么都不画，端点也就返 0 | **挂在哪**（落地页首屏？）——★ 且必须先满足 `GROWTH-SPEC.md` §4 第 1 条（`SB_TRUST_PROXY` 没配则全站塌成每天一次，看了也是假的） | 后端侧已由 23 例锁死：`counts` == 直读库 `GROUP BY` 复算、同 IP 连打两次 `providers` 只涨 1、带探针标记的 demo-login **零涨**、表里查不到 IP 明文；⚠️ **线上侧一条都还没验**（未发版）⇒ 判据＝发版后真人从自己手机访问落地页，`counts.app_open` 必须 **+1**（★ 这一条同时证明「trust proxy 生效」与「数的是真人」，已派 **MT-18**） |
| **C5** | **零门槛体验转化**（原批次 C：给共享体验号灌种子内容） | 🟡 **已落码、未发版**（批次 F-1，v0.2.113；★ 零迁移零新依赖，题材按老板拍板＝**与公开词条同源**） | `u-demo-shared` 里灌**可看的一屏内容**——★ 实际做法：八个词条（提取练习／间隔重复／遗忘曲线／必要难度／精细加工／组块／元认知／费曼学习法，**名字与 12 页公开讲解页逐个对得上**）经**真实领域函数**写入，进度按 `REVIEW_INTERVALS_DAYS` 回填，⇒ 4 条今天到期、3 个领域、一条从未复习；★ 因走领域函数 ⇒ **事件零发、每日活动零行**，C4 从结构上数不到它 | 提供内容题材（已拍板）；决定「访客彼此可见」的提示要不要加强；⚠️ **线上体验池若已有访客存的词条，种子按锁不灌**——发版前须逐条现查 | 访客点「免注册直接体验」后**首屏不是空态**（真人截图为准，§0.16）⇒ **MT-17 待老板真机跑** |
| **C6** | **中文内容社区**（知乎／少数派／即刻／小红书） | ⚪ 未开工 | 需要的是**素材**：可嵌入的讲解页（C1 已给）、真截图（C5 后才有）、一段 30 秒内说清「词条是主体」的文案 | **本人发帖**（AI 不代注册、不代发） | 帖子发出后来源分布里出现对应域名（Caddy 日志） |
| **C7** | **导航站与自托管清单**（selfh.st／AlternativeTo／Product Hunt／国内自托管导航） | ⚪ 未开工 | 一份提交资产包：一句话简介（≤140 字）、截图、标签、隐私口径（无 cookie 统计／数据自持）——都从 README 与落地页抽，不新造事实 | **本人提交** | 提交列表成文（在本表登记提交时间与链接） |
| **C8** | **更新可见性**（公开 changelog／RSS） | ⚪ 未开工 | 把 `CHANGELOG.md` 渲成一个公开静态页（★ 可与 C1 的 SSG 同一套机制复用），配 `atom.xml` | 转发大版本更新 | 页面可 curl 到、且不含内部挂账与密钥字样（★ 现在 `CHANGELOG.md` 里**有**服务器路径与运维信息，**绝不能原样公开**——要做必须另出清洗版） |
| **C9** | **搜索引擎工具面** | 🟡 **通路已开**（批次 E：验证文件的放法与 `robots` 的坑已写进 SEO-SPEC §5 第 8 条） | `sitemap.xml` 已上线；验证文件由老板从站长平台拿到后放进 `packages/web/public/`，随构建落 `dist/` 根、线上按扩展名直出（★ **坑在这里**：`robots.txt` 默认全封，不放行那一个文件名的话 Googlebot 反而读不到它，验证必失败） | 站长平台账号动作（上传什么文件、填什么 meta 都由平台定，AI 不代注册）。★★ **09-23 点名为全表最便宜、又卡着别人的一条**：验证域名 ＋ **手动提交 `sitemap.xml`**——C1 那处静态入口补完也只是「走得到」，Google 跟不跟没有任何数据能证；而**提交 sitemap 是唯一确定生效的那条路**（批次 E 已经把验证文件的通路铺好了，见 `SEO-SPEC.md` §5 第 8 条） | 验证文件线上可 curl 到（200 且内容逐字等于平台给的那一份）且平台侧显示验证通过 |
| **C10** | **度量卫生**（原任务 #4） | 🟡 **打标已落码**（批次 E）＋ §3 排除清单已成文；⚠️ 读取侧过滤归 C4 | `_probe/probe-marker.mjs` 一份标记三处共用：`User-Agent: studentbuddy-probe/1.0 (<脚本名>)` 进 Caddy 日志、`X-SB-Probe: <脚本名>` 进请求头（`prod-pulse`／`remote-signup`／`owner-isolation`）；★ 但整个 `_probe/` 在 `.gitignore` 第 23 行＝**仓外本机工具**，不随 commit 也不随发版上线，所以「新探针必须带标记」这条**没有锁守**，只有本表与 CHANGELOG 那行记着；★ 打标不等于不污染——写进真库的行还在，剔除发生在读的一侧（C4 计数必须按 §3 剔，这一条是 C4 的硬前提） | 决定 UptimeRobot 留不留 | 日志里一条 grep 能把探针流量与真实访客分开（`grep -v studentbuddy-probe`）；C4 的数按 §3 复算一次能对上 |

## 2. 执行顺序（老板 09:1x：「目前这些渠道都要做下去」）

★ 顺序按**依赖**排，不按编号：C1 收尾（目录页 URL／入口）与 C10（度量卫生）最便宜且是别人的前提 ⇒ 先做；C5（种子内容）是 C2/C6 的素材前提；C4 的口径已被今天的取证钉住，可与 C5 同批；C8 有**必须先清洗**的安全代价；C6/C7 的产品侧只有「素材包」，主体是老板动作。

| 批次 | 覆盖渠道 | 内容 |
|------|----------|------|
| **批次 E** | C1 收尾 ＋ C10 ＋ C9 | ✅ **已上线**（09-23 09:54 发版，七条判据全绿见 `SEO-SPEC.md` §6.1）：目录页 URL 修正（`/terms/index.html`）＋ 两处入口 ＋ 探针打标与 §3 排除清单（★ 打标在 `_probe/`，仓外）＋ 站长验证文件的通路 |
| **批次 F** | C5 ＋ C4 ＋ C2 | 🟡 **三小批全部落码、全部未发版**：**F-1＝C5 体验号种子 ✅**（v0.2.113，★ 零迁移零新依赖）／**F-2＝C4 诚实计数后端 ✅**（v0.2.114：三动作只在 HTTP 入口层记、IP·天去重、探针在写入口剔除、只读出口公开＋限流；★ 老板拍板**暂不显示**；★ **有迁移 v41**）／**F-3＝C2 公开页 `og:image` ✅**（v0.2.116：14 页各一张自指卡 ＋ `summary_large_image`；★ 老板另两轮拍板＝**版式 A 纸面卡**、**底部两行保留**；站内分享 URL 推迟到要动租户边界时再说）。⚠️ **三批叠在一起发版时注意**：只有 F-2 带迁移（v41），F-1／F-3 是纯代码＋静态资源 |
| **批次 G-1** | C3 ＋ C1 断口补口 | ✅ **已落码、未发版**（v0.2.117，★ 零迁移、零新依赖、**零可见 UI 改动**）：README 体验入口收口 ＋ 首页**静态可抓**入口（今天读线上数据逮到的断口）＋ 销本会话 F-3 的产物账三本。⚠️ 与另一会话的「批次 G＝AI 出题工具化」（v0.2.115）**不是一件事**，引用必带版本号 |
| **批次 G-2** | C7 | ⚪ 未开工：提交资产包（文案／标签／隐私口径／截图清单）——产品侧只是**文件**，提交动作归老板 |
| **批次 G-3** | C8 | ⚪ 未开工：**清洗版**公开更新页 ＋ `atom.xml`（★ 红线：不许复用 `CHANGELOG.md` 原文，里面有服务器路径／端口／systemd 单元名） |
| **批次 H** | C1 英文侧 ＋ C6 | 英文词条页（把英文长尾打开）＋ 社区贴文素材 |

⚠️ 每批收口都要过 §0 三条口径与闸门（四连／test-plan 登记／§0.16 真人测试单）；**发版仍归逐次点名授权**，本表不预授权。

## 3. 排除清单（哪些量是「我们自己造出来的」）

★ 任何对外挂出的数都必须先按本表剔一遍。本表是 **C4 的输入**，也是读 Caddy 日志时的 grep 依据。

| 来源 | 落在哪里 | 怎么剔 |
|------|----------|--------|
| `_probe/prod-pulse.mjs`（线上长稳脉冲，demo-login 进 `u-demo-shared`） | 真库：`sessions`／`messages`／`daily_activity`／`user_stats`／`platform_usage`／`auth_sessions`；钱：Agnes 真实计费 | 按 `owner_id = 'u-demo-shared'` 剔（★ 会话它自己会删，但活动与 token 行不删）；日志侧按 UA `studentbuddy-probe/1.0 (prod-pulse)` |
| `_probe/remote-signup.mjs` ＋ `owner-isolation.mjs` 的远端模式（真注册两个测试号） | 真库：`users`／`auth_sessions` 及其名下全部行；`docs/terms` 里 `SECRET-<stamp>` 命名的词条 | 按那两个测试号 `owner_id` 剔；词条按前缀剔 |
| 共享体验号 `u-demo-shared` 的**真实访客**（免注册直接体验） | 同上，与探针混在一个 owner 下 | ★ 剔不开——所以「体验号内容数」不能当「独立用户数」报；要报人，必须换批次 C4 的产品内动作计数 |
| UptimeRobot（`*/5 * * * *` 之外的第三方拨测，实测 952 次/5 天） | 只进 Caddy 日志与 `/api/health`（auth 免检、不写库） | 日志侧按 UA/`/api/health` 剔；留不留由老板定 |
| 扫描器（伪装成 `Baidu; P1 5.1.1) NABar/1.0` 打 `/junhuashen.php` 那一类） | 只进 Caddy 日志 | 按 UA 与非常规路径剔；★ 它不是百度，别拿它证明「百度来过了」 |
| `ntp.msn.cn` 预取、垃圾 referer（`ratelzr`/`afterawy`/`fenceud4`/`gatehk1`） | 只进 Caddy 日志 | 按 referer 域剔 |
| AI 驱动的浏览器探针（CDP／browser-use 真 Chrome） | GoatCounter 的访问数（UA 是**真的**，剔不掉） | ★ 唯一办法是**约定**：AI 浏览器探针不打 `11wand.com`，只在本地栈跑；线上验证一律用 curl（带探针 UA）或老板真人点一次 |

⚠️ GoatCounter 看不到 `/terms/*.html`（那批页零 `<script>`，没有 beacon 可发）⇒ 词条页流量只有一个来源：`grep -oE '"uri":"/terms[^"]*' /var/log/caddy/access.log`（判据与口径见 `SEO-SPEC.md` §5 第 6 条）。

## 4. 沿革

- **v0.1.6（2026-09-23 批次 G-1＝C3 收口＋C1 断口补口，v0.2.117 未发版）**：★★ **这一条的性质是「上一批的验收把自己算成了成果」**。批次 E 那七条判据当时全绿，可绿的全是「文件在不在、字节对不对」，**没有一条问「不执行 JS 的那一份首页里有没有一条走得通的路」**——于是「两处入口已上线」这句话在爬虫面前根本不成立（线上 `/` ＝ 1487 字节、零静态 `<a>`）。今天的读数据把它顶出来的过程记全：Googlebot 四轮抓 `/`＋`/robots.txt`、**零抓 sitemap**，而 robots 里 `Sitemap:` 那行早就在 ⇒ **声明有、路没有**。① 补法选了「往手写 HTML 壳里塞一条静态链接」这条最笨的路，★ 并且当场把「它必须不可见」也做成锁（`term-page.test.ts` ⑮；为什么不用「挂载后 JS 隐藏」：CSP 现为 Report-Only，将来转强制会杀掉负责隐藏的内联脚本，而 `hidden` 不需要 JS 参与）；② 同日把 C3 的入口行收口，**并把警示一起搬**（这一条不是排版洁癖——公用池全站共享，README 是新人唯一的前置阅读物）；③ 销掉自己 F-3 留的产物账（204／2817）；④ ★ **收口自查改短了注释**：初稿把取证过程与 CSP 判断写进了 `index.html`——那是**随站发布的字节**，而同族红线正是下一批 C8 要防的事（公开页不许带内部挂账）⇒ 终版只留一句「看 `SEO-SPEC.md` §5 第 11 条」的指针；★ 连带把一条**易碎判据**改了口：上线后首页壳从 1487 B 变到约 2.5 KB，所以「1487 字节＝壳」这种精确数以后一律读成「差一个 KB 级＋有没有该页正文」（`SEO-SPEC` §5 第 11 条附注②）。⚠️ **诚实记未做**：本批**不产生任何抓取效果证据**，判据全在站长平台（C9 已点名）；线上仍是旧壳（未发版）；MT-21（「首页逐像素没变」）未回收；★ 当日读数里另有两条与渠道无关但值得记的：**新注册用户 1 个**（其出口 IP 与本会话取证用的代理重合，很可能就是老板本人那条测试，未定性）、**内容安全侧一条缺陷**（搜索兜底通道里，一个学生向查询的第一条结果是成人站；本会话只报告未动码，等老板排期）。
- **v0.1.5（2026-09-23 批次 F-3＝C2 公开页分享卡，v0.2.116 未发版）**：**C2 的「公开页这半边」落码**——14 页各一张自指的 1200×630 卡 ＋ `summary_large_image`。★★ **两条值得从代码里抬出来记进台账**：① **图是提交资源、不是构建产物**——这个选择不是偷懒，是「发版 tar 与 CI 里没有浏览器」这条事实逼出来的，代价＝语料变了必须有人记得重跑 `npm run og:shots`；本批把它变成**机器可查**（「图真的在」那一组锁住文件存在、IHDR 尺寸、引用与文件对得上），并让生成器**量排版不量像素**（越界盒与最低内容底边报在退出码里，实测 14 张全在 566px < 630）；② **§7 取证第一轮逮到自己一把软锁**——「卡上数字与语料字段一致」在**语料每条恰好 3 误区 3 动作**时拦不住有人把 3 写死（16 例全绿），改成按派生判才红；这条已升格为 test-plan §7 的通用约定（**恒定字段不构成锁**）。⚠️ **未验**：`/og/*.png` 在线上是否真由 Caddy 直出（本站有「错地址也返 200 壳」的前科 ⇒ 判据要带 `content-type`）、卡片在真实对话框里的观感（**MT-20 待派**）。★ 站内分享 URL 那半边**仍未做**（老板拍板推迟，动 `term_library` 公开列＝多租户边界，不是忘了）。
- **v0.1.3（2026-09-23 批次 F-1）**：**C5 落码（未发版）**——八条词条种子灌进体验号，题材按老板拍板与 12 页公开讲解页**同名同源**（★ 同名这条不是文案偏好，是锁：`term-corpus.test.ts` 第 9 例逐条核名）。★★ **本批两条与「诚实」有关的设计值得从代码里抬出来记在台账**：① 种子**只经领域函数写** ⇒ 事件总线零事件、`daily_activity` 零行 ⇒ **F-2 的计数从结构上数不到种子**（不是「记得排除」）；② **池内已有任何访客存的词条就整批不灌**（代价：将来加种子词条，老池不会自动补）。⚠️ **一条尚未知的运行事实**：线上体验池是否已被早先访客写过**未查**——若已写过，发版后首屏仍是空的，那是**既定行为不是 bug**，判据是查库不是猜。★ C4／C2 的**范围收窄**是老板拍板，不是本台账自己缩的（见 §2 批次 F 行）。
- **v0.1.2（2026-09-23 09:54 批次 E 发版）**：C1 转 🟢 已上线两轮，验收七条全绿；★ **同批从线上验收里新逮到一条代价**——不存在的 `/terms/*.html` 返 **200＋1487 字节 SPA 壳**而不是 404（soft 404，已记入 `SEO-SPEC.md` §5 第 9 条，并把 §6 第 2 条判据改成「状态码＋字节数」）。★ 效果观测**尚未通过**：首轮 37 条 `/terms` 命中全是本轮验收自己打的 `curl`，真实第三方访客为 0——这条诚实写进 C1 判据格，不许拿它说「有流量」。
- **v0.1.1（2026-09-23 批次 E）**：C1 目录页 URL 与两处入口落码（未发版）；C10 探针打标与 §3 排除清单成文；C9 补上「`robots` 默认全封会挡住站长验证文件」这个坑与通路。
- **v0.1.0（2026-09-23）**：首版。渠道清单从对话里落成台账（C1–C10 ＋ 批次 E/F/G/H 顺序）。
