# GROWTH-CHANNELS — 免费增长渠道台账

> 版本：v0.1.14 | 状态：[活跃] | 更新：2026-09-25 21:3x（★★ **真实性补两行**：§3 排除清单加「自家裸 `curl` 灌进 `app_open`（实测 27→28，且**不落裸 IP ⇒ 不可回收**）」与「当日 13 条带 `?ref=` 的请求逐条读穿＝**全部非真人、一条都没进产品账**」。★ 后者顺带证成了一件好事：**不执行 JS 的抓取器根本触发不了 `app_open`**，所以本表 C4 那条隐患的边界比原先写的更窄。｜上一版 v0.1.13（2026-09-24 22:5x，★★ **批次 I＝归因层落码（拟 v0.2.123、⚠️ 未发版）＝本表第一次「渠道效果」有一个不靠猜的读数**：迁移 **v42** 给诚实计数加一列 `source`，公开端点多返一个 `bySource`，22 张静态页回应用的链接全部自带 `?ref=`。**动因是当天的线上取证而不是一个新想法**——现读当天 **12 个 `app_open` 桶没有一个查得出来源**（站内一跳之后 referer 就变成 `11wand.com` 自己 ⇒ 从贴吧／班级群／词条页点进来的人第二跳起再也认不出）。★★ **本批最值钱的一条是自己查出来的一处结构性盲区**：`Landing.tsx` 那句 `app_open` 的采集走的是**裸 `fetch`**，绕开了全站唯一的请求出口层 ⇒ **归因头在任何写法下都带不上去**（★ 不是漏测，是「写在哪都不会有」，行为测试全绿也照样瞎）；改走 `api.auth.surface()` 之后，因「退回裸 fetch 时所有行为测试照样绿」而**立了本仓第一条源码锁**。⚠️ **两条刻意没做（老板拍板，不是漏）**：`app_open` **采集点不换**、UA 黑名单**不加** ⇒ `bySource` 里 `direct` 那一行**含第三方抓取器**（§3 最后那行的三种修法仍未裁定，代价记进 `GROWTH-SPEC.md` §4 第 8 条）；★ 本批先把 `bySource` 拿在手里取证。⚠️ **另补了本表自己的一档欠账**：§4 沿革整条漏了 v0.1.12（现读旧首条＝v0.1.11），破的是本表自家「每版在 §4 成行」那条形状——本批补一条**指回原文的摘要**，**不重抄全文**（★ 两处各一份＝将来必有一份改不到，与 H-2 那批「过期的图会被当成证据直接发出去」同族）。｜前值 v0.1.12（批次 H-2＝C6 素材包落码，本表唯一一条纯文档批次）全文见下一行。
>
> 版本：v0.1.12 | 状态：[活跃] | 更新：2026-09-24 13:2x（★★ **批次 H-2＝渠道 C6 素材包落码（纯文档、零代码、不占版本号），本表 C6 那格从 ⚪ 转 🟡**：新文件 `GROWTH-COMMUNITY-PACK.md` v0.1.0——12 条讲解页现读成选题表、四条 30 秒主张逐条标出处、四平台各一张卡、§6 那张登记表就是 C6 的验收判据（★ 此刻空着，所以 C6 只到 🟡 不到 🟢）。★ **本批最有价值的两条都不是"素材"，是把两条我们一直在用的说法当场打折**：**① 仓里那六张真机截图是旧的，而且旧得看得见**——`landing-hero.png`（09-21 14:47 拍）图上统计条写 **149 个 REST 接口**，现读 `landing-copy.ts` 是 **150**；而 09-22 09:33 那次整页重排（`a2c49ec`）把「**词条是主体**」搬上前排＝**C6 要讲的那句话，在那张唯一的首屏图上不存在**。⚠️ 这条比「缺图」危险：缺图会让人去补，**过期的图会被当成证据直接发出去**。⇒ 本表 §3 之外新增一条维护口径：**改落地页要回头问一句那六张图还代不代表它**。**③ ★★ 149→150→151 那三级漂移不是「没人管」，是守它的那把锁安静地空转了 2 天**：`tools/metrics.mjs` 的 `landingDrift()` 用字面正则 `/<span>([^<]+)<\/span>/` 抓 `Landing.tsx` 统计块里的四句文案，而 `c3d14e7`（09-22 17:40 中英切换那批发版）把文案搬进 `landing-copy.ts` 的 `STATS` 数组 ⇒ 块里只剩 `<span key={…}>`，**一个字都抓不到 → `claims` 为空 → 调用方 `if (length)` 直接跳过明细 → 照样打印「✓ 首屏可核对数字与实测一致」**。⇒ 判据形状从此要加一条：**「✓」不等于「跑过」**，`metrics`／`gates` 里凡是「抓不到就算通过」的那类，与「抓不到就报错」的那类**不是一类东西**（本条机理、证据链与「为什么不顺手修」见 `GROWTH-COMMUNITY-PACK.md` §4 与 §7 第 8 条；★ 修法要改代码，判在老板）。**② 上一行「AI 探针只在本地栈跑」这条约定挡不住污染**——`index.html` 里那枚 GoatCounter beacon 是**绝对地址** `https://11wand.com/stats/count`，localhost 打开也打到线上，且带正常浏览器 UA、**事后剔不掉**（§3 排除清单已加一行，含未跑的判据）。⚠️ 顺带一条**只读数不改判据**：13:2x 只读实查线上 `users` 表 **6 行**（09-2x 那次是 3）⇒ 仍是「不许在对外文案里报数」那一格，但 C4/C6 的效果观测从此多一个可对账的锚。★ 另订正 `README.md` 目录树那两行**早就过期**的数：`27 份 SPEC 契约`→**29**（现读 `ls *-SPEC.md`）、`dev/ 四份`→**五份**（★ 都不是本批改坏的，本批只是打开才发现——同一条「图不会因为代码改了而自动过期」的形状）。｜前值 v0.1.11（两条取证改写判据形状＋C1 英文侧落码）全文见下一行。
> 版本：v0.1.11 | 状态：[活跃] | 更新：2026-09-24 12:2x（★★ **两条取证改写了本表的判据形状，一条落码填了 C1 的空格**。**① 落码**：批次 **H-1＝C1 英文侧**（6 条英文词条页＋英文目录页＋双向 `hreflang`＋7 张卡，拟 v0.2.119，⚠️ **未发版**）⇒ 公开页 14 → **21**。**② 取证（老板 12:0x「站长平台刚才搞了一些了」触发）**：★ **必应不是没来过——`bingbot` 全历史 11 次**（本表从前只登记 Googlebot 与「百度 0 次」，**因为词表里根本没有 bingbot 这一项**，这是判据的漏而非线上的漏）；09-24 **11:54 它抓 `/BingSiteAuth.xml` 得 404、12:04 同一 URL 得 200＋85 字节** ⇒ 老板那侧「补文件→必应重试→通过」在线上留了完整一圈痕；★ 而**线上 `robots.txt` 至今没为它开 `Allow`（现读 grep＝0），`bingbot` 照样拿到 200** ⇒ 本册与 `SEO-SPEC` §5 第 8 条那句「robots 不放行，验证文件必失败」**对必应不成立**，按实测改口（那条坑是按 Google／百度形状推的）。⚠️ **一条没解决的挂账**：那份 85 字节现在**在线上、也在我的共享工作树里，却不在 git 里**（★ 归放它的那一方提交，我不代提交＝R2）⇒ 下次从**干净检出**构建会把它丢掉，而线上那份是孤儿字节。★ **③ 同轮把另一条万能判据打折了**：`/NoSuchFile917.xml` 返**真 404／0 字节**、`/terms/bu-cun-zai.html` 返 **200／1888 字节壳** ⇒ 「错地址一律返壳」**说满了**，它只管 `/terms/` 那一族；**根级 URL 写错是抓得到的**（详见 `SEO-SPEC` §5 第 9 条回标）。★ **④ 本表 §3 从此多出两族量**：伪 iPhone Safari 的 **41-IP 农场**（01:58–03:58 顺着目录页把 12 个中文词条页逐个走完、并摸到 `/changelog/index.html` 与 `/atom.xml`）与 `Go-http-client/1.1` 五 IP（19:56 读 robots→跟 `Sitemap:`→执行 JS）⇒ C1 那句「链接图对爬虫走得通」**第一次有正向证据**；⚠️ 反面是 C4 从此挂一条隐患：**计数只剔我们自己的探针标记，而这两族会执行 JS** ⇒ `app_open` 里混着爬虫，`counts` 的绝对值在修法裁定前不许裸引（三个修法在 §3 最后那行）。｜前值 v0.1.10：线上 `/changelog/index.html` 7204 B／零 `<script>`，`/atom.xml` 9 条 `<entry>`，`robots.txt` 两扇新 `Allow` 都在，★ **那条「公开字节不许带内部字样」的新锁第一次在线上被读到＝更新页全文扫内部字样族 0 命中**。★★ **本批那条「机器拦不住、只有人守」的代价，本轮第一次被执行了**：发版当批回来往清洗表加了 v0.2.118 一行 ⇒ 从此 **仓内 10 条／线上 9 条**，下次发版该读到 10（★ 这条就是那条代价的验收判据，写进 `SEO-SPEC` §6 第 10 条回标与 §6.3）。⚠️ **同轮订正两条我自己的账**：① 回归脚本**凭记忆敲词条 slug**（12 个里 11 个不存在，全拿回 1888 B 的壳），真 slug 现读自语料文件后 12 页 6830～7070 B 全部正常——与 09-23「按裸词扫误报 5 页」同族；② 「词条页输出逐字节未变」说满了，页脚那条新入口让 12 页各涨约 485 B。★ 一条读数真相：线上 `/atom.xml` 的 `Content-Type` 是 Caddy 按扩展名给的 `text/xml`，**不是**我们声明的 `application/atom+xml`。｜前值 v0.1.9（批次 G-3＝C8 落码：手写清洗表＋公开字节红线锁第一轮就红在既有泄漏上）全文见 §4。｜前值 v0.1.8（批次 G-2＝C7 资产包）全文见 §4。｜创建：2026-09-23（老板 09:06 拍板「现在发」＋ 09:1x 口述「**目前这些渠道都要做下去**」）
>
> ★ **为什么先有这份文件**：渠道清单此前**只活在对话里**——一个都没落到仓内任何文档。这是流程漏洞（§0.13：跑掉的决策必须成文），本文件补上。
> ★ 以后**每开一个渠道先在本表成行**，再动手；不在表里的渠道＝没讨论过，别顺手做。

## 0. 三条总口径（凌驾下面所有条目）

1. **「做能力」＝做进产品，不是 AI 代做动作**。发帖、提交导航站、注册站长平台都是**老板本人的账号动作**（AI 不代做、也不代注册）；本表里凡标 `产品侧` 的格子，才是我该写代码的部分。
2. **数字只许放宽口径，不许改标签**（见 `CHANGELOG.md` 2026-09-23 08:56 行的取证结论）：现有设施算不出 UV（GoatCounter 原始 `hits` 表 0 行），所以任何对外挂出的量**只能叫「访问／次」，不能叫「人」**；要叫「人」就必须换成产品内真实动作计数。
3. **度量可信是渠道的前提**：爬虫与探针流量必须可识别（C10），否则任何渠道效果都是假的——★ 已实测到的污染源：UptimeRobot 952 次/5 天、伪装百度 UA 的扫描器、`ntp.msn.cn` 预取 15 次、referer 垃圾词 `ratelzr/afterawy/fenceud4/gatehk1`。

## 1. 渠道总表

| # | 渠道 | 状态 | 产品侧还要做什么 | 老板本人动作 | 验收判据（可证伪） |
|---|------|------|------------------|--------------|--------------------|
| **C1** | **搜索长尾**（词条讲解页） | 🟢 **已上线两轮**：09-23 09:09（12 页中文静态页＋`robots.txt` 半开放＋`sitemap.xml`）→ **09:54 批次 E**（目录页 URL 换正＋两处入口）。★ 发版后七条判据全绿，实录见 `SEO-SPEC.md` §6.1。★★ **09-23 23:1x 读线上数据定性出一条断口**：Googlebot 当天四轮抓 `/` 与 `/robots.txt` 却**零抓 `/sitemap.xml`、零抓词条页**，而线上那份 `/` 只有 1487 字节、整页**零个静态 `<a>`** ⇒ **批次 E 的「两处入口」要执行 JS 才存在，对不跑 JS 的首波抓取等于没做**（`robots` 与 `sitemap` 声明一直都在，缺的是**走得到的路**）。批次 G-1（v0.2.117）为此补了一条静态入口＋`link rel="sitemap"`，★ **09-23 23:5x 已随五批发版上线**（现读线上 `/`：该条** 1 条**、`rel="sitemap"` 1 条、带 `hidden` 的 nav 1 条，且它是整页**唯一**的一个 a 标签）；★ 「补了就有人跟」这句话本表**不承担**——判据仍在站长平台后台（C9）。★★ **09-24 这句话有了一条正向读数（但只到「走得到」为止）**：`Go-http-client` 一族读了 `robots.txt` 再抓 `/sitemap.xml`（200）并执行 JS，伪装 iPhone 的 41-IP 农场顺着目录页**把 12 个中文词条页逐个走了一遍**（另抓到 `/changelog/index.html` 与 `/atom.xml`）⇒ **「链接图对爬虫走得通」从推定变成了日志里读得出的事实**；⚠️ 而**这两族都不是人**（判读与剔除见 §3 新增两行），且**Googlebot 至今零抓 `sitemap.xml`、Baiduspider 至今全历史 0 次** ⇒ 本条「收录」判据仍未通过。★★ **同日报道 批次 H-1＝英文侧落码**（拟 v0.2.119，⚠️ **未发版**）：6 条英文词条页（`/terms/en/<ascii-slug>.html`）＋ 英文目录页 ＋ 与中文侧的**双向 `hreflang`** ＋ 7 张自指分享卡 ⇒ 公开页从 14 变 **21**、`sitemap` 的 `<loc>` 从 14 变 **21**、落盘件数 16 → **23**（★ 英文侧**没为 `robots.txt` 开新扇**，`Allow: /terms/` 那扇前缀顺带覆盖，代价见 `SEO-SPEC.md` §6 第 11 条最后一条） | ①② 已上线：目录页一律走 `CATALOG_PATH=/terms/index.html`（★ 实测 `/terms/` 线上是 SPA 壳），入口两处＝落地页页脚（站内同标签，给爬虫走得到）＋ 词条库空态（新标签）；③ ✅ **英文侧词条页已落码**（批次 H-1，拟 v0.2.119，⚠️ 未发版）——★ 但**人看得见的英文入口仍欠**：英文页此刻只被「`hidden` 容器里那条静态链的下一级」与「六条配对中文页顶栏的语言切换」两处 reach 到，落地页页脚要不要加一条 English 链是视觉改动，判定权在老板（MT-24 判据④）；⑥ ★ **09-24 14:2x 档位 1 已落码（拟 v0.2.121，⚠️ 未发版）**：✅ 三条改名全部落地（`主动回忆`／`交错学习`／`艾宾浩斯遗忘曲线`，旧名逐字留在页面上）、✅ `间隔重复` 首屏已摆 1/2/4/7/15/30/60（与产品契约同值同序，有锁）、✅ `费曼` 已补「四步＋一个走到底的例子」、✅ `认知负荷` 一个字没改（守判决）。★ 连带面两条：三张 og 卡重截（其余 18 张逐字节未变＝探针可复现的证据）、`DEMO_SEED_TERMS` 两个名字同步改名（有锁逼着两边同源）。★ 判据与口径边界已进 `SEO-SPEC.md` §4.1。⚠️ 下面照抄派发时原文、不回改：中文 12 页的**主词按两轮搜索框自动补全改口**——`提取练习`→以「**主动回忆**」为主词（"提取练习"的中文补全全跑题到"提取公因式"）、`交错练习`→「**交错学习**」、`遗忘曲线` 的 title 补「**艾宾浩斯**」并把「**复习计划表**」写进页内、`间隔重复` 首屏直接摆 1/2/4/7/15/30/60、`费曼学习法` 补「四步＋例子」；★ `认知负荷` 保持书面（补全是学术意图，硬口语化反而漂）；⚠️ **口径**：自动补全 ≠ 搜索量（它只说明"最近有人这么打"），但"哪个词在中文里是活的"这个定性判断把握度高，因为死词的补全结果完全跑题；⑦ ★ **09-24 再挂一条（档位 3，做进产品那一条）**：**免登录、纯前端、不落库的「艾宾浩斯复习计划表在线生成器」**——正面命中「…复习计划表生成／在线生成／时间表」这一族**工具意图**词（今天两轮测试里密度最高的一族），★ 而这族词**讲解页接不住**（搜"生成"的人要的是能用的东西）；复用现成 `REVIEW_INTERVALS_DAYS`＋现成 SSG 落盘管线＋OG 卡与 `Allow` 通路；⚠️ 前提纪律＝**纯前端生成、不写 `term_library` 公开列**（那扇门老板 09-23 已拍板推迟），且新开公开面**要记得回 `robots.txt` 补一扇 `Allow`**（本站默认全封，忘加＝静默不公开）；④ JSON-LD（要先实测 CSP Report-Only 会不会刷报告）；⑤ ★ 新挂一条待办：**`/terms/` 下错地址不返 404 而返 200 壳**（soft 404），要真 404 得动 Caddy，见 `SEO-SPEC.md` §5 第 9 条 | 百度搜索资源平台验证站点＋主动推送 API 提交；Google Search Console 验证（HTML 文件走 C9 那条通路） | 上线 1～2 天后 grep Caddy 日志 `/terms/*.html` 是否出现过非零，按 UA 分开看（★ **不许用 GoatCounter 判断**，词条页零 JS 它数不到；★ 也不许拿自己的验收 curl 充数）。09:54 首轮实况：**37 条命中全是我自己的 `curl/8.18.0` ⇒ 真实第三方访客仍为 0**，本条判据**尚未通过**；★ 09-23 23:1x 第二轮同口径复核：当日 `/terms` 命中**依旧全是自己的 curl**（本会话当天 87 次取证请求，全部带 `curl/` UA、可 grep 剔），真实第三方访客**仍为 0**；★ **09-24 11:5x 第三轮同口径**：当日 `/terms/*.html` 的**非我方命中第一次不为 0**——12 页各一次，全部来自那族伪装 iPhone Safari 的 41-IP 农场（剔除口径见 §3）⇒ **判据的形状要改口了**：从前「剔掉自己之后是不是 0」是二值判据，现在日志里已经住着两族会顺着链接走的抓取器，**光"非零"不再等于"有真人"**，此后这条判据只认「剔掉 §3 全部已登记来源（含自家 curl、两种探针 UA、UptimeRobot、两族爬虫、`ntp.msn.*` 预取、垃圾 referer）之后还剩不剩」。★★ **09-24 批次 I（归因层，拟 v0.2.123、⚠️ 未发版）给这条路配上了「谁点的」**：22 张静态页回应用的链接全部自带 `?ref=`（词条页 `terms`／英文页 `terms-en`／更新页 `changelog`／计划表页 `plan`）——★ 静态页零 JS，访客点进 SPA 之后那一跳**在库里与直接输网址长得一模一样**，从前这条链路根本没有出生地；现在它落在 `bySource.terms`（等）上，**不依赖 referer、也不依赖 GoatCounter**。⚠️ 代价照记：发版之前线上零数，且 `terms` 这个名字**爬虫也会带**（爬虫跟得动链接 ⇒ §3 那两族抓取器会进 `bySource.terms`，见 `GROWTH-SPEC.md` §4 第 8 条） |
| **C2** | **社交分享面**（可分享的卡片与链接） | 🟢 **已上线**（09-23 23:5x 发版；批次 F-3，v0.2.116；★ 零迁移零新依赖零运行时行为改动；★ 验收新增一条硬判据：14 张卡逐一 `curl -sI` 全为 `200 image/png` 且字节与仓内逐张相同）：14 页（首页＋目录页＋12 词条页）各有**指向自己那张**的 1200×630 卡，`twitter:card` 升到 `summary_large_image`；PNG 是**提交资源**（重新生成＝`npm run og:shots`，不是构建步骤）。⚪ **站内分享 URL 仍未做**（老板 09-23 拍板推迟——它要动 `term_library` 的公开列＝多租户边界） | 每个词条页/学习成果给**唯一可分享 URL** ＋ 完整 `og:*` ＋ 一张不依赖 JS 的卡；★ 公开页这半边已齐，**产品内（登录后）的成果卡还没有** | 把卡发到即刻／小红书／V2EX／微信群 | 分享出去的 URL 在社交平台的预览卡渲染出正确标题与图（★ 发版后 `curl -sI https://11wand.com/og/<slug>.png` 须回 **200 ＋ `content-type: image/png`**——本站有「错地址也返 200 壳」的前科，只看状态码不算过） |
| **C3** | **开源仓库位**（GitHub） | 🟢 已在做（README 是第二门面，GitHub 来源实测 6 次点击）；**批次 G-1 收口了那条入口行、09-23 23:5x 已随发版进线上**（★ 文案随同伴 22:54 的代提交 `eeb968e` 已在 main，本批对它只剩数字账）：门面条由「免安装，邮箱注册即用」改为「免安装，进首页点『免注册，直接体验』」，其下补一段零凭证通道说明，★★ **并把应用里那句既有警示一起搬了过去**——「公用池**全站共享**、访客彼此可见 ⇒ 别放个人信息」（与 `landing-copy.ts` 的 `DEMO.warn` 同一句；只搬入口不搬警告＝我在制造隐私事故） | ① 体验入口行 ✅ 已做（G-1）；② badge 与 `metrics --tests` 同源这条由 `metrics --check` 常驻守着，本批顺手把三本产物账对齐到 204／2817；③ **尚未做**：GitHub Topics 与 `awesome-*` 清单 PR（老板动作）、以及 README 那条英文访客进中文应用壳的手感（MT-13 判据④仍挂着） | 提 `awesome-*` 清单 PR、开 GitHub Topics | 来源分析里 `github.com/llwand1/...` 的点击数变化（Caddy 日志／GoatCounter referrer） |
| **C4** | **社会证明计数**（原批次 B） | 🟢 **后端已上线**（09-23 23:5x 发版，★ **预演实测迁移水位 40→41**；批次 F-2，v0.2.114；★ **有迁移 v41**，非「零迁移」）：契约新立 `docs/GROWTH-SPEC.md` v0.1.0，代码＝`server/src/growth/counters.ts` ＋ 只读出口 `GET /api/growth/counters`（★ 老板拍板**公开 GET＋IP 限流 20/时**，不挂 `requireAuth` ⇒ `AUTH-SPEC` §3 白名单同批加一行）。★★ **09-24 批次 I（归因层）给它加了「谁带来的」那一维**：契约升 `GROWTH-SPEC.md` **v0.2.0**（新 §2.5），迁移 **v42**＝一条 `ALTER TABLE growth_action_day ADD COLUMN source TEXT NOT NULL DEFAULT ''`（★ **空串不是 NULL**：空＝「没采集到」这个事实，`direct` 只在**读侧**折——写进库就永远分不出「真直接访问」与「不知道」），端点多返一个 `bySource` ⇒ **本表从此能直接读「哪个渠道带来的」**。★ 同批一条结构性修复：`app_open` 的采集请求原本裸 `fetch`、绕开唯一出口层 ⇒ 归因头天生带不上，现改走 `api.auth.surface()`（★ 三个计数里最关键的那个原本永远是瞎的）；零新依赖、**`.env` 零改动**、拟 **v0.2.123、⚠️ 未发版** | ★★ **计数只写在 HTTP 入口层**（三个采集点全在 `routes/`）⇒ ① 体验号的八条种子走 `saveOneTerm`、一个请求都不过 ⇒ **结构上进不了这份账**，不靠「记得排除」；② 探针在写入口按批次 E 的两个标记（`X-SB-Probe`／`studentbuddy-probe/*` UA）一处判掉 ⇒ `_probe/prod-pulse` 每几分钟一次的 demo-login 不再灌爆数；③ 三个动作都在**成功之后**才记（429／409／400 不算）；★ 单位是「**IP·天**」＝同一来源 IP 同一自然日只算一次，**只许叫「次」不许叫「人」**（§0 第 2 条），库里**不落裸 IP**（`sha256(盐+IP)` 前 16 位，盐由迁移 v41 用 `randomblob` 生成、不读环境变量）；⚠️ **刻意不含「词条写入」**（那是 owner 维度、与 IP 桶不同单位）；★ 老板拍板**暂不显示**：真实数为 0 时页面什么都不画，端点也就返 0；⚠️ **09-24 新登记一条已证实的口径隐患**：写入口只认我们自己的两个探针标记，**没有任何一层按 UA 族剔第三方抓取器**，而 `app_open` 挂在 SPA 首屏那条路上 ⇒ **会执行 JS 的爬虫一样算一次**（09-24 现读 `growth_action_day` 里那几个 `app_open` 桶就是这么来的，不是真人）。三个修法与「在此之前 `app_open` 一律带『含抓取器』前缀」这条临时纪律都在 §3 最后那行 | **挂在哪**（落地页首屏？）——★ 且必须先满足 `GROWTH-SPEC.md` §4 第 1 条（`SB_TRUST_PROXY` 没配则全站塌成每天一次，看了也是假的） | 后端侧已由 23 例锁死：`counts` == 直读库 `GROUP BY` 复算、同 IP 连打两次 `providers` 只涨 1、带探针标记的 demo-login **零涨**、表里查不到 IP 明文；★ **线上侧已验半条**（23:5x 发版后现读 `GET /api/growth/counters`＝三数全 0、`firstDay:null` ⇒ 带标记的探针动作**在生产上同样不计**，这条是剔除通道的生产级证据；反面：探针本是体验号主要访客，所以 `demo_enter` 大概率长期为 0＝**宁可少不可假**）｜剩下的判据＝真人从自己手机访问落地页后 `counts.app_open` 必须 **+1**（★ 这一条同时证明「trust proxy 生效」与「数的是真人」，已派 **MT-18**）｜★ **归因批新增三条本地已锁的判据**（`growth/counters.test.ts` 15→**27**、`routes/growth.test.ts` 8→**13**）：① **`Σ bySource[].counts[kind] === counts[kind]`**（契约 §5 判据 7，★ 复算不过＝归因这维在编数）；② 同 `ip·day` 换一个 ref ⇒ **来源保持第一次**（落库端先到先得，与同行的 `first_seen_at` 同义）；③ 探针标记＋ref 同带 ⇒ **仍一条不记**（★ 归因不是绕过剔除的通道）。⚠️ **MT 未做**：真人从带 `?ref=` 的链接点进落地页 → 刷新与站内第二跳仍认得出来源（`sb_ref` 活在 localStorage，jsdom 桩不到真实浏览器的那一半）⇒ 已派 **MT-30**；线上那一次 `bySource` 复算随发版做 |
| **C5** | **零门槛体验转化**（原批次 C：给共享体验号灌种子内容） | ⚠️ **码已上线、机制在线上失效**（09-23 23:5x 发版；批次 F-1，v0.2.113；★ 零迁移零新依赖，题材按老板拍板＝**与公开词条同源**）：★★ 发版后用体验号会话现读 `GET /api/terms` ⇒ 池内已有 **135 条**（09-22 93／09-23 42），而种子的触发条件是「**池子为空**」⇒ **种子永远不会灌**（同名仅「间隔重复」一条）。★ 目标侧不算坏：**首屏本来就不空**，但那不是本批的功劳；要不要「按名补齐」的版本，判定权在老板 | `u-demo-shared` 里灌**可看的一屏内容**——★ 实际做法：八个词条（提取练习／间隔重复／遗忘曲线／必要难度／精细加工／组块／元认知／费曼学习法，**名字与 12 页公开讲解页逐个对得上**）经**真实领域函数**写入，进度按 `REVIEW_INTERVALS_DAYS` 回填，⇒ 4 条今天到期、3 个领域、一条从未复习；★ 因走领域函数 ⇒ **事件零发、每日活动零行**，C4 从结构上数不到它 | 提供内容题材（已拍板）；决定「访客彼此可见」的提示要不要加强；⚠️ **线上体验池若已有访客存的词条，种子按锁不灌**——发版前须逐条现查 | 访客点「免注册直接体验」后**首屏不是空态**（真人截图为准，§0.16）⇒ **MT-17 待老板真机跑** |
| **C6** | **中文内容社区**（知乎／少数派／即刻／小红书） | 🟡 **素材与口径已就位（批次 H-2，`GROWTH-COMMUNITY-PACK.md` v0.1.1），等第一条真贴文**（★ v0.1.1 是一次**订正不是加料**：那包 §7 第 6 条原先把 C6 记成 ⚪、并把「§6 表出现第一行」当成 🟡 的门槛，与本格（🟡／🟢）互相打脸 ⇒ 现以本格为准定死三档：⚪＝没素材／🟡＝素材与口径齐／🟢＝§6 出现一个**真日期**＋referer 观察跑通） | 需要的是**素材**：可嵌入的讲解页（C1 已给）、真截图（C5 后才有）、一段 30 秒内说清「词条是主体」的文案 ⇒ ★ 三件里**两件齐**（12 页讲解页＋§1 四条主张逐条有出处），**真截图那件不仅缺、仓里那六张还当场被看出是旧的**（本包 §4：`landing-hero.png` 拍于 09-21 14:47，★ 图上统计条写着 **149 个 REST 接口**而现读 `landing-copy.ts` 是 **150**、hero 副句也不是今天那句 ⇒ **同一处位置两个数**，这不是「可能过期」是看着就不一致） | **本人发帖**（AI 不代注册、不代发） | 帖子发出后来源分布里出现对应域名（Caddy 日志）；★ 转 🟢 的判据就是素材包 §6 那张登记表出现第一行 |
| **C7** | **导航站与自托管清单**（selfh.st／AlternativeTo／Product Hunt／国内自托管导航） | 🟡 **资产包已成文，提交动作待老板**（`docs/GROWTH-SUBMISSION-PACK.md` v0.1.0，批次 G-2／2026-09-24） | ✅ 已交付：一句话简介（中／英各三候选，全部 ≤140 字／≤55 字符）、三档长简介、标签池、截图清单（现成 6 张＋缺口 4 张）、★ 隐私口径十条**每条带复验命令**（无追踪 cookie／计数不落裸 IP＝`sha256(盐+IP)` 前 16 位／数据＝一个 SQLite 单文件／key 加密不出接口／会话令牌只存哈希），★ 加**三条「不许说」**与**一条必须主动说**（内容要发给模型服务商）——⚠️ 三条硬缺口按实登记：无隐私政策页、无自助注销、Docker 路径从未实机跑通。⬜ 仍缺的**素材**：词条有内容那一屏＋一张手机竖屏（对外主打「词条是主体」却没有它的图＝货不对板，见 §6.2 缺口 1／2） | **本人提交**（★ 本包 §7 已明写「字段名以你打开的提交页为准，不要按本卡猜」——平台表单规格本会话抓不到，未实测） | 提交列表成文（★ 登记表就在本包 §8，五行：日期／平台／用了哪节资产／条目链接／**来源分布里有没有出现该域名**；最后一列取数须先按 §3 剔探针） |
| **C8** | **更新可见性**（公开 changelog／RSS） | 🟢 **已上线**（09-24 批次 G-3＝**v0.2.118，08:48 发版**；★ 本表第一条「会天天变的公开页」） | ★ **做法与建表时那条不同、且是刻意的**：不渲 `CHANGELOG.md`，改为**手写的清洗表** `packages/web/src/seo/changelog-public.ts`（对外口径唯一事实源，首版 9 条＝只收已上线 11wand.com 的 v0.2.109～v0.2.117；★ **09-24 发版当批补了第 10 条＝v0.2.118 自己** ⇒ 此刻**仓内 10 条／线上 9 条**，下次发版后线上才读到 10），由 `changelog-page.ts` 渲成 `/changelog/index.html` ＋ `/atom.xml`（同一份数据渲两遍 ⇒ 页与订阅不可能漂）；入口两处＝落地页页脚链（两语侧，英文侧如实写 `Changelog (Chinese only)`）＋ `<link rel="alternate">` 自动发现；★ **刻意不进 `sitemap.xml`**（会变的页＋没有自己的分享卡） | 转发大版本更新；★ 顺带把「公开字节里写了什么」变成机器判据（`public-hygiene.ts`＋`.test.ts`，本表 C8 与 `SEO-SPEC` §5 第 12 条同族红线一把守） | ✅ **仓内侧已成立**：页面与订阅可 curl（构建产物 7.2 KB／5.2 KB、零 `<script>`、390 与 1280 两档视口零越界元素，真截图已看）、公开字节红线锁扫 `dist` 的 20 个 html/xml/txt/svg 零命中；⚠️ ~~**线上侧判据为空**（未发版）~~（★★ **09-24 08:48 已发版，四条线上判据当轮全中**：`/changelog/index.html` **7204 B**＋`<script>` **0**＋`rel="alternate"` **1**；`/atom.xml` `<entry>` **9**；`robots.txt` 两扇新 `Allow` 各 1 行；★ 反向两条：`sitemap` 仍 **14** 个 `<loc>`、更新页内部字样族扫描 **0 命中**。★ 一条读数真相：`/atom.xml` 线上 `Content-Type` 是 Caddy 给的 `text/xml`，不是我们声明的 `application/atom+xml`。实录见 `SEO-SPEC` §6.3）；⚠️ **代价一条**：清洗表要人记得加行，机器拦不住 ⇒ 见 §4 v0.1.9 与 MT-23（★ **09-24 回标：这条纪律本轮执行了一次**——发版当批加了 v0.2.118 那一行，所以「仓内比线上多一条」现在是**正常状态**而不是异常；判据＝下次发版后 `grep -c '<entry>'` 应为 **10**，仍是 9 或更小就是忘了加） |
| **C9** | **搜索引擎工具面** | 🟡 **通路已开**（批次 E：验证文件的放法与 `robots` 的坑已写进 SEO-SPEC §5 第 8 条）。★★ **09-24 老板口述「站长平台刚才搞了一些了」＋ 本会话现读工作树与线上：他做的是 Bing**——`packages/web/public/BingSiteAuth.xml`（85 B，12:00 出现在工作树、**未跟踪**）是必应站长工具的站点验证文件，★ **它不归本会话**（由放它的那一方自己提交与登记，我不代提交＝R2）。★★ **本会话 12:1x 的线上取证把这条通路直接读穿了，三条实测**：① **文件已经在线上**——`curl -sI https://11wand.com/BingSiteAuth.xml` ⇒ **200 ＋ 85 字节 ＋ `text/xml`**（★ 与仓内那份字节数一致；而 `deploy.sh` 的 dirty-tree 闸在 12:0x 一定会拦 ⇒ 上线的那一份**不是走发版链路过来的**，谁放的、怎么放的本会话未查，⚠️ 也就是说**仓内那份与线上那份同源但没进 git**，下次从干净检出构建会丢）；② **`bingbot` 全历史其实来过 11 次**（此前本表只登记过 Googlebot 与"百度 0 次"，**必应从来没被单独查过**——因为它不在我 grep 的词表里），最近一轮 09-24 11:54 **抓 `/BingSiteAuth.xml` 拿到 404**、**10 分钟后的 12:04 同一 URL 拿到 200 ＋ 85 字节** ⇒ **老板那边"补文件→必应重试→通过"这一整圈在线上留了痕**，验证大概率此刻已成立（★ 但"平台侧显示验证通过"这句话只有老板本人看得见，本表不替他宣布）；③ ★★ **而线上 `robots.txt` 到现在都没有为它开 `Allow`（现读 grep ＝ 0）——`bingbot` 照样拿到了 200** ⇒ **§5 第 8 条那句「robots 不放行，验证文件必失败」对必应不成立**（那条坑是按 Google／百度写的形状推的，本会话把它当既成事实复述过一次，现按实测改口）。★ 一条**顺带被推翻得更彻底的**同族判据见 §5 第 9 条回标：**"错地址一律返 200 壳"是错的**——`/NoSuchFile917.xml` 返 **真 404（0 字节）**，只有 `/terms/*.html` 那一族才落进壳 | 站长平台账号动作（上传什么文件、填什么 meta 都由平台定，AI 不代注册）。★★ **09-23 点名为全表最便宜、又卡着别人的一条**：验证域名 ＋ **手动提交 `sitemap.xml`**——C1 那处静态入口补完也只是「走得到」，Google 跟不跟没有任何数据能证；而**提交 sitemap 是唯一确定生效的那条路**。★★ **09-24 现状**：必应侧的"验证域名"这一步线上已见成果（上面②），**剩下的仍然是那句「手动提交 `sitemap.xml`」**（Googlebot 到今天零抓 sitemap）；★ 另两件事归放文件那一方：**把 `BingSiteAuth.xml` 提交进仓**（否则线上那份是孤儿字节），以及要不要顺手补那行 `Allow`——⚠️ **本会话不擅自改 `robots.txt`**：两边同改一行会撞车。★ 另记一条**仓内事实**（判这把锁的值，不是判这个文件的）：`public-hygiene.test.ts` 扫 `public/` 用的是**写死的两份清单**（`robots.txt`＋`favicon.svg`）⇒ **往 `public/` 里新丢文件不会过那把「公开字节」锁、也不会红**，这次落在 `public/` 的验证文件正是一例（它同时也没进 `Allow`）。要么把那份清单改成读目录，要么定一条「进 `public/` 必同批登记」，二选一；★ 详见 `SEO-SPEC` §5 第 13 条 ③。｜★ **新挂一条给老板的必动作（把握度中，机制我熟但本站从未实测）**：验证通了之后可考虑 **IndexNow**（必应／Yandex 的即时推送协议，后台给一个 key、把 URL 一次性提交上去）——它是本表 C1「百度／Google 得靠人主动提交」的**必应对照版**，门槛比百度站长平台低；★ 而"发版时自动推送新 URL"＝运行时代码，要先立契约再说，此刻只登记为老板后台动作 | 验证文件线上可 curl 到（200 且内容逐字等于平台给的那一份）且平台侧显示验证通过。★ **Bing 这一格此刻的读数**：前半条**已成立**（线上 200 ＋ 85 B），后半条**在老板眼睛里**（本表不代宣）。⚠️ 从此「错地址返壳」不再当万能判据用：**根级 `.xml` 缺失是真 404**（本会话实测），所以"文件没放上去"这种错**能被状态码抓到**，而 `/terms/` 下写错 slug 那种仍然不能（§5 第 9 条回标）；★ 而「必应真的来过」这句话现在有了日志凭据：`grep -ahi bingbot /var/log/caddy/access.log* \| wc -l` ⇒ **11** |
| **C10** | **度量卫生**（原任务 #4） | 🟡 **打标已落码**（批次 E）＋ §3 排除清单已成文；⚠️ 读取侧过滤归 C4 | `_probe/probe-marker.mjs` 一份标记三处共用：`User-Agent: studentbuddy-probe/1.0 (<脚本名>)` 进 Caddy 日志、`X-SB-Probe: <脚本名>` 进请求头（`prod-pulse`／`remote-signup`／`owner-isolation`）；★ 但整个 `_probe/` 在 `.gitignore` 第 23 行＝**仓外本机工具**，不随 commit 也不随发版上线，所以「新探针必须带标记」这条**没有锁守**，只有本表与 CHANGELOG 那行记着；★ 打标不等于不污染——写进真库的行还在，剔除发生在读的一侧（C4 计数必须按 §3 剔，这一条是 C4 的硬前提） | 决定 UptimeRobot 留不留 | 日志里一条 grep 能把探针流量与真实访客分开（`grep -v studentbuddy-probe`）；C4 的数按 §3 复算一次能对上 |

## 2. 执行顺序（老板 09:1x：「目前这些渠道都要做下去」）

★ 顺序按**依赖**排，不按编号：C1 收尾（目录页 URL／入口）与 C10（度量卫生）最便宜且是别人的前提 ⇒ 先做；C5（种子内容）是 C2/C6 的素材前提；C4 的口径已被今天的取证钉住，可与 C5 同批；C8 有**必须先清洗**的安全代价；C6/C7 的产品侧只有「素材包」，主体是老板动作。

| 批次 | 覆盖渠道 | 内容 |
|------|----------|------|
| **批次 E** | C1 收尾 ＋ C10 ＋ C9 | ✅ **已上线**（09-23 09:54 发版，七条判据全绿见 `SEO-SPEC.md` §6.1）：目录页 URL 修正（`/terms/index.html`）＋ 两处入口 ＋ 探针打标与 §3 排除清单（★ 打标在 `_probe/`，仓外）＋ 站长验证文件的通路 |
| **批次 F** | C5 ＋ C4 ＋ C2 | 🟢 **三小批全部落码、全部已上线**（09-23 23:5x 与同伴的 v0.2.115、本会话的 v0.2.117 一次发版）：**F-1＝C5 体验号种子 ✅**（v0.2.113，★ 零迁移零新依赖）／**F-2＝C4 诚实计数后端 ✅**（v0.2.114：三动作只在 HTTP 入口层记、IP·天去重、探针在写入口剔除、只读出口公开＋限流；★ 老板拍板**暂不显示**；★ **有迁移 v41**）／**F-3＝C2 公开页 `og:image` ✅**（v0.2.116：14 页各一张自指卡 ＋ `summary_large_image`；★ 老板另两轮拍板＝**版式 A 纸面卡**、**底部两行保留**；站内分享 URL 推迟到要动租户边界时再说）。⚠️ **三批叠在一起发版时注意**：只有 F-2 带迁移（v41），F-1／F-3 是纯代码＋静态资源 |
| **批次 G-1** | C3 ＋ C1 断口补口 | ✅ **已落码并已上线**（v0.2.117，09-23 23:5x 发版；★ 零迁移、零新依赖、**零可见 UI 改动**——最后这半句机器判不了，仍待真人＝MT-21）：README 体验入口收口 ＋ 首页**静态可抓**入口（今天读线上数据逮到的断口）＋ 销本会话 F-3 的产物账三本。⚠️ 与另一会话的「批次 G＝AI 出题工具化」（v0.2.115）**不是一件事**，引用必带版本号 |
| **批次 G-2** | C7 | 🟡 **产品侧文件已交付**（`docs/GROWTH-SUBMISSION-PACK.md` v0.1.0，★ 零代码／零迁移／零新依赖／**页面什么都不画**）：文案三档·标签·隐私口径·截图清单·逐平台卡·提交登记表。⬜ 剩下的全在老板那侧：补两张缺图（§6.2 缺口 1／2）→ 挑一个平台提交 → 回 §8 登记。⚠️ 本批**不发版也不需要发版**（纯文档），★ 真人单 MT-22 验的是「拿这份包去提交，哪一格还要自己现编」 |
| **批次 G-3** | C8 | 🟢 **已落码并已上线**（v0.2.118，09-24 08:48 发版；★ 零迁移、零新依赖、`.env` 零改动）：**手写清洗表** → 公开更新页 ＋ `atom.xml` ＋ 一层共用的页面壳（`page-shell.ts`，从 `term-page.ts` 拆出）＋ ★「公开字节不许带内部字样」那把锁（红线来源就是建表时写的那句：不许复用 `CHANGELOG.md` 原文）。★ 落盘 14 件 → **16 件**、`robots.txt` 新开两扇 `Allow`、基线 204／2817 → **206／2839**；⚠️ 剩下的动作＝老板点名发版 ＋ MT-23（★ **09-24 09:2x 回标：发版已行使**——老板在同一轮提问里点名「推送＋现在就发版（推荐）」，08:48 八步 `exit 0`、4 条提交推齐、ahead 归零，线上十条判据实录见 `SEO-SPEC` §6.3；**MT-23 仍无人跑**） |
| **批次 H-1** | C1 英文侧 | 🟡 **已落码，⚠️ 未发版**（拟 **v0.2.119**；★ 零迁移、零新依赖、零运行时改动——全部产物在构建期落盘）：**6 条英文词条页**（`/terms/en/<ascii-slug>.html`）＋ **英文目录页** ＋ **双向 `hreflang`**（配对事实源只有一份＝英文侧的 `zhSlug`）＋ **7 张自指分享卡**（卡数 14 → 21、`sitemap` 的 `<loc>` 14 → 21、落盘件数 16 → 23）。★ 三条口径按老板 09-24 AskUserQuestion 拍板：① 内容＝**6 个概念**（间隔重复／提取练习／遗忘曲线／交错练习／认知负荷／费曼技巧），不为凑 12 条硬翻；② 地址＝`/terms/en/<slug>.html` **同批带双向 hreflang**（不做"先出页面、以后再补语言声明"那种半件）；③ 卡片＝**同批生成，一张不缺**。⚠️ 刻意未做：人看得见的英文入口（落地页页脚那条属视觉改动，判定权在老板）＋ JSON-LD（仍欠，见 C1 ④）。★ 台账：`SEO-SPEC` v0.1.9（§5 第 14 条新立那簇 hreflang 代价、§6 第 11 条新判据、§5 第 9 条按实测收窄），`test-plan` 表头与 §3（新文件 `src/seo/term-en.test.ts` 22 例成行），README/metrics 三本产物账，真人单 **MT-24** |
| **批次 H-2** | C6 | 🟢 **已落码**（09-24 13:2x，本表唯一一条**纯文档、零代码、不占版本号、也不需要发版**的批次）：新文件 `docs/GROWTH-COMMUNITY-PACK.md` v0.1.0（§0~§8）。★ 三件素材的交付实况：**讲解页**＝12 条中文页现读成表（含每条钩子取自哪一句）；**30 秒主张**＝四条，逐条标出处（`landing-copy.ts` 的 `HERO`／`INTRO`、词条页 `pitfalls[0]`、C7 包 §2），**没有一句是新造事实**；**真截图**＝⚠️ **这一件没交付，而且本批查出一件比"缺"更糟的事**——仓里那六张图不是缺，是**旧且会被当成证据**（`landing-hero.png` 图上写 149 个 REST 接口，现读代码是 150；它拍于 09-21 14:47，而 09-22 那次整页重排把「词条是主体」搬上前排＝**C6 要讲的那句话在图上根本不存在**）。⚠️ **剩下的动作全在老板侧**：① 挑一条主张发第一条贴文并回 §6 那张表登记（★ 转 🟢 的判据就是那张表出现第一行）；② 决定要不要先补拍那四张缺图（口径在 C7 包 §6.2 ＋ 本包 §4 那三条硬约束）。⚠️ **本包自己的四条未做**照抄在它 §7（四平台规则零实测／主张没做真人口播计时／12 行里 11 行「动作」列留「同上」／六张图未逐张重看） |
| **档位 1（09-24 老板排在 H-1 之后第一位）** | C1 中文侧 | 🟢 **已落码**（09-24 14:2x，`a761e94`，拟 v0.2.121、⚠️ **未发版** ⇒ 线上那 12 页仍是旧主词）：12 页**主词按两轮搜索框自动补全改口**（提取练习→主动回忆／交错练习→交错学习／遗忘曲线补「艾宾浩斯＋复习计划表」／间隔重复首屏摆 1/2/4/7/15/30/60／费曼补「四步＋例子」；★ `认知负荷` 保持书面不动）——零新页面、只动措辞与首屏，红线不变。★ 同批新立第四把锁（中文正文纯净：`*` 与英文碎词上屏即红，它当场逮到**已上线三处**的 markdown 星号）＋三张卡重截。⚠️ **本行是 09-24 16:3x 由档位 3 那一批补登记的**（档位 1 收工时只回写了 C1 那一行与 test-plan §3，漏了这一行）；⚠️ **同批带出一个缺陷、也在档位 3 那一批修掉了**：`term-corpus.test.ts` 有一处 tsc 错误（`noUncheckedIndexedAccess`），当时只跑了 `npm run test`＋gates＋metrics、**没跑 `npm run lint`** ⇒ 它随 `a761e94` 推上了 `origin/main`，而 `npm run build` 会被它拦下（详见 `SEO-SPEC` §5 第 15 条②）。★ **剩下的动作全在老板侧**：发版（逐次点名）＋ MT-27 那一单（判「改后的主词是不是你想搜的词」，本表这一行能不能转 🟢 的一半押在它上面） |
| **档位 3（09-24 老板拍为最推荐的一档）** | C1 ＋ §0 第 1 条「做进产品」 | 🟢 **已落码**（09-24 16:3x，拟 **v0.2.122**、⚠️ **未发版** ⇒ 线上还没有这一页）：**免登录、纯前端、不落库的「艾宾浩斯复习计划表在线生成器」＝`https://11wand.com/ebbinghaus-plan.html`**（★ 根级地址，刻意不叫 `/tools/…`——那个前缀被「公开字节不许带内部字样」那把锁算内部字样，先于任何测试红）。形态两处按老板 09-24 点名：**独立静态页＋内联脚本**／**起始日＋每日新量 → 逐日任务表**。★ 两条前提纪律都守住了：**没写 `term_library` 公开列**（那扇门仍是推迟状态）、**`robots.txt` 单开了 `Allow: /ebbinghaus-plan.html`**（本站默认全封，不补就是静默不公开）。★ 这一档的**真价值不在多一页，而在「产品的那个队列算法第一次对外可核」**：页内那段算法被 `node:vm` 抓出来与仓内 `buildPlan()` 在 70 天上逐行对账，漂一个节点即红（⇒ 想抄错都抄不动），而「不落库」是 12 个禁词的字节判据、不是形容词。⚠️ **代价已写进 `SEO-SPEC` §5 第 15 条（该条四条，本格列前三条；第四条＝英文侧缺失，见本格末那句）**：同一张表两份实现（靠锁不靠嘴）／**`npm run check` 不含构建**（本批就踩在这条上：四连全绿而 `npm run build` EXIT=1，修法判定权在老板）／**这一页零数据回流**（不落库＝隐私卖点，但 C1 想要的「有没有人用」这个信号结构上拿不到，★ 不走"把生成次数写进文案"那条路，那是使用量数字）。★ **剩下的动作**：发版（逐次点名，发完跑 `SEO-SPEC` §6 第 12 条五判据）＋ **MT-28**（那三个格子懂不懂、那张表敢不敢照着做、打印与剪贴板形状）＋ 若真要吃下工具意图流量，还有一条没做的：**英文侧要不要也出一版**（此刻只有中文，英文访客搜 `ebbinghaus schedule` 落地不到东西）——记在 §5 那条待办里，不擅自动手 |
| **批次 I** | **归因层**（C4 ＋ C1 ＋ C10 的前提，★ 不是新渠道，是让所有渠道第一次可测） | 🟢 **已落码，⚠️ 未发版**（09-24 22:5x，拟 **v0.2.123**；★ 迁移 **v42**＝一条 `ADD COLUMN`、零新依赖、`.env` 零改动）：`?ref=` → `localStorage.sb_ref`（末触）→ 唯一出口层带 `X-SB-Ref` → 服务端 `normalizeGrowthSource` → `growth_action_day.source` → 公开端点 `bySource`；22 张静态页回链全部自带 ref。★★ **本批两条值得从代码里抬出来记进台账**：① **「埋了采集点」不等于「带得上来源」**——`app_open` 那句原本绕开出口层裸 `fetch`，三个计数里最关键的一个**天生是瞎的**，而所有行为测试当时都绿；因此立了**本仓第一条源码锁**（★ 反向证据：退回裸 fetch 时行为测试一条都不会红）。② ★ **捕获口径与落库口径可以不是同一件事**——末触只管「这台设备下次报谁」，同一 `ip·day` 那一行的来源仍**先到先得**（与 `first_seen_at` 同义）；写成两边同一个口径，要么新渠道被永久遮掉、要么被转发两次的人算成"来自最后那个渠道"。⬜ **剩下**：发版（逐次点名）→ 线上 `bySource` 复算一次（§5 判据 7）＋ **MT-30**（真人第二跳与刷新后仍认得出来源）；⚠️ `direct` 含第三方抓取器这条**本批刻意未修**，见 §3 最后那行 |

⚠️ 每批收口都要过 §0 三条口径与闸门（四连／test-plan 登记／§0.16 真人测试单）；**发版仍归逐次点名授权**，本表不预授权。

## 3. 排除清单（哪些量是「我们自己造出来的」）

★ 任何对外挂出的数都必须先按本表剔一遍。本表是 **C4 的输入**，也是读 Caddy 日志时的 grep 依据。

| 来源 | 落在哪里 | 怎么剔 |
|------|----------|--------|
| `_probe/prod-pulse.mjs`（线上长稳脉冲，demo-login 进 `u-demo-shared`） | 真库：`sessions`／`messages`／`daily_activity`／`user_stats`／`platform_usage`／`auth_sessions`；钱：Agnes 真实计费 | 按 `owner_id = 'u-demo-shared'` 剔（★ 会话它自己会删，但活动与 token 行不删）；日志侧按 UA `studentbuddy-probe/1.0 (prod-pulse)` |
| `_probe/remote-signup.mjs` ＋ `owner-isolation.mjs` 的远端模式（真注册两个测试号） | 真库：`users`／`auth_sessions` 及其名下全部行；`docs/terms` 里 `SECRET-<stamp>` 命名的词条 | 按那两个测试号 `owner_id` 剔；词条按前缀剔 |
| 共享体验号 `u-demo-shared` 的**真实访客**（免注册直接体验） | 同上，与探针混在一个 owner 下 | ★ 剔不开——所以「体验号内容数」不能当「独立用户数」报；要报人，必须换批次 C4 的产品内动作计数 |
| UptimeRobot（`*/5 * * * *` 之外的第三方拨测，实测 952 次/5 天） | 只进 Caddy 日志与 `/api/health`（auth 免检、不写库） | 日志侧按 UA/`/api/health` 剔；留不留由老板定 |
| 扫描器（伪装成 `Baidu; P1 5.1.1) NABar/1.0` 打 `/junhuashen.php` 那一类） | 只进 Caddy 日志 | 按 UA 与非常规路径剔；★ 它不是百度，别拿它证明「百度来过了」 |
| `ntp.msn.cn` 预取、垃圾 referer（`ratelzr`/`afterawy`/`fenceud4`/`gatehk1`） | 只进 Caddy 日志 | 按 referer 域剔 |
| ★ **伪装 iPhone Safari 的 41-IP 农场**（UA 一律 `Mozilla/5.0 (iPhone; CPU iPhone OS 13_2_3 like Mac OS X) ... Version/13.0.3 Mobile/15E148 Safari/604.1`，IP 全在腾讯云系段 43.x／49.51.x／124.156.x／129.226.x／170.106.x） | 只进 Caddy 日志（**2026-09-24 01:58–03:58 一轮**：`/` ×70 → `/terms/index.html` → **12 个中文词条页逐个各一次** → `/changelog/index.html` → `/atom.xml`） | 按 UA 族＋IP 段剔。★★ **两条别把它读反**：① 它**不是真人**，一次都不算「有人来看」；② 它是本表 C1 那条「链接图对爬虫走得通」的**唯一正向证据**（逐页各一次＝它顺着目录页往下走，而不是撞运气），但**身份未定**——不是 Baiduspider（那个仍然全历史 0 次）、也不是 Googlebot，⇒ **不许拿它证明"被收录了"**。判据：`grep -a 'iPhone OS 13_2_3' /var/log/caddy/access.log \| grep -o 'uri":"[^"]*' \| sort \| uniq -c` |
| ★ **`Go-http-client/1.1` 一族**（五 IP：106.75.49.194／60.236／64.235／66.75／67.185 ＋ 118.194.250.22 ＋ 45.194.70.251） | 只进 Caddy 日志（**2026-09-23 19:56–20:01 一轮，全 200**：`/robots.txt` → `/sitemap.xml` → `/assets/index-*.js` → `/stats/count.js` → 两个 favicon） | 按 UA 剔。★ 记一条它带来的**新事实**：**它是本表登记过的第一个「读了 robots、跟了 `Sitemap:` 声明、并且会执行 JS」的抓取器**（同批 Googlebot 到今天仍零抓 `sitemap.xml`）——所以"我们那份 sitemap 真有人吃"从此不是推测。⚠️ **而"会执行 JS"这一条正是下面 C4 隐患的入口**：它跑的是 SPA，也就可能触发 SPA 里那些带埋点的请求。 |
| ★ **上面两族共同的后果：C4 的 `app_open` 里混着爬虫**（本表 C4 那格记一条隐患，不是记一笔流量） | 真库 `growth_action_day`（`action=app_open`）——★ 计数在**写入口**只认我们自己的两个探针标记（`X-SB-Probe`／`studentbuddy-probe/*` UA），**没有任何一条按 UA 族或按 IP 段剔第三方抓取器**；而 `app_open` 挂在 SPA 加载路径上 ⇒ 会执行 JS 的抓取器一样算一次（同一 IP·天去重仍生效，所以它涨得慢、但涨的是假的） | ⚠️ **修法待老板裁，本表只登记不代改**：① 写入口再叠一层 UA 族黑名单（现成两族的形状都在本表里，规则简单但**永远列不全**）；② 或把 `app_open` 从「SPA 首屏」挪到一个**必须有真人动作才会发**的请求上（更准，代价＝口径要重新定义并回 `GROWTH-SPEC.md`）；③ 或维持现状但**永久禁止对外引用 `app_open` 绝对值**，只引用「剔掉两族之后 Caddy 日志与库内计数的差额」这种可复核形式。★ 无论选哪条，**在此之前 `counts.app_open` 的读数一律带"含第三方抓取器"这个前缀**；★ 而 MT-18 那条「真人从自己手机访问 ⇒ +1」的判据不受影响（它验的是通道，不是量级）。★★ **09-24 批次 I（归因层）之后本行的状态**：老板拍板**本批三条修法一条都不选**（采集点不换、UA 黑名单不加），先拿 `bySource` 取证 ⇒ 本行那个"临时纪律"**从今天起是永久纪律还是可解除的，取决于 `bySource` 读到数之后**；★ 一处新增的用处：带 `?ref=` 的链接被爬虫走进去时，那一跳会**带着渠道名**落在库里（不再和真人混在同一个 `direct` 里），所以「这一格涨得离谱」从此第一件可查的事就是**回来对 §3 这两族**（代价与判据见 `GROWTH-SPEC.md` §4 第 8 条／§5 判据 8）。 |
| AI 驱动的浏览器探针（CDP／browser-use 真 Chrome） | GoatCounter 的访问数（UA 是**真的**，剔不掉） | ★ 唯一办法是**约定**：AI 浏览器探针不打 `11wand.com`，只在本地栈跑；线上验证一律用 curl（带探针 UA）或老板真人点一次 |
| ★★ **本机渲染那份带埋点的壳**（09-24 13:2x 现读 `packages/web/index.html` 查出来的，**给上一行那条「唯一办法」打折**） | 同样是 GoatCounter 的访问数——★ 那枚 beacon 写的是**绝对地址** `https://11wand.com/stats/count`（脚本 `//11wand.com/stats/count.js`），**在 localhost 打开也会把这一针打进线上统计**，而它带正常浏览器 UA、事后**没有标记可剔** | ⚠️ 「只在本地栈跑」这条约定**不足以挡住污染**：本机渲染必须先在前端拦掉 `/stats/` 那一条请求，或者干脆老板真机上拍。★ 本条按「**会记**」处理——要证实它得看统计看板，而看板凭证本会话没有、也不去猜（缺凭证就问是既定纪律）；★ 这条顺带解释了批次 G-3 那句「页脚新链只有 DOM 锁没有截图」不是偷懒。**未跑的判据**：本机 `npm run build` 后打开 `dist/index.html`，Network 里看有没有一条发往 `11wand.com/stats/count` 的请求 |
| ★ **自家手工取证的裸 `curl`**（★★ 09-25 21:1x 实测灌进去的一格，**已不可回收**） | 真库 `growth_action_day`（`action=app_open`）：`curl` 带 `Origin` 打 `GET /api/auth/providers` ⇒ 读端点看到 `counts.app_open` **27 → 28**（UA 是 `curl/8.x`，**没带 `X-SB-Probe`** ⇒ 写入口不认识我们自己是完全正当的）。★ 日志侧可复验：当日 `providers` 上 UA 含 `curl` 的落点只有两条——21:09:49（未打标＝那 +1 格）与 21:14:41（带了标 ⇒ 28 不动）；⚠️ 但 **Caddy 不记录 `X-SB-Probe`**，翻日志认不出哪条带过标，只能靠读数差。★ 判据：把 `/var/log/caddy/access.log` 与 `access-*.log.gz` 一起读（★ 必须带轮转档，09-25 20:13 轮转过一次），挑 `providers` 那条 uri ＋ UA 含 `curl` 数条数（⚠️ 服务器**没有 `jq`**，用仓库自带的 node 跑） | ★ **事后剔不掉**：库里不落裸 IP、桶是 `sha256(盐+IP)` 前 16 位 ⇒ 那一格是谁灌的认不出。⇒ 只剩**事前**这一条路：任何脚本化／手工的线上请求一律带 `-H "X-SB-Probe: <目的>"` 或用 `studentbuddy-probe/*` UA。★ 同一条请求带头后再打两次 ⇒ `app_open` 稳在 28 不动（两条剔除通道——头与 UA 前缀——**都拿到了生产级实测**）。⚠️ **给所有 AI 会话的一句硬规矩：取证前先给自己打标，否则你是在给产品造假的「人」**；细则在 `GROWTH-SPEC.md` §4 第 10 条／§5 第 9 条 |
| ★ **带 `?ref=` 也不一定有人，而且今天这一批压根不是「有人点了推广」**（★★ 09-25 21:2x~21:3x 逐条读穿当日 13 条带标请求） | **只进 Caddy 日志，一条都没进产品账**：13 条**全部**落在主站根路径 `/?ref=…`（host 只有 `11wand.com` 一个值），来自 **5 个外网 IP**（各 IP 当日总请求 **29／47／45／45／1** 条），`/api/*` 命中 **0 次**；其余路径是 `/terms/index.html`／`/terms/en/<slug>.html`／`/changelog/index.html`／`/atom.xml`／`/stats/count.js`。★ 其中 154.28.229.189 连 SPA 包 `/assets/index-BzDOurAA.js` 都抓走了却**零 `/api/*`** ⇒ **「下载了 JS」不等于「执行了 JS」** | ★★ **本行最贵的一条**：那三个 ref 名（`terms`／`terms-en`／`changelog`）**就是我们自己在 22 张静态页上挂的站内回链名**（现读 `packages/web/src/seo/paths.ts` 第 45／47／49 行的 `REF_TERMS`／`REF_TERMS_EN`／`REF_CHANGELOG`）⇒ **当日这 13 条「归因读数」没有一条来自外部投放**，全是抓取器顺着站内链接图自己走出来的。⇒ 硬规则：**外投链接必须用站内回链用不到的名字**（`tieba`／`zhihu`／`xiaohongshu` 这一类；★ 站内自己只吃 `terms`／`terms-en`／`changelog`／`plan` 四个），否则 `bySource` 那一列永远分不清「渠道」与「爬虫走链接图」。★ 五个 IP 的机器形状（逐条可复验）：① 158.69.117.45／158.69.55.82／158.69.55.148 —— UA **完全同一串** `Chrome/124.0.0.0`，且**各在同一秒内**（17:03:29~31、17:05:00）把三个 ref 各打一遍＝同一套脚本；② 154.28.229.189 —— 当日 29 条里 **UA 换了两族**（`Windows NT 10.0 … Chrome/131` 与 `X11; Linux … Chrome/130`），一台真机不会这样；③ 43.159.143.139 —— 单发一条、UA 与本表上一行那族「伪装 iPhone Safari 的 41-IP 农场」**完全同一串**（`iPhone OS 13_2_3 … Version/13.0.3`）⇒ 极可能同一族工具。⚠️ **同批更正我自己的一笔错账**：这批取证的第一版把 12:20 那三条读成「**IP 为空＝本机自测**」，真相是**脚本只读了 `X-Forwarded-For`**、而那三条是**直连 VPS 没有 XFF** ⇒ 补上回落 `remote_ip` 之后才看见它们来自 154.28.229.189——**「空 IP」从来不是「我们自己」**。★ 判据（写死可复验，★ 本批把两句合起来用）：逐 IP 问「它打过 `/api/` 吗」——**没打过一定不是人**；★ 而**打过了也未必是**：子域今天那 5 条 `/api/*` 全是 `404` 的 `/api/graphql` 探测（见下面那行）⇒ 更硬的一句是「**它有没有从 `/api/auth/providers` 或 `/api/auth/demo-login` 拿到过 200**」 |
| ★★★ **子域的流量根本不在 `access.log` 里**（★★ 09-25 21:3x 现查 `/var/log/caddy/`） | 主站日志与 `aituijian-access.log` 是**两个文件**：只吃 `access.log` ＋ `access-*.log.gz` 的取证脚本**看不见任何子域流量**。★ 当日实数＝主站 2042 条，子域**另有** 156 条／51 个独立 IP（16:57~21:30）⇒ 本表任何一句「外部导流＝0」在子域上**不成立**，它只是没被读 | ⚠️ 修法（★ 本表登记，取证脚本 `_probe/growth-ref.mjs` 的改法归下一次用到它的人）：全站取证一律吃 `/var/log/caddy/` 下**全部** `*access*.log*`，并**按 `request.host` 分组报数**；判据＝先 `ls /var/log/caddy/` 数一遍有几个 access 档。★ 顺带一条**没人登记过的代价**：新子域挂上 DNS 后**半小时内就进了扫描名单**（`l9scan` 两个 IP 各 31 条、`/.env`×5、`/console/`、`/.well-known/acme-challenge/`×5、`/api/graphql` 探测）⇒ **「开一个新子域做渠道」的第一份流量必然是扫描器，不是人**。★ 这是**同一天里第三次栽在取证脚本上**（① 只读未轮转档 ② 只读 XFF 不回落 `remote_ip` ③ 只读主站日志）⇒ 「读数之前先证明读全了」不是口号，是**每一步都要跑一次的现查** |

⚠️ GoatCounter 看不到 `/terms/*.html`（那批页零 `<script>`，没有 beacon 可发）⇒ 词条页流量只有一个来源：`grep -oE '"uri":"/terms[^"]*' /var/log/caddy/access.log`（判据与口径见 `SEO-SPEC.md` §5 第 6 条）。

## 4. 沿革

- **v0.1.14（2026-09-25 21:3x，真实性补两行，零代码，拟 v0.2.133）**：★★ 这一批不加渠道、不改产品，只把「**这个数凭什么可信**」补硬。① §3 新增两行，第一行是**我自己**：手工取证的裸 `curl` 会把 `app_open` 打涨（实测 27→28），而 §2.4 的隐私设计（不落裸 IP、桶是加盐哈希）决定了这一格**事后认不出、摘不掉** ⇒ 防线只能前移到「事前打标」，并且已经用同一条请求反向验过（带 `X-SB-Probe` 连打两次＝一字不动）。⚠️ 那 28 里今夜确实含着我家自己的一格，**不回滚**。② ★★ 第二行是**推广侧最贵的一条，它还把本表自己的一个前提推翻了一半**：当日 13 条带 `?ref=` 的请求逐条读穿，全部非真人、**一条都没进产品账**（五个外网 IP 零 `/api/*`），而那三个 ref 名**恰好就是我们在 22 张静态页上挂的站内回链名**（`packages/web/src/seo/paths.ts` 的 `REF_TERMS`／`REF_TERMS_EN`／`REF_CHANGELOG`）⇒ **今天没有任何一条可归因点击来自外部投放**，而 `?ref=` 这一维在**外投之前**先被我们自己的链接图占用了。③ ★ 由此得两条对**所有投放动作**的硬规则：**一、链接必须写成 `https://11wand.com/?ref=<渠道名>`，不带 `ref` 等于没投；二、那个名字必须是站内回链用不到的**（否则涨的是「爬虫走了我们的目录页」，不是「有人从贴吧点进来」）。★ 判据也升级了一次：本批先写的是「逐 IP 问它有没有打过 `/api/`」，落笔后端到端再查一遍，发现子域那 5 条 `/api/*` **全是 404 的 graphql 探测** ⇒ 更硬的一句是「**有没有从 `/api/auth/providers` 或 `demo-login` 拿到过 200**」（同一句话当天先写出来、再被自己的下一次现查收紧——这才是要的样子）。④ 与 `GROWTH-SPEC.md` v0.3.1 同批：那边落 §4 代价第 10 条与 §5 判据第 9 条，本表只留指路的行，不重抄细则。⑤ ★★★ 本批新增第三行，是**今天最值钱的一条**：**子域有独立的 Caddy 日志档**（`aituijian-access.log`），所以任何只读 `access.log*` 的取证脚本**结构上看不见子域**——当日主站 2042 条之外，子域另有 156 条／51 个 IP。⇒ ⚠️ **本表此前所有「外部导流＝0」的句子，口径其实都只是主站**；★ 同日第三次栽在取证脚本上（前两次＝漏轮转档、只读 XFF），⇒ 「读数之前先证明读全了」从今天起带一条**可执行的形状**：`ls /var/log/caddy/` 数 access 档 ＋ 按 `request.host` 分组报数。⑥ ★ 另一条新代价：**开新子域＝当天就多一块被扫的表面**（半小时进扫描名单：`l9scan`、`/.env`、`/console/`、acme-challenge 探测），这是渠道台账讲「挂哪」时从没记过的一面。
- **v0.1.13（2026-09-24 22:5x，批次 I＝归因层落码，拟 v0.2.123、⚠️ 未发版）**：★★ **本表从今天起对「这个渠道有没有效果」有一个不必猜的答案**——代价是这半天里最有用的一条**先证伪了自己**：`app_open` 的采集点原本绕开全站唯一的请求出口层，所以「三个计数都在记」这句话是真的、而「它们知道来源」这句从来不是。① **一条 `ADD COLUMN` 加一维**：`growth_action_day.source`（`NOT NULL DEFAULT ''`，★ **空串不是 NULL**，折成 `direct` 只发生在读侧）＋ `normalizeGrowthSource` **两侧各跑一次**（客户端那次是为了不让非法字符在 `setRequestHeader` 上抛，服务端那次才是真相）＋ 公开端点多返 `bySource`；口径＝**捕获端末触、落库端先到先得**，这两个不矛盾，作用面不同。② **出生地**：22 张静态页回应用的链接全部带 `?ref=`（零 JS 页从前点了等于没点）。③ **锁**：`counters.test.ts` 15→27、`routes/growth.test.ts` 8→13，加三个新 web 文件 15＋4＋4 例；★★ **改坏取证三格**（注释掉注入 ⇒ 恰 1 例红；把一处回链退回裸 `/` ⇒ **红在反向锁**而不是正向那条；源码锁以「同一条正则各跑一次」校而非恒绿）全文在 `docs/dev/test-plan.md` §7。④ ⚠️ **两条刻意未做（老板拍板）**：采集点不换、UA 黑名单不加 ⇒ **`bySource.direct` 含第三方抓取器**，先取证再裁；MT-18 那条判据不受影响。⑤ ★ **本批学到的一条文档纪律**：老板点单「再补充几个推广途径」，先查的**不是途径清单而是能不能证实途径**——当天那 12 桶零来源就是「先做归因」的全部理由，本表 C1–C10 的优先级**在有读数之前不动**。⚠️ **顺手补了本表自己的一处欠账**：§4 漏了一整条 v0.1.12（现读旧首条＝v0.1.11），本批**补了一条指回原文的摘要、没有重抄全文**（两处各一份＝将来必有一份改不到）。
- **v0.1.12（2026-09-24 13:2x，批次 H-2＝C6 素材包落码 ＋ 三条打折）**：⚠️ **本条是补的账、不是新事**（★ 补写人＝批次 I 那次收口时发现 §4 漏了一整条；本表自家形状「每版在 §4 成行」被破了，破账留痕不静默删）。**全文在文件头那条 v0.1.12 版本行里，不在这里重抄一份**（两处各一份＝将来必有一份改不到，正是本表 §3 那条「过期的图会被当成证据直接发出去」的同族形状）；三条要点：**① 那六张真机截图是旧的**（图上 149 个 REST 接口 vs 现读 150，且 09-22 整页重排把「词条是主体」搬上前排，而唯一的首屏图里没这句话）；**② `index.html` 里那枚 GoatCounter beacon 是绝对地址** ⇒ localhost 打开也打到线上，「AI 探针只在本地栈跑」那条约定挡不住，且事后剔不掉；**③ ★★ `landingDrift()` 那把锁安静空转了 2 天**（文案搬进 `landing-copy.ts::STATS` 后正则抓不到任何东西，函数返回零行却照打「✓ 一致」）⇒ 判据形状从此加一条：**「✓」不等于「跑过」**。
- **v0.1.11（2026-09-24 12:2x，批次 H-1 落码 ＋ 一轮把本表两条判据改写的线上取证）**：★★ **这一轮的价值不在"又多了一格已落码"，在"两条被当成万能钥匙用过的判据当场被读到反例"**。① **C9 那条"验证文件不放行 robots 就必失败"对必应不成立**——线上 `robots.txt` 至今没有 `BingSiteAuth.xml` 那行 `Allow`（现读 grep＝0），而 `bingbot` 12:04 拿到 **200 ＋ 85 字节**；同一条 grep 还翻出**本表从前根本没查过 `bingbot`**（词表里只有 Googlebot／Baidu／扫描器三类，于是"必应从来没来过"这句从没被写过、也从没被证过，实际**全历史 11 次**）⇒ ★ **教训按形状记**：「某爬虫没来过」只有在**词表里查了它**之后才算数，**没查等于没说**（与 09-23「按裸词扫误报 5 页」、09-24「凭记忆敲 slug」同族，都是判据的输入侧出错）。② **"错地址一律返 200 壳"说满了**——`/NoSuchFile917.xml` 返**真 404／0 字节**，壳那一族只有 `/terms/*.html`；⇒ §6 那条"字节数才算数"的判据**继续有效但不许再往外推广**（根级写错是抓得到的，`/terms/` 下写错抓不到，两件事别再混成一句）。③ ★ **C4 挂了一条实证的口径隐患**：两族新登记的抓取器里，`Go-http-client` 那一族**会执行 JS**（同一轮里连 `/assets/index-*.js` 与 `/stats/count.js` 都取了），而我们的计数只在写入口剔**我们自己的**两个探针标记 ⇒ `app_open` 绝对值里混着爬虫；三个修法（UA 族黑名单／换真人动作埋点／禁止裸引绝对值）列在 §3，**裁定权在老板**，本表不代改。④ **落码半边**：H-1＝C1 英文侧 6＋1 页、双向 `hreflang`、7 张卡，公开页 14 → 21（★ 三条口径都是老板 AskUserQuestion 拍板：6 个概念不为凑数硬翻／地址同批带 hreflang 不做半件／卡片同批生成一张不缺），⚠️ 未发版。⑤ ⚠️ **一条我自己的错，登记不藏**：本会话 11:5x 曾在对话里对老板说「**仓库里没有验证文件**」——那句在它说出的那一刻就已过期（文件 12:00 落盘、我在 11:5x 读的目录），**报现状的窗口期只有几分钟，说出去之前要么重读要么别说**。

- **v0.1.10（2026-09-24 09:2x，批次 G-3 发版＝v0.2.118，C8 转 🟢）**：★ **这一条不是发版流水账，是本表第一次有渠道在「落码」之外走完「线上读到」那一格**——C8 那页此刻真的可 curl（7204 B／零脚本／9 条订阅项），且那把「公开字节不带内部字样」的新锁第一次拿到**线上侧**的对账（更新页全文 0 命中）。三件只有发版当轮才看得见的事：① **人守的纪律第一次执行**：发版当批回清洗表加了 v0.2.118 那一行 ⇒ 表从此**仓内 10 条／线上 9 条**，「线上比仓内少一条」从异常变成正常状态，代价的验收判据落到「下次发版后 `<entry>` 该读到 10」（★ 预言二**这一轮没被证伪、也没被证实**——它说的是「两次发版内会忘一次」，第一次没忘，判它还得等下一次）；② **同一批里犯了两次同一族错，且都不是判据错而是输入错**：回归脚本**凭记忆敲 slug**（12 个里 11 个不存在，全拿回 1888 B 的壳），与 09-23「按裸词『已有』扫误报 5 页」同族 ⇒ 本表口径加一句：**跑判据之前先现读事实源**，否则绿灯红灯都是自己的想象；③ **一条被引用成事实的读数被推翻**：`/atom.xml` 线上 `Content-Type` 是 **Caddy 按扩展名给的** `text/xml`，不是我们写的 `application/atom+xml` ⇒ 「我们对外声明了 atom 类型」这句话以后不许再说（阅读器照吃，不算故障）。⚠️ 同批订正：上一版头里那句「词条页输出逐字节未变」说满了（页脚那条新入口让 12 页各涨约 485 B：6345～6585 → **6830～7070 B**）。★ 本批发版实况：单批单发、八步 `exit 0`、包 5.8 M／834 条目、两端 sha256 `c3c86abb…4cb0`、预演迁移水位 41→41（零迁移＝不动）、切换后 `active`；★ **推送与发版各是一次点名授权，本表不因此多拿一次权限**。⚠️ 未回收：MT-23 一条没跑。
- **v0.1.9（2026-09-24 08:1x，批次 G-3＝渠道 C8＝公开更新页 ＋ 公开字节红线锁，拟 v0.2.118 未发版）**：★★ **这一批给本表添了一条新的红线类型**——以前的红线都管「代码别写错」，这一条管**「发布出去的字节里写了什么」**，而它第一次跑就红在一处**既有**泄漏上（`public/robots.txt` 的注释带着内部称呼）。⇒ 本表由此确认一件事：**「默认全封」的 robots 口径不只挡抓取，它自己也是公开面**（`Allow` 那几行是给全世界读的话）。三条今天才学到的东西：① **内容源的选择不是风格问题而是红线问题**：正则脱敏 217 行内部账本＝把「有没有漏」交给「我有没有漏一种写法」，手写 9 行清洗表＝**没写进来的东西不可能漏出去**，代价换成一笔「要记得加行」的人工账（★ 本表如实登记，并写下预言：两次发版内会忘一次——成立就把「加一行」并进 `tools/deploy.sh` 的发版清单，不靠锁）；② **一份话术抄两遍就漂一次**：三份独立文档（12 词条页／目录页／更新页）的壳从此只有一份 `page-shell.ts`，同一份清洗数据渲两遍出页与订阅；③ ★ **量具也要取证**：`--headless --window-size=390 --screenshot` 那张图是**宽视口裁切**（同一条 50% 宽对照条在 390 图里量到 246～259 px ⇒ 布局视口 ≈492～518），差点据此报一个「移动端被裁」的假 bug；宽度必须由 CDP `setDeviceMetricsOverride` 定。⚠️ 同批还记了一条**取证工具自己的假数据**（`execFileSync('npx')` 在 Windows 上根本没起进程，抛出被当成「红」）⇒ 教训写进 CHANGELOG 该行：**探针必须能区分「跑红了」与「没跑起来」。**
- **v0.1.8（2026-09-24 00:3x，批次 G-2＝渠道 C7）**：★★ 交付 `docs/GROWTH-SUBMISSION-PACK.md` v0.1.0——本表 C7 那格的「产品侧还要做什么」从此有了一份**能直接复制粘贴**的权威件，而它的第一纪律是**「写不出处的话不进包」**。三条今天才学得到的东西：① **红线真的收钱了**——因为不许新造事实，包里第一次把三件对外会尴尬的事成文：没有隐私政策页（实测返壳）、没有自助注销路由（现读 `auth.ts` 七条）、`Dockerfile` 从未实机跑通（compose 指向仓内不存在的 `Caddyfile`），⇒ selfh.st 的 `Docker` 那格**只能答「有文件、未验证」**；② **一条能力边界如实记**：各平台表单字段规格本会话抓不到（selfh.st 提交页只拿得到标题级内容）⇒ §7 只做「资产 ↔ 字段类型」映射、不猜字段名，**「不知道」写进文档比编一个像样的字段清单便宜**；③ ★ 顺带把 `GROWTH-SPEC.md` 抬到 v0.1.1——对外卖点引用了「只用『次』这个字、不落裸 IP」，**卖点引用的契约带着过期状态＝拿旧口径对新用户说话**，故那三处「未发版」按发版事实加了回标（原句不回改）。⚠️ §8 那张提交登记表本会话**一行都不许代填**（提交是老板的动作）。
- **v0.1.7（2026-09-24 00:0x，记 09-23 23:5x 那次发版）**：★★ **五批一次上线**（v0.2.113＝C5 种子／v0.2.114＝C4 计数／v0.2.115＝同伴的 AI 出题／v0.2.116＝C2 分享卡／v0.2.117＝C3＋C1 断口），走 `tools/deploy.sh` 八步全绿、预演迁移水位 40→41，判据实录见 `SEO-SPEC.md` §6.2。★ **本表两条同时翻味，方向相反**：① **C1 那条静态入口今天才第一次为真**——码在昨天就写完、账也记了，可线上 `/` 一直是那份零个静态 a 标签的壳，直到这次发版；② **C5 的种子机制上线即失效**（现读体验号池已有 **135 条**，触发条件「池为空」不成立 ⇒ 首屏本来就不空，但那不是本批功劳）。★ **C4 顺手拿到一条生产级证据**：三次带标记探针动作后 `GET /api/growth/counters` 仍三数全 0／`firstDay:null` ⇒ 「探针不计」在线上同样成立（反面要说清：**`demo_enter` 大概率长期为 0，这就是「宁可少不可假」的样子**）。⚠️ **判据 7（有没有人来）今天还不能判**——本轮自己的请求全带 `studentbuddy-probe/1.0 (release-accept-g1)`，剔除后真人仍 0；★ 本批改的是「路有没有铺好」，不是「有没有人走」。

- **v0.1.6（2026-09-23 批次 G-1＝C3 收口＋C1 断口补口，v0.2.117 未发版）**：★★ **这一条的性质是「上一批的验收把自己算成了成果」**。批次 E 那七条判据当时全绿，可绿的全是「文件在不在、字节对不对」，**没有一条问「不执行 JS 的那一份首页里有没有一条走得通的路」**——于是「两处入口已上线」这句话在爬虫面前根本不成立（线上 `/` ＝ 1487 字节、零静态 `<a>`）。今天的读数据把它顶出来的过程记全：Googlebot 四轮抓 `/`＋`/robots.txt`、**零抓 sitemap**，而 robots 里 `Sitemap:` 那行早就在 ⇒ **声明有、路没有**。① 补法选了「往手写 HTML 壳里塞一条静态链接」这条最笨的路，★ 并且当场把「它必须不可见」也做成锁（`term-page.test.ts` ⑮；为什么不用「挂载后 JS 隐藏」：CSP 现为 Report-Only，将来转强制会杀掉负责隐藏的内联脚本，而 `hidden` 不需要 JS 参与）；② 同日把 C3 的入口行收口，**并把警示一起搬**（这一条不是排版洁癖——公用池全站共享，README 是新人唯一的前置阅读物）；③ 销掉自己 F-3 留的产物账（204／2817）；④ ★ **收口自查改短了注释**：初稿把取证过程与 CSP 判断写进了 `index.html`——那是**随站发布的字节**，而同族红线正是下一批 C8 要防的事（公开页不许带内部挂账）⇒ 终版只留一句「看 `SEO-SPEC.md` §5 第 11 条」的指针；★ 连带把一条**易碎判据**改了口：上线后首页壳从 1487 B 变到约 2.5 KB，所以「1487 字节＝壳」这种精确数以后一律读成「差一个 KB 级＋有没有该页正文」（`SEO-SPEC` §5 第 11 条附注②）。⚠️ **诚实记未做**：本批**不产生任何抓取效果证据**，判据全在站长平台（C9 已点名）；线上仍是旧壳（未发版）；MT-21（「首页逐像素没变」）未回收；★ 当日读数里另有两条与渠道无关但值得记的：**新注册用户 1 个**（其出口 IP 与本会话取证用的代理重合，很可能就是老板本人那条测试，未定性）、**内容安全侧一条缺陷**（搜索兜底通道里，一个学生向查询的第一条结果是成人站；本会话只报告未动码，等老板排期）。
- **v0.1.5（2026-09-23 批次 F-3＝C2 公开页分享卡，v0.2.116 未发版）**：**C2 的「公开页这半边」落码**——14 页各一张自指的 1200×630 卡 ＋ `summary_large_image`。★★ **两条值得从代码里抬出来记进台账**：① **图是提交资源、不是构建产物**——这个选择不是偷懒，是「发版 tar 与 CI 里没有浏览器」这条事实逼出来的，代价＝语料变了必须有人记得重跑 `npm run og:shots`；本批把它变成**机器可查**（「图真的在」那一组锁住文件存在、IHDR 尺寸、引用与文件对得上），并让生成器**量排版不量像素**（越界盒与最低内容底边报在退出码里，实测 14 张全在 566px < 630）；② **§7 取证第一轮逮到自己一把软锁**——「卡上数字与语料字段一致」在**语料每条恰好 3 误区 3 动作**时拦不住有人把 3 写死（16 例全绿），改成按派生判才红；这条已升格为 test-plan §7 的通用约定（**恒定字段不构成锁**）。⚠️ **未验**：`/og/*.png` 在线上是否真由 Caddy 直出（本站有「错地址也返 200 壳」的前科 ⇒ 判据要带 `content-type`）、卡片在真实对话框里的观感（**MT-20 待派**）。★ 站内分享 URL 那半边**仍未做**（老板拍板推迟，动 `term_library` 公开列＝多租户边界，不是忘了）。
- **v0.1.3（2026-09-23 批次 F-1）**：**C5 落码（未发版）**——八条词条种子灌进体验号，题材按老板拍板与 12 页公开讲解页**同名同源**（★ 同名这条不是文案偏好，是锁：`term-corpus.test.ts` 第 9 例逐条核名）。★★ **本批两条与「诚实」有关的设计值得从代码里抬出来记在台账**：① 种子**只经领域函数写** ⇒ 事件总线零事件、`daily_activity` 零行 ⇒ **F-2 的计数从结构上数不到种子**（不是「记得排除」）；② **池内已有任何访客存的词条就整批不灌**（代价：将来加种子词条，老池不会自动补）。⚠️ **一条尚未知的运行事实**：线上体验池是否已被早先访客写过**未查**——若已写过，发版后首屏仍是空的，那是**既定行为不是 bug**，判据是查库不是猜。★ C4／C2 的**范围收窄**是老板拍板，不是本台账自己缩的（见 §2 批次 F 行）。
- **v0.1.2（2026-09-23 09:54 批次 E 发版）**：C1 转 🟢 已上线两轮，验收七条全绿；★ **同批从线上验收里新逮到一条代价**——不存在的 `/terms/*.html` 返 **200＋1487 字节 SPA 壳**而不是 404（soft 404，已记入 `SEO-SPEC.md` §5 第 9 条，并把 §6 第 2 条判据改成「状态码＋字节数」）。★ 效果观测**尚未通过**：首轮 37 条 `/terms` 命中全是本轮验收自己打的 `curl`，真实第三方访客为 0——这条诚实写进 C1 判据格，不许拿它说「有流量」。
- **v0.1.1（2026-09-23 批次 E）**：C1 目录页 URL 与两处入口落码（未发版）；C10 探针打标与 §3 排除清单成文；C9 补上「`robots` 默认全封会挡住站长验证文件」这个坑与通路。
- **v0.1.0（2026-09-23）**：首版。渠道清单从对话里落成台账（C1–C10 ＋ 批次 E/F/G/H 顺序）。
