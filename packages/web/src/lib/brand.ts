/**
 * 品牌牌的**唯一事实源** —— 产品侧栏 logo 与落地页顶栏用的是同一对字符串。
 *
 * ★ 为什么抽出来：这两处印的是同一块牌子（吉祥物 + 名字 + 副标题）。各写一份必然漂，
 *   而落地页顶栏那句副标题一旦 ≠ 产品里的那一句，就是老板最在意的那类「货不对板」：
 *   门面上的产品名和进去以后的不是同一个。共用常量让这条差异**结构上不可能出现**。
 *
 * ★ 尺寸不在这里：产品侧栏按侧栏的语境排（14px/10px，`app.css` 的 `.sb-logo-name`），
 *   落地页顶栏按门面的语境排（20px/12.5px，`landing.css` 的 `.landing-brand-*`）。
 *   共用的是**文案**，不是字号——同一块牌子在不同场合本来就该有不同尺寸。
 */
export const BRAND_NAME = 'studentbuddy';

/** 产品自己的那一句副标题（侧栏 logo 里就挂在名字下面） */
export const BRAND_TAGLINE = '你的专属学习助手';
