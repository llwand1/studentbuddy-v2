# GROWTH-CHANNELS — 免费增长渠道台账

> 版本：v0.1.0 | 状态：[活跃] | 创建：2026-09-23（老板 09:06 拍板「现在发」＋ 09:1x 口述「**目前这些渠道都要做下去**」）
>
> ★ **为什么先有这份文件**：渠道清单此前**只活在对话里**——一个都没落到仓内任何文档。这是流程漏洞（§0.13：跑掉的决策必须成文），本文件补上。
> ★ 以后**每开一个渠道先在本表成行**，再动手；不在表里的渠道＝没讨论过，别顺手做。

## 0. 三条总口径（凌驾下面所有条目）

1. **「做能力」＝做进产品，不是 AI 代做动作**。发帖、提交导航站、注册站长平台都是**老板本人的账号动作**（AI 不代做、也不代注册）；本表里凡标 `产品侧` 的格子，才是我该写代码的部分。
2. **数字只许放宽口径，不许改标签**（见 `CHANGELOG.md` 2026-09-23 08:56 行的取证结论）：现有设施算不出 UV（GoatCounter 原始 `hits` 表 0 行），所以任何对外挂出的量**只能叫「访问／次」，不能叫「人」**；要叫「人」就必须换成产品内真实动作计数。
3. **度量可信是渠道的前提**：爬虫与探针流量必须可识别（C10），否则任何渠道效果都是假的——★ 已实测到的污染源：UptimeRobot 952 次/5 天、伪装百度 UA 的扫描器、`ntp.msn.cn` 预取 15 次、referer 垃圾词 `ratelzr/afterawy/fenceud4/gatehk1`。

## 1. 渠道总表

| # | 渠道 | 状态 | 产品侧还要做什么 | 老板本人动作 | 验收判据（可证伪） |
|---|------|------|------------------|--------------|--------------------|
| **C1** | **搜索长尾**（词条讲解页） | 🟡 **已上线**（09-23 09:09，12 页中文静态页 ＋ `robots.txt` 半开放 ＋ `sitemap.xml`） | ① 目录页 URL 从 `/terms/` 换到 `/terms/index.html`（★ 实测：前者线上返 SPA 壳，后者 200／5705 字节正常）；② 英文侧词条页；③ 应用内挂一个入口（登录后看不见这批页）；④ JSON-LD（要先实测 CSP Report-Only 会不会刷报告） | 百度搜索资源平台验证站点＋主动推送 API 提交；Google Search Console 验证（可放 HTML 验证文件，这条产品侧能配合） | 上线 1～2 天后 grep Caddy 日志 `/terms/*.html` 是否出现过非零，并按 UA 分开看（★ **不许用 GoatCounter 判断**，词条页零 JS 它数不到） |
| **C2** | **社交分享面**（可分享的卡片与链接） | ⚪ 未开工 | 每个词条页/学习成果给**唯一可分享 URL** ＋ 完整 `og:*` ＋ 一张不依赖 JS 的卡；★ 落地页已有 og 六件，产品内还没有 | 把卡发到即刻／小红书／V2EX／微信群 | 分享出去的 URL 在社交平台的预览卡渲染出正确标题与图（curl 页面 `og:` 即可预验） |
| **C3** | **开源仓库位**（GitHub） | 🟢 已在做（README 是第二门面，GitHub 来源实测 6 次点击） | README 顶部加一条「免注册直接体验」的入口行；badge 数字保持与 `metrics --tests` 同源 | 提 `awesome-*` 清单 PR、开 GitHub Topics | 来源分析里 `github.com/llwand1/...` 的点击数变化（Caddy 日志／GoatCounter referrer） |
| **C4** | **社会证明计数**（原批次 B） | ⚪ 未开工（★ 口径今天被证伪了一半，见 §0 第 2 条） | 计**产品内真实动作**（进过应用／点过体验号／生成过词条），标签写清是「次」还是「人」；⚠️ 不许把爬虫与探针算进去 | 决定挂在哪（落地页首屏？） | 前端显示数＝后端可查数，且**同一 IP 刷新不重复计**（可测）；★ 真实计数为 0 时页面不许显示假数 |
| **C5** | **零门槛体验转化**（原批次 C：给共享体验号灌种子内容） | ⚪ 未开工 | `u-demo-shared` 里灌**可看的一屏内容**（词条／题目／复习队列／当日总结），全部用**真实机制**生成，不写死假数据 | 提供内容题材；决定「访客彼此可见」的提示要不要加强 | 访客点「免注册直接体验」后**首屏不是空态**（真人截图为准，§0.16） |
| **C6** | **中文内容社区**（知乎／少数派／即刻／小红书） | ⚪ 未开工 | 需要的是**素材**：可嵌入的讲解页（C1 已给）、真截图（C5 后才有）、一段 30 秒内说清「词条是主体」的文案 | **本人发帖**（AI 不代注册、不代发） | 帖子发出后来源分布里出现对应域名（Caddy 日志） |
| **C7** | **导航站与自托管清单**（selfh.st／AlternativeTo／Product Hunt／国内自托管导航） | ⚪ 未开工 | 一份提交资产包：一句话简介（≤140 字）、截图、标签、隐私口径（无 cookie 统计／数据自持）——都从 README 与落地页抽，不新造事实 | **本人提交** | 提交列表成文（在本表登记提交时间与链接） |
| **C8** | **更新可见性**（公开 changelog／RSS） | ⚪ 未开工 | 把 `CHANGELOG.md` 渲成一个公开静态页（★ 可与 C1 的 SSG 同一套机制复用），配 `atom.xml` | 转发大版本更新 | 页面可 curl 到、且不含内部挂账与密钥字样（★ 现在 `CHANGELOG.md` 里**有**服务器路径与运维信息，**绝不能原样公开**——要做必须另出清洗版） |
| **C9** | **搜索引擎工具面** | ⚪ 未开工 | `sitemap.xml` 已上线；补 SearchSite 验证文件（Google／百度各自一个文件或 meta 标签） | 站长平台账号动作 | 验证文件线上可 curl 到且验证通过 |
| **C10** | **度量卫生**（原任务 #4） | ⚪ 未开工 | 给探针流量加可识别标记（UA 前缀或自定义头）＋ 一份「排除清单」写进统计与批次 C4 的口径 | 决定 UptimeRobot 留不留 | 日志里能一条 grep 把探针流量与真实访客分开 |

## 2. 执行顺序（老板 09:1x：「目前这些渠道都要做下去」）

★ 顺序按**依赖**排，不按编号：C1 收尾（目录页 URL／入口）与 C10（度量卫生）最便宜且是别人的前提 ⇒ 先做；C5（种子内容）是 C2/C6 的素材前提；C4 的口径已被今天的取证钉住，可与 C5 同批；C8 有**必须先清洗**的安全代价；C6/C7 的产品侧只有「素材包」，主体是老板动作。

| 批次 | 覆盖渠道 | 内容 |
|------|----------|------|
| **批次 E** | C1 收尾 ＋ C10 ＋ C9 | 目录页 URL 修正（`/terms/index.html`）＋ 应用内入口 ＋ 探针流量标记与排除清单 ＋ 站长验证文件的通路 |
| **批次 F** | C5 ＋ C4 ＋ C2 | 体验号种子内容 ＋ 诚实计数（真实动作，标签写「次」）＋ 分享 URL 与 og |
| **批次 G** | C3 ＋ C7 ＋ C8 | 提交资产包（文案／截图清单）＋ README 体验入口行 ＋ **清洗版**公开更新页 |
| **批次 H** | C1 英文侧 ＋ C6 | 英文词条页（把英文长尾打开）＋ 社区贴文素材 |

⚠️ 每批收口都要过 §0 三条口径与闸门（四连／test-plan 登记／§0.16 真人测试单）；**发版仍归逐次点名授权**，本表不预授权。
