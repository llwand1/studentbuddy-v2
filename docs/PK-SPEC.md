# AI 出题 PK 契约（PK-SPEC）

> 版本：v0.8.0 | 状态：[P0-1／P0-2／P0-3a／PVE／**P0-7**／**P0-8 投降+对战历史**／**UX 全面增强（§13）已落地**·**P0-4 公共题在途**·**P0-5／P0-6（理解题+裁判 AI）契约已立码未开**·**§14 邮箱登录 + 邀请链接已定案待实现（B1）**·**§15 对战玩法扩展契约已立待实现（判断题／词条／情景题，B2~B4）**·P0-3 真机验收未跑] | 更新：2026-09-20
> 定位：studentbuddy 的**移动端（微信内）双人对战玩法**——两人在同一局里**互出公共题**，
> 在固定时限内比「更快更准」，积分定胜负；盘中由**裁判 AI** 出理解题并给出 0~3 档理解分。
> 原则：**先立契约再改码**（AGENTS.md 已知约束）。★ **登录方式已于 2026-09-18 改判**：不再走「P0 昵称模拟 → P1 微信授权」这条路（微信网页授权需企业主体，对个人**永久不可得**，见 `AUTH-SPEC §0.1`），改为**复用统一账号体系的邮箱登录**；新增邀请链接，详见 **§14**。
> ⚠️ **「微信内」的语义要分清**：它是**运行环境**（H5 在微信浏览器里打开、邀请链接经微信分享）——**这条仍然成立**；但它**不再是登录方式**。
>
> **落地进度（逐批记账唯一处＝`CHANGELOG.md`，本节只做指针不做第二事实源）**：
> **P0-1 已完成**（2026-09-12）——① 登录链路：`pk_users` v10 迁移 / `POST /auth/login` / `GET /auth/me`
> （提交 `e02d67a`）。★ **入口已于 2026-09-18 收窄**：侧栏常驻的昵称框 `UserAuthBox` **已删除**，
> 昵称登录只在**进入 PK 房间时**发生（`features/pk/usePkIdentity` → `/api/pk/auth/login`）；
> 后端 `/api/pk/auth/*` 与 `pk_users` 表**原样保留**，PK 功能不受影响。
> 删除理由：它常驻在侧栏、看着像「已登录」却不产生任何数据归属（真账号是 `AccountBox`），
> 属「以为什么都做了」的错觉源——而错觉在权限类问题上比没有功能更危险；
> 两套身份仍并存（AUTH-SPEC §0.1：**微信 / 手机号登录已于 2026-09-18 拍板不做** ⇒ 原「M4 才合并到同一个 user」**已取消**，两套身份的合并路径**待重定**）。② 房间链路：`PkRoomState`/`PkPlayer`/`PkQuestion` 类型 + 房间常量 + `pkChannel()` 频道键、
> `pk/room.ts` 内存状态机、`/rooms`·`/rooms/join`·`/rooms/:id/start`·`/rooms/:id/state` 四端点、
> SSE `pk:` 频道与 `pk-state` 主动广播。
> **P0-2 已完成**（2026-09-13）——出题 CD / 判分 / 答题超时 / 怠慢惩罚 / 对局时钟结算全落地
> （`pk/match.ts`，**1s ticker 统一时间驱动**，计分见 §1、两端点见 §2.1）。**PVE 同批完成**（§8）。
> **P0-3a 已完成**（`#/pk` 路由 + 大厅/等待/对局实时视图，见 §5）；**P0-3 收尾的真机验收未跑**。P1 未开工。
> **P0-7 已完成**（2026-09-13，本节 §11）——**主题轮转**（两人各选一个主题、按轮次交替）／**出题跑题校验**
> （裁判 AI 判贴合度，跑题判失败、连败 3 次 −1 并给建议）／**求助道具**（每局 1 次，裁判当场联网给建议与知识，
> **绝不给答案**）／**错题二次机会**（选一道错题 → 现场解析 → 同主题相似题，答对 +2，3 分钟 CD）。
> 落地件：`pk/judge.ts`（裁判四能力，新增）、`pk/power.ts`（道具与二次机会，新增）、`pk/match.ts`（跑题校验）、
> `llm/router.ts` 新增 `judge` 角色、前端 `PkTopicBar` / `PkJudgePanel` 两组件。
> **P0-8 已完成**（2026-09-14，本节 **§12**）——**投降**（认输即对手胜、比分定格，与时钟归零结算共用
> `finishRoom` 一条收尾路径）／**对战历史**（v15 建 `pk_matches`，视角行；`GET /api/pk/matches` 列表 +
> `/matches/:id` 详情含末快照供题目回看）。★ 本批**部分推翻**决策记录 ⑤「P0 无战绩持久化」——
> **只落已结束的一局**，房间状态机仍全内存。
> ★ 头部状态曾长期停在「企划已定·未开工」而与代码事实不符（登录 2026-09-09 21:31 已提交），属状态漂移，2026-09-12 修正。
> ★ 2026-09-13 二次正名：P0-2 批次只改了本文 §1/§2/§6，却把版本头留在 v0.1.0 且留下 4 处**指向不存在章节**的
> 「§8」引用（§1、§2、§6 三处及本页）——本批补齐 §8 并正名版本头（逐批证据以 `CHANGELOG.md` 为准）。
>
> **决策记录（2026-09-09 与老板 grill 钉死）**：
> ① P0 局域网双人 Demo（同一 WiFi 手机浏览器/微信内置浏览器可开），不做公网；② 登录 = 契约先行模拟登录；
> ③ 题型 P0 纯选择题（系统秒判分，零争议），后续可拓展；~~**2026-09-13 二次 grill 部分推翻**~~：新增**理解题**＝主观判分（0~3 档，见 §9），选择题仍是唯一「秒判零争议」题种；④ 胜负 = 固定时长积分制；
> ⑤ P0 范围 = 最小核心环（登录占位→建房/入房→出题/答题→计分→结算），**无观战、无战绩持久化**；
> ~~**2026-09-14 老板点单部分推翻**~~：新增**投降**与**对战历史**（§12）⇒ 战绩从「P1 才落库」提前到本批，
> 但**只落已结束的一局**（房间仍是全内存），且**无观战**这一条不变；
> ⑥ 实时同步复用 `chat/sse-bus.ts` 机制（频道键隔离）；出题引擎复用 `learning/quiz.ts` 管道。
>
> **决策记录（2026-09-13 二次 grill 钉死：公共题 / 理解题 / 裁判 AI）**——老板原话三条：
> 「公共题的功能没有做」「每次 ai 出题最好自己也能做，作对自己的题目加一分，作对对方的题目加 2 分，
> 加一个裁判 ai，这个 ai 实时监测对战情况，然后负责理解题出题和打分」「加一个理解题，每 3 分钟固定由
> 裁判 ai 出题，按照本局对战的主题来出题，每个理解题总分 3 分，分为 0-3 分四档」。
> ⑦ **玩法改判**：原「出题人自己不答这道题」（§3 原文）**推翻** ⇒ 改成**公共题**：一道题双方都答。
> ⑧ **计分**：成功出题 +1 **保留**；新增「答对自己出的题 **+1**」；答对对方出的题仍 **+2**；
>    答错/超时/怠慢三条 −1 惩罚**全部保留**。（理由：不给出题正激励就没人愿意出题，光靠怠慢惩罚压不住。）
> ⑨ **揭示时序**：**双方都提交或都超时后才揭示**正确答案与双方结果；本人提交后**立即**知道自己对错。
>    理由：一道题两人都答时，谁提交谁揭示＝先答方把答案送给后答方（详见 §1.2）。
> ⑩ **理解题**：每 3 分钟由裁判 AI 按**本局主题**出 1 道；0~3 四档；**分数计入同一总分**；
>    对局仍 **8 分钟**（⇒ 第 3、6 分钟各一道，理解分区间 0~6 分，足以改变胜负）。
> ⑪ **裁判 AI**：「实时监测」落地为三件实活——**出理解题／判分并给理由／关键事件推一条盘中短评**。
>    播报数据**只读服务端已有状态**，不额外调模型（零额度成本、零延迟）；不做「每 3 分钟一段战况点评」
>    （那是额外的模型调用，固定节奏播报容易变噪音）。
> ⑫ **★ 反作弊红线（本批最大的架构约束，先于任何实现）**：`chat/sse-bus.ts` **只有整频道广播、
>    没有按订阅者定向的能力**（`subscribe(channel,res,since)` / `publish(channel,ev)`，见 §2.2 实测），
>    而 PK 双方订阅**同一个 `pk:<roomId>` 频道** ⇒ **任何进 SSE 的字段都是双方可见的**。
>    既有的「正确答案永不下发（含 SSE）」（§1）因此推出两条硬规矩：
>    ① **私有判定结果（我对了没有、得了几分、裁判给的理由）只能走 REST 响应体**，绝不进 SSE；
>    ② SSE 只播**公开事实**（「某人已提交」「该题已揭示」），揭示前连比分变化也不播。
>
> **决策记录（2026-09-13 三次 grill 钉死：主题轮转 / 跑题校验 / 求助道具 / 二次机会，细则见 §11）**——
> 老板原话四条：「对战的主题是两人单独选…轮着来，每一个出题轮次都只能出一类主题的题」「不符合主题就视为出题失败…
> 累计每三次都失败则扣一分，并且裁判 ai 会给你一些建议（出题的选型 + 相关知识）」「引入道具功能…求助 ai 的道具，
> 每局每个玩家只有一个…ai 裁判会当场联网搜索，然后给你建议，以及进行知识输出」「对于对战中玩家的错题…
> 获得现场解析，之后 ai 裁判会根据同一个主题再出一道类似的题目，如果这次作对，那么也能够加两分，然后有 3 分钟 cd」。
> ⑬ **两个主题、按轮交替**：双方各选一个主题，**每一轮只能用当轮主题的题**；轮次归属由服务端
>    `currentTopic` / `topicOwnerId` / `topicTurn` 三字段定，**前端不参与裁决**（防改包）。
> ⑭ **跑题 = 出题失败，不是作弊**：不贴合 → 422 `TOPIC_MISMATCH`，**不扣题、不占 CD**（可立刻重出），
>    只累计 `failStreak`；**连败满 3 次才 −1** 并由裁判给出题选型与相关知识。
>    ⇒ 判据是「失败要付代价、但代价是次数不是节奏」——罚 CD 会让跑题变成「罚站」，与「鼓励出题」的动机相反。
> ⑮ **求助道具**：每人每局 **1 次**（`helpLeft`），用完为止；裁判**当场联网搜索**后给建议 + 知识输出，
>    **★ 硬规则：求助输出绝不含本题答案**（答案一旦可求，PK 的判分就失去意义）。
> ⑯ **二次机会**：只能选**自己的错题**，先拿现场解析，再由裁判按**同一主题**出一道相似题；
>    答对 **+2**，**原来的 −1 保留**（**净 +1**）——不给「答错无代价」，否则错题可以靠补救刷回满分。
> ⑰ **3 分钟 CD**（`RETRY_CD_MS`）按玩家计，防止把「二次机会」当成常规答题通道连刷。
> ⑱ **裁判是旁挂能力，不是主路径依赖**（ADR-4 的具体化）：裁判模型未配/调用失败/输出不合法 ⇒
>    **一律放行**（跑题校验跳过、道具**不扣次数**、二次机会返回 502 且不计 CD）。
>    理由：裁判一挂就玩不了，是把「可选增强」做成了「单点故障」。

---

## 1. 玩法规则（计分唯一事实源）

一局 PK = 固定时长（默认 **8 分钟**，服务端可配）内的双向对抗。双方**同时**拥有两个动作：
**出题**（写提示词 → AI 出一道**公共题**）与**答题**（答**每一道**公共题）。

★★ **公共题（2026-09-13 改判，决策 ⑦）**：一道题**双方都要答**——
出题人答自己出的题（**+1**），对手答这道题（**+2**）。出题人信息优势最大，故分值最低。
原口径「出题人自己不答这道题」（§3）**已作废**：那条是为防「出题人泄题」写的，但代价是出题人
对自己的题毫无参与感、也拿不到除出题奖励外的任何分；改判后的动机方向依然成立——**难倒对手才划算**，
而答对自己的题只有 1 分，不足以让人靠出简单题刷分。

| 动作 | 分值 | 约束 |
|------|------|------|
| 成功出 1 题 | **+1** | 服务端强制 CD：同一玩家两次成功出题间隔 ≥ `QUIZ_CD_MS`（默认 60s）；CD 内提交 → 429，不扣分 |
| **答对自己出的题** | **+1** | 公共题新增（⑦⑧）。出题人也要答自己的题；信息优势最大 ⇒ 分值最低 |
| **答对对手出的题** | **+2** | 即原「答对 1 题 +2」。服务端判分（选择题：选项比对，毫秒级计时） |
| 答错 1 题 | **−1** | 判分即结算，无申诉 |
| 答题超时 | **−1** | 超过 `ANSWER_TIME_MS`（默认 45s）未提交，服务端自动判超时；之后该题作废不可再答 |
| 怠慢（长时间不出题） | **−1 / 次** | 对局进行中，每 `IDLE_PENALTY_MS`（默认 120s）内无一次**成功**出题提交（CD 中提交被拒不算，重试不算），扣 1 分，可累计 |
| **理解题判分** | **0 ~ 3** | 裁判 AI 出题 + 判分（§9）；**计入同一总分**（⑩），不是独立赛道 |
| **出题跑题连败 3 次** | **−1** | P0-7（§11.2，⑭）：单题跑题**不扣分**，只累计 `failStreak`；满 3 次才 −1 且 `failStreak` 归零重数 |
| **补救题（二次机会）答对** | **+2** | P0-7（§11.4，⑯）：**原错题的 −1 保留** ⇒ 净 +1；3 分钟 CD 内不可再请求（⑰） |

- **分数可为负**（增加紧张感，不设下限）。
- **结算**：对局时钟归零 → 分高者胜；平分比正确率（答对数），再平判为平局。
  ★ 理解分按 ⑩ **已计入 `score`**，故结算规则**零改动**——这是「计入同一总分」相对「单列赛道」的
  唯一好处：不必发明一套双维度比较（总分 → 理解分 → 正确率）并为此改结算、改前端、改测试。
- 所有计时（CD / 答题时限 / 怠慢惩罚 / 对局时钟 / 理解题出题节拍）**全部在服务端**，客户端时间只作展示。
- **正确答案永不下发给答题方**（含 SSE 事件），直到该题**被揭示**——揭示时序见 §1.2。

### 1.1 一道题的生命周期

```
出题（写提示词） → 生成公共题 → 双方各自作答（各自独立计时，同一 deadlineAt）
                                        ↓
                        双方都提交 / 都超时 / 一方已交一方超时
                                        ↓
                                揭示（答案 + 双方结果公开）
```

- 每道题**双方各有一份作答记录**（§2 `PkAttempt`）。`deadlineAt` 对双方相同（自 `createdAt` 起算），
  但两份作答各自先到或超时，故 `revealed` 在**最后一份落定后**才置真。
- **出题人也答自己的题，且出题与作答并行**——不必等对手先答。串行化会把 CD 与怠慢惩罚的节奏拖死。
- PVE 下 AI 出题后**AI 也答自己的题**；公平性残留风险见 §8.5（已知并接受，记录在案）。

### 1.2 揭示时序与私有反馈（决策 ⑨⑫ 的落地；**本批最容易做错的一处**）

一道题两人都答，若沿用「谁提交谁立刻判分 + 广播」，会同时踩两个洞：

1. **答案泄露**：先答方一提交，后答方就从 SSE 拿到正确答案 ⇒ 等于送分。
2. **无法定向**：本仓 `chat/sse-bus.ts` **没有按订阅者裁剪的能力**，而双方订阅的是同一个
   `pk:<roomId>` 频道 ⇒ **任何进 SSE 的字段双方都看得见**（⑫ 实测，见 §2.2）。

故定死三条：

| 规矩 | 内容 |
|------|------|
| **私有反馈走 REST** | `POST /answer`（选择题）与 `POST /comp/answer`（理解题）的**响应体**回本人 `{ correct, score, delta, reason }`。这是「本人提交后立即知道对错」的**唯一**通道 |
| **SSE 只播公开事实** | 提交只播「某人对某题已提交」（`pk-attempt`，**不含**对错、不含选择）；揭示时才播答案与双方结果（`pk-verdict`） |
| **揭示前不播本题分数** | 未揭示题的分数变化**不进** SSE 快照 ⇒「对手答对了」这条信息不会提前泄漏。<br>**代价（已知并接受）**：本人**刷新页面**后，未揭示的题看不到自己刚答的对错（前端把 REST 回执缓存在内存，刷新即丢），下一次揭示会补齐 |

> **为什么不改造 sse-bus 支持定向推送**：那会动到聊天主链路的公共实现（R1 红线：一个模块一个工作面，
> 且该文件有其他会话在途），而收益只是省一次 REST 回执；REST 回执天然点对点，比在广播通道上加白名单更不易错。
- **常量归属**：P0-1 登记 `PK_MATCH_MS`（8 分钟）／`PK_MAX_PLAYERS`／`PK_ROOM_CODE_LEN`／`PK_ROOM_TTL_MS`／
  `PK_FINISHED_KEEP_MS`；**P0-2 已补登记** `QUIZ_CD_MS`（60s）／`ANSWER_TIME_MS`（45s）／`IDLE_PENALTY_MS`（120s）／
  `PK_PROMPT_MAX`（300）＋ PVE 三常量 `AI_USER_PREFIX`（`'ai-'`）／`AI_FIRST_QUIZ_DELAY_MS`（5s）／`AI_RETRY_DELAY_MS`（10s），
  全在 `shared/src/pk.ts`（§8）。**P0-5 待补登记**（理解题与裁判，§9）：`COMP_INTERVAL_MS`（180s，每 3 分钟一道）／
  `COMP_ANSWER_MIN`（作答最少字数，少于则不送判分、直接 0 分）／`COMP_ANSWER_MAX`（作答上限，服务端截断）／
  `COMP_SCORE_MAX`（3，档位上限）／`JUDGE_USER_ID`（裁判出题时 `fromUserId` 的固定值）。

## 2. 数据契约（先在 `packages/shared/src/pk.ts` 登记再实现）

```ts
/**
 * 一名玩家对某道题的作答（★★ 公共题：双方各一份，P0-4 起）。
 *
 * 公开 vs 私有的分界（§1.2，**最要紧的一条**）：
 * ① `status` / `submittedAt` 是**公开事实**——双方都要等对方，不知道在等谁就没法玩；
 * ② `chosen` / `text` / `correct` / `score` / `delta` / `reason` 是**私有结果**，
 *    只在 `PkQuestion.revealed === true` 之后才出现在**任何**载荷里。
 */
interface PkAttempt {
  userId: string;
  status: 'pending' | 'submitted' | 'timeout';
  submittedAt?: number;       // 公开：前端据此显示「对手已交卷 / 等待对手」
  // ── 以下字段仅 revealed=true 后出现（揭示前连键都没有，不给手滑留口子）──
  chosen?: number;            // single：所选选项下标
  text?: string;              // comprehension：作答原文（§9）
  correct?: boolean;          // single：判对否
  score?: number;             // comprehension：0~3 档理解分（§9.3）
  delta?: number;             // 本题对总分的净影响
  reason?: string;            // comprehension：裁判给的判分理由
}

/** 题目（公共题：**双方都答**；对答题方始终隐藏 answer，直到 revealed） */
interface PkQuestion {
  id: string;             // pq-<roomId>-<seq>
  roomId: string;
  /** single = AI 出的四选一（P0-4）；comprehension = 裁判 AI 出的理解题（P0-5，§9） */
  kind: 'single' | 'comprehension';
  /** 出题人：single = 玩家 userId 或 AI 座位 `ai-<roomId>`；comprehension = `JUDGE_USER_ID` */
  fromUserId: string;
  prompt: string;         // 出题提示词原文（≤300 字）；理解题存「本局主题」
  stem: string;           // 题干
  options: string[];      // single 4 项；comprehension 为**空数组**（无客观选项）
  createdAt: number;
  deadlineAt: number;     // createdAt + ANSWER_TIME_MS；**双方同一个**
  attempts: PkAttempt[];  // 公共题：双方各一份（长度 = 房内玩家数）
  /** 最后一份作答落定（都提交 / 都超时 / 一交一超）后置真；此时才揭示答案与双方结果 */
  revealed: boolean;
  answerRevealed?: number; // single 专用；revealed 之前连键都没有
}

/** 对局快照（GET state / SSE `pk-state` 载荷；**两者共用同一形状＝「公开版」**，§1.2） */
interface PkRoomState {
  roomId: string;
  roomCode: string;      // 6 位数字，入房凭证
  status: 'waiting' | 'active' | 'finished';
  mode: 'pvp' | 'pve';   // PVE 建房即占 AI 座位（§8）
  players: { userId: string; nickname: string; score: number;
             correct: number; answered: number; lastQuizAt: number }[];
  nextQuizAt: Record<string, number>;   // 各玩家 CD 解锁时刻
  endsAt: number;                       // 对局截止（active 时有效）
  questions: PkQuestion[];              // 公开版：answer 与未揭示的私有判定全部剥离
  aiTopic?: string;                     // PVE 可选主题方向
  /** 裁判 AI 的最近一条盘中短评（P0-5，§9.4）；无则不下发 */
  judgeNote?: PkJudgeNote;
  winner?: string;                      // finished 时（全平不下发）
}

/** 裁判盘中短评（P0-5，§9.4）：**只读服务端已有状态**组装，不额外调模型 */
interface PkJudgeNote {
  at: number;                                    // 产生时刻
  kind: 'idle' | 'streak' | 'comp' | 'lead';     // 触发来源，前端据此配色/去重
  text: string;                                  // 已组装好的一句话（服务端产生，前端不拼）
}
```

> ★ **快照只有「公开版」一种形状，REST 与 SSE 共用**（这是 P0-4 特意选的简化）：
> 若让 `GET /state` 按请求者裁剪、而 SSE 广播公开版，两个通道就会出现**同一份 state 两种形状**，
> 前端与测试都要按来源分支——那是比「刷新后看不到自己未揭示的对错」（§1.2 已接受的代价）更贵的坑。
> 私有结果的唯一通道是 REST 响应体（`POST /answer` / `POST /comp/answer`），见 §1.2。

### 2.1 REST 端点

| 方法 | 路径 | 说明 |
|------|------|------|
| ~~POST~~ | ~~`/api/pk/auth/login`~~ | **已废弃（2026-09-18）**：原 P0 昵称模拟登录（`{ nickname }` → 服务端生成 `u-<uuid>`，`openid=mock_<userId>`）。现改为**复用统一账号体系**——PK 身份直接取 `/api/auth/me`（邮箱登录的 httpOnly cookie 会话），**不再有 PK 专属登录端点**，详见 **§14**。★ 端点与 `pk_users` 表**保留不删**（存量 `pk_matches` 历史行仍指向 `pk_users.id`），但**前端不再调用** |
| POST | `/api/pk/rooms` | `{ userId }` → `{ roomId, roomCode }`；每人同时只能有 1 个 waiting 房 |
| POST | `/api/pk/rooms/join` | `{ roomCode, userId }` → `{ roomId }`；满员/不存在 → 404/409 |
| POST | `/api/pk/rooms/:id/start` | `{ userId }`（房主）→ 双方在线即 `active`，`endsAt = now + MATCH_MS` |
| POST | `/api/pk/rooms/:id/quiz` | `{ userId, prompt }` → 服务端调 AI 出 **1 道公共选择题**（**双方都要答**）。CD 内 → 429；对局未开始/已结束 → 409。成功后出题人 +1。AI 失败可免费重试（不计 CD） |
| POST | `/api/pk/rooms/:id/answer` | `{ userId, questionId, choice }` → 判**本人那一份**作答。响应体 `{ correct, delta, score, revealed }` 是**本人即时反馈的唯一通道**（§1.2）。已答过/已超时 → 409；该题没有你的作答位 → 403 |
| POST | `/api/pk/rooms/:id/comp/answer` | **P0-5（契约已立、码未开）**：`{ userId, questionId, text }` → 裁判 AI 判 0~3 分，响应体 `{ score, delta, reason, revealed }`；`text` 去空白后短于 `COMP_ANSWER_MIN` → 400（**不送判分**，堵「用一个『嗯』骗 1 分」） |
| POST | `/api/pk/rooms/:id/topic` | **P0-7**：`{ userId, topic }` → 选/改自己的对战主题（**仅 waiting 期**，`TOPIC_MAX` 20 字，trim 后空 → 400 `TOPIC_INVALID`）。开局后改 → 409 |
| POST | `/api/pk/rooms/:id/help` | **P0-7**：`{ userId, questionId }` → 用掉**唯一一次**求助道具，裁判**当场联网检索**后回 `{ advice[], knowledge, refs[] }`（**不含答案**，⑮）。次数用尽 409 `HELP_EXHAUSTED`；裁判不可用 502 `JUDGE_UNAVAILABLE` **且不扣次数**（⑱） |
| POST | `/api/pk/rooms/:id/retry` | **P0-7**：`{ userId, questionId }` → 挑一道**自己的错题**要二次机会，回 `{ explanation, question }`（相似题，同源主题）。CD 内 429 `RETRY_ON_COOLDOWN`；非错题/别人的题 → 404 `RETRY_NO_TARGET` / `RETRY_NOT_YOURS`；裁判不可用 502 `JUDGE_UNAVAILABLE` 且不计 CD（⑱） |
| GET | `/api/pk/rooms/:id/state` | 全量快照（断线重连对齐用）。**与 SSE 共用「公开版」形状**，见 §2 末注 |
| GET | `/api/pk/stream?roomId=&userId=&since=` | SSE，见 §2.2 |

> ★★ **表中所有 `{ userId }` / `userId=` 入参在 §14 落地后改为从会话取**（服务端 `req.user.id`），前端**不再显式传**——这是 §14 的**破坏性面**，受影响文件清单见 §14.4。理由：显式传 userId 等于**把身份交给客户端自证**（改个参数就冒充别人），单机 demo 期无害，一旦多用户上线就是**越权漏洞**。改走 cookie 会话后，「我是谁」由服务端说了算。

### 2.2 SSE 事件（复用 sse-bus，频道键 `pk:<roomId>`）

继承 sse-bus 全部机制（seq 单调去重 / since 增量回放 / 15s 心跳 / 断链回收），
但**与聊天会话频道严格隔离**（频道键前缀 `pk:`，不复用 sessionId 空间，防串台——v1 教训）。
新事件类型在 `packages/shared/src/sse-events.ts` 登记：

> ★★ **本通道没有「按订阅者定向」的能力**（2026-09-13 实测 `sse-bus.ts`：只有
> `subscribe(channel, res, since)` 与 `publish(channel, ev)`，载荷对频道内所有订阅者完全相同），
> 而 PK 双方订阅的就是同一个 `pk:<roomId>`。⇒ **凡进 SSE 的字段，双方都看得到**。
> 因此**私有判定结果一律走 REST 响应体**（§1.2），SSE 只承载「公开事实」。
> 改本条之前先想清楚：在广播通道上做定向，等价于把「谁能看什么」的责任交给客户端。

| type | 载荷 | 语义 |
|------|------|------|
| `pk-state` | PkRoomState（公开版） | 房间状态变化（开局/结束/玩家变动/揭示/裁判短评） |
| `pk-question` | PkQuestion（公开版） | 新**公共题**到达（双方都收；answer 与私有判定已剥离） |
| `pk-attempt` | questionId / userId / status | **P0-4 新增**：某人「已提交 / 已超时」。**公开事实，不含对错、不含所选**——双方据此知道在等谁（§1.1） |
| `pk-verdict` | questionId / answerRevealed / attempts[] | **语义改（P0-4）**：该题**已揭示**，此时才公开答案与双方结果。<br>~~原「某题判分结果，双方都收」~~ 的单方判分不再走这里——那会泄露答案（§1.2），改走 REST 响应体 |
| `pk-judge` | PkJudgeNote | **P0-5**：裁判 AI 盘中短评（只读已有状态，不额外调模型） |
| `pk-end` | winner / finalState | 结算 |

服务端定时器（对局时钟、答题超时、怠慢惩罚、CD 解锁）到期时**主动 publish 对应事件**，
不依赖客户端轮询。Android 微信 X5 内核对 EventSource 支持不稳，前端加**轮询兜底**
（SSE 断开 3 次重连失败后降级为每 2s GET state）。

## 3. 出题管道（复用现有能力，加法改造）

- `prompt` 作为 topic/material 传入现有 `learning/quiz.ts → generateQuiz`，外加**PK 系统约束层**：
  ① 只出 **1 道四选一选择题**；② 与提示词主题相关；③ 输出严格 JSON（沿用 `quiz-json-repair.ts` 兜底 + 解析失败重试 1 次，再失败 502 不计 CD）。
- **提示词注入风险**（已知并接受）：出题人可写诱导性提示词。缓解：系统约束层不可被覆盖 +
  输出 JSON schema 校验（stem/options/answer 字段齐全、answer ∈ 选项）。
  ★ **P0-4 起「出题人自己不答这道题」这条缓解手段已删除**（决策 ⑦）：公共题下出题人**也要答自己的题**。
  替代性论证——**动机方向依然天然正确**：出题人拿分只有两条路，答对对手的题（+2）与答对自己的题（+1），
  而「出简单题」对自己是**净亏**（题简单 ⇒ 对手也答得对 ⇒ 对手 +2，而自己只多拿 +1 的答题分）。
  真正的自利方向仍是**出得准**。故不需靠「不让他答」来防。
- P0 用**每次请求现出**（复用 llm/router 已配置模型），不预生成题库。
- **联网检索（2026-09-13，契约 `docs/QUIZ-SEARCH-SPEC.md`）**：PK 出题调
  `generateQuiz(topic, undefined, PK_QUIZ_MIX, undefined, undefined, online=true)`——**硬编码联网**，
  与人人对战、题库存量出题同口径。PK 入口不面向用户、**不传搜索报告**（不记账），
  检索失败照常降级出模型知识出题（ADR-4，不影响计分）。

## 4. 存储（P0 刻意从简）

- **P0 房间状态全内存**（模块级 `Map<roomId, Room>`）：局域网单进程 demo 足够，无战绩落库、
  无 schema 改动；进程重启即丢局（可接受，已在企划声明）。
- 房间 TTL：`finished`/`waiting` 超 30 分钟回收；对局结束快照保留 10 分钟供双方回看，之后删除。
- P1 才引入 `pk_rooms`/`pk_questions`/`pk_results` 表（better-sqlite3）与战绩排行。
- ★ **`pk_users`（v10）废弃但保留**（2026-09-18，见 §14）：身份改由统一账号体系（`users`）承担，`pk_users` **不再写入**。**不删表**的理由：存量 `pk_matches.user_id` 指向 `pk_users.id`，删表会让历史战绩变成悬空引用；而 `pk_matches` 行内已冗余 `opponent_nickname` 快照，**历史行本身仍自洽可读**（同 v15 的既有取向：快照冗余换查询简单）。

## 5. 前端（web 包，移动优先）

- 新增 `/pk` 路由，移动优先布局（≥375px 视口适配），桌面浏览器同套页面可玩（局域网 demo 不挑设备）。
  **实现口径（2026-09-12）**：零依赖 hash 路由（`#/pk`，`main.tsx` 按 hash 二选一渲染），**不引 react-router**——`@sb/web` 保持零运行时依赖，契约只要求「有独立 /pk 入口」，实现方式从简。
- 三个页面：**大厅**（~~昵称→登录~~ → **已登录态下直接建房 / 输码 / 点邀请链接入房**，见 §14）→ **对局**（双栏比分条 + 自己的「出题/答题」双动作区 +
  对局时钟 + CD 倒计时，全部读服务端时间）→ **结算**（比分、正确率、胜负、回看题目）。
- ~~登录按钮即替换 `App.tsx:189-198` 现有「微信登录」展示占位（该占位本就为此预留），登录态存
  localStorage（userId/nickname），失效自动重登。~~ → **2026-09-18 改判（§14.1）**：PK **不再自持登录态**，改为**复用侧栏 `AccountBox` 的邮箱账号**（httpOnly cookie 会话，JS 读不到）。`usePkIdentity` 从「localStorage 存 userId → `GET /api/pk/auth/me` 校验」改为「`GET /api/auth/me` 取当前用户」。★ **未登录时大厅展示「去登录」引导**（指向账号框），**不再有 PK 专属登录表单**。
- ~~**文案修正（2026-09-12 老板拍板）**：P0-1 落地后占位已替换为真组件，但标签沿用了占位文案「微信登录」而表单只收昵称——affordance 说谎，现改「**昵称登录**」并在 `title` 注明「P0 模拟登录；真微信授权在 P1 接入」。~~ → **已被 §14.1 整体取代**（昵称登录这个形态本身消失）。
- UI 规范照旧：极简浅色、禁 emoji、SVG line-icon；**这是正式功能不是教学 demo，不做装饰性特效**。

## 6. 分阶段计划（一批一主题，不一把梭）

| 阶段 | 内容 | 验收 |
|------|------|------|
| P0-1 | shared 登记 `pk.ts` 类型 + `/api/pk/auth/login`（模拟）+ 建房/入房/start + SSE `pk:` 频道 | 单测：建房/入房/满员/隔离；curl 双端登录互通 |
| P0-2 | 出题端点（CD+AI 管道）+ 答题端点（判分+超时定时器+怠慢惩罚）+ 对局时钟与结算 | 单测覆盖计分全表（§1 每行一条）+ 超时/怠慢用 fake timer |
| P0-3 | 前端三页面 + App.tsx 占位接入 + 轮询兜底 | **真机端到端**：两台手机同 WiFi 微信内打开，完整打完一局（QUIZ-IMAGE-SPEC 教训：单测全绿≠落地，必须真机跑通） |
| **P0-4** | **公共题**（决策 ⑦⑧⑨）：`PkQuestion` 改「双方各一份作答」+ 计分加「答对自己题 +1」+ 揭示时序（双方答完才揭晓）+ 私有反馈改走 REST 回执 | 单测覆盖**新计分表每行一条** + **揭示时序**（先答方的结果与答案不泄露给后答方）+ 双方各答一份互不覆盖 + 超时/怠慢仍用 fake timer |
| **P0-5** | **理解题 + 裁判 AI**（决策 ⑩⑪）：新题种 `comprehension` + `COMP_INTERVAL_MS`（3 分钟）调度 + `judge` 模型角色 + 0~3 档判分 rubric + 盘中短评 | 单测：调度节拍（假时钟）/ 0~3 四档 rubric 边界 / 作文字数下限 / 判分失败降级不阻断（ADR-4）；**真机核判分稳定性**（同一答案两次判分是否同档） |
| **P0-6** | 前端补齐：公共题双作答区（我的题也要答）+ 理解题文本框 + 裁判短评条 + 回看视图按 `attempts` 重画 | **真机端到端**：两台设备打完一局（含理解题），逐项核对计分与揭示时序 |
| **P1** | 公网部署（HTTPS 域名）→ **邮箱登录替换昵称模拟**（§14.1）→ **邀请链接**（§14.2）→ 战绩落库/排行 → 客观题种拓展（判断题等，仍在「秒判零争议」范围内） | 真机端到端：两台设备各用**邮箱账号**登录，经**邀请链接**入房、完整打完一局 |

> **进度对账（2026-09-13）**：P0-1 ✅ 2026-09-12 完成（登录 + 房间 + SSE 频道）。P0-3a ✅ 2026-09-12 完成
> （`#/pk` 路由 + 大厅/等待/对局实时视图，SSE 活水 + 轮询兜底）。**P0-2 ✅ 2026-09-13 完成**
> （计分全表 + 出题/答题端点 + ticker 时间驱动；测试 `routes/pk-match.test.ts` 15 例 + `routes/pk-pve.test.ts` 7 例，
> 全量 47 文件 624 例全绿）。**PVE ✅ 同批完成**（§8）。**P0-2 验收按契约 §6 是「计分全表每行一测 + fake timer」——已完成**；
> **P0-3 收尾欠真机验收**：两台手机同 WiFi 微信内完整打完一局的目检（人机与人人两条路径），待老板有空跑。
> P1 未开工。
>
> **2026-09-13 二次 grill 追加（本页 v0.3.0 的由来）**：**P0-4（公共题）在途**；
> **P0-5／P0-6（理解题 + 裁判 AI）契约已立、码未开**；P0-3 的真机验收欠账**仍未还**。
> ★ **P0-4 是破坏性改造，不是加法**：`PkQuestion` 去掉 `toUserId` / `chosen` / `status` 三个字段
> （换成 `attempts[]`，因一道题有两份作答），`POST /answer` 的语义从「答题方专属」变成「判本人那一份」，
> `pk-verdict` 从「单方判分」变成「揭示事件」。前端 `pk-view.ts` 的 `myPendingQuestion` /
> `pendingToOpponent` 与 `PkMatch.tsx` 的答题区、`PkRoom.tsx` 的回看视图**全部因此改写**。
> 受影响测试：`routes/pk-match.test.ts`（15 例）、`routes/pk-pve.test.ts`（7 例）、`features/pk/pk-view.test.ts`（16 例）。

## 7. 可行性与难度评估（老板自定义问题，2026-09-09 答）

**结论：能行。P0 难度 = 中低（2~3 个工作阶段可落地），P1 难度 = 中。**

- **为什么不难**：出题（quiz.ts+json-repair）、实时推送（sse-bus）、判分（选择题字符串比对）、
  存储（内存 Map）四块全是**复用现有已验证能力**，PK 本身只是新增一个状态机（房间→对局→结算）+
  一组薄路由 + 三个页面。没有需要攻关的新技术。
- **真正的硬点按序**：① **P1 微信授权资质**——个人主体申请不到已认证服务号，需借用主体或走
  微信开放平台（这是企划能否走到「真微信」的瓶颈，代码反而不是）；② **P1 公网部署**——HTTPS+
  域名是微信网页授权的前置条件，内网穿透只够开发调试；③ **微信内置浏览器兼容**——X5 内核 SSE
  不稳（已备轮询兜底）；④ 提示词注入（§3 已缓解，朋友间对战剩余风险可接受）。
- **玩法可行性自检**：计分闭环无死锁（双方 CD 内仍可答题，怠慢惩罚防摆烂，超时惩罚防拖局）；
  8 分钟局 + 60s 出题 CD ≈ 每人最多出 7 题，节奏成立。

---

## 8. PVE（人机对战，2026-09-13 与 P0-2 一体化落地）

老板 2026-09-12 拍板三口子：**与 P0-2 一体做**／**AI 与人完全同口径答题**／**人机对称出题**。
本节是 PVE 的契约条文；实现落点 `pk/ai-bot.ts`（对手）+ `pk/room.ts`（占座）+ `pk/match.ts`（共用计分）。

### 8.1 建房与占座

- `POST /rooms` 收 `mode: 'pvp' | 'pve'`（缺省 `pvp`）与可选 `aiTopic`（主题方向，截 50 字）。
- `mode='pve'` 时**建房即占掉第二个座位**，AI 座位 id = `ai-<roomId>`（`AI_USER_PREFIX`）——
  此后人再输码入房被「满员」挡下（409），不会挤进人机局；房主无需等人、**直接开局**
  （`startRoom` 时置 `aiNextQuizAt = now + AI_FIRST_QUIZ_DELAY_MS`）。
- AI 在 `players[]` 里就是一名**普通玩家**（同 score/correct/answered/lastQuizAt 字段），
  前端据 `isAiUserId()` 区分展示图标，**计分侧零特例**。

### 8.2 AI 出题（`runAiQuiz`）

- **触发**：`tickMatches` 的 1s ticker 见 `mode==='pve' && !aiBusy && now >= aiNextQuizAt` 即调，调前置 `aiBusy` 防重入。
- **时机**：开局后 **`AI_FIRST_QUIZ_DELAY_MS`（5s）** 出第一题；成功后与真人**同一条 CD**（`QUIZ_CD_MS` 60s）；
  失败不扣分、**`AI_RETRY_DELAY_MS`（10s）** 后重试（`scheduleNextAiQuiz(room, ok, now)` 二选一）。
- **主题**：建房指定 `aiTopic` 优先；否则 10 主题池轮换（科学常识/世界历史/地理/文学名著/信息技术/
  数学基础/生物/天文/语言文字/生活百科，起始下标随机）。
- **出题**：复用 `generateQuiz(topic, undefined, PK_QUIZ_MIX, ..., online=true)`（`PK_QUIZ_MIX` = 仅 1 道单选），
  取 `type==='single'` 的那道，走**与真人同一入口** `pushGeneratedQuestion`（成功 +1）。
  LLM 在途期间对局可能已结束——`room.status !== 'active'` 时不再收题。

### 8.3 AI 答题（`runAiAnswer`）

- **触发**：真人出题成功后调用（`submitQuiz` 内）。
- **无泄漏**：AI 走**独立 LLM 调用**（`routeRole('solver')`），上下文**只喂题干 + 选项**、答案不在其中——
  与真人拿到的信息面完全相同。
- **判分**：走**与真人同一入口** `submitAnswer`——答对 +2 / 答错 −1 / 超时 −1 全同人。
- **AI 无特权**（三条如实兜底，都是「宁可判罚也不作弊」）：
  1. 流式超过 `q.deadlineAt` 即放弃 → 交给 ticker 按超时判罚，**不占时限便宜**；
  2. `parseChoice` 解析不出选项（优先首个 A-D 字母，其次 0-3 数字）返回 `null` → **不乱猜**；
  3. `routeRole('solver')` 没配模型 → 直接不作答 → ticker 按超时判罚，**不静默造分**。

### 8.4 计分对称性（本节的唯一保证）

出题（+1、60s CD、失败免费重试）与答题（+2/−1/超时 −1）两条路径，AI 与真人**共用 `match.ts` 同一套导出函数**，
`pk/ai-bot.ts` **没有任何自己的计分逻辑**——这是「同口径」在代码层唯一的可验证保证，
也是回归锁 `routes/pk-pve.test.ts`（7 例）的断言对象。**P0-4 起此保证扩到三层**：AI 也要答**自己出的题**（§8.5），
走同一条 `submitAnswer`。

### 8.5 公共题下的 PVE 残留风险（P0-4 记录在案，**已知并接受**）

改动后 AI 出题 → AI 也要答自己的题（+1）。这条新增了 PVE 特有的不对称：

- 现有机制能挡住**直接读答案**（`runAiAnswer` 走独立 LLM 调用、只喂题干+选项，§8.3）；
- 但挡不住**「同一模型刚生成过这道题」的记忆效应**——AI 答自己出的题的正确率会显著高于它答人出的题。

**接受理由**：① 值 1 分，不改变"难倒对手更划算"的动机方向；② 不让 AI 答自己的题会让「公共题」
在人机局里名不副实（一边是公共题、一边是专属题，比多给 AI 1 分更糟）。
**若真机实测发现 AI 靠这条稳定刷分**（如连续 3 局 AI 自答题全对而人出题答题正确率明显更低），
再按「AI 对自己出的题不倒扣、但答对只 +0」处理——**留一个可调的档位，不现在发明规则**。

---

## 9. 理解题与裁判 AI（P0-5：契约已立、码未开）

### 9.1 「实时监测」落地成什么（决策 ⑪）

老板原话：「加一个裁判 ai，这个 ai 实时监测对战情况，然后负责理解题出题和打分」。
按字面做成「每秒观察」既是纯烧钱、也无实事可做，故定死为**三件实活**：

| 职责 | 落地 | 调模型？ |
|------|------|---------|
| **出理解题** | 每 `COMP_INTERVAL_MS`（180s）按本局主题出 1 道 | ✅ 一次 |
| **判分** | 收卷后按 0~3 档 rubric 打分并给出理由（理由下发到屏上） | ✅ 每份作答一次 |
| **盘中短评** | 关键事件推一条一句话短评（§9.4） | ❌ **只读已有状态，零额度成本** |

- **模型档位**：新增 `ModelRole = 'judge'`（`MODEL_ROLES` 第 6 项，label「裁判（理解题判分）」）。
  成本低——`MODEL_ROLES` 是数组驱动的（默认绑定 INSERT 与设置页渲染都遍历它），**不需要写迁移**。
  出题与判分**共用同一角色**：裁判的两种能力是一件事的两面（都要读懂题目与作答）。
- **主题来源**：PVE 取建房 `aiTopic`，否则沿用 §8.2 的 10 主题池（与本局 AI 出题同一个池）；
  PVP **没有建房主题** ⇒ 取本局**已出过题目的提示词**拼成主题摘要；一题都没有（开局 3 分钟内双方都没出题）
  ⇒ 退化为通用主题（「本局暂无主题，请就『学习方法与思维』出题」）。
  ★ 退化路径必须写死：**不能因为没主题就不出题**——那等于让摆烂方顺带取消了理解题。

### 9.2 出题节拍（收口在 `tickMatches`，不新起定时器）

- 起点 `room.startedAt`；首道在 `startedAt + COMP_INTERVAL_MS` 出。**不是开局就出**——开局 3 分钟内双方还在
  互出选择题，塞一道大题会打断节奏。
- `status==='active' && now >= compNextAt` ⇒ 出题，然后 `compNextAt += COMP_INTERVAL_MS`。
- **8 分钟局 ⇒ 第 3、6 分钟各一道**，理解分区间 0~6 分，足以改变胜负（决策 ⑩）。
- 出题失败（模型不可用/输出不合法）⇒ 不阻断对局（ADR-4）、`compNextAt` 照推不重试，
  但**屏上要如实说明**「本次理解题没出成」（ADR-5）——否则玩家以为裁判没上班。

### 9.3 0~3 档 rubric（判分唯一口径）

| 档 | 判定 | 说明 |
|----|------|------|
| **0** | 理解方向都不对 | 答非所问、概念用反、与题目无关 |
| **1** | 方向对但有偏差 | 抓到方向，但有事实性错误 / 关键概念张冠李戴 |
| **2** | 总体准确 | 无明显错误，说得对——但本质是**标准答案的复述** |
| **3** | **总体准确 ＋ 有原创性** | 满足 2 档的全部条件，**且**有自己的例子 / 类比 / 迁移 / 反例 |

- **3 档是「2 档 ∧ 原创性」的合取**，不是「比 2 档更详细」。提示词里必须写死
  「内容更详细但仍是复述 ⇒ 仍判 2 分」，否则模型会把「话多」当「原创」。
- **判分顺序三步**：先判方向（0 vs ≥1）→ 再判准确性（1 vs ≥2）→ 最后判原创性（2 vs 3）。
  写成三步而不是四个并列描述——并列描述在模型侧容易退化成「看心情给分」。
- 输出协议 `{"score":0|1|2|3,"reason":"…"}`（解析沿用 `parseQuizBlock` 的五级阶梯思路）；
  **score 越界/非整数 ⇒ 整份判分作废**，按 0 分记账 + 如实告知 + 记 `report.failure`。
  **宁可判低不判高**：裁判给错分比不给分更伤信任。
- **已知风险：LLM 主观打分不稳定**（同一答案两次可能不同档）。缓解：`temperature` 压到出题档水平（≤0.4）
  + 提示词给每档正反例。**本契约不承诺「同一答案永远同档」**——那做不到；只承诺「档位定义可解释、
  理由必须给」。真机验稳定性的方法写在 §6 P0-5 验收栏。

### 9.4 盘中短评（`PkJudgeNote`，四类，不按固定节拍刷屏）

| kind | 触发 | 文案模板 |
|------|------|---------|
| `idle` | 距上次成功出题 ≥ `IDLE_PENALTY_MS`（与怠慢惩罚**同一锚点**） | 「XX 已经 {n} 分钟没出题了」 |
| `streak` | 连错 ≥ 3 题 | 「XX 连续答错 {n} 题，建议换个思路」 |
| `comp` | 理解题判分完成 | 「理解题判分：XX {score}/3」+ 理由摘要 |
| `lead` | 分差 ≥ 5 且领先方易主 | 「XX 领先 {n} 分」 |

- **不调模型**：文案在服务端拼好（`text` 是成品，**前端不拼**），零额度成本、零延迟。
- **同类去重**：同一 `kind` 60s 内不重复推；快照里只留**最近一条**（`judgeNote` 是单值不是数组）。
- 短评**不是计分依据**，只是可读性补充——不看也不影响胜负。

### 9.5 理解题的作答与判分失败降级

- 自由文本，服务端截断到 `COMP_ANSWER_MAX`。
- **字数下限 `COMP_ANSWER_MIN`**：去空白后不足 ⇒ 400 且**不送判分**（堵「用一个『嗯』骗 1 分」）。
  为什么不给 0 分而是 400：0 分是「答了但不对」，不该与「没答」混为一谈。
- **理解题也是公共题**（双方各答一份）⇒ 完全复用 §1.1/§1.2 的作答与揭示机制，**不另写一套**。
  这正是 **P0-4 必须先做、顺序不能反**的原因。
- **判分失败**（模型不可用/输出不合法）：该份作答按 0 分记账并如实告知，
  **不阻断对局、不重试**（ADR-4 + 不静默）。
- **AI 也答理解题**（§8.4 同口径），走 `routeRole('solver')`，只喂题干、**不喂判分 rubric**
  （喂了等于告诉它评分标准＝作弊）；同样经裁判判 0~3 分进 AI 的 `score`。

---

## 10. P0-4 破坏性影响清单（**改码前先过一遍，防漏改**）

`PkQuestion` 去掉 `toUserId` / `chosen` / `status`、换成 `attempts[]`，波及面比"加一道题种"大得多：

| 受影响 | 具体 |
|--------|------|
| `shared/src/pk.ts` | `PkQuestion` 三字段移除 + `kind` / `attempts` / `revealed` 新增；`PkAttempt` / `PkJudgeNote` 新增；新错误码（无你的作答位 / 作文字数不足 / 已揭示不可再提交 / 裁判不可用） |
| `server/src/pk/room.ts` | `PkRoomQuestion` 内部结构同步（内部多存 `answer`，对外由 `snapshotQuestion` 剥） |
| `server/src/pk/match.ts` | `pushGeneratedQuestion`（建双方作答位）、`submitAnswer`（判**本人那份** + 判定是否该揭示）、`tickMatches`（两份作答各自超时）、`settleRoom`（**无改动**——理解分已并入 `score`，这是决策 ⑩ 的红利） |
| `server/src/pk/ai-bot.ts` | AI 出题后**也要答自己的题**（新增一条调用）；AI 答人出的题不变 |
| `server/src/routes/pk.ts` | `/answer` 响应体扩为含 `revealed`；`pk-verdict` 只在**揭示**时发；新增 `pk-attempt` |
| `web/src/features/pk/pk-view.ts` | `myPendingQuestion` / `pendingToOpponent` 按 `attempts` 重写；新增「我该答的题」＝所有 `attempts` 里我那份仍 `pending` 的题（**含我自己出的题**） |
| `web/src/features/pk/PkMatch.tsx` | 答题区从「对手发给我的题」改成「所有我还没答的题」；出题区加「你出的题你也要答」提示 |
| `web/src/features/pk/PkRoom.tsx` | 回看视图按 `attempts` 画双方作答 |
| 测试 3 个文件 | `routes/pk-match.test.ts`（15）、`routes/pk-pve.test.ts`（7）、`features/pk/pk-view.test.ts`（16）——**共 38 例全部要按新结构改**，不是新增 |
| 文档 | `docs/dev/test-plan.md` §3 例数 + 本文件 + `CHANGELOG.md` + `AGENTS.md` 目录表（同批刷新） |

---

## 11. P0-7 主题轮转 / 跑题校验 / 求助道具 / 二次机会（2026-09-13 老板点单，已落地）

### 11.1 主题轮转（⑬）

一局有**两个主题**：房主一个、对手一个，**按出题轮次交替**——每一轮只能用当轮主题的题。

| 字段 | 位置 | 语义 |
|------|------|------|
| `topic` | `PkPlayer` | 该玩家自己选的主题（≤ `TOPIC_MAX` 20 字，trim 后空 → 400 `TOPIC_INVALID`） |
| `currentTopic` | `PkRoomState` | **本轮**生效的主题；出题人必须出这个主题的题 |
| `topicOwnerId` | `PkRoomState` | 本轮主题归谁（前端据此显示「本轮：张三的历史」） |
| `topicTurn` | `PkRoomState` | 轮次序号，从 0 起；成功出 1 题 +1 |

- **选主题只在 waiting 期**（`POST /rooms/:id/topic`，开局后改 → 409）。建房时房主可顺带
  `POST /rooms { topic }`，对手入房后自行补选；**PVE 的 AI 座位没有主题 ⇒ 兜底 `PK_DEFAULT_AI_TOPIC`（`'通用知识'`）**。
- **双方都选完才能开局**：任一玩家 `topic` 为空 ⇒ `start` 返 409 `TOPIC_NOT_SET`。
  这一条曾经一次打红 23 例既有测试（三个测试的建房 helper 没带主题），**修法是给 helper 补主题，不是放宽规则**——
  放宽等于把「两人各选一个主题」这条需求作废。
- **轮转时机**：**成功**出题后 `advanceTopic()` 切到对手主题；出题失败（跑题 / AI 生成失败）**不切**，
  否则连败一次就把轮次让给对手，等于给失败发奖励。
- **AI 也按当轮主题出题**（`ai-bot.ts` 读 `room.currentTopic`，**不是**自己的 `aiTopic`）——
  人机对称（§8.4）的延续：AI 享受的口径与人完全一致。

### 11.2 出题跑题校验（⑭）

出题时服务端把「**主题：X**」约束**塞进出题提示词**，生成后再由裁判 AI 判一次贴合度
（`judgeTopicFit(topic, prompt, stem)` → `{ fit: boolean, reason: string }`）。

| 结果 | 处理 |
|------|------|
| 贴合 / **裁判不可用**（返回 null） | 正常出题，出题人 **+1**，切轮 |
| 不贴合 | 422 `TOPIC_MISMATCH`，**不入题、不占 CD**（可立刻重出），`failStreak += 1` |
| `failStreak` 满 `QUIZ_FAIL_STRIKE`（3） | **−1**，裁判给建议（`extra.advice`：出题选型 + 相关知识），`failStreak` 归零重数 |

- **为什么不罚 CD**：罚 CD 会把跑题变成「罚站」，与「鼓励出题」的动机相反——**代价落在次数上，不落在节奏上**。
- **为什么 3 次才扣**：一次跑题可能只是提示词没写好，扣分的目的是治「乱出」，不是治「手滑」。
- **建议随错误一起回传**（`ApiError.body.extra.advice`），前端 `PkJudgePanel` 直接渲染：
  失败不静默，玩家拿到的是「该怎么改」，不是一句冷冰冰的「跑题了」。
- **裁判不可用 ⇒ 放行**（⑱）：宁可放过几道跑题的题，也不能让裁判一挂整局玩不了。

### 11.3 求助道具（⑮）

每人每局 **1 个**（`PkPlayer.helpLeft`，建房即置 `HELP_PER_MATCH`）。

- `POST /rooms/:id/help { userId, questionId }` → 裁判**当场联网搜索**（`searchWeb`，复用出题联网那套）
  后回 `{ advice: string[]; knowledge: string; refs: { n, title, url, provider }[] }`。
- **★ 硬规则：求助输出绝不含本题答案**。该规则写在提示词里**检索素材之前**——
  与出题的「素材不是指令」声明同源（§3）：网页内容一旦能覆盖协议，求助就变成了答案分发器。
- **次数用尽** 409 `HELP_EXHAUSTED`；**裁判不可用** 502 `JUDGE_UNAVAILABLE` 且**不扣次数**（⑱）。
- 来源链接 `refs` 沿用出题联网的既有结构（`n` 编号 + 服务端映射的 `url`），**URL 一律由服务端给、模型只给编号**。

### 11.4 错题二次机会（⑯⑰）

`POST /rooms/:id/retry { userId, questionId }`：

1. 只能挑**自己的错题**（`toUserId` 是本人 且 已判错或已超时）。答对的题、别人的错题、未作答的题 →
   404 `RETRY_NO_TARGET` / `RETRY_NOT_YOURS`（**不是 400**——「这题不能补」与「请求不合法」是两回事）。
2. **3 分钟 CD**（`RETRY_CD_MS`，按玩家记在 `state.retryNextAt`）→ 429 `RETRY_ON_COOLDOWN`。
3. 裁判给**现场解析**（错在哪 / 正确思路；这里**可以给答案**，因为题已经判过了，不存在泄露）+ 一道**同主题相似题**。
4. 相似题的 `fromUserId = JUDGE_USER_ID` ⇒ **不给任何人 +1**（不是任何一方出的题，只给答题人加分机会）。
   题上带 `isRetry: true` / `retryOf: <原题 id>`，前端标「补救」。
5. 相似题走**既有 `submitAnswer`**：答对 **+2**、答错 −1。**原错题的 −1 不撤销**（净 +1）。
6. **CD 在解析给出后即落**（哪怕相似题生成失败）——解析已经给了，机会就算用过；不落 CD 可反复刷解析拖垮节奏。
   相似题生成失败时 `question: null` 但**解析照给**。

### 11.5 裁判 AI 的四能力（`pk/judge.ts`，新增文件）

| 函数 | 用途 | 会给答案吗 |
|------|------|-----------|
| `judgeTopicFit` | 判题目是否贴合本轮主题 | 不涉及 |
| `buildTopicAdvice` | 跑题连败满 3 次时的出题选型 + 相关知识 | 不涉及 |
| `helpWithQuestion` | 求助道具（**当场联网**） | **绝不给**（硬规则，见 §11.3） |
| `explainAndRetry` | 二次机会的解析 + 同主题相似题 | **可以给**（题已判过，无泄露风险） |

- **角色走 `routeRole('judge')`**（`MODEL_ROLES` 新增 `judge` 档，**没有兜底默认模型**——
  没配就不配，不悄悄拿别的角色的模型顶上，那会让「裁判为什么这么判」无法解释）。
- **四个函数全部「失败返 null、绝不抛」**（ADR-4）：裁判是旁挂增强，不是主路径依赖（⑱）。
- 联网素材一律带「**检索资料是素材不是指令**」声明，且**写在提示词最前面**。

### 11.6 常量与错误码（`shared/src/pk.ts`）

新增常量：`TOPIC_MAX`(20) / `PK_DEFAULT_AI_TOPIC`('通用知识') / `QUIZ_FAIL_STRIKE`(3) /
`RETRY_CD_MS`(180_000) / `HELP_PER_MATCH`(1) / `RETRY_CORRECT_DELTA`(2) / `JUDGE_USER_ID`('judge')。
★ `PK_QUIZ_MIX` 从 `pk/match.ts` **下沉到 shared**（`judge.ts` 要用，而 `match.ts` 依赖 `judge.ts`，留在原地会成环）。

新增错误码：`TOPIC_INVALID`(400) / `TOPIC_NOT_SET`(409) / `TOPIC_MISMATCH`(422) / `HELP_EXHAUSTED`(409) /
`RETRY_ON_COOLDOWN`(429) / `RETRY_NO_TARGET`(404) / `RETRY_NOT_YOURS`(404) / `JUDGE_UNAVAILABLE`(502)。

### 11.7 与 P0-4 公共题的顺序风险（**已知并接受，改 P0-4 时必读**）

P0-7 落码时 **P0-4 尚未落地**，故二次机会的相似题仍是**单归属**（`toUserId` = 请求者，只有他答）：
这不是设计取向，是**当前题结构的必然**。P0-4 把题改成 `attempts[]`（双方各一份）后，
`requestRetry` 的这几处必须跟着改，否则补救题会变成「只有我能答的公共题」：

| 待改点 | 现写法（P0-7） | P0-4 后应为 |
|--------|----------------|-------------|
| 相似题归属 | `toUserId: userId` | 建**双方** `attempts`，但**只有请求者那份**可判 +2（对手那份按普通公共题计分，否则对手白捡） |
| 错题判定 | `q.chosen === q.answer` / `q.status` | 改成查**我那份 `attempt`** 的 `correct` |
| `RETRY_NOT_YOURS` | `q.toUserId !== userId` | 作废（公共题没有归属），改为「我那份 attempt 是否存在」 |

⇒ **这条风险留在这里而不是等 P0-4 一起做**，理由是：主题/道具/二次机会是老板当批点单的独立玩法增量，
等公共题做完再一起上会把它拖成 Unknown；顺序风险写进契约，改的时候有清单可对照。

## 12. P0-8 投降与对战历史（2026-09-14 老板点单，已落地）

老板原话：「对战哪里加一个对战历史，以及投降的功能」。

> ★ 本节**部分推翻了决策记录 ⑤** 里那句「P0 无战绩持久化」（P0 原把战绩落库划给 P1）。
>   「对战历史」的语义就是**重启后还能查**，所以这批提前吃掉了 P1 的一个切片，
>   但范围卡得很死：**只落「已结束的一局」**——房间状态机仍是全内存（`room.ts` 口径不变，
>   进程重启仍丢进行中的局，不做断点续局）。
>   一句话：**房间是过程**（进程内活着就够）**，历史是结果**（重启后还得能查）。

### 12.1 投降（`forfeit`）

- 端点：`POST /api/pk/rooms/:id/forfeit`，体 `{ userId }` → `{ state }`。
- 判定：**对手直接胜**（PVE 则 AI 座位胜），**比分定格**——不额外扣分、也不退回分。
  ★ **分更高的一方投降同样判负**：投降是认输，不是比分比较（用例 `分更高的一方投降也判负` 钉死）。
- 守卫逐条：不在房内 403 `NOT_A_PLAYER`；非 `active`（waiting / 已结束）409 `ROOM_NOT_ACTIVE`；
  未知 userId 401（不进域层）；房不存在 404 `ROOM_NOT_FOUND`。
- **收尾路径与时钟归零结算共用** `finishRoom`（新 `pk/settle.ts`）：置 `finished` → 定胜者 →
  记 `endReason` → 广播 `pk-end` → 落历史。两种结束方式只差「谁赢」与「为什么结束」，
  分开写就会出现「投降忘了广播」「投降不落历史」这类**只在一半代码里成立**的规则。
- `PkRoomState.endReason?: 'forfeit'` —— **`timeup` 刻意不写**（「时钟归零」本来就是老字段的语义，
  不必再标一遍）；前端据此把结果说成「对方认输」/「自己认输」，只写「对局结束」看不出是谁点了投降。
- **服务端不加二次确认门**：该端点是幂等的状态转换（`active`→`finished`），重复调用只会撞 409。
  两段确认放在交互层（`PkForfeit`）——「**确认门过严 ＝ 用户以为功能不存在**」是本仓既有教训。

### 12.2 对战历史（`history`）

- 落库：**v15 迁移**建 `pk_matches`（**P0 里第一处让对局落库**）。
- **视角行**（每人一行，PVP 两行 / PVE 一行）而不是「一局一行 + 两个玩家字段」：
  历史永远是**按人查**的（`WHERE user_id=? ORDER BY ended_at DESC`），视角行让这条查询直接走索引、
  前端也不必自己算「我是哪一侧」。代价是 PVP 一局两行、快照 JSON 各存一份（一局几 KB）——
  与 `quiz_notes` 冗余 `quiz_title` 同一取向：**快照冗余换查询简单**。
- **只给真人写行**：`ai-<roomId>` 永远不会登录来查战绩，给它写行只是往库里塞孤儿数据。
- **幂等由库保证**：`UNIQUE(room_id, user_id)` + `INSERT OR IGNORE`（不靠调用方记得只调一次）。
- `opponent_nickname` 存**快照**、不设外键：对手改名后历史仍显示当时对战的名字。
- 保留量 `PK_HISTORY_KEEP`(100)（超出即删最旧，与房间 TTL 同一条「防无界增长」）；
  ★ **硬约束：保留量 ≥ 单页上限 `PK_HISTORY_MAX`(100)**，否则列表永远填不满；要抬就两个一起抬。
- 端点：
  | 端点 | 入参 | 出参 |
  |------|------|------|
  | `GET /api/pk/matches` | `userId` + `limit?`（归一：缺省 20 / 负数钳 1 / 超上限钳 100） | `{ matches: PkMatchRecord[] }` |
  | `GET /api/pk/matches/:id` | `userId` | `{ match: PkMatchDetail }`（含末快照，供题目回看） |
  ★ **查不到与「不是你的」同为 404 `MATCH_NOT_FOUND`**：分开（403/404）等于把「这个 id 存在」
  告诉一个没权限的人——别人的对局是否存在，不关你的事。
- 落库失败**不阻断收尾**（ADR-4 旁挂不拖垮主路径）：`recordMatch` 吞异常但**必须 `console.error` 留痕**
  （ADR-5：静默失败会让「历史少了几局」变成查不出来的悬案）。
- 快照 JSON 解析失败**不兜底造假**：宁可 500，也不返回一份「没有题目的空局」——
  坏数据装成正常的一局，比报错更难查。

### 12.3 附带的显示修正（同批，不是新功能）

`PkRoom` 的结算回看与历史回看共用新组件 `PkReviewList`，判词收口到纯函数 `reviewVerdict`。
★ **修掉的真 bug**：对局在时钟归零那一刻可能还有题**没答也没判超时**，此时 `chosen` 与
`answerRevealed` **双双 undefined**，旧写法 `chosen === answerRevealed` 恰好为 true ⇒
屏幕上把「还没答」显示成「**答对 +2**」。现在显式返回「未作答」。
（不落 `bug-ledger`：该账本只记**反复/修不动**的 bug，本处一次读码即定位并修掉，收敛计数 1。）

### 12.4 文件落点与门禁

| 层 | 落点 |
|----|------|
| 契约 | `shared/src/pk.ts`：`PkEndReason` / `PkOutcome` / `PkMatchRecord` / `PkMatchDetail` + `PK_HISTORY_LIMIT/MAX/KEEP` + `PkRoomState.endReason` + 错误码 `MATCH_NOT_FOUND`。**纯加法**（唯一的既有字段改动是加可选的 `endReason`） |
| 域 | 新 `pk/settle.ts`（`finishRoom` / `settleRoom` / `forfeitRoom`）＋ 新 `pk/history.ts`（`recordMatch` / `listMatches` / `getMatchDetail` / `clampHistoryLimit` / `resetMatches`）；`pk/match.ts` 的 `settleRoom` **搬进 settle.ts**（该文件当时 389/400 已贴顶，只有 11 行余量） |
| 存储 | `storage/migrations.ts` **v15**：建 `pk_matches` + `idx_pk_matches_user`（索引与建表同批，v2 铁律） |
| 路由 | `routes/pk.ts`：`/forfeit` + `/matches` 两端点（薄路由只做校验与错误码映射，业务规则一律不在这一层） |
| 前端 | 新 `PkForfeit` / `PkHistory` / `PkReviewList` / `usePkIdentity`；`pk-view.ts` 加 6 个纯函数（`reviewVerdict` / `outcomeLabel` / `reasonLabel` / `myOutcome` / `finishTitle` / `formatEndedAt`）；`PkApp` 加视图切换（**历史与房间互斥、房间优先**） |

★ 本批有**三处主动拆文件**（不是顺手，是门禁逼出来的，也是本仓「贴顶就拆、别等撞线」的延续）：
① `pk/settle.ts` ← `match.ts` 389/400；② `PkForfeit` / `PkHistory` / `PkReviewList` ←
`PkMatch.tsx` 276/300、`PkRoom.tsx`（且回看块两页共用，各写一份必然有一天只改一边）；
③ `usePkIdentity.ts` ← `PkApp.tsx` 一度到 **296/300**，抽出登录态恢复后回落 **277**。

⚠️ **与 P0-4 的顺序风险**：本节不触碰题结构（不依赖 `toUserId`/`chosen` 的归属语义），
故 §11.7 那三条待改点**不因本批增加**——但 `reviewVerdict` 读的是 `chosen`/`answerRevealed`，
P0-4 把题改成 `attempts[]` 时它也要跟着改（与 §11.7 同批处理）。

---

## 13. UX 全面增强（2026-09-15 老板点单，已落地）

老板原话一条（本节需求的唯一出处）：「对战那里的排版太挤了,这些按键都挤在一起,然后题目对错的动画反馈太少,
并且还有出题之前的过渡也没有做,就是在玩家看来,对面出题就是突然题目出现了,所以我建议如果对方提示词写好了,
ai出题的间隔就可以出一个提示了,就是对方正在ai出题的动画提示,然后对局胜利失败,回答正确等事件的 ui,动画效果
都作对太少了,这次的目的就是全面加强对战的用户使用体验」。

四条诉求 → 13.1~13.3 三处落点（诉求②④合并为「事件动画」）。**本轮无新玩法、无计分变更**，
纯呈现层 + 一个契约字段；§1 计分规则、§8.4 计分对称性**一字未动**。

### 13.1 诉求③：出题过渡（本批唯一新增的**契约字段**）

**症状（老板实测）**：「对面出题就是突然题目出现了」。
**根因**：`submitQuiz` 落 CD 后立即 `publishState`，随后 `await generateQuiz`（**数秒**）——
这段时间答题方**拿不到任何信号**，题目只能凭空出现。★ 纯前端补不了：**它根本不知道对方在出题**，
所以必须有服务端字段（这是本批唯一无法在前端独立完成的诉求）。

**落点**：`PkRoomState.quizPending?: { userId, at }`（见 §2 契约，纯加法，老客户端忽略即降级为旧行为）。

- **为什么放房间状态、而不是单发一条 SSE 事件**：事件是瞬时的、**刷新就丢**；状态是持久的，
  重连拿快照就知道「对手还在出题」。本条与 §2.2「SSE 只播公开事实」并列，不冲突。
- **为什么可以走 SSE（决策 ⑫ 的边界）**：`quizPending` 是**公开事实**（与比分同类），
  不属 ⑫ 所限制的「私有判定结果」。★ **只带「谁在出」+「何时开始」，绝不预带题目内容**。
- **三条出口都必须清**：跑题失败（`TOPIC_MISMATCH`）／生成失败／生成成功。
  漏一条对手就**永远**看到「正在出题」——**比当初干脆不给信号更糟**（他会以为卡死了）。
  ★ 清理必须在 `pushGeneratedQuestion` **之前**：它成功后会立刻 `publishState`，
  清晚了对手收到的就是「题目已到 + 我还在出题」这种**自相矛盾**的状态。
- **PVE 同口径**：`runAiQuiz` 也置位（人机对齐是本仓既有口径，§8.4 的对称性思想同样适用于提示）；
  `status !== 'active'` 时也要清——别把「AI 正在出题」带进已结束的快照（**它还会进历史 JSON**）。
- 前端 `PkQuizPending` 只做**呼吸点 + 骨架屏 + 「已过 Ns」**，**不做假进度条**：
  真的不知道还要几秒，画一根会走完的进度条等于撒谎（走完了题没来，玩家直接判定卡死）。

### 13.2 诉求①：排版（横屏对阵）

老板拍板「**把他改成横屏设计**」（**推开**了给出的三个选项）。
★ 落点是**响应式**而不是锁死横屏：横屏左右对阵（`对手 | 主区 | 我`），竖屏回落上下排列，
并给一条**轻提示**「横屏体验更佳（竖屏也能玩）」——本仓既有教训：**拿提示挡住功能**是错的。
容器 `max-width` 480 → **920**：三栏了还按 480 排必然更挤，**加宽与分栏是同一条改动**，不能只做一半。
另修两处真「挤」：选项间距 `--sb-sp-2`(8px) → `--sb-sp-3`(12px)；触控目标
`.sb-pk-option` ≥48px、`.sb-pk-btn`／`.sb-pk-input` ≥44px（移动端可点性，不是审美）。
★ **中央区只放「当前唯一要做的事」**（答题 / 出题过渡 / 出题入口，**三选一互斥**）；
「出题入口」降级进底部折叠区但**永不消失**——两件事本可并行，只是不该同屏占满。

### 13.3 诉求②④：事件动画（老板选「分层」）

原则：**只在真事件上动，不搞常驻装饰**（常驻动画＝噪音，且会与真正该被看见的事件抢注意力）。
- **判分**：`PkVerdict`（自绘 SVG line-icon）替代原来一行 `flash` 文字，四态
  `correct/wrong/timeout/info`（`info` 专供错误原因／「已出类似题」——红叉配它不合适），2.5s 后自隐。
- **结算**：`PkResult` **结果面板弹入**（老板选此项），胜负徽章（win 自绘奖杯）+ 双方终分 +
  胜者高亮 + 题目回看默认收起；面板内元素**递增 `animation-delay`**（0.1s→0.44s）形成入场节奏。
- 6 个手写 keyframes（`sb-pop`／`sb-shake`／`sb-breathe`／`sb-sheen`／`sb-rise`／`sb-num`），
  **零第三方库**（本仓前端禁用外部动画库）；文件末尾 `@media (prefers-reduced-motion: reduce)` **全关**。

### 13.4 纯函数与测试（§3 登记）

`pk-view.ts` 新增 3 个纯函数（`.tsx` 无测试环境 ⇒ 判定逻辑一律下沉，先例 `doc-name.ts`）：
`quizPendingSec`（向下取整、钳 0 不出负数）、`quizPendingLabel`（**自己出题时刻意返 null**——
按钮本身就在转圈，再插一条「你正在出题」是刷存在感）、`verdictKind`（**与 `reviewVerdict` 同口径**：
未作答**不算答对**——P0-8 修过的老坑不重蹈）。
测试 **+6 例** → `pk-view.test.ts` **28 → 34**；全仓 **67 文件 / 850 例（849 passed + 1 skipped）**。

★ 本批**当场兑现了一次「双真相源必配漂移锁」**：`quizPendingLabel` 返回 `text`，而组件当时**自己又拼了一遍**
「正在出题」——同一句话写两处，组件拼的是 `AI正在出题`、测试锁的是 `AI 正在出题`，两边不一致但只有一边在跑。
修法不是改断言迁就，而是**让组件直接渲染 `text`、拼装只留一处**，并顺手补上中英混排空格
（`AI 正在出题` vs `对手正在出题`）。

### 13.5 文件落点与门禁

| 层 | 落点 |
|---|---|
| 契约 | `shared/src/pk.ts`：新增 `PkQuizPending` + `PkRoomState.quizPending?`（纯加法） |
| 服务端 | `pk/room.ts` 新增 `setQuizPending`／`clearQuizPending`；`pk/match.ts` 三处出口；`pk/ai-bot.ts` PVE 同口径 |
| 前端 | 新 `PkQuizPending`／`PkVerdict`／`PkArena`／`PkResult`／`PkAnswerBlock`／`PkQuizBlock`；重写 `PkMatch.tsx`；`PkRoom.tsx` 结算块外移；`pk.css` 追加 UX 样式与动画段 |

★ **两处主动收口**（同 §12.4 的规矩：贴顶就拆、别等撞线）：
① `setQuizPending`／`clearQuizPending` 放 `room.ts` 而**不是** `match.ts`——`match.ts` 本批前已
**394/400**，两个小函数放它那儿必撞线；② `PkAnswerBlock`／`PkQuizBlock` 从 `PkMatch.tsx` 拆出。
行数实测：`match.ts` **394/400**、`room.ts` **395/400**、`PkMatch.tsx` **272/300**。

⚠️ **诚实记账**：`.tsx` 无测试环境 ⇒ 横屏对阵观感、出题过渡动画、对错/胜负动画、结算弹入
**均属体验类症状**，须老板**实机双签**；自动化只覆盖 13.4 那 3 个纯函数（6 例）。
真机冒烟（`smoke-ux.mjs`，工作区工具不入仓）覆盖的是**协议层**：出题期间 `quizPending` 下发、
出题人正确、结束后**清除**、题目入列且**不含 `answer`**（决策 ⑫ 不外泄私有判定的回归）。

## 14. 邮箱登录与邀请链接（2026-09-18 老板点单，定案待实现）

老板原话：「**pk就邮箱登录，然后通过邀请连接邀请**」。

⇒ 两条改动：**① PK 身份并入统一账号体系**（废弃 P0 的昵称模拟登录）；**② 新增邀请链接**。

### 14.1 PK 身份并入统一账号体系

**为什么必须改**（不只是「老板想要」）：P0 的身份模型是**客户端自证**——前端把 `userId` 存 localStorage、每个端点显式传 `{ userId }`。单机 demo 期无害，但一旦多用户上线，**改一个参数就能冒充别人**（§2.1 末注已把这条钉住）。统一到邮箱登录后，「我是谁」由服务端 cookie 会话说了算。

| 项 | 改前（P0） | 改后（§14） |
|---|---|---|
| 身份来源 | `pk_users` 表，`openid = mock_<userId>` | **统一 `users` 表**（`AUTH-SPEC §1`，邮箱账号） |
| 登录方式 | 昵称（无密码） | **邮箱 + 密码 / 验证码**（`AUTH-SPEC §2` / `§2.5`） |
| 登录态载体 | localStorage 存 `userId` | **httpOnly cookie 会话**（JS 读不到） |
| 服务端取身份 | 请求体 `{ userId }` | **`req.user.id`**（`requireAuth` 中间件注入） |
| 昵称 | `pk_users.nickname` | **`users.nickname`**（注册时从邮箱派生，可在账号设置改） |
| 前端入口 | `PkLogin` 昵称表单 | **复用侧栏 `AccountBox`**，PK 内不再有登录表单 |

★ **合并成本比预想低**：`pk_users.id` 当初就用 `u-<uuid>` 前缀（v18 迁移注释写明「与 `users.id` 同前缀，便于合并身份时对账」）——**这条预留正好用上**。

★ **`pk_users` 保留不删**（理由见 §4）：存量 `pk_matches.user_id` 指向它，删表会让历史战绩变成悬空引用。

⚠️ **历史战绩的归属断层（诚实记账）**：`pk_matches` 里已有的行，其 `user_id` 是 `pk_users.id`，而新身份是 `users.id` ⇒ **同一自然人在库里是两个 id**。本批**不做自动认领**（同 `TENANCY-SPEC §3` 的取向：隐式认领在并发下归属不确定且无法撤销）。后果：**老战绩在新账号下查不到**（等于归档）。若要保留，走显式认领脚本（照 `_probe/claim-legacy.mjs` 的手法）。

### 14.2 邀请链接

★ **不新造邀请机制**：链接里带的就是**既有 `roomCode`**（`room.ts` 的 `genCode()`，6 位数字，`PK_ROOM_CODE_LEN = 6`）。

**链接格式**：

```
https://<域名>/#/pk?code=<6位房间码>
```

★ **必须带 `#`**：本仓前端是**零依赖 hash 路由**（§5，`#/pk`），路径式 `/pk?code=` 会 404。
★ **`?code=` 放在 hash 之后**：`#/pk?code=123456` 的 query 属于 **hash 片段**，前端必须从 `location.hash` 里解析，**不能**用 `location.search`（那是 `#` 之前的部分，取不到）。

**流程**：

1. A 建房（`POST /api/pk/rooms` 已返回 `roomCode`）→ 前端拼链接 → A 复制/分享（最可能经微信发出）
2. B 打开链接 → 前端从 hash 解析 `code`
3. B **未登录** → 引导登录（**登录后必须跳回原链接**，见 §14.3）→ 登录成功 → 自动 `POST /api/pk/rooms/join { roomCode }`
4. B **已登录** → 直接入房
5. 入房失败（房间已过期 / 已满 / 已开局）→ **明确提示，不静默吞掉**

**为什么不用新端点**：`roomCode` 建房时就返回了，前端拼字符串即可 ⇒ **零后端改动**（§0.7 简洁优先）。将来若要短链或带邀请人标识，再另立批次。

⚠️ **有效期即房间 TTL（2026-09-18 拍板「跟房间走」）**：房间 30 分钟无活动即被 `sweepExpired` 回收（`PK_ROOM_TTL_MS`），**链接随之失效**。⇒ 前端在入房失败时必须能区分「房间不存在」与「房间已过期」，**文案要能自解释**（「邀请已过期，请让好友重新建房」优于笼统的「房间不存在」）。

### 14.3 登录后跳回（本节的隐藏依赖）

★ 邀请链接的体验成败**不在链接本身，在「未登录时点链接」这一步**——若登录后落到首页，用户得**再点一次链接**，邀请链路等于断了一半。

⇒ 需要 `returnTo`：未登录访问受保护路由时，前端记住目标（**存内存或 sessionStorage，不放 localStorage**——跨会话残留会让下次登录莫名跳到旧链接），登录成功后跳回。

⚠️ **安全约束（P1，开放重定向）**：`returnTo` **只接受站内相对路径**，**必须拒绝绝对 URL**（`https://evil.com`）——否则是个**开放重定向**漏洞（钓鱼链接可借本站登录页跳去外站）。判定口径：**只允许 `#/` 开头**，其余一律丢弃回落默认页。

### 14.4 破坏性面（改码前先过一遍，同 §10 的规矩）

| 面 | 改动 | 影响 |
|---|---|---|
| `pk/auth.ts` | 整体废弃（不再写入 `pk_users`） | 其单测作废或改写 |
| `routes/pk.ts` | 所有端点从 `{ userId }` 改取 `req.user.id`；挂 `requireAuth` | **全部 PK 路由** |
| `shared/src/pk.ts` | `PkIdentity.openid` 失去意义（微信 openid 已不做） | 类型收敛 |
| `features/pk/usePkIdentity.ts` | 改读 `GET /api/auth/me` | 前端身份来源换根 |
| `PkLogin` 组件 | 删除，改「去登录」引导 | 大厅入口改版 |
| `pk-view.ts` / `PkMatch.tsx` / `PkRoom.tsx` | 入参去掉 userId | 前端全链路 |
| 受影响测试 | `routes/pk-*.test.ts`、`features/pk/*.test.ts` | 需逐条改写（**不是新增，是改写**） |

★ **这是 P1 里最大的一次改造**，比 P0-4（公共题）影响面更广——P0-4 改的是**题目模型**，这次改的是**身份模型**。

### 14.5 未定与未验（诚实记账）

- ⬜ **未实现**：以上全部是契约，**一行代码未写**。
- ⚠️ **未验**：多设备真机走通「A 发链接 → B 未登录点开 → 登录 → 自动入房」整条链，**必须真机跑**（`.tsx` 无测试环境，同 §13 的既有缺口）。
- ⚠️ **需真机确认**：**微信内置浏览器对 `#` 的处理**——部分 WebView 在分享/跳转时会截断 hash，若被截断则 `code` 丢失。这是邀请链接最常见的失效方式，**上线前必须在微信内实测一次**。
- ❓ **待老板定（安全，我判风险中低但不为零）**：房间码是 **6 位数字**且**当前没有任何试码限流** ⇒ 10^6 空间可被脚本枚举、闯进别人的对战。缓解的代价很小（加一条试码频率限制即可）。★ 我判中低的依据：PK 房间无敏感数据、TTL 仅 30 分钟、闯入者也只能答题；但**邀请链接会把码暴露在聊天记录里**，暴露面确实比「口头报码」大。**建议加限流**，等老板点头。

---

## 15. 对战玩法扩展（2026-09-20 老板点单）

老板原话：「studentbuddy 的对战功能很久没有更新了，我打算更新一下，因为**我们的项目已经上线了，并且不再是完全的本地项目**，所以我们的**双人对战也要能够实际使用**，然后现在**情景题出得不错，所以我认为情景题可以加进对战中**，然后再是**词条功能也要加进对战中**，具体的加入方式就是**在出题的时候，用户可以从自己的词条库中选择合适的词条来出题**，然后**除了选择题还可以加一个判断题**，**在出题设置中以及对战中都可以加判断题**」。

拆成四条：① PVP 真能用（→ **§14** 承载，本节只标顺序）；② 情景题进对战；③ 词条进对战；④ 判断题（设置页 + 对战）。本节管 ②③④，① 直接落 §14 的既有契约。

### 15.1 老板拍板（2026-09-20，四问四答）

| # | 问题 | 拍板 | 落点 |
|---|---|---|---|
| **D1** | 这批先落哪头 | **先身份改造**（§14），玩法改动排后 | §14 → §15，两批不交叉（都改 `routes/pk.ts`，并行必冲突） |
| **D2** | 对战「出什么题型」怎么定 | **出题时现选**（不动设置页配比语义） | §15.5 |
| **D3** | 情景题折算成分数 | **整页算一题**（评分点全中 +2，有错 −1） | §15.4 |
| **D4** | 词条是硬约束还是软素材 | **可选**（老板补充：「不然新用户都没有词条可以选」）→ 不选＝与今天逐字一致；选了即硬绑定 | §15.3 |

★ D4 那个补充是关键约束：**词条不能做成必填**。新账号词条库是空的，必填等于把新用户挡在出题功能外——这是「确认门过严＝用户以为功能不存在」的同类病（本仓已吃过一次亏，见 `AGENTS.md` 产品取向）。

### 15.2 现状（实测行号，2026-09-20）

| 面 | 现状 | 依据 |
|---|---|---|
| 对战题型 | **写死一道单选**：`PK_QUIZ_MIX = { single: 1, 其余 0 }` | `shared/src/pk.ts:58` |
| 判分链路 | 只认 `options[] + chosen: number`（下标） | `match.ts:70-76`（`answerIndex`）、`match.ts:280-284`（`submitAnswer`） |
| 题目形状 | `PkQuestion` 只有 `stem/options/chosen`，**无题型字段** | `shared/src/pk.ts:139-166` |
| 情景题 | 配比为 0（关着）；形状是「整套 HTML demo + N 个评分点」 | `pk.ts:58`、`shared/src/scenario.ts:47` |
| 词条 | 词条库 CRUD 齐全且 owner 隔离，但 PK 出题的素材位恒空 | `learning/terms.ts`、`match.ts:162`（`generateQuiz(prompt, **undefined**, …)`） |
| 判断题 | **已在 P1 规划里**：「客观题种拓展（判断题等，仍在秒判零争议范围内）」 | 本文档 §6 分阶段表 P1 行 |

### 15.3 判断题：独立档位 `judge`（D2 的题型基础）

**工程判断（决定成本的关键一条）**：判断题＝二选一（对/错），`options` 恒两项、`answer` 是下标 ⇒ **对战判分链路一行不用改**（`answerIndex` / `options.length >= 2` / `chosen: number` 全部原生成立）。真正的工作量只在**加第 5 档**。

**为什么不复用 `single`**（三条，缺一条就不成立）：① 设置页要能独立配「判断题 3 道」；② 出题提示词要给它不同的格式约束（判断题干必须是可判真伪的陈述句、选项恒两项由系统补、不得自造选项）；③ 错题本/统计要能分辨题型。

**契约**：

```ts
// shared/src/content-blocks.ts
export type QuizType = 'single' | 'multiple' | 'fill' | 'essay' | 'judge';
export const QUIZ_TYPES = ['single', 'multiple', 'fill', 'essay', 'judge'] as const;
export const QUIZ_TYPE_LABELS = { …, judge: '判断题' };

/** 判断题选项恒此两项（**服务端补齐**，不信模型自造） */
export const JUDGE_OPTIONS = ['正确', '错误'] as const;
```

★ **选项恒定性是硬约束**：模型给的判断题 `options` 一律**丢弃并由服务端置为 `JUDGE_OPTIONS`**——让模型自造选项就会出现「对/错」「正确/不正确」「√/×」三种写法在同一套题里混排，而 `answer` 是下标，选项文本一漂移，前端渲染与错题回看就对不上。

★ **判定口径**：`answer` 为 `[0]`（正确）或 `[1]`（错误）。`normalize` 阶段同 `single` 一样校验「答案下标在 options 范围内」。

**打穿清单（6 处硬编码五档字面量 + 3 处清单派生点，逐处必改，漏一处就是运行时 `undefined`）**：

| 文件:行 | 现写法 | 改后 |
|---|---|---|
| `shared/src/content-blocks.ts:66` | `QUIZ_TYPES` 四类 | 加 `'judge'` |
| `shared/src/content-blocks.ts:100` | `DEFAULT_QUIZ_MIX` 五档 | 加 `judge: 0`（默认不出，老用户零变化） |
| `shared/src/pk.ts:58` | `PK_QUIZ_MIX` 五档 | 加 `judge: 0`（对战改由入参决定，见 §15.5） |
| `shared/src/quiz-source.ts:32` | `DEFAULT_QUIZ_SOURCE_MIX` 五档 | 加 `judge: 0`（网上真题可以摘判断题，但默认不出） |
| `server/src/learning/quiz.ts:233` | `actual: QuizMix` 五档 | 加 `judge: 0` |
| `server/src/learning/quiz-blend.ts:75` | 零值 mix 五档 | 加 `judge: 0` |
| `server/src/learning/quiz-source-mix.ts:20` | `ZERO_AI_MIX` 五档 | 加 `judge: 0` |
| `server/src/routes/quiz.ts:107` | `zeros` 四档（仅传统题） | 加 `judge: 0` |
| `server/src/learning/quiz.ts:46`（提示词） | `[QUIZ]` 格式示例 | 加一条 `judge` 示例 + 规则行 |

**测试影响面（6 个文件含五档字面量，需逐条确认零回归，红则为真回归不得改测试迁就）**：
`shared/quiz-mix.test.ts`、`shared/quiz-source.test.ts`、`server/learning/quiz.test.ts`、`server/routes/quiz-mix.test.ts`、`server/routes/settings-tenancy.test.ts`、`web/lib/api.test.ts`。

**前端两处**：
- 设置页 `QuizMixCard.tsx` 走 `MIX_KINDS.map`（`MIX_KINDS = [...QUIZ_TYPES, 'scenario']` 自动带出），**零改动**——这是当初把档位清单抽成常量的回报；
- 答题渲染 `PkAnswerBlock`：判断题**不显示 A/B 字母**，改「✓ 正确 / ✗ 错误」两个大按钮（`optionLetter(i)` 那种字母排布用在判断题上是错的产品形态）。★ 但不新造题型适配层：按 `question.type === 'judge'` 分支渲染，选项文本仍取 `options[]`。

### 15.4 情景题进对战（D3：整页算一题）

**形状冲突（本批最大的一块）**：`PkQuestion` 是「一道题 = 题干 + 选项 + 交出下标」，情景题是「一套 = 一个可玩 HTML demo + N 个评分点，对错由服务端按 `criteria` 判」。二者**不能靠加字段硬塞**。

**契约（`shared/src/pk.ts`，纯加法）**：

```ts
/** 对战题目种类：客观题（现有链路） / 情景题（iframe demo） */
export type PkQuestionKind = 'quiz' | 'scenario';

export interface PkQuestion {
  …现有字段…
  /** 缺省 `'quiz'`：历史题与老快照无此键 ⇒ 前端按客观题渲染（不做数据迁移） */
  kind?: PkQuestionKind;
  /** kind='scenario' 时才有：demo 地址 + 评分点清单（**不含 criteria**） */
  scenario?: {
    demoId: string;
    title: string;
    /** 仅 prompt/hint —— 与「正确答案永不下发」同一条硬约束：criteria 绝不出现在任何载荷里 */
    tasks: { id: string; prompt: string; hint?: string }[];
  };
  /** 判定后回填：各评分点是否命中（与 answerRevealed 同一条「判定后才下发」纪律） */
  taskResults?: Record<string, boolean>;
}
```

★★ **`criteria` 永不下发**：这是 §1「正确答案不下发」在情景题上的同一句话。情景题的 criteria 就是答案，一旦随快照出去，`postMessage` 回传与「谁真的操作对了」就全可伪造。⇒ 下发形状里**结构性地没有 criteria 这个键**，不给手滑留口子（与 `pending` 阶段连 `answerRevealed` 键都没有同理）。

**通道**：**零新造**——iframe 沙箱 + `SCENARIO_BRIDGE_JS` + `SCENARIO_REPORT_TYPE` 回传全部沿用 `shared/src/scenario.ts` 既有实现；服务端判分复用 `judgeTask(criteria, observed)` 纯函数。

**计分（D3 落点）**：判定时刻统一结算 → 评分点**全中** `+2`，**有错**（含未上报）`−1`。与既有「一道题」同口径，**不新增计分档位**。

**三条既有机制对情景题的处置（逐条定死，不留模糊）**：

| 机制 | 处置 | 理由 |
|---|---|---|
| 答题超时 | 到点整页结算：未上报的评分点算错 ⇒ 有错 ⇒ `−1` | 与「每题 45s 超时 −1」同一条规则，不新造 |
| 二次机会 | **不参与**（候选过滤掉 `kind='scenario'`） | 二次机会要「现场再出一道类似题」，情景题那是一次整页 demo 生成——「贵一个量级」（`MAX_SCENARIO_PER_MIX = 3` 就是为此），拿它当补救手段会把 `RETRY_CD_MS` 的 3 分钟变成 3 分钟的模型满负荷 |
| 求助道具 | **沿用**（裁判给「这套 demo 的目标与思路」，**不给答案**） | 道具语义是「给思路」不是「替玩」，与 demo 内操作不冲突 |
| 跑题裁判 | **沿用**（情景题同样要贴当前轮次主题） | 主题轮转是出题侧约束，与题型无关 |

**成本与风险记账（不得当已验收）**：
1. ⚠️ **8 分钟一局能否生成并玩完一套 demo，未验**。出题方要等一次整页生成（数十秒级），答题方要真的在 demo 里操作完成 N 个评分点。**这条决定功能是否成立**，必须真机实测（同 §13 的既有缺口：`.tsx` 无测试环境）。
2. ⚠️ **iframe 在移动端的可用性未验**：demo 是整页交互，375px 宽下的操作区域、滚动嵌套、微信内置浏览器的 iframe 策略都要实测——`OPEN-QUESTIONS` 口径同 `SCENARIO-SPEC` 的既有欠账。
3. **出题方等于是「拿自己的 60s CD 换一次数秒到数十秒的生成」**：CD 与失败回滚沿用既有逻辑（失败免费重试），不因题型加长。

### 15.5 对战题型现选（D2 落点）

**白名单（不是全题型开放）**：

```ts
/** 对战可选的题型：**只收秒判零争议的**（fill/essay 无法机器判分，情景题单列走 kind） */
export const PK_QUIZ_TYPES: readonly QuizType[] = ['single', 'judge'];
```

★ 为什么 `fill`/`essay` **永远不进对战白名单**：对战计分是即时判分（答对 +2/答错 −1），而填空题的等价写法（「浮力」「浮力大小」）、解答题的评分要点，机器判都做不到零争议。这不是「本批不做」，是**结构性不适用**——写进契约以免将来有人「顺手补上」。

**入参**：`submitQuiz(roomId, userId, prompt, quizType?, …)` 尾参追加；`routes/pk.ts` 的 `/quiz` 增 `quizType`（缺省 `'single'` ⇒ 老前端零改动，**向后兼容锁**）。

**`PK_QUIZ_MIX` 保留不删**：`pk/judge.ts` 出「二次机会的类似题」时仍用它，且它被 `match` 依赖——配比单一事实源不能有第二份（`pk.ts:54` 的原注释已说明这层）。

### 15.6 词条出题（D4：可选 + 选了即硬绑定）

**流程**：出题区加词条选择器（读自己的词条库，**可多选，可不选**）→ 选中即把词条约束拼进出题提示词 → 跑题判定沿用既有 `judgeTopicFit`。

**为什么沿用既有裁判而不新写一套「词条命中校验」**：选中词条后，出题提示词里的硬约束从「必须围绕主题「X」」变成「必须围绕主题「X」，且必须考察以下词条：A（释义）、B（释义）」。裁判判的是**整体贴合度**，词条要求是同一句约束的一部分。⇒ **零新增裁判逻辑**，代价是判定强度略弱于「逐词条校验命中」。

⚠️ **记账（B3 实测后回首）**：若实测出现「点名了词条但裁判仍放行」，再补专用校验（扩 `judgeTopicFit` 入参），**本批不做**（不做没有证据的防御）。

**owner 隔离是硬约束**：词条必须按 `ownerId` 取（TENANCY-SPEC），**不能取到别人的词条**——`learning/terms.ts` 的既有函数签名已带 `ownerId` 参数，照传即可。跨用户取词条在这条链路上等价于「读别人的私有资料」。

**上限**：单次最多 5 条（与 `MAX_QUIZ_REAL_PER_TYPE` 同族）。★ 词条**不占** `PK_PROMPT_MAX = 300` 的额度（那是玩家自己写的提示词上限）——词条走独立入参独立拼接，不受 300 字钳制。

**域错误码（新增）**：`TERM_NOT_FOUND`（词条 id 不存在或不属于你 → 404）、`TERM_LIMIT_EXCEEDED`（超过 5 条 → 400）。★ 两者与 `MATCH_NOT_FOUND` 同一条取向：**「不存在」与「不是你的」合成一个码**，不向无权限的人泄露「这个 id 存在」。

### 15.7 破坏性面（改码前先过一遍，同 §10 的规矩）

| 面 | 改动 | 影响 |
|---|---|---|
| `shared/src/pk.ts` | 加 `judge` 档、`PkQuestionKind`/`scenario`/`taskResults`、`PK_QUIZ_TYPES`、两枚域错误码 | 类型层（纯加法） |
| `shared/src/content-blocks.ts` | `QuizType`/`QUIZ_TYPES`/`QUIZ_TYPE_LABELS`/`DEFAULT_QUIZ_MIX` + `JUDGE_OPTIONS` | **6 处硬编码字面量全线连带**（§15.3 表） |
| `server/src/pk/room.ts` | `PkRoomQuestion.answer` 对情景题的承载（沿用 `answer` 下标 或 独立 `criteria` 字段） | 内部结构 |
| `server/src/pk/match.ts` | `pushGeneratedQuestion` 支持情景题、`submitAnswer` 分派、题型入参 | **已贴 400 行门禁**，情景题判分应另开文件 |
| `server/src/learning/quiz.ts` | 提示词加判断题段；`judge` 选项服务端补齐 | **375/400 行，仅剩 25 行** |
| `web/src/features/pk/*` | 出题区题型切换 + 词条选择器；答题区按题型/kind 分派渲染；新增情景题 iframe 宿主组件 | 组件 ≤300 行，大概率要新拆文件 |
| 受影响测试 | `routes/pk-*.test.ts`（6）、`shared/quiz-*.test.ts`（2）、`learning/quiz.test.ts`、`web/features/pk/*.test.tsx` | 需逐条改写 |

### 15.8 分阶段计划（一批一主题，不一把梭）

| 批 | 内容 | 依赖 | 可独立回滚 |
|---|---|---|---|
| **B1** | **§14 身份改造**（邮箱登录 + 邀请链接 + 登录后跳回） | 无（统一账号体系已落地） | ✅ |
| **B2** | 判断题加档（设置页 + 对战现选，§15.3 + §15.5） | 无（B1 不交叉） | ✅ |
| **B3** | 词条出题（§15.6） | B2（出题区已改过一轮，避免二次返工） | ✅ |
| **B4** | 情景题进对战（§15.4） | B2/B3（题目模型已扩展） | ✅ |

**B1 先行不是「顺手排的」**：它是 PVP 唯一的真入口——不落 §14，B2/B3/B4 做完了也**邀不到真人来打**，只能自己开两个浏览器自娱。

### 15.9 测试计划

| # | 层 | 测什么 |
|---|---|---|
| T1 | 纯函数 | `judge` 档 normalize（选项被强制置为 `JUDGE_OPTIONS`、answer 越界拒）；`normalizeQuizMix` 六档钳位；`QUIZ_TYPES` 顺序即提示词顺序 |
| T2 | server 出题 | 配比含 judge 时提示词出现判断题格式段；AI 自造 `options` 被覆盖；不含 judge 时提示词**逐字不变**（向后兼容锁） |
| T3 | server 对战 | `submitQuiz` 带 `quizType='judge'` 出的题 `options` 恒两项；缺省与改动前逐字一致；`PK_QUIZ_TYPES` 拒绝 fill/essay |
| T4 | server 词条 | 选词条后提示词含词条约束；**跨 ownerId 取不到别人的词条**（隔离回归锁）；超 5 条 → 400；词条 id 不存在 → 404 |
| T5 | server 情景题 | `criteria` **不出现在任何对外载荷**（含 SSE 帧）；全中 +2 / 有错 −1；超时未上报算错；二次机会候选过滤掉情景题 |
| T6 | 前端 | 题型切换；判断题渲染为「正确/错误」而非 A/B；词条选择器不选时请求体与改动前逐字一致 |
| T7 | 真机探针 | **B4 必跑**：两台设备打一局含情景题的完整对局，记录「生成耗时 / demo 玩完耗时 / 8 分钟内是否来得及」三个数字（§15.4 风险 1 的清偿） |

### 15.10 本次不做（显式划界）

- **不做** fill/essay 进对战（结构性不适用，见 §15.5）；
- **不做**情景题的逐评分点计分（D3 已拍板整页算一题）；
- **不做**词条的「逐词条校验命中」（§15.6 记账，等实测证据）；
- **不做**对战题目难度标注、真题进对战（`QUIZ-BLEND-SPEC` 的合流是**出题侧**能力，对战侧不接）；
- **不做**房间落库（`§4` 的划界不因本批改变；发版重启丢局仍是有意接受项，**但 B1 上线后邀请链接会让这个代价更可见**——记在 §15.11 风险）。

### 15.11 风险与对冲（诚实记账，不得当已验收）

1. ★ **B1 上线后「发版重启丢局」的可见度上升**：以前用的人少、口头报码居多；邀请链接一旦在微信里流转，用户会在「点开链接却找不到房间」时才第一次感知到 TTL 与重启的存在。**处置**：§14.2 已定文案要能自解释（「邀请已过期，请让好友重新建房」优于「房间不存在」）；
2. ★ **§14.5 那条试码限流仍未拍板**：邀请链接把 6 位房码暴露在聊天记录里（§14.5 已预警），暴露面确实变大。**建议本批一并加**（对正常用户零影响），**等老板点头**；
3. **判断题可能拉低题目质量**：二选一题有 50% 盲猜命中率，AI 也容易出「送分题」。**对冲**：提示词明确要求「判断题干必须是**可判真伪的具体陈述**，不得出'以下说法是否正确'这类空壳题」，并靠 `judge` 的跑题裁判兜底；
4. **六档以上的配比卡在 375px 宽下会更挤**（§13 的场景记忆犹新：老板实测原话「按键都挤在一起」）。**处置**：B2 落码时先把 `QuizMixCard` 的移动端布局扫一遍，不靠"应该没事"。

### 15.12 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-20 | v0.8.0 | 新增 §15。老板点单四条；四问拍板 D1~D4（先身份改造／对战题型现选／情景题整页算一题／词条可选且选了即硬绑定）；判断题落为独立档位 `judge` 并列出 6 处硬编码打穿清单；情景题 `criteria` 永不下发与三条机制处置定死；`fill`/`essay` 结构性不进对战白名单；分四批 B1~B4（B1 ＝ §14） |
