# 真人测试登记簿（manual-test）

> 版本：v0.1.7 | 状态：[活跃] | 更新：2026-09-21（**派 MT-10**：落地页新增那一节「对战时你看到的，只是两块屏中的一块」的首次真人目检——★★ 这一节的验收口径仍然是老板那句「实际效果和演示效果差别不要太大，不许货不对板」，但**本批把它从「靠自觉」变成了「靠结构」**：新节直接 `import` 产品的 `pk.css`、复用产品组件 `PkQuizPending`／`PkVerdict`，没有第二套样式可漂；★ 于是留在真人这边的只剩两件事——**「像不像两块真屏」**（叙述层面，机器写不出判据）与**换帧时还跳不跳版**（★★ jsdom 无布局：本批两条跳版 bug 全靠 CDP 量出来并已修，复测恒宽恒高，但「您还觉得跳吗」只有您答得数）。同批自查另逮到两处「产品里不可能出现」的状态并已当场改码补锁（hero 演示的「求助 2」／两块屏主题归属写成同一个词的「上帝视角」）。上一单 2026-09-21（**派 MT-09**：正式版 hero 三个演示的首次真人目检——★ 这批的验收口径是他自己定的「实际效果和演示效果差别不要太大，不许货不对板」，而**「像不像吹的」这句话机器写不出判据**；同批真机目检已逮到一条上线以来的真 bug，其观感侧也一并交他判）

> **本簿是《AI-DEVELOPMENT-GUIDE》§0.16 真人测试规范的项目载体**（细则见《计划重写文档》§10.8）。
> 与 `bug-ledger.md` **分家**：bug-ledger 记「AI 修不动的」，本簿记「**人觉得不对的**」。
> 两本簿的计数**严格分开**，混计会同时污染两个指标（§0.16 第 3 节）。
>
> ★ **派单前先读本簿**：把「第 N 次真人验收」写进真人测试单与回复首行——计数必须对 AI 可见（§10.6 规则 2 同源）。

## 字段说明（7 字段，真人验收计数为首位）

| 字段 | 说明 |
|------|------|
| **真人验收计数** | 同一**测试目标**每派一次且判「不通过/部分通过」+1；判通过即封存不再计数。**不按**改动次数、提交次数、对话轮次计 |
| 编号 | `MT-NN`，与派发时那张真人测试单的编号一致（可回溯） |
| 派发日 / 目标 / 判据 | 抄自真人测试单，保证单子与册子对得上（对不上＝派单没留痕） |
| 结果 | 通过 · 不通过 · 部分通过 |
| 体感记录 | 卡/闪/慢/糊/错字/反直觉/想点错——真人测试独有的价值，不许空 |
| 处置 | 改（第 1 次）／换方案（第 2 次）／回需求层重判或砍（第 3 次） |
| 转成的自动化锁 | 体感问题里能写出断言的部分，回填 `test-plan.md` |

## 阈值（与 §10.6 同构，语义不同）

| 真人验收计数 | 强制动作 |
|---|---|
| 1 | 正常改（人对第一次手感有意见，改了再验） |
| 2 | 可以改，但**必须换方案**——禁止同一设计的第二次微调（「再调调间距」不算换方案） |
| **3** | **强制升级**：回需求层重判——要么重做，要么按 §0.14 砍掉。**禁止第 4 次同向微调** |

## 登记条目

| 真人验收计数 | 编号 | 派发日 | 目标 | 判据 | 结果 | 体感记录 | 处置 | 转成的自动化锁 |
|---|---|---|---|---|---|---|---|---|
| 1 | MT-01 | 2026-09-18 | 题库页新增的「搜集题目」整条链路从输入主题到入库**可用且可信**（v0.2.57 首批，契约 `docs/RESOURCE-SPEC.md` §1 六步闭环的首个人工入口） | ① 预览先于入库、勾选后只入库勾过的题；② 入库的题在题库列表能认出是搜集来源，点「出处」能回到原网页；③ 没搜到/模型没配/抓页失败各有一句话说得清下一步，不白屏不静默；④ 搜集题能照常进练习与判分；⑤ 摘出来的题读起来像原题而非模型口吻（「摘录不是创作」的最终裁判） | 待验 | 待填 | — | 待回填 |
| 1 | MT-02 | 2026-09-19 | 聊天输入框一次贴多图时，**超出上限那几张的处理认不认得出来**（v0.2.69 源码四修批①：上限此前可被同一次操作突破，机制已锁，越线的反馈无人验） | ① 托盘里的图数不超过上限；② 被丢弃的图**有没有一句说得清的话**告诉用户「为什么第 5 张不在里面」；③ 带着 4 张图点发送，本轮真的把图送出去并得到与图相关的回应（不静默无反应） | 待验 | 待填（**这条是决策点**：要不要加「最多 4 张」提示，取决于他验的时候觉不觉得需要） | — | 待回填 |
| 1 | MT-03 | 2026-09-19 | PK 对战里**连着答两题时，第二题的对错反馈看不看得见**（v0.2.69 源码四修批②：旧实现第一条的计时会抹掉第二条，机制已锁，可见时长够不够没人验） | ① 连答两题后第二条反馈仍在屏上；② 停留时长够看清「答对/答错 + 分值」，不需要截图；③ 反馈消失后不误以为「刚才那题没判」 | 待验 | 待填（若答「一闪就没」＝时长本身要重定，不是再修一次串抹） | — | 待回填 |
| 1 | MT-04 | 2026-09-19 | 让模型输出一段**很长的代码**，看着它一边流一边变折叠的那一下**抖不抖**（v0.2.69 源码四修批③：折叠此前只在开头算一次，长块到 40 行也不折，机制已锁，跨过 25 行那一刻的观感没人验） | ① 折与不折的分界出现时**不出现整块重排/闪回纯文本**；② 想看全部时点「展开全部 N 行」，展开后模型还在继续输出也**不被拉回折叠**；③ 折叠状态下仍能看出这是一段代码（语言标/首几行可读） | 待验 | 待填 | — | 待回填 |
| 1 | MT-05 | 2026-09-19 | 在聊天里让 AI 替你删一批词条，从**弹出来的那张「要不要让它删」的卡**到事后把删掉的**找回来看一遍**，整条链你放不放心（P3 确认门批：两阶段写＋撤销快照机制已锁，§5.1 四硬要求的「认不认得出要删什么」、60s 过期的观感、撤销数字对不对，全没人验——这是 AI 第一次只能「用户点头之后」才能改库） | ① 卡上「做什么＋几条＋清单（≤8 行）＋拒绝后 AI 不会重复发起」同屏一次看全，清单里的词条你认得就是你要删的那批；② 点「拒绝」后 AI 只用文字给建议，**同一批不会马上再弹一张**；③ 点「本会话允许」后，同会话同类操作不再连弹，且卡上写明了授权范围（允许什么、到哪儿为止）；④ 去词条页找得到撤销入口，撤销后 flash 的还原条数与刚才删的对得上、撞名未动的有条数说明；⑤ 放着卡不理 60 秒：卡过期收起，且过期文案**不写成「你拒绝了」** | 待验 | 待填（重点记：清单行数多了/少了够不够扫完、倒计时看不看得见催不催人、按钮位置顺不顺手、「本会话允许」的行为与你读文案时的预期是否一致、flash 停留够不够看清数字） | — | 待回填 |
| 1 | MT-06 | 2026-09-19 | 去设置页把**确认门阈值**试一遍，再看**工具统计**那几张数是不是你认识的（P3 批设置面首见：坏值永不 400 的归一机制已锁，但「归成默认值」在真机是贴心还是见鬼没人验；统计数字可信度同理） | ① 把阈值改成 0：红字警告**当场**出现，且一句话讲清「0＝AI 删词条不再问你」——你读完知道这不是你想要的（或正是）；② 离开页面再回来，0 还挂着；③ 在框里敲进字母/负数/超大值，失焦后自己跳回一个数——这个行为你**接受还是觉得坏了**（这是本条的决策点）；④ 刚才聊天里真跑过几次工具之后，统计卡的分档用时与次数和你体感对得上（跑过 3 次就显示 3 次）；⑤ 断网/服务没起时打开设置页，统计处有话说得清，不白屏不转圈到死 | 待验 | 待填（重点记：阈值是打字改还是被归一值抢改、统计读起来像不像「我干过的事」、③ 的归一行为算智能还是算 bug——判定权在他） | — | 待回填 |
| 1 | MT-07 | 2026-09-20 | 对话里**发一张图**，两条路各看一眼：看得懂图时**描述对不对你的预期**；把「视觉（看图）」故意绑成一个不能看图的模型时，**弹出来的那句话你读完知不知道下一步点哪里**（v0.2.91「看图失败说人话」批：翻译器八分支已锁 16 例、库副本端到端已跑，但「人话」成不成立只有真人读一遍算数——**这条判据机器写不出来**） | ① 正常那张图能得到与图内容对得上的回应（不是「我无法看图」）；② 出错时气泡上**不是**一段英文 JSON／光秃秃的「400」，且第一句话就点名是哪个模型出的问题；③ 读完这句话，你**不用回问 AI 也不用查代码**就知道该去设置页改哪个下拉；④ 末行那句「上游原文」你觉得**有用（能贴给人问）**还是**碍眼（该收进折叠区）**——这是本条的决策点 | 待验 | 待填（重点记：① 描述读起来像不像「真看了图」；② 报错文案有没有长到你需要滚动才看完；③ 「上游原文」那行你会不会真去复制；④ 从贴图到出结果这几秒等待你知不知道发生了什么） | — | 待回填（若判「碍眼」→ 前端把原文收进 `<details>`，同批补一条渲染锁） |
| 1 | MT-08 | 2026-09-20 | 把浏览器窗口拉到**半屏高**（900–1100 宽、600–700 高），进词条库把复习计划的两个开关都打开，**在队列里真打卡一道题**——那两颗键要不要滚一下才够得着、为了够着你得先做什么（v23.2 半屏裁切批 B-017：几何已由无头浏览器 270 条断言锁死，但「滚一滚才够得着」在真机算自然还是算别扭，机器写不出这条判据） | ① 队列里的「记住了/忘了」在**不拉高窗口**的前提下点得到；② 「复习范围」的领域行同样滚一滚全点得到；③ 滚到面板任意位置后，顶部那两枚开关**还能点回来**（不跟着一起滚走）；④ 词条库本体没因这次改动变看不见——**添加框现在可能要滚才出现，请明确答「可接受／不可接受」（这是本条的决策点）**；⑤ 横向没有任何按钮被切掉半截 | 待验 | 待填（重点记：滚轮在「面板内」与「整页」之间切换时有没有一瞬不知道在滚谁、`.rv-head` 两枚开关之间那段约 137px 空白看着像不像坏了、右侧三颗键折成两行碍不碍眼、为了打卡您总共多做几个动作） | — | 待回填（几何侧已锁：探针 I6/I7/I8 与 `reachable()` 判据；观感侧若判「不可接受」→ 队列改抽屉或调低 `QUEUE_LIMIT`，另行拍板） |
| 1 | MT-09 | 2026-09-21 | 打开落地页**首屏右侧那个会自己动的窗**，从第 1 格看到它自己转完一圈再回到第 1 格——你看完之后**还信不信它演的是这个产品**（v0.2.95 hero 对战演示批：正式版第一次出现三个演示并列，① 词条旅程从五张静态卡改成会转的回路、② 新增「对战 · PK」5 帧、③ 知识图剥掉常亮激活层。★ 这批的验收口径是老板原话「实际效果和演示效果之间的区别，最好差别不要太大……不会货不对板」，而**「读起来像不像吹的」写不出断言**——机器侧只锁得住「每帧画了哪几块」） | ① 不滚动就能看见窗里有东西在动，且**页脚那行说明**每格都跟着换、没有哪一格是空白或纯静态图；② 上面那三个标签各点一次，每次都能从头看到尾，**没有任何一格让你觉得「这一格在演一个产品里根本没有的动作」**（★ 这条就是本单的判决，请逐格答「有／没有」，有就说哪一格）；③ 中间那个标签走到第 2 格时，正文正下方会出现一张**带蓝边的卡**（里面有词名、一行小字、两个可点字样）——★★ 这张卡在真机上**上线以来从未显示过**（本批目检逮到并修），请明确答「看得见／看不见」；④ 最后一个标签的五格里，出题那一格**不该出现任何进度条走到一半的假动作**，答那一格的倒计时你会不会觉得它在催你；⑤ 第一个标签那个圆环转满一圈后，环心那个数字**比上一圈多 1**；⑥ 把系统的「减弱动态效果」打开后刷新：页面**停在最后一帧**，不是继续动、也不是整块消失 | 待验 | 待填（重点记：① 环一圈约 13 秒、词条演示一轮约 14 秒——**你在第几秒想切走**；② 打字机 42ms/字是快是慢；③ 三个标签并排会不会太多、你会不会只看第一个；④ 页脚五句说明里有没有哪一句你读完觉得「产品里不是这样」——这是本单最有价值的一条；⑤ 那张蓝边卡出现的位置和正文的关系，你第一眼以为它是「弹出来的」还是「本来就在那儿的」） | — | 已回填：`demo/PkFlowDemo.test.tsx` **9 例**（帧 × 内容矩阵、出题格**禁止 progressbar 元素**、比分序列 `[3,3,3,5,5]`、帧时长 ≥ 帧内计时器耗时）＋ `app/Landing.test.tsx` 第 **6** 例（蓝边卡挂载位置结构锁）＋ 既有 `useDemoPlayer` 的 reduce 冻结锁。**待回填**：判据②④若他报「有／觉得催／没停住」，能写成断言的部分回填 `test-plan.md` §3 |
| 1 | MT-10 | 2026-09-21 | 往下滚到落地页**那一节标题写着「对战时你看到的，只是两块屏中的一块」的双屏区**，看它自己走完**两整圈**（10 次换帧）——★★ 你看完能不能说出**这两块屏为什么是两个不同的人在看**（v0.2.96 对战双屏节批：新增一整节，五帧 × 左右两块屏并排。★ 这一节与 hero 不同，它**不吃演示 CSS**——直接 import 产品的 `pk.css`、复用产品组件 `PkQuizPending`／`PkVerdict`，所以「像不像产品」在代码侧已经成立了一半；★★ **本单判的是另一半：这一节唯一在售的东西是「像不像两块真屏」，而机器结构上量不到**（jsdom 无布局：本批两条跳版真 bug 全靠 CDP 量出来，见 §4）。★ 另有一条判决权在您：**新增这一节把整页推长 1510px**（@1440 实测整页 4805px），值不值） | ① ★★ 两块屏**同一秒的剩余读数是同一个数**、**出题冷却各自算**——请您确认这两件事您**看懂了**（看懂的判据：不用回看代码或问我，就能答出「为什么左边写着 44s 右边也写着 44s」）；② ★ 走到第 01 帧时，左屏出题、右屏显示「对手正在出题」的呼吸＋骨架屏，**这一帧左右两边都不该出现任何进度条**；③ ★★ 那块「主题归属」小字：两块屏上**一块写「你的主题」、一块写「对手 的主题」**，且换帧时随角色互换而互换——★ 若您看到的是两边同一个词，那就是本批刚修掉的「上帝视角」又回来了，请指名帧号；④ 每 2.5 秒换帧时**整页有没有跳版**（左右两块的宽度变没变、下面五步清单有没有被顶上下）；★ 这是本批 CDP 量到并修掉的两条 bug（宽 438↔259／高 595↔629），修后 140 次采样恒宽、七档恒高，但**只有真人能判「还跳不跳」**；⑤ 把系统「减弱动态效果」打开后刷新：这一节应**冻结在第 01 帧的双屏**（不是消失、不是继续动），且★ 冻结后单看这一帧您能不能明白它在讲什么；⑥ ⑤ 之外请把窗口拉到 **1024／900／780／725／480** 各看一眼——★ 窄到 780 以下两块屏会**上下叠**，叠起来之后您还认为它是「两块屏」吗（这是本单最有价值的一条）；⑦ 滚动条走到这一节时您**是否会读完**那两句页脚说明，还是直接划过 | 待验 | 待填（重点记：① 这一节五帧 × 2.5 秒 ＝ 一圈 12.5 秒，**您在第几帧开始不看了**；② ★★ 两块屏并排会不会被读成「同一个界面的两个版本」而不是「两个玩家的界面」——若是，请说您以为怎么改能分开（换底色？加名牌？把「你/对手」写在屏标题上？）；③ 「已过 Ns」那个真数字会不会反而让人觉得慢；④ 这一节和首屏那个对战演示**内容重叠**，您觉得是重复讲述还是正好互补；⑤ 整页 4805px，您划到第几屏想停） | — | 已回填：`app/PkJourney.test.tsx` **8 例**（★★ 三把一致性锁——同帧两块屏剩余秒**逐字相同**（帧 1/3 × 格 0/2/4 共 9 组对照，改坏取证：一侧写成 `left - 1` 即报 `'44s'` 不为 `'45s'`）／比分只按契约涨（每圈 +3、不倒退、加分帧与 `match.ts:107`·`match.ts:275` 对得上）／主题归属行**恰好一块屏**写「你的主题」且两块屏不得同文（两块屏写成同值即红））＋「求助 N ≤ `HELP_PER_MATCH`」「出题中不得有 progressbar」「双屏整块 `aria-hidden`」。**锁不上的两类如实记**：跳版与换帧抖动（jsdom 无布局，判据④）、以及「像不像两块真屏」本身（判据①⑥⑦）——★ 这三条只能靠本单，若他报「仍跳／读成同一屏」，几何侧回 `landing.css` 的七档实测表重量，叙述侧另行拍板 |

> 版本注：本簿 v0.1.0 → **v0.1.1**（首条 MT-01 登记，2026-09-18 现场搜集批派发）→ **v0.1.2**（MT-02／03／04 三条，2026-09-19 前端组件源码四修批派发；同批不派第 4 条改动「发送/停止按钮补 `type`」——当前外层无 `form`，真机无从观测，属预防性改动）→ **v0.1.3**（MT-05／06 两条，2026-09-19 P3 确认门收口批派发）→ **v0.1.4**（MT-07 一条，2026-09-20「看图失败说人话」批派发）→ **v0.1.5**（MT-08 一条，2026-09-20 词条页「复习计划」半屏裁切批 B-017 派发）→ **v0.1.6**（MT-09 一条，2026-09-21 hero 对战演示批派发）。★ **顺带记一处本簿自身漂移（§0.11）**：下方「变更记录」表 v0.1.5 与 v0.1.4 两行**是追加序不是时序**（0.1.5 写在 0.1.4 前面），而页脚长期停在「v0.1.4」落后表头两档——本批把页脚与表头对齐为 **v0.1.6**，★ **但不回改那两行的先后**（历史登记行回标不回改）→ **v0.1.7**（MT-10 一条，2026-09-21 对战双屏节批派发；★ 本单与 MT-09 的分工：MT-09 判「像不像吹的」，MT-10 判「**像不像两块真屏**」——后者这一节已改为直接 import 产品 CSS 与产品组件，代码侧不再有第二套样式可漂，所以留在人这边的只剩**叙述与观感**，加上两条**机器结构上量不到**的跳版判据）。

---

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-09-13 | v0.1.0 | 初始创建（个人文档 §0.16 真人测试规范 [P-059] 执行入口接线批次）：字段表 7 列（真人验收计数首位）＋阈值 1/2/3＋登记条目表。此前本仓无本簿＝「派单前读计数」第一步就空转，本批补齐载体 |
| 2026-09-19 | v0.1.2 | **前端组件源码四修批派发 MT-02／03／04 三条**（附件上限的越线反馈／PK 连答两题的闪光可见性／长代码块流式折叠抖不抖）——本批四条改动里**三条是纯观感**，组件测试只能锁机制，故逐目标分派**不并成一条**（计数按目标算，并条会让「哪个又挨了一次」失去意义）。★ 第 4 条（发送/停止按钮补 `type="button"`）**不派**：当前外层无 `form`、真机无从观测，属预防性改动，写在这里当不派依据。★ MT-02 附带一个**决策点**：越线丢弃要不要显式提示「最多 4 张」——机制已修，提示语属「值不值」层，等他验完再定。★ 顺带把本簿状态从 [WIP] 转 [活跃]（在册 4 条，派发路径已跑过两次：MT-01 与本次三条）。 |
| 2026-09-19 | v0.1.3 | **P3 确认门收口批派发 MT-05／06 两条**（确认卡全链的「放不放心」／设置页阈值与统计的可信度）——触发闸门命中三条：**首次可交互**（确认卡与撤销条是这批才第一次能点）、**涉模型输出**（卡上清单来自模型提议的删除计划，单测绿≠真人认得出那些词条）、**新锁首次上线**（§5.1 四硬要求与「拒绝后不重复发起」是契约级承诺，第一次上真机）。★ 拆两条不并一条，仍按 MT-02/03/04 先例**按目标计数**：确认门与设置页是两个互不重叠的验收目标，「阈值归一改不改」不该被「卡放不放心」的结果带走。★ 两条各带一个**决策点**：MT-06③「坏值失焦后自动跳回」是贴心还是见鬼（判据写法上就没有替他下结论）；MT-05⑤「过期文案 ≠ 拒绝文案」若他读不出差别，改文案而不是再修计时。★ 同批不派：SSE `step` 帧新可选字段的旧前端兼容、迁移 v34/v35 的可逆性——真机无从观测，属机制层，已有自动化锁。 |
| 2026-09-20 | v0.1.5 | **词条页「复习计划」半屏裁切批（`bug-ledger` B-017，test-plan v0.2.92）派发 MT-08 一条**——触发闸门命中三类：**体验类 ✅**（「够不够得着／要不要多几个动作」写不出断言）、**首次可交互 ✅**（`.rv-body` 是全新的「谁负责滚」那一层，第一次由真人滚）、**新锁首次上线 ✅**（`reachable()` 判据与 I6/I7/I8 是这批新立的，机器侧全绿但从未与真人手感对过表）；★ 决策点是判据④：**添加框从「固定可见」变成「滚一滚够得着」**——这是有意的取舍（静默裁切比要滚严重得多），值不值由他判；若判不可接受 → 队列改抽屉或调低 `QUEUE_LIMIT`，属换方案不是微调。★ 不派的部分：几何本身（270 条断言 + 反向锁 16 红已覆盖）、横向裁切（I8 已锁）。
| 2026-09-20 | v0.1.4 | **「看图失败说人话」批（v0.2.91，屎山表 B-016）派发 MT-07 一条**——触发闸门命中三类：**涉模型输出**（「把 400 翻译成人话」这件事，绿单测只证明八分支按正则命中，证明不了那句话**人读了知道下一步做什么**）、**首次可交互**（新文案第一次有机会出现在真气泡上）、**真实环境**（错绑／欠费／429 全是运行态形态，而库副本上跑的是产品函数、不是界面）。★ **只派一条、不拆两条**：本批代码改动**只有一个关注点**（失败怎么说），「描述对不对」与「报错认不认」是**同一次贴图操作的两侧观察面**，拆开会把同一个动作的验收计成两个目标（与 MT-05／06 因「两个互不重叠界面」才拆两条的判据一致）。★ 单内嵌一个**决策点**（判据④）：末行「上游原文」是**有用**（能贴给人问）还是**碍眼**（该收进折叠区）——写判据时不替他下结论。★ **同批不派**：八分支翻译器本体、`vision.ts` 的 `try/catch` 接线、429／5xx／超时各分支的路由——**纯函数与协议层**，16 例已锁，真机无从额外观测。 |
| 2026-09-21 | v0.1.6 | **hero 对战演示批（test-plan v0.2.95）派发 MT-09 一条**——触发闸门命中三类：**体验类 ✅**（环一圈 13 秒会不会等、倒计时催不催人，写不出断言）、**首次可交互 ✅**（正式版第一次出现三个演示并列，第 3 个「对战 · PK」第一次有机会被真人看到；① 词条旅程也从五张静态卡第一次改成会转的回路）、**新锁首次上线 ✅**（`PkFlowDemo.test.tsx` 的「帧 × 内容矩阵」与 `Landing.test.tsx` 第 6 例「速览卡挂载位置」都是本批新立，机器侧全绿但从未与真人眼睛对过表）。★ **为什么这批必派**：老板给这条批的验收口径是**「不许货不对板」**，而「像不像吹的」是**人对一句话的判断**，不是对 DOM 的判断——机器能锁「这一帧画了哪几块」，锁不住「这五块读起来是否都产品里真有」。★★ 同一条单里还挂着一个**已修 bug 的真人复核**：蓝边速览卡上线以来从未显示过（被 `overflow:hidden` 裁切），修完只有真人答「看得见／看不见」才算闭环。★ **只派一条、不拆三条**：本批三块改动（①回路／②对战／③剥激活层）**共用同一个用户动作**＝「打开落地页看那个窗」，拆开会把一次观看计成三个验收目标（与 MT-07 的判据一致）；③ 单独不派的原因＝它只是**删掉**了演示里产品没有的动效，删完的观感已被同一次观看覆盖。★ **同批不派**：`registry.ts` 加演示的注册表接线（纯结构，`PkFlowDemo.test.tsx` 锁了顺序与字段完整性）、`demo.css` 的 keyframes 值同源（CSS 值 jsdom 读不到，但它是样式不是行为，且真机截图已逐帧核过）。 |
| 2026-09-21 | v0.1.7 | **对战双屏节批（test-plan v0.2.96）派发 MT-10 一条**——触发闸门命中三类：**体验类 ✅**（「像不像两块真屏」＋「换帧跳不跳版」写不出断言）、**首次可交互 ✅**（落地页第一次出现「两屏并排」这一节，之前只有 hero 单屏）、**★ 机器量不到的那两类 ✅**（jsdom 无布局：本批两条跳版 bug 100% 由 CDP 量出，说明这一类的判据在仪器侧是**空白**，只能由人眼看）。★ **与 MT-09 不合并的理由**（吸取 v0.1.6 那次「三个演示共用一个动作所以合成一单」的判例）：这次**用户动作不同**——MT-09 是「打开首屏看那个会自己动的窗」，MT-10 是「往下滚到中间那一双屏」，且**验收目标不同**（前者判「像不像吹的」，后者判「像不像两个不同的人在看同一场对战」），合并会把两次观看计成一个目标；★ 但两单的**判据④同源**（换帧跳版），故 MT-10 的处置列写明：他若报「仍跳」，几何侧直接回 `landing.css` 的七档实测表重量，不必重开一单。★ **同批不派**：`pk-boards.tsx` 的帧数据与 `PK_SCORE` 公式（纯结构，`PkJourney.test.tsx` 8 例锁了剩余秒一致／比分按契约涨／主题归属恰好一块屏，三把都做过改坏取证）、`Landing.test.tsx` 那四把旅程锁的**限定作用域**改动（例数不变、只是把选择器收窄，`PkJourney.test.tsx` 自有测试锁自己）、§4 那条 `sb-shake` 假引用（纯文档漂移，无运行体可验） |

---

*最后更新: 2026-09-21 · 本簿 v0.1.7 · 计数与结果的权威记录在「登记条目」表，沿革见本表与 `CHANGELOG.md`*
