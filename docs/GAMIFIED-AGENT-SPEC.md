# 游戏化学习 Agent 产品口径契约（GAMIFIED-AGENT-SPEC）

> 版本：v0.2.0 | 状态：**[活跃·口径已拍板，施工未开工]——§6 五项已于 2026-09-25 全部拍板清零（见该节记录）；本文仍是零代码、零迁移、零发版，动码从 §5 S1 批起、每批照常先改契约再动码** | 更新：2026-09-25
> 定位：把 2026-09-25 的口头口径变更钉成唯一事实源——**产品叙事从「本地优先 AI 学习助手」转向「游戏化学习的 Agent 产品」，形态与功能同步向轻量化靠拢**。参照形态＝Quizlet（轻卡片/短练习）＋多邻国（streak/成长外显），⚠️ 参照的是**形态**，不是抄功能全集，更不许对外宣称与二者有任何关联（对外口径仍受 `GROWTH-SUBMISSION-PACK.md` §4 那条「零优劣断言」划线约束）。
> 立约动机出处（如实记账，两笔都是现查）：① 老板 2026-09-25 11:3x 口述「可以继续上线吧，因为整体要向轻量化学习靠拢」（原文在 `CHANGELOG.md` 未发布表 v0.2.125 发版行）；② 同日批次 K 把**学习流／知识图整族下线**（`26f0766`，迁移 v44 DROP 七表），但 tombstone 与 CHANGELOG 记的判决理由是「与主链路联动性太低」——**「轻量化」这个动机当时没有成文**，本文补的就是这一格。

## 1. 一句话叙事（新口径）

**「一个陪你把学习变成游戏的 AI 伙伴：随时来一局（PK）、每天连着练（streak）、看得见成长（XP/段位）、内容拿得出去（分享开放）」。**

- 与旧口径的关系：README 现句「学 → 练 → 析 → 忆 → 反馈 五环闭环」**不删，降级为实现层描述**；对外一句话定位改在 §5 S1 批次里与 README／Landing 同批换，本文不落笔改它们。
- ★ 「agent」这一维**不是新造**：现役工具生态（`TOOL-ECOSYSTEM-SPEC.md` v1.4.6，10 个已实装工具）＋ AI 主动约战（`offer_pk_battle`，v0.2.124 已上线）就是它的底座。新叙事是把「AI 会自己发起学习玩法」从隐藏能力提到台面主打。

## 2. 三个词各拆成可判过/不过的口径

「轻量化／可玩性／开放性」不拆成判据就会变成三个谁都能解释的词。以下每条都能用一次操作或一条查询判定：

### 2.1 轻量化（形态）
| # | 判据 | 现状达标？ |
|---|---|---|
| L1 | 主要学习动作在 **≤30 秒**内完成一轮（一组 5 题卡、一次复习打卡、一局迷你对战） | 部分：复习队列与出题已是短流程；对战是长局 |
| L2 | 移动端（微信内 H5）任一核心玩法**不需要注册第二个账号体系**（PK 复用统一邮箱登录，`AUTH-SPEC §0.1` 口径不变） | ✅ 已成立（v0.2.94 起） |
| L3 | 每批新功能**默认不新增一级导航项**（侧栏维持六项；超出须老板点名） | ✅ 现役六项 |
| L4 | 不做需要常驻后台的重机制（实时联赛、推送服务、匹配队列跨服）——单人运维扛不动，见 [[feedback-growth-metrics-honesty]] 的诚实边界 | 本文即闸门 |

### 2.2 可玩性（功能）
| # | 判据 | 现状 |
|---|---|---|
| P1 | **每天回来有理由**：streak（连续天数）＋到期复习队列合成一个「今日目标」视图 | 复习队列有（`ReviewPanel`），**全仓无 streak**（2026-09-25 grep 实测）＝最大缺口 |
| P2 | **赢要有代价感**：对战结果沉淀成玩家战绩（累计胜场/XP/段位），不再打完即弃 | `pk_matches` 只留最新 N 条胜负快照（`history.ts`），**零成长体系**＝第二缺口 |
| P3 | **玩法可被 AI 发起**：对话里 AI 主动约战/主动出题/主动cue复习，走既有工具注册表 | 约战✅ 出题✅（`generate_quiz` v1.4.6）复习 cue❌（无此工具） |
| P4 | 每个游戏化数值**必须能由学习行为解释**（XP 来自做对题/打完一局/毕业词条），禁止无行为空转的签到 | 立约即锁 |

### 2.3 开放性（三层，老板 2026-09-25 已选**全都要**，分期见 §5）
| 层 | 含义 | 底座 |
|---|---|---|
| O1 玩法开放 | 用户自定义玩法参数：难度配比、出题风格、复习目标、对战规则子集——都挂 `app_settings`（v30 起已有 `owner_id`，多用户不互踩） | ✅ 现成，缺的只是把这些键包装成「玩法预设」UI |
| O2 Agent 工具生态开放 | 学习动作继续做成 agent 可调用工具，用户可用一句话驱动一切；外部 MCP 按 `TOOL-ECOSYSTEM-SPEC` S3 既有契约走，不另起炉灶 | ✅ 注册表 10 工具在册 |
| O3 内容分享与外部开放 | 题库/词条卡组可导出与公开分享链接（Quizlet 式 set 分享），**先只读分享、不做他人可编辑**；⚠️ 公开分享页过 tenancy 前先核 `canAccessSession` 同源闸门，跨 owner 一律 404 不是 403（[[reference-studentbuddy-v2-code]] 口径） | ❌ 未做 |

## 3. 已有资产盘点（2026-09-25 现查，全部带出处）

| 资产 | 现状 | 在新叙事里的角色 |
|---|---|---|
| PK 对战（`PK-SPEC` v0.9.1，1214 行契约） | PVE+PVP+AI 主动约战已上线；计分细则完整（`PK-SPEC` §85-113）；power 是两条补救通道（求助 AI＋错题二次机会），**不是道具墙** | 「随时来一局」主玩法；战绩体系的**唯一行为源之一** |
| 艾宾浩斯复习（`EBBINGHAUS-SPEC` v1.2） | 7 节点 1/2/4/7/15/30/60 天；四态队列；日目标可自定义（`review_goal`）；同日重复不推进 | 「每天回来」的内容源；streak 挂靠的行为流 |
| 今日总结（`routes/activity.ts`＋`DailySummaryPage`） | 吃 `/api/activity/today`＋`/week`，一级导航 | 成长外显的**现成落点**，streak/XP 展示位首选这里 |
| 工具生态（`TOOL-ECOSYSTEM-SPEC` v1.4.6） | 10 工具在册（含 `offer_pk_battle`、`generate_quiz`） | agent 叙事的底座；P3 缺的那格＝复习 cue 工具 |
| `app_settings` 七键 | `answer_style`/`quiz_mix`/`quiz_image`/`quiz_source_mix`/`review_goal`/`speech`/`confirm_threshold`（＋`search_key_*` 动态拼键）；v30 后归主 | O1 玩法预设的既有参数池 |
| 薄弱点分析（`QUIZ-WEAK-SPEC` v1.1） | `WeakPoint[]` 实时聚类＋三层降级 | 「析」环保留，不做游戏化改造（避免重走「功能做太重」的老路） |

**结论：新口径不需要推翻任何东西——两个缺口（streak、战绩成长体系）＋一个包装层（玩法预设）＋一条对外叙事。**

## 4. 明确不做（防「又做重」闸门）

1. **不做实时联赛/全球排行榜**——跨用户实时排名要匹配与反作弊，单人运维等于第二个产品（L4 判据直接判死）。
2. **不做道具墙/抽卡/任何数值付费暗示**——power 保持「补救通道」语义不改（P4 判据：数值必须能由学习行为解释）。
3. **不做原生 App**——形态口径保持 Web H5（`GROWTH-SUBMISSION-PACK` §7.2 对 AlternativeTo 填的真实答案不变）。
4. **不动 PK 计分细则**——战绩体系只做**累加**（跨局聚合），不改单局规则，否则 `settle.test` 一族锁定测试与 1214 行契约全部要重谈。
5. **不把「游戏化」做成第三套数据**——streak/XP 一律从既有行为流水（`term_review_log`、`pk_matches`、quiz 判分）派生，`next_review_at` 式「派生不落库」的既有原则沿用，能少一张表就少一张表。

## 5. 分期（每批独立可发、独立可回退；顺序已按拍板④定为 S1 先行）

| 期 | 内容 | 新表 | 依赖 |
|---|---|---|---|
| S0 | ✅ **已完成（2026-09-25）**：本契约拍板收口＝v0.2.0；README/Landing 叙事换口径**未做**（拍板①只定了口径，动文件留到与 S1 同批＋真人测试单） | 无 | — |
| S1 | **streak＋今日视图合流**：连续学习天数（派生自 activity 行为流水；断签口径已按拍板③定死＝**当周任 5 天即续**），显示在今日总结页 | 0 或 1（若纯派生则 0） | 无 |
| S2 | **XP/段位**：跨局聚合 `pk_matches`＋复习毕业＋答对计数，只读战绩页 | 0（派生聚合）~1（若缓存） | S1 |
| S3 | **玩法预设**（O1）：把七键包装成 2~3 套具名预设＋一键切换 | 0（存 `app_settings`） | 无 |
| S4 | **卡组分享**（O3）：词条/题库只读公开分享链接 | 1 | tenancy 现状核一遍再开工 |
| S5 | **复习 cue 工具**（P3 缺口）：agent 主动提醒到期复习并发起小测 | 0 | 工具生态 S 系 |

## 6. 拍板记录（2026-09-25 12:4x，老板口述「都要改，并且是游戏化学习」＝五项全按「改」清零）

| # | 议题 | 裁决 | 落点 |
|---|---|---|---|
| ① | 对外一句话定位 | **改**：§1 新叙事升为对外口径；README/Landing 实际换文与 S1 同批＋真人测试单 | S0 尾巴 |
| ② | XP/段位在对战结算页当场外显 | **要**：结算页显示双方战绩增量（对手可见）⇒ 触 `PK-SPEC` 结算呈现面，施工批**先改那份契约再动码** | S2 |
| ③ | streak 断签口径 | **走推荐宽口径：当周任 5 天即续**——不做冻结道具（无行为数值，违背 P4） | S1 |
| ④ | 分期顺序 | **S1 streak 先行**，S3 玩法预设随后 | §5 |
| ⑤ | 卡组分享页进 SEO | **进**：与词条 SEO 12 页同域同策略，同批带只读防滥用闸门（★ 参照线上实测那条「不存在的页面返 200 壳」——分享页路由要先定清 404 语义再谈收录） | S4 |

★ 诚实限定：本记录依据＝老板在本会话内的口述原句，**没有逐字点选五档的结构化存档**；若事后任一档被否，按「作废要登记、不回改历史行」处理（个人规范 [[doc-revocation-not-deletion]] 口径）。

## 7. 记账与归属

- 本文登记入个人开发文档 `META.md` 清单（随下一笔个人台账批，不在本文自提版本）。
- 09-25 那句口头指令的**行为账**（学习流下线）已在批次 K 行；本文只补**动机账**，不改写任何历史行（[[feedback-ledger-numbers-must-be-recounted]]、[[feedback-doc-revocation-not-deletion]] 口径）。
- 施工时每批照常走「先立/改契约再改码」＋ test-plan 登记 ＋ 真人测试单（§0.16）。
