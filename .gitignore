node_modules/
dist/
*.log
test-results/
.runtime/
.vite/

# 约定（对齐 eslint.config.mjs 第 11 行）：下划线前缀 = 会话级临时诊断脚本。
# eslint 与 tsconfig 早已排除它们，但此前漏了 git ⇒ 临时脚本会以未跟踪状态长期污染
# `git status`（本仓曾积压 7 个 `_tidydiag*.ts`）。这条把约定补齐到 git 层。
**/_*.ts
**/_*.tsx
# 2026-09-13 补：真机复验用的临时探针恰好是 `.mjs`，同一条约定在 mjs 上也得生效
# （来源标注批的 `tools/probes/_probe-config.mjs` 与 `tools/probes/_e2e/` 就是这么积起来的）。
# 入库探针的正名不带下划线（如 `quiz-e2e-cdp.mjs`），不冲突。
**/_*.mjs
tools/probes/_e2e/
