# AGENTS.md — studentbuddy v2 项目导航

> AI 改本仓代码的第一入口。**v1 的 gateway/office 命名/双轨工具/工作区全家桶一律不存在**——开工前先读规划。

## 形态声明（最高优先级）

- 纯本地 Web：`api :18791(仅 127.0.0.1) + web :5173`；SQLite WAL；单用户
- **npm workspaces 三包**：`@sb/shared`（契约）/ `@sb/server` / `@sb/web`
- 端口被 v1 占用时：`SB_PORT=18792 SB_PROXY_TARGET=http://127.0.0.1:18792`

## 六条 ADR（凌驾模块设计，全文见规划 §2.0）

1. 需求为纲：模块必须答「服务学习闭环哪一环」
2. 简洁优先：安全做必要最小（密钥加密/Origin/SSRF+白名单三件），无审批门无策略引擎
3. 域自治：chat/quiz/terms/feedback 只经 shared 契约 + 事件总线交互
4. 失败隔离：次级功能失败只记日志；LLM 输出 normalize+降级不崩
5. 体验契约：操作必有成功/失败/进行中三态，禁止静默
6. 数据容错：schema_version 逐语句迁移；迁移永不触碰 v1 原库

## 工程红线（CI 强制）

- 单文件：server ≤400 行 / web 组件 ≤300 行
- 禁内联 `style={{`（用 tokens.css token）；禁 `any`；测试也禁 `!` 非空断言（`no-non-null-assertion` 随 `tseslint.configs.recommended` 生效，要断存在就写会抛错的辅助函数）
- `npm run check` = tsc×3 + eslint + vitest + gates，全绿才许提交
- **改代码必带测试**（个人开发文档 §0.8）：跑完同步 `docs/dev/test-plan.md` 的基线数与不变量清单；**改 bug 前先读 `docs/dev/bug-ledger.md` 查收敛计数**（§0.15 第 0 步，该表是流程的数据源，不读等于第一脚空转）

## 目录速查

| 路径 | 职责 |
|------|------|
| `packages/shared/src/sse-events.ts` | SSE 事件契约（先登记再实现，同步 docs/SSE-CONTRACT.md） |
| `packages/shared/src/content-blocks.ts` | 内容块协议（演进③；新卡片=登记 kind+注册渲染器）**+ 出题题型配比契约**：`QuizType`/`QuizMix`/`DEFAULT_QUIZ_MIX`/`normalizeQuizMix`（负数小数→0/取整、超上限从后往前削、全 0 回退默认）/`QuizMixReport`，前后端共用一份 |
| `packages/shared/src/answer-style.ts` | **回答方式偏好契约**（契约 `docs/ANSWER-STYLE-SPEC.md` v1.0，前后端共用一份）：四维 `AnswerStyle`（verbosity 详略 / tone 口吻 / support 辅助 / shape 形状）+ `ANSWER_STYLE_FIELDS`（每个选项的 label/hint/**要注入 prompt 的那句 line**——UI 文案与提示词同源，不可能各说一套）+ `SETTING_KEY_ANSWER_STYLE`（`app_settings` 键名唯一登记处）+ `normalizeAnswerStyle`（**逐字段**回落默认，一个非法值不作废整份偏好）+ `styleSummary` + `buildAnswerStyleBlock(style, target?)`（`target:'quiz'` 那支额外带「反斜杠写两根」与「整套题一次写完」两句护栏）。**默认档必须逐字等价现状口径**——新增强指令会让所有历史行为漂移 |
| `packages/shared/src/doc-rag.ts` | **文档检索常量单一事实源**（契约 `docs/DOC-RAG-SPEC.md`）：`MAX_DOC_CHARS=60_000`（★ 语义已是「**直塞阈值**」而非「截断上限」——超过它就换形状注入，不再砍尾）/ `DOC_CHUNK_CHARS=800` / `DOC_CHUNK_OVERLAP=120` / `DOC_TOP_K=12` / `DOC_INJECT_BUDGET_CHARS=12_000` / `DOC_EXTRACT_CHUNKS=36` / `DOC_EXTRACT_BUDGET_CHARS=30_000` + `RetrievedChunk`。**存在理由：前端 `DocModeControl` 曾手抄一份 60k 阈值，双真相源已造成一次漂**；★ 每个常量的取值理由都是 §8 的探针实测数，**改值必须重跑 `tools/probes/doc-rag-bm25.mjs`，不许拍脑袋** |
| `packages/shared/src/memory.ts` | **长期记忆契约**（契约 `docs/MEMORY-SPEC.md` v1.0，2026-09-15 新建，前后端共用一份）：`MEMORY_KINDS` 四类白名单（`profile`/`preference`/`weakness`/`goal`）+ `isMemoryKind`（**非法值一律丢弃、不回落成某一类**——猜错的类别比没类别更糟：一条「薄弱点」被当成「学习偏好」注入会误导此后所有会话）+ `MemoryItem`/`MemoryDraft`/`CompactResult` + 六个常量（`COMPACT_KEEP_TOKENS`/`COMPACT_MIN_DISCARD_TOKENS`/`SUMMARY_MAX_CHARS`/`MEMORY_MAX_ITEMS`/`MEMORY_INJECT_MAX_CHARS`/`MEMORY_MIN_IMPORTANCE`）+ `normalizeImportance`（**★ 空值必须回落 0.5 而不是 0**——`Number(null) === 0` 而**不是 NaN**，直接过 `Number()` 会把「模型没给字段」变成「最不重要」⇒ 那条记忆被淘汰且永不注入；这是本批实测逮到的真缺陷）+ `normalizeMemoryContent`。**判定逻辑（切点/淘汰/协议解析）一律留在 server，前端不重算** |
| `packages/shared/src/study-flow.ts` | **学习流契约的单一事实源**（v0.2.27 新建，契约 `docs/STUDY-FLOW-SPEC.md`，前后端共用一份）：三层类型（`FlowStepKind`/`FlowStepMeta`/`FlowParamMeta`、`FlowDef`/`FlowStepDef`/`FlowEdgeDef`/`FlowPort`、`FlowRun`/`FlowRunStep`/`FlowRunStatus`、`KnowledgeNode`/`KnowledgeEdge`/`KnowledgeNeighborhood`/`KnowledgeGraphStats`）+ **`FLOW_STEP_METAS`（六种「学习交互体验」的唯一清单：explain/quiz/grade/review/digest/summary，各带 `typeVersion`/`params`/`produces`/`awaitsUser`）** + `findFlowStepMeta` + `FLOW_MAX_STEPS=30`。★ **为什么元信息放 shared 而不放 server**：节点面板、参数表单、画布配色全靠这份清单渲染，放 server 就得让前端再抄一份哑数据（`AGENTS.md` 禁的「类型双写漂移」同款病）——server 侧只额外持有执行器（`learning/flow-registry.ts`）。★ **`FlowStepKind` 是有限枚举不是自由文本**——这是「用户可编排」与「用户可创造交互」的分界线（契约 §1.3 范围红线）。★ `produces` 与 `awaitsUser` 是**运行器的行为依据**（前者决定跑完要不要落图、后者决定跑完是否置 `paused`），不是给前端看的装饰 |
| `packages/shared/src/study-flow-params.ts` | **学习流·步骤参数校验的唯一一份**（2026-09-17 前端批从 `server/src/learning/flow-registry.ts` **上提**，契约 `docs/STUDY-FLOW-SPEC.md` §5.2）：`validateStepParams`（未知 kind 拒 + 必填缺失拒 + 选填落默认 + **数值越界钳制而非拒绝**）+ `ParamCheck`。★ **为什么必须上提**：前端参数表单要「提交前先拦」、服务端执行期还要校验；各写一份就必然出现「表单放行、服务端拒绝」或更糟的「一边钳到 10、一边钳到 5」——**校验本身就是契约**，与 `FLOW_STEP_METAS` 同属一个事实源。★ 服务端侧不重复实现：`learning/flow-registry.ts` 只 **re-export** 本文件（既有 `import { validateStepParams } from './flow-registry.js'` 调用方零改动）。★ **不并进 `study-flow.ts` 的原因很实在**：该文件已 352 行，而 **shared 单文件同样受 400 行红线**（gates 判据是「非 web 包一律 400」） |
| `packages/shared/src/domain.ts` | Session/Message/RoleBinding(演进①)/TermItem(词条库字段) |
| `packages/server/src/index.ts` | Express 入口（安全头/CORS/originCheck/body 大小限制）。**路由挂载点唯一登记处**：`/api/chat`、`/api/quiz`、`/api/terms`、`/api/doc`、`/api/settings`、`/api/pk`、`/api/choices`、**`/api/memory`**（2026-09-15 长期记忆逃生口，见 `routes/memory.ts`）。★ 新增路由=本文件加一行 `app.use`；**域逻辑不进这里**。★ **body 上限按路径分流**（v17 看图，2026-09-16）：默认仍是 2mb（防大 payload DoS，继承 v1），**只有 `/api/chat/send` 抬到 24mb**——图片走 base64 内联，一张截图就 2~5MB，**沿用 2mb 会让请求在到达业务校验前就被 413 打回**（实测：前端表现为「点了发送没反应」，极易误判成前端 bug）。24mb ＝ 单张 5MB × 最多 4 张 + 余量，**与 `chat/vision.ts` 的 `MAX_DATAURL_CHARS` 是同一套账，改一处必须同步改另一处** |
| `packages/server/src/chat/flow.ts` | 一轮对话编排（`persistRounds`/`loadHistory` 2026-09-14 已搬去 `chat/persist.ts`；**附加 system 段的构造/落位/预算 2026-09-15 已搬去 `chat/context-segments.ts`**）。本文件现在只留三步：**调一次 `collectContextSegments` → 截断 → 调一次 `assembleContextMessages`**（顺序不可换：截断要用段清单算出的预算，装配要用截断后的历史）。★ **长期记忆（2026-09-15，契约 MEMORY-SPEC）**：`loadHistory` 读全量后由 `collectContextSegments` 内部丢掉已被摘要覆盖的部分（`dropSummarizedHistory`——摘要段已提供其内容，重复发等于同一段占两份预算）；流式收尾后 `void compactIfNeeded(sessionId)` **异步压缩、不 await**（下一轮生效，本轮用户已拿到回答），且排在词条抽取**之后**串行（两者都要打一次 LLM，并发会同时占两个配额槽）。★ **工具循环预算**：窗口 − `systemPromptTokens`（含全部附加段）− 已载历史 − 预留，每轮回灌后核对；轮数上限由 8 提到 15 后，**这条预算闸是唯一的窗口守门人**。★ **本文件 398/400 行**（2026-09-15 收口重构 −41 行；2026-09-16 加 v17 看图 + v18 grill-me 后又贴线）：加逻辑前先看 `chat/context-segments.ts`／`compact.ts`／`memory.ts`／`persist.ts`／`vision.ts`／`grill.ts` 有没有现成落点。★ **v18 grill-me 接线（三处，都在本文件）**：① 开场把 `GRILL_PRE` 作为一条**内存 user 消息** push（不落库——它是指令不是用户发言），并在 `turn===0` 传 `toolChoice: GRILL_TOOL_CHOICE`（**只绑 turn 0**：turn 1 起必须放开，否则模型被锁死在提问上、正文永远出不来）；② `execTool` 给强绑产生的 `ask_choice` 打 `grillPhase:'pre'`（不打的话前端认不出它是 grill 卡，会掉回输入框上方浮层）；③ 落库之后、`chat_done` 之前调 `runGrillClosing`（不 await 用户点选） |
| `packages/server/src/chat/context-segments.ts` | **附加 system 段的唯一装配处**（2026-09-15 新建；2026-09-17 加日期段后 **169 行**）：`collectContextSegments`（造齐七段 + 算好窗口占用 + 给出已过滤历史）／`assembleContextMessages`（按清单顺序装配，返回 `nudgeMsg` **引用**供首轮后精确摘除）／`countSegmentTokens`。★★ **存在理由：同一份清单此前写了两遍**——一遍在 `systemPromptTokens` 的预算核算（7 项 `estimateTokens` 相加）、一遍在 `messages` 的组装（6 处 `push`/`splice` 加非空条件），于是加一段要同步改**三处**（预算项／组装语句／非空条件），漏一处**不报错、只静默漂**：预算漏算 ⇒ 窗口明明不够却按满额载历史（v1 老坑）；组装漏写 ⇒ 段算进了预算却没上屏（白扣窗口）。与 `doc-rag.ts` 那次「前端手抄 60k 阈值」同源，故收成一份清单。★★ **数组顺序就是注入顺序**：`assembleContextMessages` **不做任何排序**，要调顺序就调 `collectContextSegments` 里的字面量顺序——那才是唯一真相源。**位置为什么不能随手改**：`openai` 适配器把多段 system **全量透传**（`anthropic` 侧合并成一段故无差异），位置对模型有语义；回归锁见 `flow.test.ts` 的「长期记忆注入」describe（七段全满的逐段按位顺序 + 画像段不插队）。★ **头部段自 2026-09-17 起有两类**：`date`（**前提**——「今天是几号」与基础提示词的「你是谁」同属一次对话的起点，故排在最前）与 `summary`（**历史的开头**，它替换掉的是**历史本身**）；其余段在历史之后（**辅助材料**：词条/资料/偏好/画像/触发增强）。★ **日期段恒非空、且一天之内恒定**（刻意不含时分秒，否则 system 前缀每轮都变、prompt 缓存永不命中——见 `date-context.ts`）★ **头部段用递增下标插入**（不是恒插 1）——将来多一个头部段时，恒插 1 会让它们**倒序**（已有单测锁）。★ **空内容段统一剔除**：预算与组装看到的是**同一份**清单，不可能一边算一边不算。★ **偏好段恒非空**（至少含 scope 那句），故无需条件判断 |
| `packages/server/src/chat/date-context.ts` | **日期段——让模型知道「今天几号」（2026-09-17 新建，50 行）**：`buildDateBlock(now?)` 纯函数。★ **存在理由：这是事实缺失，不是能力缺失**——模型没有时钟，提示词里也从没有过日期，用户问「今天几号」「距考试还有几天」时它手里没有任何起点，只能瞎猜或答「我的知识截止到……」。修法是**每轮喂事实**（同词条/资料/画像一个手法），而不是在提示词里教它算日期（那是在错的层修）。★★ **三条改了就出事的约束**：① **只到「日」、不含时分秒**——段内容必须**一天之内恒定**：本仓 system 前缀是 prompt 缓存（Anthropic/OpenAI 的 prefix cache）的匹配前缀，带时间戳会让缓存**永不命中**（代价＝模型不知道「现在几点」，本需求用不到）；② **本地时区，绝不用 `toISOString()`**——它按 UTC 出日期，东八区的 09-17 00:30 会算成 **09-16**，而用户一大早问「今天几号」正是这个时刻；故一律走 `getFullYear`/`getMonth`/`getDate`；单机应用（127.0.0.1 + 本地 SQLite）里服务端时区 === 用户时区；③ **`now` 参数是给测试的**——不注入就测不到凌晨/跨月/跨年/闰日这些唯一会错的边界（生产调用不传）。★ **措辞三句各有分工**（删任一句掉一个能力）：事实（几号、星期几）／用法（时间判断以它为准、相对时间按它推算）／边界（训练数据早于今天，不确定就说不确定、不要编日期）——「编一个像真的日期」比「说不确定」危害更大。★ 落位与预算由 `context-segments.ts` 定（它是**头部段**，紧跟基础提示词）；接线回归锁在 `flow.test.ts` 的「日期段注入」describe |
| `packages/server/src/chat/vision.ts` | **「纯文本主模型读图」的视觉蒸馏层**（v17，2026-09-16 新建，93 行）：`describeImages(images, signal?)`（图 → `vision` 角色模型 → 一段中文描述）＋ `parseIncomingImages(raw)`（HTTP 边界校验：最多 4 张、单张 ≤5MB、只认 `data:image/` 前缀）。★★ **架构定位：视觉是**可插拔的外部能力**，不是把主模型换成多模态**——主模型始终是纯文本，它拿到的只是一段「画面的文字版」（拼进该轮 user 消息的 `content` 一起持久化）。三个好处：主模型选型不被看图绑架／视觉模型挂了或换了对主 Agent 其它能力零影响／**历史回放与重新生成都不再二次调视觉模型**（描述已在库里）。★ **未配视觉模型 → 抛可直接照做的报错**（「请到设置页为『视觉（看图）』绑定 qwen-vl-plus / gpt-4o / glm-4v」），**不是笼统的「模型不可用」**（ADR-5：`routeRole('vision')` 在没绑定时返回 `model:''`，空 model 会让视觉调用直接 400，必须与「没配」同一处置）。★ `parseIncomingImages` **先过滤再限流**：脏值不该占额度，否则夹带垃圾就能把合法图片挤掉。★ **上限两处必须同步**：单张 `MAX_DATAURL_CHARS=7_000_000`（≈5MB）与 `index.ts` 里 `/api/chat/send` 的 24mb body 上限是同一套账。⚠️ 提示词把**图示细节/文字/数值放最前**——视觉模型撞 `max_tokens` 时截断是从后往前吃，放最后＝每回先丢最有价值的信息（FrameWise 实测坑） |
| `packages/server/src/chat/tools.ts` | 单轨工具注册表（`tidy_terms` 词条整理 + `search_web` + **`ask_choice` 方案选择框**（2026-09-14；定义与执行在 `chat/choice-tool.ts`，此处只 `registry.set` 一行）；新增工具=加定义+加 `step` 上报；flow 循环零改动）；**生态扩展契约见 `docs/TOOL-ECOSYSTEM-SPEC.md`（M5，待实施——S1 落地后本行改指 `chat/tools/` 目录）。v1.2 已定：词条升为 AI 全权 CRUD（`lookup_terms`/`upsert_term`/`delete_terms` 随 S2）+ 写操作走两阶段 plan→confirm→apply（影响 >3 条弹确认卡，删除一律弹，`term_delete_log` 快照按批可撤销）。★ **v18**：`ToolContext` 加可选 `grillPhase?`（`'pre'|'post'`）——只由 `flow.ts` / `grill.ts` 在 grill-me 强绑路径上注入，普通触发不带；可选字段故既有工具桩零改动 **|
| `packages/server/src/search/index.ts` | 搜索聚合（Exa/Tavily/智谱按 key 并行 → 全无 key 走 Bing 免费通道（cn.bing.com，RSS 主 + HTML 兜底）；24h 缓存；SSRF 护栏） |
| `packages/server/src/chat/system-prompt.ts` | 系统提示词（对话固定前缀）单开文件：它是最常被改的一段文案（每加一个工具就要加一条使用引导），留在 flow.ts 里改会顶破 400 行红线。⚠️ 计入 `systemPromptTokens`——**加长它等于缩小可载历史长度** |
| `packages/server/src/chat/task-list.ts` | 任务清单工具 `update_tasks`（标准 CoT 进度面板）。**不进 tools.ts 注册表**：该文件被并行会话在途改动（R1 避让），本工具走 flow 的 exec 注入接入，S1 拆目录时并回。入参两模式**互斥**：`tasks`＝全量覆盖（仅首次列清单）/ `updates`＝按 **1 基序号**增量（长任务中途只发改动条目，缺 index 即追加）——旧版只有全量，10 条清单每完成一步都要重发 10 条。三段纯函数：`parseTaskArgs`（解析+形状校验）/ `applyTaskPatch`（**整批原子**：任一条越界则整批不生效，并把「当前清单+序号」回灌自纠——半应用会让模型以为的清单与用户看到的分叉）/ `formatTaskList`（回灌**必须带序号**，那是 patch 定位的锚点）。★ 三态 `TaskItem` 的唯一事实源在 `packages/shared/src/task-list.ts`（此前 server/web/SSE 契约三处各写一份） |
| `packages/server/src/chat/choice.ts` | **方案选择框服务端核心**（2026-09-14 新建，契约 `docs/ASK-CHOICE-SPEC.md` v1.0）：把「AI 提一个问题」变成**一个可 `await` 的 Promise**。`askChoice`（校验→落库 `ask_choices`→**先 `waiters.set` 再 `publish`**→await，顺序颠倒＝丢一次唤醒）/`answerChoice`（条件更新 `WHERE status='pending'` 是并发双端点击的**唯一裁决**，changes=0 即回 409 且不覆盖首答）/`cancelChoice`/`cancelChoicesBySession`（逃生口①②）/`listPendingChoices`（前端刷新后把卡捞回来）/`sweepStaleChoices`（逃生口③）/`choiceToolHint`（回灌文案）。★★ **与 ai-orchestrator-v2 的关键差异：不需要内部端点与轮询**——那边工具跑在 opencode 内核、与编排层跨进程；本仓工具与用户答复在**同一 Node 进程**，一个 `waiters` Map 就够，少两条端点少一圈延迟。★★ **waiters 恒 resolve、绝不 reject**：作废也要让工具拿到一份权威记录去决定怎么回灌——用异常表示「作废」会走 `runTool` 的 catch 变成「工具执行失败」，模型收到报错而不是「用户没选，请直接作答」。★ **作废必须就地回填 `status`**（`asCancelled`）：`SELECT` 拿的是改库**之前**的内存快照，不回填就会造出一份 status 仍为 `pending` 的记录。★ **v18 加 `offerChoice`（只提问不等待）**：与 `askChoice` 共用同一套 `waiters` 与落库，唯一差别是**不 `await`**——收尾那一步本轮已结束，挂起等于把 busy 卡死（grill-me 专用，语义见 `chat/grill.ts`）。★ **`grillPhase` 只在内存与 SSE 下发里活**（`ask_choices` 表**刻意不加列**）：前端靠 SSE 那一帧决定「点完开新一轮」，刷新后从 `listPendingChoices` 捞回的卡降级为普通卡——这个降级可接受（刷新时那张卡本身也已不在原语境里），代价已在 `chat/grill.test.ts` 里钉成断言 |
| `packages/server/src/chat/choice-nudge.ts` | **方案选择框「触发增强」**（2026-09-14 夜新建，契约 §6）：功能全通但模型**不主动调** ⇒ 用保守词表识别「这条提问是不是在做选择/规划」，命中则在**首轮**注入硬指令「必须先调 ask_choice」，**首轮结束即由 `flow.ts` 摘掉**（留着会误导后续轮再问一次，问第二次比不问更糟）。红线＝**宁可漏不可误**：排除表拦掉求讲解／排错／`还是不懂` 类追问态；纯函数零 IO，改完跑 `src/chat/choice.test.ts` 即验。**只加工具不加触发层，模型就会永远不用它** |
| `packages/server/src/chat/choice-tool.ts` | `ask_choice` 工具定义与执行（2026-09-14 新建）：`CHOICE_TOOL`（参数 `question`/`options`/`allowCustom`）+ `runChoiceTool(args, ctx)`（取 `ctx.sessionId`→`askChoice`→回 `choiceToolHint`）。**长等待工具单独成文件**，注册仍集中在 `tools.ts`（`registry.set('ask_choice', ...)`）——避免把 `tools.ts` 顶到 400 行红线。★ 校验失败走 `{ok:false,error}` 正常回灌而非抛异常（模型可自纠）。★ **v18 加 `mode?: ChoiceToolMode`（`offerOnly`）**：grill-me 收尾那一轮走 `offerChoice` **只提问不等待**——本轮回答已经给完了，再 `await` 会把这一轮钉在 busy 上直到用户点一下，「可以跳过」就成了假承诺（不点＝卡住）。`ctx.grillPhase` 随提问一起下发，前端据此决定「点完是续本轮还是开新一轮」 |
| `packages/server/src/chat/grill.ts` | **grill-me 模式（v18，2026-09-16 新建，126 行）**：把「要不要弹选择框」从**模型的概率判断**改成**用户的显式开关**。★ 存在理由＝`ask_choice` 工程链路早就全通却真机见不到：触发权在模型手里，系统提示那句「遇到岔路先问」是原则性描述，模型在 `tool_choice='auto'` 下有「直接把答案讲完」的出厂倾向，而 `choice-nudge.ts` 的保守词表**只提高倾向、不保证**、且其排除表恰好覆盖学习场景 90% 的真实提问。★ **两段提问语义相反，别搞混**：`pre`（开场 `GRILL_PRE`）答复**要回灌**（模型据此决定讲什么）⇒ 必须挂起等待；`post`（收尾 `GRILL_POST`）本轮已答完、答复**不回灌**（前端把选中项作为**新一轮提问**发出）⇒ **绝不挂起**（`offerOnly`）。★ **强绑靠常量不靠提示词**：`GRILL_TOOL_CHOICE = {type:'function',name:'ask_choice'}` 由 `flow.ts` 交给适配器，Anthropic 侧要转成 `{type:'tool',name}`（同名不同形，原样透传直接 400），回归锁见 `llm/tool-choice.test.ts`。★ **收尾失败一律静默**：问不出「下一步」不该把已经上屏的回答标成出错。★ 指令文本与调度分家——提示词会随实测反复调（同先例 `choice-nudge.ts`）；`grillInstruction(phase)` 是纯函数出口 |
| `packages/server/src/chat/tool-exec.ts` | 工具执行器（含 `DEFAULT_TOOL_TIMEOUT_MS` 30s 超时 race）。**2026-09-14 加两个可选口子**：`ToolExecOptions.noTimeout?: string[]`（名单内工具**跳过超时 race**——`ask_choice` 要一直等用户点选，被 30s 掐掉就等于功能不存在）与 `sessionId?`（透传进 `ToolContext`，`ask_choice` 靠它知道该把卡发给哪个会话）；`callCtx` 同步透传。★ 两个字段都是**可选**的，既有调用点零改动 |
| `packages/server/src/chat/persist.ts` | 消息落库与历史装载（2026-09-14 **从 `flow.ts` 原样搬出**，零行为改动）：`persistRounds`/`loadHistory`。搬出理由＝`flow.ts` 加逃生口后触 400 行红线，而这两件事与「一轮编排」是两种生命周期。**★ 2026-09-15 长期记忆批**：`loadHistory` 改返回 `HistoryMessage[]`（= `ChatMessage` + `rowid`）——压缩需要一个**单调、可比较、删除后不复用**的锚点来记住「摘要覆盖到哪一条」；做成**子类型**而非给 `ChatMessage` 加字段（`llm/types.ts` 是**适配器契约**，混进 DB 概念不干净），既有调用方零改动 |
| `packages/server/src/chat/compact.ts` | **长期记忆第一层·会话内压缩**（2026-09-15 新建，契约 `docs/MEMORY-SPEC.md` §4）：机制照搬 Pi 的 Compaction——超窗口时把「要丢弃的那一段」先交给 LLM 摘要、存进 `sessions.summary`，下次组装用「摘要 + 最近消息」代替全量历史。`findCutIndex`（从最新往前累积到 `COMPACT_KEEP_TOKENS`，**结果过 `alignToolRoundBoundary`**——切点落在工具轮中间会造出孤儿 tool 消息被 API 400）/`buildCompactPrompt`（学习版六段模板 + `[MEMORY]` 块，已有摘要作迭代输入）/`parseCompactReply`（**分级降级**：`[SESSION_SUMMARY]` 缺失＝整体失败、`[MEMORY]` 缺失不算失败）/`compactIfNeeded`（对外唯一入口，**在途并发防护 Set**，调用方必须 `void` 掉、不 await）。★ **解析失败绝不写库**——脏摘要比没摘要更糟（会持续污染此后每一轮），没摘要只是回到现状、下轮重试。★ `buildMemoryContext` 是给 `flow.ts` 的打包口（摘要段 + 画像段 + 历史过滤三件**同源**的事一起发生）。★ 复用 `llm/router.ts` **既有的 `summarizer` 角色**，零迁移 |
| `packages/server/src/chat/memory.ts` | **长期记忆第二层·跨会话画像**（2026-09-15 新建，契约 `docs/MEMORY-SPEC.md` §5）：`upsertMemoryItems`（**同 kind + 同 content 只刷 importance 取 MAX、不新增行**，同 `term_library` 的 `UNIQUE(term,domain)` 手法）/`loadMemoryItems`/`pruneMemoryItems`（超 `MEMORY_MAX_ITEMS` 按 importance 升序**真删**——画像没有下游引用，软删只是留垃圾）/`buildMemoryBlock`（**纯函数**：按 kind 固定顺序分组、组内 importance 倒序、硬限长 `MEMORY_INJECT_MAX_CHARS`）/`markMemoryUsed`（只写 usage_count，**不参与排序**——它是调参观察窗不是淘汰依据）/`injectMemoryBlock`（读-选-建-记一步到位）。★ **只由压缩过程写入**（借道 `[MEMORY]` 协议块）：独立抽取任务需要「何时跑／跑多少／从哪段跑」三套策略，而压缩已是天然的「该沉淀了」信号点。★ **恒注入不检索**：词条是「与本次提问相关的知识」检索合理，画像是「你是谁」——「他偏好先看例子」对任何提问都成立，检索只会把它漏掉 |
| `packages/server/src/routes.ts` | 路由含 `settingsRouter`（搜索 key 读写 + 连通自检，密钥不出接口；出题配比/配图/回答方式三张卡的读写也收口在此）：`GET/PUT/DELETE /api/settings/answer-style` 一律回 `{ style, configured }`——**`configured` 是 L1 弹卡的开关量**（只回 style 表达不了「没配过」与「配成恰好等于默认」的区别），入参非法逐字段回落不 400，DELETE＝删键回到没配过 |
| `packages/server/src/routes/choice.ts` | 方案选择框 HTTP 薄路由（2026-09-14 新建）：`GET /api/choices?sessionId=`（挂起卡捞回，刷新/重连用）/`POST /api/choices/:id/reply`。**域层不碰 HTTP**——404/409/400 的状态码由 `choice.ts` 的 `AnswerResult.status` 直通，本文件不翻译不包装。★ 挂点 `app.use('/api/choices', choiceRouter)` 在 `index.ts` |
| `packages/server/src/routes/memory.ts` | **长期记忆逃生口路由**（2026-09-15 新建，契约 `docs/MEMORY-SPEC.md` §7）：`GET /api/memory`（画像列表）/`DELETE /api/memory/:id`（**未知 id 回 404**——与 terms 的恒 200 刻意不同，前端要能区分「删错了」）/`DELETE /api/memory`（全部清空）/`GET|DELETE /api/memory/summary/:sessionId`（会话摘要查询与清空）。★ **这几个端点是必做不是锦上添花（ADR-5 不静默）**：记忆是**自动写入**的，看不见改不了就只是黑箱，而一条写脏的画像会**污染此后每一个会话**（不像会话摘要只影响一个）。★ **刻意不设写入端点**：画像的写入路径唯一（压缩过程），多开一条手写路径等于承认「模型自动写的东西需要人补」，那不如直接修提示词 |
| `packages/server/src/learning/quiz.ts` | 出题引擎（协议解析/题库/逐题统计）**+ 题型配比**：`loadQuizMix`/`saveQuizMix`（`app_settings` 键 `quiz_mix`）、`buildMixInstruction`（配比指令拼进提示词）、`applyQuizMix`（多出裁掉、自造题型丢弃、少出记进 report 不补题）——配比规则一律过 shared，不在此另立标准。**+ 出题配图**（契约 `docs/QUIZ-IMAGE-SPEC.md` v1.1.1）：`loadQuizImage`/`saveQuizImage`、`buildImageInstruction(on)`（**开＝正面强制口径 + 属性单引号 + 每组最多 3 张；关＝明令一律 `""`**，v1.0 那套劝说式负面措辞实测 0 出图）、`parseQuizBlock` 五级阶梯（前置无损补括号 → 原样 parse → 剥图重试 → 截断逐题回退 `salvageTruncatedQuiz` → 回退后再剥图）+ 产出 `QuizImageReport{on,delivered,droppedSvg,truncated}`；`normalizeQuiz(payload, { allowSvg, report })` **开关关着就在服务端无条件剥 `svg`（硬门，不靠提示词碰运气）**；`parseQuizBlock(text, report?, allowSvg?)` 同样收这两个参数。**＋ 回答方式偏好**：`generateQuiz(topic, material?, mix?, report?, styleArg?, online?)` 第五参省略＝读库内偏好、显式传入＝**只覆盖本次不落库**（L1 就地弹卡用）；**第六参 `online`**（2026-09-13，契约 `docs/QUIZ-SEARCH-SPEC.md`）＝本次是否联网，命中则由 `quiz-search.ts` 的 `buildQuizSearchBlock` 注入参考资料段；注入段顺序固定为 **配比→配图→偏好→联网**（flow/quiz 两处都有回归锁钉顺序）。**＋ 失败真因**：出题失败时回填 `report.failure = 'no-model'｜'parse'`——谁真知道原因谁填（引擎知道），路由只据此选文案、**不反推**（反推在被 mock 的测试与「角色绑定存在但 provider 被停用」边缘态会判错）。**＋ 出题专用模型参数**：适配器调用显式传 `temperature: QUIZ_TEMPERATURE`(0.4) 与 `maxTokens: getQuizMaxOutputTokens(model)`（见 `llm/model-limits.ts`）。**★ v1.1 修真 bug**：联网判据原写 `online && report`，而 PK 传 `online=true` 但**不传 report** ⇒ PK 的「硬编码联网」**从未真正联网过**；现改为**只看 `online`**（`report` 只是「要不要记账」的可选出参），并按 `mapQuizSources(parsed, found.refs)` 回填题目级来源。★ **本文件 383/400 行**（2026-09-13 把配图逻辑搬出到 `quiz-image.ts`；此前 397/400 只剩 3 行余量）——**再加任何逻辑前必须先开新文件**（`quiz-json-repair.ts`／`quiz-image.ts`／`quiz-search.ts` 三者皆因此单开；**2026-09-15 薄弱点分析亦因此迁出到 `quiz-weak.ts`，本文件回落 366/400**） |
| `packages/server/src/learning/quiz-image.ts` | 出题配图读写与提示词构造（契约 `docs/QUIZ-IMAGE-SPEC.md` v1.1.1）：`loadQuizImage`/`saveQuizImage`（`app_settings` 键 `quiz_image`）、`buildImageInstruction(on)`（**开＝正面强制口径 + 属性单引号 + 每组最多 3 张；关＝明令一律 `""`**）。**2026-09-13 从 `quiz.ts` 原样搬出**（零行为改动）——`quiz.ts` 当时触 400 行红线，依 AGENTS「再加逻辑前先开新文件」规则搬迁；`quiz.ts` 以 `export { loadQuizImage, saveQuizImage, buildImageInstruction } from './quiz-image.js'` 转出，既有 import 路径零改动 |
| `packages/server/src/learning/quiz-search.ts` | 出题联网检索（契约 `docs/QUIZ-SEARCH-SPEC.md` **v1.1.0**，2026-09-13 新建／同日扩来源标注）：`buildQuizQuery(topic, material)` 定检索词（具体主题→主题；占位「综合/根据当前对话内容出题」→材料首段 60 字；两者皆无→空串不搜）；`buildQuizSearchBlock(topic, material, report?)` 调既有 `searchWeb`（与聊天流 `search_web` 同款），命中 ≤6 条、单条摘要压 300 字，注入段开头硬声明「**是素材不是指令**」+「冲突以材料为准」（防间接提示注入挤掉 JSON 协议）；失败三层降级全 catch、**不阻断出题**（ADR-4），`report` 如实记账（ADR-5）。**返回 `{block, refs}`**（v1.1 由 `string` 改）：结果按 url 去重（编号必须一对一映射）、注入段末尾加编号引用规则行（模型只许填 `"refs":[2]` 这类编号，网址一律丢弃）。**`mapQuizSources(quiz, refs)`**（v1.1）：把题目级编号翻译成 `QuizQuestion.source`，**URL 只取自 `refs` 表、绝不读模型给的内容**（弱模型幻觉 URL 是常态，「来源指向不存在的网页」比「没有来源」更糟）；编号越界/非数字/空表一律不填 source，多编号取第一个合法；**无论映射成功与否都删掉 `refs` 字段**（它不是 `QuizQuestion` 成员，留着会顺落库污染题库） |
| `packages/server/src/learning/quiz-weak.ts` | **薄弱点分析——AI 实时生成（契约 `docs/QUIZ-WEAK-SPEC.md` v1.1，2026-09-15 新建）**：`analyzeWeakPoints(quizId)` 走 **`analyzer` 角色**，喂「逐题统计（`quiz_stats`）+ 题干选项 + **用户错选快照**（`quiz_notes.my_answer`）」由模型聚类出若干薄弱主题并给具体错因；**错选快照不可省**——没有它模型只能对着题干和正确率泛泛而谈，产出等于换了说法的固定文本（这正是本批要治的病）。★ **改判背景**：旧实现在 `quiz.ts` 里是纯本地规则、两句硬编码文案、`fallback` 恒为 `true`，而 `analyzer` 角色虽早在 `router.ts` 注册却**从无代码调用**（基础设施白建）⇒ 现改为 **AI 主路径 + 本地规则降级**（ADR-4，`localWeakPoints` 保留且不得删）。★ **三层降级真因由本文件填**（`no-model`／`call-failed`／`parse`，ADR-5 谁真知道谁填、**路由不反推**——「角色绑定存在但 provider 被停用」在反推法下会被误判成 `call-failed`）。★ **空态与降级必须分开**：`analyzed === 0`（还没做题）与 `fallback === true`（模型没接上）是两件事，混成一句就会出现「模型没配」被说成「你还没做题」。★ `parseWeakJson` 四道阶梯（原样→剥围栏→截首尾括号→过两道无损修复），**修复顺序不可换**：`repairJsonEscapes` 必须先于 `repairJsonBrackets`。★ 参数：`WEAK_TEMPERATURE`(0.3，低于出题 0.4——分析要稳不要发挥) + `WEAK_MAX_QUESTIONS`(12，错题封顶) |
| `packages/shared/src/scenario.ts` + `packages/server/src/learning/scenario.ts` + `packages/server/src/learning/scenario-protocol.ts` + `packages/server/src/routes/scenario.ts` + `packages/web/src/features/quiz/ScenarioPanel.tsx`（+ `scenario-view.ts`、`web/src/features/chat/chat-blocks.ts`/`use-quiz-actions.ts`/`chat-menu.tsx`） | **情景题域（契约 `docs/SCENARIO-SPEC.md` v1.1，2026-09-17 新建，M1~M4 全部落地，2026-09-17 分期收官）**：AI 生成 demo 的可回传交互题——**每个评分点在 `quiz_stats` 里就是一道普通题**（占一个 `question_index`），统计/笔记/薄弱点分析零接线复用。★ **裁判在服务端**：demo 只上报事实（observed），对错由 `judgeTask(criteria, observed)` 按 criteria 判（三判型 choice/state/order，形状不对一律判错不抛错）；demo 内本地反馈只是体验。★ **回传唯一通道**：`SCENARIO_BRIDGE_JS`（服务端出页注入，`__SB_DEMO_ID__` 占位符替换）→ `window.SBScenario.report(taskId, observed)` → parent.postMessage → 宿主白名单（type→demoId→taskId 三道）→ REST → **服务端第二道 taskId 白名单**再判分。★ **白送的安全边界**：CSP sandbox 源为 null ⇒ demo 调不动写接口，伪造数据只能走桥接独木桥。★ **存储 1:1 分家**：quiz_bank 存 ScenarioPayload（`source='scenario'`），demo HTML 存 `scenario_demo` 表（**迁移 v20**；不复用 preview 内存暂存——demo 是题目本身不能丢）；`listQuiz` 计数按「有无 `tasks` 键」分派两种形状；删套题连带删 demo（**接线在 routes/quiz.ts，不在 quiz.ts——避免与 scenario.ts→recordAnswer 构成循环依赖**，同 QUIZ-WEAK-SPEC §9 先例）。★ **M2 出题协议**（scenario-protocol.ts，契约 §6）：**双标记**——`[SCENARIO]` JSON + `[SCENARIO_HTML]` 裸 HTML 分离（HTML 不进 JSON 字符串，转义是 svg 字段翻车坑的放大版）；`parseScenarioBlock` 四档救援阶梯（原样→漏转义漏括号修复→JSON 截断线性逐题回退→HTML 截断收口抢救，全部无正则回溯）+ **引用完整性静态检查**（id 接不上 demo 的评分点整条丢弃——死题不进库）；`generateScenario` 复用 quiz-generator 角色/温度/输出上限，落库走 M1 同一条 `saveScenario` 闸门；`POST /api/scenario/generate` 502 真因分开说（no-model/parse），不设 save 开关（不落库就没 demo 可玩）。★ **ScenarioPanel 刻意不提供新标签页打开**（parent 一换回传链当场断）；完成态只认服务端判分响应，results 只增不改。★ **M3 聊天流卡片（已落地）**：`POST /generate` 带 `sessionId` → SSE block 事件（blockId=`scenario-<demoId>`）+ 历史落库 `[SCENARIO]` 登记文本（payload 顶层 + quizId/demoId 登记键，quiz_bank data 不污染）；`chat-blocks.ts` 承载 block 分派（**quiz 分派从 useChatStream 挪入**——398/400 贴线，行为逐字同构）与历史还原（**quiz 块历史上没做历史还原，情景题从第一天生效**）；MessageRow 内嵌 ScenarioPanel（宿主面板进消息流，无新标签页）；composer「+」菜单「情景题」入口（`FlaskIcon`）。★ **三个贴线文件的拆分先例**：ChatView 299→`use-quiz-actions.ts`（出题动作域 hook）、ChatComposer 296→`chat-menu.tsx`（菜单条目构造）、useChatStream 398→`chat-blocks.ts`——加聊天流功能前先看这三处有没有现成落点。★ **M4 编排+薄弱点（已落地）**：`FlowStepKind` 第 7 种 `'scenario'` + 专用执行器 `scenarioStep`（flow-executors.ts，**唯一不走 chatStep 的步骤**——直接调 generateScenario + `announceScenarioToSession`（与 REST 同一份下发，learning/scenario.ts 导出）+ 停步等人玩；`buildStepPrompt('scenario')` 抛哨兵=穷尽性契约锁）；quiz-weak.ts `scenarioTasksOf` 形状探测（修掉对情景套题分析 TypeError 崩溃）+ `buildScenarioWeakPrompt`（任务+判据做素材，上界=tasks.length）。★ **配比档（v0.2.40，SPEC §6.1）**：情景题是题型配比第 5 档（`QuizMixKind`/`MIX_KINDS`/`MAX_SCENARIO_PER_MIX=3`，DEFAULT 0 老用户零变化；normalize 超限先砍情景）；generate 路由**纯情景配比跳过传统引擎**、逐套失败不连坐（`scenarios[]` 如实回账）；设置卡第 5 行按套计 + `scenarioMixNote` 补白。★ **未做**（契约 §9）：observed 快照落库/情景题刷题笔记（quiz_notes.question_data 是 QuizQuestion 形状）、联网检索接入出题 |
| `packages/web/src/features/quiz/weak-report.ts` | **薄弱点分析的展示文案纯函数**（2026-09-15 新建，照 `mix-report.ts` 先例，契约 `docs/QUIZ-WEAK-SPEC.md` §6）：`weakView` 把一次分析结果翻成渲染模型（多主题列表 + 口径行 + 空态/降级提示），`indexText` 做 0 基→1 基题号。★ **两条「写错不报错、只是骗用户」的锁**：① **降级必须如实标注**——三条真因文案两两不同、每条都含「本地规则版」，**不得把降级结果渲染成 AI 分析**（用户为此去设置页绑模型才是正解）；② **空态与降级分开**——`analyzed===0` 说「先做题」，**有错题却没分析出结果时不许说「先做题」**（那是误导）。★ 判定逻辑一律下沉到本文件：本仓 `.tsx` 无测试环境，留在组件里就测不到 |
| `packages/web/src/features/settings/QuizMixCard.tsx` | 设置页「出题题型配比」卡：4 预设 chip + 四题型步进器（0..10）+ 总题数提示，读写 `GET/PUT /api/settings/quiz-mix`；配比全局一份，对话页「出题」与题库页「一键出题」共用 |
| `packages/web/src/features/settings/QuizImageCard.tsx` | 设置页「出题配图」总开关（契约 `docs/QUIZ-IMAGE-SPEC.md`）：**默认关**，读写 `GET/PUT /api/settings/quiz-image`；点选即存。关＝服务端硬门剥掉 `svg`（模型给了也不渲染）；开＝提示词正面强制「涉及示意图的题目必须给 `svg`」，不必配图的题给 `""`，每组最多 3 张。题内渲染走 `SvgPreviewCard`（唯一注入点）；没图/丢图/被截断由 `mix-report.ts` 的 `imageNote()` 如实补一句（ADR-5 不静默） |
| `packages/web/src/features/settings/AnswerStyleCard.tsx` | 设置页「回答方式偏好」卡（**L0**）：四维 chip 行 + 当前摘要 + 「恢复默认」（＝调 DELETE 删键，回到没配过的态，下次出题会重新问）。点选即存，屏上永远回显服务端归一后的值（不能选 A 存 B） |
| `packages/web/src/features/chat/AskStyleCard.tsx` | **L1「出题前先问一次」**：`useAskStyle(run)` 返 `{ tap, card, summary, hint }`——没配过时点「出题」只就地展开内联选项卡（**不新增 modal、不加遮罩**：打断感是这套功能最大的风险），勾「记住」则顺手 PUT。三条实测决定的行为：`configured` **初值 true**（读回来前按已配过处理，宁少问一次也不在出题路上插队）、**落库失败也照常出题**（本次覆盖照样生效）、「别问了，直接出」＝ `PUT 默认值` 后按归一值出题（屏上与库里同源）。聊天页与题库页**共用同一 hook**（两页各写一遍必然漂成两种行为），对话发送永不弹卡、只在未配过时于 composer 上方常驻一句 `hint` |
| `packages/web/src/components/StyleChips.tsx` | 四维 chip 行共用件（设置卡与出题前弹卡同一套渲染，题面/选项全取自 `ANSWER_STYLE_FIELDS`，前端不复制一份文案）；样式自卡在 `style-chips.css`，不依赖调用方的 css |
| `packages/web/src/lib/svg-utils.ts` | SVG 净化（DOMParser 快路径 + 线性正则回退）+ L1 自愈（补闭合/钳宽/主题色）+ `openSvgDocument` 下载/新标签页（入参必须是**净化后**的 SVG，blob 文档继承本应用源），port from v1。**剥除白名单**：script/foreignObject/iframe/object/embed/**image**（`<image href>` 是外链信标，会让本地应用向外部发请求泄露 IP，2026-09-04 补） |
| `packages/web/src/features/quiz/mix-report.ts` | 配比/配图/联网展示文案纯函数（`mixSummary` 摘要 / `shortfallText` 缺题文案 / `imageNote` 配图提示 / `searchNote` 联网提示 / `refsList` 来源清单），组件只负责挂——判定逻辑留在组件里就测不到。`searchNote(report?)`（2026-09-13，契约 `docs/QUIZ-SEARCH-SPEC.md` §2.7）：没联网→`null`；有命中→报条数+来源；只命中缓存→说明；开了全失败→带真因；开了没搜到→也留一句（ADR-5 不静默）。`refsList(report?)`（**v1.1**，§2.9）：返 `report.refs ?? []`（没联网/无报告/老服务端缺键都返空数组，**前端不发明来源**）；与 `searchNote` **二选一**——有清单时由清单承担告知，`searchNote` 只兜「没取到」 |
| `packages/web/src/features/quiz/RefList.tsx` + `ref-list.css` | **套题级来源清单**（**v1.1 新建**，契约 `docs/QUIZ-SEARCH-SPEC.md` §2.9）：默认折叠的 `<details>`，标题「联网：本次参考了 N 条资料」，展开为 `[n]` 编号 + 可点击标题（`target="_blank" rel="noreferrer noopener"`）+ 来源名；**空清单返 `null`**（由 `searchNote` 兜底，不重复说）；URL 全部来自服务端映射的真实检索结果，本组件只渲染、不拼链接、不补全。题库页（**列表视图与练习视图两处都挂**）与对话页共用——两页各写一遍必然漂成两种样式 |
| `packages/server/src/routes/terms.ts` | 词条库路由：`/api/terms` CRUD + `POST /extract`（AI 抽取入库）+ 领域口（**2026-09-17 起与词条 CRUD 对等**）——`GET /domains`（统计，**含空领域**）、`POST /domains`（新建，**可零词条**；已存在回 200 且不覆盖 note）、`PUT /domains/:name`（带 `name`＝改名、只带 `note`＝改说明）、`DELETE /domains/:name`（词条**迁 general、一条不删**，回迁移条数）。★ 状态码由域层 `DomainError.status` **直通**（400/404/409，同 `routes/choice.ts` 手法）；★ `:name` 是**领域名本身**（可含中文/空格/`%`），前端 `encodeURIComponent`、服务端**不再二次解码**（Express 已解码，再解一次会让含 `%` 的名字炸） |
| `packages/server/src/learning/domains.ts` | **领域库（领域升为一等实体，迁移 v19，2026-09-17 新建）**：登记册 `term_domain`（`name` 主键 / `note` / 时间戳）的增删改查——`normalizeDomainName`（小写 + 截 30，与 `normalizeTerms`/`tidy.normDomain` **同口径**；不一致会让 `Math` 与 `math` 在库里存成两行）、`createDomain`（幂等、不覆盖既有 note）、`updateDomain`（只动 note）、`renameDomainEntry`、`removeDomain`（迁 general、**拒删 general**）、`domainStats`（**登记册为左表** ⇒ 空领域也出现；末段 `UNION ALL` 兜**孤儿域**，正常恒空集）。★★ **三条容易想反的语义**：① **改名的权威动作是改词条、不是改登记册**——顺序必须「先迁词条（事实）→ 再同步登记册（索引）」，反了会留下「词条指着一个登记册里已不存在的领域」的中间态；② 迁词条**一律复用 `tidy.renameDomain`**，因为目标域已有同名词条时会撞 `UNIQUE(term, domain)`，它内含「先并入再改名」的防御（本仓实测踩过该约束）；③ **不变式 `term_library.domain ⊆ term_domain.name`** 靠**写入侧**维持（`saveTerms`/`saveOneTerm` 落新域时 `INSERT OR IGNORE` 登记）——`extractTerms` 只做「优先复用」的软引导、不做白名单硬拦（硬拦会把没归好类的词条憋回去），模型随时吐新领域名，靠人工同步必然滞后 ⇒ Tab 漏项；`domainStats` 的孤儿兜底是第二道，**宁可在 Tab 上多一格，也不让词条"隐身"**。★ **依赖方向刻意单向（断环）**：`domains.ts → tidy.ts → terms.ts`，而 `terms.ts` **不反向依赖**本文件（其 `extractTerms` 的领域引导自带 SQL 直查 `term_domain`）；否则成 `terms → domains → tidy → terms` 环（先例：`web/src/lib/api-request.ts` 抽出时同样为断环）。★ 红线：本文件 **220** 行——`terms.ts` 381 行加领域统计会顶破 400 ⇒ 统计归位至此，`terms.ts` 回落 386 |
| `packages/server/src/pk/` + `packages/server/src/routes/pk.ts` | **PK 域（AI 出题微信双人对战，契约 `docs/PK-SPEC.md` v0.5.0）——P0-1 已完成**（2026-09-12）。① 登录：`pk_users` 表（v10 迁移；`openid` 是 `UNIQUE` 且**按真 openid 设计**，P0 只写模拟值 `mock_<userId>`）+ `pk/auth.ts` + `POST /auth/login`（非法昵称 400 且一字不落库）、`GET /auth/me`（未知账号 404），提交 `e02d67a`。② 房间：`pk/room.ts` **全内存状态机**（无落库、无 schema 改动，进程重启即丢局）——`createRoom`（幂等：重复建房返回原房，不制造第二间）／`joinRoom`（满员 409、重复入房幂等、**校验全过才摘旧房**，否则失败会把人踢出自己原来的房）／`startRoom`（仅 `players[0]` 房主可开、双方已进房才 active、`endsAt = now + PK_MATCH_MS`）／`sweepExpired`（**惰性** TTL 回收，不起定时器：waiting 30min／finished 10min／active 取「对局时钟 + 保留期」，P0-1 **不结算**、不擅自写 winner）＋端点 `POST /rooms`、`POST /rooms/join`、`POST /rooms/:id/start`、`GET /rooms/:id/state`、`GET /stream`。③ **身份闸门**：userId 必须命中库里账号（401），房内昵称从库取、**不信客户端自报**；**域错误码 → HTTP 状态与文案的映射只此一处**（`ERROR_STATUS`/`ERROR_TEXT`），域层不碰 HTTP。**P0-2 已落地（2026-09-13）**：`pk/match.ts` 计分引擎（§1 全表 +1/+2/−1/−1/−1；**1s ticker 统一时间驱动**——答题超时/怠慢/对局时钟结算/AI 出题 CD 全收口 `tickMatches` 一处，测试用假时钟直调不起真定时器）＋ `pk/ai-bot.ts` PVE 对手（AI 出题/答题与人**同一条计分路径**，`PK-SPEC` §8）＋ 计分两端点 `/rooms/:id/quiz`（**先落 CD 再生成，失败回滚 CD＝免费重试**）、`/rooms/:id/answer`（即时判分）。**PK 出题已接联网**（2026-09-13）：`match.ts`/`ai-bot.ts` 调 `generateQuiz(..., online=true)` **硬编码联网**，AI 出题与人对局同口径（契约 `docs/QUIZ-SEARCH-SPEC.md` §2.2；不传 report＝不面向用户就不记账）。P0-3 已落 a 批（见下行）。P1 换真微信网页授权只改 openid 写入方，`PkIdentity` 与前端零改动。**P0-4／P0-5／P0-6 契约已立、码未开**（2026-09-13 二次 grill，本文升 v0.3.0）：① **公共题**（一题双方都答；答对自己出的题 +1、答对手的题 +2、成功出题 +1、三条 −1 惩罚保留）；② **理解题**（裁判 AI 每 `COMP_INTERVAL_MS`=3 分钟按本局主题出 1 道，0~3 四档，计入同一总分）；③ **裁判 AI**（出题／判分给理由／盘中短评，短评只读已有状态不调模型）。★★ **本域最大的架构约束（决策⑫，改码前必读）**：`chat/sse-bus.ts` **只有整频道广播、无按订阅者定向能力**，而 PK 双方订阅同一个 `pk:<roomId>` ⇒ **凡进 SSE 的字段双方都看得到** ⇒ **私有判定结果只走 REST 响应体，SSE 只播公开事实**（契约 §1.2）。★ **P0-4 是破坏性改造不是加法**（`PkQuestion` 去 `toUserId`/`chosen`/`status` 换成 `attempts[]`），受影响 10 处落点与 3 个测试文件 38 例清单见契约 **§10**。**P0-7 已落地（2026-09-13 深夜，契约升 v0.4.0，细则 **§11**）**：① **主题轮转**：`PkPlayer.topic` + `PkRoomState` 的 `currentTopic`/`topicOwnerId`/`topicTurn`，两人各选一主题**按轮交替**，成功出题才切轮（失败不让轮）；选主题只在 waiting 期（`POST /rooms/:id/topic`），**双方都选完才能开局**（否则 409 `TOPIC_NOT_SET`），PVE 的 AI 座位兜底 `PK_DEFAULT_AI_TOPIC`。② **跑题校验**：出题时提示词塞「主题：X」约束 + 生成后由裁判 `judgeTopicFit` 判贴合度（新文件 `pk/judge.ts`，**失败返 null 绝不抛**，ADR-4）；不贴合 → 422 `TOPIC_MISMATCH` **不入题不占 CD**、只累计 `failStreak`，**满 3 次才 −1** 并随错误回传裁判建议（`extra.advice`）。③ **求助道具**：`PkPlayer.helpLeft`（每局 1 个），`POST /rooms/:id/help` 走裁判**当场联网**给 `advice/knowledge/refs`，**★ 硬规则：绝不含本题答案**（该规则写在提示词里检索素材之前）；用尽 409、裁判挂了 502 **且不扣次数**。④ **二次机会**：新文件 `pk/power.ts`——`POST /rooms/:id/retry` 只能挑**自己的错题**，给现场解析 + 同主题相似题（题上带 `isRetry`/`retryOf`，`fromUserId=JUDGE_USER_ID` 故不给任何人 +1），答对 **+2 且原 −1 保留**（净 +1），**3 分钟 CD 按玩家记**（`retryNextAt`），CD 在解析给出后即落（防刷解析）。⑤ `llm/router.ts` 的 `MODEL_ROLES` 新增 **`judge` 角色**（**无兜底默认模型**：没配就不配，不拿别的角色的模型顶上）；⑥ 常量 `TOPIC_MAX`/`QUIZ_FAIL_STRIKE`/`RETRY_CD_MS`/`HELP_PER_MATCH`/`JUDGE_USER_ID` 等登记在 `shared/src/pk.ts`，**`PK_QUIZ_MIX` 从 `match.ts` 下沉到 shared**（`judge.ts` 要用，留原地会与 `match.ts` 成环）。⚠️ **P0-4 公共题落地时必须回头改 `requestRetry`**（相似题现在是**单归属**，与 `attempts[]` 不兼容），待改点清单见契约 **§11.7**。**P0-8 ✅ 已落地（2026-09-14，契约 v0.5.0 细则 §12）**：① **投降** `POST /rooms/:id/forfeit`——认输即**对手胜、比分定格**（不额外扣分、不退回分），与时钟归零结算**共用 `finishRoom`** 一条收尾路径（新 `pk/settle.ts`：置 finished→定胜者→记 endReason→广播 `pk-end`→落历史）；`PkRoomState.endReason` **只在非时间到时写**（`timeup` 是老字段语义，不重复标注）；**服务端不加二次确认门**（幂等状态转换，重复调用撞 409——防误触是交互层的活）。② **对战历史**：**DB v15** 建 `pk_matches`（**P0 里第一处让对局落库**，**部分推翻决策记录⑤**——范围卡死：**房间是「过程」仍全内存，历史是「结果」才落库**）+ 新 **`pk/history.ts`**（**视角行**：PVP 一局两行、各存一份快照，查询走 `idx_pk_matches_user`；**只给真人写行**、AI 座位不写；幂等靠 `UNIQUE(room_id,user_id)`+`INSERT OR IGNORE`；**昵称存快照**——对手改名后仍显示当时对战名；**落库失败不阻断收尾**但必留痕 = ADR-4/ADR-5）+ `GET /matches`（列表，`limit` 归一 20／上限 100）与 `GET /matches/:id`（详情含末快照供题目回看；**「不存在」与「不是你的」故意合成同一个 404 码 `MATCH_NOT_FOUND`**，否则等于把「这个 id 存在」告诉没权限的人）。★ **常量硬约束（有不变式用例锁）**：`PK_HISTORY_KEEP ≥ PK_HISTORY_MAX`——保留量小于单页上限，后 100 条就永远拉不出来。★ **端点顺序坑**：`/matches/:id` 的 `:id` 会吞掉 `/stream`，故历史端点排在 `/stream` 之前 |
| `packages/web/src/features/pk/`（`PkApp`/`PkLobby`/`PkRoom`/`pk-view.ts` + `pk.css`） | **PK 前端 P0-3a**（契约 `docs/PK-SPEC.md` §5，2026-09-12）：顶层 `#/pk` **零依赖 hash 路由**（`main.tsx` 按 hash 二选一渲染，不引 react-router，`@sb/web` 保持零运行时依赖）。大厅（昵称登录→建房→输码入房，建房幂等）→ 等待房（大房号＋双方座位＋房主开局）→ 对局视图（**SSE `pk-state` 活水**：实时比分＋对局时钟，时钟钳 0、只作展示）。**SSE 连续 3 次重连失败降级 2s 轮询**（契约 §2.2，Android 微信 X5 内核兜底），轮询 404＝房被 TTL 回收，自动回大厅并如实告知。★ 判定逻辑全在 `pk-view.ts` 纯函数（10 例回归锁），组件只挂 UI（先例 `doc-name.ts`）。★ `sse-client.ts` 已泛化（`connectSse(streamUrl)`），聊天与 PK 同一套重连门控。**未做（不放假按钮）**：出题/答题动作区与结算页属 P0-2 及其后；刷新后丢 roomId 回大厅（无「按 userId 查房」端点，建房幂等可找回原房）。**2026-09-16 响应式断点重构**：废弃 `@media (orientation: landscape)` 判据——**设备方向 ≠ 内容需要多宽**，它同时错判 iPad 竖屏 768（限死 480 白扔 288px）/ 手机横屏 667（强上三栏挤爆）/ 桌面窄窗三种情形；改**屏宽三档 + 屏高补充档**（移动 `<720`／平板 `720~1023`／桌面 `≥1024`／**矮屏横放** `600~1023 宽且 ≤520 高`——手机横屏的病根是屏矮不是屏宽，故判据换屏高、单独立档）。比例全收进 `.sb-pk` 上的 CSS 变量（`--sb-pk-max`／`--sb-pk-content-max`／`--sb-pk-cols`／`--sb-pk-score`），四个断点块**只覆盖变量、规则本身只写一次**；选项容器 `flex`→`grid` 以支持桌面两列。**改比例＝改一个数**。核验走新探针 `tools/probes/pk-breakpoint-cdp.mjs`（9 档视口全绿）。详见 `CHANGELOG.md` 2026-09-16 10:05 行 |
| `packages/shared/src/pk.ts` | PK 契约类型与常量（P0-1：`PkIdentity`／`PkPlayer`／`PkQuestion`／`PkRoomState`／`PkRoomError` + `PK_MATCH_MS`/`PK_MAX_PLAYERS`/`PK_ROOM_CODE_LEN`/`PK_ROOM_TTL_MS`/`PK_FINISHED_KEEP_MS` + `pkChannel()` 频道键）。★ `QUIZ_CD_MS`/`ANSWER_TIME_MS`/`IDLE_PENALTY_MS` **刻意未登记**（P0-1 用不到，不提前造常量），随 P0-2 同批落地。★ **`PkQuestion` 类型层面就没有 `answer` 字段**——「正确答案永不下发」是硬约束，不给「不小心下发」留口子 |
| `packages/web/src/components/UserAuthBox.tsx` + `packages/web/src/lib/auth.ts` | 侧栏底部用户框（PK 登录 P0-1）：登录/改名/退出三态，localStorage 只记「这台设备是谁」（`userId`/`nickname`），启动过 `/auth/me` 校验、账号不存在即静默清除。★ 自持登录态；P0-2 房间逻辑需要 userId 时再提升到 App（届时此处改受控即可） |
| `packages/server/src/security.ts` | 安全三件之 Origin 校验（**不放行 `'null'`**：sandbox 预览页的源就是字符串 null，放行等于让模型写的网页能调写接口） |
| `packages/web/src/features/study-flow/`（`FlowPage` / `FlowCanvas` / `StepPanel` / `RunPanel` / `DefList` / `TemplatePicker` / `ZoomBar` + `KnowledgeGraphPage` / `NeighborhoodGraph` / `NodeDetailCard` + `graph-visual` / `flow-layout` / `flow-viewport` / `use-flow-viewport` / `flow-form` / `run-status` / `flow-actions` / `flow-templates` + `flow.css` / `graph.css`，共 18 文件） | **学习流前端**（2026-09-17 新建，契约 `docs/STUDY-FLOW-SPEC.md` §2／§3／§6）——上一批只做了后端，这批补的正是「图在屏幕上的样子」。**编排页**（`FlowPage`）：左「我的学习流」列表 → 中 **SVG 画布**（节点**可拖拽改 `position` 并落库**，拖动过程不落、松手才落）→ 右步骤面板（**参数表单 + `next`/`correct`/`wrong` 三出口连线下拉**）→ 下运行面板（**步进「继续下一步」+ 逐步轨迹 + 本地产出节点**）。**知识图页**（`KnowledgeGraphPage`）：统计 → 节点列表 → **邻域子图**（点任一节点即以它为新的中心重画，这就是「顺着关系走下去」）+ 节点详情（**它指向谁 / 谁指向它**、手工连边、删边）。★ **纯逻辑全部抽成 `.ts`**（`graph-visual`/`flow-layout`/`flow-form`/`run-status`/`flow-actions`，**79 例回归锁**）——本仓 `.tsx ≤300 行` 是硬红线（本批 `FlowPage.tsx` 首跑 329、抽完仍 301，把 `newStepId` 挪走才落回 300），且这些几何/映射与 React 无关（同 `features/chat` 下 `xxx.ts + xxx.test.ts` 范式）。★ **三处刻意的设计**：① **画布坐标口径只有一处定义**——`FlowStepDef.position` 是**卡片中心**（不是左上角），拖拽吸附 16px 网格；② **三出口的锚点必须分开**（correct 偏上／wrong 偏下／next 居中）——挤在一点上用户就分不清连的是哪条分支，**分支功能等于白给**；三种 `origin` 的边同理用**三套线型**（实线/短虚线/点线）而非只换颜色（缩略图下只靠颜色会糊成一片）；③ **有未保存改动时禁止开跑**——运行冻结的是库里那一版（§6.3），不然用户以为"我改了它却不按我改的跑"。★ **删除动作两个出口刻意分开**：`user`/`ai` 边可单删，`derived` 结构推导边只能整批「清理自动关系」（那些边由规则重算，单删了下次又冒出来）★ **模板批（v0.2.32，2026-09-17）**：「新建」改为**先选模板**（`TemplatePicker` + `flow-templates`，对齐 Dify 的「从模板创建」与 n8n 的「每个参数必须有 `default`」）——原来的「新建」只给一个**空 `explain` 步骤**而 `topic` 是必填，用户点完新建**一进面板就是"必填空着"**、保存/开跑必被 400 拒（**是起点给错了，不是校验错了**）；三个模板（四步课堂／错题重练／考前速通）**参数与连线都已配好**，`flow-templates.test.ts` 逐模板逐步骤过 shared 校验，把「开箱可跑」钉成机器事实，且**刻意没有「空白起步」**（缺必填的起点必然被服务端**保存期**校验拒掉）；参数补 `example` 占位提示（**不是默认值**，不参与提交）；`collectParamProblems` 改带 `stepId` ⇒ 出错**自动跳到那一步**，`runBlockReason` 让「开始新一轮」被拦时能指到具体步骤 ★ **画布缩放/平移批（v0.2.34，2026-09-17）**：`FlowCanvas` 的 `viewBox` 从「`contentBounds` + `meet` = **永远把内容整体塞进容器**」改为**受控视口**——那种写法下**缩放权在内容手里不在用户手里**：内容一宽就自动缩，节点卡片 10px 的字缩到 6px，**用户没有任何手段拉回来**（老板原话「构建的主图应该自带放大缩小的功能,不然看不清」）。现在：默认 **100%**（看全貌交给「适配窗口」按钮，对齐 Figma/Dify/n8n 的通用取舍）、滚轮缩放（**以光标为锚**）、空白拖动平移、右下角**常驻** `ZoomBar`（− / 百分比 / + / 适配 + 「滚轮缩放 · 拖动平移」提示——**滚轮若没有可见控件＝用户以为没这个功能**）。几何在 `flow-viewport.ts`（纯函数，可单测）、事件与状态在 `use-flow-viewport.ts`（hook）。★ **三条做错就出脏 bug 的细节**：① `viewBox` 宽高恒为「容器像素 / scale」⇒ **宽高比与容器严格相等、永无 letterbox**——一旦有 letterbox，`getScreenCTM()`（`toSvg` 的拖拽换算靠它）的「鼠标 → 用户坐标」会**整体偏移、拖拽发飘**（看着正常就是拖不准）；② 滚轮**必须**走原生 `addEventListener({ passive: false })`——React 把 `wheel` 注册成 passive，`preventDefault()` 会失效，画布内滚轮会**同时缩放并滚动整页**；③ `deltaMode` 必须折算（火狐与部分鼠标只报「行」，照像素算一档只变 0.4%＝用户以为滚轮坏了），平移位移**除以 `scale`**（否则放大后一拖就飞）。★ 视口只在**换流 / 内容首次到位**时自动定，用户摸过后一律不再自动改（否则一次 re-render 就把刚平移到的视角弹回中心）；`resetKey` 取 `steps[0]?.defId`（**不必给正卡在 299/300 红线的 `FlowPage` 加 prop**）。★ 顺带修掉**「点节点会立刻被取消选中」**（`setPointerCapture` 期间 `click` 被重定向到 svg，正好命中「点空白＝取消选中」⇒ 参数面板永远打不开），并把「点空白仍能取消选中」也钉成断言（**修 bug 最容易顺手把老行为一起吞掉**）。★ 这一层的仪器是**真机 CDP 探针** `tools/probes/flow-canvas-zoom-cdp.mjs`（31 条断言），不是 vitest——本仓 `.tsx` 无 jsdom，几何能单测、事件接线只能真点 |
| `packages/web/src/lib/api-study-flow.ts` + `api-request.ts` + `api-terms-domain.ts` | 学习流 REST 封装（20 个端点）、**抽出来的最低层**（`ApiError` + `request`）与**领域组**（统计 + 4 个写口 + 4 个类型）。★ **领域组于 2026-09-17 抽出（v0.2.33 领域一等实体批）**：`api.ts` 加完领域组即 **402/400** 撞 gates 红线 ⇒ 按同一手法再抽一次（`api.ts` → **353**），以 `...termsDomainApi` 挂引用 + 顶部 `export type {...}` 转出 ⇒ **调用方零改动**（`api.terms.domainAdd` 这层路径不变）。★ 抽出理由：`api.ts` 已 357/400 行，而放新文件会形成 `api.ts ↔ api-study-flow.ts` **环**（本仓明令避让，见 `QUIZ-WEAK-SPEC` §9 那次「刻意不做 re-export」）⇒ 把共用层抽出来，双方单向依赖本文件、**环断在这里**；`api.ts` 仍 `export { ApiError }` 转出，既有三处调用方零改动。★ **加超时是 SPEC §13 点名的必做项，但处置与原判断不同**：本仓 `request` 用原生 `fetch`——**它根本没有默认超时**，所以缺口不是「放宽」而是「**无限静默等待**」。故给长任务（`advanceRun`）显式 `LONG_TASK_TIMEOUT_MS = 180_000` + 可中断 `signal`，并加 `NO_RESPONSE = 0` 把「等太久/被取消」与真实 HTTP 错误分开——超时文案必须写明「**这一步在服务端可能仍在执行**」 |
| `packages/web/src/app/App.tsx` | 应用壳：180px 侧栏（功能列表 = **学习流／知识图**／题库／**对战**／笔记／词条／今日总结／设置 + 可折叠历史对话 + 底部用户框）。**2026-09-17 导航新增「学习流」「知识图」并把它们排在最前**——它们是**学习主线**（编排怎么学 → 看学出了什么），题库/笔记/词条是素材、今日总结是回顾；新功能成组放最前，而不是塞在末尾当"附加功能"。另加「知识图 → 词条库」带词跳转（`TermsPage` 接 `initialKeyword`，调用方用 `key` 强制重挂）。**「对战」是例外项**（2026-09-13 加，老板实测「找不到入口」）：PK 页是 `#/pk` 上的独立移动优先页（契约 `docs/PK-SPEC.md` §5），这一项只把 hash 改过去、不进 `View` 联合也不 `setView`，根切换交给 `main.tsx`；返回靠 PkApp 头部的「← 学习助手」把 hash 置回 `#/` |
| `packages/web/src/styles/tokens.css` | 设计 token 唯一事实源（改它必须同步设计系统 demo） |
| `packages/web/src/components/icons.tsx` | SVG line-icon 基座（禁 emoji） |
| `packages/web/src/lib/markdown.ts` | 零依赖正文解析：块级切分 + 行内标记；围栏白名单只有 `svg` / `chart` / `html`，其余语言一律代码块（禁裸注入） |
| `packages/web/src/lib/svg-utils.ts` | SVG 净化（DOMParser 快路径 + 线性正则回退）+ L1 自愈（补闭合/钳宽/主题色）+ `openSvgDocument` 下载/新标签页（入参必须是**净化后**的 SVG，blob 文档继承本应用源），port from v1 |
| `packages/web/src/lib/chart-utils.ts` | 图表 DSL：```chart 围栏 JSON 容错（port from v1 fixEcharts）+ bar/line/pie 零依赖自绘 SVG，渲染卡在 features/chat/ChartCard.tsx |
| `packages/server/src/routes/preview.ts` | ```html 预览出页：内存暂存（上限 20 条 / 512KB）+ `CSP: sandbox`（无 allow-same-origin ⇒ 源为 null）；本页 `X-Frame-Options` 由全局 DENY 放宽到 SAMEORIGIN（侧栏面板要嵌它，外站仍嵌不到） |
| `packages/web/src/features/chat/HtmlCard.tsx` | html 卡片：永不内联，点「侧栏预览」送右侧面板、「新标签页」走同一上传路径，三态齐备（进行中/失败/成功） |
| `packages/web/src/features/preview/PreviewPanel.tsx` | 应用右侧内置浏览器面板：只挂 `/api/preview/:id` 沙箱文档，iframe 再叠 `sandbox` 双保险；**无地址栏**（不承诺打开任意 URL） |
| `packages/web/src/lib/preview-store.ts` + `preview-api.ts` | 面板状态（`useSyncExternalStore` 微型 store，跨层不套 provider）与 `uploadPreview()`/`pickTitle()` |
| `packages/server/src/learning/` | 练+忆+学域：quiz 出题引擎（+ **`quiz-json-repair.ts`**：两道无损前置修复——补模型漏写的 `]`（`repairJsonBrackets`）+ 把字符串内漏根的 LaTeX 单反斜杠补成两根（`repairJsonEscapes`，**必须先跑它再补括号**，否则非法转义会让括号修复白做）；两道在**合法 JSON 上永不触发**，故可当所有解析尝试的基底）/ **terms AI 词条库**（`[TERMS]` 协议抽取 / `UNIQUE(term,domain)` 入库 / 相关性检索注入 / usage 计数）/ **tidy 词条整理**（v2.1：`[TIDY]` 协议同义词归一+领域归一、别名感知入库防再分裂；契约 `docs/TERM-TIDY-SPEC.md`）/ **document 文档模式资料**（会话绑定；≤ 60k 整篇直塞、> 60k 走 BM25 切块检索，契约 `docs/DOC-RAG-SPEC.md`；**仍不做 embedding、不做跨会话资料库**）/ activity 打卡（srs+memorize 已废弃，2026-09-01） |
| `packages/server/src/storage/answer-style.ts` | 回答方式偏好读写（46 行，套路照 `loadQuizMix`/`saveQuizMix`）：`loadAnswerStyle`（未配过/坏 JSON/非法值→默认）/ `isAnswerStyleConfigured`（**只查 `app_settings` 键在不在**，`SELECT 1`）/ `saveAnswerStyle`（归一后 upsert，返回归一值）/ `resetAnswerStyle`（DELETE 删键）。★ 「配过没有」只能靠键存在性判断——存进去的恰好是默认值时 style 长得一模一样 |
| `packages/server/src/storage/migrations.ts` | **迁移执行器**：`migrate(db)`（逐版本幂等，≤ current 跳过，每版一个事务失败整体回滚）。★ **2026-09-16 二次拆分**：清单数据（v1~v17）搬到 **`storage/migrations-list.ts`**，本文件只留执行器。★ **2026-09-16 三次拆分（学习流批）**：v18 的七张表让清单再涨 ⇒ 按**版本区间**切成 **`storage/migrations-list-v1-9.ts`** + **`storage/migrations-list-v10.ts`**，`migrations-list.ts` 降为**聚合出口**（`export const MIGRATIONS = [...MIGRATIONS_V1_9, ...MIGRATIONS_V10]`）。**零行为改动**（迁移语句与注释一字未改）；这正是上一档预留的「按版本区间再切」预案，**不是压注释换行数**。理由与 2026-09-14 从 `db.ts` 搬出时完全一致——清单**只会单向增长**（v17 看图加列后本文件到 404 行、再次触「.ts ≤400 行」红线），而执行器自那次拆分后一行未改。**零行为改动**：迁移语句与注释一字未改。⚠️ 若清单再涨到 400 行：**按版本区间再切**，不要用「压注释」换行数（那些注释记的是每张表为什么这么建）。★ 加新迁移＝在 **`storage/migrations-list-v10.ts`**（v10 及以后）数组尾部追加一项（v 号顺延），**不要动既有项**——已应用的版本号是历史锚点。⚠️ **v18 是「就地修正过一次」的特例**：`flow_step` 主键由 `id` 单列改为 `(def_id, id)` 复合（克隆流会撞唯一约束，实测踩到），因**该版本尚未应用于任何真实库**故就地改；**若已发布则绝不可如此**，必须新增版本号。★ **v18（2026-09-16，学习流，契约 `docs/STUDY-FLOW-SPEC.md`）**：**七张表、分三层、纯加法**（只 CREATE TABLE/INDEX，不动既有表）——控制流定义 `flow_def`/`flow_step`/`flow_edge`、控制流运行 `flow_run`（**带 `def_snapshot` 定义快照，不可省**）/`flow_run_step`、知识数据图 `knowledge_node`/`knowledge_edge`。三个库层幂等锚：`flow_run_step UNIQUE(run_id, seq)`、`knowledge_node UNIQUE(kind, ref_id)`、`knowledge_edge UNIQUE(from_node_id,to_node_id,kind)`（后者让重复抽取走 `INSERT OR IGNORE`）。⇒ **回放迁移链的测试本批无需 DROP 新列**（纯加法），但断言表清单/版本号处须补 18。★ **v17（2026-09-16，看图）**：`messages` 加 `images` 列（base64 dataURL 的 JSON 数组），**只用于历史回显缩略图**；主模型看到的是 `chat/vision.ts` 蒸馏后的文字（拼进同一行的 `content`），故**历史回放/重新生成都不再二次调视觉模型**。⚠️ **回放迁移链的测试必须把新列也 DROP 掉**（`ALTER TABLE ADD COLUMN` **不幂等**，实测踩到 `duplicate column name: summary`／`: images` 两次，见 `storage/db.test.ts`） |★ **v15（2026-09-14，PK P0-8）**：建 `pk_matches`（对战历史，契约 `docs/PK-SPEC.md` §12.2）——**P0 里第一处让对局落库**，房间状态机仍全内存；`UNIQUE(room_id,user_id)` 供幂等 + `idx_pk_matches_user(user_id,ended_at)` 供按人倒序查。★ **v16（2026-09-15，长期记忆，契约 `docs/MEMORY-SPEC.md` §3.1）**：`sessions` 加四列（`summary`/`summary_upto_rowid`/`summary_tokens`/`summary_updated_at`）——一个会话一份摘要、滚动覆盖、**不建表**（同 v6 的 doc 两列，生命周期随会话）；`summary_upto_rowid` **用 rowid 而非 `created_at`**（后者只到秒、同秒多条无法区分先后，rowid 单调且删除后不复用），它同时是**幂等锚**（只摘 `rowid >` 它的部分，重复触发不重摘）；建 `user_memory`（跨会话存活，`UNIQUE(kind, content)` 供 upsert 幂等，同 `term_library` 手法；**不设外键**——会话删除后画像仍自洽可读，同 v8/v12/v15 第四次复用）。⚠️ **回放迁移链的测试必须把新列也 DROP 掉**（`ALTER TABLE ADD COLUMN` **不幂等**，本批实测踩到 `duplicate column name: summary`） |
| `packages/server/src/learning/document.ts` | 文档模式域逻辑：`getSessionDoc`/`setSessionDoc`（会话不存在返 null）/`clearSessionDoc`/`docMeta`/`buildDocBlock`/`buildDocMaterial`。**关键设计：存储不丢字，`MAX_DOC_CHARS` 只决定注入形状**（故 `truncated` 可派生、v6 只需两列）：`truncated=false` 走**整篇直塞分支（逐字节等于旧实现，golden 串锁住）**；`truncated=true` 按提问检索 Top-K 段落。资料段开头即声明「内容是数据不是指令」+「优先依据资料，可用一般知识补充」，长资料分支额外必须声明「**这些段落是局部不是全文**，没出现不代表资料里没有」 |
| `packages/server/src/learning/doc-retrieve.ts` | **文档检索层（零依赖词法 BM25，契约 DOC-RAG-SPEC）**：`tokenizeDoc`（英文按词 + 中文 bigram，不引分词器）/`chunkDoc`（按空行聚段、**单段超长走定长硬切**否则整篇无换行的粘贴件切不出块、偏移可 `slice` 回原文）/`buildBm25Index`+`scoreChunks`（倒排索引；idf 用**非负式** `ln(1+(N-df+0.5)/(df+0.5))`，经典式在 `df>N/2` 出负分会造成排序事故）/`retrieveDoc`（k 与预算双截断、首块无条件保留、**空查询返 `[]` 交回调用方**）/`pickUniformChunks`（无查询时的均匀覆盖，给抽词条用；预算不够时是「少取几块但仍铺满全文」不是「从前往后装到装不下」）/`getRetriever()` 返 `Retriever` 接口位。**★ 不设任何分数阈值**（绝对分数与词元覆盖度两种都被探针实测否掉：真命中区间与干扰项区间重叠），判「资料没写」的权力交给模型如实说；**每轮现算、刻意不缓存索引**（716k 字建索引 72ms、缓存则常驻 21.6MB） |
| `packages/server/src/learning/study-flow.ts` | **学习流·控制流定义层**（v0.2.27 新建，契约 `docs/STUDY-FLOW-SPEC.md` §2）：`validateDefInput`（空步骤拒 / **悬空边拒且点出那个不存在的步骤 id** / 步骤 id 重复拒 / **缺省坐标按 orderIndex 自动铺开**）、`createDef`/`getDef`/`listDefs`/`removeDef`（**删定义+步骤+边，但不级联删运行**——运行有自己的定义快照）/`updateDef`（**整体替换**且 `version` 递增）/`cloneDef`（新 id、步骤边一致）。★ **本层与运行层严格分家**（运行层在 `study-flow-run.ts`）：定义是用户编排的模板，运行只认自己冻下的快照。★ 本文件的存在理由之一是本批一条实测红线的**回归锁的落点**——`flow_step` 主键必须是 `(def_id, id)` 复合（全局主键会让「克隆一条流」直接撞唯一约束） |
| `packages/server/src/learning/study-flow-run.ts` | **学习流·运行层**（v0.2.27 新建，契约 §5~§6）：`createRun`（**自动建会话**——学习流的价值就是「在对话里学」，无会话无处产出；冻 `def_snapshot`）/`advanceRun`（**步进式，一次只走一步**）/`getRun`/`listRuns`/`cancelRun`。★★ **核心设计（改码前必读）**：运行器是**步进式**不是后台 while 循环，推进所需状态全在 `flow_run` 行里（`current_step_id`/`cursor`/`step_count`/`status`），**进程内存不持有任何运行状态** ⇒ ① 进程重启不丢（**故不需要 LangGraph 的 checkpointer**）② 天然支持「停在任一步」③ 无后台任务/定时器 ④ 可测（fake executor 下是确定性序列）。代价：**一步 = 一次 HTTP 请求里跑完一整轮 LLM 对话**，数十秒 ⇒ 前端必须放宽超时。★★ **`running` 与 `paused` 都可推进**（paused 自动翻回 running）——原实现只允许 `running`，导致停在 `quiz` 步后**永远推不动**，本批实测逮到。★ **`def_snapshot` 不可省且 `getRun` 不读 `flow_def`**：用户改完定义后已跑过的运行必须仍按**当时的定义**解释轨迹（Dify `WorkflowRunHistory` 同款）；两条后果都有回归锁（改定义不影响在途运行、**定义被删也照样能跑完**）。★ **如实报错不静默**：未接执行器/超 `FLOW_MAX_STEPS`(30)/执行器抛错/快照损坏 ⇒ 运行置 `failed` 且错误点名真因（超限明说是**防死循环保护**不是静默截断）。★ `emitTermNodes` **只做 `term`**（词条表有 `source_session_id`，可按「会话 + 时间窗」精确定位本步新增；`note`/`turn` 缺锚点**不猜**） |
| `packages/server/src/learning/knowledge-graph.ts` | **学习流·知识数据图读写**（v0.2.27 新建，契约 §3~§4）：`ensureNode`（**同 kind+refId 返既有节点**——同一词条在两条流里仍是一个点）/`addEdge`（同 `(from,to,kind)` 幂等、**自环静默返 null**、weight 钳 0~1）/`removeEdge`/`purgeDerivedEdges`（**只删 `derived`**）/`neighborhood`（局部子图，`NEIGHBORHOOD_MAX_DEPTH=3`/`MAX_NODES=60`，**超限如实置 `truncated`**——不把局部图冒充全图）/`deriveDomainEdges`（**`origin='derived'` 的唯一产地**，同域两两建 `relates`，`evidence` 存规则名如 `同域：算法`；**已知 O(n²)，由调用方限定同域节点数**）/`graphStats`/`listNodes`/`nodesOfRun`。★★ **节点只存引用 + 抗删快照**：`ref_id` 指向源表、正文不复制，但 `ref_text` 留一份快照 ⇒ **源行删除后图仍自洽可读**（同 `evolution_event.term_text` 手法，**第五次复用**）。★★ **边必须分 `origin` 三值且刻意不平权**（`user` 最高可信 / `ai` 模型抽取可能幻觉 / `derived` 可被规则重算与批量撤销）——混作一谈，模型幻觉出的关系就再也纠不回来 |
| `packages/server/src/learning/flow-registry.ts` | **学习流·步骤注册表的行为半边**（v0.2.27 新建，契约 §5/§7，**纯逻辑零 IO**）：★ **`validateStepParams` 自 2026-09-17 起已上提 `shared/src/study-flow-params.ts`**（前端参数表单要「提交前先拦」、服务端执行期还要再拦一次，两边各写一份必口径漂移 ⇒ 合成**前后端唯一一份**；本文件改为 `export { validateStepParams } from '@sb/shared'`，既有调用方零改动；**三条规则本身一字未改**——必填缺失即拒、不静默落默认值；选填落声明的 default；**数值越界钳到区间而非报错**，同 `normalizeTerms` 手法；select 非法值拒；布尔接受 `"true"/"false"` 字符串）/`buildStepPrompt`（**纯函数**，按 kind 翻译成一句脚本化提问，返 `{text, awaitUser, pauseReason}`）/`registerExecutor`/`getExecutor`/`registeredKinds`/`clearExecutorsForTest`。★ **与 shared 的分工不可混**：`shared/study-flow.ts` 的 `FLOW_STEP_METAS` 是**元信息**（前端渲染节点面板与表单要用 ⇒ 必须前后端同源），本文件是**行为**（校验+executor，只有服务端要）⇒ 加一种新交互 = shared 加元信息 + 本文件注册 executor。★ **「未注册 executor」绝不静默跳过**（跳过会让用户以为跑了、知识数据却缺了）⇒ 运行失败并点名是哪种类型没接 |
| `packages/server/src/learning/flow-executors.ts` | **学习流·六种步骤的默认执行器**（v0.2.27 新建，契约 §5）：`registerDefaultExecutors()` 给六种 kind 注册**同一个 `chatStep`**，差异全由 `buildStepPrompt` 承担。★★ **本文件是整个学习流设计里最省事也最关键的一处（改码前必读）**：每步的「执行」**不是另起一套 LLM 调用**，而是**该会话里的一句脚本化提问**交给既有 `chat/flow.ts` 的 `handleMessage` ⇒ 工具循环、联网检索、文档 RAG、词条抽取、消息落库、思考链回放**一行都不用重写**；引擎**零新增 LLM 调用路径**（不存在「学习流有自己的提示词、与主流程逐渐漂移」这个老毛病）；产出**天然是真实学习数据**（真词条进 `term_library`、真消息进 `messages`）。**加一种新交互若执行形态也是「一轮对话」⇒ 本文件什么都不用改**。★ 无会话时**如实抛错不静默跳过**。⚠️ 代价：步骤产出是**一轮对话**而非结构化对象，`output` 只存提问原文 + 那条回答的 id（结构化卡片是下一批，契约 §12） |
| `packages/server/src/routes/study-flow.ts` | **学习流薄路由**（v0.2.27 新建，契约 §9）：`/steps`/`/defs` CRUD+`/clone`/`/runs`+`/advance`+`/cancel`/`/graph/*`。★ `/steps` 带 **`wired` 标记**——前端据此**禁用还没接执行器的步骤类型**，把错误挡在**配置期**而不是运行期。★★ **手工加边的 `origin` 由路由强制为 `user`**，不接受客户端自称（否则用户手画的边能伪装成 `ai` 抽取的边，§4.2 的可信分层当场作废）。★ **状态码原样透传、不反推**（域层已把「不存在/状态不对/参数非法」分清楚；路由自己发明状态码会让前端分不清「你传错了」和「服务端炸了」）。★ 挂点 `app.use('/api/study-flow', studyFlowRouter)` 在 `index.ts`，启动块须在**接请求前**调 `registerDefaultExecutors()` |
| `packages/server/src/storage/migrations-list.ts` + `-v1-9.ts` + `-v10.ts` | 迁移清单（**三拆后**：聚合出口 + 两个版本区间文件）。见上方 `storage/migrations.ts` 行 |
| `packages/server/src/routes/document.ts` | `/api/doc` 薄路由（GET/POST/DELETE）：只回元信息、**永不回显正文**；不落盘、不校验扩展名（约束在 UI）；缺参 400 / 会话不存在 404 |
| `packages/web/src/features/quiz·terms·summary/` | 题库 / 词条库（TermsPage 列表管理页）/ 今日总结三屏 |
| `packages/web/src/features/chat/DocModeControl.tsx` | 文档模式的**状态 pill 与载入面板**（挂在 composer 上方）：已载入 pill（名·字数·截断标·清除）/ 展开面板可粘贴或选 `.txt,.md,.markdown` 文件；刷新后只凭 `GET /api/doc` 的元信息复原。**→ 2026-09-13 起「触发器」不再在本文件**——「文档模式 / 换资料」按钮搬进输入框的「+」菜单（见 `components/ComposerMenu.tsx`），本文件改消费 `useDocMode` 的状态。理由：pill 属于**会话状态**，藏进折叠菜单等于把状态藏了（与「联网开关」同一处理：**动作可折叠、状态不藏**）。切换会话时 `useEffect([sessionId])` 重取（`alive` 标志防串台）在 hook 里。**→ 2026-09-07 文案修正三处**：按钮 `title` 按「有无资料」分两态（有资料时直说会替换哪一份）、pill 里扩展名单独成 span（截断时 `.txt` 不丢）、面板顶部补一行机制说明（阈值与会替换原先只在 tooltip 里）——详见 `docs/DOC-RAG-SPEC.md` §4.1。**→ 2026-09-07 打磨两项**：清除按钮 chip 化（可点性）、名与扩展名贴回一体（gap 不再插进文件名中间），after 图证 `shots/after/` |
| `packages/web/src/features/chat/doc-name.ts` | `splitDocName()` 纯函数（pill 文件名拆扩展名，`.gitignore` 类点在首字符的不算扩展名）。从组件里抽出来是为了能测（先例 `features/quiz/mix-report.ts`），配套 `doc-name.test.ts` 6 例——本仓 `.tsx` 无测试环境，纯函数放组件里就进了不了测链路 |
| `packages/web/src/features/chat/ChatComposer.tsx` | **对话页输入区**（2026-09-13 新建）：textarea + 发送/停止 + 「+」折叠菜单 + 文档模式 pill/面板 + 本轮出题提示（配比摘要 / 选项卡 / 连接提示 / 「回到最新」）。**从 `ChatView` 整段搬出**——ChatView 已贴 300 行门禁（gates 第 1 条），输入区自带二十多个受控 props，留在原处必然超线。★ **触发器只在 `sessionId === null` 时整体禁用**：生成中只禁被真正影响的菜单项，**「导出对话」与「联网开关」照旧可用**（ADR-5：能做的别藏着）。给触发器写 `disabled={blocked}` 是最省事也最错的写法——它会把这两条既有行为静默收掉 |
| `packages/web/src/features/chat/ChoiceCard.tsx` + `choice.css` | **方案选择框卡片**（2026-09-14 新建，契约 `docs/ASK-CHOICE-SPEC.md`）：渲染在**输入框上方浮层**（与 `AskStyleCard` 同处置，**不动 `messages` 表、零 schema 风险**）。三态（待选 / 已选 / 作废）＋自由输入出口；props `{request, onReply, onDismiss}`，**组件不自己调接口**（答复走 `useChoiceQueue` 的 `replyChoice`）。★ 自绘 SVG line-icon、禁 emoji（`components/icons.tsx` 基座）。★ 不做乐观切态（见 `useChoiceQueue`） |
| `packages/web/src/features/chat/useChoiceQueue.ts` | 方案选择框前端状态 hook（2026-09-14 新建）：`applyEvent(ev)` 消化 `choice-asked`/`choice-replied`/`choice-cancelled` 三帧（**幂等覆盖、切态不出队**——卡要留在屏上显示结果，作废也要看得见）、`replyChoice`（**刻意不做乐观切态**：并发双端点击的结果由服务端裁决，前端先切会让 409 那次的用户看到假成功）、`dismissChoice`、`reset`；**切会话时调 `api.choices.pending` 把挂起卡捞回来**（SSE 缓冲 60s 无订阅即回收，光靠流会漏）。从 `useChatStream` 抽出（该文件触 400 行红线）。★ **v18 加 `skipChoice(requestId)`**：**真去后端作废**那条挂起提问（`POST /api/choices/:id/cancel`）——原来的 `dismissChoice` 只动本地 state，后端 `askChoice` 还在等，而 grill-me 开场那张卡是**阻塞整轮**的，不真取消工具就永久悬挂、「可以跳过」成空话 |
| `packages/web/src/features/chat/useGrillChoice.tsx` | **grill-me 模式的前端侧（v18，2026-09-16 新建，71 行）**：模式开关 + 点选编排 + 消息流卡片接线。★ **hook 直接吐 `grillNode`**：`ChatView.tsx` 贴 300 行门禁，这一套三十余行塞进去必破线；与本仓「从 ChatView/useChatStream 往外抽」的既有手法同源（`useChoiceQueue`／`useSendActions`／`composer-status`），只是进一步把卡片也一并托管，调用方只落 4 行。★ **模式是会话级前端状态（不落库）**：与文档模式、联网 pill 同口径，切会话靠 ChatView 重新挂载而重置，零 schema 改动。★ **两段语义相反**：`pre` 段答复由服务端回灌，前端什么都不做；`post` 段本轮已结束、答复不回灌，故**前端必须把选中项作为新一轮提问发出**——否则点了选项毫无反应（这是最容易写错的一处）。★ **状态仍由 `useChoiceQueue` 单点持有**：本 hook 只做筛选（`pendingChoice?.grillPhase` 才算 grill 卡）与编排，**不造第二份队列** |
| `packages/web/src/features/chat/GrillPill.tsx` | grill-me 的**状态 pill**（v18，2026-09-16 新建）：模式收在「+」折叠菜单里，**开着必须在框外说一句**并给一键关闭。理由同 `composer-status.ts` 的「联网开着必说」——动作可以折叠，**状态不能藏**；不提示的话用户会以为这个模式没生效（这恰恰是它上一版失败的方式） |
| `packages/web/src/features/chat/AttachmentTray.tsx` | 输入框上方的**待发送图片缩略图托盘**（v17 看图，2026-09-16 新建）：每张带移除按钮。从 `ChatComposer` 抽出——该文件已贴 300 行门禁，本批 grill-me 与看图两处增量叠加后必须腾地方（AGENTS「贴线前先开新文件」） |
| `packages/web/src/features/chat/useSendActions.ts` | 发送侧动作 hook（2026-09-14 新建）：`send`/`rerun`/`regenerate`/`resend`/`stop` 五个动作。同样为 `useChatStream` 腾 400 行门禁空间抽出，type-only 依赖 `StreamMessage`，**不含任何状态**（纯动作，状态仍归 `useChatStream`） |
| `packages/web/src/components/ComposerMenu.tsx` + `composer-menu.css` | 输入框「+」展开菜单（2026-09-13 新建）：**通用件，不认识任何具体功能**，只消费 `items` 数组——以后加功能＝往数组里加一项，本文件与 CSS 都不动（老板明确要求的「预留」）。两种条目：`action`（点完收起）/ `toggle`（点完**保持展开**并在右侧显示「已开/已关」——收起会把反馈一起收掉）。`status` 摘要挂在触发器上一直可见（**动作可折叠、状态不能藏**）；支持 ESC 与点外部关闭（缺这两条，菜单只能靠再点一次「+」关）。「+」展开时纯 CSS 转 45° 变「×」。目前只有对话页用它：题库页那行只有「联网」一个次级控件、主 CTA 必须常驻可见 |
| `packages/web/src/features/chat/composer-status.ts` | **菜单触发器的状态摘要 `menuStatus({online, docBase})`**（2026-09-13 新建，纯函数 `composer-status.test.ts` 6 例）：联网开着**必说**（这是收进折叠菜单后唯一会失去可见性的状态，写错不报错、只是少一句提示，用户会以为联网没开）、联网关着不说（主动选择不是异常）、已载资料报「资料 名字」（名超 10 字截断）。判定抽到这里才进得了测链路（先例 `features/quiz/mix-report.ts`、`features/chat/doc-name.ts`） |
| `packages/web/src/features/chat/useDocMode.ts` | 文档模式的状态与动作（2026-09-13 从 `DocModeControl` 抽出）：`meta/dn/name/text/busy/hint/overCap` + `submit/onPickFile/clear`。**抽出来的是状态不是判定**，故没有可直接上仪器的纯函数（缺口登记在 test-plan §6）。刻意**不接 `blocked` 参数**：动作禁用是调用方的事，状态层再设一道闸只会有两个真相源 |
| `packages/web/src/features/chat/useChatStream.ts` | SSE 生命周期 + 流式文本/step/block 累积编排（前端加一种 SSE 事件渲染从这里接）。**2026-09-14 拆出两个 hook**（`useChoiceQueue` 方案选择框状态 / `useSendActions` 发送侧五个动作）——加方案选择框后本文件 485 行触 400 红线，抽出两个内聚单元后回落 **392 行**；事件入口改为 `if (applyChoiceEvent(ev)) return;` 先把 choice 三帧转发给 `useChoiceQueue`。★ **切轮时 `beginRound` 要调 `resetChoices()`**，否则上一轮的卡会串进下一轮 |
| `packages/web/src/features/chat/Markdown.tsx` | 助手正文渲染器（注入点只有 SvgPreviewCard / ChartCard 里净化后的 SVG，其余走 React 转义） |
| `packages/web/src/features/chat/Welcome.tsx` + `Mascot.tsx` | 空会话欢迎页（「未选会话」与「新会话无消息」共用一套；建议卡只填输入框不自动发送）；吉祥物是 **16×16 像素点阵**（`SPRITE` + 眨眼合帧 `LID` 由 `toRuns()` 并成 `<rect>`，`crispEdges` 且只按 4× 整数倍放大）；配色档位与 `steps()` 动效时序在 `chat.css`（组件内不写内联 style），`Mascot.test.ts` 用 `spriteErrors()` 钉住点阵自洽（行宽／字母登记／合帧必须正压眼位） |
| `tools/gates/check.mjs` | 行数/内联样式/any 门禁 |
| `tools/dev.mjs` | **一条命令并行拉起前后端 dev**（`npm run dev` 的载体，2026-09-12 新增）。替代原先 `npm run dev:server & npm run dev:web`——`&` 在 Windows 的 cmd 里是顺序而非后台，`tsx watch` 不退出 ⇒ 前端永远起不来。这里用 `spawn` 真并行、`stdio: inherit`、任一退出即收掉另一个并透传退出码（防空壳半跑）。零依赖 |
| `tools/probes/doc-rag-bm25.mjs` + `.result.txt` | **首个入库探针**（与同目录 `doc-rag-bm25.result.txt` 一起）：量切块/BM25 在 72k~716k 字资料上的召回、耗时、内存与 k 拐点，`docs/DOC-RAG-SPEC.md` §8 的每个数字都出自它。**纯 JS 不碰 DB 也不联网，Node 20/22 都可跑**；改 `doc-rag.ts` 里任何常量前重跑它。★ 它**不 import 仓内实现**（复刻同参数算法），两边的关联靠 `doc-retrieve.test.ts` 的召回锁；计时与内存行天然浮动，可复现的是召回与块数 |
| `tools/probes/quiz-e2e-cdp.mjs` | **出题页真机渲染探针**（2026-09-13 来源标注批入库）：零依赖（Node 22 内置 `fetch` + `WebSocket` 直驱 CDP）驱动无头 Chrome，真点「题库 → 填主题 → 一键出题」并抓屏上文案（联网提示、题内「来源：」行、`RefList` 的折叠清单与链接 href），截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**）。**存在理由：本仓 web 侧无渲染测试基建**（无 jsdom/testing-library，见 test-plan §1）——「屏幕上一行文案到底有没有出来」纯函数测不到，这是当前唯一能自动核验渲染层的手段。用法与前置（隔离实例 + 5174/18799）写在文件头；★ **必须「点选项 → 等一拍 → 点提交」**（同 tick 会因 picked 还是空而直接返回、题目不揭晓，看着像功能 bug 实为探针太快） |
| `tools/probes/chat-composer-cdp.mjs` | **输入区改版真机渲染探针**（2026-09-13 入库，零依赖同上一行）：11 条断言覆盖「按钮真的从输入框那行消失（只剩「+」与发送）」「来源清单不在常驻区」「侧栏含「对战」且点得进 PK 页 / 回得来主壳」「「+」点得开且 5 项俱在（`aria-expanded` 同步、联网是 `menuitemcheckbox`）」「ESC 收得起」「触发器挂「联网已开」摘要」「文档模式面板开在 composer 上方而非菜单里」。**默认不碰 LLM**；`SB_PROBE_QUIZ=1` 时**真出一次题**（主题「二重积分」）以验「从菜单触发」这条新接线——原为行内按钮直连、现经菜单条目转发，**纯函数测不到**（菜单点不着或转发丢了，单测与 tsc 全绿而用户按「出题」没反应）。★ **自带数据自清**：为点开菜单必须真建一个会话，跑完用页内 `fetch` 删掉会话**连同出题顺带写进题库的条目**，并复核两侧条数回到跑之前——不给用户真实库留垃圾（test-plan §7）。前置同上一行：前端 `vite` 在 **5174**（探针把端口写死；5173 上跑的是另一个项目）、后端 18791 |
| `tools/probes/weak-analysis-cdp.mjs` | **「薄弱点分析」真机渲染探针**（2026-09-15 薄弱点分析批入库，零依赖同上一行）：**三个分支都验**（五次连跑全 EXIT=0）——**AI 主路径**（该套题有错题）**17 条断言**、**空态分支**（`analyzed:0`，服务端不调模型）**15 条**、**降级分支**（`SB_PROBE_DEGRADE=no-model|call-failed|parse`，用 CDP **Fetch 域拦下 analyze 请求并伪造响应** ⇒ **零副作用**：不去设置页解绑模型、不碰真实库、不花模型额度）**17 条**。覆盖「**三态三头**」——点前无残留结果块／**在途**按钮转「分析中…」且 `disabled=true`／**落屏后**按钮复位为「薄弱点分析」且解禁（可重复分析，漏验这一头＝用户只能分析一次）；结果层「顶部口径行 `AI 分析（基于 N 道错题）`」「多主题卡片四要素（主题／题号／理由／建议）齐全」「题号是 1 基（无「第 0 题」）」「**非降级时 `.quiz-weak-fallback` 节点不存在**」（「降级不得冒充 AI 分析」ADR-5 的反向锁）；空态层「文案是『先做题』」「**不挂口径行**」「**不挂降级提示**」（空态与降级必须分得开，契约 §2.1）。★ **只读**：只调 `GET /api/quiz/analyze/:id`、服务端不写库 ⇒ **可对真库跑**（与 `quiz-e2e-cdp.mjs` 会真出题落库不同），但**真花模型额度**，别放进 CI 循环。前置：后端 18791 起着 + 前端 `vite` 在 **5174**（探针写死）+ 设置页已给「薄弱点分析」绑过模型。★ 三条坑：**「分析中…」窗口只有几秒 ⇒ 必须 80ms 密轮询**（间隔 1s 会跳过、把三态已实现误判成没实现）；**截图前必须把按钮滚进视口**（`captureScreenshot` 只拍视口，否则白截一张）；★ **不变量要判「空真」而非「失败」**——零延迟分支（空态不调模型）的在途窗口可能比 80ms 采样还短，**同一套代码两次跑一次采到一次采不到**，判红即**假红灯**（会逼人去改本来正确的代码）⇒ 首次采样前已出结果就记 **SKIP**，用 `skipped` 单独计数、不计入失败；**④★ 端口随机 + 起 Chrome 前先探测端口**——固定端口连跑时上一次的 Chrome 未必已释放端口，新实例**绑不上**、`/json/version` 反而连到**残留的旧浏览器**，探针跑到「别人家的实例」上去了（症状＝请求暂停后永不 fulfil、按钮永远停在「分析中…」、吃满超时整片红，**看着像产品挂了**；本批 `parse` 首跑 11/18 红、单独复跑 17/17 才定位到）⇒ 端口取 `9300+rand(200)`（可 `SB_CDP_PORT` 覆盖），起前探到有人就 `exit(2)` **拒绝跑**——宁可拒绝跑，也不给假结论。用法 `node tools/probes/weak-analysis-cdp.mjs [套题标题]`，截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**） |
| `tools/probes/flow-canvas-zoom-cdp.mjs` | **编排画布「缩放/平移」真机核验探针**（2026-09-17 画布缩放批 v0.2.34 入库，零依赖同上一行；**31 条断言**）：改的整块是**布局与事件接线**（`viewBox` 换受控视口、滚轮换原生非 passive 监听、空白拖动改平移、修「点节点被立刻取消选中」），而 tsc／eslint／gates 对事件接线**全绿也证明不了任何事**、本仓 `.tsx` 又无 jsdom ⇒ 交互层只能真点。断言口径全落在 DOM 事实：控制条真渲染（3 按钮 + 百分比 + 提示）、默认 100%、滚轮真改 `viewBox` **且 `defaultPrevented` 为真**（这条直接判「有没有图省事用 React 的 passive `onWheel`」——用了就红）、**滚轮锚点不漂**（光标下的用户坐标缩放前后一致，用 DOM 里的 viewBox 独立反算、**不复用产品代码**）、**`getScreenCTM` 与 viewBox 在 100% 与 143% 两种缩放下都零偏移对齐**（＝拖拽换算不发飘）、`+`/`−`/`100%`/适配各自生效、适配后**所有节点进得了可见区**（**前置条件先制造「5/5 节点被挤出」**，否则这条断言没有判别力）、空白拖动＝平移且**不清除已有选中**、点空白仍能取消选中、**全程零写入**（定义条数前后一致，收尾复核）。用法：`node tools/probes/flow-canvas-zoom-cdp.mjs`（前置：`npm run dev` 已起；CDP 端口 9400+rand(100)，被占则 exit(2) 拒绝跑；截图落 `SB_SHOT_DIR`，**不入仓**；**不烧任何额度**——纯前端视口变换，**不碰 LLM**）★ 已知未覆盖：缩放态下**真拖节点**（库内坐标未吸附 16px 网格，真拖会写库并吸到 144 ⇒ 不愿污染老板的示例流）、触屏双指缩放（未实现） |
| `tools/probes/pk-breakpoint-cdp.mjs` | **PK 响应式断点真机核验探针**（2026-09-16 PK 断点重构批入库，零依赖同上一行）：**9 档视口矩阵**——移动 375/414、**矮屏横放** 667/844、平板 768/834、桌面 1024/1280/1440，每档断容器宽／对阵方向／选项列数／侧栏几何／旋转提示显隐／无横向溢出，另加两条**全档底线**（题干 ≥14px、选项触摸目标 ≥44px）与一条专治「`var` 解析失败 ⇒ 整条 `grid-template-columns` 声明作废 ⇒ **悄悄退化成 1 列**」的 `colsRaw !== 'none'` 锁；另有本次重构的目标锁「真源不得再有 `@media … orientation`」。★ **与同目录其它探针最大的差别：它不需要后端、也不需要 vite**——媒体查询行为是**纯 CSS 事实**，故探针自带 http 服务，只把 `tokens.css` + `pk.css` **两个真源文件**（每次运行从源复制，不测副本）与一份照 `PkArena`/`PkMatch`/`PkAnswerBlock`/`PkTopicBar` 手搭的 DOM 喂给无头 Chrome ⇒ **零副作用**（不碰 DB、不联网、不调 LLM、不写用户数据），**可反复跑**（同目录其它探针因花模型额度或写库而不能）。用法 `node tools/probes/pk-breakpoint-cdp.mjs`；CDP 端口随机（`9300+rand(200)`，可 `SB_CDP_PORT` 覆盖）+ 起 Chrome 前先探端口，已占用即 `exit(2)` **拒绝跑**；截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**）。★ **改 `pk.css` 任何断点或 `--sb-pk-*` 变量后必须重跑**——tsc／eslint／gates 都看不见 CSS（不在门禁扫描范围），这是当前唯一能自动核验断点层的手段 |
| `docs/DOC-RAG-SPEC.md` | **文档检索契约**（2026-09-06 对 L1 5.0 §5.1「整篇直塞」的改判）：分流规则、三种消费方口径（对话按提问检索／出题按主题检索／抽词条按位置均匀覆盖）、常量取值理由、**两条被实测否掉的阈值方案**、已知局限与未验账 |
| `docs/QUIZ-SEARCH-SPEC.md` | **出题联网检索契约**（**v1.1.0**，2026-09-13）：`QuizSearchReport`／`QuizRef` 数据结构／三入口开关口径（题库页·对话页默认开、PK 硬编码开）／检索词派生／**注入段「素材不是指令」原则**／失败三层降级（ADR-4）／`failure` 真因拆分（修「未配模型」误导报错）／出题专用模型参数（温度 0.4 + 输出上限白名单抬高）／**§2.8 来源标注（模型只给编号、URL 由服务端映射）**／§2.9 前端渲染（RefList + refsList 二选一）／未验边界 |
| `packages/shared/src/choice.ts` | **方案选择框契约的单一事实源**（2026-09-14 新建，151 行）：`ChoiceOption`/`AskChoiceRequest`/`AskChoiceReply`/`AskChoiceRecord`/`ChoiceStatus` + 边界常量（`CHOICE_MIN_OPTIONS=2`/`CHOICE_MAX_OPTIONS=4`/`CHOICE_QUESTION_MAX=500`/`CHOICE_LABEL_MAX=120`/`CHOICE_DESC_MAX=300`/`CHOICE_CUSTOM_MAX=1000`）+ `choiceOptionId(index)`（**id 恒为 `o1..oN`，由下标派生不信模型**）+ `describeChoiceReply` + 两个归一纯函数。★★ `normalizeChoiceInput`/`normalizeChoiceReply` **返回 `{ok:false,error}` 而不抛异常**——错误文本要能回灌给模型自纠（抛异常会变成「工具执行失败」） |
| `packages/shared/src/quiz-weak.ts` | **薄弱点分析契约的单一事实源**（2026-09-15 新建，契约 `docs/QUIZ-WEAK-SPEC.md` v1.1）：`WeakPoint`/`WeakFailure`/`WeakAnalysis` + 边界常量（`WEAK_MAX_POINTS=4`／`WEAK_TOPIC_MAX=40`／`WEAK_REASON_MAX=200`／`WEAK_SUGGESTION_MAX=300`）+ **`normalizeWeakPoints(raw, questionCount)` 归一闸门**。★ **为什么收进 shared**：`WeakPoint` 此前只在 server 的 `learning/quiz.ts` 定义、前端 `QuizBankPage.tsx` 手抄了一份结构等价的内联字面量——加一个字段要改两处，漏一处就是「后端发了新字段、前端不认」的静默降级（与 `TaskItem` 曾有三份定义同源的老病）。★ **三道丢弃规则**（模型输出一律不可信）：三要素缺一即丢／越界题号过滤后一条不剩即丢（**题号是分析唯一能落地的锚点**）／跨条目去重（聚类是对错题的**划分**）。★ `WeakAnalysis.analyzed` 与 `fallback` **是两件事**——0 是「还没做题」（正常空态），`fallback` 才是降级 |
| `docs/QUIZ-WEAK-SPEC.md` | **薄弱点分析契约 v1.1**（2026-09-15 新建，同日升 v1.1）：改判动机（旧实现是纯本地规则 + `analyzer` 角色空转）／数据契约与归一闸门／**数据来源三合流**（统计 + 题干 + 错选快照，及「错选只在带 `answer` 提交时才有」的边界）／提示词口径三条硬约束／失败三层降级与真因／前端渲染口径（三态 + 降级标注 + 多主题列表）／常量取值理由／**§8 本版未做**（不缓存、不走 SSE、GET 带 LLM 副作用的取舍、不落库、不做跨题库）／**§9 影响面**（★ 已更正：`quiz.ts` **刻意不做 re-export**，会构成循环依赖）／**§10 验收状态**（三分支探针证据齐 + 明确「**算不算顺眼**未验收、判定权在老板」） |
| `docs/ASK-CHOICE-SPEC.md` | **方案选择框契约 v1.0**（2026-09-14）：形态（输入框上方浮层）／数据契约／三个 SSE 事件（`choice-asked`/`choice-replied`/`choice-cancelled`）／数据流图／**逃生口三路**（停止生成·删会话·进程重启，事后可恢复而非事前拦截）／边界与**本版未做**（答复留痕不入消息流——会破坏 OpenAI 消息配对约束） |
| `docs/MEMORY-SPEC.md` | **长期记忆契约 v1.0**（2026-09-15 新建，**机制照搬 Pi 的 Compaction + AGENTS.md，不引向量库/不引新依赖**）：§1 现状取证（根因＝`truncateHistoryToBudget` 是**纯截断**，数据在 `messages` 表里没丢、丢在**组装时**）／§2 形态（**与 Pi 的三处刻意分歧**：异步不阻塞、学习版摘要模板、画像搭车压缩同一次 LLM 调用省一次配额）／§3 数据契约（v16 迁移 DDL + 类型 + 常量取值理由）／§4 第一层会话内压缩（切点算法／分级降级／`alignToolRoundBoundary` 复用）／§5 第二层跨会话画像（upsert 幂等／恒注入不检索／真删淘汰）／§6 数据流／§7 逃生口与降级／§8 安全风险（P0 无／P1 提示注入·画像污染／P2 本地存储）／§9 测试计划／§10 落点清单（**含 `flow.ts` 触线的预案**）／§11 本版未做（前端页面·压缩提示条）／§12 未验账 |
| `docs/STUDY-FLOW-SPEC.md` | **学习流契约 v1.0**（2026-09-16 新建，v0.2.27）：§1 三层架构（步骤注册表／控制流／知识数据图）+ **§1.1「图静态、流动态」**（读 LangGraph `StateGraph` 源码得出：`addNode`/`addEdge` 必须 `compile()` 前写完 ⇒ 用户编排的流**当数据**由固定运行器解释执行，**不建成图结构**）+ §1.2 与 n8n `INode` / Dify `WorkflowRunHistory` 的逐条对应 + §1.3 范围红线（用户可自定义「选哪种+填什么参数」，不可造新交互）／§2 定义层（含**复合主键就地修正**的理由）／§3 节点与邻域（**只存引用+抗删快照**、`truncated` 不冒充全图）／§4 边与出处分层（**三值不平权**）／§5 执行器（`awaitsUser` = LangGraph interrupt；**委派既有 `handleMessage`，零新增 LLM 调用路径**）／§6 运行器（**步进式**、`def_snapshot` 不可省、上限与失败口径）／§7 待接清单／§8 安全（**手工边 origin 强制 user**）／§9 路由与状态码／§10 影响面／**§11 验收状态**（58 例 + 全仓回归全绿；**真实模型端到端与前端均未做**，如实标注）/§12 本版未做/§13 未验账（时间窗定位法的秒级边界、`deriveDomainEdges` 的 O(n²)、前端超时未定）。★ **附「本文件与代码的章节号对应表」**——代码注释里写死了 `SPEC §n`，改本文件前必须核对该表 |
| `docs/dev/test-plan.md` | **§0.8 的强制载体**：测试策略/基线用例数/逐文件不变量/已发现 bug 登记。改代码不同步本表即违规 |
| `docs/dev/bug-ledger.md` | **§0.15 第 0 步的数据源**：只记反复/修不动的 bug，首位字段是**收敛计数**（1=正常修／2=必须换根因假设／**3=禁止第 4 次同向尝试**） |

## 里程碑

| 阶段 | 状态 |
|------|------|
| M0 地基（脚手架/门禁/token/图标/文档骨架） | ✅ 2026-08-23 |
| M1 对话核（SSE/单轨工具/模型路由/内容块流） | 🔶 2026-08-28：SSE+单轨工具循环+角色路由真机通过；正文 Markdown + SVG 卡片 + chart 数据图内联渲染、```html 走右侧内置浏览器面板（沙箱 iframe，也可新标签页）；卡片带下载/放大，零依赖；SSE `block` 事件通道仍只服务 quiz |
| M2 练+析（出题/题库/golden dataset） | ✅ 2026-08-23：出题引擎/题库/QuizCard/逐题统计/薄弱点；**2026-09-02 加题型配比**——用户可配每题型题数（0..10，总上限 20），设置页存 `app_settings`，两处出题共用；模型没出齐时裁剪多余 + 如实告知缺哪类（不静默补题、不重试）；**2026-09-04 加出题配图**（契约 `docs/QUIZ-IMAGE-SPEC.md`）——`QuizQuestion` 可选 `svg` 字段（纯加法，老题不迁移）、设置页总开关**默认关**+模型自决、**丢图保题**三道防线（校验丢弃／整字段删除／parse 失败剥图重试救回整组题）；**同日 v1.1/v1.1.1 修「真机出图率 0」**：根因在提示词层（`svg` 没进 `[QUIZ]` 格式示例 + 劝说式负面措辞），改为字段进示例 + 正面强制口径；总开关改成服务端硬门；新增截断逐题回退与 `repairJsonBrackets` 补括号（撞 `max_tokens` 与模型漏写 `]` 都不再整组 502）；`QuizImageReport` 全链上报没图/丢图/被截断。**已真机端到端复验**（接口→落库→题库回读→浏览器渲染→清理）：4 题 3 图、实拍为切题真矢量图、控制台零 error；**同日再加「回答方式偏好」**（契约 `docs/ANSWER-STYLE-SPEC.md` v1.0）——四维偏好存 `app_settings` 键 `answer_style`，**L0** 设置页常驻卡 + **L1** 没配过时点「出题」先就地问一次（勾「记住」才落库），**默认档逐字等价现状口径**（老用户零感知）；真机量化：简短+列点 51 字 / 默认 136 字 / 详细+列点 312 字且 30 行里 26 行是列点（0.87），两维相乘生效。顺带修掉真机暴露的 LaTeX 非法转义导致整组题 502（新增 `repairJsonEscapes`：该组合 7 次里 3 次挂 → 修复后复验 4/4 成功）。**→ 2026-09-15 薄弱点分析改 AI 实时生成**（契约 `docs/QUIZ-WEAK-SPEC.md` v1.1）：旧实现是两句硬编码文案（`reason: '正确率低于 60% 的题目'`）、`fallback` 恒为 `true`，而 `analyzer` 角色虽早在 `llm/router.ts` 注册却**全仓无一行代码调用**；现改为走 `analyzer` 实时聚类出多主题薄弱点（喂统计 + 题干 + 用户错选快照），本地规则退为降级（ADR-4），`WeakPoint` 收进 shared。契约先行、单开 `learning/quiz-weak.ts`（`quiz.ts` 383/400 触线） |
| M3 忆（AI 词条库） | 🔶 2026-08-23：SM-2 调度 + 背背背翻卡；**2026-09-01 重构忆域 v2**：旧 SRS 翻卡废弃（源码删除、数据清空），改 AI 术语库——对话/手动双通道抽词入库、回复前软性注入优先使用、回复后自动计数；**2026-09-03 v2.1 词条库 AI 整理**：`tidy_terms` 对话工具（auto 全量整理 / merge 点名合并 / rename_domain 领域改名），DB 迁移 v7 加 `aliases` 列，别名感知入库防再分裂（契约 `docs/TERM-TIDY-SPEC.md`） |
| M4 反馈+迁移+定稿 | 🔶 2026-08-23：反馈环（事件总线/XP连签/今日总结/近7天趋势）+ v1 全量数据迁移工具已落；定稿未做 |
| M5 工具生态（S1 内核 / S2 内循环工具 / S3 MCP 接入） | 🔶 2026-09-06：**契约 v1.2 已立（`docs/TOOL-ECOSYSTEM-SPEC.md`）；截至 2026-09-13 已落 S1 内核「调度器」部分（并行/超时/取消，提交 `0c16251`/`54e2737`）+ `manage_terms` 直写工具（`67d2bf9`）；S1 注册表拆目录与契约字段、S2 确认门与删除撤销、S3 MCP 接入仍未开工**。拍板四项——先做内核不先堆工具、**MCP 首任客户＝自研 server**（故 stdio + Streamable HTTP 双做、`trusted` 免确认档、§6.6 自研最小实现规范）、~~确认门只对外部工具（内建 write 免确认）~~、里程碑单开本行。**→ 2026-09-06 深夜 v1.2 改判**（老板「让 term 变成 ai 自动增删改查，但改动多要向用户确认」）：确认门**扩至内建 write 并按影响条数分档**（read 永不问／≤3 条直接做／>3 条弹卡／删除一律弹／阈值可调），词条升为 **AI 全权 CRUD**，删除走 `term_delete_log` 快照**按批可撤销**，确认门机制由 S3a **提前到 S2**，S2 工具数 6→9。**S1 前置已满足**（doc-rag 已提交 `a399d5f`，深度理解批不碰 flow）**唯一残留前置＝深度理解的 v8 迁移尚未提交**（`migrate()` 按 `MAX(version)` 跳号，低号晚于高号提交会被静默跳过；建表号一律开工时 grep 取，不照契约预写的号）。现役 **4 个工具**（`search_web`/`tidy_terms`/**`manage_terms`**（词条 AI 全权 CRUD，2026-09-13 落）/`ask_choice`）；`update_tasks` 走 flow exec 注入、不在 registry |
| M6 PK 微信答题（AI 出题双人对战，契约 `docs/PK-SPEC.md` v0.5.0） | 🔶 2026-09-09 契约定稿（与老板 grill 钉死六项——8 分钟固定时长局／P0 纯选择题／出题成功 +1、答对 +2、答错 −1、超时 −1、怠慢 −1 可累计且**分数可为负**／**全部计时在服务端**防作弊／**正确答案永不下发**给答题方／P0 局域网双人不做公网）。**P0-1 ✅ 2026-09-12 完成**：登录（`pk_users` v10 + `/auth/login`、`/auth/me` + `UserAuthBox`，提交 `e02d67a`）＋房间（`pk/room.ts` 状态机 + `/rooms` 四端点）＋SSE `pk:` 频道与 `pk-state` 广播；测试 `pk-auth.test.ts` 11 例 + `pk-room.test.ts` 19 例，全量 44 文件 586 例全绿。**P0-3 已落 a 批（2026-09-12）**：`#/pk` 路由＋大厅/等待/对局实时视图（比分、房号、时钟均为真实数据；SSE 3 次重连失败降级 2s 轮询）；**出题/答题动作区与结算页待 P0-2**（不放假按钮）。**P0-2+PVE ✅ 2026-09-13 完成**：计分引擎 `pk/match.ts`（1s ticker 时间驱动收口）＋ PVE 对手 `pk/ai-bot.ts`（AI 与人完全同口径：独立 LLM 调用只喂题干+选项、超时不占便宜、解析不出不乱猜；人机对称出题，建房可填主题方向，`PK-SPEC` §8）＋ 出题/答题两端点 ＋ `PkMatch` 动作区与结算回看。测试 `pk-match.test.ts` 15 + `pk-pve.test.ts` 7 + `pk-view.test.ts` +6，**全量 47 文件 624 例全绿**。**P0-3 收尾仍欠真机验收**（两台手机同 WiFi 微信内完整打完一局，契约 §6）。★ 本行 **2026-09-12 补登**（此前 PK 整条线在本文档无索引，属 §0.11 漂移），**「M6」编号亦为本次新增，待老板确认**。瓶颈不在代码：P1 真微信卡在**资质**（个人主体申请不到已认证服务号）＋公网 HTTPS/域名前置。**2026-09-13 二次 grill 追加（契约 v0.3.0）**：**P0-4（公共题）在途**、**P0-5（理解题＋裁判 AI）／P0-6（前端补齐）契约已立、码未开**；本行「P0 纯选择题」的旧口径**部分被推翻**——新增**理解题**＝主观判分（0~3 档），选择题仍是唯一「秒判零争议」题种；**P0-3 的真机验收欠账仍未还**。**P0-7 ✅ 2026-09-13 深夜完成（契约 v0.4.0 §11，老板当轮点单）**：主题轮转（两人各一主题按轮交替、未选主题不许开局）／出题跑题校验（裁判判贴合度，跑题 422 不占 CD、连败 3 次 −1 且给建议）／求助道具（每局 1 次，裁判当场联网、**不给答案**）／错题二次机会（解析 + 同主题相似题，答对 +2 而原 −1 保留，3 分钟 CD）。新增 `pk/judge.ts`（裁判四能力，失败返 null 不抛）与 `pk/power.ts`（道具/二次机会，因 `match.ts` 已贴 400 行门禁而拆出），新增端点 `/topic`、`/help`、`/retry`，测试 `routes/pk-power.test.ts` 11 例 + `pk-view.test.ts` +4，**全量 62 文件 / 757 passed + 1 skipped 全绿**；**真机冒烟已跑**（18791 上建双主题房→开局 `currentTopic=世界历史`/`topicOwnerId=房主`/`topicTurn=0`，三个新端点均返回域内错误码而非未知路由；**冒烟账号已自清并复核**）。**★ 同批修 23 例既有红灯**：三个 PK 测试的建房 helper 没带主题导致 `start` 全 409，修法是补主题而非放宽规则。**P0-8 ✅ 2026-09-14 完成（契约 v0.5.0 §12，老板当轮点单）**：**投降**（认输即对手胜、比分定格，与时钟归零**共用 `finishRoom`** 一条收尾路径；服务端不加确认门）+ **对战历史**（**DB v15** 建 `pk_matches`——P0 里第一处让对局落库，**视角行**／只给真人写／昵称存快照／幂等靠 `UNIQUE(room_id,user_id)`；`GET /matches` 列表 + `/matches/:id` 详情含末快照供题目回看）。新增 `pk/settle.ts`、`pk/history.ts` 与前端 `PkForfeit`/`PkHistory`/`PkReviewList`/`usePkIdentity`；测试 `routes/pk-history.test.ts` **22** 例 + `pk-view.test.ts` 20→**28**，**全量 67 文件／839 例全绿**；**真机冒烟 25/25 通过**，其中**重启后端后历史仍在**是本批唯一新增的能力（此前进程一重启对局即全丢）；**冒烟数据已自清并复核** |
| 5.0 §5 学环·**文档模式**（简易 RAG） | ✅ 2026-09-02：DB 迁移 v6 给 `sessions` 加 `doc_name`/`doc_text`（不建表，资料生命周期随会话）；`/api/doc` 三端点 + 前端 DocModeControl；`flow.ts` 注入资料段并计入预算；quiz/terms 缺材料时回退用会话资料。**同批修掉 B-001**（anthropic 丢弃第二条 system，忆域 v2 的词条注入因此静默失效）。**→ 2026-09-06 改判并落地**（契约 `docs/DOC-RAG-SPEC.md`）：旧实现不是真 RAG（>60k 直接砍尾，700k 字资料内容覆盖率实测 **0/13**），现改为≤ 60k 逐字等价直塞（golden 串锁住、零行为漂移）／> 60k 走**零依赖词法 BM25** 切块检索 Top-12 段落并声明非全文；新 `doc-retrieve.ts` + `shared/doc-rag.ts`，**零新依赖、零 DB 迁移、零新 SSE 事件**。T6/T7 已在 Node 22 实测全绿（19/18 例，全量 `npm run check` 亦绿）；**T9 真机端到端已于 2026-09-07 跑过**：合成 77,291 字／492 段讲义、冷事实埋第 423 段（86% 处），`truncated:true` 当场证明走的是检索分支，Q1 关键词型四项事实全中且逐字引用、Q2 改写型三项中，跑完已净库——★ **但模型标的「资料段 N」是本轮注入块内的相对序号、不是原文位置**（needle 实在原文件第 423 段而应答写「资料段 1」），所以上线口径只能说“可溯源到注入的第几段”，**不能说“可定位到原文”**（契约 §6.1 末行已登记此边界）；**仍欠**：T9 的老板主观确认（属 AI 忠实度类，不自行验收）、**pill 与文案的肉眼视觉**（可测部分已 DOM 实测，并已用 headless Chrome + CDP 出 6 张真页图（本批打磨后另拍 6 张 after 存 `shots/after/`；`shots/`＝AI 会话工作区目录、**不在本仓库内**，仓里 grep 不到 png 不等于没拍到，1280 与 643 两档视口，含一张专测中文长文件名被截断的）——内嵌 Browser 视图在 hidden 态被工具层拒出图，故自开无头实例；★ **图已拍到，但顺不顺眼仍归老板判**）、embedding 端点可用性（见 test-plan §6）；另有一处口径**已由老板 2026-09-07 拍定**（保持现状，不再按 `truncated` 拆态）——短资料会话的 `title` 里也带「超长资料按提问检索段落」这句，我判它是机制陈述而非状态断言，故未再按 `truncated` 分态（`DOC-RAG-SPEC` §4.1 末段已登记） |

## 已知约束

- **Node 版本必须「装依赖」与「运行时」一致**（**现役 Node 22.22.2 / ABI 127**，仓库带 `.nvmrc`）：`better-sqlite3` 原生模块产物按**安装那一刻**的 Node ABI 编译，换 Node 大版本**必须重装依赖**（删 `node_modules` 后 `npm install`），只换运行时无效——否则涉库代码全线 `ERR_DLOPEN_FAILED: was compiled against a different Node.js version`，症状是所有 HTTP 接口 500、测试大面积红，极易误判成新代码写错。历史：2026-09-02 踩到（Node 20 装的产物用 PATH 默认 Node 22 跑）；**2026-09-05 已正式迁到 Node 22**——删 node_modules 用 22 重装（prebuilt-install 直接下 v127 官方二进制，未本地编译），`npm run check` 26 文件/302 例全绿 + 隔离实例（`SB_PORT=18799` + 临时 `SB_DATA_DIR`）端到端验过建会话落库、配比 PUT 后 GET 回读一致、WAL 正常生成。**→ 2026-09-06 实测补登三处漂移**（都是当场误判的直接成因，别重踩）：① 本机确实有 Node 22，但**它在 `C:\nodejs22\node-v22.23.2-win-x64`，而 PATH 默认 node 是 `C:\nodejs20\node-v20.18.3-win-x64`（ABI 115）**——跑任何测试/服务前先 `$env:Path = 'C:\nodejs22\node-v22.23.2-win-x64;' + $env:Path`，否则涉库测试直接 `ERR_DLOPEN_FAILED: NODE_MODULE_VERSION 127 vs 115`，症状与本条开头那个坑完全一样，极易被误读成「环境不具备、只能交给用户」；② `.nvmrc` 写 `22.22.2`、实装 `v22.23.2`（同 ABI 127，不致病，但别把 `.nvmrc` 当装机清单）；③ 测试基线已到 **28 文件 / 343 例**（逐文件对账见 `docs/dev/test-plan.md` §3）
- dev 端口与 v1 冲突时用 `SB_PORT` 切换，**不杀 v1 进程**
- **文档模式：短文档直塞、长文档 BM25 检索**（2026-09-06 改判，契约 `docs/DOC-RAG-SPEC.md`）：≤ `MAX_DOC_CHARS=60_000` **逐字等价整篇直塞**；> 60k 才切块 + 按提问取 Top-12 段落（带段号、必须声明非全文）。**仍没有的：embedding 向量、跨会话资料库、持久化索引、pdf/docx 解析、可点击溯源**——要加这些需**先改契约（L1 5.0 §5.1 + L3 DOC-RAG-SPEC）再改码**，不得先写实现再补文档。词法路线的已知天花板：改写型提问（用户不用资料里的词）在 700k 字规模召回收敛在 **8/13 ≈ 62%**，多给块救不回来
- **免 key 兜底已从 DDG 换血为 Bing**（2026-09-17）：原 DDG 通道（`lite.duckduckgo.com` + `api.duckduckgo.com`）自 2026-08-27 实测起在本机网络直连超时、彻底不可用（三路全挂约 20s 且零结果）。现换为 **Bing 免费通道**：`cn.bing.com/search`（实测直连 200 无重定向）——**RSS 主**（带 `&format=rss` 返回标准 RSS2.0，`<item>` 内 title/link/description 齐全，`<link>` 已是干净真实 URL）+ **HTML 兜底**（`b_algo` 块，标题在 `h2>a`、摘要在 `.b_caption>p`，链接同为真实直链、**无 `bing.com/ck/a` 跳转链**）。单路软降级不报失败，双路全挂才逐路冒泡原因。⚠️ RSS 端点自带 Microsoft 版权声明（限个人非商业用途的 RSS 聚合器呈现）——本仓为本地个人学习工具，用途相符；将来若商用需另评估。配了任一 key 时仍走托管三路，Bing 只在三家全无 key 时兜底
- vite 监听 IPv6 `::1`：本机验证用 `http://localhost:5173`（127.0.0.1 不通）
- **`npm run dev` 已能一条命令拉起两端**（2026-09-12 修，载体 `tools/dev.mjs`）：原脚本 `dev -w @sb/server & dev -w @sb/web` 里的 `&`，在 Windows 下 npm 把脚本交给 cmd 执行、而 cmd 的 `&` 是**命令分隔符、不是 shell 后台运算符**，前半个 `tsx watch` 又不退出 ⇒ 不能指望这一条同时把两端拉起（前端永远起不来）。现改为 `node tools/dev.mjs`：`spawn` 真并行 + `stdio: inherit` + 任一退出即收掉另一个并透传退出码。单端调试仍可分别跑 `npm run dev:server`（:18791）/ `npm run dev:web`（:5173）。历史坑留存：改前若误用 `npm run dev`，本机 5173/18791 已被占用时会先触发 vite `strictPort` 报错、干扰判读
- **行内公式不渲染**：`$a^2+b^2=c^2$` 按原文显示（未引 katex，保持 @sb/web 零运行时依赖）
- **转义修复只补「漏根」，不修「错命令」**：`repairJsonEscapes` 能把 LaTeX 单根反斜杠构成的非法 JSON 转义补成两根（真机 502 的主因，统计与复验见 `docs/ANSWER-STYLE-SPEC.md` §8.3）；但 `\theta` / `\nu` 这类恰好等于合法转义（`\t` / `\n`）的写法**无法与真制表符/换行区分，故意不修**——命中时该题仍走逐题回退，最坏丢那一题不连坐整组
- **重型图库仍未实现**：`mermaid` / `echarts` 围栏照旧降级代码块（刻意不引库）；数据图走自绘 ```chart，交互动画走 ```html 沙箱预览（2026-08-28 已上，真机验证预览文档源为 `null`、应用侧读不到其 DOM、读写接口均被拒）
- **预览页只活内存不落盘**：上限 20 条、服务重启即失效，页内提示回对话重新点「侧栏预览」；不做分享链接（本地单用户形态无场景）
- 面板宽 `min(--sb-browser-w, 46vw)` 且 `flex-shrink: 0`：窗口很窄时优先保面板、对话区被挤。未做可拖拽分隔条（内联 style 被门禁禁，需走 CSS 变量 + `documentElement.style.setProperty`，等真需要再加）
