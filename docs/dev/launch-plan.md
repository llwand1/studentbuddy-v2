# 上线台账（launch-plan）

> 版本：v0.1.9 | 状态：[活跃] | 更新：2026-09-21（**v39「零配置平台通道 + 每 5 小时 250 次配额」批落码 ＋ SSH 生产实测**）。★ **本批交付**：平台通道**默认零配置**（凭据从 env `SB_PLATFORM_API_KEY`／`SB_PLATFORM_BASE_URL`／`SB_PLATFORM_MODEL` 读，**数据库里平台行那把 key 永远是空的** ⇒ 落实老板「不让用户看到」）＋ **每用户每 5 小时 250 次**上游调用配额（**迁移 v39** 建 `platform_usage`；★ **落库而非进程内 `Map`**——次数是滚动窗口累计量，放进程内则**每次部署都会把用户额度洗回 250**）＋超限时**引到既有 BYOK 通道**（M2c 已建好，本批不改它）。★ 实测：`npm run test` **189 文件 / 2622 passed + 1 skipped（0 failed）**、`npm run lint` 全绿、`gates` 全绿。★ **台账同步**：① §3.2 配置闸门**新增第 5、6 项**——**三个 `SB_PLATFORM_*` 必须配齐**（**不配 = 全站 AI 完全不可用**，且症状会被误读成「用户自己没配模型」，**这正是线上 09-21 的真实状态**：`providers.api_key = ''`、`role_bindings` 八行 `model` 全空、`messages` 表 3 条 user / **0 条 assistant**）＋「部署前跑一次迁移」；② §3.5 **新增第 3 条实测缺口** —— ★★ **线上跑的代码不是本地正式版**（老板 2026-09-21 明示「现在这个真正运行的代码不是正式的代码，现在本地的才是正式版」）⇒ ★ **§3.3 那六项「已达成」证明的是「机器 + 域名 + 证书能用」，不是「正式版在跑」，两者不能互相背书**；③ §3.5 第 2 条 **Docker 侧已收口**（真根因＝`/etc/docker/daemon.json` 里写的**不是 JSON 而是一行 shell**，已按仓内 `tools/docker/daemon.json` 修好并验通 `docker run --rm hello-world`，坏文件备份在 `/etc/docker/daemon.json.bak-2026-09-21`；**Caddyfile 侧仍未收口**——服务器上其实有 `/etc/caddy/Caddyfile`（529B，含 SPA 兜底），缺的只是**仓里那份**）；④ §3.5 新增一条**方法论记账**：第 3 条是**读文档永远读不出来**的缺口（`README`／`DEPLOY`／`metrics`／本台账四份口径一致地写着「已上线」，而真相是「旧代码在跑」）⇒ 判据必须落在**生产机的实际状态**，不能落在任何一份文档上。★ 同批移除服务器上与本项目无关的 `/root/ai_stack/` 与 `ai_task.service`／`ai_task.timer`（已先备份）。**前任 v0.1.8**：**★★ §3.3 外部依赖闸门实测订正（非批次汇报）—— 6 项 ⬜ → ✅，并订正 §2 `M3` 行「未实机验证」这句已被证伪的话**）。★ **订正方法＝对生产环境实测取证**（不是读文件推断）：`https://11wand.com` HTTPS **200** 且响应头 `Server: Caddy`（自动 TLS 已签发）／`GET /api/health` → `{"ok":true}`／`GET /api/auth/providers` → `{"providers":{"github":false},"form":"cloud"}`／DNS 解析 `107.172.96.209`、NS 为 **Cloudflare**（`alice.ns.cloudflare.com`）／发信子域 `send.11wand.com` → CNAME → Resend 的 `send.forge.rmta.net`，**SPF** `v=spf1 ip4:52.3.252.119 ip4:44.222.39.36 ip4:199.249.231.0/24 ~all`、退信 **MX** `feedback.forge.rmta.net`、根域 **DKIM** `resend._domainkey` 在位、**DMARC** `v=DMARC1; p=none;`。⇒ **站点自 2026-09-19 起已实机运行**（与 `README.md` §当前状态、`DEPLOY.md` 文首「线上状态」一致；**滞后的一直是本文档**）。★ 同批新增两条**实测发现的缺口**（§3.5）：**① 线上构建落后本地约 25 个提交**（线上产物对应 `404374d`，2026-09-20 11:23；实测特征串判定见 §3.5）；**② `docker-compose.yml` 的 caddy 服务挂载 `./Caddyfile` 而仓里没有该文件** ⇒ 容器化路径一跑就挂（现网跑的是 **systemd 形态**，与容器化不是同一条路）。★ **仍不能记「M3 已交付」**：§3.2 配置闸门与 §3.4 人工验收闸门未过，§3.5 两条缺口未收口。**前任 v0.1.7**：**★ M3 状态订正（非批次汇报）**：§2 `M3` 行 ⬜未开工 → 🟡**进行中 · 未实机验证**——容器化那批文件（`Dockerfile` 19 行／`docker-compose.yml` 59 行／`.dockerignore`／`tools/docker/{daemon.json, 部署切换手册.md}`／`packages/web/public/*`）已随收尾提交 `a197916` 入库，但**无 Caddy 配置、无域名/DNS、手册自述「全部步骤未实机验证」**、容器层又不在 vitest／gates 扫描范围 ⇒ 按 §2 判据「**状态的判据是「有实测证据」，不是「代码写完了」**」，**只记「落码」不记「交付」**。★ 顺带订正第二处同源漂移：§3.1 标题「当前全部未做」→「**✅ 5/5 已落码**」（表内 5 行早已全 ✅，标题落后于表）。★ **上线阶梯总体进展未变**：仍 **12 个批次 12 个已交付**（代码面全部完成），「什么时候能上线」的答案仍是 §3.2 配置闸门 → §3.3 域名/DNS → §3.4 人工验收，**这三项只能老板推进**。★ 本条与 §2/§6 的订正均由**收尾会话代补**（对方只落文件、未写任何台账行），**未代改该批任何实现文件**）。**前任 v0.1.6**第 7 次总体进展汇报**：**M2 收口**已交付过测（v0.2.70）——§3.1 五条代码闸门全部落码/锁死：`SB_TRUST_PROXY`/`SB_ALLOWED_ORIGINS`/`SB_HOST` 三个新 env + `SB_COOKIE_SECURE`/`SB_REQUIRE_AUTH` 强开验证（`routes/auth-require.test.ts` 8 例）。**⇒ 代码面全部完成**：12 个批次 12 个已交付；「能不能上线」只剩 §3.2 配置闸门（M3 部署时逐项验）与 §3.3 域名/§3.4 人工验收——后两项只能老板推进）。**前任 v0.1.5**第 6 次总体进展汇报**：**M2d-3 其余表归主**已交付过测（迁移 v33，八张 uuid 主键表加列 `owner_id` + `knowledge_node`/`knowledge_edge` 已上线旧洞收口）⇒ 阶梯 **12 个批次 11 个已交付**；§2 `M2d-3` 行转 ✅。**M2d 至此全部完成**，最快路径只剩「M2 收口 → M3 部署」）。**前任 v0.1.4**第 5 次总体进展汇报**：**M2d-2 词条库归主**已交付过测（迁移 v31，`term_library`/`term_domain`/`term_mention_log` 三张重建 + `SCOPE_JOIN` 跨用户串台修复）⇒ 阶梯 **12 个批次 10 个已交付**；§2 `M2d-2` 行转 ✅ 并写入「开工实测逮到契约没写的串台」这条证据）。**前任 v0.1.3（第 4 次汇报：M2d-1 设置与反馈环归主交付）见 §5**
>
> **这份文档回答一个问题：什么时候可以正式部署上线。** 它是**唯一的进展台账**——
> 每次完成一个「上线」规划里的小任务，就在 §2 改状态、在 §5 追加一次总体进展汇报。
>
> ★★ **它不是第二真相源**（本仓对「同一个事实两处各写一遍」付过多次学费）：
> **每个批次「要做什么」的准绳在各自的契约里**，本文只记**做到哪了 + 上线还差什么**。
>
> | 想知道 | 去看 |
> |---|---|
> | 账号线 M1/M1.5/M1.6/M2c/M2d/M3 各批要做什么 | `docs/AUTH-SPEC.md` **§7 后续批次** |
> | M2c / M2d 的归属改造细节（表、迁移、13 个消费点、并发闸门） | `docs/TENANCY-SPEC.md` **§8**（§8.1 = M2c，§8.2 = M2d） |
> | 产品功能里程碑 M0~M6（**与上线阶梯不是同一套编号**） | `AGENTS.md` §里程碑、`README.md` §里程碑 |
> | 每批的测试基线数与登记行 | `docs/dev/test-plan.md` |
> | 真人验收记录（老板觉得不对的） | `docs/dev/manual-test.md` |
> | AI 修不动的缺陷 | `docs/dev/bug-ledger.md` |

---

## 1. 两套编号，别混（★ 先说清，否则「M2」会被读成两件事）

| 编号体系 | 出处 | 含义 |
|---|---|---|
| **M0~M6** | `AGENTS.md` / `README.md` | **产品功能**里程碑（对话核 / 出题 / 忆域 / 反馈 / 工具生态 / PK） |
| **M1 / M1.5 / M1.6 / M2a~d / M3** | `AUTH-SPEC §7` + `TENANCY-SPEC §8` | **上线**里程碑（本地单用户 → Web 多用户） |

⇒ 本文说的「M2c」一律是**上线**里程碑。产品功能里程碑不决定能不能上线。

---

## 2. ★ 上线阶梯（进展台账）

| 批次 | 内容一句话 | 状态 | 证据 |
|---|---|---|---|
| **M1** | 邮箱+密码账号体系 + 会话 + 鉴权中间件（迁移 v21） | ✅ **已交付** 2026-09-17/18 | `routes/auth.test.ts` **20 例**；契约 `AUTH-SPEC` §2/§4.4。★ 落库于沉淀批 `8f99748`（2026-09-18，**四线混批**，v20~v25 五版迁移一次性落地） |
| **M1.5** | 邮箱验证码登录（`auth_codes` v27 + `send-code`/`login-by-code` + Resend 通道 + 五道限流） | ✅ **已交付** 2026-09-18 | 提交 `588bf40`；单测 **91 例**（见 `test-plan` §3）；契约 §2.5/§4.5/§4.6 |
| **M2a** | 数据隔离地基：`sessions.user_id`（v22）+ `ownerIdOf` + SSE 隔离 | ✅ **已交付** 2026-09-17/18 | `routes/tenancy.test.ts`；契约 `TENANCY-SPEC` §1~§6。★ 落库于 `8f99748`（同上混批） |
| **M2b** | `user_memory` 归主（v24，重建表） | ✅ **已交付** 2026-09-17/18 | `chat/memory.test.ts` **22 例**；契约 §7。★ 落库于 `8f99748`（同上混批） |
| **M1.6** | 注册即验证（`register` 必填 `code`）+ 限流三桶作用域拆分（零迁移） | ✅ **已交付** 2026-09-18 `d512e81` | 真机冒烟 PASS=62；契约 §2.7 |
| — | **`retryAfterMs` 真机漂移收口**（429 响应体补字段）+ 冒烟脚本入库 | ✅ **已交付** 2026-09-18 `0c55c3c` | 真机冒烟 PASS=62 FAIL=0（两个独立实例）；契约 §2.5/§4.5 |
| **M2c**（归属改造） | `providers` + `role_bindings` + `routeRole` 归属（**谁付模型钱**）——迁移 v29 | ✅ **已交付** 2026-09-18 `9bb5f88`（本批 v0.2.60） | 单测 **+2 文件 / +28 例**（新 `llm/router.test.ts` **10** + 新 `routes/providers-tenancy.test.ts` **13** + `storage/db.test.ts` 40→**45**）；全量 **127 文件 / 1795 例（1794 绿 + 1 skipped）0 failed**；契约 `TENANCY-SPEC` §8.1 v1.5。★ **开工实测**逮到「复合 PK 在 SQLite 下拦不住 `NULL`」⇒ 补平台行部分唯一索引（详见 `test-plan` §4 末两行） |
| **M2c**（两层并发闸门） | 内层「每用户 2」＋ 外层「全站封顶 N」（`llm/upstream-gate.ts` 加 owner 维度 + 第二个全站桶） | ✅ **已交付** 2026-09-18 `8b43518`（本批 v0.2.61） | 单测 **+0 文件 / +15 例**（`llm/upstream-gate.test.ts` 13→**24** + `llm/router.test.ts` 10→**14**）；全量 **127 文件 / 1810 例（1809 绿 + 1 skipped）0 failed**；契约 `TENANCY-SPEC` §8.1.3.1（v1.6）。★ 配额经 `bindQuota()` **绑在 `routeRole` 返回的 adapter 上** ⇒ 15 个调用点零改动、结构上不可能漏传。★ **新锁非空转已按 §7 三次取证**（忽略 owner 分桶→6 例红；不归还内层槽→恰好 1 例红；`bindQuota` 原样返回→3 例红）。★ **N 仍是占位值 8**（`SB_UPSTREAM_SITE_MAX_CONCURRENT`，队列上限 20）——**待老板给业务值**；★ 已知边界：进程内 `Map`，多实例部署容量 × 实例数（本批不做） |
| **M2d-1** | 设置与反馈环归主：`app_settings` / `daily_activity` / `daily_summaries` / `user_stats`——四张「约束会跨用户撞键」的表重建（**迁移 v30**） | ✅ **已交付** 2026-09-18 `d39e987`（本批 v0.2.62） | 单测 **+1 文件 / +17 例**（新 `routes/settings-tenancy.test.ts` 端到端 + `storage/db.test.ts` 40→**45** + `learning/quiz-search.test.ts` 1 例升级为归属锁）；全量 **128 文件 / 1827 例（1826 绿 + 1 skipped）0 failed**；契约 `TENANCY-SPEC` §8.2。★ 改前这四张表**全是全局表**：`app_settings` 是**全局写口**（A 改出题配比／回答方式／配图开关／搜索 key，**全站所有人跟着变**）、`user_stats` 里的 `xp`（**A 和 B 的 XP 是同一个数**）、`daily_summaries` 的 `PK(day)`（**B 直接读到 A 的今日总结**，改前注释已把这条记为「已知缺口」）、`daily_activity` 的 `PK(day,type)`（**A、B 同一天聊天直接撞主键**）。★★ 归属值取 `''`（**不是 NULL**，与 v29 `providers` **刻意相反**：v29 的 NULL = 平台通道 = **所有人可见**，本批的 `''` = 无主行 = **谁都看不见**——同一个「没有主人」在两组表里可见性相反，取值就必须不同）。★★ **读写两侧同口径（本批最关键的一处纠正）**：初稿按 `ownerFilter` 的「`null` 就不加条件」写读侧，开工时发现这四张表的读形状是**单值或聚合**（`.get()` / `SUM`）⇒ 豁免过滤后库里多行会返回**任意一行** ⇒ 全部改成 `ownerForWrite`。判据一句话：**读形状是「一批行」的用 `ownerFilter`（`sessions`/`user_memory`），是「一个值」的用 `ownerForWrite`**。★ 4 处 `ON CONFLICT(key)` / `ON CONFLICT(day)` **必须跟着主键改**（SQL 是字符串，**编译期零信号，不改就是运行时 500**）；`events/bus.ts` 四个活动类事件的 `ownerId` 改**必填**（订阅者按人记 XP，漏传是静默少算）⇒ 6 个发布点被 `tsc` 逼着回答「这笔算在谁头上」。★ 另扩展 `_probe/claim-legacy.mjs`：**补上 M2b 漏掉的 `user_memory`**（那个洞此前没有任何认领通道）＋ v30 四张表，并预置 M2d-2/M2d-3 的表（表或列不存在即跳过） |
| **M2d-2** | `term_library` / `term_domain` 归主（`UNIQUE(term,domain)` → 含 `owner_id`；`name` 单列 PK → `PK(owner_id,name)`）+ `term_mention_log` 口径对齐（现 `NULL`=无主，且 `mentionTrend` 在未登录时会**跨用户求和**） | ✅ **已交付** 2026-09-18（本批 v0.2.63） | 单测 **+1 文件 / +18 例**（新 `routes/terms-tenancy.test.ts` 8 例端到端 + `storage/db.test.ts` 52→**62**）；全量 **129 文件 / 1845 例（1844 绿 + 1 skipped）0 failed**；契约 `TENANCY-SPEC` §8.2。★ 改前三处洞：`term_library` 的 `UNIQUE(term,domain)`（**A、B 各有一条「牛顿第二定律/物理」就互相并入**）、`term_domain` 的 `name` 单列 PK（**A 建了「物理」B 就建不了**）、`term_mention_log` 口径（未登录 `mentionTrend` **跨用户求和**）。★★ **开工实测逮到一处契约没写的跨用户串台**：`term-review.ts` 的 `SCOPE_JOIN` 原先**只按 `name` 连领域** ⇒ `term_domain` 归主后 A 的词条会读到 **B 的同名领域开关** ⇒ 连接条件补 `owner_id`。★ **`general` 改每用户懒建 + 读路径补建**（老板拍板）：写入侧已有 `INSERT OR IGNORE` 自动登记，领域接口首次访问再补建一份自己的 ⇒ 领域 Tab 恒有一格 `general`（观感不变）。★ **拆文件（被 server ≤400 红线逼出，照仓规不压注释）**：`terms.ts` → `term-usage.ts`；`tidy.ts` → `tidy-plan.ts`；`term-review.ts` → `term-review-scope.ts`；`study-flow-run.ts` 的 `emitTermNodes` 移入 `knowledge-graph.ts`。★ 连带必改：`terms.ts` 两处 `ON CONFLICT(term,domain)` 同步改复合；`ToolCtx.ownerId` 由可选改**必填**（逼出 `chat/flow.ts` 一处漏传——此前工具里的词条增删改查全落无主行）。|
| **M2d-3** | 其余表：`quiz_bank`/`quiz_stats`/`quiz_notes`、`flow_def`/`flow_run`(+step)、`knowledge_node`/`knowledge_edge`——**加列即可**（主键是全局唯一 uuid，不会跨用户撞键） | ✅ **已交付** 2026-09-19（本批 v0.2.67，迁移 v33 八处加列） | 单测 **+1 文件 / +9 例**（新 `routes/quiz-tenancy.test.ts` 6 + `db.test.ts` 65→68）；全量 **137 文件 / 1948 例 0 failed**；契约 `TENANCY-SPEC` §8.2。★ **收口一条已上线旧洞**：`knowledge_node`/`knowledge_edge` 改前无任何归属过滤（A 的知识图谱 B 能看见，M2d-1 普查发现），归属过滤随本批上线并由 e2e 锁死（B 的 stats 零图、邻域 404）。★ 子表 `flow_step`/`flow_edge` 刻意不加列（恒经 def_id 触达，归属随 flow_def 传递，同 messages 口径）。★ 两条口径照抄 M2d-1/M2d-2：归属值 `''`（无主谁都看不见）、读写两侧 `ownerForWrite`。★ 按仓规拆 `recordAnswer` 入新 `quiz-record.ts`（quiz.ts 加归属触 400 红线）。★ 未做（挂 §6 观察）：`scenario_demo` 与 `evolution_*` 未加列（契约未点名；触达均经已归主父表的闸门） | 契约 `TENANCY-SPEC` §8.2。★ 加列虽简单，但**消费点最多**（`quiz_*` 25 个文件、`flow_*` 9 个）；★ 已知一处**已上线的旧洞**：`knowledge_node`/`knowledge_edge` 目前无任何归属过滤 ⇒ **A 的知识图谱 B 能看见**（本批普查发现，随 M2d-3 收口） |
| **M2 收口** | 与 `SB_REQUIRE_AUTH=1` **同批开**（在此之前强开会让全站既有接口一律 401） | ✅ **已交付** 2026-09-19（本批 v0.2.70：§3.1 五条闸门落码/锁死 + `auth-require.test.ts` 8 例生产 env 组合验证；`_probe/claim-legacy.mjs` 已支持全表认领，真实库 `users` 当前为空、无孤儿可认领——首注册后随时可跑） | `AUTH-SPEC` §3；`TENANCY-SPEC` §9 |
| **M3** | **部署上线**：Docker + Caddy 自动 TLS + 海外域名 + 持久卷 + 备份 + 发信域 DNS | 🟡 **进行中 · 站点已实机运行（未验收）**（2026-09-21 **二次订正**：原记「未实机验证」**已被生产实测证伪**；一次订正原记「⬜ 未开工」） | ★★ **站点自 2026-09-19 起已实机运行**——2026-09-21 生产实测（`curl`／`nslookup`，逐条见 §3.3 证据列）：`https://11wand.com` **200** · `Server: Caddy`（自动 TLS 已签发）· `/api/health` → `{"ok":true}` · DNS 解析 `107.172.96.209`、NS 在 **Cloudflare** · 发信子域 SPF/DKIM/DMARC 全在位。★ **与 `README.md` §当前状态、`DEPLOY.md` 文首「线上状态」一致——滞后的一直是本文档**。★ **仍不记「已交付」**：§3.2 配置闸门与 §3.4 人工验收未过、§3.5 两条实测缺口未收口。见下方 §3 闸门清单。★ **已落码**（随 `a197916` 入库；**非本会话产出**）：`Dockerfile` 19 行 / `docker-compose.yml` 59 行 / `.dockerignore` 12 行 / `tools/docker/{daemon.json, 部署切换手册.md}`；另 `packages/web/public/`（`favicon.svg` · `robots.txt` · `.well-known/security.txt`）+ `index.html` 加 favicon link 一行。★★ **为什么仍不记「已交付」**（v0.1.8 重写此段——原写「四类闸门**一条都没动**——无 Caddy 配置、无域名/DNS」，**已被 v0.1.8 的生产实测证伪**）：闸门**没有全过**——§3.2 配置闸门**未逐项验在位**（并发 N 仍是占位值 8）、§3.4 人工验收闸门**三项一项未做**（邮件三档送达是「本线最大风险」且只能真人判）、§3.5 两条实测缺口未收口。⇒ 按本表 §2 的判据「**状态的判据是「有实测证据」，不是「代码写完了」**」：**部署动作已有证据、验收动作没有**，故停在「已实机运行（未验收）」。★ 原段两条至今仍成立、且正是 §3.5 缺口的来源：容器层**不在 vitest／gates 扫描范围**（零自动化测试）、`tools/docker/部署切换手册.md` **自述 v0.1「全部步骤未实机验证」**。★ 该批**未写任何台账行**（test-plan 与 CHANGELOG 双缺），本行与 §5 订正条由**收尾会话代补**（CHANGELOG 同步代补一行、验证列如实写「未实测」） |

★ **状态的判据是「有实测证据」，不是「代码写完了」**。一行状态要能指向一个可复现的命令或一份登记。

---

## 3. ★★ 部署闸门（M3 的前置清单——**v0.1.8 起本节的答案已变**：站点已在线运行，「什么时候能上线」不再是问题，本节回答的是「**在线状态下还差哪些**」）

**M3 不是"跑个 docker compose"**。下面四类闸门**全部**满足才能上线；缺任何一条，
上线后的症状都不是"功能少一点"，而是**全站不可用或静默烧钱**。

> ★★ **v0.1.8 实测补充（先读这条，再看下表）**：站点**已经在线上跑**（2026-09-19 起，证据见 §2 `M3` 行与 §3.3），
> 所以下面四类闸门此刻的含义**不是「能不能上线」，而是「在线状态下已经过了哪些、还差哪些」**。现状一览：
>
> | 闸门 | 现状 | 一句话 |
> |---|---|---|
> | §3.1 代码 | **5 / 5 ✅** | 五条 env 已落码并锁死；「配套」（Caddy 下发 XFF、env 置 1）在部署时验 |
> | §3.2 配置 | **未逐项验**（**v0.1.9 起 6 项**） | 部署时逐项验在位；并发 N 仍是占位值 8；★ **新增 3 个 `SB_PLATFORM_*`**（不配 = 全站 AI 不可用，且症状会被误读成「用户自己没配模型」） |
> | §3.3 外部依赖 | **6 / 6 ✅**（v0.1.8 实测订正） | 域名／DNS／发信域／服务器全部就位；唯 2 项从外部不可验，见该表「未验」列。★ **v0.1.9 加一句限定**：这六项证明的是「**机器 + 域名 + 证书能用**」，**不是「正式版在跑」**（见 §3.5 第 3 条），两者不能互相背书 |
> | §3.4 人工验收 | **0 / 3 ⬜** | 邮件三档送达 + 渲染层目检 + 真机端到端，判定权在老板 |
> | §3.5 实测缺口 | **0.5 / 3 已收口** | ① 线上构建落后 ⬜ ② 容器化缺 `Caddyfile` **🟡（Docker 侧 ✅ / Caddyfile 侧 ⬜）** ③ **线上跑的代码不是本地正式版 ⬜**（v0.1.9 新增） |

### 3.1 代码闸门（必须在仓库里改完——**✅ 5/5 已落码**，2026-09-19 M2 收口；剩余「配套」在 M3 部署时验）

| # | 项 | 不改的症状 | 位置 |
|---|---|---|---|
| 1 | ✅ **已落码** `SB_TRUST_PROXY`（2026-09-19 M2 收口，`index.ts` `app.set('trust proxy',…)`；`'1'`=信任最近 1 跳）+ 接线锁 `auth-require.test.ts` | 不配则 `req.ip` 恒为反代自身地址 ⇒ 全站共用一个 IP 桶（限流退化为全站上限） | `index.ts`；`routes/auth.ts#clientIp` 注释；`AUTH-SPEC` §4.6 末段。★ M3 部署还差配套：Caddy 下发 `X-Forwarded-For` + env 置 1 |
| 2 | ✅ **已落码** `SB_ALLOWED_ORIGINS`（2026-09-19 M2 收口，逗号分隔进白名单；localhost 兜底正则保留）+ e2e 锁 `auth-require.test.ts` | 部署域名不在白名单 ⇒ 所有 POST 403 ⇒ 全站瘫痪 | `security.ts`。★ 配错 env 的症状是「合法域名也 403」——宁可显式失败不放宽 |
| 3 | ✅ **已落码** `SB_HOST`（2026-09-19 M2 收口，缺省 `127.0.0.1` 行为不变） | 容器内只监听回环 ⇒ Caddy 连不上后端 | `index.ts` |
| 4 | ✅ **已落码** `SB_COOKIE_SECURE`（M1.5 已实现；M2 收口补 e2e 锁：登录 Set-Cookie 必含 Secure） | HTTPS 站点下发非 Secure cookie | `auth/middleware.ts` |
| 5 | ✅ **已落码 + 强开过测**（开关 M1 已建；M2 收口以 `routes/auth-require.test.ts` 8 例在生产 env 组合下全站验证：401/豁免/登录链路/白名单/Secure/接线锁） | 不强制鉴权 ⇒ 任何人可读写全站数据 | `AUTH-SPEC` §3；`index.ts` |

### 3.2 配置闸门（部署时必须逐项验在位，不是"配了就算"）

| # | 项 | 误配的症状 |
|---|---|---|
| 1 | `RESEND_API_KEY` **与** `SB_MAIL_FROM` **两个都配齐** | 只配一个 ⇒ 走**控制台兜底**（只打日志不真发信）⇒ **「点了发送、界面说成功、邮箱里永远没有」**，服务端零错误、日志里也看不出异常——**最难查的一类故障** |
| 2 | `SB_DATA_DIR` 指向**持久卷** | 容器重建即丢全部数据 |
| 3 | `SB_PORT` / 反代目标端口一致 | Caddy 502 |
| 4 | **`SB_UPSTREAM_SITE_MAX_CONCURRENT`（全站免费通道并发封顶 N）按业务值配**；队列上限 `SB_UPSTREAM_SITE_QUEUE_MAX`（缺省 20） | 沿用**占位值 8** 上线 ⇒ 全站免费通道只允许 8 路并发，用户一多就收到「当前免费通道繁忙，请稍后再试」。★ 但**不配不会静默出错**（有队列上限兜底、会**明确拒绝**而不是卡死），比上面三条轻——**N 的具体数值待老板给值**（`TENANCY-SPEC` §8.1.3.1） |
| 5 | ★★ **`SB_PLATFORM_API_KEY` + `SB_PLATFORM_BASE_URL` + `SB_PLATFORM_MODEL` 三个配齐**（v39「零配置」批新增，2026-09-21） | ★★ **不配 = 全站 AI 完全不可用**，且**症状极难从表面判断**：数据库里平台 provider 的 `api_key` 是**空串**、`role_bindings` 八个角色的 `model` 也**全空**（种子如此）⇒ `routeRole()` 返回的 `model` 为空 ⇒ `roleReady()` 报「该角色还没绑定模型」，用户看到的是**「模型没配」而不是「服务端没配」**，会被误当成用户自己没设置。★ **这正是线上 2026-09-21 的真实状态**（SSH 实测：`providers.openai-default.api_key = ''`、`role_bindings` 八行 `model` 全空、`messages` 表 3 条 user / **0 条 assistant** ⇒ 从没有人成功拿到过一次回复）。★ 三条**各自独立**回落数据库值（配一条不等于全用 env）；**空串与纯空白视同未配**。★ 凭据只从 env 读的**设计目的**就是「**不让用户看到 key**」——数据库里那把 key 永远是空的，任何拖库／接口泄漏都拿不到它 |
| 6 | **部署前跑一次迁移**（v39 起含 `platform_usage`） | 迁移**不跑 = 平台通道一被调用就 500**（`no such table: platform_usage`）。★ 与上面几条不同，这条**会明确报错**、不会静默；但注意 `platform_usage` 是**用量账本**，清空它等于**把所有人的额度洗回 250**（`TENANCY-SPEC` §8.1.3.3），故**不要**用「删库重建」的方式排障 |

### 3.3 外部依赖闸门（钱与资质，**只能老板推进**）—— **v0.1.8：6 / 6 ✅ 已达成（2026-09-21 生产实测）**

> ★ **订正说明**：本节 v0.1.7 及以前**六项全记 `⬜`**（域名未购、DNS 未配……），与生产实际**完全不符**。
> 下表状态列已按 **2026-09-21 实测**逐项改写，证据列写**可复现的命令与返回值**——照本表 §2 判据
> 「状态的判据是「有实测证据」」，这些项**有证据**，故转 ✅；从外部**验不了**的两项如实留 `⬜ 未验`。

| # | 项 | 状态 | 实测证据（2026-09-21，可复现） |
|---|---|---|---|
| 1 | **域名** | ✅ **已购并解析** | 主站 **`11wand.com`** → A 记录 **`107.172.96.209`**（`nslookup 11wand.com`）。★★ **口径偏离需老板裁**：`AUTH-SPEC` §0.3 的拍板是「**主站 `.xyz` 不动**，发信另购 `.com` 双域」，而**实测主站用的是 `.com`**、发信走它的**子域** `send.11wand.com` ⇒ 落地形态与 §0.3 第 1 条不一致（§0.3 第 2 条「发信与主站分离」的精神由**子域隔离**满足）。本条**只如实记录，不代改契约** |
| 2 | 注册商 **Cloudflare Registrar**（$10.44/年，注册=续费同价） | ✅ **DNS 侧已确认** | 权威 NS 实测为 `alice.ns.cloudflare.com`（`nslookup -type=SOA 11wand.com`）⇒ 域名确用 Cloudflare 托管。★ 「注册商就是 CF Registrar 本身」**从外部无法独立确认**（NS 相同也可能是 CF 免费 DNS + 别家注册商），列 `⬜ 未验` 部分 |
| 3 | **DNS 顺序**：先 **DKIM + SPF** 完成域名验证 → **验证通过后**再加 `_dmarc`，且**先 `p=none`** | ✅ **三项全在位** | **SPF**（发信子域）：`nslookup -type=TXT send.11wand.com` → `v=spf1 ip4:52.3.252.119 ip4:44.222.39.36 ip4:199.249.231.0/24 ~all`；**DKIM**：`resend._domainkey.11wand.com` → `p=MIGfMA0GCSq…`（RSA 公钥在位）；**DMARC**：`_dmarc.11wand.com` → **`v=DMARC1; p=none;`** ⇒ **恰是本节要求的「先 p=none，调通再收紧」**。★ 退信通道亦在位：`send.11wand.com` 的 **MX** → `feedback.forge.rmta.net` |
| 4 | ★ **CNAME 记录绝不能开 Cloudflare 代理（橙色云朵）**——官方原话：会阻止验证完成 | ⬜ **未验（外部不可判）** | 该项只能在 **Cloudflare 控制台内**看代理开关状态，`dig`／`nslookup` 判不出来。★ **间接反证（不能当证据）**：DKIM/SPF/DMARC 已在位 ⇒ 域名验证**已完成过**，说明当时未踩这个坑。**仍需老板在控制台确认一次**并记录 |
| 5 | Resend 发送区选**东京**（`ap-northeast-1`）。⚠️ 区域只决定发信路由，**账号数据仍存美国**；改区域要删域名重加 | ✅ **域已接入** / ⬜ **区域未验** | `send.11wand.com` → CNAME → **`send.forge.rmta.net`**（`nslookup -type=TXT send.11wand.com`）⇒ 发信域**已由 Resend 接管**。★ **发送区（东京／其他）从 DNS 完全判不出来**，只能登录 Resend 看，列 `⬜ 未验` |
| 6 | 服务器（海外，**不备案** ⇒ 国内发信厂商全部出局） | ✅ **已就位并对外服务** | A 记录 `107.172.96.209`（海外 IP）；`curl -sSI https://11wand.com` → **`HTTP/1.1 200 OK`** + **`Server: Caddy`** + `Alt-Svc: h3=":443"` ⇒ **Caddy 自动 TLS 已签发、HTTP/3 已启用、站点对外可达**；`curl -s https://11wand.com/api/health` → **`{"ok":true}`** ⇒ 后端进程在跑 |

### 3.4 人工验收闸门（★ **判定权在老板，AI 不自行判定通过**）

| # | 项 | 为什么只能人判 |
|---|---|---|
| 1 | ★★ **邮件三档送达**：QQ / 163 / Gmail 各发一封，确认落**收件箱**而非垃圾箱（记录实测日期） | 这是**本线最大的风险**——服务器在海外，而目标用户是国内学生、QQ 邮箱占绝大多数。同类调研共识：家宽/廉价 VPS 直发**基本被直接拦截**。**AI 无法代替收件人** |
| 2 | **前端渲染层目检**：`AccountBox` 三条通道（登录/注册切换、验证码行与倒计时、切换链接）、`AccountTrigger` 收起态、注册表单 | 本仓**无 jsdom**，渲染层无自动化测 ⇒ 行数门禁是绿的但**门禁看不见渲染** |
| 3 | 真机端到端主观确认（含 AI 忠实度类，如资料模式"资料段 N"的可溯源口径） | 属体验/忠实度类，**AI 不自行验收** |

★ **v0.1.8 实测注（对第 1 项）**：发信域的**前置条件已全部满足**（SPF + DKIM + DMARC 在位，见 §3.3 #3）——
这**不等于送达成功**，但意味着若三档实测被判失败，**原因大概率在「海外 IP 信誉」而不在「DNS 没配」**，
排查方向应据此收敛。**判定权仍只在老板**（AI 无法代替收件人）。

---

### 3.5 实测缺口（**v0.1.8 新增；v0.1.9 起含 SSH 生产实测** —— §3.1~§3.4 四类闸门之外另行逮到的缺口）

| # | 缺口 | 症状与证据 | 状态 |
|---|---|---|---|
| 1 | **线上构建落后本地约 25 个提交** | 线上静态页 `Last-Modified: Sun, 20 Sep 2026 03:26:01 GMT`（= 北京 09-20 11:26）⇒ 对应提交 **`404374d`**（`docs: README 展示面刷新 + 新增《部署手册》`，09-20 11:23）。**特征串判定**（下载线上 bundle 后 grep，minify 不改字符串字面量）：`fetch_image` **0** · `.rv-body` **0** · `今日张数` **0** · `复习目标` **0** · `上游原文` **0** · `词条旅程` **0** ⇒ **缺** v0.2.89（`fetch_image` 图片搬运）／v0.2.91（看图失败说人话）／v0.2.92（半屏裁切）／v0.2.94（自定义复习目标）／v0.2.95（hero 对战演示）。★ 旁证：线上 `/robots.txt`、`/favicon.svg`、`/.well-known/security.txt` 三路径**均返回 `Content-Type: text/html`**（被 SPA 兜底吞掉）⇒ 这三个文件**在线上根本不存在**；线上 `index.html` 也**缺 `<link rel="icon">`**。★★ **2026-09-21 SSH 实测确认**：服务器上源码部署时刻 = **2026-09-20 11:18:40**、`dist` 产物 = **11:26:01** —— 与上面按 `Last-Modified` 反推的 `404374d`（09-20 11:23）**对得上**；且服务器上**没有** `Dockerfile`／`docker-compose.yml`／`DEPLOY.md`／`tools/` ⇒ 容器化那批文件**从未上过服务器** | ⬜ **未收口** |
| 2 | **容器化路径缺 `Caddyfile`**（★ **Docker 守护进程侧已收口，Caddyfile 侧仍未**） | `docker-compose.yml` 的 `caddy` 服务写 `volumes: - ./Caddyfile:/etc/caddy/Caddyfile:ro`，但 `find . -iname "Caddyfile*" -not -path "*/node_modules/*"` **返回空** ⇒ 挂载一个**不存在的文件**，`docker compose up` 起不来。★ 现网跑的是 **systemd 形态**（`DEPLOY.md`：`systemctl reload caddy`、`MemoryMax=400M`、`ExecStart` 为 `npx tsx src/index.ts`）——**两条路不是同一条**，容器化那批文件目前**没有任何一条被执行过**。★ 连带：`tools/docker/daemon.json` 的 JSON 合法性此前已记为有问题（见 `docs/metrics.md` §线上运行态快照），与 `docker.service` 状态同批核。★★ **2026-09-21 SSH 实测的两个新事实**：**① Docker 起不来的真根因已定位并修好** —— `/etc/docker/daemon.json` 里写的**不是 JSON**，而是一行 shell（`host.docker.internal:host-gateway systemctl restart docker`）⇒ 守护进程每次启动即解析失败。已按仓内 `tools/docker/daemon.json` 改写为合法 JSON（日志驱动 `json-file` + `max-size 10m` + `max-file 3`），坏文件备份在 `/etc/docker/daemon.json.bak-2026-09-21`；`systemctl enable --now docker` 后 `docker info` 报 Engine **29.8.1** / compose **v5.5.1**、`docker run --rm hello-world` 通过、`NRestarts=0`、`/var/lib/docker` 38M。**② Caddyfile 在服务器上是存在的**（`/etc/caddy/Caddyfile`，529B，含 `try_files {path} /index.html` 的 SPA 兜底）⇒ 缺的只是**仓里那份**；容器化路径要真跑通仍得把它落进仓（**本批未做**——现网跑的是 systemd 形态，改它等于同时动两条路，且老板本批只要求「把 docker 弄成能正常用」） | 🟡 **部分收口**（Docker ✅ / Caddyfile ⬜） |
| 3 | ★★ **线上跑的代码不是本地正式版**（老板 2026-09-21 明示：「现在这个真正运行的代码不是正式的代码，现在本地的才是正式版」） | 与第 1 条**同源但性质更重**：第 1 条说的是「落后 25 个提交」，这条说的是**部署形态本身** —— 线上是**手工 `npx tsx src/index.ts` + systemd** 起的一份**旧源码**，而本地 `main` 才是被验收过的正式版。★ 后果（★ 这条最要紧）：**线上一切「已达成」的实测（§3.3 那六项）证明的是「这套机器 + 域名 + 证书能用」，不是「正式版在跑」** —— 两者不能互相背书。★ 2026-09-21 SSH 实测旁证：服务器上**没有** `Dockerfile`／`docker-compose.yml`／`DEPLOY.md`／`tools/`，源码目录只有线上那一份；另实测发现 `/root/ai_stack/` 与 `ai_task.service`／`ai_task.timer`（每分钟跑一次的无关任务）与本站无关，已备份后移除 | ⬜ **未收口**（★ 下一步就是它：把本地正式版部署上去） |

★ **这三条的共性**：都属于「**文件在库里 ≠ 动作被执行过**」，与 §2 那条判据同源。
它们的价值不在「要修」，而在**说明 §3.3 的「已达成」是靠生产实测拿到的、容器化那批文件并不是它的证据**。

★ **v0.1.9 补一条方法论记账（本次 SSH 实测新学到的）**：第 3 条是**读文件永远读不出来**的那类缺口——仓库里的 `README.md`、`DEPLOY.md`、`docs/metrics.md` **全都写着「已上线」**，台账也记「站点已实机运行」，**四份文档口径一致、没有任何冲突**，而**真实情况是「旧代码在跑」**。⇒ 判据必须落在**生产机的实际状态**（源码部署时刻、`schema_version`、`providers.api_key` 是否为空），不能落在任何一份文档上。

---

## 4. ★ 汇报纪律（老板 2026-09-18 指令，**本文档存在的理由**）

> **老板原话**：「每次完成一个"上线"这个规划的小任务时，你就汇报一次总体进展情况，
> 这个你要存进记忆也要录入"上线"的相关文档中」。

⇒ 固化为三条：

1. **触发点**：完成一个**上线阶梯**（§2 表）里的批次时——**不是**完成每个 commit、也不是产品功能里程碑。
2. **汇报内容**（缺一不可）：① 本批做完了什么 + **实测证据**；② **总体进展**（§2 表里到哪了 / 还剩几批）；③ **离上线还差什么**（§3 四类闸门里哪些已满足）；④ 下一步与阻塞项。
3. **落盘**：同一次动作里**同时**更新 §2 状态 + 在 §5 追加一条汇报；并写入 `AGENTS.md` / `CHANGELOG.md` / `test-plan.md` 的常规登记面。

★ 这条纪律与 `AUTH-SPEC §7` 的关系：§7 是**计划**（要做什么），§5 是**台账**（做到哪了）。改计划去 §7，记进展来 §5。

---

## 5. 进展汇报（按时间倒序，**只追加**）

### 2026-09-21 12:10 — v39「零配置平台通道 + 每 5 小时 250 次配额」批落码 ＋ SSH 生产实测（**不占「总体进展汇报」编号** —— 本批**不在** §2 的 12 个上线阶梯批次内，与 09:56 那条同性质）

**① 本批做完了什么 ＋ 实测证据**

老板拍板：「**api 哪里就改成默认零配置**，但是有限额，那就是**每 5 小时限定 250 次 ai 调用**，然后**直接使用我的 key 的额度**，但是**不让用户看到**。如果默认模型额度不够或者用户想自己配置模型，那就**按常规通道去配置模型**」。

- **零配置**：平台通道的 `apiKey`／`baseUrl`／`model` 改从 **env** 读（`SB_PLATFORM_API_KEY`／`SB_PLATFORM_BASE_URL`／`SB_PLATFORM_MODEL`），**数据库里平台行那把 `api_key` 永远是空的** ⇒ 这是「不让用户看到」的落点（`getProviders()` 本来就不返回 `api_key`，key 留在 env 后任何拖库／接口泄漏都拿不到）。★ **只对平台通道生效**——BYOK 用户自己的 key／地址／模型一个都不许被顶掉（顶掉就是「你配了自己的 key，花的却是平台的钱」）。
- **限额**：**每用户每 5 小时 250 次上游调用**，计数单位是**每次上游请求**而非每轮对话（一轮实际产生 1~3 次：主链回复 ＋ `extractTerms` ＋ `compactIfNeeded`）。**迁移 v39** 建 `platform_usage(owner_id, ts)` ＋ 索引；★ **落库而非进程内 `Map`**——次数是滚动窗口累计量，放进程内则每次部署都把用户额度洗回 250（本仓 09-20 一天重启 4 次）。
- **保底通道**：超限时**引到既有 BYOK 通道**（自建 provider ＋ 角色绑定，M2c 已建好，本批不改它）；错误文案指向「去设置页配自己的模型」而不是一句无用的「稍后再试」。
- **实测证据**：`npm run test` 全量 **189 文件 / 2622 passed + 1 skipped（0 failed，共 2623）**；`npm run lint`（tsc×3 ＋ eslint `--max-warnings 0`）全绿；`gates` 全绿（**189 个测试文件全部在 test-plan.md 成行**）。本批 **+1 文件 / +32 例**（新 `llm/platform-quota.test.ts` **21** ＋ `llm/router.test.ts` 14→**21** ＋ `storage/db.test.ts` 71→**75**）。★ **自查抓到一处真回归**（默认兜底路径把绑定表里的 `model` 丢了，已修并立锁）＋ **两处测试自查**（边界用例自己算错、排队项被 `await` 成死锁，都是**用例错不是实现错**）。
- **同批的运维动作（SSH 到生产机）**：① 移除与本项目**无关**的 `/root/ai_stack/` 与 `ai_task.service`／`ai_task.timer`（每分钟跑一次；**已先备份**到 `/root/ai_stack-backup-20260921-112332.tar.gz`）；② ★ **修好 Docker** —— 真根因是 `/etc/docker/daemon.json` 里写的**不是 JSON 而是一行 shell**（`host.docker.internal:host-gateway systemctl restart docker`），守护进程每次启动即解析失败；已按仓内 `tools/docker/daemon.json` 改写为合法 JSON（坏文件备份 `/etc/docker/daemon.json.bak-2026-09-21`），`systemctl enable --now docker` 后 `docker info` 报 Engine **29.8.1**／compose **v5.5.1**、`docker run --rm hello-world` 通过、`NRestarts=0`。

**② 总体进展**

★ **未变**：上线阶梯仍是 **12 个批次 12 个已交付**（**代码面**全部完成）。本批是**阶梯之外的新功能批**（老板在阶梯交付后追加的需求），**不改变**「什么时候能上线」的答案。

**③ 离上线还差什么**

- §3.1 代码闸门 **5/5 ✅**（未变）
- §3.2 配置闸门 **未逐项验**，★ **v0.1.9 从 4 项增至 6 项** —— 新增的三个 `SB_PLATFORM_*` **不配 = 全站 AI 完全不可用**，且**症状会被误读成「用户自己没配模型」**（`roleReady()` 报的是「该角色还没绑定模型」）。★ **这正是线上当前的真实状态**（见下）
- §3.3 外部依赖闸门 **6/6 ✅**，★ **但本批给它加了一句限定**：这六项证明的是「**机器 + 域名 + 证书能用**」，**不是「正式版在跑」**
- §3.4 人工验收闸门 **0/3 ⬜**（未变，判定权在老板）
- §3.5 实测缺口 **0.5/3 已收口**（新增第 3 条，见下）

**④ 下一步与阻塞项**

- ★★ **阻塞项（本批实测新发现，最要紧的一条）**：**线上跑的代码不是本地正式版**（老板原话：「现在这个真正运行的代码不是正式的代码，现在本地的才是正式版」）。SSH 实测旁证：线上源码部署时刻 = **2026-09-20 11:18:40**、`dist` = **11:26:01**，服务器上**没有** `Dockerfile`／`docker-compose.yml`／`DEPLOY.md`／`tools/` ⇒ **线上一切「已达成」的实测证明的不是「正式版在跑」**。**下一步就是把本地正式版部署上去**（见 §3.5 第 3 条）。
- ★ **另一条实测发现**：线上 AI **完全不可用**——`providers.openai-default.api_key = ''`、`role_bindings` 八个角色 `model` **全空**、`messages` 表 **3 条 user / 0 条 assistant**（从没有人成功拿到过一次回复）。**本批的零配置正是为它准备的解**，但**必须等三个 `SB_PLATFORM_*` 写进服务器 `.env` 才生效**。
- ★ **待老板给**：① 平台 key 的**具体值**（老板已答「我发给你，你写进服务器」）；② `SB_UPSTREAM_SITE_MAX_CONCURRENT` 的业务值（现仍占位 8）。
- ★ **仍未收口**：§3.5 第 1 条（线上构建落后）与第 2 条的 Caddyfile 侧（服务器上其实有 `/etc/caddy/Caddyfile`，529B，含 SPA 兜底；**缺的只是仓里那份**）。

### 2026-09-21 09:56 — ★★ 订正：§3.3 外部依赖闸门 6 项 `⬜` → `✅`（**非批次汇报**，不占编号）

**这不是第 8 次总体进展汇报** —— 本批**没有任何批次完成**，订正的是**一类从未被实测过的状态**，故不占「汇报」编号（与 v0.1.7 那条同性质）。

**起因**：老板问「studentbuddy 的正式上线开放的规划已经做到多少了」。读本文档发现 §3.3 六项全 `⬜`、§2 `M3` 写「未实机验证」，
但 `README.md` §当前状态与 `DEPLOY.md` 文首**都写着「已上线」** ⇒ **两处口径直接冲突，靠读文档判不出谁对**。

**订正方法（不推断，实测）**：对生产环境跑 `curl` / `nslookup`，逐条取可复现证据（命令与返回值全部写进 §3.3 证据列）。
结论：**站点自 2026-09-19 起已实机运行**，§3.3 六项**全部达成** ⇒ `README` / `DEPLOY` 是对的，**滞后的一直是本文档**。

**订正四处**：
1. **§3.3 表**：6 项 `⬜` → `✅`（其中 2 项**从外部验不了**、如实留 `⬜ 未验`：CF 代理开关、Resend 发送区）；
2. **§2 `M3` 行**：🟡「未实机验证」→ 🟡「**站点已实机运行（未验收）**」——原表述**已被实测证伪**；
3. **§3 开头新增「四类闸门现状一览表」**——站点已在跑 ⇒ 闸门的含义从「能不能上线」变成「还差哪些」；
4. **§3.5 新增两条实测缺口**（线上构建落后 25 提交 / 容器化路径缺 `Caddyfile`）。

**为什么仍不记「已交付」**：§3.2 配置未逐项验（并发 N 仍是占位值 8）、§3.4 人工验收 **0/3**、§3.5 两条未收口。
按 §2 判据「状态的判据是「有实测证据」」——**部署动作有证据、验收动作没有**，故停在「已实机运行（未验收）」。

**总体进展（数字未变）**：上线阶梯仍 **12 / 12 已交付**；测试基线 **186 文件 / 2575 例**。
⇒ 变的不是「做到多少」，而是**「做到多少」这个数第一次有了生产实测背书**。

**离上线还差（重排后）**：
1. **§3.4 人工验收 `0/3`（★ 最重）** —— 邮件三档送达（本线最大风险，**AI 无法代替收件人**）、渲染层目检、真机端到端；
2. **§3.5 缺口 2 条** —— 线上构建落后（重部署一次即可收敛）、容器化路径缺 `Caddyfile`；
3. **§3.2 配置** —— 部署时逐项验在位；★ **并发 N 的业务值仍待老板给**（占位值 8）；
4. **`AUTH-SPEC` §0.3 口径需老板裁** —— 拍板是「主站 `.xyz` 不动」，实测主站是 `.com`（见 §3.3 #1）。

**★ 一条自我提醒（本次漂移的根源）**：§3.3 这类「**外部世界的事实**」被写进台账后**从无人复核**——
代码类状态每批都在动、有人改；而「域名买了没」这种外部事实，**没有任何机制会提醒它过期**。
⇒ 建议此后凡「外部世界事实」行，**必须带实测日期**（本次已照此写）。

### 2026-09-21 01:20 — ★ 状态订正：M3「⬜未开工」→「🟡进行中·未实机验证」（**非批次汇报**）

**这不是第 8 次总体进展汇报** —— 本批**没有任何批次完成**，只是把一处**状态快照漂移**订正掉，故不占「汇报」编号。

**订正的事**：`Dockerfile` / `docker-compose.yml` / `.dockerignore` / `tools/docker/{daemon.json, 部署切换手册.md}` / `packages/web/public/*` 这批容器化文件，由**并行会话**在 2026-09-20 00:43~00:47 落盘，随收尾提交 `a197916` 入库；但该会话**未更新本文档**（§2 与 §5 均未动），于是 §2 里 M3 一行仍写着「⬜ 未开工」——**文件已在库里、文档说没开工**。

**为什么不改成「已交付」**：§3 四类闸门**一条都没动**（无 Caddy 配置、无域名/DNS），`部署切换手册.md` **自述 v0.1「全部步骤未实机验证」**，且容器层**不在 vitest／gates 扫描范围**。按本表 §2 的判据「**状态的判据是「有实测证据」，不是「代码写完了」**」——**只记「落码」，不记「交付」**。

**顺带订正的第二处**：§3.1 标题原写「**当前全部未做**」，而表内 5 行**已全是 ✅ 已落码**（v0.1.6 第 7 次汇报已确认）⇒ 标题改为「**✅ 5/5 已落码**，剩余「配套」在 M3 部署时验」。★ 两处同源：**状态写进多个位置后，改一处就得连带刷新其余**（§0.11 防漂移）。

**总体进展（未变）**：上线阶梯仍是 **12 个批次 12 个已交付**（**代码面**全部完成）；M3 的「进行中」指的是**容器化落码**，**不改变**「什么时候能上线」的答案——仍是 §3.2 配置闸门逐项验 + §3.3 域名/DNS/服务器 + §3.4 人工验收单，**这三项只能老板推进**。

**代补声明**：本条与 §2 `M3` 行的订正、§6 变更记录行，均由**收尾会话（2026-09-21）代补**；本会话**未代改该批任何实现文件**，只动台账。

### 2026-09-19 11:05 — 第 7 次总体进展汇报

**本批（刚完成）**：**M2 收口** —— ① §3.1 五条代码闸门收口：新 env `SB_TRUST_PROXY`（反代信任，缺省不信任——盲信 X-Forwarded-For 等于把限流键交给可伪造头）、`SB_ALLOWED_ORIGINS`（部署域名白名单，localhost 兜底保留）、`SB_HOST`（监听地址可配，缺省行为不变）；`SB_COOKIE_SECURE` 与 `SB_REQUIRE_AUTH` 的接线 M1 已预埋，本批以生产 env 组合强开过测。② 新 `routes/auth-require.test.ts` **8 例**：未登录 401/豁免清单公开/登录链路照旧/部署域名过门/外部域名 403/Set-Cookie 带 Secure/trust proxy 接线锁。③ 全量 check 137→**138 文件 / 1956 例 0 failed**。

**总体进展**：上线阶梯 **12 个批次 12 个已交付**。**代码面全部完成**——「什么时候能上线」的答案从「还差哪批代码」变成「§3.2 配置逐项验 + §3.3 域名 + §3.4 人工验收」。

1. **M3 部署** —— 按 §3.2 配置闸门逐项验在位（**不是"配了就算"**：`RESEND_API_KEY`+`SB_MAIL_FROM` 两个都配齐才真发信；`SB_TRUST_PROXY=1` 与 Caddy 下发 XFF 是同一配置的两处故障）；§3.3 域名/DNS/服务器；§3.4 人工验收单。

**⇒ 结论：代码闸门全部通过，待部署资源就绪即可上线。** 只能老板推进：域名/DNS/服务器与邮件送达判定、外层并发 N 的业务值。

### 2026-09-19 10:45 — 第 6 次总体进展汇报

**本批（刚完成）**：**M2d-3 其余表归主** —— 迁移 v33 给八张「主键是全局 uuid」的表加列 `owner_id`（`NOT NULL DEFAULT ''`），**并收口一条已上线旧洞**（`knowledge_node`/`knowledge_edge` 此前无任何归属过滤）。代码与文档已完成并过测，本批随并行会话的工具生态批次一并通过 `npm run check`（137 文件 / 1948 例 0 failed）。

**总体进展**：上线阶梯 **12 个批次里 11 个已交付**（M1 / M1.5 / M1.6 / M2a / M2b / M2c×2 / M2d-1 / M2d-2 / M2d-3 / `retryAfterMs` 收口）。**M2d 全部完成**。

1. **M2 收口** —— 契约 §9 验收判据逐条过（跨用户不可见 e2e 汇总、claim-legacy 全表认领、§3.1 五条代码闸门：`trust proxy` / `ALLOWED_ORIGINS` / `HOST` / `SB_COOKIE_SECURE` / `SB_REQUIRE_AUTH`）；
2. **M3 部署** —— 依赖 §3.2 配置闸门（外层并发 N 待老板给业务值）与 §3.3 域名/服务器/邮件送达。

**⇒ 结论：仍不能部署上线。** 代码面只剩「M2 收口」一批 + §3.1 五条闸门；§3.3 域名未购、§3.4 人工验收未做。

**下一步**：M2 收口。★ 两件只能老板推进：全站并发封顶 N 的业务值、域名/DNS/服务器与邮件送达判定。

### 2026-09-19 00:55 — 第 5 次总体进展汇报

**本批（刚完成）**：**M2d-2 词条库归主** —— `term_library` / `term_domain` 随**迁移 v31** 重建（`UNIQUE(term,domain)` → `UNIQUE(owner_id,term,domain)`；`name` 单列 PK → `PK(owner_id,name)`），`term_mention_log` 口径对齐（`NULL` → 空串）。**代码与文档已完成并过测，尚未提交**。

- **这一批解决了什么**：三处洞都是「**别人能改到你 / 你俩互相污染**」：① `term_library` 的 `UNIQUE(term,domain)` —— **A、B 各有一条「牛顿第二定律/物理」就会互相并入**（后写的那条把先写的并掉）；② `term_domain` 的 `name` 单列 PK —— **A 建了「物理」B 就建不了**（撞主键）；③ `term_mention_log` 未登录时 `mentionTrend` **跨用户求和**（契约 §8.2 已点名）。
- **★★ 开工实测逮到一处契约没写的跨用户串台**：`term-review.ts` 的 `SCOPE_JOIN` 原先**只按 `name` 连领域**（`term_domain.name` 当时是全局唯一 PK，所以够用）⇒ `term_domain` 归主后，A 的词条会读到 **B 的同名领域开关**（复习范围判定串台）。连接条件补 `owner_id` 后修复。**这条如果只按契约清单做，会漏。**
- **`general` 改每用户懒建 + 读路径补建**（老板 2026-09-18 拍板）：写入侧已有 `INSERT OR IGNORE INTO term_domain` 会自动登记，再让领域接口**首次访问时补建**一份自己的 `general` ⇒ 领域 Tab 恒有一格 `general`（与改前观感一致）；孤儿行的 `general` 留给本地单人模式。
- **★ 拆文件（被 server ≤400 红线逼出，照仓规「拆文件、不压注释」）**：`terms.ts` → `term-usage.ts`（`countUsage`）；`tidy.ts` → `tidy-plan.ts`（`parseTidyBlock`/`normalizeTidyPlan`/`renameDomainTx`）；`term-review.ts` → `term-review-scope.ts`（范围开关/清零/打卡）；`study-flow-run.ts` 的 `emitTermNodes` 移入 `knowledge-graph.ts`（产物层归位）。前两者由 `terms.ts` **re-export** 保住导入面（多处 `vi.mock("../learning/terms.js")` 指着它）。
- **让 `tsc` 逼着回答「这笔算在谁头上」**：`ToolCtx.ownerId` 由可选改**必填** ⇒ 实测逮到 `chat/flow.ts` 的主对话路径**此前根本没传**（工具里的词条增删改查全落**无主行**，主人登录后看不到且全程不报错）。
- **实测**：`tsc`×3 + eslint 全绿；**全量 vitest 129 文件 / 1845 例（1844 passed + 1 skipped）0 failed**；gates 全绿。★ **新锁非空转已按 §7 三次取证**（读侧去掉归属 → 2 例红；`UNIQUE` 退回单用户口径 → 5 例红含 e2e 撞键；`renameDomainTx` 批量 UPDATE 去掉归属 → 恰好 1 例红；均恢复后全绿）。

**总体进展**：上线阶梯 **12 个批次里 10 个已交付**（M1 / M1.5 / M2a / M2b / M1.6 / `retryAfterMs` 收口 / M2c 归属改造 / M2c 两层并发闸门 / M2d-1 设置与反馈环归主 / **M2d-2 词条库归主**）。

**离上线还差**：
1. **M2d-3** —— 其余表加列（`quiz_*` / `flow_*` / `knowledge_*`，主键是全局唯一 uuid、**不会跨用户撞键**，故只需加列，是 M2d 里风险最低的一批）；
2. **M2 收口** —— 与 `SB_REQUIRE_AUTH=1` 同批开（在此之前强开会让全站既有接口一律 401）；
3. **M3 部署** —— 且 §3.1 的**全部 5 条代码闸门一条未做**（`trust proxy`、`ALLOWED_ORIGINS`、`HOST`、`SB_COOKIE_SECURE`、`SB_REQUIRE_AUTH`）、§3.2 配置、§3.3 外部依赖（**域名未购**）、§3.4 人工验收。

**⇒ 结论：现在还不能部署上线。** 最快路径是「M2d-3 → M2 收口 → M3」，且 §3.1 那 5 条代码闸门必须在 M3 之前落完。

**下一步**：M2d-3（`quiz_*` / `flow_*` / `knowledge_*` 加列）。★ 加列型迁移要**幂等**（`ALTER TABLE ... ADD COLUMN` 不幂等 ⇒ 回放迁移链的 7 处退列点要同步补）；归属值仍取**空串**、聚合读仍用 `ownerForWrite`（M2d-1/M2d-2 两条口径照抄）。

**阻塞项**：无代码阻塞。⚠️ **两条待老板推进**（与第 3/4 次相同）：① **全站并发封顶 N**（现为**占位值 8**，`SB_UPSTREAM_SITE_MAX_CONCURRENT`；队列上限 20）——契约明说「结构可先落、外层留 config」；② §3.3 的域名/DNS/服务器与 §3.4 的邮件送达判定（只能老板推进）。

---

### 2026-09-18 23:20 — 第 4 次总体进展汇报

**本批（刚完成）**：**M2d-1 设置与反馈环归主** —— `app_settings` / `daily_activity` / `daily_summaries` / `user_stats` 四张「**约束会跨用户撞键**」的表随**迁移 v30** 重建。**代码与文档已完成并过测，尚未提交**。

- **这一批解决了什么**：这四张表改前**全是全局表**，而且**没有一条是"只是有点脏"，每一条都是别人能改到你**：`app_settings` 是**全局写口**（A 改出题配比／回答方式／配图开关／搜索 key，**全站所有人跟着变**）、`user_stats` 里存 `xp`（**A 和 B 的 XP 是同一个数**）、`daily_summaries` 的 `PK(day)`（**B 直接读到 A 的今日总结**）、`daily_activity` 的 `PK(day,type)`（**A、B 同一天聊天直接撞主键**）。
- **判据不是习惯，是约束本身**（本批定的可复用口径）：一张表若它的 `PRIMARY KEY` / `UNIQUE` **包含一个全局取值列**（`key` / `name` / `day` / `(term, domain)`），它天然只允许「全站一条」⇒ 多用户必然互撞 ⇒ **必须重建**；主键是 uuid 的 ⇒ **加列即可**。
- **★★ 归属值取 `''`（无主 = 谁都看不见），不是 `NULL`**——与 M2c 的 `providers`/`role_bindings` **刻意相反**（那里的 `NULL` = **平台通道 = 所有人可见**，因为免费额度是给全体用户用的）。同一个「没有主人」的处境，两组表的**可见性相反** ⇒ 取值必须不同。★ 若图省事统一，某个通用过滤函数会把「孤儿不可见」**静默变成「全站可见」**（反向泄露）。
- **★★ 本批最关键的一处纠正（读写两侧同口径）**：初稿按 `ownerFilter` 的「`null` 就不加条件」写读侧，开工时发现这四张表的读形状是**单值或聚合**（`SELECT value … .get()` 与 `SUM(count)`）⇒ 豁免过滤后库里多行时 `.get()` 会返回**任意一行**，未登录请求读到**某个用户**的设置/总结/XP（**静默串台**）。⇒ 全部改成 `ownerForWrite(ownerId)`。**判据一句话：读形状是「一批行」的用 `ownerFilter`（`sessions`/`user_memory`），是「一个值」的用 `ownerForWrite`**。
- **SQL 是字符串，编译期零信号**：4 处 `ON CONFLICT(key)` / `ON CONFLICT(day)` **必须跟着主键一起改**——不改就是**运行时 500**，而 `tsc` 与现有单测**都不会红**。
- **让 `tsc` 逼着回答「这笔算在谁头上」**：`events/bus.ts` 四个活动类事件的 `ownerId` 改**必填**（订阅者按人记 XP，漏传是**静默少算**）⇒ 6 个发布点被编译器逐一点名；`searchWeb` / `loadQuizMix` / `loadQuizImage` / `loadAnswerStyle` / `saveProviderKey` 等 12 处签名加 `ownerId`，14 个调用点穿透。
- **★ 顺手补上一个已上线的旧洞**：`_probe/claim-legacy.mjs` 此前**只覆盖 `sessions`**，M2b 的 `user_memory` 孤儿行口径（`user_id = ''`）**没有任何认领通道**——您本地的画像数据在登录后已经看不见了。本批把 `user_memory` ＋ v30 四张表一并纳入认领脚本，并预置 M2d-2/M2d-3 的表（表或列不存在即跳过）。
- **实测**：`tsc`×3 + eslint 全绿；**全量 vitest 128 文件 / 1827 例（1826 passed + 1 skipped）0 failed**；gates 全绿。★ **新锁非空转已按 §7 取证**（摘掉 `loadQuizMix` 的读侧归属 → 跨用户用例当场红；写侧归属写死成无主行 → 跨用户用例红；均恢复后全绿）。

**总体进展**：上线阶梯 **12 个批次里 9 个已交付**（M1 / M1.5 / M2a / M2b / M1.6 / `retryAfterMs` 收口 / M2c 归属改造 / M2c 两层并发闸门 / **M2d-1 设置与反馈环归主**）。

**离上线还差**：
1. **M2d-2** —— `term_library` / `term_domain` 归主（**本仓最大的单片**：~80 个 SQL 点、12 个文件）＋ `term_mention_log` 口径对齐；
2. **M2d-3** —— 其余表加列（`quiz_*` / `flow_*` / `knowledge_*`，消费点最多）；
3. **M2 收口** —— 与 `SB_REQUIRE_AUTH=1` 同批开（在此之前强开会让全站既有接口一律 401）；
4. **M3 部署** —— 且 §3.1 的**全部 5 条代码闸门一条未做**（`trust proxy`、`ALLOWED_ORIGINS`、`HOST`、`SB_COOKIE_SECURE`、`SB_REQUIRE_AUTH`）、§3.2 配置、§3.3 外部依赖（**域名未购**）、§3.4 人工验收。

**⇒ 结论：现在还不能部署上线。** 最快路径是「M2d-2 → M2d-3 → M2 收口 → M3」，且 §3.1 那 5 条代码闸门必须在 M3 之前落完。

**下一步**：M2d-2（`term_library` / `term_domain` 归主）。★ 开工即按本批定下的两条口径走（归属值 `''`；聚合读用 `ownerForWrite`），并按 §0.15 的新策略**先写"不同语义必须落不同桶"的锁再写实现**。

**阻塞项**：无代码阻塞。⚠️ **两条待老板推进**（与第 3 次相同）：① **全站并发封顶 N**（现为**占位值 8**，`SB_UPSTREAM_SITE_MAX_CONCURRENT`；队列上限 20）——契约明说「结构可先落、外层留 config」；② §3.3 的域名/DNS/服务器与 §3.4 的邮件送达判定（只能老板推进）。

---

### 2026-09-18 22:05 — 第 3 次总体进展汇报

**本批（刚完成）**：**M2c 两层并发闸门** —— 免费通道「每用户 2 + 全站封顶 N」（`llm/upstream-gate.ts`，零迁移零新文件）。**代码与文档已完成并过测，尚未提交**。

- **这一批解决了什么**：原闸门只有「同一 `baseUrl` 最多 2 路并发」——它是**技术**闸门（防上游打爆），**既不知道「谁在问」，也不知道「全站一共能开几路」**⇒ 事实上的"全站 2"：谁多用一点，别人就排队。本批按契约 §8.1.3.1 改成**两层**：内层 `(baseUrl, 请求者)` 每用户 2、外层**只约束平台通道**全站封顶 N + 队列上限 M。
- **配额怎么到闸门（本批最关键的结构选择）**：`routeRole` 用 `bindQuota()` 把 `{ownerId, platform}` **绑在它返回的 `adapter` 上** ⇒ **15 个 `target.adapter.chat(...)` 调用点零改动、且结构上不可能漏传**（拿不到 `routeRole` 的返回值就没有 `apiKey`/`baseUrl`，也就发不出请求）。★ 这与 §8.1.4 否掉 ALS 的理由**不矛盾**：ALS 的危险在于**有"缺省"这个状态**（漏设就静默退化成平台通道），绑在返回值上**没有缺省态**。
- **★ 三条"写反了也不报错"的口径**：① 配额里的 `ownerId` 是**请求者**、**不是 provider 的 owner**（免费通道 provider 的 `owner_id` 恒为 `NULL`，写反 ⇒ **所有免费用户挤进同一个容量 2 的桶**，等于白加）；② **外层必须显式按 `platform` 标志判断**——契约原话「BYOK 不同 `baseUrl` ⇒ 天然另一个桶」**只在两家服务商不同时成立**（用户自带 OpenAI key 时 `baseUrl` 与平台完全相同）⇒ **这一条已回写契约 §8.1.3.2**；③ **内层先、外层后**（一个人超额时不占全站名额），且外层失败**必须归还内层槽**（否则该用户的内层槽**永久泄漏**，表现为"他之后永远排不上队"）。
- **外层队列满 ⇒ 明确拒绝**（文案「当前免费通道繁忙，请稍后再试」）——不让用户无限期转圈（契约 §8.1.3.1 第 2 条：**"卡死"比明确拒绝更糟**）。
- **实测**：`tsc`×3 + eslint 全绿；**全量 vitest 127 文件 / 1810 例（1809 passed + 1 skipped）0 failed**；gates 全绿。★ **新锁非空转已按 §7 三次取证**（忽略 owner 分桶 → **6 例红**，含「A 占满不影响 B」；不归还内层槽 → **恰好 1 例红**；`bindQuota` 原样返回 → **3 例红**，含端到端接线锁）。
- **★ 纪律事件（§0.15 收敛计数 3/3 ⇒ 强制换策略）**：自审逮到 `innerKey` 把 `null`（未登录）与 `''`（空串账号）**折叠成同一个桶**——与 v0.2.58 M1.5「最小间隔错用到 IP 桶」、M1.6「作用域被放大到跨用途」**同族**（共同形状＝「把语义不同的两个东西写成同一个值」，且**三次都没有任何运行时错误**）。⇒ 已换策略为「**键的构造只允许经单一入口 + 每类键都配一条"不同语义必须落不同桶"的锁**」，M2d 凡新键**先写锁再写实现**。

**总体进展**：上线阶梯 **11 个批次里 8 个已交付**（M1 / M1.5 / M2a / M2b / M1.6 / `retryAfterMs` 收口 / **M2c 归属改造** / **M2c 两层并发闸门**）。**⇒ M2c 至此全部完成。**

**离上线还差**：
1. **M2d** —— 其余业务表归属（`quiz_*`／`term_*`／`daily_activity`／`flow_*`／**`app_settings` 改每用户**）；
2. **M2 收口** —— 与 `SB_REQUIRE_AUTH=1` 同批开（在此之前强开会让全站既有接口一律 401）；
3. **M3 部署** —— 且 §3.1 的**全部 5 条代码闸门一条未做**（`trust proxy`、`ALLOWED_ORIGINS`、`HOST`、`SB_COOKIE_SECURE`、`SB_REQUIRE_AUTH`）、§3.2 配置、§3.3 外部依赖（**域名未购**）、§3.4 人工验收。

**⇒ 结论：现在还不能部署上线。** 最快路径是「M2d → M2 收口 → M3」，且 §3.1 那 5 条代码闸门必须在 M3 之前落完。

**下一步**：M2d（其余业务表归属 + `app_settings` 改每用户）。

**阻塞项**：无代码阻塞。⚠️ **两条待老板推进**：① **全站并发封顶 N**（现为**占位值 8**，`SB_UPSTREAM_SITE_MAX_CONCURRENT`；队列上限 20）——契约明说「结构可先落、外层留 config」；② §3.3 的域名/DNS/服务器与 §3.4 的邮件送达判定（只能老板推进）。

---

### 2026-09-18 21:40 — 第 2 次总体进展汇报

**本批（刚完成）**：**M2c 归属改造** —— `providers`／`role_bindings` 加归属列 + `routeRole` 第三参 + **14 个消费点全量穿透**（迁移 v29）。**代码与文档已完成并过测，尚未提交**。

- **这一批解决了什么**：把「**谁付模型钱**」从「代码里看不见」变成「数据上可归属」。此前 A 配的 key，**B 聊天会烧 A 的额度**——因为 `routeRole()` 取 provider 时**根本不知道「这轮是谁在问」**。
- **双通道口径**：`owner_id IS NULL` = **平台通道**（免费额度），非空 = 用户自带 key。★ **判据是 `IS NULL` 而不是 `''`**（`''` 会落进「一个不存在的用户」）。
- **读/写判据刻意不同**：读/用 = **平台或自己的**（否则免费通道**绑不上**、功能变装饰）；写/删 = **严格本人**（否则一个用户改平台 provider 会改掉**所有人**的模型）。平台行给登录用户改 → **403**（它确实在他列表里，谎称 404 才是撒谎），别人的行 → **404**（不确认 id 存在）。
- **★ 开工实测逮到一条会静默上线的缺陷**：`PRIMARY KEY (owner_id, role)` 在 SQLite 下**拦不住 `owner_id IS NULL`**（每个 NULL 互不相等）⇒ `seedIfEmpty` 的 `INSERT OR REPLACE` 会退化成普通 INSERT、**每调一次多一条平台绑定**，而 `routeRole` 按行序取第一条 ⇒ **绑定变抽签**（`/roles` 显示一条、实际命中另一条，**且不报任何错**）。已用探针在 SQLite 3.49.2 上实测取证并补平台行部分唯一索引。**这一条如果只靠推演，会直接上线。**
- **实测**：`tsc`×3 + eslint 全绿；**全量 vitest 127 文件 / 1795 例（1794 passed + 1 skipped）0 failed**；gates 全绿（含 127 个测试文件全部在 `test-plan §3` 成行）。★ **新锁非空转已按 §7 取证**：摘掉归属判据 → 跨用户用例当场红 → 恢复 → 10/10 绿。

**总体进展**：上线阶梯 **11 个批次里 7 个已交付**（M1 / M1.5 / M2a / M2b / M1.6 / `retryAfterMs` 收口 / **M2c 归属改造**）。

**离上线还差**：
1. **M2c 余量** —— 两层并发闸门（⬜ 未开工，见 §2 拆行）；
2. **M2d** —— 其余业务表归属（`quiz_*`／`term_*`／`daily_activity`／`flow_*`／`app_settings` 改每用户）；
3. **M2 收口** —— 与 `SB_REQUIRE_AUTH=1` 同批开；
4. **M3 部署** —— 且 §3.1 的**全部 5 条代码闸门一条未做**（`trust proxy`、`ALLOWED_ORIGINS`、`HOST`、`SB_COOKIE_SECURE`、`SB_REQUIRE_AUTH`）、§3.2 配置、§3.3 外部依赖（**域名未购**）、§3.4 人工验收。

**⇒ 结论：现在还不能部署上线。** 最快路径是「M2c 闸门 → M2d → M2 收口 → M3」，且 §3.1 那 5 条代码闸门必须在 M3 之前落完。

**下一步**：M2c 两层并发闸门（**结构先落**，N 待老板给值）。

**阻塞项**：无代码阻塞。⚠️ **两条待老板推进**：① **全站并发封顶 N**（`TENANCY-SPEC` §8.1.3.1）；② §3.3 的域名/DNS/服务器与 §3.4 的邮件送达判定（只能老板推进）。

---

### 2026-09-18 21:10 — 第 1 次总体进展汇报

**本批（刚完成）**：`retryAfterMs` 真机漂移收口 + 冒烟脚本入库（提交 `0c55c3c`）。
- 修掉一条**只在真机可见的契约漂移**：契约与文档都写着 429 带 `retryAfterMs`，但该字段**从未到达过响应体**（`sendCode` 把它丢掉、只抛裸错误码），而**所有单测都是绿的**。是 `_probe/auth-smoke.mjs` 真机第 11 节揪出来的。
- 实测：`tsc`×3 / eslint / gates 全绿；vitest **125 文件 / 1767 例**（1766 绿 + 1 skipped）；真机冒烟 **PASS=62 FAIL=0**，两个独立实例各跑一次。
- 顺手订正 `AUTH-SPEC §2.7` 三处契约↔代码漂移（错误码名不存在 / 执行顺序漏一步 / M1.6 状态陈旧），契约 v0.5.0 → v0.5.1。

**总体进展**：上线阶梯 **10 个批次里 6 个已交付**（M1 / M1.5 / M2a / M2b / M1.6 / `retryAfterMs` 收口）。

**离上线还差**：**M2c（进行中）→ M2d → M2 收口 → M3**，以及 §3.1 的**全部 5 条代码闸门**（`trust proxy`、`ALLOWED_ORIGINS`、`HOST`、`SB_COOKIE_SECURE`、`SB_REQUIRE_AUTH`）、§3.2 配置、§3.3 外部依赖（域名未购）、§3.4 人工验收。

**下一步**：M2c 落码（迁移 v29 + `routeRole` 第三参 + 13 个消费点穿透 + 两层并发闸门）。

**阻塞项**：无代码阻塞。§3.3 的域名/DNS/服务器与 §3.4 的邮件送达判定**只能老板推进**。
⚠️ 一条待老板给值的参数：**全站并发封顶 N**（`TENANCY-SPEC` §8.1.3.1，结构可先落、留 config）。

---

## 6. 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-21 | v0.1.9 | **v39「零配置平台通道 + 每 5 小时 250 次配额」批落码 ＋ SSH 生产实测（**不占「总体进展汇报」编号**——本批不在 §2 的 12 个上线阶梯批次内）**。★ **代码**：平台凭据改从 env `SB_PLATFORM_API_KEY`／`SB_PLATFORM_BASE_URL`／`SB_PLATFORM_MODEL` 读（**数据库里平台行那把 key 永远是空的** ⇒ 落实老板「不让用户看到」）＋ **每用户每 5 小时 250 次**上游调用配额（**迁移 v39** 建 `platform_usage`；★ **落库而非进程内 `Map`**，否则每次部署都把用户额度洗回 250）＋ 超限时引到既有 BYOK 通道。★ **台账同步四处**：① §3.2 配置闸门 **4 → 6 项**（新增三个 `SB_PLATFORM_*` —— **不配 = 全站 AI 完全不可用**，且症状会被误读成「用户自己没配模型」，**这正是线上 09-21 的真实状态**；另加「部署前跑一次迁移」）；② §3.5 **新增第 3 条实测缺口** —— ★★ **线上跑的代码不是本地正式版**（老板 2026-09-21 明示）⇒ **§3.3 那六项「已达成」证明的是「机器 + 域名 + 证书能用」，不是「正式版在跑」，两者不能互相背书**；③ §3.5 第 2 条 **Docker 侧收口**（真根因＝`/etc/docker/daemon.json` 里写的**不是 JSON 而是一行 shell**；已按仓内样本修好并验通 `hello-world`，坏文件备份在 `/etc/docker/daemon.json.bak-2026-09-21`；**Caddyfile 侧仍未收口**——服务器上其实有 `/etc/caddy/Caddyfile`，缺的只是仓里那份）；④ §3.5 新增一条**方法论记账**（第 3 条是**读文档永远读不出来**的缺口：`README`／`DEPLOY`／`metrics`／本台账四份口径一致地写着「已上线」，而真相是「旧代码在跑」⇒ 判据必须落在**生产机的实际状态**）；另 §3 闸门一览表、§3.3 限定语、版本头 v0.1.8→**v0.1.9**。★ **同批运维**：移除服务器上与本项目无关的 `/root/ai_stack/` 与 `ai_task.service`／`ai_task.timer`（**已先备份**）。★ 实测：`npm run test` **189 文件 / 2623 例（2622 绿 + 1 skipped）**、`npm run lint` 全绿、`gates` 全绿；本批 **+1 文件 / +32 例** |
| 2026-09-21 | v0.1.8 | **★★ §3.3 外部依赖闸门实测订正（非批次汇报，不占编号）**。六项 `⬜` → `✅`，依据＝**对生产环境的实测取证**（`curl` / `nslookup`，命令与返回值写进 §3.3 证据列）：`https://11wand.com` 200 · `Server: Caddy` · `/api/health` ok · DNS 在 Cloudflare · 发信子域 SPF + DKIM + DMARC 在位 ⇒ **站点自 2026-09-19 起已实机运行**。★ 同时订正 §2 `M3` 行——🟡「未实机验证」→ 🟡「**站点已实机运行（未验收）**」（原表述被证伪），并重写该行「为什么只记进行中」那段（原文写「四类闸门一条都没动、无 Caddy 配置、无域名/DNS」，已证伪）；★ §3 开头**新增四类闸门现状一览表**；★ **新增 §3.5「实测缺口」**（① 线上构建落后约 25 提交，对应 `404374d`／② `docker-compose.yml` 挂 `./Caddyfile` 而仓里无此文件，容器化路径一跑就挂）；★ §3.4 补实测注（发信域前置条件已满足 ⇒ 若三档失败，排查方向应收敛到「海外 IP 信誉」而非「DNS 没配」）。★ **漂移根源登记**：§3.3 属「外部世界的事实」，写进台账后**从无复核机制**，故本次起此类行**必须带实测日期**。★ 未改任何契约文件（`AUTH-SPEC` §0.3「主站 `.xyz` 不动」与实测 `.com` 的口径偏离**只记录、请老板裁**） |
| 2026-09-21 | v0.1.7 | **★ M3 状态订正（非批次汇报）**。§2 `M3` 行 ⬜未开工 → 🟡**进行中·未实机验证**（容器化那批文件已随 `a197916` 入库，但无 Caddy、无域名/DNS、手册自述未实机验证 ⇒ 按「有实测证据」判据**只记落码不记交付**）；§3.1 标题「当前全部未做」→「✅ 5/5 已落码」（表内 5 行早已全 ✅，标题落后于表）；§5 追加订正条并**明确声明「这不是第 8 次汇报」**（无批次完成，不占编号）。★ **两处同源漂移**，正是 §0.11「状态写进多个位置后改一处就得连带刷新其余」的实例。★ 由**收尾会话代补**（对方只落文件、未写任何台账行）；**未代改该批任何实现文件** |
| 2026-09-19 | v0.1.6 | **第 7 次总体进展汇报（M2 收口交付）**。§3.1 五条闸门全部转 ✅（三个新 env + 两个既有开关强开过测）；§2 `M2 收口` 行转 ✅；§5 追加第 7 次汇报。★ 12 个批次 12 个已交付——代码面全部完成，上线只剩配置闸门（M3 部署时验）、域名与人工验收 |
| 2026-09-19 | v0.1.5 | **第 6 次总体进展汇报（M2d-3 其余表归主交付）**。§2 `M2d-3` 行转 ✅（迁移 v33 八处加列 + `knowledge_node`/`knowledge_edge` 旧洞收口）；§5 追加第 6 次汇报。★ M2d 三批至此全部完成——12 个批次 11 个已交付，代码面只剩 M2 收口 + §3.1 五条部署闸门 |
| 2026-09-19 | v0.1.4 | **第 5 次总体进展汇报（M2d-2 词条库归主交付）**。§2 `M2d-2` 行转 ✅ 并写入实测证据；§5 追加第 5 次汇报。★ 本批最有价值的一条是**开工实测逮到契约没写的跨用户串台**——`term-review.ts` 的 `SCOPE_JOIN` 只按 `name` 连领域，`term_domain` 归主后 A 的词条会读到 B 的同名领域开关；★ 同时登记本批的四处拆文件（`term-usage`/`tidy-plan`/`term-review-scope` + `emitTermNodes` 移入 `knowledge-graph`），都是被 server ≤400 红线逼出的、照仓规不压注释 |
| 2026-09-18 | v0.1.3 | **第 4 次总体进展汇报（M2d-1 设置与反馈环归主交付）**。§2 把原 `M2d` 一行**拆成三行**（M2d-1 ✅ / M2d-2 ⬜ / M2d-3 ⬜）——此前一行混装会让「M2d 做完了吗」有三个答案；`M2c（两层并发闸门）` 补提交号 `8b43518`；§5 追加第 4 次汇报。★ 本批把「**判据不是习惯，是约束本身**」写进 §2 证据列（PK/UNIQUE 含全局取值列 ⇒ 必须重建；主键是 uuid ⇒ 加列即可）；★★ 并把一条**反向泄露风险**固化：本批归属值取 `''`（无主 = 谁都看不见），与 M2c 的 `NULL`（平台通道 = 所有人可见）**刻意相反**——若图省事统一，会把「孤儿不可见」静默变成「全站可见」 |
| 2026-09-18 | v0.1.2 | **第 3 次总体进展汇报（M2c 两层并发闸门交付 ⇒ M2c 全部完成）**。§2 `M2c（归属改造）` 补提交号 `9bb5f88`、`M2c（两层并发闸门）` 转 ✅；§5 追加第 3 次汇报。★ 本批把闸门从「一层技术闸门」改成「内层业务 + 外层业务」，并把三条**"写反了也不报错"**的口径写进 §2 证据列；★ 同时登记一条纪律事件——**§0.15 收敛计数达 3/3 ⇒ 强制换策略**（本批 `innerKey` 把 `null` 与 `''` 折叠成一个桶，与 M1.5／M1.6 两次同族：共同形状＝「把语义不同的两个东西写成同一个值」，三次都没有任何运行时错误）⇒ 换为「键的构造只允许经单一入口 + 每类键都配一条"不同语义必须落不同桶"的锁」 |
| 2026-09-18 | v0.1.1 | **第 2 次总体进展汇报（M2c 归属改造交付）**。§2 把原 M2c 一行**拆成两行**——`M2c（归属改造）` ✅ 已交付 / `M2c（两层并发闸门）` ⬜ 未开工（此前一行混装会让「M2c 做完了吗」有两个答案）；§5 追加第 2 次汇报。★ 同时把「开工实测逮到复合 PK 在 SQLite 下不拦 NULL」写进 §2 证据列——**状态判据是「有实测证据」**，这条正是「推演会漏、实测才逮到」的实例 |
| 2026-09-18 | v0.1.0 | 新建。老板指令「每次完成一个上线小任务就汇报一次总体进展，并录入上线文档」⇒ 本文档作为**唯一进展台账**；同时把「什么时候能上线」的答案固化为 §3 四类部署闸门。真相源指针见文首（**不复制**各契约的批次定义，避免第二真相源） |
