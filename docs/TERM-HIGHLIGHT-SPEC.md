# AI 回复词条高亮 + 悬浮卡（Term Highlight）功能契约 v1.0

> 版本：v1.0 | 状态：[WIP] | 更新：2026-09-19
> 适用仓库：`Desktop\studentbuddy-v2`（monorepo：server / shared / web）
> 铁律来源：`AGENTS.md`（六条 ADR + 工程红线）；本文是「先改契约再改码」的载体。
> 方案对比 demo（老板据此拍板）：`Desktop\优秀demo参考\studentbuddy-词条高亮悬浮卡-方案demo.html`

## 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-09-19 | v1.0 | 首次立契：交互形态取 demo 方案 **G**（下划线两态：hover 速览 + click 固定）+ 数据路线 **①**（前端索引匹配） |

---

## §0 原提示词溯源（§0.4）

- **[P-101]** 老板：「现在studentbuddyv2我需要加一个功能,也是词条系的功能,当ai的新回复出现了用户词条库出现过的词条时,这个回复中的这个词条就会被加一个特殊的方框或者下划线,然后用户鼠标如果移动到对应的位置就会弹出来一个小卡片,这个卡片显示的就是这个词条的相关信息,这个相关信息就会和词条库联系上了,我表述的应该很清楚了,你先明确一下,然后出一个方案对比demo来供我选择,具体的demo表现形式在我桌面的优秀demo参考中有,你去看一下」
- **[P-102]** 老板（看 demo 后拍板）：「可以,就按你推荐的组合来实现」

**方案来源与取舍**：demo 里给了 7 个形态方案（A 虚线下划线 / B 圆角方框 / C 淡底纹+角标 / D 点击锚定卡 / E 右侧联动面板 / F 首现强调策略 / G 组合）与 3 条数据路线（① 前端索引 / ② 服务端标注回传 / ③ 混合）。[P-102] 选定 **G + ①**，即本契约的实施范围。其余方案**不进本契约**，也不留开关——形态是拍板结果，不是待选项（要改另立契约）。

---

## §1 目标与服务哪一环

**一句话**：词条库的匹配能力**已经存在**（`learning/term-usage.ts` 每轮回复完成后就在扫命中、+1 `usage_count`、落提及流水），但**只写进数据库、没上屏幕**——用户在对话里看不见「我刚才这段回答里用了哪些他学过的词条」。本功能把这条关系**显性化**：正文里的词条标出来，悬停看词条卡，点击能回写词条库。

- 服务闭环：**忆（M3 词条库）的读侧闭环**。词条库此前只有「词条库页」一个可见面；本功能让它在**对话现场**可见可用（ADR-1 自检通过，不新建第六环）。
- 入口闸门（§0.14）：三个月后被删的唯一可能是忆域整体换记忆机制 ⇒ 白写率低。
- 设计范式：取「**术语链接（term link）**」这一成熟做法（词典/维基/wiki 内链的既有直觉），不取它所属产品的包袱（不做词条页跳转即唯一出口、不做自动弹窗）。

---

## §2 现状与代码实证

| 事实 | 位置（文件:符号） |
|---|---|
| 匹配规则**已实现**（term + aliases、大小写不敏感、英文按词边界防子串误报、中文子串） | `packages/server/src/learning/term-usage.ts` · `replyHitsKey` |
| 命中后**已在写库**（`usage_count + 1` + `last_used_at` + 提及流水，同事务） | 同上 · `countUsage` |
| 正文渲染的**唯一入口**（流式与历史都走它） | `packages/web/src/features/chat/Markdown.tsx` · `InlineNodes` 的文本分支（`default: n.v`） |
| 词条字段 | `packages/web/src/lib/api.ts` · `TermItem` |
| 全量词条入口（无参 = 全量） | `GET /api/terms`（`api.terms.list()`） |
| 复习状态判定（唯一实现，前后端共用） | `packages/shared/src/ebbinghaus.ts` · `computeReviewState` / `reviewStateLabel` |
| 复习范围开关（词条级） | `api.terms.scopeTerm(termId, enabled)`（`PUT /api/terms/review/scope`） |
| 跨页入口（词条库） | `packages/web/src/app/App.tsx` · `openTerms(keyword)` |

⇒ **本功能的工程量集中在「呈现层」**，匹配与统计一行都不用重造（§0.9 利用 AI 已有能力）。

---

## §3 交互形态（老板拍板 = demo 方案 G）

**一个组件的三个状态**，不是三个组件：

```
① 默认态（阅读中）  只有下划线：首现＝实线＋极淡底纹；复现＝虚点线
② 悬停态（想扫一眼）速览卡：词名 + 领域 + 释义 + 已用次数（锚定该词条，移开即走）
③ 点击态（想深入）  完整卡：＋别名 ＋复习状态 ＋动作按钮（点击后固定，点空白/× 关闭）
```

- ★ **为什么是两态而非二选一**：需求里藏着两个不同强度的诉求——「扫读时别打扰我」与「我要能点进去看全甚至操作」。参考 `COACH-SPEC` 的「一个组件两种状态」先例（胶囊↔抽屉），折叠态低打扰、展开态高承载。
- ★ **首现强调的口径：同一个渲染块内首次出现**（块 = 段落 / 列表项 / 引用行 / 表格单元格）。
  **为什么不做「整条回复内首现」**：那需要每个块知道自己在全文中的字符偏移，而 `parseBlocks` 只返回块、不返回偏移；为它改块协议（`lib/markdown.ts`）的收益不抵复杂度。块内首现已能消除长回复的主要视觉噪声（同一段里同一词出现 3 次不再变成一排框）。
- ★ **移动端没有 hover**：点击态是天然路径（tap 即弹完整卡），不需要为移动端单独做一套形态。
- 高亮元素必须是 `<span>`（不是 `<a>`）：它不是导航，是**信息浮现**；用 `<a>` 会误导用户以为会跳页，且污染「链接」的可访问性语义。

---

## §4 匹配口径（唯一事实源）

★ **规则上提到 `packages/shared/src/term-highlight.ts`，服务端与前端共用一份**。理由与 `doc-rag.ts` 常量双写、`ebbinghaus.ts` 判定双写同源：规则写两份，必然出现「服务端统计命中了 3 个、正文只标出 2 个」这种当场露馅的不一致。

| 规则 | 口径 | 为什么 |
|---|---|---|
| 匹配键 | `term` + `aliases`，**大小写不敏感** | 别名是 tidy 合并的产物（`TERM-TIDY-SPEC` §7.3 已定：别名在回复中出现照样计数），呈现侧必须同口径，否则「统计里有、屏幕上没有」 |
| 含拉丁字母的键 | `(?<![a-z0-9])…(?![a-z0-9])` **词边界** | 防子串误报：`let` 不该命中 `outlet`（既有实现已验证） |
| 纯中文键 | 子串匹配 | 中文无词边界概念 |
| 同一位置多个键命中 | 取**更长**者 | 防长词被短词截断（`机器学习` vs `机器`） |
| 匹配失败/空文本 | 返回空数组，不抛错 | ADR-4 失败隔离：高亮是次级功能，不能影响正文渲染 |

★ **服务端 `replyHitsKey` 改为调用 shared 的 `textHitsKey`（行为逐字等价）**——这是本次唯一碰服务端统计链路的改动，故必须由既有 `term-usage` / `terms` 测试锁住等价性。

---

## §5 数据路线：前端索引匹配（路线 ①）

```
TermIndexProvider（对话页挂载）
  → api.terms.list() 拉全量词条
  → 建索引：key(小写) → TermItem，并按长度降序缓存合并正则
  → 正文渲染时 findTermHits(text, keys) 一次扫描拿全部命中
```

**为什么不走服务端标注回传（路线 ②）**（记账，防将来重复讨论）：

1. **流式要求逐字即时高亮**——服务端 `countUsage` 在**回复完成后**才跑，路线 ② 只能等整段答完才上色，与「边出字边看」冲突；
2. **历史消息也要能高亮**——路线 ② 要么落「命中快照」（新增迁移 + 与词条变更失同步），要么每次回放重算（等于又回前端）；
3. **不改 SSE 契约**——本仓契约先行，加事件类型的成本远高于收益。

**代价与处置**：① 规则必须抽 shared（§4，已做）；② 词条量大时前端匹配有开销 → 合并正则**按索引缓存**（不是每次渲染重编译），且高亮挂在既有 `stableCut` 增量结构上（`stable` 块的命中结果随块 memo 复用，只有尾块每帧重扫）；③ 卡片数据**每次现取当前词条状态**，不做快照（用户改了释义/合并了别名，卡片跟着变）。

---

## §6 卡片内容与动作

| 档位 | 字段 | 来源 |
|---|---|---|
| 速览卡 | `term` / `domain` / `definition` / `usage_count` | `TermItem` |
| 完整卡 | ＋`aliases` ＋复习状态（`computeReviewState` → `reviewStateLabel`） | `TermItem` + `shared/ebbinghaus` |

**动作（只做真实存在的接口，不做假按钮）**：

| 动作 | 调用 | 说明 |
|---|---|---|
| 纳入复习 / 移出复习 | `api.terms.scopeTerm(id, enabled)` | 按当前 `review_in_scope` 决定方向；★ 纳入会**清零进度**（老板拍板），故反馈文案必须说出来 |
| 打开词条库 | `onOpenTerms(term)`（App 的 `openTerms`） | 带词条名直达词条库搜索（复用既有跨页入口） |

⚠️ **「标记已掌握」不在本契约**：现有接口里没有它（复习推进只有 `POST /api/terms/:id/review {remembered}`，语义是「复习一次」而非「免复习」）。demo 里那个按钮是形态示意，**落地时必须砍掉或先立接口契约**——不做没有接口的按钮。

**词条不在复习范围时**：完整卡显示「未纳入复习范围」并把主按钮显示为「纳入复习」；已在范围内则显示复习状态 + 「移出复习」。

---

## §7 不做（范围红线）

- **不做高亮开关设置项**：首版不做（形态是拍板结果）。若实测视觉噪声不可接受，再单独立项。
- **不改 SSE 契约**、**不新增 HTTP 路由**、**不新增迁移**（本功能零 DB 改动）。
- **不在代码块 / 行内 `code` 内高亮**：代码里出现词条名（如 `let`）属于语法，标出来是污染。
- **不做「整条回复内首现」**（见 §3 口径说明）。
- **不做服务端命中清单回传**（见 §5）。
- **不碰 `usage_count` 统计口径**：高亮是纯读侧呈现，不因「用户看见了」而计数（计数仍只由回复文本命中驱动）。

---

## §8 模块与红线

| 文件 | 职责 | 实际行数（落地后实测） |
|---|---|---|
| `packages/shared/src/term-highlight.ts`（新） | 匹配规则唯一事实源：`textHitsKey` / `findTermHits` / `createTermMatcher` / `TermKey` / `TermHit` | **185**（≤400 ✓） |
| `packages/shared/src/term-highlight.test.ts`（新） | 规则回归（边界/大小写/别名/重叠/空值/一致性/匹配器复用） | **199**（24 例） |
| `packages/server/src/learning/term-usage.ts` | `replyHitsKey` 删除、改调 shared（**净减 3 行**） | 73 → **70** |
| `packages/web/src/features/chat/term-index.tsx`（新） | `TermIndexProvider` + `useTermIndex`（拉全量、建索引、缓存正则） | **96**（≤300 ✓） |
| `packages/web/src/features/chat/TermCard.tsx`（新） | 卡片两态（速览 / 完整） | **113**（≤300 ✓） |
| `packages/web/src/features/chat/TermText.tsx`（新） | 正文切分 + 高亮 span + hover/click 交互 + portal 卡片 | **191**（≤300 ✓） |
| `packages/web/src/features/chat/term-highlight.css`（新） | 样式（全用 tokens.css 变量，**禁内联 style**） | **184** |
| `packages/web/src/features/chat/Markdown.tsx` | `InlineNodes` 文本分支走 `TermText` | 249 → **257**（≤300 ✓） |
| `packages/web/src/app/App.tsx` | 挂 `TermIndexProvider`（复用既有 `openTerms`） | → **294**（≤300 ⚠️ **贴线**：下次改它之前先看要不要拆） |

★ **`ChatView.tsx` 最终零改动**——Provider 挂在 App 层（`{view === 'chat' && <TermIndexProvider>…}`），
ChatView 不必知道这件事。这是接线点选择带来的直接收益：改一处 App，而不是穿透
`ChatView → MessageRow → Markdown` 三层 props。

★ 行数红线是**硬门禁**（`tools/gates`）：server/shared 非 web 包一律 ≤400，web 组件 ≤300。**贴线时按仓规拆文件，不压注释换行数**。

---

## §9 测试清单（§0.8 强制）

| 文件 | 用例要点 |
|---|---|
| `shared/term-highlight.test.ts`（新） | 英文词边界（`let` 不命中 `outlet` / `letdown`，命中独立 `let`）；大小写不敏感（`Closure`/`closure`）；中文子串（`闭包` 命中 `闭包捕获`）；别名命中且回传**主词条名**；同位置重叠取更长（`机器学习` vs `机器`）；跨位置重叠不重复计数；空文本/空词条表返回空；无 `aliases` 字段不报错 |
| `server/learning/terms.test.ts`（既有） | **不新增用例，但必须全绿**——它是 `countUsage` 的等价性回归锁（规则上提后行为不得漂） |
| `web/features/chat/TermText.test.tsx`（新） | 命中词条渲染成 `span.term-hl`；无命中时渲染纯文本且**不产生多余节点**；块内首现＝`first`、复现＝`again`；点击出完整卡且含动作按钮；卡片动作调用 `scopeTerm` / `onOpenTerms`；**无 Provider 时不报错、不高亮** |
| ~~`web/features/chat/term-index.test.ts`~~ | **并入上一行，不另建文件**——索引的构建/降级/「无 Provider」三条行为都由 `TermText.test.tsx` **走真实组件**覆盖（同一路径两处断言没有增量价值；且索引层是薄壳，真正会错的是它与渲染层的接线）。落地后如实订正契约：**本批最终 +2 个测试文件，不是 +3** |

通过后同步 `docs/dev/test-plan.md` 基线与不变量清单；`npm run check` 全绿。

---

## §10 风险（§0.5）

| 级别 | 风险 | 处置 |
|---|---|---|
| P1 | **流式性能**：尾块每帧重解析，若每帧对全量词条重扫会掉帧 | 合并正则按索引**缓存**（索引不变则复用同一个 `RegExp` 实例）；命中结果随既有 `stableCut` 的 stable 块 memo 复用，只重扫尾块；实测长回复（>2000 字、词条 300+）确认无可感掉帧 |
| P1 | **服务端统计漂移**：`replyHitsKey` 上提后行为若不等价，`usage_count` 会静默变口径 | 抽 shared 时**逐字保留**原判定逻辑；既有 `terms.test.ts` / `term-usage` 用例全绿作为等价性证据；新增边界用例（词边界、大小写） |
| P2 | **误高亮泛词**：词条库里若有「引用」「快照」这类泛词，正文会花斑 | 本契约**不加停用词表**（拍脑袋阈值不可解释）；先上，靠「首现强调」压噪声；若实测不可接受，另立「泛词过滤」契约 |
| P2 | **卡片遮挡与边缘溢出** | 定位做视口边缘夹取 + 空间不足时上下翻转；卡片宽度固定、不超出容器 |
| P2 | **数据一致性**：用户在别处改了释义/合并了别名 | 卡片数据一律现取当前 `TermItem`（索引在会话开始与词条变更后刷新），不缓存释义文本 |
| P2 | **词条数增长**：全量拉取在词条上千时变重 | 首版直接全量（实测 278 条）；上千时再评估增量/分页，本契约不预做 |

---

## §11 验收（人工项，探针不替人做审美验收）

1. 对话里问一个已入库词条相关的问题 → 回复正文中该词**首现实线下划线**、复现虚点线；
2. 悬停 → 速览卡出现（词名/领域/释义/已用次数），移开消失；
3. 点击 → 卡片固定，出现别名、复习状态与两个动作；
4. 点「纳入复习」→ 词条库该词条变为已纳入（进度清零的提示文案必须出现）；
5. 点「打开词条库」→ 跳到词条库页且搜索框已填该词条名；
6. 长回复（含代码块）→ 代码块内**没有**高亮；正文滚动流畅无可感掉帧。
