node_modules/
dist/
*.log
test-results/
.runtime/
.vite/

# 约定（对齐 eslint.config.mjs 第 11 行）：下划线前缀 = 会话级临时诊断脚本。
# eslint 与 tsconfig 早已排除它们，但此前漏了 git ⇒ 临时脚本会以未跟踪状态长期污染
# `git status`（本仓曾积压 7 个 `_tidydiag*.ts`）。这条把约定补齐到 git 层。
**/_*.ts
**/_*.tsx
# 2026-09-13 补：真机复验用的临时探针恰好是 `.mjs`，同一条约定在 mjs 上也得生效
# （来源标注批的 `tools/probes/_probe-config.mjs` 与 `tools/probes/_e2e/` 就是这么积起来的）。
# 入库探针的正名不带下划线（如 `quiz-e2e-cdp.mjs`），不冲突。
**/_*.mjs
tools/probes/_e2e/

# 2026-09-17 补：会话级临时诊断**目录**（下划线前缀约定的目录形态）。
# `**/_*.mjs` 只匹配文件名，`_probe/` 里 `setup.mjs` 这类不带下划线的脚本仍会污染
# `git status`；更要紧的是该目录会放真实库副本（`_probe/data/*.db`，含用户数据），
# 一旦手滑 `git add -A` 就会把用户数据推进仓库。
_probe/
# ★ 例外（2026-09-18 v0.2.59）：`_probe/auth-smoke.mjs` **刻意入库**——`AGENTS.md` 把它当
#   「真机冒烟入口」引用，不入库就是一条**悬空指针**（clone 下来的人找不到它）。
#   做法是 `git add -f` 强制加一次：**已跟踪的文件不再受本规则过滤**，此后 `git add -A`
#   照常带上它的修改。故本规则**一个字都不用改**，数据目录（`data*/`）与其他脚本仍全忽略。
