# studentbuddy v2 · 屎山指数登记表（bug-ledger）

> 版本：v0.1.20 | 状态：[活跃] | 更新：2026-09-25（**登记 B-019 ＝ 线上联网搜索整条哑掉而一句错都不报**（老板口述「现在studentbuddy线上的联网搜索总是失败,你去看看为什么」＋ GitHub issue #11 独立同判）。★ **一句话根因**：免 key 兜底通道写死的 `cn.bing.com/search` 上游改判成 **301 → `https://www.bing.com/?q=…`——`/search` 路径被丢掉**，落点是首页，于是 RSS 路拿回一篇「不是 RSS」的文档、HTML 路拿回一页「没有 `b_algo` 结果区」的首页，**两路各解析出 0 条**。★ **而代码把这报成 `rss: 空结果`／`html: 空结果`**——「通道形状变了」与「这个查询词真的没有结果」在同一个字符串里，看 `event_log` 的人（我和老板）第一反应都是「搜索没搜到」，不是「通道没了」。★ **第三层**：`tool_stats.ok` **在 0 结果时仍是 1**（`chat/tools/web-search.ts` 空结果回的是「本次联网检索没有返回结果…」这句**引导文案**，`outcome.ok` 真），所以「这三天有没有失败」在工具统计表里**根本读不出来**——135 行 `ok=1` 与 12 条 `search_empty` 事件是同一件事的两半。★ **取证**（全部在 VPS 现读，零写生产库）：`event_log` 里 `search_empty` 12 条、起于 2026-09-24 12:3x（北京时区）、此后每一次检索都在内；`curl -sI https://cn.bing.com/search?q=test` ⇒ `301 → location: https://www.bing.com/?q=test`；同一查询词换 `www.bing.com/search?q=…&format=rss` ⇒ 真 RSS、9 个 `<item>`。★ **本机对照**（同一条代码、不同出口 IP）：`www` 302 → `cn/search` 也拿得到结果 ⇒ **哪个主机名可用取决于出口 IP，写死任一个都会再坏一次**。★ **修法＝三步**（老板拍板「最小修 + 加探测」）：① 主机名换成 `www.bing.com/search`；② ★ **形状不符必须显式抛错**（`Bing RSS 返回的不是 RSS（HTTP 200、15027 字符、最终落在 www.bing.com/）`——状态码、字节数、**最终落点**三项都在文案里，这是本条最要紧的一处）；③ 新 `tools/probes/bing-channel-live.mjs` 真网络探针（不进 CI，`RESULT: PASS|FAIL` ＋ 退出码，**在 VPS 上实跑过并 PASS**）。★ **通道从 `search/index.ts` 拆进新 `search/bing-channel.ts`**（拆的原因是 index.ts 加完形状判据 412 行 > 400 红线，**不是为拆而拆**），新锁 `search/bing-channel.test.ts` **6** 例。★ ★ **通则（本条的存在理由）**：① **一个「没有结果」的空数组，可能是「你根本没在问一个搜索接口」**——解析外部响应时，「解析出 0 条」之后必须再问一句「这串字节像不像我以为的那种文档」；**结构判据（有没有 `<rss>`／`b_results`）比数量判据便宜，且是唯一能区分这两件事的东西**。本仓同族＝B-011/012/013 三条 `fetch_page`（都是「拿回来的不是我以为的东西」），区别在**那三条骗的是模型，本条骗的是自己的日志**。② ★ **「软降级」不许把降级原因吃掉**：`failed.push('空结果')` 这种自己造的字符串，等于在**唯一一条能定位故障的信息位置**上填了个占位符；错误文案要写「我看了什么、它是什么」，不写「我没拿到」。③ ★★ **一个统计字段可以既正确又无用**——`ok` 的语义是「工具函数没抛异常」，它没撒谎；但读它的人要的是「用户有没有得到结果」。**当失败被实现成「一次成功的返回＋一句道歉文案」时，任何基于 `ok` 的告警结构上看不见它**；要么给这条路径单独一个可数的事（本仓已有＝`search_empty` 事件），要么别指望统计表。④ ★ **外部主机的可达性是运行态事实，不是常量**——本批的修法（换域名）**恰好是上一条病灶的复发温床**：下次漂移会换个方向坏。所以这一条的**交付物是探针不是补丁**，域名那一行只是让今天能用；`cn`／`www` 谁活着的读数由探针现报，代码里不留第二个判据。★ **如实标出未验**：**未发版 ⇒ 线上此刻仍在报「没有返回结果」**（发版逐次点名授权，老板已明确「改完跑 check，发版单独点头」）；老板自己有没有真的在界面里搜成功一次 ⇒ **派 MT-34**；托管 provider（Exa／Tavily／智谱）配不配到 `.env`＝老板另行选型，本批只留了「零 key 时只有一条腿」这句事实。**前任 v0.1.19**：2026-09-21（**登记 B-018**：**`/api/auth/me` 返回占位邮箱 `gh-<github_id>@users.noreply.invalid`** —— `auth/users.ts` 与 `auth/github.ts` **各写一份 `toAuthUser`**，本批改口径时只改了一份 ⇒ **tsc 全绿、eslint 全绿、既有 108 例全绿**（被读的 `row.email` 类型仍是 `string`，缺陷只存在于语义层）；**本会话跑自身新测试时当场逮到**（3 例 `/me` 断言红），修法＝抽**单份** `auth/user-row.ts#rowToAuthUser()`；★ 屎山指数记 **0** —— 理由是**收敛成一份实现**，而不是在第二份上再补一处判断。★ 本条的**族属**：与 B-014「同一个约束被三处各自持有」同族，但错在**映射逻辑**而非数值。★ **本条同时也是一条收尾教训**：`bug-ledger` 登记**不在四条机器门禁里**（`gates` 不扫它），本批全绿、提交、推送完成后才想起补登 —— 与 B-018 同批的「双份实现」一样，**没有机器守着的规矩就是最容易漏的规矩**）。**前任 v0.1.18**：2026-09-20（**登记 B-017**：**词条页「复习计划」面板半屏高度下整块被静默裁掉 ⇒ 打卡键一颗都点不到**（老板原话「词条库的哪个复习计划哪里前端ui太窄了,用户都点不到按键了」）。★ **本条最要紧的定性：它是 B-008 的同页同机制反向复发，而 B-008 当日立的机器判据全程绿灯没拦**——判据只测「词条库本体在不在视区」，v28 新长的两块（复习范围选择器、队列打卡键）**根本不在断言集合里** ⇒ 收敛计数累计到 **2**。★ **症状措辞与病因无关**：写着「太窄」，实测**横向零裁切**（I8 全绿），病在纵向——`.rv-panel` 同时挂 `flex-shrink:1` 与 `overflow:hidden`，900×620 时面板被 crush 到 **43px**、`.rv-queue` 自己的 `max-height:240px; overflow-y:auto` **救不了一个已被裁出父盒的盒子**。★ **修法＝把「谁负责滚」收敛到一处**：新 `.rv-body` 成面板内唯一滚动区（两枚开关刻意留在体外，否则「展开」是单向操作），`.rv-panel` 的 45vh 上限**由构造保证**，两处内层限高滚动拆掉。★ ★ **通则（本条的存在理由）**：① **内层限高内滚在外层 `overflow:hidden` 裁切时完全无效**，一条溢出链上必须有且只有一层负责滚；② **判据的口径会把实现推向病灶**——旧 I4「CRUD 控件全部在视区」在 620px 内物理上不可能满足，于是实现学会了对超出部分 `overflow:hidden`，**「可见性」判据的劣化均衡就是静默裁切**，正确判据是**可达性**（走到根，任一层不可滚的裁切盒装不下才算丢）；③ **探针的 DOM 骨架会腐烂**——本批同步骨架时先红 18 条，说明此后每次绿灯测的是一棵已不存在的树，新自问：**探针最近一次变红，是因为产品的哪一行？** ★ **实测**：探针 **5 视口 × 5 态 = 270 断言全绿**；反向锁（只把 `terms.css` 换回 HEAD 那份）⇒ **254 passed / 16 failed**；打卡键可达数 `0/40 → 40/40` 覆盖 5 档视口。★ **如实标出未验**：老板**真机那一遍**（内置浏览器 `innerWidth=0` 不可用，全部证据来自无头 CDP）；`.term-side` 120px 让三颗键折两行、`.rv-head` 约 137px 死空间——两条是**独立观察**，未混进本批。前任 v0.1.17 | 状态：[活跃] | 更新：2026-09-20（**登记 B-016**：**视觉角色绑错成「生成图片」模型 ⇒ 上游一句英文 400 JSON 被原样弹给用户**（老板真机原话「我使用视觉模型它提示400」）。★ **本条最要紧的定性：症状是配置错触发的，但记账的是代码洞**——`chat/vision.ts` 的流式循环**没有 `catch`**，于是 `flow.ts` 把 `OpenAI API error 400: {"error":{"code":"invalid_request","message":"模型 agnes-image-2.5-flash 是 image 模型，请使用 /v1/images/generations"}}` **逐字**当 `chat-error` 发出去 ⇒ **违反 ADR-5「失败必须可读、可重试」**（用户既看不懂也不知道该改哪里，而答案其实就写在报文里）。★ **修法只补代码洞、不代改运行态配置**：新 `chat/vision-error.ts` 纯函数 `explainVisionFailure`（八分支：生成模型误绑／不支持图片／404／401·403／429／5xx／超时／**兜底不猜病因**，每条末行附截 240 字的`上游原文`——**翻译不是掩埋**），`vision.ts` 仅一处 `try/catch` 接线、**不改错误类型也不改 SSE 帧形**；改绑由老板自己做。★ **客观验证走产品代码不走复现**：`_probe/vision-e2e.mts` 在**真库副本**上 `import` 产品 `describeImages`，错绑 ⇒ **1.3s** 出中文指引、正确绑 ⇒ **22.6s** 描述正确；**零写生产 DATA_DIR**。★ ★ **通则（本条的存在理由）**：① **「用户报的配置错」与「代码缺错误翻译」是两条独立的债**——把绑定改对就收工，下一个绑错的人（或换服务商改名）会原样复发；**凡"只有绑错／填错才走到"的失败分支，都必须有一条可读文案**，这是产品对运行态的**下限承诺**。② **上游报文属于内部事实，不该当用户文案外发**，但也不能不给用户——正确形态是**人话在前、原文附在末行备查**（本仓同族先例：`routes/auth.ts`／`routes/pk.ts` 的「域错误码 → 文案」）。③ **分支判据必须配反向锁**：把报文里的「是 image 模型」遮掉后不得仍判成生成模型，否则那条分支会因为别的关键字常绿（与 B-013「判据命中从未被验」、B-011「探针空洞证明」同一课）。★ **如实标出未验**：老板改绑 `agnes-2.5-flash` 后**同探针当日 22:33 复跑 = 5.5s、答案卡要素全读对**（时效已清偿，比 `3.0-flash` 的 22.6s 快约 4 倍）；★ 仍欠**应用内真机那一遍**（在界面贴图看回复，而非探针直调函数）；真照片准确率对照、智谱 `glm-4v-flash` 均未测。前任 v0.1.16 | 状态：[活跃] | 更新：2026-09-20（**登记 B-015**：**`POST /api/sessions/:id/fork` 对「不存在的会话」回 201**，并写下一行指向**幽灵父会话**的 `forked_from_id`。★ **无用户可感知症状、无机器拦**（`forked_from_id` **刻意不设外键**——追问会话要能在父会话被删后继续存在，是**设计决定**；而 `tsc`／`eslint`／`gates`／既有 2319 例单测**全部零信号**）。★ **根因**＝端点的唯一存在性判据 `canAccessSession` 在**未登录模式**（`ownerId === null`）下**不查库、直接返回 `true`**（那是「本机单人模式」的既有语义、**本身没错**）⇒ fork 成了全仓**唯一一个「父行可以不存在」的写口**。★ **修法**＝在 `canAccessSession` **之后**补 `sessionExists(sessionId)`（查 `sessions` 且 `deleted_at IS NULL`）⇒ 回 **404**，回归锁 `routes/fork.test.ts` 首例。★★ **本条是「先写用例再跑」逮到的**——写第一条用例「会话不存在 → 404」时**先跑了一遍，发现它回的是 201**；若先写完实现再补用例，用例会照着实测值断言（「201 也对嘛」），这条就永远浮不出来 ⇒ 通则：**给一个写口写第一条用例时，先写「不存在 ⇒ 非 2xx」**，成本几乎为零。★ 同批另锁一条**顺序类**判据（写反了**没有任何运行时报错**）：**归属断言必须排在归一之前**——不存在的会话 ＋ 超长 `term` ⇒ **404 而非 400**，否则靠状态码就能区分「这个 id 存不存在」。★ **两条通则留痕**：① ★★ **「能不能访问」与「存不存在」是两件事**（授权 vs 存在性）——凡「从父行出发建子行」的写口，都必须额外自证父行存在；更普适的形态是★ **同一个 helper 在两种运行模式下强度不同**（这里：登录时查库、未登录时恒真）⇒ 「为登录模式写对的代码，在未登录模式下会**静默变松**」，模式差异必须写进该 helper 的契约注释；② ★★ **「查不到就该 404」要排在参数校验之前**（顺序写反只泄露信息、不报错）。前任 v0.1.15 | 状态：[活跃] | 更新：2026-09-20（**登记 B-014**：**三处图片上限各自合法、乘起来超了** ⇒ 贴满 4 张图点发送，请求被 express 在**路由之前**拒掉，前端只看到「点了发送没反应」（**静默失败**：服务端零日志、零业务代码执行、零库行）。★ **与本表 B-011/012/013 同族、不同层**：那三条错在**判据与声明**，本条错在**数值**——**没有任何一行代码是错的**，故 code review / lint / tsc / 单测**全部零信号**。★ **根因**＝三处上限分散在三个包（`ChatComposer.tsx` 4 张 ／ `chat/vision.ts` 单图 `7_000_000` **字符** ／ `server/index.ts` `express.json('24mb')` **字节**），**字符 → 字节的 4/3 base64 膨胀被漏掉了**（7M×4 ≈ 28MB > 24mb）；而注释里其实**已经写对了**「改一处必须同步改另一处」——**写下了规矩，但没有机器守着它**。★ **修法**＝收进 `shared/src/chat-limits.ts` 单一事实源（单图 7M→5M 字符，按最坏报文反推），并把耦合约束写成**不等式**（`worstCaseChatBodyBytes() ≤ CHAT_BODY_LIMIT_BYTES`）与 ★ **反向锁**（**旧 7M 值代回必须超限**——证明的是「旧配置确实会 413」，不是「新配置看着更保守」）。★★ **两条可复用判断**：① **「各自合法」≠「组合合法」**——只要几个上限出现在同一条链路上，就必须有一次**把最坏情形乘起来算**的断言（同族先例：`shared/src/auth.ts` 的两条常量不变式）；② **「静默失败」优先查「请求有没有到达业务代码」**——没有日志／没有错误／没有库行时，**先怀疑 express 与中间件层**（body 限额、CORS、鉴权闸门），别从前端交互查起）。前任 v0.1.14 —— **登记 B-013**：`fetch_page` 的 **PNG 魔数判据自落码起从未命中**——字符串层正则 `/^(?:%PDF-|GIF8|\uFFFD PNG|…)/` 里 **`\uFFFD` 与 `PNG` 之间多了一个 ASCII 空格**，而真 PNG 按 UTF-8 解出来是 `\uFFFD`+`PNG`（**无空格**）⇒ 该分支恒不匹配；且判据放在**解码后的字符串**上，会被同日新增的编码层**回退 GB18030** 时抹掉（`89 50` 被吃成一个汉字）。**无观测症状**：真 PNG/PDF 头部控制字符占比 9%~12%，一直被控制字符规则兜住 ⇒ 判「非活洞」。**但它证明了一件事：B-011 那批的 5 例新测试全都区分不出层次**（全是高控制占比样本，短路哪条判据都照样红）。修法＝把 `looksBinary` 移到**原始字节**上（严格 ≥ 字符串层、与编码解耦）+ 新 1 例（`fetch-page.test.ts` 16→**17**，**全文件唯一能区分两层**的用例：128 字节夹具实测字符串层 UTF-8 0.78%／GB18030 1.47% 均漏放、字节层魔数命中）；§7 改坏挪回字符串层 ⇒ **恰好 1 例红、其余 17 例全绿**）。前任 v0.1.13 —— **登记 B-012**：`fetch_page` **把 GBK 页以乱码形态当「正文」回灌**——根因是 `res.text()` **恒按 UTF-8 解码、忽略 `content-type` 的 `charset`**，GBK 字节退化成满屏 `\uFFFD` 仍被当正文。真机实测湘潭市政府 **61.8%**／岳阳市政府 **65.5%**／★ **当当网 `content-type` 明写 `charset=GBK` 也 60.7%**（服务端说了，我们没听）。★★ **本条与 B-011 是同一天同一工具同一症状、不同根因**：B-011 是「不是文本却被当文本」、本条是「是文本但用错编码解」——**修完 B-011 它仍然存在，且新加的内容闸门结构上拦不住它**（替换符占比不是二进制判据，拿它拦会误杀正常页）。修法＝`decodeBody()`（声明 charset 优先 → 未声明时替换符超 1% 回退 GB18030，取更少者）+ 新 3 例（含「不误伤」）；§7 改坏恰好 2 例红；修复后真机三例替换符 **0.0% 且内容真的解对**）。前任 v0.1.12 —— **登记 B-011**：`fetch_page` **把 PDF／PNG 当「正文」回灌给模型**——只判 `res.ok` 就直接 `res.text()`、**全程无内容类型检查**；`res.text()` 解二进制不抛错、`htmlToText` 只剥标签、`\s+` 又不匹配 NUL ⇒ 二进制一路穿到回灌文案，而文案还写着「以下为网页正文」。真机实测 PDF 回灌 **8021 字**（850 控制字符）、PNG **4736 字**乱码；**单测 8 例全绿却零信号**（没有一条碰过内容类型）。修法＝两道闸（`content-type` 白名单 + 二进制嗅探兜底）+ 新 5 例（含「谎报类型」与「不误杀」两侧），§7 改坏恰好 4 例红，登记 B-011。★ 契约 §5.3 原文早写「PDF 明确不支持」而实现零行表达 ⇒ 通则：**契约写「不支持 X」，实现里必须有一处可指行的拒绝分支**）。前任 v0.1.11 —— **登记 B-010**：step 终态按「name+倒扫最近 running」配对 ⇒ **同名并行工具调用串卡**（A 的 done 盖到 B 的卡，两卡各错一半）；修法＝`tool-exec` 统一在终态帧注入 `toolCallId`、前端 `step-fold.ts` 严格只认 id 配对、**配不上另起一条绝不回退倒扫**（契约 TOOL-ECOSYSTEM-SPEC §4.7，随 P1 批交付；锁＝`step-fold.test.ts` 7 例 + §7 改坏退回倒扫**恰好 1 例红**）。与 B-009 同一课：**前端猜不如服务端带上**）。前任 v0.1.10 —— **登记 B-009**：「思考中」计时**切走会话再回来就从 0 重数**——根因三层：`Thinking.tsx` 的基准是**组件挂载时刻**、SSE 事件里**根本没有「轮起点」这一帧**、`useChatStream` 切会话不清基准。定性为**未实现而非回归**（`git log -S` 追到 `c962040` 起就是挂载-local 写法）。修法＝新登记 `round-start` 帧（每轮缓冲**第一帧**，随回放自动恢复基准，契约 SSE-CONTRACT §2）+ 前端改吃服务端基准、本地 `Date.now()` 仅兜底；同批兑现 B-004 旧债（动 `useChatStream` 前先拆——`beginRound` 拆进新 `useRoundBegin.ts`）。真机「切走再切回」路径待老板复验封口）。前任 v0.1.9 —— **登记 B-008**：词条页「**词条库本体整个不可达**」——`80e97a4` 把复习面板插进词条页后，面板按内容撑到 **2073px**，而该页是「固定高度 + `overflow:hidden` + 只有 `.term-list` 一个弹性子项」的 flex 列 ⇒ 面板吃光容器、列表被压成 **4px**、工具栏/添加框被推出视口并被裁掉**且无滚动条**（用户**滚不到**）。★ **代码与数据一行未删**——`git log --stat` 逐笔查过、后端 `/api/terms` 与 278 条词条都在，是**布局不变量缺失**；当日补 `tools/probes/terms-layout-cdp.mjs` 把「容器不许被裁」立成机器判据）。前任 v0.1.8 —— **登记 B-007**：SSE 帧去重**两端口径不一致**——服务端每轮把 `seq` 重置为 1、客户端 `since` 跨轮单调不重置 ⇒ **第二轮的回复整轮被静默丢弃**、前端 `busy` 永久卡死；在跑督促小窗**渲染层探针**时暴露，属「复用一套流设施到第四个消费者才撞出的隐含假设」）
> **定位**：《计划重写文档》§10.4 在本仓的实例——记**修不动的/反复的** bug 及其屎山指数，作将来重写时的**负向 ADR 原料**。
> **★ 本表同时是 `AI-DEVELOPMENT-GUIDE.md` §0.15「改 bug 强制流程」第 0 步的数据源**：每次改 bug 前必须先读本表查「**收敛计数**」，缺这个字段第 0 步就读不到数、整套流程第一脚空转。
> **执行入口在 GUIDE §0.15**（六步序列 + 强制回复模板），**重写评估阈值在《计划重写文档》§10.2**——本表**只记账，不复述规范正文**（v1 与 META v0.1.19 的教训：规范正文复制一份就会漂）。
> 建表触发：本仓此前无此文件，§0.15 第 0 步无处可读（2026-09-02 随「文档模式」批次补齐；B-001 发现于 2026-09-01 会话）。

## 字段说明

| 字段 | 含义 |
|------|------|
| **收敛计数** | **首位字段**。「AI 声称修复 → 客观验证/复现失败」累计轮数；1=正常修／2=必须换根因假设／**3=强制切换策略，禁止第 4 次同向尝试**。**不按**改动次数、提交次数、对话轮次计数 |
| 症状 | 观察到的问题（一句话） |
| 报告版本 | 发现时所在提交（`git rev-parse --short HEAD`） |
| 预测根因 | 修**前**猜测的根因，具体到 `文件:行号 / 函数 / 机制` |
| 是否猜中 | 用测试或可复现步骤**客观**验证（✔／✘／△部分／未验），不采信 AI 自评 |
| 用的模型 | 做预测与修复的模型标识（防把"模型弱"误读成"架构烂"） |
| 屎山指数 | 同**根因**累计修不好次数（一次修好=0） |
| 升级架构问题 | 指数 ≥ 阈值或判定结构性 → 是/否 + 去向（冻结区 / ADR 编号） |

## 登记表

| # | 收敛计数 | 症状 | 报告版本 | 预测根因 | 是否猜中 | 用的模型 | 屎山指数 | 升级架构问题 |
|---|---------|------|---------|---------|---------|---------|---------|-------------|
| **B-019** | **1**（一次修好；屎山指数 0。★★ **与本表 B-011／B-012／B-013 同属「拿回来的字节不是我以为的那种文档」族**，而那三条都在 `fetch_page`、本条在**搜索兜底通道**——同一条链路上第四次撞同一课，说明这一族的**根因是「解析外部响应时只判数量不判结构」**，不是某一个函数写错） | **线上（11wand.com）联网搜索每一次都「没有返回结果」**，AI 回复里带那句引导文案，而**监控面上一句错都没有**：老板口述「现在studentbuddy线上的联网搜索总是失败,你去看看为什么」。★ **判别特征（为什么它能静默三天）**：三层各对一半——① 上游 301 把 `/search` 路径丢了（真故障）；② 代码把「解析出 0 条」报成 `rss: 空结果`／`html: 空结果`（**文案把故障类型洗掉了**）；③ `tool_stats.ok` 在 0 结果时**仍为 1**（`chat/tools/web-search.ts` 空结果走「成功返回＋道歉文案」），⇒ 135 行 `ok=1` 与 12 条 `search_empty` 事件是同一件事的两半，只看统计表永远查不出来 | `35f9d11`（2026-09-25 工作区；老板真机报告 ＋ GitHub issue #11 另一会话独立同判） | `packages/server/src/search/index.ts` 里写死的 `BING_SEARCH = 'https://cn.bing.com/search'`：该主机名对 `?q=` 的请求开始回 **301 → `https://www.bing.com/?q=…`**（★ 落点把 `/search` **路径**一起丢了＝首页），于是 RSS 路拿到一篇**不是 RSS 的文档**、HTML 路拿到一页**没有 `b_algo` 结果区**的首页，两路各 0 条 ⇒ 一路「软降级」到另一路，两路都「空」。**预测根因落在①（域名），但病灶有三层，只改① 只能让今天能用** | ✔ 猜中（**先取证再改码**，全部在 VPS 现读、零写生产库：① `event_log` 里 `search_empty` 12 条、起于 **2026-09-24 12:3x**（北京时区）此后每次检索都在内；② `curl -sI https://cn.bing.com/search?q=test` ⇒ **`301` ＋ `location: https://www.bing.com/?q=test`**；③ 同查询词换 `www.bing.com/search?q=…&format=rss` ⇒ 真 RSS、**9 个 `<item>`**；④ ★ **本机对照把「换个域名就完了」这个结论否掉了**——同一份代码在本机出口 IP 上是 `www` 302 → `cn/search` 且拿得到结果 ⇒ **哪个主机名可用取决于出口 IP，写死任一个都会再坏一次**。修法＝三步（老板拍板「最小修 + 加探测」）：域名换 `www.bing.com/search` ＋ ★ **形状不符显式抛错**（`Bing RSS 返回的不是 RSS（HTTP 200、15027 字符、最终落在 www.bing.com/）`，状态码／字节数／**最终落点**三项齐）＋ 新 `tools/probes/bing-channel-live.mjs`（真网络、不进 CI、`RESULT: PASS|FAIL` ＋ 退出码，**在 VPS 上实跑 PASS**）。★ **验证是在 VPS 上走真代码路径**（探针 esbuild 打包**出货的那份 `searchWeb`**，不是另写一份复现件）：0 key ⇒ `providers:["bing"]`、8 条结果、`failed:[]` | Qoder（本会话） | 0（一次修好；★ 记 0 的理由是**修法包含了「下一次换方向坏时谁会响」这道闸**，不是只把域名改对——探针是这一条的实质交付物） | 否 → 实现级缺陷（外部响应缺结构判据），**但记一条升级到架构层的问句留老板裁**：零 key 部署**只有一条腿**（Bing 免 key 兜底），而这条腿的可用性由第三方决定 ⇒ 是否给生产 `.env` 配一家托管 provider（Exa／Tavily／智谱）＝**待选型**，本批不动。**四条通则留痕**：① ★★ **「解析出 0 条」之后必须再问一句「这串字节像不像我以为的那种文档」**——结构判据（有没有 `<rss>`／`b_results`）比数量判据便宜，而且是**唯一能区分「没结果」与「通道没了」**的东西；缺了它，故障就会以「一次正常空查询」的形状出现（本仓同族三条全在这一点上栽过）。② ★ **「软降级」不许把降级原因吃掉**：`failed.push('空结果')` 是在**唯一一条能定位故障的信息位**上填占位符；错误文案要写「我看了什么、它是什么」，不写「我没拿到」。③ ★★ **一个统计字段可以既正确又无用**——`ok` 语义是「工具函数没抛异常」，它没撒谎；读它的人要的是「用户有没有得到结果」。**当失败被实现成「一次成功的返回 ＋ 一句道歉文案」时，任何基于 `ok` 的告警结构上看不见它**；要么给这条路径一个单独可数的事（本仓已有＝`search_empty` 事件，本批把它记全），要么别指望统计表。④ ★ **外部主机的可达性是运行态事实，不是常量**——本批的修法（换域名）**恰好是病灶复发的温床**，所以登记时把「交付物是探针不是补丁」写进字段，防止下一个读码人以为已经收口。⚠️ **未验（诚实记账）**：**未发版 ⇒ 线上此刻仍在报「没有返回结果」**（发版逐次点名授权）；老板在界面里真搜成功一次未跑 ⇒ **派 MT-34**；托管 provider 未配＝另一条待决；★ 探针只在**两个出口 IP**（本机／VPS）各跑过一次，第三地未测。 |
| **B-018** | **1**（一次修好；屎山指数 0。★ 与本表 **B-014**「同一个约束被三处各自持有」／**B-007**「两端去重口径不一致」**同属「同一个事实有两份实现」族** —— B-014 错在**数值**被三处持有，本条错在**映射逻辑**被两个文件各写一份） | **`/api/auth/me` 返回的是占位邮箱 `gh-<github_id>@users.noreply.invalid`，而不是用户的真实（展示）邮箱**：GitHub 账号登录后，账号菜单里显示的是一串 RFC 2606 保留域名的**内部占位串**。★ **判别特征（为什么它能活过编译与既有全部测试）**：**tsc 全绿、eslint 全绿、既有 108 例全绿** —— 因为被读的那一列（`row.email`）**类型仍然是 `string`**，两份实现都完美通过类型检查；缺陷只存在于**语义**层（`email` 在本批之后已成「内部占位 / 真实邮箱」两义，而**展示必须走 `github_email ?? email`**）。★ 它是**本批自己引入的**（口径反转改变了 `email` 列的含义），不是历史遗留 —— 这也说明：**改一个列的语义，等于改所有读它的地方** | 本批（2026-09-21 v0.2.105 工作区；**本会话跑自身新测试时当场逮到** —— 3 例 `/me` 断言失败；**非老板报告**） | `packages/server/src/auth/users.ts` 与 `packages/server/src/auth/github.ts` **各有一份 `toAuthUser`**：本批改口径时只改了 github 那份（`row.github_email ?? row.email`），users 那份**仍读 `row.email`** ⇒ `/api/auth/me` 与另外 5 处调用点走的还是旧映射。★ 根因**不在任何一处实现**，而在**「同一个映射有两份」这件事本身** —— 只要它还是两份，下一次改口径必然重演 | **✔ 猜中**（**测试先红，不是先猜后验**）：`auth-github.test.ts` **3 例 `/me` 断言红**，实际值逐字为 `gh-9001@users.noreply.invalid`（与占位串格式逐字吻合，直接指向 `email` 列）；修法＝抽成**单份** `auth/user-row.ts` 的 `rowToAuthUser()`（文件头留档本教训）、两文件共用、**5 处调用点**改引用（`replace_all` 后回读验证无重复前缀、无残留本地定义）；修后 **13/13 绿**、回读确认已无第二份实现；全量 `npm run check` **EXIT=0** —— **192 文件 / 2664 例（2663 passed + 1 skipped + 0 failed）**、gates 三绿 | WorkBuddy（本会话） | **0**（一次修好，记 0；★ 记 0 的理由是修法**把两份实现收敛成一份**，而不是在 users 那份上再补一处判断 —— 后者只会让「下一个人改哪个」变成新的问题） | 否 → 结构性缺陷（**同一个事实有两份实现**），**收口到 `auth/user-row.ts` 单一入口**。**三条通则留痕**：① ★★ **跨文件的重复实现，是唯一没有编译期护栏的那类缺陷** —— 同文件内的重复会被 lint（未使用符号等）抓到，而「两个文件各写一份同样的映射」在类型系统里**长得完全合法**，两份都能通过 tsc ⇒ **发现它的唯一手段是对输出内容（响应体）下断言**；② ★★ **「类型对」≠「语义对」** —— `row.email` 在本批之后已是**内部实现细节**（占位串），直接拿它当展示值，**类型上无懈可击、语义上全错**；凡「某一列的含义在同批里变了」，就该把**读该列的每一处**列出来清点（本仓同族：`doc-rag.ts` 常量双写、`toAuthUser` 双写）；③ ★ **反向锁比期望值锁更能跨实现存活** —— 本批 `/me` 那条「**必不含 `noreply.invalid`**」本意是防「占位串漏给前端」，实际**同时**逮到了双份映射；若当时只断言「email 等于某个值」，调用方就得先知道那个值是什么，而**照着实测的红值去写断言**恰恰会把这条锁写歪（§7「回归锁没红过＝它锁不住任何东西」的同构一句：**只断言"等于我这次看到的东西"的锁，锁的是当时那版实现**） |
| **B-017** | **2**（★★ **本条是 B-008 的同页同机制反向复发**——B-008 是「复习面板把词条库本体挤出视口」，本条是「页面框架把复习面板自己挤出视口」，同一个 `.term-page`「固定高度 + `overflow:hidden`」骨架、同一类「内容在 DOM 里、在像素上不在」。按 §0.15「同根因开始累计」计到 **2**。★ 更要紧的是：**B-008 当日就建了机器判据（P1 探针 56 断言），而这次判据全程绿灯**——它没拦住，因为判据只测「本体在不在视区」，**新长的两块（复习范围选择器、队列打卡键）不在断言集合里** ⇒ 见「升级架构问题」②） | **词条页「复习计划」面板里的按钮点不到**——老板原话「词条库的哪个复习计划哪里前端ui太窄了,用户都点不到按键了」；AskUserQuestion 追问锁定两件事：**点不到的是「队列里的『记住了/忘了』看不见了」**、**窗口是「半屏窄窗（约 900–1100 宽）」**。★ **症状措辞与病因无关**：写着「太窄」，实测**横向零裁切**（I8 全绿：侧栏按钮一颗都没被裁出列宽），病在**纵向**。量出来的现场：900×620 时 `.rv-panel` 高 **43px**、复习范围领域行 **11 行里 0 行可达**、队列打卡键 **40 颗里 0 颗可达**；1100×560 时面板 **26px**） | `v23.2`（老板 2026-09-20 22:5x 口头报告；骨架病灶自 `80e97a4` 的「固定高度 + `overflow:hidden`」就在，**把它踩穿的是 v28 新加的复习范围选择器**——`.rv-scope` 是 `flex-shrink:0`，于是压缩全部推到别处） | `packages/web/src/features/terms/terms.css`：`.rv-panel` 同时挂 `flex-shrink:1` 与 `overflow:hidden`，而整条高度链上没有任何一层「负责滚」；★ **同页第二条洞**——`.rv-queue` 写着 `max-height:240px; overflow-y:auto`（看上去「溢出已经处理过了」），但**滚动条挂在一个已经被裁出父盒的盒子上等于没挂** | ✔ 猜中（§0.15 第 0 步先读 B-008 那条「容器不许被裁」，直接按同族去量 `.rv-panel` 的 `flex-shrink`／`overflow` 组合，一次到位）。★ 留痕一处**差点走偏**：「太窄」这个措辞让第一遍去查了**列宽**（`.term-side` 只有 120px ⇒ 纳入复习/编辑/删除折成两行）——那确实是洞，但**与本 bug 无因果**，最后作为独立观察挂进 `test-plan.md` §6，**没有混进本批** | WorkBuddy（本会话） | 0（一次修好；★ 屎山指数记 0 的理由是修法**把「谁负责滚」收敛到一处**（`.rv-body`），而不是每块各自加一条 `max-height` 继续叠） | 否 → 实现级缺陷（布局不变量缺失），**但留下了升级到架构层的问句：「页面高度分配」该不该有一个共享不变量层**，而不是每个面板自己 `max-height`。**三条通则留痕**：① ★★ **「内层限高内滚」在外层 `overflow:hidden` 裁切时完全无效**——一条溢出链上必须有且只有一层负责滚，且那一层要挂在**装得下内容的父盒**上；两层都能滚＝滚轮不知道该给谁，外层裁切＝内层滚动条静默作废。② ★★ **判据的口径会把实现推向病灶**：旧探针 I4「CRUD 控件**全部在视区**」在 620px 高内**物理上不可能满足**（20 条队列 + 词条库本体 + 添加框），于是任何朝它靠拢的实现都学会了对超出部分 `overflow:hidden`——**「可见性」判据的劣化均衡就是静默裁切**。正确判据是**可达性**：从元素走到根，任一层**不可滚**的裁切盒装不下它才算丢（可滚层算「滚一滚就到」）。本批把这条写成 `reachable()`，并新增 **I6**（队列打卡键）／**I7**（领域行）把**新长的块纳进断言集合**。③ ★ **探针的 DOM 骨架会腐烂**：本批把骨架同步到当前 JSX 时**先红了 18 条**——骨架停在 v28 之前，意味着**此后每一次绿灯，测的都是一棵已经不存在的树**。⇒ 与 §7「回归锁没红过＝它锁不住任何东西」同构的一句新自问：**这个探针最近一次变红，是因为产品的哪一行？** |
| **B-016** | **1**（一次修好；屎山指数 0。★★ **与本表 B-006／B-011 同属「喂给用户／模型的东西不诚实」族，但触发方在库外**——B-006 是回灌内部配置细节＋给放弃台阶、B-011 是把二进制当正文、本条是**把上游报文原文当用户文案**。★ 另与 **B-012 的「修完 B-011 它仍然存在」同构**：本条真正的病灶有两条（配置绑错 ／ 代码缺翻译），**改绑只关掉触发条件，代码洞原封不动**） | **视觉角色一绑错，用户看到的是一句英文 JSON ＋ 「400」，不知道自己做错了什么**：老板真机原话「我使用视觉模型它提示400」。实际弹出的是 `OpenAI API error 400: {"error":{"code":"invalid_request","message":"模型 agnes-image-2.5-flash 是 image 模型，请使用 /v1/images/generations (request id: …)","type":"AgnesAI_error"}}`。★ **判别特征（为什么这类洞能长期存在）**：**服务端的日志与上游报文全是「对」的**——HTTP 400 确实发生、`chat-error` 帧确实发了、SSE 链路一行没写错，**唯一错的是「这句话该不该给用户看」**，而这不在任何机器判据里（`tsc`／`eslint`／`gates`／单测**全部零信号**，因为没有一条断言过「失败时用户看到什么」）。★ **可观测后果**：老板为此怀疑的是**产品**（「图片输入功能坏了」）而不是**设置**，排查方向被带偏一整轮 | 本会话真机复现（2026-09-20 工作区，老板口头报告；**病灶自 v17 看图批 2026-09-16 落码起就在**——`describeImages` 自始至终没有 `catch`） | 两层，缺一不可：① **触发条件在库外**：`role_bindings` 把 `vision` 绑成了 `agnes-image-2.5-flash`——那是**图片生成**模型，官方只收 `/v1/images/generations`、不接受 `image_url`，故 400 是**它的正确行为**（★ 关键取证：以同一把 key 手工重放上游 `POST /v1/chat/completions`，报文与前端弹出内容逐字一致 ⇒ **不是我们拼错了请求**）；② **代码洞**：`packages/server/src/chat/vision.ts` 的 `for await (const chunk of target.adapter.chat(…))` **裸奔无 `catch`**，异常直接冒到 `chat/flow.ts:71` 的 `chat-error` 分支，而那里**刻意「原样外发上游 message」**（本意是「别吞掉真信息」，实际效果是「把内部事实当用户文案」）。⇒ **预测根因落在②**：①不在代码的可修范围（运行态配置），且**修好①也不会让②消失** | **✔ 猜中**（客观验证，四类证据，不采信 AI 自评）：① **上游重放**钉死「400 由绑定引起、与我们的请求拼装无关」；② **端到端走产品代码**：新 `_probe/vision-e2e.mts` 在**真库副本**上做 `initCrypto()` ＋ `openIsolated()` ＋ 直接 `import` 产品 `describeImages`（★ 不另写一份实现，否则验的是复现件不是出货件；★ 必须 `.mts`——tsx 把 `_probe/*.ts` 当 CJS、顶层 await 直接报错），错绑 ⇒ **1.3s** 给出「看图失败（视觉模型 …）：这个模型只会\*\*生成图片\*\*，不会\*\*读图片\*\*…请到设置页「角色模型绑定」把「视觉（看图）」改绑一个支持图片输入的对话模型」，改绑 `agnes-3.0-flash` ⇒ **22.6s** 描述正确；③ **新 14 例**（`chat/vision-error.test.ts`）用**本次真机抓到的那条报文原文**当语料，含 ★ **反向锁**（把「是 image 模型」遮掉后**不得**再判成生成模型——否则该分支因别的正则常绿，正是 B-013 那一课）与**兜底逐字锁**（认不出来就不猜病因）；④ `chat/vision.test.ts` 10→**12** 两条**接线锁**（错绑 ⇒ 中文指引；429 ⇒ **不得**冒充成绑错），全量 vitest **181 文件 / 2494 passed ＋ 1 skipped 全绿**、`npm run lint` EXIT=0、`node tools/gates/check.mjs` 全绿。★ **如实标出未验**：**「应用内真机那一遍」未跑**（在设置页改绑 → 对话里贴图 → 看气泡上的人话／看描述出来）——本批端到端验的是**库副本上的产品函数**，界面层那一环仍要靠老板或 §6 探针补；★ **时效已清偿**：老板改绑 `agnes-2.5-flash` 后同探针当日复跑 **5.5s、答案卡要素全读对**（`2x+3=9`／`f(x)=3`／`12,340`／`40`／三色柱状图），比 `agnes-3.0-flash` 的 22.6s 快约 4 倍；真照片识别准确率（pro vs flash）未做对照；智谱 `glm-4v-flash` 本机无 key 未测；`thinking:false` 在 openai 适配器**根本不下发**（pro 实测仍烧 269–461 reasoning 字）——本批刻意不动、要改另行拍板 | WorkBuddy（本会话） | 0（代码洞一次修好，记 0；★ 但**触发条件由老板运行态自改，本表不代其封口**） | 否 → 实现级缺陷（**「内部事实」与「用户文案」同一条通道直通**），不动架构。**三条通则留痕**：① ★★ **「用户报的配置错」与「代码缺错误翻译」是两条独立的债**——把绑定改对就收工，下一个绑错的人（或服务商改名／下线）会原样复发。**凡「只有绑错／填错才走到」的失败分支，都必须有一条可读文案**，这是产品对运行态的下限承诺，不是锦上添花；② ★★ **上游报文不该外发，但也不该吞掉**——正确形态是**人话在前、`上游原文` 附在末行**（截 240 字）。仓内同族先例是「域错误码 → 文案」（`routes/auth.ts`／`routes/pk.ts`），本条是该规矩在**LLM 上游边界**上的落地：凡把第三方报文直接写进用户可见文案的调用点，都要自问「这句话他看得懂吗」；③ ★★ **判「无机器信号」的洞，靠的是有没有断言「失败时长什么样」**——本批之前**没有一条测试**关心用户看到的错误文本，所以它坏得无声；新 14 例的第一价值不是防回归，是**第一次把「可读性」变成了会红的东西**。★ 附一条**取证顺序教训**：**先重放上游、再改代码**——若先写翻译器，就会拿「它现在说人话了」当成根因已证，而真正的根因（生成模型误绑）当时仍未确认 |
| **B-015** | **1**（一次修好；屎山指数 0。★ 与本表 **B-004**「贴在会话上的状态漂移」／**B-007**「两端去重口径不一致」**同属「同一件事有两个判据、各管一半」族**——B-007 是两端对同一个 `seq` 给了两种解读，本条是把「**能不能访问**」与「**存不存在**」两件事**合并成了一个判据**，而未登录模式把前一半**恒真**之后，后一半就**没人兜**了） | **`POST /api/sessions/:id/fork` 对「不存在的会话」回 201，并写下一行指向幽灵父会话的 `forked_from_id`**：对任意不存在的 session id 发起「向 AI 追问」，接口**照样成功**，库里真多出一行 `forked_from_id` 指向**根本不存在的父会话**。★ **判别特征（为什么能活过交付）**：**前端一切正常**——追问按钮转一下、切到新会话、能聊能落库能出现在侧栏，**没有任何一条用户可感知的症状**；也**没有机器拦**——`forked_from_id` **刻意不设外键**（追问会话要能在父会话被删后继续存在，这是**设计决定**不是疏漏），故**库层不会拒**；`tsc`／`eslint`／`gates`／既有 2319 例单测**全部零信号**。★ 用户视角的唯一后果是「侧栏里多了一个源出不明、无从追溯的会话」，以及**数据完整性上多了一类孤儿** | 本批（2026-09-20 v0.2.84 工作区，**本会话自审发现**；★ 起因是给 `routes/fork.test.ts` 写第一条用例「会话不存在 → 404」——**先跑了一遍，发现它回的是 201**。若先写完实现再补用例，用例会照着实测值去断言，这条就永远浮不出来） | `packages/server/src/routes.ts` 的 fork 端点，唯一的存在性判据是 `canAccessSession(sessionId, ownerId)`。而该函数在**未登录模式**（`ownerId === null`）下**不查库、直接返回 `true`**——那是「本机单人模式」的**既有语义**（`ownerFilter(null)` 不加条件、行为与多租户改造前逐字一致），**本身没错**。错在**fork 成了全仓唯一一个「父行可以不存在」的写口**（其余写口都从既有行出发）。**两层叠加**：① 该端点**把「能不能访问」与「存不存在」合并成了一个判据**——两者是**授权**与**存在性**，本就该分开；② 未登录模式把授权判据**恒真**之后，存在性判据**没有任何人兜**，于是缺陷**只在未登录模式下存在**（登录模式下 `canAccessSession` 会查 `user_id`、不存在自然查不到 ⇒ **换个部署形态这条就没了**，这也是它最容易被漏掉的另一层原因） | **✔ 猜中**（客观验证，四类证据）：① **改前实测**：`POST /api/sessions/no-such-id/fork` ＋ 合法 `Origin` ⇒ **201**，且真库里多了一行 `forked_from_id = 'no-such-id'`（**先证伪「是不是我想多了」**）；② **修法**＝在 `canAccessSession` **之后**补 `sessionExists(sessionId)`（查 `sessions` 且 `deleted_at IS NULL`）⇒ 同一请求回 **404**；③ **新 9 例**（`server/src/routes/fork.test.ts`）**首例即这条回归锁**；★★ ④ **同批另锁一条「顺序类」判据**（写反了**没有任何运行时报错**）：**归属断言必须排在归一之前**——不存在的会话 ＋ 超长 `term` ⇒ **404 而非 400**，否则调用方能靠状态码区分「这个 id 存不存在」；⑤ 收尾 `npm run lint`（tsc×3 ＋ eslint `--max-warnings 0`）**EXIT=0**、`node tools/gates/check.mjs` **全绿**、全量 vitest **168 文件 / 2319 例（2318 passed + 1 skipped + 0 failed）**。★ **如实标出未验**：**真机端到端未跑**（本 bug 只在 supertest 层验过），见 `test-plan.md` §6 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**两个判据被合并成一个**），**不动架构**。**两条通则留痕**：① ★★ **「能不能访问」与「存不存在」是两件事**——前者是**授权**、后者是**存在性**。本仓 `canAccessSession` 在未登录模式下把授权判据**恒真**，故**凡「从父行出发建子行」的写口，都必须额外交一次「父行存在」的自证**（本批 fork 是第一个；`study-flow` 的 `createRun` 也会自动建会话，但其父是**定义行**、走的是另一条路，本批已核，不受影响）。★ 更普适的形态：**同一个 helper 在两种运行模式下强度不同**（这里：登录时查库、未登录时恒真）⇒ 「为登录模式写对的代码，在未登录模式下会静默变松」，故**模式差异必须写进该 helper 的契约注释**，而不是靠调用方记得。② ★★ **「查不到就该 404」这条判据必须排在参数校验之前**——顺序写反**不会报任何错**，只会把「这个 id 存不存在」**送给调用方**（信息泄露类缺陷的固定形态：**行为全对、只有状态码泄露**，与 B-011「契约写了不支持、实现零行表达」同属「不报错的错」）。★ 另留痕一条**测试工程教训**：**这条 bug 是「先写用例再跑」逮到的**——**给一个写口写第一条用例时，先写「不存在 ⇒ 非 2xx」**（而不是先写 happy path），它比任何设计评审都便宜，且**成本几乎为零** |
| **B-014** | **1**（一次修好；屎山指数 0。★ **与本表 B-011／B-012／B-013 同属「写了一套、实际运行走的是另一套」族**——那三条错在**判据与声明**，本条错在**数值**：三处上限**各自都合法**，只是**乘起来超了**。★ 判别特征：**没有任何一行代码是错的** ⇒ code review、lint、tsc、单测**全部零信号**） | **贴满 4 张图点发送 → 请求在「路由之前」被 express 拒掉，前端只看到「点了发送没反应」**（静默失败：服务端**零日志、零业务代码执行、零库行**）。★ **可观测性最差的一类症状**——用户不知道发生了什么，开发者也没有现场（没有 500、没有堆栈）。★ **成因是三个数字分散在三个包里**：`web/features/chat/ChatComposer.tsx` 的 `MAX_IMAGES = 4`、`server/chat/vision.ts` 的 `MAX_DATAURL_CHARS = 7_000_000`、`server/index.ts` 的 `express.json({ limit: '24mb' })` ⇒ 单图 7M 字符 × 4 ≈ **28MB > 24mb**。★ 三处**各自都对**：注释甚至写着「与 24mb 是同一套账，改一处必须同步改另一处」——**写下了规矩，但没有机器守着它** | 本会话审计（查真库发现 `messages` 表**零图片行** ⇒ 顺着「为什么一次都没发成」把上限算了一遍；**非老板报告**，但与老板历史上踩过的「点发送没反应」是同一症状） | 数值之间**缺一条耦合约束**：24mb 原本由「单图 ≤5MB × 最多 4 张 + 余量」反推而来（AGENTS.md 原话即如此），但单图上限后来被定成 `7_000_000` **字符**，**没人再把它换算回字节乘一遍** ⇒ **字符数 → 字节数之间的 4/3 base64 膨胀被漏掉了**。★ 更根本的一层：**「一次最多几张」「单张多大」「body 上限」是同一个约束的三个面，却分别由前端组件、视觉层、服务启动文件各自持有**（三个包、三种生命周期、三拨改动的人） | **✔ 猜中**（客观验证，四类证据）：① **算术取证**：`7_000_000 × 4 = 28_000_000` 字符，即使按 base64 折回仍 ≫ 24mb body；② **新 6 例**（`shared/src/chat-limits.test.ts`）里 ★ **第 ③ 条是反向锁**——**把旧的 7M 代回 `worstCaseChatBodyBytes()` 必须超限**（证明的是「旧配置**确实**会 413」，而不是「新配置看起来更保守」）；③ **§7 改坏取证**：把上限调回 7M ⇒ ①②③ 条**同时红**；④ **7M→5M 是反推出来的**：`4 × (5M 字符 + 64) + 20_000 字符文本预算` ≈ 20MB ≤ 24mb，余量 ≈ 4MB > 设下的 2MB 下限 | WorkBuddy（本会话） | 0（一次修好，记 0——避免把一次普通配置收口误判成屎山） | 否 → 结构性缺陷（**同一个约束被三处各自持有**），但**改动是收口、不是重构**：三个数收进 `shared/src/chat-limits.ts` 单一事实源，并让 `chat-limits.test.ts` 把**耦合约束写成不等式 + 反向锁**。**四条通则留痕**：① ★★ **多个上限必须显式写出它们之间的不等式，并对最坏情形取值**——「各自合法」≠「组合合法」；只要几个数出现在同一条链路上，就该有一条测试**把最坏情形乘起来算一次**（本仓同族先例：`shared/src/auth.ts` 的两条常量不变式「尝试上限必须 < 10^6 空间」「同邮箱上限必须严于同 IP 上限」，正是同一个道理）；② ★★ **注释里的规矩没有机器效力**——本处注释**已经写对了**（「改一处必须同步改另一处」），缺陷照样存活 ⇒ **凡是"必须同步改"的东西，要么合并成一处、要么写一条会红的断言**；③ ★★ **「静默失败」优先查「请求有没有到达业务代码」**——没有日志、没有错误、没有库行时，**先怀疑 express／中间件层**（body 限额、CORS、鉴权闸门），别从前端交互查起；④ ★ **base64 内联的 4/3 膨胀是这一族的固定坑**——凡以「dataURL **字符数**」设上限、又以「**字节数**」设 body 限额，中间必须过一次 `× 0.75` 换算 |
| **B-013** | **1**（一次修好；屎山指数 0。★ 与本表 B-005「`else` 分支从未走过」、B-011「契约写了『不支持 X』而实现零行表达」**同属「写了但从未被走到」族**——本条是该族**第三种形态**：**判据写了、用例也写了，但从没验证过判据真的会命中**） | **`fetch_page` 的 PNG 魔数判据自落码起从未命中过（潜伏，无观测症状）**：旧 `looksBinary(raw: string)` 的字符串层正则写成 `/^(?:%PDF-\|GIF8\|\uFFFD PNG\|\uFFFD\uFFFD\uFFFD)/`——**`\uFFFD` 与 `PNG` 之间多了一个 ASCII 空格**，而真 PNG 按 UTF-8 解码是 `\uFFFD`+`PNG`（**无空格**）⇒ **该分支一次都没命中**。真实文件一直被**控制字符占比**那条规则兜住（实测真 PNG 头部 11.7%、真 PDF 9.2%，均 > 2% 阈值）⇒ **无用户可感知症状**，这也正是它能活过 B-011 那批「5 例新测试 + §7 改坏取证」的原因：那批用例**全是高控制占比样本**，短路哪条判据都照样红。★ 判别特征：**单测全绿、真机全绿、gates 全绿，缺陷只存在于「判据是否真的会命中」这一问里** | `a2055a0` 后工作区（本会话**自审**发现；起因是问「编码层会不会削弱魔数层」，量 70 字节真 PNG 时顺手核了旧正则字面量；**非老板报告**） | `packages/server/src/chat/tools/fetch-page.ts` 的 `looksBinary`，**两条叠加**：① **正则字面量里混入空格** ⇒ PNG 分支恒不匹配（`GIF8`／`%PDF-`／`\uFFFD\uFFFD\uFFFD` 三条无空格、是真的在生效，**唯独 PNG 这条是死的**）；② 更根本的一层——**判据放在了「解码后的字符串」上**，而同日新增的编码层会**回退 GB18030**，`89 50` 被吃成一个汉字 ⇒ **即便把空格修掉，GB18030 路径下魔数仍会整个消失**。⇒ **魔数这一层在真实链路上始终是零作用**（控制字符规则单独扛着）。★ 判「非活洞」的依据是**实测而非推理**：**造不出**低控制占比的**真实**文件（真 PNG/PDF 头部控制字符占比 9%~12%），只有合成夹具能做到 | **✔ 猜中**（客观验证，四类证据）：① **字面量取证**：`git show HEAD:…/fetch-page.ts \| grep FFFD \| cat -A` ⇒ 空格是**真实 ASCII 空格**（非终端/编码假象）；② **两层判定对照实测**（128 字节夹具＝PNG 签名＋高字节，Python 复算）：字符串层 **UTF-8 控制占比 0.78%／GB18030 1.47%**、魔数**均不命中** ⇒ 判「非二进制」**漏放**；字节层 PNG 魔数命中 ⇒ **拦下**；③ **新 1 例**（`fetch-page.test.ts` 16→**17**，`★★ 魔数必须在字节层判`）——**本文件唯一能区分「判字节」与「判解码后字符串」的用例**；④ **§7 改坏取证**：把嗅探挪回字符串层（`looksBinary(new TextEncoder().encode(decodeText(bytes, ct)))`）⇒ **恰好 1 例红**（就是新那条），**其余 17 例全绿** ⇒ 反向证明旧用例确实**都不区分层次** | WorkBuddy（本会话） | 0（一次修好，记 0） | 否 → 实现级缺陷（**判据放错了层**），不动架构。**三条通则留痕**：① ★★ **「写了判据」≠「判据会命中」**——新增任何魔数／白名单／正则分支，**必须配一条能证明它命中**的用例，否则就是 B-005 的 `else`、B-011 的「契约里的不支持」的同类：**代码在、行为不在**；② ★★ **判据属于哪一层，就放在哪一层**——关于**字节**的判据放在**字符串**上，会被**同批新增的解码层**悄悄削弱甚至抹掉 ⇒ **「这一批新加的东西，会不会让上一批的判据失效」应当成为批次收尾的固定自问**（本批正是这一问救回来的）；③ ★ **怀疑字面量写错时先 `cat -A`**——`\uFFFD` 与空格在编辑器里几乎无法分辨，肉眼看代码不可靠 |
| **B-012** | **1**（一次修好；屎山指数 0。★ **与 B-011 是同一天、同一个工具、同一个症状、不同根因**——B-011 是「**不是文本却被当文本**」，本条是「**是文本但用错编码解**」。★★ **本条最要紧的一课：修完 B-011 它仍然存在，且新加的内容闸门在结构上拦不住它**——`looksBinary` **刻意不看**替换符占比，正因为替换符不是二进制判据；若图省事拿它拦，会把正常中文页也拒掉（**修 A 引入 B**）。**「同症状 ≠ 同根因」在本表是第二次应验**（首次见 B-008／B-004／B-007「屏幕上少了东西」族）） | **`fetch_page` 把 GBK 页面以乱码形态当「正文」回灌给模型**：对 GBK 编码的中文页发 GET，站点正常 200，工具把**满屏 `\uFFFD` 替换符**的文本当作正文、贴上「以下为网页正文」的护栏交给模型。★ **判别特征**：**与 B-011 症状一模一样**（都是「乱码冒充正文」），**故极易被当成同一条修掉就算完**——但 B-011 修完后真机复测三例**全部照旧乱码**，这才暴露出是另一条根因。★ **实测体量**：湘潭市政府 **61.8%** 替换符（6429 字里 3973 个）／岳阳市政府 **65.5%**（7127 字里 4668 个）／★ **当当网 `content-type` 明写 `charset=GBK` 也照样 60.7%** | `5270317` 后工作区（同日真机探针 4d 段暴露；**非老板报告**） | `packages/server/src/chat/tools/fetch-page.ts` 用 `html = await res.text()` 读响应体。**undici 的 `res.text()` 恒按 UTF-8 解码、完全忽略 `content-type` 里的 `charset`** ⇒ GBK 字节里大量非法 UTF-8 序列退化成 `\uFFFD`（**解码不抛错**，与 B-011 同一条「静默」性质）；`htmlToText()` 不碰这些字符、`if (!text)` 判空也过 ⇒ 乱码进回灌。★★ **决定性一例**：当当网的 `content-type` 是 `text/html; charset=GBK`——**服务端已经明确声明了**，而代码里**没有一处读它**。**「对端说了，我们没听」比「对端没说，我们猜错」更难辩解** | **✔ 猜中**（四类证据，不采信 AI 自评）：① **候选侦察**（非 UTF-8 字节判定，脚本按 `decode('utf-8')` 是否抛错分类）：扫 47 个中文站点，**命中 3 个真 GBK 页**（湘潭市政府／岳阳市政府／当当网），其余全部合法 UTF-8——**这一步是防空转的**：若不做，很容易拿一堆 UTF-8 页测出「未见异常」而其实从未验到 GBK；② **修复前真机**：三例**全部**「当正文回灌」且替换符占比 61.8%／65.5%／60.7%；③ **修复后同一凭证复跑**：三例替换符 **0.0%**，且**内容真的解对了**（"湘潭市人民政府门户网站…政务公开 政务服务 互动交流"／"岳阳市人民政府 个人门户…"／"当当网 欢迎光临当当，请登录免费注册…"）——**不只是数字归零，是文本正确**；④ **新 3 例**（`fetch-page.test.ts` 13→**16**：声明 `charset=GBK` 按声明解／未声明但字节是 GBK 靠嗅探回退／★ **不误伤**——未声明 charset 的 UTF-8 页必须照常按 UTF-8 解）；⑤ **§7 改坏取证**：退回 `res.text()` → **恰好 2 例红**（两条 GBK 用例），「不误伤」那条**仍绿**（符合预期）→ 恢复后 16/16 绿 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**用了「够用就好」的 API，而没读对端已声明的元数据**），不动架构。**四条通则留痕**：① ★★ **`res.text()` 是一个「假设 UTF-8」的 API**——凡按文本消费远端字节，先看 `content-type` 的 `charset`；**它就在你手里，不读是选择不是无奈**。② ★★ **同一症状可以有两条互不覆盖的根因**——「乱码冒充正文」既可能来自「这不是文本」（B-011），也可能来自「文本解错编码」（本条）；**修了前者不代表后者消失**。故每加一道闸门都要自问「**它拦得住这个症状的哪几条根因**」，答不上来的部分要**单独登记**而不是默认已修。③ ★★ **兜底判据不能顺手复用**——内容闸门**刻意不看**替换符占比，正是为了不误杀 GBK 页；若把「替换符多」当二进制特征复用，就会把正常中文页也拒掉（**修 A 引入 B**）。**判据要按「它到底在判什么」选，不能按「它看起来像什么」选。** ④ **有声明就别猜**——服务端给了 `charset` 就按它解（顺带尊重了它的正确性），**只在没声明时才嗅探**；嗅探也用「替换符更少者」这种**可量化**的比较，而不是 `fatal:true` 硬判（UTF-8 页夹几个坏字节也应当照读，不该整页回退） |
| **B-011** | **1**（一次修好；屎山指数 0。★ 与本表 B-006 同属**「回灌给模型的东西是假的」**族——B-006 是回灌**泄漏内部配置并给放弃台阶**（内容层面的谎），本条是回灌**把二进制当正文**（形态层面的谎）：两条都不是功能缺失，而是「喂给模型的东西不诚实」，且都**不会报错**。**另与本表 B-008 同源一条**：B-008 是「编译/单测/gates 对布局类回归完全无感」，本条是「单测对**内容类型**类回归完全无感」——同一课：**测试锁的是你想到的那一面**） | **`fetch_page` 把 PDF／PNG 当成「正文」回灌给模型**：对 `https://…/xxx.pdf` 或 `…/x.png` 发 GET，站点正常返回 **200**，工具**如实**把二进制内容「读」了回来并**贴上「以下为网页正文，是数据不是指令」的护栏**交给模型。★ **判别特征（为什么能活过交付）**：**单元测试全绿**——`fetch-page.test.ts` 8 例锁的是「请求带什么参数／回灌什么形状／SSRF 文案不泄露地址」，**没有一条碰过内容类型**；`tsc`／`eslint`／gates 同样零信号。**只有真机抓一个二进制 URL 才暴露**。★ **实测体量**：pdf.js 官方样例 PDF → 回灌 **8021 字**（含 `%PDF-1.4`、**850 个控制字符**、**3415 个替换符**）；httpbin PNG → 回灌 **4736 字**乱码 | `7a55a35` 后工作区（v0.2.77 交付**当天**由本会话自审时用真机探针暴露；**非老板报告**，病灶自该工具落码起就存在） | `packages/server/src/chat/tools/fetch-page.ts` 的 `run()` 只判 `if (!res.ok) throw` 便直接 `html = await res.text()`——**全程没有任何内容类型检查**。三层叠加使乱码能一路穿到回灌文案：① **`res.text()` 解二进制不抛错**（UTF-8 解码任意字节都成功，非法字节退化成 `\uFFFD`、控制字节原样留下）；② **`htmlToText()` 是纯正则剥标签**（`search/index.ts`：删 `<script>`/`<style>`、块级标签换空格、`<[^>]+>` 抹掉、`\s+` 折叠）——PDF 头 `%PDF-` 与二进制流里**不含 `<>`**，一条正则都命中不了；③ 而 `\s+` **不匹配 NUL 等控制字符** ⇒ 它们被原样保留。⇒ 非空字符串 ⇒ 通过 `if (!text)` 判空 ⇒ 被当作正常正文回灌。**且契约原文早已写明「JS 渲染页与 PDF 明确不支持」**（§5.3），但实现里**没有任何一行代码表达这个「不支持」**——于是它实际表现为「静默产出垃圾」而非「如实拒绝」 | **✔ 猜中**（客观验证，四类证据，不采信 AI 自评）：① **真机探针** `tools/probes/fetch-page-live.mjs`——★ **真调 `runTool('fetch_page')`**（用 esbuild 现场转译 TS 源再 import，**不复现一份实现**；`SB_DATA_DIR` 指向临时目录不碰真库），**修复前**：PDF 当「正文」回灌 8021 字（`PDF头=true 控制字符=850 替换符=3415`）、PNG 4736 字（`控制字符=541 替换符=2129`）；② **修复后同一凭证复跑**：PDF／PNG／SVG **三例全部如实拒绝、零字节二进制外泄**（回灌文案「这个地址不是网页正文（内容类型 application/pdf），本工具只读网页……」）；③ **新 5 例**（`fetch-page.test.ts` 8→**13**）：`application/pdf` 拒绝且断言 `not.toContain('%PDF-')`／`image/png` 拒绝／★ **谎报类型**（声明 `text/html` 实为 PDF）由兜底嗅探拦下／★ 不给 `content-type` 且控制字符密集拦下／**不误杀**（`text/html; charset=utf-8`／`text/plain`／`application/xhtml+xml` 照常读）；④ **§7 改坏取证**：把两道闸各自短路成 `false` → **恰好 4 例红**（「不误杀」那条**仍绿**，符合预期——短路闸门不影响放行路径）→ 恢复后 13/13 绿；⑤ 全量 `npm run check` **CHECK_EXIT=0**：158 文件 / **2179 passed + 1 skipped**（2180）+ gates 四项全绿 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**只判传输层成功、不判内容层形态**），不动架构。**四条通则留痕**：① ★★ **「读到了」≠「读对了」**——凡 `res.text()` 这类「按文本消费远端字节」的读口，必须先回答「对端给的到底是什么类型」；**类型闸门要放在解码之前**（在 `res.text()` 前判 `content-type`，既正确又省掉把整个 PDF 读进内存）。② ★★ **解码器不抛错 ≠ 内容可用**——UTF-8 解任意字节都成功，**静默产出乱码是它的默认行为**；凡「把外部字节变成字符串」的地方，都必须自问「如果对端不是文本，我会知道吗」。③ ★ **「明确不支持」的口径必须是「如实拒绝」，不能只是文档里的一句话**——契约写了「JS 渲染页与 PDF 明确不支持」，实现里零行代码表达它 ⇒ 「不支持」静默退化成「产出垃圾」。**凡契约写了「不支持 X」，实现里必须有一处可指行的拒绝分支。** ④ ★ **探针的空洞证明**：本批首版探针的非 HTML 段只挂**单候选**（w3.org `dummy.pdf`），被 **403** 拒掉 ⇒ **那条路径根本没被执行**，而结论却写了「非 HTML 资源形态未见异常」。改为**多候选**并在「候选全被拒」时强制输出「**未验到，不得据此说未见异常**」。**同族提醒：探针报「全过」之前，先确认它真的走到了那条分支**（与 §7「回归锁没红过＝它锁不住任何东西」同构） |
| **B-010** | **1**（一次修好；屎山指数 0。★ 与 B-009 同属「P1 计时呈现线」在过程卡上的姊妹洞：B-009 碎的是**轮基准**、本条碎的是**卡的归属**——都是「前端拿不可靠的线索猜事实」，修法同向：**事实由服务端随帧带上**（B-009 带 `startedAt`，本条带 `toolCallId`+`durationMs`） | **同一轮里两个同名工具并行调用时，过程卡串卡**：模型一次发两个 `search_web`（并行 tool_calls），前端 step 折叠按「`name` + 倒扫最近一条 running」配对终态 ⇒ A 的 done 帧**盖到 B 的卡**上，两张卡各错一半（detail/result/耗时互换了主人）。★ 判别特征：**单工具轮永远复现不了**——只有同名并行才炸，故此前零报告（缺陷在数据形状里，不在用户路径上） | `ac50186` 后工作区（P1 批实现期自审发现，非老板报告；病灶自过程卡上线起就存在） | `useChatStream` 旧的 step 折叠逻辑用 `tool.name` 找「最近一条 running」做配对键，而 `name` **在同轮并行时不唯一**；真正的唯一键 `toolCallId`（模型侧 call id）一直存在于 `messages.tool_call_id` 与调度器上下文里，只是**从没上过 SSE 终态帧** ⇒ 前端**无从按唯一键配对**，与 B-009「线路上没有轮起点帧」同构 | **△ 机制已锁、真机观感待老板目检**（不采信 AI 自评）：① 修法＝`tool-exec` 调度器给所有 `onStep` 终态统一注入 `toolCallId`（`StepPayload` 一处注入、工具实现零改动），前端 `step-fold.ts` 严格只认 `toolCallId` 配对，**配不上另起一条、绝不回退「name+倒扫」**（宁可多一张卡也不盖别人的）；② `step-fold.test.ts` **7 例**逐条钉（同名并行各归各卡／无 id 终态另起／running 不带终态字段…）；③ **§7 改坏取证**：把配对退回旧「name+倒扫」→ **恰好 1 例红**（「终态缺 toolCallId 绝不回退倒扫」那条）→ 恢复回绿。★ 如实标出未验：真机双 `search_web` 并行的观感未目检（机制由帧序锁定），随 P1 挂 §6 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**配对键选了不唯一的字段**），不动架构。**通则留痕**：① ★ **「倒扫最近一条」类启发式在多实例并发下必然错配**——凡按某字段配对事件，先问「这个字段在并发下同刻唯一吗」；不唯一就找真 id 上线路，别让前端猜（与 B-009 同一课：**猜不如带上**）；② **配不上就新开、绝不回退猜测**——兜底方向也要选「错得可见」的一边（多一张卡是可见的小错，盖卡是不可见的大错） |
| **B-009** | **1**（一次修好；屎山指数 0。★ 与本表 B-004／B-008 同属**「切会话」族**——B-004 是状态漂到别的会话头上、B-008 是内容被裁出视口、本条是**基准随组件重挂清零**：同一触发条件（切换会话），根因分在「清理缺失」「布局不变量」「状态生命周期」三层。**判为「未实现」而非回归**（考古见「预测根因」末）；同类第三条再出现应按同族累计） | **回复中切走会话再回来，「思考中」的已用时从 0 重新数**：正在生成时切到别的会话看一眼、再切回来，等待气泡上的计时**凭空归零重数**（服务端这一轮早就在跑，已用时本应是真实的）。★ 判别特征：正文与步骤不丢（回放照常），**只有计时碎**——最软的一类状态，坏得最安静 | `a9c829a`（老板 2026-09-18 口头报告；病灶由 `c962040` 09-13「对话体验升级」批引入挂载-local 计时器） | **三层叠加、前两层是承重墙**：① `web/src/features/chat/Thinking.tsx` 的计时基准是 `useEffect` 里的**挂载时刻** `Date.now()`——组件挂载 ≠ 轮开始，而切会话让 ChatView 子树整体卸载重挂，基准必然重取；② SSE 事件里**压根没有「轮起点」这一帧**（`shared/sse-events.ts` 全部事件不带时间戳），重挂后前端**无从恢复**真实基准——这不是 Thinking 能自己修的事；③ `useChatStream.ts` 的 `startedAtRef` 挂在 hook 实例上、切会话 effect 清了耗时镜像却**从不重置基准**（跨会话残留旧值）。★ 定性依据：v0.2.18（test-plan §2）修的是同族**徽标**漂移、从未碰计时；`git log -S` 追到 `c962040` 起就是这个挂载-local 写法 ⇒ **属「从未实现过轮绑定基准」，不是谁改坏的回归** | **△ 机制已锁、真机症状待老板复验**（不采信 AI 自评，按字段口径如实记）：① `flow.test.ts` +2——成功轮与失败轮后缓冲**第一帧**必是 `round-start`（恒为首帧 ⇒ 回放必带着它，这是「可恢复」的结构前提）；② `sse-bus.test.ts` +1——进行中的一轮重订阅，回放首帧携带**原** `startedAt`，且 `since≥1` 的增量重连**不该**再收到它（顺带证明新帧不破坏 seq 去重口径）；③ `tsc`×3 + gates 四项 + 全量 vitest 全绿（数字记于 CHANGELOG 本批行）。★ **如实标出未验**：「切走再切回、计时不重启」这条**真机路径本人未点过**（本仓无 jsdom，SSE 全链路重挂只能在浏览器里验），已随批挂【交付自检 · 真人测试】单——**老板复验通过后方可把本格升级为 ✔** | WorkBuddy（本会话） | 0（一次修好，真机复验后封口） | 否 → 实现级缺陷（**跨挂载存活的状态放进了挂载期局部变量，且无事件承载**），不动架构。**通则留痕两条**：① ★★ **「只活在组件挂载期的状态」在「切会话＝整树重挂」的前端里必然重置**——凡「本轮」态要么上提到跨切换存活的层（本仓即 `useChatStream` 的 ref 族），要么**有一帧可回放的事件承载**（本批的 `round-start`），没有第三条路。B-004 通则「贴会话的状态必须随会话一起清」是本条的下半场：**清了还得能恢复，恢复的唯一事实源在服务端**；② ★ **计时这类「软状态」坏得最安静**——正文/步骤丢了必报 bug，计时归零只有盯着才发现；故契约 §2 给它立了「每轮缓冲第一帧」的硬不变量，并用假 Response 直接钉住回放序列（不靠人眼）。★ **同批兑现一笔旧债**：B-004 留痕「下次动 `useChatStream.ts` 前必须先拆分」——本批把 `beginRound` 拆进新 `useRoundBegin.ts`（理由只记在门禁红线，未靠砍注释自救） |
| **B-008** | **1**（一次修好；屎山指数 0，续作「什么不是屎山」对照样例。★ **2026-09-20 同页同机制反向复发 ⇒ 见 B-017，同族计数累计记在那边，本条不回填**；仪器现已扩到 **5 态 × 5 视口 270 断言**，本行下方写的「三态 × 两视口 56 断言」是**登记当时的历史事实**、不回改。★ 但**与本表 B-004／B-007 同属「屏幕上少了东西」这一族**——B-004 是状态漂到别人头上、B-007 是帧被静默丢掉、本条是**内容被裁在视口外**，三者症状都是「用户看不见」，而根因分在「状态」「协议」「布局」三层；若下一条同类再出现，应按「同根因」开始累计） | **词条页里「词条库本体」整个不可达**——老板原话：「词条库本体哪里，你把原功能给覆盖了……以前的增删改查功能和查询功能都没了，还有滑动列表也没了」。实际表现为：页面上只剩顶部的**复习面板（艾宾浩斯记忆曲线）**，词条**列表／搜索框／手动添加框／每条的编辑与删除按钮全部消失，且整页滚不动** ⇒ 用户**在物理上无法访问**这些功能。★ **判别特征（与「真被删」的区别）**：**接口一切正常**——`GET /api/terms` 回真词条、`/api/terms/domains` 回 **total 278**、`/api/terms/review/overview` 回 `due 271 / overdue 217`，且前端源码里 add/update/remove/list 一个不少 ⇒ **数据在、代码在、只有像素不在** | `df8250b`（老板 2026-09-18 14:44 口头报告；症状由 `80e97a4` 引入） | `packages/web/src/features/terms/terms.css` 的**布局不变量缺失**：`.term-page` 是「固定高度（`.sb-shell{height:100%}` → `.sb-main{flex:1}`）+ `overflow:hidden` + **只有 `.term-list` 一个弹性子项**（`flex:1; min-height:0`）」的 flex 列，其它兄弟（`.term-head`/`.term-stats`/`.term-toolbar`/`.term-add`）**全是 `flex-shrink:0`**；而 `TermsPage.tsx:151-152` 把复习面板**刻意放在列表之前**（注释写「先于列表，因为它回答的是「现在该干什么」」），该面板 `.rv-panel` **既无 `max-height` 也无 `flex-shrink`**、高度纯由内容撑 ⇒ flex 项默认 `min-height:auto` **不能压到内容高度以下** ⇒ 面板**一口吃光容器**，唯一的弹性子项被压成 4px，后面的工具栏/添加框被推出视口，父级 `overflow:hidden` **直接裁掉且不给滚动条** ⇒ 老板**根本滚不到**。**不是「AI 删了功能」**：`git log --stat` 逐笔查过，`TermsPage.tsx` 只有 5 笔提交、唯一带删除的 `ce96927` 只是把内联 `.term-tabs` 抽成 `DomainBar` 组件；历史上真删过的只有 `15accb0`（09-01 忆域 v2 重构删掉旧 `features/memorize/MemorizePage.tsx`），**而把那个被删文件读出来看，它本来就只有「添加 + 翻卡复习」**，从来没有完整增删改查/查询/滑动列表 ⇒ 老板说的「原功能」也不是它 | **✔ 猜中**（客观验证：① **真机像素对照**——从源读真 `tokens.css`/`app.css`/`terms.css`，按 `TermsPage.tsx` 的 JSX 结构手搭 DOM 喂无头 Chrome，**同一份 DOM 跑「有面板／无面板」两遍**：有面板时 `.term-page` `clientH 800 / scrollH 2494`（`overflowClipped: true`）、`.rv-panel` 高 **2073px**、`.term-list` 高 **4px 且在视口外**、搜索/添加/编辑/删除/添加按钮**全部不可见**；摘掉面板后 `scrollH 800` 不裁、`.term-list` **407px 可见**、上述控件**全部可见** ⇒ 因果闭合；② **两档视口（800／1000）面板高度都是 2073px**（内容撑的、不随视口变）⇒ **任何屏幕都坏**，不是「小窗口才有的」；③ **修复后复跑同一探针**：收起态面板 49px、列表 **334px（800 视口）／534px（1000 视口）**，展开态面板 223/423px、列表**仍保有 160px 保底**、队列 42/240px 内滚，三态 × 两视口 **56 断言全绿 EXIT=0**；④ `npx eslint <两文件> --max-warnings 0` EXIT=0、`npm run gates` 全绿、全量 vitest **116 文件 / 1580 例（1579 passed + 1 skipped）**。★ **如实标出未验与代价**：**「面板展开时」在 800px 高视口下队列只剩 42px**（列表 160px 保底 + 其余固定块把空间分完，队列靠内滚仍可达，但一次只看得到半张卡）——这是「词条库本体优先」的**自觉取舍**，不是缺陷，但观感须老板目检；**点击收起/展开的真实交互**未在真机点过（探针只验两种静态结构，不模拟点击）；`npm run lint` 本轮**未同轮全绿**，原因在批外（并行会话在途的 `shared/src/coach.ts` 引入 `CoachTrendCard` 使 `CoachCardViews.tsx` 两处 tsc 报错，HEAD 里该类型尚不存在）——已按 §9 用独立通道验自己的部分 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**布局不变量缺失**），**不动架构**。**三条通则留痕**：① ★★ **「功能没了」≠「代码删了」**——老板报「功能被删」时，正确动作是**三条证据交叉**：`git log --stat` 逐笔看增删 ＋ **读被删文件的内容**（而非只看它存在过）＋ 问后端真值（接口/库）。本次若跳过后两条，会顺着「AI 删了老板功能」的错误前提去「恢复代码」——**白改一通且根本找不回来（因为没丢）**；而把 `15accb0` 的删除读到底才发现「老页面本来就没那些功能」，避免了拿错锚点。② ★★ **「固定高度 + `overflow:hidden` + 只有一个弹性子项」的容器是脆弱结构**：往里插入**任何内容驱动高度的兄弟节点**，都会**静默吃掉那个弹性子项的空间**，而且**因为没有滚动条，内容不可达**——比溢出更难查（溢出至少能滚）。这类容器必须二选一：给弹性子项 `min-height` 保底，或给大个子兄弟 `max-height` + 内滚/可折叠。本批两者都做了（`.term-list{min-height:160px}` + `.rv-panel` 自收敛 + 队列内滚 + **队列默认收起**）。③ ★★ **编译/单测/gates 对布局类回归完全无感**——本批 `tsc`／`eslint`／`vitest` 1580 例／`gates` 四条**全绿**，同一个 commit 里页面却有 2073px 的面板撑爆 800px 的容器；本仓 web 侧**无 jsdom**（见 test-plan §1）⇒ **布局不变量只能靠真机量 rect**，故当日补探针 `tools/probes/terms-layout-cdp.mjs`（三态 × 两视口、**6 条不变量 56 断言**，零依赖零副作用、不需后端不需登录）并把「容器不许被裁」写成机器判据——**「当日建仪器＝当日清偿」** |
| **B-007** | **1**（一次修好；屎山指数 0，续作「什么不是屎山」对照样例。★ 但**与本表 B-004／B-005 同属「卡在回复中」这一族**——B-004 是前端状态漂移、B-005 是上游兜底失效、本条是**帧静默丢弃**，三者症状相似而根因各在一层；若下一条同类再出现，应按「同根因」开始累计） | **第二轮起的 SSE 帧被静默丢弃**：同一会话**第二次**提问时，模型回复**不显示**、输入框永久停在「停止」（`busy` 卡死），而**服务端其实一切正常**——真库里有完整的 ai 消息、`/api/chat/active` 也已释放。★ 判别特征：**一轮比一轮更容易中**——第二轮若比第一轮短（帧数少），去重会把该轮**连 `done` 一起**吃掉，于是「静默」得比第一轮更彻底（第一轮至少正文出得来、只是尾巴没收好） | `d9b9a92`（在跑督促小窗渲染层探针 `tools/probes/coach-cdp.mjs` 时暴露；症状最初伪装成「探针选择器抓错了卡」，见下方「是否猜中」第 ③ 条） | `packages/server/src/chat/sse-bus.ts` 与 `packages/web/src/lib/sse-client.ts` 的**去重口径不一致**：服务端 `startNewRound()` **每轮把 `seq` 重置为 1**（`publish` 里 `seq = buf.events.length > 0 ? last.seq + 1 : 1`），而客户端 `since` **单调递增、跨轮从不重置**（`handleEvent` 里 `if (ev.seq <= since) return;`）⇒ **第二轮的全部帧 `seq` 都 `≤` 上一轮攒下的 `since`**，被前端一条不剩地当成重复帧丢掉。两端各错一半：客户端**该重置时没重置**；服务端 `subscribe()` 又拿请求里那个**陈旧 `since`** 当回放起点，于是**连全量回放都不可达**（含 `done`）——合起来是「这一轮等于没发生」 | **✔ 猜中**（客观验证：① **隔离实例两轮实验**（`_probe/coach-seq-diag.mjs`，端口 18798 + `SB_DATA_DIR=_probe/data-coach-cdp`，**不碰生产库**）：第一轮 `seq` 1–56 正常；第二轮服务端 `seq` **重新从 1 起**，而临时卡正文**从句子中间开始**（`""，是"根本没碰过"。…`）——这正是「丢帧」的行为签名，与「第二轮被整轮吃掉」逐字吻合；② **修复后同一实验复跑**：第二轮临时卡**从头开始**（`你问了两遍。快照里只有硬数据：从未复习过 278 条，今日已…`）；③ **16 例新／改测试全绿**——新 `web/src/lib/sse-seq.test.ts` **8** 例（纯函数 `acceptSeq`：同轮重号丢弃、**跨轮 `seq===1` 必须重置**、第二轮更短也要整轮收下、`since=1` 接受 1、非 1 的乱序仍拒）+ `server/src/chat/sse-bus.test.ts` 6→**8**（新增「★ 陈旧 `since` 重连不被饿死」与「不越界：`since` 未超过本轮已产出的 `seq` 时，回放口径**一字不变**」——后者是**防过度修复**的锁，避免把「重置」写成「一律回放」）；④ **全量 vitest 115 文件 / 1555 例（1554 passed + 1 skipped）全绿**（本批**登记这一刻**的实测值；总量由三个并行批共同推进，以最新全量实测为准）。★ **但探针与复跑都在隔离实例上**：生产实例（18791，老板在用）的复现属**推断**——不过该缺陷与数据无关、纯口径问题，**开第二轮必现**，故风险判断不受影响；另**督促小窗的 LLM 路径已用真模型（agnes）跑通**，但「真机上第二轮」未单独截图留证 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**两端去重口径不一致**），**不动架构**。**三条通则留痕**：① ★★ **「`seq` 从几开始」是本仓第四个消费者才撞出来的契约空洞**——聊天流、PK 流、督促流**共用** `lib/sse-client`，前两个都是「一轮到底」（`seq` 单调成立的假设从没被检验过），**只有督促小窗会在一轮结束后又开一轮**（换个问题就是新一轮）⇒ 单调假设**第一次被打破即出 bug**。**凡「复用一套设施」，必须回头把它的隐含假设写成断言**，而不是等第四个消费者来撞——本仓已有同族先例（`api.ts` 的 `request` 无默认超时，也是复用久了才暴露）；② ★ **去重逻辑的「重置信号」必须显式化**：服务端其实一直在用「`seq` 重新等于 1」表达「这是新一轮」，**客户端却完全不知道有这回事**——两边对同一个数字给了两种解读而没人写明。修法是不让客户端「猜」，而把这个信号抽成纯函数（`acceptSeq(since, seq)`：`seq === 1` 即视作换轮、`since` 随之重置）；③ ★ **服务端的回放起点要自证有效**：`since` 是**客户端给的外部输入**，用之前必须判它是否落在本轮区间内（`since > maxSeq` ⇒ 退回 0 全量回放），否则一个陈旧值就能让整轮不可达——**与「客户端给的游标一律当不可信输入」是同一条**。★ 附一条**探针工程教训**：症状最初指向「探针抓错了卡」（首版选择器取 DOM 里第一个 `.coach-card-ai`，抓到的是上一轮 401 失败卡），**差点把真缺陷当成探针 bug 收工**；直到改成「以服务端 `/api/coach/messages` 的真值为基线、再回 DOM 找那张新卡」才露出 `apiAi:8 / domAi:7 / busyStop:true` 这条**决定性对照**（服务端 8 张、屏上 7 张）——**「探针红」的第一反应该是「谁的真值更可信」，而不是「改探针让它变绿」** |
| **B-006** | **1**（一次修好；屎山指数 0，续作「什么不是屎山」对照样例。★ 但**这是本表第二条「加工具不配触发层」**——与 `choice-nudge` / grill-me 同源，若下一条同类再出现，应按「同根因」开始累计） | **模型明明能联网搜索、却一口咬定自己没有这个功能**：老板要求「联网搜索」时，模型反复回「我这边没有联网搜索工具，真的搜不了」。★ **同源第二症状（前端骗人）**：对话页「联网已开」pill 开着，用户以为已授权联网，实际该开关**只对出题生效**、对普通对话零作用。★ **同源第三症状（会话内自我强化）**：一旦说过一次「我没有联网工具」，这句进了历史后，同会话后续轮会自我复述、语气越来越肯定（实测 05:50→05:51→12:23 三次） | `9bc8623`（老板口头报告；取证用的真库会话 `97a0b339` / `3c7f7d9b` 等） | **先把「能力缺失」排除掉（四条工程链路逐条证伪）**：`chat/tools.ts:46` 有 `registry.set('search_web', …)`、`chat/flow.ts:172` 把 `toolDefinitions()` 拼进出站 `tools`、两个适配器都真下发（`llm/openai.ts:101` 流式 / `:219` once、`llm/anthropic.ts:83`）、`search/index.ts` 的 provider 链（exa→tavily→zhipu→bing 免 key 兜底）完整、key 也配着（真库 `app_settings` 里有 `search_key_exa` / `search_key_tavily`）⇒ **不是链路问题**。预测根因落在**「声明 + 触发」两处都缺**：① `chat/system-prompt.ts` 的 `SYSTEM_PROMPT` **对 `search_web` 零声明**（chart / html / update_tasks / ask_choice 全都有使用引导，`ask_choice` 甚至有 12 行强制引导含「本轮第一个动作就是它」），而模型判断「我有什么能力」主要依据系统提示词的能力声明；② `tool_choice='auto'` 下模型完全自由，助手的出厂倾向是「直接把答案讲完」，**没被声明的工具它不会主动内化为自身能力**；③ 辅因（放大器）：`tools.ts:69-71` 的失败回灌把「未配置搜索 key / 本网络可能不可达」这类**内部配置细节**甩给模型，还附一句「或基于已有知识回答」的放弃台阶；④ 前端 `online` 状态只进 `useQuizActions`（出题 → `quiz-search`），**从未进 `/api/chat/send`** | **✔ 猜中**（三类客观证据：① **真库读码取证**——`messages` 表里 2026-09-14 那轮 `search_web` **成功返回三组结果**、2026-09-09 模型自己说过「我的联网搜索功能确实存在」⇒ 能力在、只是不调；而 B-006 现场那轮的 `tool_calls` 给的是 **`ask_choice`** 而不是 `search_web`——正是「提示词把 ask_choice 捧成第一个动作、对搜索一字未提」的行为签名；② **新 30 例**（`chat/search-nudge.test.ts`，语料即现场原话，含「搜搜」2 字专钉 `MIN_LEN=2`、学科术语守门人、`SYSTEM_PROMPT` 防回退锁）；③ **一条反向证据**：改完回灌文案后，`flow.test.ts` 原有的「无 key → 引导配 key」用例**当场红**（它正向断言旧文案），按 §4B 改为反向断言，**该红灯即本批修复生效的机器证明**）。★ **但「模型最终会不会调」锁不了**——词表只保证指令注入了，属 provider 行为，须真机验（挂 `test-plan.md` §6） | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷（**缺触发层**）+ 一处产品缺口（前端开关未接线），**不动架构**。**三条通则留痕**：① ★★ **「加工具不配触发层 = 功能不存在」在本仓已是第二次应验**（首次是 2026-09-14 的 `ask_choice`，见 `choice-nudge.ts` 文件头；本次是 `search_web`）⇒ **新增「AI 主动调用型」工具时，验收项必须包含三层触发**（提示词场景清单 + 工具描述带示例 + 输入侧词表硬提醒），**只注册工具不算交付**；② ★ **系统提示词里的能力清单就是模型的「自我认知」来源，漏一个工具等于那个能力不存在**——工具清单（`tools` 参数）只说明「可以调什么」，而**能力声明**（system prompt）才决定「我是什么」；两者不可互相替代，故 `chat/system-prompt.ts` 与 `chat/tools.ts` 必须同步维护；③ ★ **给模型的错误回灌不许泄漏内部配置、不许给放弃台阶**——模型会把「未配置 key / 网络不可达」原样转述成「我没有这个能力」，而「或基于已有知识回答」这类台阶会直接终结重试；正确口径是「能力在、这次没命中」并给可执行的下一步。另留痕一条**产品债**：前端开关（`OnlineToggle`）与后端能力之间**没有任何契约**，「UI 打开了但后端不认」属**用户可感知的谎报** ⇒ **✔ 已闭合（2026-09-17 v0.2.44）**：老板拍板走方案 ②（**`tool_choice` 强绑**，比「注入硬指令」硬——不依赖模型自觉），前端 `online` 经 `useChatStream`→`useSendActions`→`api.chat.send/regenerate/resend` 全链路出站，三条路由都收该参数，首轮强绑 `search_web`（装配层 `chat/opening.ts`，与 grill-me 共用同一个强绑名额、按「grill 优先」定优先级）；回归锁 `chat/opening.test.ts` **10 例** + `chat/flow.test.ts` **+4 例**（含一条被它逮到的真缺陷：联网指令若留到次轮会让模型**每轮重搜**，遂改为首轮一过就摘）。剩余未验三条已挂 `test-plan.md` §6（真机端到端／默认「开」的成本／词表与强绑的覆盖关系） |
| **B-005** | **1**<br>（一次修好；屎山指数 0，续作「什么不是屎山」对照样例） | **两路会话同时运行时双双永久卡在「回复中」**：侧栏两间都亮着「回复中」永不消失、输入框永久禁用；已上屏的部分文本留在屏上，但这一轮**永远等不到 done**。单独一路跑同样会中招，只是概率低（需命中「上游收下请求但不回数据、也不断连」这个特定形态）。★ **同源第二症状（配额踩踏）**：上游一挂住，被挂起会话的配额也一直占着——每轮 `done` 之后固定追加的 `extractTerms`／`compactIfNeeded` 两次 LLM 调用会**立刻抢走刚空出的配额**，让恢复更难 | `32143fc`（老板实测报告：「限制并发,但是应该两个对话同时运行还是可以的,要解决这个问题」；诊断全文 `_probe/DIAGNOSIS.md`） | `packages/server/src/llm/openai.ts` 与 `anthropic.ts` 的等待兜底都写成 **`if (req.signal) { 桥接外部信号 } else { setTimeout(abort, 120_000) }` 的二选一**——生产路径（`/chat/send`、`/regenerate`、`/resend`）**总是**传 `signal`（`aborters` 里那个 `AbortController` **只在用户点「停止」时才 abort**）⇒ **那句 120s 从未生效**，两个适配器实际上**没有任何上游无响应兜底**；上游挂住时 `fetch` 永久 pending → `chat/flow.ts` 的 `runTurn` 不 settle → `aborters` 永不摘除（侧栏「回复中」＝读 `aborters.keys()`）→ 该会话 SSE 再无 done 帧 → 前端 `busy` 永久 true。**两层债**：① 兜底分支的**可达性从未被验证**（写了 `else` 就当它生效了）；② 全仓 14 个适配器调用点**无任何并发上限**，配额互相踩踏 | **✔ 猜中**（读码取证：两适配器的 signal/else 二分写法可指到行；`flow.ts` 的 `aborters` 生命周期与 `routes.ts` 的 `/api/chat/active` 读同一份状态）。客观验证：① 新 `llm/upstream-gate.test.ts` **13** + `llm/upstream-timeout.test.ts` **12** + `llm/upstream-wiring.test.ts` **8**（**全假时钟 + 假上游**；接线三个文件用「永不应答的假上游」把「挂起→兜底→抛可读错误」端到端钉死，并锁「容量满时第三个请求的 `fetch` 从未被调用」）；② 全量 `npm run check` **CHECK_EXIT=0**：101 文件 / **1344 passed + 1 skipped**（1345）+ gates 全绿；③ **隔离实例真机核验**（假 provider 端口 18801 只服务 1 路、多余请求丢弃不回；隔离服务 18799，`SB_DATA_DIR` 指向**库副本**、不碰生产库）：被丢弃那路**在 180s 被兜底清掉**，`/api/chat/active` 由 `[B]` 转 `[]`——**「永久卡住」不复现**。**但前置条件未成立**（脚本自检「两路确实同时进入生成态」返回**否**：A 路被正常服务并快速落库、只有 B 路被丢弃）⇒ 「**两路都在生成态**时兜底是否仍生效」仍属推断；【真机真 provider】【容量 2 与真实网关配额对齐】【后台让路后的时延观感】三条未验，已挂 `test-plan.md` §6 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷 + 一处设计缺口（无并发上限），**不动架构**。**三条通则留痕**：① ★ **`else` 分支等于「从未走过」**——兜底/降级这类「只在异常路径生效」的代码，**不写用例就约等于不存在**，而它偏偏是出事时唯一的那根绳；本仓同类风险点应逐个自查「这个 `else` 有没有测试真的走到过」；② ★ **「限制并发」与「超时兜底」必须同批**——闸门把无限并发变成排队，而排队安全性**完全依赖「持有者一定会释放」**；兜底没修就上闸门，容量会被永久占满，**比不限并发更糟**（从「各自卡」变成「互相拖死」），该判断已写进 `upstream-gate.ts` 文件头注释；③ **配额是「上游」的属性、不是「配置记录」的属性**——故闸门按 `baseUrl` 分桶而非按 provider 记录分，否则同一上游的两条记录会绕开闸门。另留痕一条**容量债**：`openai.ts`／`anthropic.ts` 本批为「顶部 acquire + catch 改写 + finally 释放」各增数行，下次动它们前先看 `llm/` 是否该再拆 |
| **B-004** | **1**<br>（一次修好 → 不再计数；屎山指数 0，作「什么不是屎山」的对照样例） | 侧栏会话列表的「回复中」徽标**指错了会话**：点开一个无关会话，徽标出现在**刚点开的那一项**上，而**原本正在回答的那一项反倒没了标记**；顺带同源的隐性症状——新会话凭空长出「思考中」气泡、新会话输入框被禁用（要再切一次页才解得开） | `32a133b`（老板口头报告，未给提交；更早的 `422d488` 引入「回复中」徽标） | 两处叠加，缺一不可：① **信号源取错**——`App.tsx` 的徽标判据是 `busySid === s.id`，而 `busySid` 来自 `useChatStream` 的 `busy` 上报，**上报本身用了「当前挂载的 sessionId」**（`packages/web/src/features/chat/useChatStream.ts` 的上报 effect，依赖数组 `[busy, sessionId]`）；② **切会话不清 `busy`**——该文件的 SSE 重建 effect 在 sessionId 变化时清了 streamingText/steps/tasks/reasoning/usage/elapsed，**唯独没清 `busy`**，于是切页那一刻 effect 以 `(busy=true, 新 sessionId)` 上报一次 ⇒ 上一间的「在生成」被记到新会话头上。**且 `busy` 会永久卡住**：新会话没有流、永远等不到 `done` 来复位 ⇒ 输入框一直 `blocked`。第三层缺的是「服务端真相」：生成**不随切页中止**（`chat/sse-bus.ts` 的 `res.on('close')` 只 `clients.delete`、不碰 AbortController，`flow` 照跑照落库），切走的那间客户端无从推断 | **✔ 猜中**（读码取证：三处病灶都能在源码里指到行——上报 effect 的依赖数组、SSE effect 缺 `setBusy(false)`、`sse-bus.ts` 的 cleanup 只删订阅者。客观验证：① 新增 `routes/chat-active.test.ts` **7 例**锁服务端「在跑集合」（含「两间同时生成两间都在」「收尾即摘除」「只摘自己那间」）；② 新增 `features/chat/session-busy.test.ts` **11 例**锁并集判据，其中一条**专门把「没清局部信号」的坏输入形状钉下来**，说明「B 不该亮」必须由 hook 清 `busy` 挡住、不能指望判据函数兜；③ 全量 `npm run check` 66 文件 / **808 passed + 1 skipped** 全绿。**视觉症状（切页那一刻的 DOM）按维护约定 4 仍需老板双签**——AI 不自行验收 | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷 + 一处设计缺口，不动架构。**但暴露两条通则**：① **「贴在会话上的状态」必须与它所属的会话一起切换或一起清掉**——`busy`/streamingText/steps 这一族全是「本轮」状态，切会话就是换轮，清一半等于把剩下那一半漂到别人头上（本例漂的正是 `busy`）；② **凡是「离开页面后仍会发生的事」，客户端局部状态必然推不出来，必须由服务端给事实源**——本仓已有的 `aborters` 登记表恰好就是那个事实源（`GET /api/chat/active` 直接读它，不造第二份状态），比在前端加定时器猜「大概还在跑吧」可靠得多。另留痕一条**容量债**：`useChatStream.ts` 修完 **397/400 行**，本批为放注释曾涨到 405 触红线 ⇒ **下次动该文件前必须先拆分**，别靠砍注释自救 |
| **B-003** | **1**<br>（一次修好；屎山指数 0，续作「什么不是屎山」对照样例） | **出题配比功能 web 侧整链断头，用户看不到该设置**：① `api.settings.quizMix()`/`saveQuizMix()` 在 `api.ts` 中**根本不存在**（`settings` 对象只有 searchKeys 三方法），而 `QuizMixCard.tsx:37/60` 与 `QuizBankPage.tsx:37` 都在调 ⇒ 设置页一挂载就抛 `TypeError: quizMix is not a function`、题库页摘要同样拉不到；② `ChatView.tsx` 的 JSX/按钮 title 已引用 `mixTip`（162/164/174/175 行）但 **state 从未声明** ⇒ 对话页 render 即 `ReferenceError` **白屏**。结果：老板把需求原文再发一遍（「现在的出题模式可以加一个设置…」）——不是没做，是做了却看不见 | **WIP 未提交**（HEAD `15accb0`；09-02 配比批 + 09-03 收口批全在工作区 `M`/`??` 未跟踪） | 09-03 11:05「收口」批**交付不完整**：改 ChatView 时只写了引用（JSX/摘要行/按钮 title）与 chat.css 样式，**漏了 `mixTip` 的 state 声明与挂载加载 useEffect**；api.ts 客户端方法（本批新增 GET/PUT `/api/settings/quiz-mix` 的封装）**整体漏加**。而该批 CHANGELOG 验证列却写「`npm run check` 全绿」——**验证失实**：web tsc 实况 8 处错（TS2304×4 + TS2339×3 + TS7006 级联），任何一次真跑 `tsc -p web` 都会当场拦下 ⇒ 推断那批只跑了 vitest（esbuild 转译不查类型）未跑/未看 tsc 输出 | ✔ 猜中（客观验证：修复前 `npx tsc --noEmit -p packages/web` 实报 8 错；补齐 api.ts 两方法 + ChatView state/useEffect 后 tsc×3 + eslint + vitest + gates 全绿，基线 19→20 文件 / 175→**177** 例；新增 `web/src/lib/api.test.ts` 2 例按项目 fetch-stub 范式钉死「方法在、路径对、GET/PUT 动词对、PUT 带 `{ mix }` 体」，杜绝同类整链缺失再次静默） | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷，不动架构。**但暴露一条通则 + 一笔流程债**：① 客户端封装新增方法与组件引用新 state，**tsc 必拦**——前提是「真跑了 tsc 且看了输出」；② 09-03 收口批与 B-002 修复同批执行，验证环节却两次走样（B-002 后补测试、本批连 tsc 都没真跑），「批交付的验证列必须逐命令核对真实输出、不凭印象写全绿」应成为硬约定。另留痕：老板三处 UI（设置页/题库页/对话页）共用一套配比契约，断一处即全局不可见——**跨页共用的前端契约（api 客户端）是断链高发点，值得在测试里钉死** |
| **B-002** | **1**<br>（一次修好；屎山指数 0，与 B-001 同作「什么不是屎山」的对照样例） | 出题配比设置页**只警告不拦截**：四档可配到 40 题（远超总上限 20），超限仅显示一行 warn，`保存配比` 按钮照样可点 ⇒ PUT 后被服务端 `normalizeQuizMix` 从后往前削（解答题先没），保存完数字闪一下变样，用户无从预期 | **未提交 WIP**（HEAD `15accb0`；`QuizMixCard.tsx` 当时尚在 `??` 未跟踪，故按「WIP/未提交」记，不写提交号） | `packages/web/src/features/settings/QuizMixCard.tsx` 的 `bump()` 只写 `Math.min(MAX_QUIZ_PER_TYPE, …)`，**完全没有消费同在 shared 的 `MAX_QUIZ_TOTAL`**；`save()` 的 disabled 只判 `total === 0 \|\| !dirty`，不判超限。根因**不是「忘了写」**，而是**契约导出了两个上限常量而 UI 只消费了其中一个**——常量躺在 shared 里不等于各处都用上了 | ✔ 猜中（客观验证：新 `shared/src/quiz-mix.test.ts` 12 例纯函数锁住钳位规则，含「总额到 20 后加不进任何题型且已有档位一格不动」「先减后加这条路径要通」；真机隔离实例 PUT 四档全 10 → 服务端仍兜底削到 20，证明兜底路径未被本次改动破坏）。**但视觉症状属体验类**：按维护约定 4，须「可复现步骤 + 老板主观确认」**双签**，AI 不得自行验收——**待双签** | WorkBuddy（本会话） | 0（一次修好，记 0 避免把普通 bug 误判成屎山） | 否 → 实现级缺陷，不动架构。**但暴露一条通则**：shared 契约导出多个上限常量时，UI 极易只消费其中一个而漏掉另一个（本例漏 `MAX_QUIZ_TOTAL`）；「常量已定义却未被消费」这类缺口，**应在测试里钉死，而不是在常量旁写注释**。另：**本次改 bug 前未先读本表**（§0.15 第 0 步漏做），事后补读并补登记，流程债留痕于此 |
| **B-001** | **1**<br>（一次修好 → 不再计数；屎山指数 0，作「什么不是屎山」的对照样例） | 忆域 v2 的词条注入在 **Anthropic 型服务商**上完全无效：回复不带术语偏好、也无任何报错。同一份 `flow.ts` 在 openai 型（agnes/openai/deepseek…）上表现正常 ⇒ 缺陷被真机验证掩盖 | `15accb0` | `packages/server/src/llm/anthropic.ts:17` 用 `messages.find(m => m.role==='system')` **只取第一条** system，L18 又 `filter(m => m.role!=='system')` 把其余消息全部剔出非 system 序列，L56 只下发 `system: systemMsg?.content` → 第二条及之后的 system（词条提示、即将新增的资料提示）**在出站请求里凭空消失**。`chat/flow.ts:97` 的注入写法本身没错，错在适配层未做「多 system 合并」；`llm/openai.ts` 全量透传 messages 所以不受影响。**无测试覆盖**（`flow.test.ts` mock 掉了 `router.js`，适配器出站体从未被断言） | 待验证→**✔ 猜中**（反向取证：`git checkout` 退回修复前跑新测 → 10 例中 **2 例红**，首条失败文案 `expected '基础提示词' to contain '忆域词条段'`——正是「第二条 system 凭空消失」的行为签名；次条 `expected '' to be '有内容'` 证 `find('')` 也会吞后段。恢复修复版 → 17 文件 **144 例全绿**、gates 无红。预测与根因完全对应，无需换假设） | Qoder（强） | 预测 0（若一次修好即记「对照样例」，避免把普通 bug 误判成屎山） | 否 → 属实现级缺陷，不动架构。**但暴露一个结构性欠账**：适配器层零出站体断言，跨 Provider 的语义差异（system 拆分/tools 格式/stop_reason）只能靠真机逐个撞——本批已补 `llm/anthropic.test.ts`（10 例），`openai.ts` 仍按 test-plan §6 P1 排队 |

## 维护约定

1. **只进不删**：修好的条目保留并标「已修/对照」，作"什么不是屎山"的参照；
2. **同根因才 +1**：换假设/换模型后的新尝试不重置计数，但须记 `用的模型`；
3. **触发重写评估**：满足《计划重写文档》§10.2 量化闸门任一即强制评估——**本表只引用不复制阈值**（v2 建立时基线：白写率 1.6%，见 `计划重写文档.md` v0.4.4 §10.2 表）；
4. **体验类 bug**（UI 假死/闪烁/卡顿等写不出断言的）：必须「可复现步骤 + 老板主观确认」**双签**，AI 不得自行验收；
5. **★ 计数必须对 AI 可见**：本表若只由人维护、AI 读不到等于不存在。每次改 bug 开工前强制读对应条目，并把计数写进回复首行（GUIDE §0.15 模板）。

## 变更记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-09-20 | v0.1.18 | 登记 **B-017**：**词条页「复习计划」面板半屏高度下整块被静默裁掉** ⇒ 队列打卡键 40 颗 0 颗可达（900×620 时面板高仅 43px）。★ **本表 B-008 的同页同机制反向复发**（B-008 是本体被挤出、本条是面板自己被挤出），**收敛计数累计到 2**；★★ **B-008 当日立的机器判据这次全程绿灯没拦住**——判据里没有 v28 新长的两块，且旧 I4「全部在视区」的口径本身在把实现推向 `overflow:hidden`。★ 修法：新 `.rv-body` 成面板内唯一滚动区（两枚开关留在体外）＋ `.rv-panel` `max-height:45vh` 由构造保证 ＋ 拆掉两处内层限高滚动；探针从「可见」改为「**够得着**」（`reachable()` 走到根判裁切），三态×两视口 → **五态×五视口 270 断言**。★ 实测 270/0 全绿；反向锁（CSS 换回 HEAD 那份）⇒ **254 passed / 16 failed**；打卡键 `0/40 → 40/40`。★ **未验**：老板真机那一遍（内置浏览器 `innerWidth=0` 不可用，证据全出自无头 CDP）；`.term-side` 120px 按钮折两行、`.rv-head` ~137px 死空间为**独立观察**、未混进本批。前任 v0.1.17 的 变更记录行**由「看图失败说人话」批漏登**（B-016 已进登记表、头部已更 v0.1.17，本节缺一行）——**不代其补登**，留痕于此由该会话自行补齐 |
| 2026-09-20 | v0.1.16 | 登记 **B-015**：**`POST /api/sessions/:id/fork` 对「不存在的会话」回 201**，并写下一行指向幽灵父会话的 `forked_from_id`。★ 根因＝端点的唯一判据 `canAccessSession` 在**未登录模式**（`ownerId === null`）下**不查库直接返回 `true`**（本机单人模式的既有语义、本身没错）⇒ fork 成了全仓**唯一「父行可以不存在」的写口**。**修法**＝在其后补 `sessionExists(sessionId)`（查 `sessions` 且 `deleted_at IS NULL`）⇒ 回 404，回归锁 `routes/fork.test.ts` 首例。★ **本条是「先写用例再跑」逮到的**（写第一条用例「不存在 → 404」时先跑了一遍，发现它回 201）⇒ 通则：**给写口写第一条用例时先写「不存在 ⇒ 非 2xx」**，成本几乎为零。★ 同批另锁一条**顺序类**判据：归属断言必须排在归一之前（不存在的会话 ＋ 超长 term ⇒ **404 而非 400**，否则泄露 id 存在性）。**两条通则留痕**：① ★★ **「能不能访问」与「存不存在」是两件事**（授权 vs 存在性），凡「从父行出发建子行」的写口都必须额外自证父行存在；★★ **同一个 helper 在两种运行模式下强度不同**（登录时查库／未登录时恒真）⇒ 「为登录模式写对的代码，在未登录模式下会静默变松」，模式差异必须写进该 helper 的契约注释。② ★★ **「查不到就该 404」要排在参数校验之前**（顺序写反不报错，只泄露「这个 id 存不存在」） |
| 2026-09-20 | v0.1.15 | 登记 **B-014**：**三处图片上限各自合法、乘起来超了** ⇒ 贴满 4 张图点发送被 express 在**路由之前**拒掉，前端只看到「点了发送没反应」（静默失败）。★ 与 B-011/012/013 **同族但不同层**：那三条错在**判据与声明**，本条错在**数值**——没有任何一行代码是错的，故 code review / lint / tsc / 单测**全部零信号**。★ 根因＝`ChatComposer.tsx`（4 张）／`chat/vision.ts`（单图 `7_000_000` **字符**）／`server/index.ts`（`'24mb'` **字节**）三处分散，**字符 → 字节的 4/3 base64 膨胀被漏掉**（7M×4 ≈ 28MB > 24mb）；注释**已经写对了**「改一处必须同步改另一处」，但没有机器守着它。★ 修法＝新 `shared/src/chat-limits.ts` 收口 + 不等式锁 + ★ **反向锁**（旧 7M 代回必须超限）；单图 7M→5M 字符（按最坏报文反推）。★ 本批同时补上输出侧 markdown 图片语法（渲染层此前不认 `![](url)`），并把前端「过大的图当场被拒」补成可测纯函数。实测 `npm run check` CHECK_EXIT=0（160 文件 / 2211 passed + 1 skipped + gates 四绿）。**未验一条如实挂 §6**：真机端到端（贴图 → 视觉蒸馏 → 主模型作答）未跑 |
| 2026-09-20 | v0.1.14 | 登记 **B-013**（**`fetch_page` 的 PNG 魔数判据自落码起从未命中**：字符串层正则 `/^(?:%PDF-\|GIF8\|\uFFFD PNG\|…)/` 里 **`\uFFFD` 与 `PNG` 之间多了一个 ASCII 空格**，而真 PNG 按 UTF-8 解出来是 `\uFFFD`+`PNG`（**无空格**）⇒ 该分支恒不匹配；且判据放在**解码后的字符串**上，会被同日新增的编码层**回退 GB18030** 时抹掉（`89 50` 被吃成一个汉字）⇒ **魔数这层在真实链路上始终零作用**，一直由控制字符占比单独扛着）。**无观测症状**（真 PNG/PDF 头部控制字符占比 9%~12%，控制字符规则独立即可拦），判「非活洞」——**但由此证明 B-011 那批的 5 例新测试全都区分不出层次**：那批用例全是**高控制占比**样本，短路哪条判据都照样红。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附**四类证据**：`cat -A` 核出空格是**真实 ASCII 空格**；**两层判定对照实测**（128 字节夹具＝PNG 签名＋高字节）字符串层 UTF-8 **0.78%**／GB18030 **1.47%**、魔数均不命中 ⇒ **漏放**，字节层魔数命中 ⇒ **拦下**；**新 1 例**（`fetch-page.test.ts` 16→**17**，**全文件唯一能区分两层**的用例）；**§7 改坏取证**（嗅探挪回字符串层 ⇒ **恰好 1 例红、其余 17 例全绿**）。**三条通则留痕**：① ★★ **「写了判据」≠「判据会命中」**——新增任何魔数／白名单／正则分支**必须配一条能证明它命中**的用例，否则就是 B-005 的 `else`、B-011 的「契约里的不支持」同类：**代码在、行为不在**；② ★★ **判据属于哪一层就放哪一层**——关于**字节**的判据放**字符串**上，会被**同批新增的解码层**悄悄削弱甚至抹掉 ⇒ **「这一批新加的东西会不会让上一批的判据失效」应成为批次收尾的固定自问**（本批正是这一问救回来的）；③ ★ **怀疑字面量写错先 `cat -A`**——`\uFFFD` 与空格在编辑器里几乎无法分辨 |
| 2026-09-20 | v0.1.13 | 登记 **B-012**（**`fetch_page` 把 GBK 页以乱码形态当「正文」回灌**：`res.text()` **恒按 UTF-8 解码、忽略 `content-type` 里的 `charset`** ⇒ GBK 字节退化成满屏 `\uFFFD`（**解码不抛错**，与 B-011 同一条「静默」性质），`htmlToText` 不碰这些字符、判空也过 ⇒ 乱码进回灌。★★ **决定性一例**：当当网 `content-type: text/html; charset=GBK`——**服务端已明确声明，而代码里没有一处读它**；「对端说了我们没听」比「对端没说我猜错」更难辩解）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附**五类证据**：**候选侦察**扫 47 个中文站点命中 **3 个真 GBK 页**（湘潭市政府／岳阳市政府／当当网；**这一步是防空转的**——不做就容易拿一堆 UTF-8 页测出「未见异常」而其实从未验到 GBK）；**修复前真机**三例全部「当正文回灌」且替换符占比 61.8%／65.5%／60.7%；**修复后同一凭证复跑**三例替换符 **0.0% 且内容真的解对**（"湘潭市人民政府门户网站…政务公开 政务服务 互动交流"／"岳阳市人民政府 个人门户…"／"当当网 欢迎光临当当，请登录免费注册…"）——**不只是数字归零，是文本正确**；**新 3 例**（`fetch-page.test.ts` 13→**16**：声明 GBK 按声明解／未声明但字节是 GBK 靠嗅探回退／★ **不误伤**——未声明 charset 的 UTF-8 页必须照常按 UTF-8 解）；**§7 改坏取证**（退回 `res.text()` → **恰好 2 例红**，「不误伤」那条仍绿 → 恢复 16/16 绿）。**四条通则留痕**：① ★★ **`res.text()` 是「假设 UTF-8」的 API**——凡按文本消费远端字节，先看 `content-type` 的 `charset`，**它就在你手里，不读是选择不是无奈**；② ★★ **同一症状可以有两条互不覆盖的根因**——「乱码冒充正文」既可能来自「这不是文本」（B-011）也可能来自「文本解错编码」（本条），**修了前者不代表后者消失**；故每加一道闸门都要自问「**它拦得住这个症状的哪几条根因**」，答不上来的部分要**单独登记**；③ ★★ **兜底判据不能顺手复用**——内容闸门**刻意不看**替换符占比正是为了不误杀 GBK 页，若把「替换符多」当二进制特征复用就会把正常中文页也拒掉（**修 A 引入 B**）；④ **有声明就别猜**，只在没声明时才嗅探，且嗅探用「替换符更少者」这种**可量化**比较而非 `fatal:true` 硬判 |
| 2026-09-20 | v0.1.12 | 登记 **B-011**（**`fetch_page` 把 PDF／PNG 当「正文」回灌给模型**：`run()` 只判 `if (!res.ok) throw` 便直接 `res.text()`，**全程无内容类型检查**；而 `res.text()` 解二进制**不抛错**（非法字节退 `\uFFFD`、控制字节原样留）、`htmlToText()` 是**纯正则剥标签**（`%PDF-` 与二进制流不含 `<>`，一条正则都命中不了），`\s+` 又**不匹配 NUL 等控制字符** ⇒ 非空字符串通过 `if (!text)` 判空 ⇒ 被当正常正文回灌。★ **契约 §5.3 原文早已写明「JS 渲染页与 PDF 明确不支持」，但实现里零行代码表达它** ⇒ 「不支持」静默退化成「产出垃圾」）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附**五类证据**：**真机探针** `tools/probes/fetch-page-live.mjs`（★ 真调 `runTool`，esbuild 现场转译 TS 源、不复现实现；`SB_DATA_DIR` 指向临时目录不碰真库）修复前 PDF 回灌 **8021 字**（PDF头=true／控制字符 850／替换符 3415）、PNG **4736 字**；**修复后同一凭证复跑** PDF／PNG／SVG **三例全拒、零字节二进制外泄**；**新 5 例**（`fetch-page.test.ts` 8→**13**，含「谎报类型」与「不误杀」两侧）；**§7 改坏取证**（两道闸各自短路 → **恰好 4 例红**，「不误杀」那条仍绿）；全量 `npm run check` **CHECK_EXIT=0**（158 文件 / 2179 passed + 1 skipped + gates 四绿）。**四条通则留痕**：① ★★ **「读到了」≠「读对了」**——`res.text()` 这类读口必须先答「对端是什么类型」，**类型闸门放在解码之前**（既正确又省内存）；② ★★ **解码器不抛错 ≠ 内容可用**——UTF-8 解任意字节都成功，**静默产出乱码是它的默认行为**；③ ★ **契约写「不支持 X」，实现里必须有一处可指行的拒绝分支**，否则「不支持」就是一句空话；④ ★ **探针的空洞证明**——首版非 HTML 段只挂**单候选**（w3.org `dummy.pdf`）被 **403** 拒掉 ⇒ 那条路径**根本没被执行**，结论却写「未见异常」；改多候选 + 「候选全被拒即报未验到」。★ **旧账如实记账（不回填）**：本表「变更记录」缺 **v0.1.10（B-009）** 与 **v0.1.11（B-010）** 两行——两批交付时只更新了顶注与登记表、未落变更记录；本次只补本行，不代造历史行 |
| 2026-09-18 | v0.1.9 | 登记 **B-008**（**词条页里「词条库本体」整个不可达**：`80e97a4` 把艾宾浩斯复习面板插到词条列表**之前**，而词条页是「固定高度 + `overflow:hidden` + **只有 `.term-list` 一个弹性子项**（其余兄弟全是 `flex-shrink:0`）」的 flex 列 ⇒ 面板无 `max-height`、内容撑到 **2073px**，flex 项默认 `min-height:auto` 压不下去，于是**列表被压成 4px、搜索/添加/编辑/删除被推出视口、父级 `overflow:hidden` 直接裁掉且不给滚动条**（**滚不到**，看起来就像功能被删了）。老板原话是「词条库本体哪里，你把原功能给覆盖了……增删改查/查询/滑动列表都没了」。**但代码与数据一行未删**：`TermsPage.tsx` 提交史逐笔 `--stat` 查过（唯一带删除的 `ce96927` 只是把内联 Tab 抽成组件），历史上真删过的只有 `15accb0`（09-01 忆域 v2 删掉旧 `MemorizePage.tsx`）——**而把那个被删文件读出来看，它本来就只有「添加 + 翻卡复习」**，从没有完整增删改查 ⇒ 老板说的「原功能」不是它；后端真值也一致（`/api/terms` 有词条、`/api/terms/domains` **total 278**、`review/overview` `due 271 / overdue 217`）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附**真机像素对照**：从源读真 CSS + 按 JSX 手搭 DOM 喂无头 Chrome，**同 DOM 跑「有面板／无面板」**——有面板时 `.term-page` `clientH 800 / scrollH 2494`、`.rv-panel` **2073px**、`.term-list` **4px 且在视口外**、搜索/添加/编辑/删除**全不可见**；摘掉面板后 `scrollH 800` 不裁、列表 **407px 可见**、上述控件**全可见**（因果闭合）；**两档视口（800/1000）面板高都是 2073px**（内容撑的，不随视口变）⇒ **任何屏幕都坏**。**修法**：`.rv-panel` 自收敛（flex 列 + `flex-shrink:1` + `min-height:0` + `overflow:hidden`）+ 队列限高内滚（`max-height:240px`）+ **队列默认收起**（收起态保留一行欠账摘要，信息不丢）+ `.term-list` 给 **160px 保底**。修复后复跑探针：收起态列表 **334px（800）/534px（1000）**、展开态仍保 160px、队列内滚，三态 × 两视口 **56 断言全绿 EXIT=0**；gates 全绿、全量 vitest **116 文件 / 1580 例**。★ **如实标出两条**：**未在真机点过**收起/展开的点击（探针只验两种静态结构）；**800px 高视口下展开态队列只剩 42px**（列表 160px 保底 + 其余固定块分完空间，靠内滚仍可达）——是「词条库本体优先」的**自觉取舍**，观感须老板目检。**三条通则留痕**：① ★★ **「功能没了」≠「代码删了」**——报「功能被删」时正确动作是**三条证据交叉**（`git log --stat` 逐笔 ＋ **读被删文件的内容**而不只看它存在过 ＋ 后端真值），本次若跳过会顺着「AI 删了老板功能」的错误前提去「恢复代码」，**白改且根本找不回来（因为没丢）**；② ★★ **「固定高度 + `overflow:hidden` + 只有一个弹性子项」是脆弱结构**——插入任何内容驱动高度的兄弟都会**静默吃掉**它、且**因无滚动条而不可达**（比溢出更难查）；必须给弹性子项 `min-height` 保底，或给大个子兄弟 `max-height` + 内滚/可折叠；③ ★★ **编译/单测/gates 对布局类回归完全无感**——本批 tsc／eslint／1580 例／四条 gates **全绿**，同一 commit 里页面却有 2073px 面板撑爆 800px 容器 ⇒ 本仓无 jsdom，布局不变量**只能真机量 rect**，故当日补 `tools/probes/terms-layout-cdp.mjs`（**6 条不变量 56 断言**，零依赖零副作用、不需后端不需登录） |
| 2026-09-18 | v0.1.8 | 登记 **B-007**（**第二轮起的 SSE 帧被静默丢弃**：服务端 `startNewRound()` 每轮把 `seq` **重置为 1**，而客户端 `since` **跨轮单调不重置** ⇒ 第二轮全部帧的 `seq` 都 `≤` 上一轮攒下的 `since`、被前端一条不剩当重复帧丢掉；若第二轮比第一轮短，**连 `done` 一起被吃掉** ⇒ 回复不上屏 + `busy` 永久卡死。**两端各错一半**：客户端该重置时没重置、服务端 `subscribe()` 又把陈旧的 `since` 当回放起点 ⇒ 连全量回放都不可达）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附**四份证据**：隔离实例两轮实验（`_probe/coach-seq-diag.mjs`）丢帧前后的**正文起点对照**、修复后复跑第二轮**从头开始**、16 例新／改测试（`sse-seq.test.ts` 8 + `sse-bus.test.ts` 6→8，含一条**防过度修复**的「不越界」锁）、全量 vitest **115 文件 / 1555 例全绿**。**如实标出探针与复跑都在隔离实例上**，生产实例的复现属推断（但纯口径问题、开第二轮必现）。**三条通则留痕**：① ★★ **「`seq` 从几开始」是第四个消费者才撞出的契约空洞**——聊天流／PK 流／督促流共用 `lib/sse-client`，前两个「一轮到底」从没检验过单调假设，而督促小窗**会在一轮结束后又开一轮** ⇒ 假设一破即出 bug（**凡复用一套设施，必须回头把隐含假设写成断言**）；② **去重的「重置信号」必须显式化**（服务端一直用「`seq` 重新等于 1」表达新一轮，客户端却完全不知道有这回事 ⇒ 抽 `acceptSeq(since, seq)` 纯函数，不让客户端猜）；③ **服务端的回放起点要自证有效**（`since` 是客户端给的外部输入，用前必须判它是否落在本轮区间内，与「客户端给的游标一律当不可信输入」同一条）。★ 另附**探针工程教训**：症状最初伪装成「探针抓错了卡」，**差点把真缺陷当成探针 bug 收工**——直到改用服务端真值做基线，才露出 `apiAi:8 / domAi:7 / busyStop:true` 这条决定性对照。**「探针红」的第一反应该是「谁的真值更可信」，不是「改探针让它变绿」** |
| 2026-09-17 | v0.1.7 | **B-006 开关侧闭合（v0.2.44 联网开关强绑批，老板拍板方案 ②）**：B-006 当初留了一条产品债——前端「联网已开」pill 与后端能力之间**没有任何契约**（`online` 只喂给出题），属「UI 打开了后端不认」的用户可感知谎报，接法待拍板。老板选 **② `tool_choice` 强绑**（而非 ① 注入硬指令：强绑是**工程保证**、不依赖模型自觉）。**四层接线**：前端 `ChatView.online` 经 `useChatStream`→`useSendActions`→`api.chat.send/regenerate/resend`（走 deps 不新增 `send` 参数 ⇒ `useGrillChoice` 零改动）／`/chat/send`、`/regenerate`、`/resend` **三条路由都收 `online`**（**重跑与发送同口径**，只接一条会变成「同一次提问两种行为」）／新装配层 `chat/opening.ts`（`buildOpening`／`dropOpening` 纯函数，与 `chat/grill.ts` 同范式）／`flow.ts` 的 `toolChoice` 改由 `opening.toolChoice` 供给（`turn===0` 才绑）。**强绑只有一个名额**：grill 与联网同开取 **grill 优先**（`GRILL_PRE` 是硬性表述 vs `SEARCH_FORCE` 是软性的，冲突时强绑 search 会让提示词与出站参数互相打架），由 `opening.test.ts` 锁死。★ **测试当场逮到一条真缺陷**：初版照搬 grill 的「出循环即摘」，而联网指令说「先搜再答」、留到次轮会让模型**每一轮都重搜直到撞轮次上限** ⇒ 改为**首轮一过就摘**（循环外留幂等兜底，覆盖「首轮无工具调用直接 break」）。★ **行数门禁处置**：`flow.ts`(399/400) 与 `routes.ts`(399/400) **都贴线**，故把装配/摘除收进新文件（`flow.ts` 净减 1 行），并清掉 `routes.ts` 两处**等价冗余**（同源 `sse-bus` import 合并、`getProviders().map((p) => p)` 恒等映射——已核实 `getProviders` 内部本就显式挑字段、无 `api_key`）。基线 1417 → **1431**（**+1 文件 / +14 例**：新 `chat/opening.test.ts` **10** + `chat/flow.test.ts` 30→**34**）；`npm run check` CHECK_EXIT=0。★ **未验三条挂 `test-plan.md` §6**（真机端到端／默认「开」的成本与延迟／词表与强绑的覆盖关系） |
| 2026-09-17 | v0.1.6 | 登记 **B-006**（**模型明明能联网搜索、却一口咬定自己没有这个功能**：`search_web` 的工程链路逐条证伪后全通（注册表 / `flow.ts:172` 下发 / 两个适配器都带 `tools` / provider 链与 key 齐全）、真机上 2026-09-14 也**成功搜出过结果**，但 `chat/system-prompt.ts` 对它**零声明**（chart / html / update_tasks / ask_choice 都有引导、`ask_choice` 甚至有 12 行强制引导），而 `tool_choice='auto'` 下模型**不会主动内化未声明的工具** ⇒ 经常直接回「我没有联网工具」、连一次调用都不发；同源症状还有三条：**前端「联网已开」pill 对普通对话零接线**（`online` 只进 `useQuizActions` 出题）、**会话内自我复述**（说过一次就进历史，实测同会话三次越发肯定）、**失败回灌反向教育**（旧文案甩「未配置搜索 key / 本网络可能不可达」并附「或基于已有知识回答」的放弃台阶）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附三类证据：**真库读码取证**（09-14 成功搜索记录 + 09-09 模型自述「联网搜索功能确实存在」+ 现场那轮 `tool_calls` 给的是 `ask_choice`）、**新 30 例**（语料即现场原话）、以及一条**反向证据**（改回灌文案后既有用例当场红 ⇒ 修复生效的机器证明）。**三条通则留痕**：① **「加工具不配触发层 = 功能不存在」在本仓第二次应验**（首次 2026-09-14 `ask_choice`）⇒ 主动调用型工具的验收项必须含三层触发，只注册不算交付；② **system prompt 的能力清单＝模型的自我认知来源**，漏一个工具等于那个能力不存在——工具清单（`tools`）说明「可以调什么」，能力声明（system prompt）才决定「我是什么」，二者不可互相替代；③ **给模型的错误回灌不许泄漏内部配置、不许给放弃台阶**。另记一条**产品债**：前端开关与后端能力之间没有任何契约（「UI 打开了后端不认」＝用户可感知的谎报），接法待老板拍板 |
| 2026-09-17 | v0.1.5 | 登记 **B-005**（两路会话同时运行时双双永久卡在「回复中」：两个适配器的等待兜底写成 `if (req.signal) {桥接} else {120s 兜底}` 的**二选一**，而生产路径**总是**传 signal ⇒ 那句 120s **从未生效**，上游挂住时 fetch 永久 pending、`runTurn` 不 settle、`aborters` 不摘除；同源第二症状是**配额踩踏**——每轮 `done` 后的 `extractTerms`／`compactIfNeeded` 会立刻抢走刚空出的配额）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」给 ✔ 并附三份证据：新 33 例（gate 13 + timeout 12 + wiring 8，全假时钟假上游）、全量 `check` 101 文件 / 1344 passed + 1 skipped + gates 全绿、**隔离实例真机核验**（被丢弃那路在 180s 被兜底清掉、`active` 由 `[B]` 转 `[]`）。**同时如实标出前置条件未成立**（脚本自检「两路同时进生成态」返回否）与三条未验项，均挂在 `test-plan.md` §6。**三条通则留痕**：① 只在异常路径生效的 `else`，**不写用例就约等于不存在**；② **「限制并发」与「超时兜底」必须同批**（兜底没修就上闸门，容量会被永久占满，比不限并发更糟）；③ 配额是**上游**的属性、不是配置记录的属性（故按 `baseUrl` 分桶）。另记容量债：两适配器各增数行，下次动前先看 `llm/` 是否该再拆 |
| 2026-09-14 | v0.1.4 | 登记 **B-004**（侧栏「回复中」徽标漂移：徽标绑在「当前挂载的会话」而不是「正在生成的会话」上——`useChatStream` 的 busy 上报用了当前 sessionId，且切会话时漏清 `busy`，于是上一间的在生成被记到刚点开的那间头上、原来那间反没了标记；同源隐性症状还有新会话凭空长出「思考中」气泡、新会话输入框被 `blocked = busy` 一直禁用）。收敛计数 1、屎山指数 0；「是否猜中」给 ✔ 并附**三处可指行的读码取证**与**18 例新测试**（服务端在跑集合 7 + 徽标判据纯函数 11）+ 全量 `check` 66 文件 / 808 passed + 1 skipped 的证据。**视觉部分按维护约定 4 标「待老板双签」**——AI 不自行验收切页那一刻的 DOM。**两条通则留痕**：①「贴在会话上的状态」必须与所属会话一起清（清一半等于把剩下的漂到别人头上）；②「离开页面后仍会发生的事」客户端推不出来，必须由服务端给事实源（本例复用既有 `aborters`，不造第二份状态）。另记**容量债**：`useChatStream.ts` 修完 397/400，本批曾涨到 405 触红线 ⇒ 下次动它前先拆分 |
| 2026-09-03 | v0.1.3 | 登记 **B-003**（出题配比 web 侧整链断头：api.ts 缺 `settings.quizMix`/`saveQuizMix` 两方法 + ChatView 引用未声明的 `mixTip` ⇒ 设置页 TypeError、对话页白屏，用户看不见已实现的功能；同批 CHANGELOG「check 全绿」验证失实——web tsc 实况 8 处错）。收敛计数 1、屎山指数 0，续作对照样例；「是否猜中」✔ 附 tsc 8 错实证与补后全绿（20 文件 / 177 例）证据。**通则留痕**：批交付验证列必须逐命令核对真实输出；**跨页共用的 api 客户端契约是断链高发点，已在 `web/src/lib/api.test.ts` 钉死** |
| 2026-09-03 | v0.1.2 | 登记 **B-002**（出题配比设置页「只警告不拦截」：UI 只消费 `MAX_QUIZ_PER_TYPE` 漏掉 `MAX_QUIZ_TOTAL`，40 题能配能存、保存后被服务端从后往前削，数字闪变）。收敛计数 1、屎山指数 0，与 B-001 同列「什么不是屎山」对照样例；「是否猜中」给 ✔ 并附客观证据（12 例纯函数测试 + 真机隔离实例验证兜底路径未破坏）。**体验类症状按维护约定 4 标「待双签」**——AI 不自行验收视觉闪变。**同时留痕一条流程债**：本次改 bug 前未先读本表（§0.15 第 0 步漏做），事后补读补登记，未因漏读而改变处置（本表此前无同根因条目） |
| 2026-09-02 | v0.1.1 | **B-001 收口**：收敛计数停在 1（一次修好）、屎山指数记 0 作对照样例、「是否猜中」由「待验证」改为 **✔ 猜中** 并写入**反向取证证据**（先把 `anthropic.ts` 退回修复前版本、确认新测试真的红、再恢复修复版），不把「AI 自称修好」当验收。同日「升级架构问题」列里的欠账已标上已补部分（`openai.ts` 仍挂 P1） |
| 2026-09-02 | v0.1.0 | 建表（§0.15 第 0 步载体补齐）。首条登记 **B-001**：`anthropic.ts` 丢弃第二条 system ⇒ 词条注入在 Anthropic 型 provider 上静默失效，收敛计数 1、预测根因已入账、验证待出站体断言给出。刻意**不复述** GUIDE/§10.2 的规范正文与阈值数值，只留指针（防 §0.11 双真相源漂移） |
