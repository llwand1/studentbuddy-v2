# AGENTS.md — studentbuddy v2 项目导航

> AI 改本仓代码的第一入口。**v1 的 gateway/office 命名/双轨工具/工作区全家桶一律不存在**——开工前先读规划。

## 形态声明（最高优先级）

- 纯本地 Web：`api :18791(仅 127.0.0.1) + web :5173`；SQLite WAL；单用户
- **npm workspaces 三包**：`@sb/shared`（契约）/ `@sb/server` / `@sb/web`
- 端口被 v1 占用时：`SB_PORT=18792 SB_PROXY_TARGET=http://127.0.0.1:18792`

## 六条 ADR（凌驾模块设计，全文见规划 §2.0）

1. 需求为纲：模块必须答「服务学习闭环哪一环」
2. 简洁优先：安全做必要最小（密钥加密/Origin/SSRF+白名单三件），无审批门无策略引擎
3. 域自治：chat/quiz/terms/feedback 只经 shared 契约 + 事件总线交互
4. 失败隔离：次级功能失败只记日志；LLM 输出 normalize+降级不崩
5. 体验契约：操作必有成功/失败/进行中三态，禁止静默
6. 数据容错：schema_version 逐语句迁移；迁移永不触碰 v1 原库

## 工程红线（CI 强制）

- 单文件：server ≤400 行 / web 组件 ≤300 行
- 禁内联 `style={{`（用 tokens.css token）；禁 `any`；测试也禁 `!` 非空断言（`no-non-null-assertion` 随 `tseslint.configs.recommended` 生效，要断存在就写会抛错的辅助函数）
- `npm run check` = tsc×3 + eslint + vitest + gates，全绿才许提交
- **改代码必带测试**（个人开发文档 §0.8）：跑完同步 `docs/dev/test-plan.md` 的基线数与不变量清单；**改 bug 前先读 `docs/dev/bug-ledger.md` 查收敛计数**（§0.15 第 0 步，该表是流程的数据源，不读等于第一脚空转）
- **★ 上线进展与部署闸门看 `docs/dev/launch-plan.md`**（2026-09-18 新建）：它是**上线阶梯的唯一进展台账**，也是「**什么时候可以正式上线**」的唯一答案（§3 四类闸门：代码 / 配置 / 外部依赖 / 人工验收）。★ 老板指令（2026-09-18）：**每完成一个上线阶梯批次就汇报一次总体进展**，并同时更新该文档 §2 状态与 §5 汇报。⚠️ 批次「要做什么」的准绳在 `AUTH-SPEC §7` 与 `TENANCY-SPEC §8`，本文档**只记做到哪了**（不复制，避免第二真相源）

## 多 AI 并行施工指针（同行 09 补充）
> **多会话同仓施工时**先读 `Desktop\llwan的个人开发文档\多AI协作施工规范.md` v0.2.0 [DONE]——红线 9 条（含运行时资源并发 R7 / 未入库模块引用 R8 / 版本撞号 R9）＋纪律 12 条。个人文档是唯一真相源，不在本仓复制（防第二真相源）。

## 目录速查

| 路径 | 职责 |
|------|------|
| `packages/shared/src/sse-events.ts` | SSE 事件契约（先登记再实现，同步 docs/SSE-CONTRACT.md） |
| `packages/web/src/lib/sse-seq.ts`（＋消费点 `sse-client.ts`、＋服务端 `packages/server/src/chat/sse-bus.ts` 的回放起点） | **SSE 帧去重口径的单一事实源**（2026-09-18 新建，`bug-ledger` **B-007** 的修复产物）：`acceptSeq(since, seq)` —— **`seq === 1` 视作「换轮」并把 `since` 重置**，其余情况 `seq <= since` 即丢弃。★★ **为什么需要它**：服务端 `sse-bus.ts` 的 `startNewRound()` **每轮把 `seq` 重置为 1**（`publish` 里 `seq = buf.events.length > 0 ? last.seq + 1 : 1`），而客户端 `since` **跨轮单调递增、从不重置** ⇒ **第二轮全部帧的 `seq` 都 `≤` 上一轮攒下的 `since`**、被前端一条不剩当成重复帧丢掉；第二轮比第一轮短时**连 `done` 一起被吃掉** ⇒ 回复不上屏 + `busy` 永久卡死（症状与 B-004／B-005 同族，而根因各在一层：B-004 是前端状态漂移、B-005 是上游兜底失效、本条是**帧静默丢弃**）。★ **修法即「把重置信号显式化」**：服务端其实一直在用「`seq` 重新等于 1」表达「这是新一轮」，只是客户端**完全不知道有这回事**——抽成纯函数后**不让客户端猜**；服务端 `subscribe()` 同步加「`since > maxSeq` ⇒ 退回 0 全量回放」（★ `since` 是**客户端给的外部输入**，用前必须判它是否落在本轮区间内，否则一个陈旧值就能让整轮**连回放都不可达**）。★ **为什么现在才炸**：聊天流／PK 流／督促流**共用** `lib/sse-client`，前两个都是「一轮到底」（「`seq` 单调」这个隐含假设**从没被检验过**），**只有督促小窗会在一轮结束后又开一轮**（换个问题＝新一轮）⇒ **单调假设第一次被打破即出 bug**。⇒ **凡复用一套设施，必须回头把它的隐含假设写成断言**，而不是等第四个消费者来撞（同族：`api.ts` 的 `request` 无默认超时，也是复用久了才暴露）。★ 本文件是 `.ts`（**按 ≤400 判**），配纯函数测试 `sse-seq.test.ts` **8 例** |
| `packages/shared/src/content-blocks.ts` | 内容块协议（演进③；新卡片=登记 kind+注册渲染器）**+ 出题题型配比契约**：`QuizType`/`QuizMix`/`DEFAULT_QUIZ_MIX`/`normalizeQuizMix`（负数小数→0/取整、超上限从后往前削、全 0 回退默认）/`QuizMixReport`，前后端共用一份 |
| `packages/shared/src/answer-style.ts` | **回答方式偏好契约**（契约 `docs/ANSWER-STYLE-SPEC.md` v1.0，前后端共用一份）：四维 `AnswerStyle`（verbosity 详略 / tone 口吻 / support 辅助 / shape 形状）+ `ANSWER_STYLE_FIELDS`（每个选项的 label/hint/**要注入 prompt 的那句 line**——UI 文案与提示词同源，不可能各说一套）+ `SETTING_KEY_ANSWER_STYLE`（`app_settings` 键名唯一登记处）+ `normalizeAnswerStyle`（**逐字段**回落默认，一个非法值不作废整份偏好）+ `styleSummary` + `buildAnswerStyleBlock(style, target?)`（`target:'quiz'` 那支额外带「反斜杠写两根」与「整套题一次写完」两句护栏）。**默认档必须逐字等价现状口径**——新增强指令会让所有历史行为漂移 |
| `packages/shared/src/doc-rag.ts` | **文档检索常量单一事实源**（契约 `docs/DOC-RAG-SPEC.md`）：`MAX_DOC_CHARS=60_000`（★ 语义已是「**直塞阈值**」而非「截断上限」——超过它就换形状注入，不再砍尾）/ `DOC_CHUNK_CHARS=800` / `DOC_CHUNK_OVERLAP=120` / `DOC_TOP_K=12` / `DOC_INJECT_BUDGET_CHARS=12_000` / `DOC_EXTRACT_CHUNKS=36` / `DOC_EXTRACT_BUDGET_CHARS=30_000` + `RetrievedChunk`。**存在理由：前端 `DocModeControl` 曾手抄一份 60k 阈值，双真相源已造成一次漂**；★ 每个常量的取值理由都是 §8 的探针实测数，**改值必须重跑 `tools/probes/doc-rag-bm25.mjs`，不许拍脑袋** |
| `packages/shared/src/chat-limits.ts` | **聊天图片三类上限的单一事实源**（2026-09-20 看图链路收口批新建）：`MAX_CHAT_IMAGES`（一次最多几张）／`MAX_IMAGE_DATAURL_CHARS`（单张 dataURL 字符上限）／`CHAT_BODY_LIMIT` + `CHAT_BODY_LIMIT_BYTES`（`/api/chat/send` 的 body 上限）＋ `worstCaseChatBodyBytes()`／`maxImageSizeHint()`。★★ **存在理由是一次真被踩到的 413**：这三个数原本分散在 `ChatComposer.tsx`（4 张）、`chat/vision.ts`（单张 7M）、`server/index.ts`（`'24mb'`）三处，**各自都对、乘起来超了**（7M×4 ≈ 28MB > 24mb）⇒ 贴满 4 张图点发送，express 在**路由之前**就把请求拒掉，**前端一无所知**（症状＝「点了发送没反应」，服务端零日志可查）。★ **单图上限 7M→5M 不是拍脑袋**：`worstCaseChatBodyBytes()` 按「4 × (单图 + JSON 开销) + 文本预算」反推，无非负约束只有一条——**最坏报文必须 ≤ body 上限**；`chat-limits.test.ts` 把这条不等式连同**反向证明**（旧 7M 值代回必须超限）一起锁住。★ **改这三个数只需改本文件**：三个消费点全部改为从它 import（`vision.ts` 与 `ChatComposer.tsx` 的本地字面量已删、`index.ts` 读 `CHAT_BODY_LIMIT`）。★ 同族先例：`doc-rag.ts`（常量双真相源已造成过一次漂） |
| `packages/shared/src/memory.ts` | **长期记忆契约**（契约 `docs/MEMORY-SPEC.md` v1.0，2026-09-15 新建，前后端共用一份）：`MEMORY_KINDS` 四类白名单（`profile`/`preference`/`weakness`/`goal`）+ `isMemoryKind`（**非法值一律丢弃、不回落成某一类**——猜错的类别比没类别更糟：一条「薄弱点」被当成「学习偏好」注入会误导此后所有会话）+ `MemoryItem`/`MemoryDraft`/`CompactResult` + 六个常量（`COMPACT_KEEP_TOKENS`/`COMPACT_MIN_DISCARD_TOKENS`/`SUMMARY_MAX_CHARS`/`MEMORY_MAX_ITEMS`/`MEMORY_INJECT_MAX_CHARS`/`MEMORY_MIN_IMPORTANCE`）+ `normalizeImportance`（**★ 空值必须回落 0.5 而不是 0**——`Number(null) === 0` 而**不是 NaN**，直接过 `Number()` 会把「模型没给字段」变成「最不重要」⇒ 那条记忆被淘汰且永不注入；这是本批实测逮到的真缺陷）+ `normalizeMemoryContent`。**判定逻辑（切点/淘汰/协议解析）一律留在 server，前端不重算** |
| `packages/shared/src/domain.ts` | Session/Message/RoleBinding(演进①)/TermItem(词条库字段) |
| `packages/shared/src/term-highlight.ts` + `packages/web/src/features/chat/{term-index.tsx, TermText.tsx, TermCard.tsx, term-highlight.css}` + `packages/server/src/learning/term-usage.ts` | **AI 回复词条高亮 + 悬浮卡（2026-09-19 新建，契约 `docs/TERM-HIGHLIGHT-SPEC.md` v1.0）**：正文里命中词条库的词标出来（下划线：**首现**强调 / **复现**淡显），悬停出速览卡、点击出完整卡（别名 + 复习状态 + 纳入/移出复习 + 打开词条库）。★★ **匹配规则上提为唯一事实源**——`textHitsKey`（服务端 `countUsage` 复用）/ `findTermHits` / `createTermMatcher`（前端复用，合并正则**编译一次、扫描多次**）；`term-usage.ts` 原私有 `replyHitsKey` **已删除**。规则留两份必然出现「统计说命中 3 个、屏幕只标出 2 个」（`doc-rag.ts` 常量双写同款病），等价性由 `terms.test.ts` **21 例**锁住。★ **数据路线＝前端索引**（契约 §5 记账）：流式要**逐字即时**高亮 ＋ 历史消息也要能高亮，两条只有前端索引能同时满足；**零迁移、零新路由、零 SSE 契约改动**。★ **卡片两态**（同 COACH-SPEC「一个组件两种状态」）：悬停＝速览、点击＝完整；卡片走 `createPortal` 到 body——正文宿主是 `<p>`/`<li>`，卡片是 `<div>`，内联即非法嵌套且会被 `.chat-scroll` 的 `overflow` 裁掉。★ **「标记已掌握」刻意不做**：没有对应接口（复习推进只有 `POST /api/terms/:id/review`，语义是"复习一次"而非"免复习"），**不做假按钮**。★ **首现口径＝同一渲染块内首现**（整条回复内首现需要块在全文中的字符偏移，而 `parseBlocks` 只给块、不给偏移；块内首现已能消除长回复的主要噪声）。★ **行内 `code` 与围栏代码块不高亮**（代码里出现 `let` 是语法，不是词条）。★ 接线只两处：`Markdown.tsx` 的 `InlineNodes` 文本分支走 `TermText`；`App.tsx` 挂 `TermIndexProvider`（复用既有 `openTerms`，**ChatView 零 props 改动**） |
| ~~学习流／知识图 全家~~ | ⚰️ **2026-09-25 整族删除（批次 K，老板判决：与主链路联动性太低）**——原 28 件源文件（`shared/study-flow*`／`server/learning/{study-flow,study-flow-run,knowledge-graph,flow-registry,flow-executors,follow-up-links}`／`routes/study-flow`／`web/features/study-flow/`／`lib/api-study-flow`／探针 `flow-canvas-zoom-cdp.mjs`）＋ 12 件测试 ＋ `/api/study-flow` 20 端点 ＋ **迁移 v44 DROP 那七张表**（不备份、不归档）。沿革与三方取材判断留在 `docs/STUDY-FLOW-SPEC.md` 墓碑；逐文件清单见 `CHANGELOG.md` 2026-09-25 本批行。★ **本表刻意不留指向不存在文件的行**（那是下一读码人最贵的时间陷阱），但**功能曾经完整交付过**这件事留在这一行里。⚠️ **一处未收口的尾巴**：落地页 hero 的第二个场景仍在演示「追问→自动连边」，那个动作已经不存在了 ⇒ 演示货不对板，等老板定替换方案（`app/demo/graph-*` 是临时住户）。 |
| `packages/web/src/app/Landing.tsx` ＋ **`LandingIntro.tsx`（产品整体介绍段：一句话定义 ＋ 「一切都以词条为主体」五个去向 ＋ 三句话说清 ＋ 信任标签条）／`LandingFeatures.tsx`（功能区：五步动线 ＋ 两屏演示 ＋ 工程）／`landing-data.ts`（文案常量，`WALK` 是唯一的动线表）／`landing-icons.tsx`（key → line-icon 映射，两个组件共用一张表）——四件均为 2026-09-22 v0.2.107 拆出**（＋`TermJourney.tsx` 段 ＋ `app/demo/` **六件套**：`registry.ts`／`useDemoPlayer.ts`／`TermFlowDemo.tsx`／`LandingDemo.tsx`＋**`graph-demo.ts`／`GraphDemo.tsx`（2026-09-20 v0.2.86 新增，第二个演示「知识图 · 追问长出关系」五帧。⚠️ **2026-09-25 批次 K：这个演示的取材对象（知识图 + 「追问自动连边」）已整族下线** ⇒ hero 现在演的是**一个产品里已经不存在的动作**，属于货不对板。`app/demo/` 下的 `graph-demo.ts`／`GraphDemo.tsx`／`graph-visual.ts`／`graph-types.ts`／`graph.css` 因此是**临时住户**（各自带测试），等老板定 hero 替换方案后一并处理）**） | **未登录落地页门面**（2026-09-19 上线批；2026-09-20 hero 重构批把词条提到主线位并加过程式演示动画）。★ **hero 是双栏**（左文案 ＋ 右侧实时演示，≤880px 堆叠），登录卡在 hero **下方**——双栏后卡挤在左栏会把演示窗顶出首屏。★★ **演示动画的装配点**：`demo/registry.ts` 把「一个演示」拆成 **帧序列 stages ＋ 帧渲染器 View**，`LANDING_DEMOS` 是唯一注册表——**新增一个演示 ＝ 新写一个 View ＋ 追加一项，外壳/播放器/样式零改动**（★★ **v0.2.86「知识图演示」落地时这句话被真验证：本目录以外一行未改、Tab 切换器自动出现**——该分支在 v0.2.82 落地时因只有一个演示而从未被执行过）；`useDemoPlayer.ts` 是**唯一的计时器持有者**，命中 `prefers-reduced-motion` 时**停在最后一帧**（最后一帧＝完整界面，第一帧＝空舞台），且 `matchMedia` 在 effect 里问而非渲染期问（jsdom 未实现它，渲染期调用会让组件测试直接抛）；`TermFlowDemo.tsx` 五帧（打字机 → 词条高亮 → 悬浮速览卡 → 入库 chip → 复习翻牌），★ 打字机是**真逐字**而非 CSS 宽度裁剪（中文 `em` 宽度随字体浮动，`steps()` ＋ 固定宽度会在换行/异体字下错位），`highlightShown` 抽成可测纯函数并以**跨段共享的 `seen`** 保证「首现/复现」判定确定性；`LandingDemo.tsx` 的**多演示切换 Tab 只在 `LANDING_DEMOS.length > 1` 时渲染**（只有一个演示时刻意不画＝一个点无可点的装饰控件，同「未配 GitHub 凭据就不画登录按钮」的纪律；★ **自 v0.2.86 起 `LANDING_DEMOS` 有 2 项，该分支第一次真跑**）。★ **演示全部前端写死**（老板 2026-09-20 拍板：落地页的职责是讲清楚、不是当 demo 环境）；★ **动画一律 CSS**（`@media (prefers-reduced-motion)` 全套关掉）——「刻意不引库」在此同样生效，不引 framer-motion／Lottie／GSAP。★ **`TermJourney.tsx` 是核心机制段**：五步（抽词/高亮/注入/复习/沉淀）全部有代码对应，其中理解链（L0→L4）如实标**「部分落地」**——落地页宁可少讲一条，不给一句会被真机打脸的话；它与五环闭环的**先后顺序不可换**（词条是**机制**、五环是**结果**）。★ 门面动线（默认不出登录卡／两条 CTA 的初始模式／演示窗从第 0 帧起／高亮两档线型）＋ ★★ **区块顺序**（2026-09-22 v0.2.107／108：`.landing-intro` 必须在 `.landing-zone` 之前且中间零兄弟；「它是什么」段**不许出现「词条」二字**；动线编号 01–05 ＋ 九入口 ＋ 动线在演示之前）＋ 共享体验账号入口（v0.2.106）由 `app/Landing.test.tsx` **15 例**锁住（6→10→12→15，三批累计）。★★ **顺序类的锁必须锁 DOM 顺序，不能锁「文案在不在」**：把介绍段挪到功能区之后时，「文案在不在」式的用例**照样全绿**（文本一个没少），只有顺序锁会红——已按改坏取证核实。⚠️ **已知未接设计系统**：`landing.css` 有 20+ 处引用 `--sb-border`／`--sb-text`／`--sb-text-secondary`，而这三个变量在 `tokens.css` 里**并不存在**（实际名是 `--sb-line`／`--sb-ink`／`--sb-ink-secondary`）⇒ 一直靠 fallback 字面量生效；**待单独一批归位**，勿与功能改动混提 |
| `packages/shared/src/ebbinghaus.ts` + `packages/server/src/learning/term-review.ts` + `routes/terms.ts`（复习三端点）+ `packages/web/src/features/terms/ReviewPanel.tsx` | **艾宾浩斯复习（v23，2026-09-17 新建，契约 `docs/EBBINGHAUS-SPEC.md` v1.0）**：给词条库装复习时钟（此前只进不出，入库越勤欠账越多）。★ **判定唯一实现在 shared**——前端显示「N 天没复习」与服务端排今日队列必须同答，各算一份就会出现「页面说该复习、队列里没有它」（`doc-rag.ts` 常量双写同款病）。**七个经典节点 1/2/4/7/15/30/60 天**（原始曲线前三个复查点在当天内，按「天」粒度合并成第 1 天；`stage=7` 毕业＝长期记忆，仍记天数但不进队列）。★ **三条口径**：① **天数按本地日历日**（不按满 24 小时）；② **存储按 UTC 读、日历按本地判**（`datetime('now')` 无时区标记，混用＝差一天＝「今天该不该背」当场露馅）；③ **到期＝保持率掉到 70%**（`R=0.7^(t/间隔)`，间隔与曲线同一件事的两面）。★ **忘了归零**（不做退一级的折中：本功能全部价值在**可预期**）。★ **库里只存 `review_stage` + `last_reviewed_at`**：`next_review_at` 是派生值，落库则改间隔序列就得洗全表（`summary_upto_rowid`/`expires_at` 已两次付过学费）；`last_reviewed_at` 可空＝「从未复习」（起算点退到 `created_at`，否则新词条永不进队列）。★ **队列＝先还旧账**（`overdueDays` 降序）——按到期时间排会让人永远在刷新账、老账越滚越多。**端点**：`GET /api/terms/review/overview`（欠账统计 + 近 7 天，缺天补 0）／`GET /api/terms/review/queue?limit=&domain=`（归一 1..100）／`POST /api/terms/:id/review`（`{remembered: boolean}`，**必须布尔**：没有合理默认，猜成记住会白丢一次复习）。**前端**：`ReviewPanel` 先翻牌再看释义（默认盖住＝先回忆再对照）、不做乐观切态；列表徽标**只有逾期上色**。⚠️ **加迁移 v23 后**：三个「退版本重放迁移链」用例已同步 DROP 两列（先删 `idx_term_library_last_reviewed` 再删列）与 `term_review_log`。**已接续**：AI 督促悬浮小窗（v0.2.48 落码，见下一行）。★★ **v0.2.53 布局回归修复（`bug-ledger` B-008）——老板报「词条库本体被覆盖了、增删改查/查询/滑动列表都没了」，查证结论：一行功能代码都没删，是布局把本体挤出了可视区**。三证交叉定「丢没丢」：① `TermsPage.tsx` 提交史逐笔 `--stat`——唯一带删除的 `ce96927` 只是把内联 `.term-tabs` 抽成 `DomainBar` 组件，**CRUD/搜索/列表一行未动**；② 历史真删过的只有旧 `features/memorize/MemorizePage.tsx`（09-01 忆域 v2 重构），**而把那个文件读出来看，它本来就只有「添加 + 翻卡复习」**、从来没有完整增删改查 ⇒ 老板说的「原功能」也不是它（**只查「文件存在过」会拿错锚点去「恢复」**）；③ 后端真值 `GET /api/terms/domains` total **278**、`/api/terms/review/overview` due **271**/overdue **217** 全正常。★ **根因**：词条页是**固定高度 flex 列**（`.term-page{flex:1;min-height:0;overflow:hidden}`，`.term-list` 是**唯一**弹性子项、`.term-head`/`.term-stats`/`.term-toolbar`/`.term-add` 全 `flex-shrink:0`），而 `TermsPage.tsx:151-152` 把艾宾浩斯面板**刻意放在列表之前**（注释写「先于列表，因为它回答的是现在该干什么」）⇒ `.rv-panel` **既无 `max-height` 也无 `flex-shrink`**、高度纯由内容撑（272 条 stage0 转「待复习」⇒ 队列顶满 20 条），flex 项默认 `min-height:auto` **压不下去** ⇒ 面板一口吃光容器、唯一的弹性子项被压成 **4px**、工具栏/添加框被推出视口，父级 `overflow:hidden` **直接裁掉且不给滚动条** ⇒ 用户**滚不到**（所以看着像被删）。**真机像素对照**：同一份手搭 DOM 喂无头 Chrome 跑「有面板/无面板」两遍——有面板时 `.term-page` `clientH 800 / scrollH 2494`（`overflowClipped`）、`.rv-panel` **2073px**、`.term-list` **4px 且在视口外**、搜索/添加/编辑/删除**全不可见**；摘掉面板后 `scrollH 800` 不裁、`.term-list` **407px 可见**、控件全可见；**两档视口（800/1000）面板都是 2073px**（内容撑的）⇒ **任何屏幕都坏**。★ **修法四条**：① `.rv-panel` 高度自收敛（`display:flex;flex-direction:column;flex-shrink:1;min-height:0;overflow:hidden`）；② `.rv-queue` 限高内滚（`max-height:240px;overflow-y:auto`）；③ **队列默认收起**——收起态保留一行欠账摘要（「逾期 N · 待复习 M · 今日已复习 K」，**与展开态同源 `overview`**、不另算一套口径，**信息不丢**），展开/收起是**本页布局的必需开关**而非可选装饰；④ `.term-list` 给 **160px 保底**（防被压成 0，非布局值）。★ **仪器** `tools/probes/terms-layout-cdp.mjs`（三态 × 两视口 **56 断言**）。★★ **v0.2.56 复习范围选择式批（契约升 v1.1，迁移 v28）——复习从"全库自动进池"改为"只有用户勾选的领域/词条才复习"**：`有效范围 = COALESCE(term_library.review_enabled, term_domain.review_enabled, 0)`。★ **为什么必须两层**：读真机库实测（278 条 / 12 领域），`general` 域 66 条里**正经术语与娱乐词（谐音梗/二创/邪典片/审丑狂欢）混装**、`cs` 域 67 条里混着 `low-poly` ⇒ 纯领域级一键会把娱乐内容一起收进来，领域开关只是"粗筛"。★ **`NULL` ＝ 跟随领域**（`term_library.review_enabled` 可空）：① **写入侧零改动**——新词条天生 `NULL` 自动跟随领域（＝老板要的「领域开关持久，新词条自动纳入」）；② **覆盖位不冗余**——目标值 === 领域值就写回 `NULL`，只有不一致才落显式 0/1（否则领域一改，满库显式值就变成"用户当时的意思"而用户从没表达过）；③ 领域级一键开关＝**清掉该域全部覆盖位**。★ **领域开关的事务顺序刻意**：**先按旧开关算清零 → 再清覆盖位 → 最后改开关**（反过来会把本来该复习的词条一起清零）。★ **清零重来只在「由关变开」方向触发**，`term_review_log` 流水**不抹**（历史事实不篡改）。★ **判定只有一份实现**：`term-review.ts` 导出 `SCOPE_FLAG`/`IN_SCOPE`/`SCOPE_JOIN`，`terms.ts`（列表 `review_in_scope`）、`domains.ts`（`reviewCount`）、`term-review.ts`（队列+概览+流水统计）三处共用。★ **新端点** `PUT /api/terms/review/scope`（`domain` 与 `termId` **二选一**）；`POST /:id/review` 与 `POST /api/coach/review` 未纳入范围一律 **409**（404＝词条没了、409＝不在范围里）。★ **前端**：新 `ReviewScopePicker.tsx`（三态勾选框：全选✓/部分横杠/未选空；点一下"没全选→补全、已全选→清空"）+ `ReviewPanel` 加「复习范围」开关（与队列展开态**两个独立 state**）+ `TermsPage` 每行「纳入复习/移出复习」按钮 + **`ReviewBadge` 条件渲染**（未纳入不显示徽标）。★ **迁移 v28 是 `ADD COLUMN` 型不幂等** ⇒ 7 处回放点必须补退列，且**先 `DROP INDEX` 再 `DROP COLUMN`**（SQLite 不留悬空索引） |
| `packages/shared/src/coach.ts` + `packages/server/src/learning/coach.ts` + `packages/server/src/learning/coach-prompt.ts` + `packages/server/src/routes/coach.ts` + `packages/web/src/features/coach/*`（7 个）+ `packages/web/src/lib/api-coach.ts` | **复习督促小窗——B+C+E 三合一（v0.2.48，2026-09-18 新建，契约 `docs/COACH-SPEC.md` v1.0，迁移 v25）**：**B** 任务胶囊（收合态，右下角报数小药丸）+ **C** 侧边抽屉（展开态，贴右缘）+ **E** 卡片流（抽屉里不是气泡而是一列卡）。★★ **一个组件两种状态，不是两个组件**：胶囊与抽屉共享同一份数据与**同一条 SSE 连接**；拆两个必然各拉一遍 `/state`、各持一条流，重连时两条流对账出**两套 seq**。★ **复用 `lib/sse-client` 而不新造流**（eventSource + seq 去重 + 断线重连 + `/live` 对账，本仓第四次复用）；频道 `coachChannel(ownerId)` ＝ `coach:<owner>`——**与会话 `sessionId` 直传、PK 的 `pk:` 前缀三向隔离**：不隔离则督促帧混进聊天流、或反过来被聊天的 seq 去重吃掉（**跨频道 seq 不可比**，混用会大面积误丢帧）。★ **队列卡与流卡刻意分家**：队列卡＝「等一下要做的题」（翻转揭晓 → 记住了/忘了 → `POST /review`），流卡＝「已经发生的事」（`CoachCard` 四类 `ai`/`me`/`review`/`nudge`）——**待办与历史混成一列，用户就分不清「该我动了没有」**，而这正是小窗唯一要解决的问题。★★ **卡片按 id 合并而非追加（`mergeCards`）**：同一条卡会从**乐观插入 → 落库回读 → 断线重连重放**三处各到一次，只靠追加**必然出现三张一样的卡**；按 id 覆盖是唯一能同时覆盖三条路径的写法（`coach-cards.test.ts` 三条路径各钉一例）。★ **督促冷却在服务端**（`NUDGE_COOLDOWN_MS=2h`，`POST /nudge` 服务端判）——放前端一刷新就归零、用户**每次进页面都被催一遍**（督促变骚扰，功能反向）；`shouldNudge()` 四优先次**有序不可换**：空库不催（没东西可复习）→ 冷却不催 → 逾期 ≥`NUDGE_MIN_OVERDUE_DAYS` 催 → 积压 ≥`NUDGE_MIN_BACKLOG` 催。★ **模型角色新增 `'coach'`，`resolveCoachTarget()` 缺省回落 `'explain'` 且刻意不报错**：督促是陪伴层，没绑专属模型时复用讲解模型即可；若照抄「未绑定即抛错」，小窗会在任何未配置 `coach` 的机器上**直接不可用**。★ **`owner_id` 取 `NULL` 非 `DEFAULT ''`**（沿用 v22/v24 口径：NULL＝本地单人模式，`WHERE owner_id = ?` 天然不匹配）；**独立表不复用 `messages`**——督促不属任何会话，塞进去就得给一张「全世界都假设必有 `session_id`」的表开洞。★ **胶囊与抽屉同源取数**（都喂 `coachSnapshot()`）——各算一套会出现「胶囊说 5 条、抽屉里 3 张卡」的自相矛盾（同 v0.2.43 `createdAt` 两事实源的同类病）。★ **`POST /review` 的 `remembered` 必须布尔**（字符串 `"false"` 是真值，不校验就会把「答错」写成「答对」）。⚠️ **验收状态（2026-09-18 v0.2.50 批更新，原四条挂账逐条给结论）**：① **`.tsx` 渲染层已补仪器**——`tools/probes/coach-cdp.mjs` 默认档 **38 断言全绿**（胶囊文案与色调／打开抽屉／**队列区在流区之上**／队列卡四字段／翻转揭晓／快捷动作／输入框禁用与解禁／复开抽屉不重复）；**「胶囊位置与抽屉宽度顺不顺眼」仍需目检**——探针只核「有没有、对不对」，**不替人做审美验收**。② **真实模型端到端已跑通**（真模型 agnes，产出忠于快照：「先啃这 5 条：Vector Database、C语言、结构体 struct、内存泄漏、函数工厂。它们都 17 天没碰，记忆只剩 5% 左右，逾期 16 天…」）。③ **18791 无需重启，原判断本身有误**——复核生产库：`schema_version` 已 **26**、`coach_messages` 与 `term_review_log` 两表在位、`coach_messages` **7 行**、`GET /api/coach/state` 回 **200** ⇒ v25 早已生效（连并行批的 v26 都已上线）。④ **督促频率阈值仍未验**（如实留级——`NUDGE_MIN_OVERDUE_DAYS`／`NUDGE_MIN_BACKLOG` 是拍的数、无真实语料支撑，见 `test-plan.md` §6）。★ **本批另修出一个跨域真 bug（`bug-ledger` B-007）**：见上方 `sse-seq.ts` 行——**两端 seq 去重口径不一致**（服务端每轮重置为 1、客户端 `since` 跨轮不重置）⇒ **第二轮回复整轮不上屏 + `busy` 永久卡死**，**不是督促小窗的 bug 而是所有 SSE 消费者共用的那条路的 bug** |
| `packages/server/src/index.ts` | Express 入口（安全头/CORS/originCheck/body 大小限制）。**路由挂载点唯一登记处**：`/api/chat`、`/api/quiz`、`/api/terms`、`/api/doc`、`/api/settings`、`/api/pk`、`/api/choices`、**`/api/memory`**（2026-09-15 长期记忆逃生口，见 `routes/memory.ts`）、`/api/scenario`（情景题）、**`/api/coach`**（v0.2.48 复习督促小窗，见 `routes/coach.ts`）。★ 新增路由=本文件加一行 `app.use`；**域逻辑不进这里**。（★ 原挂载表里还有一项 `/api/study-flow`，2026-09-25 随学习流整族下线删除，批次 K。）★ **body 上限按路径分流**（v17 看图，2026-09-16）：默认仍是 2mb（防大 payload DoS，继承 v1），**只有 `/api/chat/send` 抬到 24mb**——图片走 base64 内联，一张截图就 2~5MB，**沿用 2mb 会让请求在到达业务校验前就被 413 打回**（实测：前端表现为「点了发送没反应」，极易误判成前端 bug）。★ **2026-09-20 起三个数收进 `shared/src/chat-limits.ts` 单一事实源**（本行改读 `CHAT_BODY_LIMIT`）：原先「4 张」「单张 7M」「24mb」分散三处、**各自都对、乘起来超了**（7M×4 ≈ 28MB > 24mb）⇒ 贴满 4 张图点发送被 express 在**路由之前**拒掉，症状是「点了发送没反应」**且服务端零日志**；现由 `worstCaseChatBodyBytes() ≤ CHAT_BODY_LIMIT_BYTES` 这条不等式（含反向证明）锁住，**改上限只改 chat-limits.ts 一处** |
| `packages/server/src/chat/flow.ts` | 一轮对话编排（`persistRounds`/`loadHistory` 2026-09-14 已搬去 `chat/persist.ts`；**附加 system 段的构造/落位/预算 2026-09-15 已搬去 `chat/context-segments.ts`**）。本文件现在只留三步：**调一次 `collectContextSegments` → 截断 → 调一次 `assembleContextMessages`**（顺序不可换：截断要用段清单算出的预算，装配要用截断后的历史）。★ **长期记忆（2026-09-15，契约 MEMORY-SPEC）**：`loadHistory` 读全量后由 `collectContextSegments` 内部丢掉已被摘要覆盖的部分（`dropSummarizedHistory`——摘要段已提供其内容，重复发等于同一段占两份预算）；流式收尾后 `void compactIfNeeded(sessionId)` **异步压缩、不 await**（下一轮生效，本轮用户已拿到回答），且排在词条抽取**之后**串行（两者都要打一次 LLM，并发会同时占两个配额槽）。★ **工具循环预算**：窗口 − `systemPromptTokens`（含全部附加段）− 已载历史 − 预留，每轮回灌后核对；轮数上限由 8 提到 15 后，**这条预算闸是唯一的窗口守门人**。★ **本文件 2026-09-18 由 406 行降到 383 行**：多租户 M2b 批给它加 `ChatOptions.ownerId` 时把**出入参契约**整段切到新 `chat/options.ts`（门禁 `.ts ≤400` 红线；按仓规**切文件、不压注释换行数**）——**归属能传到记忆层全靠这里那个可选参数**（见 `chat/options.ts` 行）。此前沿革：2026-09-15 收口重构 −41 行；2026-09-16 加 v17 看图 + v18 grill-me 后又贴线；2026-09-17 v0.2.44 把 grill/联网的开场装配与摘除搬去 `chat/opening.ts`，净减 1 行。★ 加逻辑前先看 `chat/context-segments.ts`／`compact.ts`／`memory.ts`／`persist.ts`／`vision.ts`／`grill.ts` 有没有现成落点。★ **v18 grill-me 接线（三处，都在本文件）**：① 开场把 `GRILL_PRE` 作为一条**内存 user 消息** push（不落库——它是指令不是用户发言），并在 `turn===0` 传 `toolChoice: GRILL_TOOL_CHOICE`（**只绑 turn 0**：turn 1 起必须放开，否则模型被锁死在提问上、正文永远出不来）；② `execTool` 给强绑产生的 `ask_choice` 打 `grillPhase:'pre'`（不打的话前端认不出它是 grill 卡，会掉回输入框上方浮层）；③ 落库之后、`chat_done` 之前调 `runGrillClosing`（不 await 用户点选）。★ **v0.2.44 起开场装配改走 `chat/opening.ts`**：`GRILL_PRE` / `SEARCH_FORCE` 两段指令与首轮强绑都由 `buildOpening({ grill, online })` 产出，本文件的 `toolChoice` 那行变成 `turn === 0 ? opening.toolChoice : undefined`——**grill 与联网同开时强绑名额归 grill**（`GRILL_PRE` 是硬性表述、`SEARCH_FORCE` 是软性的，冲突取前者）。★ **摘除位置也搬了**：开场指令现在**首轮工具轮之后**就摘（与 nudge 段同处），**不再只等出循环**——留到次轮的代价不是多几句话，而是联网指令会让模型**每一轮都重搜直到撞轮次上限**（初版照搬 grill 的「出循环即摘」，被 `flow.test.ts` 当场逮到），循环外那次降级为幂等兜底（覆盖「首轮无工具调用直接 break」） ★★ **M2d-2（v31）**：主对话路径的 `runToolCalls` 此前**没传 `ownerId`** ⇒ 工具里的词条增删改查全落**无主行**（主人登录后看不到、且全程不报错）；`ToolCtx.ownerId` 改必填后由 `tsc` 逼出来并补上。★ **v0.2.84**：收尾段（抽词 → 落库 → 连知识图边 → 压缩）顶到 **394** 行（★ 该四环是 **v0.2.84 时点**的形态；**2026-09-25 批次 K 摘掉了「连知识图边」这一环**，现在 `afterTurn()` 是三环） ⇒ 整体拆到新 `chat/post-turn.ts` 的 `afterTurn()`（本文件回落 **386** 行）。★ **拆的理由不只是行数**：`flow.ts` 装的是「这一轮**怎么跑**」，`post-turn.ts` 装的是「这一轮**跑完之后**还要补哪些账」——**后者是 fire-and-forget 的**（发生在 HTTP 响应之后），两者是**不同生命周期**，混在一个文件里时「谁先谁后」只靠读代码猜。★ 语义上**顺序不可换**：**先 `linkFollowUpEdges` 再 `compactIfNeeded`**——压缩会把消息折叠成摘要，而连边要读本会话**新落的词条行**（`term_library.source_session_id`）。 |
| `packages/server/src/chat/context-segments.ts` | **附加 system 段的唯一装配处**（2026-09-15 新建；2026-09-17 加日期段后 **169 行**）：`collectContextSegments`（造齐七段 + 算好窗口占用 + 给出已过滤历史）／`assembleContextMessages`（按清单顺序装配，返回 `nudgeMsg` **引用**供首轮后精确摘除）／`countSegmentTokens`。★★ **存在理由：同一份清单此前写了两遍**——一遍在 `systemPromptTokens` 的预算核算（7 项 `estimateTokens` 相加）、一遍在 `messages` 的组装（6 处 `push`/`splice` 加非空条件），于是加一段要同步改**三处**（预算项／组装语句／非空条件），漏一处**不报错、只静默漂**：预算漏算 ⇒ 窗口明明不够却按满额载历史（v1 老坑）；组装漏写 ⇒ 段算进了预算却没上屏（白扣窗口）。与 `doc-rag.ts` 那次「前端手抄 60k 阈值」同源，故收成一份清单。★★ **数组顺序就是注入顺序**：`assembleContextMessages` **不做任何排序**，要调顺序就调 `collectContextSegments` 里的字面量顺序——那才是唯一真相源。**位置为什么不能随手改**：`openai` 适配器把多段 system **全量透传**（`anthropic` 侧合并成一段故无差异），位置对模型有语义；回归锁见 `flow.test.ts` 的「长期记忆注入」describe（七段全满的逐段按位顺序 + 画像段不插队）。★ **头部段自 2026-09-17 起有两类**：`date`（**前提**——「今天是几号」与基础提示词的「你是谁」同属一次对话的起点，故排在最前）与 `summary`（**历史的开头**，它替换掉的是**历史本身**）；其余段在历史之后（**辅助材料**：词条/资料/偏好/画像/触发增强）。★ **日期段恒非空、且一天之内恒定**（刻意不含时分秒，否则 system 前缀每轮都变、prompt 缓存永不命中——见 `date-context.ts`）★ **头部段用递增下标插入**（不是恒插 1）——将来多一个头部段时，恒插 1 会让它们**倒序**（已有单测锁）。★ **空内容段统一剔除**：预算与组装看到的是**同一份**清单，不可能一边算一边不算。★ **偏好段恒非空**（至少含 scope 那句），故无需条件判断。★ **v0.2.42（B-006）**：`nudge` 段现由**两条触发增强**共用且 **search 优先**——`searchNudge(text) ?? choiceNudge(text) ?? ''`（学习者明说「搜一下」是**动作指令**不是岔路，而实测现场模型偏偏弹了 `ask_choice` 让他先拍板）。两条共用同一段 ⇒ **`flow.ts` 的注入与摘除逻辑零改动**（它只认一个 `nudgeMsg` 引用）** |
| `packages/server/src/chat/date-context.ts` | **日期段——让模型知道「今天几号」（2026-09-17 新建，50 行）**：`buildDateBlock(now?)` 纯函数。★ **存在理由：这是事实缺失，不是能力缺失**——模型没有时钟，提示词里也从没有过日期，用户问「今天几号」「距考试还有几天」时它手里没有任何起点，只能瞎猜或答「我的知识截止到……」。修法是**每轮喂事实**（同词条/资料/画像一个手法），而不是在提示词里教它算日期（那是在错的层修）。★★ **三条改了就出事的约束**：① **只到「日」、不含时分秒**——段内容必须**一天之内恒定**：本仓 system 前缀是 prompt 缓存（Anthropic/OpenAI 的 prefix cache）的匹配前缀，带时间戳会让缓存**永不命中**（代价＝模型不知道「现在几点」，本需求用不到）；② **本地时区，绝不用 `toISOString()`**——它按 UTC 出日期，东八区的 09-17 00:30 会算成 **09-16**，而用户一大早问「今天几号」正是这个时刻；故一律走 `getFullYear`/`getMonth`/`getDate`；单机应用（127.0.0.1 + 本地 SQLite）里服务端时区 === 用户时区；③ **`now` 参数是给测试的**——不注入就测不到凌晨/跨月/跨年/闰日这些唯一会错的边界（生产调用不传）。★ **措辞三句各有分工**（删任一句掉一个能力）：事实（几号、星期几）／用法（时间判断以它为准、相对时间按它推算）／边界（训练数据早于今天，不确定就说不确定、不要编日期）——「编一个像真的日期」比「说不确定」危害更大。★ 落位与预算由 `context-segments.ts` 定（它是**头部段**，紧跟基础提示词）；接线回归锁在 `flow.test.ts` 的「日期段注入」describe |
| `packages/server/src/chat/vision.ts` | **「纯文本主模型读图」的视觉蒸馏层**（v17，2026-09-16 新建，2026-09-20 收口批后 100 行）：`describeImages(images, signal?)`（图 → `vision` 角色模型 → 一段中文描述）＋ `parseIncomingImages(raw)`（HTTP 边界校验：最多 4 张、单张 ≤3.8MB、只认 `data:image/` 前缀；★ **三个数全部从 `@sb/shared` 的 `chat-limits.ts` import，本文件不再持有任何上限字面量**）。★★ **架构定位：视觉是**可插拔的外部能力**，不是把主模型换成多模态**——主模型始终是纯文本，它拿到的只是一段「画面的文字版」（拼进该轮 user 消息的 `content` 一起持久化）。三个好处：主模型选型不被看图绑架／视觉模型挂了或换了对主 Agent 其它能力零影响／**历史回放与重新生成都不再二次调视觉模型**（描述已在库里）。★ **未配视觉模型 → 抛可直接照做的报错**（「请到设置页为『视觉（看图）』绑定 qwen-vl-plus / gpt-4o / glm-4v」），**不是笼统的「模型不可用」**（ADR-5：`routeRole('vision')` 在没绑定时返回 `model:''`，空 model 会让视觉调用直接 400，必须与「没配」同一处置）。★ `parseIncomingImages` **先过滤再限流**：脏值不该占额度，否则夹带垃圾就能把合法图片挤掉。★ **上限已收进单一事实源（2026-09-20 看图链路收口批）**：单张 `MAX_IMAGE_DATAURL_CHARS`（5M 字符 ≈ 3.8MB）与 `index.ts` 的 `CHAT_BODY_LIMIT`（24mb）**同出 `shared/src/chat-limits.ts`**——原先三处各自写字面量（4 张／7M／24mb）**乘起来超了**（7M×4 ≈ 28MB > 24mb）⇒ 贴满 4 张点发送被 express 拒在**路由之前**，前端看到的是「点了没反应」；本文件只 import，**改值只改 chat-limits.ts**（`chat-limits.test.ts` 的不等式与反向锁会当场告诉你哪条被破坏）。⚠️ 提示词把**图示细节/文字/数值放最前**——视觉模型撞 `max_tokens` 时截断是从后往前吃，放最后＝每回先丢最有价值的信息（FrameWise 实测坑） |
| `packages/server/src/chat/tools/`（目录）| 单轨工具注册表（**2026-09-19 S1 拆目录，替代原 `chat/tools.ts`（158 行已删），契约 `docs/TOOL-ECOSYSTEM-SPEC.md` §4.1**，M5 S1/S2 已落地、S3 MCP 未开工）：`index.ts` 注册触发点＋对外门面（import 顺序＝下发顺序；`ask_choice` 在此补注册，单一注册入口）、`registry.ts` 单一事实源 Map＋`registerTool`＋§4.2 元数据（`kind`/`timeoutMs`/`idempotent`/`scenes`/`needsConfirm`——kind 是分档超时/重试资格/确认门缺省三消费方共同事实源，`needsConfirm` P2 只落类型、P3 接确认门）、`schema.ts` 零依赖 JSON Schema 子集校验器（runTool 执行前**参数预闸**）、`budget.ts` `toolDefinitionTokens`（§4.4 第 1 条：tools JSON 计入输入预算）；工具各自注册：`web-search.ts`/`fetch-page.ts`（2026-09-20 新增，契约 §5.3 网络读工具）/`fetch-image.ts`（2026-09-20 新增，契约 `docs/IMAGE-FETCH-SPEC.md`：图片搬运工具——取图→字节魔数判型→落盘缓存→回灌站内地址与「写进正文」用法；判型在字节上不在声明上，刻意不收 SVG）/`term-tidy.ts`/`term-ops.ts`（词条三工具）/`generate-quiz.ts`＋`generate-quiz-format.ts`（★ 2026-09-23 批次 G＝出题工具化新建，契约 §5.4：把 `routes/quiz.ts` 那条 REST 出题引擎接进模型手里，出卡走唯一门面 `learning/quiz-announce.ts`；纯文本层刻意分文件——import 注册文件会自己完成注册 ⇒「撤掉 `index.ts` 那行 import」这条 §7 改坏不会红，而「模型拿不到工具」正是本批最初那个线上症状本身）；另有 S2 两件——`confirm.ts`（plan→confirm→apply 确认门）与 `write-gate.ts`（`needsConfirm` 缺省由 kind 推的唯一判定处）。★ **2026-09-20 订正（§0.11 防漂移）**：本行原文记「S2 确认门未开工」并指向 `term-manage.ts`，**两者均已过期**——S2 确认门与词条三工具随 **P3 批（2026-09-19，契约 v1.4.1）** 落码，`manage_terms` 按拍板⑭ 退役、`term-manage.ts` 已删。**新增工具＝建文件 `registerTool`，flow 循环零改动**。历史口径随迁不丢：★ **v18** `ToolContext.grillPhase?`（`'pre'|'post'`，只由 grill-me 强绑路径注入）；★ **v0.2.42（B-006）** `search_web` 的 `description` 是**写给模型的提示词不是人看的文档**（「你具备这个能力」正面陈述＋触发场景＋示例），失败回灌不甩内部配置细节、不给放弃台阶（旧文案是模型自称「我无法联网」的直接来源）；★★ **M2d-2（v31）** `ToolCtx.ownerId` **必填**——词条 CRUD 本身就是归属操作，改判时实测逮到 flow.ts 漏传。v1.2 拍板不变：词条升 AI 全权 CRUD（`lookup_terms`/`upsert_term`/`delete_terms` 已随 P3 批 2026-09-19 落码）＋写操作两阶段 plan→confirm→apply（>3 条弹卡、删除一律弹、`term_delete_log` 按批可撤销）。★ `update_tasks` 刻意**不注册**（有状态工具，走 flow exec 注入，见 `chat/task-list.ts`）；`ask_choice` 定义与执行在 `chat/choice-tool.ts`（长挂起例外，超时豁免靠 `noTimeout` 名单不靠 kind） |
| `packages/server/src/search/index.ts` | 搜索聚合（Exa/Tavily/智谱按 key 并行 → 全无 key 走 Bing 免费通道（**`www.bing.com/search`**，RSS 主 + HTML 兜底，**2026-09-25 从 cn 换过来，见下条与 `bug-ledger` B-019**）；24h 缓存；SSRF 护栏。**★ 本文件 2026-09-25 起 244 行**：Bing 那一整块拆进 `search/bing-channel.ts`，`htmlToText`／`bingSearch`／`combineSignals`／`SearchResult` 仍由本文件原样 re-export ⇒ **调用方一行未改**，`search.test.ts` 有一条锁专门钉住这件事） |
| `packages/server/src/search/bing-channel.ts` | **Bing 免 key 通道本体**（2026-09-25 从 `search/index.ts` 拆出，167 行）：`bingSearch(query, signal?)` 走 `www.bing.com/search`，RSS 主 → HTML 兜底软降级。★★ **本文件的存在理由是一条线上真故障的形状**：**「通道没了」与「这个词真没结果」以前是同一个字符串**（旧码只判 `res.ok`，被 301 到首页后报成 `rss: 空结果`）⇒ 现在**形状不符即刻抛错**并带上 HTTP 状态＋字节数＋最终落点 `host+path`，读侧（`search_empty` 事件）从此可判。真网络复验＝`tools/probes/bing-channel-live.mjs`（**不进 CI**，老板 2026-09-25 拍板） |
| `packages/server/src/chat/system-prompt.ts` | 系统提示词（对话固定前缀）单开文件：它是最常被改的一段文案（每加一个工具就要加一条使用引导），留在 flow.ts 里改会顶破 400 行红线。⚠️ 计入 `systemPromptTokens`——**加长它等于缩小可载历史长度**。 ★ **v0.2.120（批次 I 图文结合批）**：老板点单「AI 回答要图文结合」——查证发现渲染层 ```svg 通道早已存在而提示词从没教过模型用（同 B-006「漏写引导＝能力对模型等于不存在」，第三个同族实例：前两个是 search_web 与 generate_quiz）；本批在 chart／html 引导旁补 ```svg 引导一条，正面强制措辞取自 `quiz-image.ts` v1.1 实测结论，独立成册回归锁 `chat/system-prompt.test.ts` 3 例（含反锁「提示词绝不含 mermaid」——渲染器无该通道，mermaid 在 `markdown.test.ts` 被显式锁成代码块）。★ **v0.2.42（B-006）**：本文件的**能力声明清单**是模型判断「我自己有什么能力」的主要依据——**漏写一个工具，那个能力对模型就等于不存在**。本轮补上「【你有联网搜索能力】」段（场景清单 + 「学习者说'搜一下/联网查'时**第一个动作就是调 `search_web`**」+ 硬约束「任何情况下都不要回答'我没有联网功能'」）：修复前 chart / html / update_tasks / ask_choice 全都有引导，**唯独 `search_web` 一字未提**——这正是「模型咬定自己不能联网」的头号根因。★ 回归锁：`chat/search-nudge.test.ts` 断言本常量必含 `search_web`**；★ **v0.2.115（批次 G＝出题工具化）同一条坑第二次踩（这次是预防，不是补票）**：加 `generate_quiz` 时**同批**补「【出题有专用工具 `generate_quiz`】」段——点名触发场景（「考我／来几道题／做个测试／巩固一下」）＋「要出题就调它，**在正文里打字出题等于没出**」，并把本批根因那句**悬空指令**（原写「给出题时遵循协议」却从没给过协议 ⇒ 模型手里没工具只能退回文字）就地删掉；回归锁在 `chat/tools/generate-quiz.test.ts`「★ 提示词这半边也到位」一例（断言 `SYSTEM_PROMPT` 必含 `generate_quiz`，并锁那句悬空指令不许回来）。★ **口径沉淀：新工具＝同批加提示词引导，否则那个能力对模型等于不存在**（B-006 的防线，本批起有锁而不只是有教训） |
| `packages/server/src/chat/task-list.ts` | 任务清单工具 `update_tasks`（标准 CoT 进度面板）。**不进注册表（`chat/tools/`）**：原避让理由（`tools.ts` 被并行会话在途改动，R1）已随 2026-09-19 S1 拆目录消失，现仍未并回——P2 按纯重构交付，并回需先收「当前清单状态归属」与注册表无状态签名的接缝，留后续批次；本工具走 flow 的 exec 注入接入。入参两模式**互斥**：`tasks`＝全量覆盖（仅首次列清单）/ `updates`＝按 **1 基序号**增量（长任务中途只发改动条目，缺 index 即追加）——旧版只有全量，10 条清单每完成一步都要重发 10 条。三段纯函数：`parseTaskArgs`（解析+形状校验）/ `applyTaskPatch`（**整批原子**：任一条越界则整批不生效，并把「当前清单+序号」回灌自纠——半应用会让模型以为的清单与用户看到的分叉）/ `formatTaskList`（回灌**必须带序号**，那是 patch 定位的锚点）。★ 三态 `TaskItem` 的唯一事实源在 `packages/shared/src/task-list.ts`（此前 server/web/SSE 契约三处各写一份） |
| `packages/server/src/chat/tool-dispatch.ts` | 单轮工具循环的「执行器装配」（2026-09-19 P1 从 flow.ts **原样搬出、零行为改动**——flow.ts 加计时线后 410/400 破线，按仓规**拆文件不压注释**，先例 `persist.ts` 同因搬出）。`createExecTool` 收敛两件事：`update_tasks` 的清单合并语义（`tasks` 全量 / `updates` 增量、**整批原子**、事件恒发完整清单）与其余工具转发 `runTool`（grill 开场给强绑产生的 `ask_choice` 打 `pre` 标记）。★ `update_tasks` 刻意**不进注册表（`chat/tools/`）**（有状态工具与注册表无状态签名不同接缝；原「R1 避让 tools.ts」理由已随 2026-09-19 S1 拆目录消失，接入方式因 P2 纯重构口径保持不变），definition 由调用方拼进 tools 列表、执行走 flow 的 exec 注入点。★ 清单状态走 `getTasks/setTasks` 闭包注入而非本文件持有：归属权仍在 flow.ts 的 `latestTasks`（收口落库读的是同一份），这里只是**唯一写口** |
| `packages/server/src/chat/choice.ts` | **方案选择框服务端核心**（2026-09-14 新建，契约 `docs/ASK-CHOICE-SPEC.md` v1.0）：把「AI 提一个问题」变成**一个可 `await` 的 Promise**。`askChoice`（校验→落库 `ask_choices`→**先 `waiters.set` 再 `publish`**→await，顺序颠倒＝丢一次唤醒）/`answerChoice`（条件更新 `WHERE status='pending'` 是并发双端点击的**唯一裁决**，changes=0 即回 409 且不覆盖首答）/`cancelChoice`/`cancelChoicesBySession`（逃生口①②）/`listPendingChoices`（前端刷新后把卡捞回来）/`sweepStaleChoices`（逃生口③）/`choiceToolHint`（回灌文案）。★★ **与 ai-orchestrator-v2 的关键差异：不需要内部端点与轮询**——那边工具跑在 opencode 内核、与编排层跨进程；本仓工具与用户答复在**同一 Node 进程**，一个 `waiters` Map 就够，少两条端点少一圈延迟。★★ **waiters 恒 resolve、绝不 reject**：作废也要让工具拿到一份权威记录去决定怎么回灌——用异常表示「作废」会走 `runTool` 的 catch 变成「工具执行失败」，模型收到报错而不是「用户没选，请直接作答」。★ **作废必须就地回填 `status`**（`asCancelled`）：`SELECT` 拿的是改库**之前**的内存快照，不回填就会造出一份 status 仍为 `pending` 的记录。★ **v18 加 `offerChoice`（只提问不等待）**：与 `askChoice` 共用同一套 `waiters` 与落库，唯一差别是**不 `await`**——收尾那一步本轮已结束，挂起等于把 busy 卡死（grill-me 专用，语义见 `chat/grill.ts`）。★ **`grillPhase` 只在内存与 SSE 下发里活**（`ask_choices` 表**刻意不加列**）：前端靠 SSE 那一帧决定「点完开新一轮」，刷新后从 `listPendingChoices` 捞回的卡降级为普通卡——这个降级可接受（刷新时那张卡本身也已不在原语境里），代价已在 `chat/grill.test.ts` 里钉成断言 |
| `packages/server/src/chat/choice-nudge.ts` | **方案选择框「触发增强」**（2026-09-14 夜新建，契约 §6）：功能全通但模型**不主动调** ⇒ 用保守词表识别「这条提问是不是在做选择/规划」，命中则在**首轮**注入硬指令「必须先调 ask_choice」，**首轮结束即由 `flow.ts` 摘掉**（留着会误导后续轮再问一次，问第二次比不问更糟）。红线＝**宁可漏不可误**：排除表拦掉求讲解／排错／`还是不懂` 类追问态；纯函数零 IO，改完跑 `src/chat/choice.test.ts` 即验。**只加工具不加触发层，模型就会永远不用它** |
| `packages/server/src/chat/search-nudge.ts` | **联网检索「触发增强」**（2026-09-17 新建，bug-ledger **B-006**）：与 `choice-nudge.ts` 是**同一个坑的第二例**——`search_web` 的工程链路全通（注册表 / 两个适配器都带 `tools` / 超时 / SSE 帧登记齐全）、真机上确实搜出过结果，但模型**经常一口咬定「我没有联网搜索工具」**、连一次调用都不发。根因是**声明与触发两处都缺**（`system-prompt.ts` 里一字未提 + `tool_choice='auto'` 下助手的出厂倾向是「直接把答案讲完」）。本文件补**第三层（输入侧词表）**：`searchNudge(text)` 纯函数，命中则**只注入首轮**的硬指令「你具备联网能力、第一个动作就调 `search_web`、不要说自己是'没有联网能力'」。★ **与 `choice-nudge` 的两条刻意差异**：① **search 优先于 choice**（`context-segments.ts` 里 `searchNudge ?? choiceNudge`）——明说「搜一下」是**动作指令**不是岔路；② **`MIN_LEN=2` 而非 4**——「搜搜」「搜下」本身就是完整诉求（B-006 现场原话之一就是「搜搜」），门槛设 4 会整片漏掉。★ **误搜守门人**：`EXCLUDE_SIGNALS` 拦学科术语（「二分搜索 / 搜索算法 / 广度优先搜索 / 优先查找」等）——学习场景里「搜索」高频作算法术语，不拦会把讲解题变成检索题。★ **红线与 choice-nudge 相反**：那条是「宁可漏不可误」（误弹框会打断讲解），这条**误搜代价低**（多一次检索、百毫秒级，模型看到结果用不上也无害），故词表取宽。★ 实测踩坑：初版信号收了 `'先搜'`/`'先查'`，被「优**先查**找」**假命中** ⇒ **短前缀式信号一律不收**，改由排除表兜 `优先搜索 / 优先查找`。★ 回归锁 `src/chat/search-nudge.test.ts` **30 例**（语料即 B-006 真机原话，非编造例句）** |
| `packages/server/src/chat/options.ts` | **单轮对话的出入参契约**（`ChatOptions` / `ChatResult`，2026-09-18 M2b 批从 `chat/flow.ts` 切出——加完多租户 `ownerId` 后 flow.ts 406 行触红线）。★ **切开的理由不只是行数**：`flow.ts` 装的是「这一轮**怎么跑**」的编排，本文件只是「这一轮的输入输出**长什么样**」，且**读者不同**——路由层（`routes/chat.ts`）只关心本文件。★ **`flow.ts` 仍转出这两个类型**（`export type { … } from './options.js'`）：契约搬家不该逼 6 个调用点跟着改 import。★★ **`ownerId` 为什么必须显式传**（TENANCY-SPEC §7）：本链路是 **fire-and-forget** 的，压缩与记忆写入发生在 **HTTP 响应之后**，那时已无 `req` 可取的上下文——不传下来，长期画像就只能写成**无主行**、或串到别人名下。★ 参数**可选默认 `null`** ＝「未登录的单人本地模式」（此时 `ownerFilter` 不加条件、行为与改造前逐字一致）：**让默认值落在安全那一侧**，漏传退回现状而不是串台 |
| `packages/server/src/chat/opening.ts` | **「本轮首轮强制动作」的装配层**（2026-09-17 v0.2.44 新建）：把 grill-me 与**联网开关**两种「开场强制动作」的装配与摘除收在一处——`buildOpening({ grill, online })` 纯函数 → `{ msg, toolChoice, grill }`（`msg` 是一条承载指令的 user 消息，两个开关都关时为 `null`；`grill` 供 `flow.ts` 给强绑产生的 `ask_choice` 打 `pre` 标记）、`dropOpening(messages, opening)` 幂等摘除。★★ **强绑只有一个名额，所以有优先级**：`tool_choice` 一轮只能指向一个工具，**grill 与联网同开时取 grill 优先**——`GRILL_PRE` 是「你的第一个动作**必须**是调用 ask_choice」的**硬性**表述，此时强绑 `search_web` 会让提示词与出站参数**互相打架**（模型在「先搜还是先问」之间摇摆）；而 `SEARCH_FORCE` 是**软性**表述、turn 1 起 `auto` 仍会命中搜索，代价只是「换个轮次搜」。这条规则由 `opening.test.ts` 锁死，**改错了不会报错、只会让模型行为变飘**。★ **两段指令合成同一条消息**（同位置、同用途；拆两条只多占一次消息开销，还逼摘除逻辑维护两个引用）。★ **摘除有两处调用点，缺一不可**：**首轮工具轮之后**（主路径，理由见 `chat/flow.ts` 行）＋ **循环之外**（兜底：首轮无工具调用时循环直接 break，走不到前者）。★ **为什么单开文件**：`flow.ts` 贴 400 行门禁，且「开场指令」是会随实测反复调的**提示词**，与调度逻辑分开改更稳（同 `chat/grill.ts` / `chat/choice-nudge.ts` 先例）。★ 回归锁 `src/chat/opening.test.ts` **10 例**（纯函数零 IO 零 DB）** |
| `packages/server/src/chat/choice-tool.ts` | `ask_choice` 工具定义与执行（2026-09-14 新建）：`CHOICE_TOOL`（参数 `question`/`options`/`allowCustom`）+ `runChoiceTool(args, ctx)`（取 `ctx.sessionId`→`askChoice`→回 `choiceToolHint`）。**长等待工具单独成文件**，注册集中在 `chat/tools/index.ts` 一处完成（2026-09-19 S1 起；原 `tools.ts` 已删）——单一注册入口，工具清单不留第二份事实源。★ 校验失败走 `{ok:false,error}` 正常回灌而非抛异常（模型可自纠）。★ **v18 加 `mode?: ChoiceToolMode`（`offerOnly`）**：grill-me 收尾那一轮走 `offerChoice` **只提问不等待**——本轮回答已经给完了，再 `await` 会把这一轮钉在 busy 上直到用户点一下，「可以跳过」就成了假承诺（不点＝卡住）。`ctx.grillPhase` 随提问一起下发，前端据此决定「点完是续本轮还是开新一轮」 |
| `packages/server/src/chat/grill.ts` | **grill-me 模式（v18，2026-09-16 新建，126 行）**：把「要不要弹选择框」从**模型的概率判断**改成**用户的显式开关**。★ 存在理由＝`ask_choice` 工程链路早就全通却真机见不到：触发权在模型手里，系统提示那句「遇到岔路先问」是原则性描述，模型在 `tool_choice='auto'` 下有「直接把答案讲完」的出厂倾向，而 `choice-nudge.ts` 的保守词表**只提高倾向、不保证**、且其排除表恰好覆盖学习场景 90% 的真实提问。★ **两段提问语义相反，别搞混**：`pre`（开场 `GRILL_PRE`）答复**要回灌**（模型据此决定讲什么）⇒ 必须挂起等待；`post`（收尾 `GRILL_POST`）本轮已答完、答复**不回灌**（前端把选中项作为**新一轮提问**发出）⇒ **绝不挂起**（`offerOnly`）。★ **强绑靠常量不靠提示词**：`GRILL_TOOL_CHOICE = {type:'function',name:'ask_choice'}` 由 `flow.ts` 交给适配器，Anthropic 侧要转成 `{type:'tool',name}`（同名不同形，原样透传直接 400），回归锁见 `llm/tool-choice.test.ts`。★ **收尾失败一律静默**：问不出「下一步」不该把已经上屏的回答标成出错。★ 指令文本与调度分家——提示词会随实测反复调（同先例 `choice-nudge.ts`）；`grillInstruction(phase)` 是纯函数出口 |
| `packages/server/src/chat/tool-exec.ts` | 工具执行器（含 `DEFAULT_TOOL_TIMEOUT_MS` 30s 超时 race）。**2026-09-14 加两个可选口子**：`ToolExecOptions.noTimeout?: string[]`（名单内工具**跳过超时 race**——`ask_choice` 要一直等用户点选，被 30s 掐掉就等于功能不存在）与 `sessionId?`（透传进 `ToolContext`，`ask_choice` 靠它知道该把卡发给哪个会话）；`callCtx` 同步透传。★ 两个字段都是**可选**的，既有调用点零改动 |
| `packages/server/src/chat/persist.ts` | 消息落库与历史装载（2026-09-14 **从 `flow.ts` 原样搬出**，零行为改动）：`persistRounds`/`loadHistory`。搬出理由＝`flow.ts` 加逃生口后触 400 行红线，而这两件事与「一轮编排」是两种生命周期。**★ 2026-09-15 长期记忆批**：`loadHistory` 改返回 `HistoryMessage[]`（= `ChatMessage` + `rowid`）——压缩需要一个**单调、可比较、删除后不复用**的锚点来记住「摘要覆盖到哪一条」；做成**子类型**而非给 `ChatMessage` 加字段（`llm/types.ts` 是**适配器契约**，混进 DB 概念不干净），既有调用方零改动 |
| `packages/server/src/chat/compact.ts` | **长期记忆第一层·会话内压缩**（2026-09-15 新建，契约 `docs/MEMORY-SPEC.md` §4）：机制照搬 Pi 的 Compaction——超窗口时把「要丢弃的那一段」先交给 LLM 摘要、存进 `sessions.summary`，下次组装用「摘要 + 最近消息」代替全量历史。`findCutIndex`（从最新往前累积到 `COMPACT_KEEP_TOKENS`，**结果过 `alignToolRoundBoundary`**——切点落在工具轮中间会造出孤儿 tool 消息被 API 400）/`buildCompactPrompt`（学习版六段模板 + `[MEMORY]` 块，已有摘要作迭代输入）/`parseCompactReply`（**分级降级**：`[SESSION_SUMMARY]` 缺失＝整体失败、`[MEMORY]` 缺失不算失败）/`compactIfNeeded`（对外唯一入口，**在途并发防护 Set**，调用方必须 `void` 掉、不 await）。★ **解析失败绝不写库**——脏摘要比没摘要更糟（会持续污染此后每一轮），没摘要只是回到现状、下轮重试。★ `buildMemoryContext` 是给 `flow.ts` 的打包口（摘要段 + 画像段 + 历史过滤三件**同源**的事一起发生）。★ 复用 `llm/router.ts` **既有的 `summarizer` 角色**，零迁移 |
| `packages/server/src/chat/memory.ts` | **长期记忆第二层·跨会话画像**（2026-09-15 新建，契约 `docs/MEMORY-SPEC.md` §5）：`upsertMemoryItems`（**同 kind + 同 content 只刷 importance 取 MAX、不新增行**，同 `term_library` 的 `UNIQUE(term,domain)` 手法）/`loadMemoryItems`/`pruneMemoryItems`（超 `MEMORY_MAX_ITEMS` 按 importance 升序**真删**——画像没有下游引用，软删只是留垃圾）/`buildMemoryBlock`（**纯函数**：按 kind 固定顺序分组、组内 importance 倒序、硬限长 `MEMORY_INJECT_MAX_CHARS`）/`markMemoryUsed`（只写 usage_count，**不参与排序**——它是调参观察窗不是淘汰依据）/`injectMemoryBlock`（读-选-建-记一步到位）。★ **只由压缩过程写入**（借道 `[MEMORY]` 协议块）：独立抽取任务需要「何时跑／跑多少／从哪段跑」三套策略，而压缩已是天然的「该沉淀了」信号点。★ **恒注入不检索**：词条是「与本次提问相关的知识」检索合理，画像是「你是谁」——「他偏好先看例子」对任何提问都成立，检索只会把它漏掉 |
| `packages/server/src/routes.ts` | 路由含 `settingsRouter`（搜索 key 读写 + 连通自检，密钥不出接口；出题配比/配图/回答方式三张卡的读写也收口在此）：★ **v0.2.44**：`/chat/send`、`/regenerate`、`/resend` **三条发起路径都收 `online`** 并转成 `handleMessage` 的 `online`（**重跑与发送同口径**——只接一条会变成「同一次提问两种行为」）；本文件 399/400 行，为腾行清掉两处等价冗余（同源 `sse-bus` import 合并、`getProviders().map((p) => p)` 恒等映射——`getProviders` 内部已显式挑字段、无 `api_key`）。`GET/PUT/DELETE /api/settings/answer-style` 一律回 `{ style, configured }`——**`configured` 是 L1 弹卡的开关量**（只回 style 表达不了「没配过」与「配成恰好等于默认」的区别），入参非法逐字段回落不 400，DELETE＝删键回到没配过。★ **v0.2.84**：新增 **`POST /api/sessions/:id/fork`**（「向 AI 追问」）——两道判据 `canAccessSession` ＋ **`sessionExists`**（★ **归属在归一之前**：不存在的会话 ＋ 超长 `term` ⇒ **404 而非 400**，**不泄露 id 存在性**；顺序写反**没有任何运行时报错**，只把「这个 id 存不存在」送给了调用方）⇒ `createFollowUpSession` 建 fork 会话 ⇒ `startFollowUpRun` **复用既有 `trackRun`**（中止器注册表与 `/chat/active` 因此天然可用，**不新造一套并发账**）。★ 响应体**不带 `prompt`**（首问正文走 `messages` 表 ⇒ **不造第二个真相源**，有专锁）。★★ **2026-09-21（一键默认设置批）：本文件的 providers 端点整块搬去 `routes/providers.ts`** —— 本批加「一键默认设置」与「免费额度查询」两端点后会越过 400 行红线，按仓规**拆文件不压注释**；本文件改 **re-export**（`export { providersRouter } from './routes/providers.js'`）⇒ `index.ts` 的 `app.use('/api/providers', providersRouter)` 与全部调用方**零改动**。★ 本文件 353→**172** 行 |
| `packages/server/src/routes/chat.ts` | **聊天域路由**（2026-09-17 从 `routes.ts` 切出——加完多租户归属断言后 `routes.ts` 涨到 487 行触 `.ts ≤400` 红线；按本仓规矩**按域再切、不压注释换行数**）。含 `/send` `/regenerate` `/resend` `/abort` `/active` `/stream`。★ **本文件最要紧的是 `aborters`**：它是**进程内存，不随 SQL 的 `WHERE user_id = ?` 一起被过滤**，故中止器自带 `ownerId`（`PendingRun`），`/active` 与 `/abort` 都按 owner 收敛——否则 `GET /active` 会把**别人正在生成的会话 id** 报出去（会话 id 属敏感标识）。★ 三条发起路径共用 `trackRun` 登记与摘除：三份 `finally` 各写各的，迟早有一份忘记摘除（表现为「生成完了侧栏还显示回复中」） |
| `packages/server/src/routes/choice.ts` | 方案选择框 HTTP 薄路由（2026-09-14 新建）：`GET /api/choices?sessionId=`（挂起卡捞回，刷新/重连用）/`POST /api/choices/:id/reply`。**域层不碰 HTTP**——404/409/400 的状态码由 `choice.ts` 的 `AnswerResult.status` 直通，本文件不翻译不包装。★ 挂点 `app.use('/api/choices', choiceRouter)` 在 `index.ts` |
| `packages/server/src/routes/memory.ts` | **长期记忆逃生口路由**（2026-09-15 新建，契约 `docs/MEMORY-SPEC.md` §7）：`GET /api/memory`（画像列表）/`DELETE /api/memory/:id`（**未知 id 回 404**——与 terms 的恒 200 刻意不同，前端要能区分「删错了」）/`DELETE /api/memory`（全部清空）/`GET|DELETE /api/memory/summary/:sessionId`（会话摘要查询与清空）。★ **这几个端点是必做不是锦上添花（ADR-5 不静默）**：记忆是**自动写入**的，看不见改不了就只是黑箱，而一条写脏的画像会**污染此后每一个会话**（不像会话摘要只影响一个）。★ **刻意不设写入端点**：画像的写入路径唯一（压缩过程），多开一条手写路径等于承认「模型自动写的东西需要人补」，那不如直接修提示词 |
| `packages/server/src/learning/quiz.ts` | 出题引擎（协议解析/题库/逐题统计）**+ 题型配比**：`loadQuizMix`/`saveQuizMix`（`app_settings` 键 `quiz_mix`）、`buildMixInstruction`（配比指令拼进提示词）、`applyQuizMix`（多出裁掉、自造题型丢弃、少出记进 report 不补题）——配比规则一律过 shared，不在此另立标准。**+ 出题配图**（契约 `docs/QUIZ-IMAGE-SPEC.md` v1.1.1）：`loadQuizImage`/`saveQuizImage`、`buildImageInstruction(on)`（**开＝正面强制口径 + 属性单引号 + 每组最多 3 张；关＝明令一律 `""`**，v1.0 那套劝说式负面措辞实测 0 出图）、`parseQuizBlock` 五级阶梯（前置无损补括号 → 原样 parse → 剥图重试 → 截断逐题回退 `salvageTruncatedQuiz` → 回退后再剥图）+ 产出 `QuizImageReport{on,delivered,droppedSvg,truncated}`；`normalizeQuiz(payload, { allowSvg, report })` **开关关着就在服务端无条件剥 `svg`（硬门，不靠提示词碰运气）**；`parseQuizBlock(text, report?, allowSvg?)` 同样收这两个参数。**＋ 回答方式偏好**：`generateQuiz(topic, material?, mix?, report?, styleArg?, online?)` 第五参省略＝读库内偏好、显式传入＝**只覆盖本次不落库**（L1 就地弹卡用）；**第六参 `online`**（2026-09-13，契约 `docs/QUIZ-SEARCH-SPEC.md`）＝本次是否联网，命中则由 `quiz-search.ts` 的 `buildQuizSearchBlock` 注入参考资料段；注入段顺序固定为 **配比→配图→偏好→联网**（flow/quiz 两处都有回归锁钉顺序）。**＋ 失败真因**：出题失败时回填 `report.failure = 'no-model'｜'parse'`——谁真知道原因谁填（引擎知道），路由只据此选文案、**不反推**（反推在被 mock 的测试与「角色绑定存在但 provider 被停用」边缘态会判错）。**＋ 出题专用模型参数**：适配器调用显式传 `temperature: QUIZ_TEMPERATURE`(0.4) 与 `maxTokens: getQuizMaxOutputTokens(model)`（见 `llm/model-limits.ts`）。**★ v1.1 修真 bug**：联网判据原写 `online && report`，而 PK 传 `online=true` 但**不传 report** ⇒ PK 的「硬编码联网」**从未真正联网过**；现改为**只看 `online`**（`report` 只是「要不要记账」的可选出参），并按 `mapQuizSources(parsed, found.refs)` 回填题目级来源。★ **本文件 383/400 行**（2026-09-13 把配图逻辑搬出到 `quiz-image.ts`；此前 397/400 只剩 3 行余量）——**再加任何逻辑前必须先开新文件**（`quiz-json-repair.ts`／`quiz-image.ts`／`quiz-search.ts` 三者皆因此单开；**2026-09-15 薄弱点分析亦因此迁出到 `quiz-weak.ts`，本文件回落 366/400**）。**★ B2 判断题（2026-09-20）**：题型加 `judge`（QUIZ_TYPES **末位**，勿中插——顺序即出题排列序与削档序）；提示词格式示例/normalizeQuiz/applyQuizMix 三处已同步；judge 真题侧恒 0（`sourceKindCap`），判断题只走 AI 侧 |
| `packages/server/src/learning/quiz-record.ts` | 逐题统计落库（2026-09-19 M2d-3 从 `quiz.ts` 拆出——该文件加归属后触 400 行红线，按仓规拆文件不压注释）。`recordAnswer(quizId, index, correct, ownerId)`：quiz_stats 主键是 `(quiz_id, question_index)` 复合、quiz_id 是全局 uuid 不撞键 ⇒ v33 只加列不重建；读侧按 `ownerForWrite` 过滤（聚合读形状）；`ON CONFLICT` 目标不变（主键没动） |
| `packages/server/src/learning/quiz-announce.ts` | **题组「进会话」的唯一出口＝出卡门面（2026-09-23 批次 G 新建，契约 `docs/TOOL-ECOSYSTEM-SPEC.md` §5.4；43 行 + `quiz-announce.test.ts` 7 例）**：`quizRowContent`（`[QUIZ]{…}` 登记行，★ 自本批起顶层带 `quizId` 登记键，与 `[SCENARIO]` 的 `quizId`/`demoId` 同构）＋ `announceQuizToSession`（先 SSE `block` 帧、再落一行登记文本）。★ **为什么单开文件**：这段原先只写在 `routes/quiz.ts`，现在聊天工具的 `generate_quiz` 要出**同一张卡**——抄第二份就是三处互相不校验的漂移点（`blockId` 形状／登记行 content／前端 `restoreQuizBlock` 的解析口径），漂了不报错、只表现为「刷新后卡片变成一坨 JSON」。与同族 `announceScenarioToSession`（`learning/scenario.ts`）逐字同形。★ `blockId` **只算一次**：改前 `quiz-${quizId ?? Date.now()}` 在同一段写了**两遍** ⇒ 未落库时两次取时可比 1 毫秒不同，前端反解出的 quizId 与帧对不上，症状是**答了不记账**（零报错零日志、界面完全不可见）。★ **无 `sessionId` 时调用方自己判**（`generate_quiz` 只在有会话时才出卡）——本函数不猜会话 |
| `packages/server/src/learning/quiz-image.ts` | 出题配图读写与提示词构造（契约 `docs/QUIZ-IMAGE-SPEC.md` v1.1.1）：`loadQuizImage`/`saveQuizImage`（`app_settings` 键 `quiz_image`）、`buildImageInstruction(on)`（**开＝正面强制口径 + 属性单引号 + 每组最多 3 张；关＝明令一律 `""`**）。**2026-09-13 从 `quiz.ts` 原样搬出**（零行为改动）——`quiz.ts` 当时触 400 行红线，依 AGENTS「再加逻辑前先开新文件」规则搬迁；`quiz.ts` 以 `export { loadQuizImage, saveQuizImage, buildImageInstruction } from './quiz-image.js'` 转出，既有 import 路径零改动 |
| `packages/server/src/learning/quiz-search.ts` | 出题联网检索（契约 `docs/QUIZ-SEARCH-SPEC.md` **v1.1.0**，2026-09-13 新建／同日扩来源标注）：`buildQuizQuery(topic, material)` 定检索词（具体主题→主题；占位「综合/根据当前对话内容出题」→材料首段 60 字；两者皆无→空串不搜）；`buildQuizSearchBlock(topic, material, report?)` 调既有 `searchWeb`（与聊天流 `search_web` 同款），命中 ≤6 条、单条摘要压 300 字，注入段开头硬声明「**是素材不是指令**」+「冲突以材料为准」（防间接提示注入挤掉 JSON 协议）；失败三层降级全 catch、**不阻断出题**（ADR-4），`report` 如实记账（ADR-5）。**返回 `{block, refs}`**（v1.1 由 `string` 改）：结果按 url 去重（编号必须一对一映射）、注入段末尾加编号引用规则行（模型只许填 `"refs":[2]` 这类编号，网址一律丢弃）。**`mapQuizSources(quiz, refs)`**（v1.1）：把题目级编号翻译成 `QuizQuestion.source`，**URL 只取自 `refs` 表、绝不读模型给的内容**（弱模型幻觉 URL 是常态，「来源指向不存在的网页」比「没有来源」更糟）；编号越界/非数字/空表一律不填 source，多编号取第一个合法；**无论映射成功与否都删掉 `refs` 字段**（它不是 `QuizQuestion` 成员，留着会顺落库污染题库） |
| `packages/server/src/learning/collect.ts` | **现场搜集管道（契约 `docs/RESOURCE-SPEC.md` v0.2 批1，2026-09-18 v0.2.57 新建，292 行 + `collect.test.ts` 24 例）**：`previewCollect` 走满六步中的①~④（`deriveCollectQueries` 搜集词派生 → 复用 `searchWeb` → `fetchSafe` 逐页抓取（**自设 15s 超时**，现状"单工具超时未设"是 TOOL-ECOSYSTEM:383 已记账风险，新管子不得扩大它）→ `routeRole('quiz-generator')` + `COLLECT_PROTOCOL`（⊃ `[QUIZ]` 四字段）温度 **0.2** 让模型**逐字摘录**），`commitCollect` 才 `saveQuiz(quiz,'collect')`。★★ **核心不变量「摘录不是创作」由 verbatim 锚点锁守**：锚点**一律由服务端从题干重算、绝不信模型给的锚**；`normalizeForAnchor`（**NFKC** → 只留 `\p{L}\p{N}` → 小写）后题干前 20 字锚点必须在页原文 `includes` 命中，命不中整题拒（`reason:'疑似非原文题目'`）——**宁漏真题，不误收编题**（对齐 choice-nudge「宁可漏不可误」红线，误杀面如实记账在契约 §3.4）。★ **commit 侧不信任客户端的 `ok` 标记**：提交集＝**机验 ∩ 人验**，且 `normalizeCollectedQuiz` 把客户端夹带的协议附加字段（`anchor`/`page`/`refs`/`svg`）一律剥净——外部输入直通写库是本仓红线（TOOL-ECOSYSTEM §结果治理：外部结果永不直接写库）。★ **复用不新造**：抓页正文用 `search/index.ts` 的 `htmlToText`（本批由内部函数转导出，零新解析器）、落库走 `quiz.ts` 的 `saveQuiz`（`quiz_bank.source` 列无 CHECK ⇒ **零迁移**），quiz_stats/笔记/判分全链路自动接住搜集题 |
| `packages/server/src/learning/quiz-weak.ts` | **薄弱点分析——AI 实时生成（契约 `docs/QUIZ-WEAK-SPEC.md` v1.1，2026-09-15 新建）**：`analyzeWeakPoints(quizId)` 走 **`analyzer` 角色**，喂「逐题统计（`quiz_stats`）+ 题干选项 + **用户错选快照**（`quiz_notes.my_answer`）」由模型聚类出若干薄弱主题并给具体错因；**错选快照不可省**——没有它模型只能对着题干和正确率泛泛而谈，产出等于换了说法的固定文本（这正是本批要治的病）。★ **改判背景**：旧实现在 `quiz.ts` 里是纯本地规则、两句硬编码文案、`fallback` 恒为 `true`，而 `analyzer` 角色虽早在 `router.ts` 注册却**从无代码调用**（基础设施白建）⇒ 现改为 **AI 主路径 + 本地规则降级**（ADR-4，`localWeakPoints` 保留且不得删）。★ **三层降级真因由本文件填**（`no-model`／`call-failed`／`parse`，ADR-5 谁真知道谁填、**路由不反推**——「角色绑定存在但 provider 被停用」在反推法下会被误判成 `call-failed`）。★ **空态与降级必须分开**：`analyzed === 0`（还没做题）与 `fallback === true`（模型没接上）是两件事，混成一句就会出现「模型没配」被说成「你还没做题」。★ `parseWeakJson` 四道阶梯（原样→剥围栏→截首尾括号→过两道无损修复），**修复顺序不可换**：`repairJsonEscapes` 必须先于 `repairJsonBrackets`。★ 参数：`WEAK_TEMPERATURE`(0.3，低于出题 0.4——分析要稳不要发挥) + `WEAK_MAX_QUESTIONS`(12，错题封顶) |
| `packages/shared/src/scenario.ts` + `packages/server/src/learning/scenario.ts` + `packages/server/src/learning/scenario-protocol.ts` + `packages/server/src/routes/scenario.ts` + `packages/web/src/features/quiz/ScenarioPanel.tsx`（+ `scenario-view.ts`、`web/src/features/chat/chat-blocks.ts`/`use-quiz-actions.ts`/`chat-menu.tsx`） | **情景题域（契约 `docs/SCENARIO-SPEC.md` v1.1，2026-09-17 新建，M1~M4 全部落地，2026-09-17 分期收官）**：AI 生成 demo 的可回传交互题——**每个评分点在 `quiz_stats` 里就是一道普通题**（占一个 `question_index`），统计/笔记/薄弱点分析零接线复用。★ **裁判在服务端**：demo 只上报事实（observed），对错由 `judgeTask(criteria, observed)` 按 criteria 判（三判型 choice/state/order，形状不对一律判错不抛错）；demo 内本地反馈只是体验。★ **回传唯一通道**：`SCENARIO_BRIDGE_JS`（服务端出页注入，`__SB_DEMO_ID__` 占位符替换）→ `window.SBScenario.report(taskId, observed)` → parent.postMessage → 宿主白名单（type→demoId→taskId 三道）→ REST → **服务端第二道 taskId 白名单**再判分。★ **白送的安全边界**：CSP sandbox 源为 null ⇒ demo 调不动写接口，伪造数据只能走桥接独木桥。★ **存储 1:1 分家**：quiz_bank 存 ScenarioPayload（`source='scenario'`），demo HTML 存 `scenario_demo` 表（**迁移 v20**；不复用 preview 内存暂存——demo 是题目本身不能丢）；`listQuiz` 计数按「有无 `tasks` 键」分派两种形状；删套题连带删 demo（**接线在 routes/quiz.ts，不在 quiz.ts——避免与 scenario.ts→recordAnswer 构成循环依赖**，同 QUIZ-WEAK-SPEC §9 先例）。★ **M2 出题协议**（scenario-protocol.ts，契约 §6）：**双标记**——`[SCENARIO]` JSON + `[SCENARIO_HTML]` 裸 HTML 分离（HTML 不进 JSON 字符串，转义是 svg 字段翻车坑的放大版）；`parseScenarioBlock` 四档救援阶梯（原样→漏转义漏括号修复→JSON 截断线性逐题回退→HTML 截断收口抢救，全部无正则回溯）+ **引用完整性静态检查**（id 接不上 demo 的评分点整条丢弃——死题不进库）；`generateScenario` 复用 quiz-generator 角色/温度/输出上限，落库走 M1 同一条 `saveScenario` 闸门；`POST /api/scenario/generate` 502 真因分开说（no-model/parse），不设 save 开关（不落库就没 demo 可玩）。★ **ScenarioPanel 刻意不提供新标签页打开**（parent 一换回传链当场断）；完成态只认服务端判分响应，results 只增不改。★ **M3 聊天流卡片（已落地）**：`POST /generate` 带 `sessionId` → SSE block 事件（blockId=`scenario-<demoId>`）+ 历史落库 `[SCENARIO]` 登记文本（payload 顶层 + quizId/demoId 登记键，quiz_bank data 不污染）；`chat-blocks.ts` 承载 block 分派（**quiz 分派从 useChatStream 挪入**——398/400 贴线，行为逐字同构）与历史还原（**quiz 块历史上没做历史还原，情景题从第一天生效**）；MessageRow 内嵌 ScenarioPanel（宿主面板进消息流，无新标签页）；composer「+」菜单「情景题」入口（`FlaskIcon`）。★ **三个贴线文件的拆分先例**：ChatView 299→`use-quiz-actions.ts`（出题动作域 hook）、ChatComposer 296→`chat-menu.tsx`（菜单条目构造）、useChatStream 398→`chat-blocks.ts`——加聊天流功能前先看这三处有没有现成落点。★ **M4 编排+薄弱点（2026-09-17 落地；★ 2026-09-25 批次 K 之后只剩"薄弱点"半边）**：编排半边＝`FlowStepKind` 第 7 种 `'scenario'` + 专用执行器 `scenarioStep`（原 `flow-executors.ts`，**唯一不走 chatStep 的步骤**——直接调 generateScenario + 停步等人玩）＋`buildStepPrompt('scenario')` 抛哨兵的穷尽性契约锁，**随学习流整族下线删除**（`learning/scenario.ts` 里 `announceScenarioToSession` 的头注就是它留在这儿的墓碑；那个函数本身活着，因为 REST 路由仍是唯一出题入口）；薄弱点半边原样活着：quiz-weak.ts `scenarioTasksOf` 形状探测（修掉对情景套题分析 TypeError 崩溃）+ `buildScenarioWeakPrompt`（任务+判据做素材，上界=tasks.length）。★ **配比档（v0.2.40，SPEC §6.1）**：情景题是题型配比第 5 档（`QuizMixKind`/`MIX_KINDS`/`MAX_SCENARIO_PER_MIX=3`，DEFAULT 0 老用户零变化；normalize 超限先砍情景）；generate 路由**纯情景配比跳过传统引擎**、逐套失败不连坐（`scenarios[]` 如实回账）；设置卡第 5 行按套计 + `scenarioMixNote` 补白。★ **未做**（契约 §9）：observed 快照落库/情景题刷题笔记（quiz_notes.question_data 是 QuizQuestion 形状）、联网检索接入出题 |
| `packages/server/src/llm/router.ts` | **LLM 路由与成本归主（2026-09-18 M2c 批改，契约 `docs/TENANCY-SPEC.md` §8.1）**：`routeRole(role, fallbackModel?, ownerId?)` 是「这一轮该用哪个 provider/模型」的唯一落点，M2c 给它加了**第三参 `ownerId`**（**显式传参、不引 `AsyncLocalStorage`**——ALS 在两条定时器路径上只能读到 `undefined`，且「这笔钱记在谁头上」应当在代码里看得见）。★ **三级取用**：① 自己的绑定（`owner !== null` 时）→ ② 平台绑定（`owner_id IS NULL`）→ ③ 平台默认 provider；★ **回落换整条链，不是只换 model 名**——在别人的 provider 上填自己的 model 是必然失败。★ **三态 `ownerId` 语义**：`string` = 该用户；`null` = **平台通道**；**省略** = 也走平台通道（保住加参之前的行为，故既有单测无需改）。★ **平台通道的判据是 `owner_id IS NULL`，不是 `''`**——`''` 会落进「一个不存在的用户」。★ **读侧与写侧判据刻意不同**：`getProviders`/`providerById` 用 `owner_id IS NULL OR owner_id IS ?`（否则免费通道**绑不上**、功能变装饰），而增删改在 `routes.ts` 用**严格本人**。★ `seedIfEmpty` 改 `INSERT OR IGNORE`——它**依赖 v29 的平台行部分唯一索引**（复合 PK 在 SQLite 下拦不住 NULL，见 `ownership.ts` 行末与 `TENANCY-SPEC §8.1.2`）。★ **`bindQuota`（v0.2.61 两层闸门批）**：`routeRole` 返回的 `adapter` 已被 `bindQuota()` 包过——`{ ownerId: 请求者, platform }` 每次 `chat()` 自动塞进 `ChatRequest.quota` ⇒ **15 个调用点零改动、结构上不可能漏传**（`listModels` 不闸——元数据调用不花 token；`type` 原样透传——`chat/flow.ts` 据它决定是否开思考链）。★ 该函数此前**只在别处被 mock、零直接断言**，M2c 起由 `llm/router.test.ts`（**真库 + 真函数，只桩网络**）**14 例**在守。★★ **2026-09-21（一键默认设置批）：本文件 413 行 ⇒ 拆出 `llm/platform-channel.ts`**（平台落点 `firstPlatformProviderId` ＋ env 凭据/模型解析 `resolveProviderCredentials`／`platformDefaultModel`，**搬的是原文**，含并行批 v39.1 双入口随机的全部注释），本文件 413→**307**；★★ 同批修一处**真实潜伏 bug**：`targetFromProvider` 的 ① 分支原硬写 `platform: false` ⇒ **登录用户把角色绑到平台 provider** 时 env 里的平台 key **不被注入**（`apiKey` 变空串）**且**该轮调用**不计入** 250 次/5 小时免费额度；判据改为「provider 行是不是 `owner_id IS NULL`」后 ①②③ 三分支同口径（一次性探针实测 ＋ `router.test.ts` 回归锁）；★ `roleReady` 同批补**密钥检查**（此前只判 `model` 非空——v39 起「model 有值但 key 是空的」成了真实状态，文案「平台免费通道还没开通（服务商密钥未配置）」） |
| `packages/shared/src/platform-channel.ts` + `packages/server/src/llm/platform-channel.ts` + `packages/server/src/routes/providers.ts` + `packages/web/src/lib/api-providers.ts` + `packages/web/src/features/settings/PlatformChannelCard.tsx` | **设置页「一键默认设置」+ 模型下拉（2026-09-21 v0.2.99 新建，零迁移零新依赖）**——老板点单「配置设置哪里可不可以改得更加方便一点,就是直接根据服务商识别出有哪些可以选择的模型,然后直接用选项选,并且搞一个"一键默认设置"按钮,按了之后就一键全部配完,使用服务默认的agnes-2.5-flash的免费限额」。★ **一句话**：把「手打模型名 + 逐角色配置」改成「选一个 + 按一下」。★★ **同名不同层，别混**：`shared/` 那个只放**前后端共用常量**（`DEFAULT_PLATFORM_PROVIDER_ID`／`DEFAULT_PLATFORM_MODEL = 'agnes-2.5-flash'`，与 `platform-quota.ts` 刻意分家——后者头注写明「本文件只放**配额**的常量与形状」）；`server/llm/` 那个放**只有服务端能碰**的东西（平台落点 `firstPlatformProviderId` ＋ `resolveProviderCredentials` ＋ `platformDefaultModel`）。★★ **`resolveProviderCredentials()` 是全仓「用哪把 key、打哪个地址」的唯一实现**（`env > 库`，且**只对平台行**注入 env——BYOK 行被 env 覆盖＝「你配了 key，但花的还是平台的钱」）；**其返回值带明文 key ⇒ 绝不可回给前端**（老板要求「key 用户不可见且无法通过其他手段获取」）；平台行的 `api_key` 库里**永远是空串**，key 只在 env ⇒ **拖库也拿不到**是结构性保证。★★ **刻意不给 baseUrl 默认值**：老板给的两个 agnes 地址 `https://api.agnes-ai.cn/v1` 与 `https://apihub.agnes-ai.com/v1` 从**生产机实测都通**（均 401 ＝ 存在且要鉴权），到底哪个配这把 key 只有 key 持有者知道——**猜错的表现是「连得上但 401」**，会把排查引到 key 上去，比连不上更难查。★ **`POST /api/providers/roles/default`**：把**请求者自己**的 8 个角色全绑到平台通道、`model` 一律写**空串**（★ 写死 `agnes-2.5-flash` 就是**第二个真相源**：`role_bindings.model` 优先级高于 `SB_PLATFORM_MODEL`，运维改 env 换模型会全站纹丝不动）；无启用平台 provider ⇒ **409 且一行不写**（不假成功、也不回 500——那是已知状态不是故障）；8 条在**一个事务里同生共死**；`owner === null`（本地单人）才写平台行，否则写用户行（写平台行＝「任何人点一下都改到全站所有人」的权限事故）。★ **`GET /api/providers/quota`**：`limited` 与 `upstream-gate.ts` 的计量口径**逐字对齐**（`platform && ownerId !== null` 才计量；本地单人 ⇒ `limited:false`）。★ **`GET /:id/models` 同批必须修**：原用 `decryptSecret(row.api_key)`，而平台行 `api_key` v39 起恒空 ⇒ 免费通道**永远拉不到模型列表**（`listModels` 拿不到 key 只会**静默返回 `[]`**）。★ **`PUT /roles/:role` 的 `model` 由必填改为允许空串**（空＝用默认，与一键默认写同一个值——界面给得出这个选项而服务端 400，是最难解释的一类不一致）；**非字符串一律当未提供**（`String(123)` 会造出叫 "123" 的幽灵模型名）。★ **`RoleRow.tsx` 模型列**：有候选 ⇒ 真 `<select>`（含「（用默认模型）」项）；★★ **值不在候选里 ⇒ 落「自定义…」并给输入框**（下拉遇到未知值**不许静默显示第一项**——用户会「看着像选对了、实际存的是别的模型」，且一保存就把绑定改掉）；★★ **空模型 ≠ 自定义**（`models.includes('') === false`，写成 `custom \|\| !models.includes(model)` 会让**每个尚未绑定的角色**都冒出空输入框——**这条是本批自己写的用例逮到的真 bug**）；候选为空 ⇒ 降级成输入框（ADR-4）。★ **行数红线三处拆文件**：`routes.ts` 353→172、`web/lib/api.ts` 397→377、`llm/router.ts` 413→拆出 `llm/platform-channel.ts`（307）。★★ **收口（v0.2.101，2026-09-21）—— 真机探针逮到并修掉一个「单测全绿、真机上点了没反应」的真 bug**：点「一键默认设置」后提示说「已配好 8 个角色」，而绑定表里「讲解」那一行**仍显示旧服务商 + 旧模型**（服务端已改、界面没跟）。根因＝`RoleRow` 用 `useState(initialProvider)`（**只在挂载时取一次值**），而父组件 key 固定为 `key={r.role}` ⇒ `reload()` 换了 props 也换不掉行内 state；★★ **本仓此前 8 条 `RoleRow` 渲染锁全绿，没有一条能拦住它**（jsdom「只渲染一次」看不见这类缺陷）。修法＝把 props 当**真相源**（`RoleRow.tsx` 的 `useEffect`，依赖**只有两个服务端来的值**——把 `providers` 放进去会把**用户正在编辑的行**打回原状；空值回落用函数式更新拿 `prev`）；同批补两条回归锁（`RoleRow.test.tsx` 8→**10**）。★ 仪器＝`tools/probes/settings-platform-cdp.mjs`（见下方探针表）。★ **未验**：线上真机一键配置未跑（生产 `.env` 的 `SB_PLATFORM_*` 仍 0 条）；两个 agnes 地址哪个是真上游未定；★ 一键默认**会覆盖用户已有绑定**（含自填模型名）且**当前无二次确认**，待老板拍 |
| `packages/server/src/llm/upstream-gate.ts` | **上游并发闸门（2026-09-17 新建；2026-09-18 v0.2.61 改两层）**：`acquireUpstream(baseUrl, purpose='main', signal?, quota?)` 给每个上游一扇信号量，内层容量 `UPSTREAM_MAX_CONCURRENT=2`。★ **存在理由＝一次真实事故**：两路会话同时运行会双双卡在「回复中」（根因是等待兜底从未生效，见下一行），而每轮 `done` 后还固定追加两次 LLM 调用（`extractTerms`／`compactIfNeeded`）**立刻抢走刚空出的配额**、让被挂起的会话更难恢复。★ **容量 2 是「两路对话可并行」的下限**：给 1 会把两路对话焊成串行、一路慢就拖死另一路（正是事故现象）。★ **按 `baseUrl` 分桶而不是按 provider 记录**——配额是**上游**的，同一上游的两条 provider 记录共享闸门；空 `baseUrl` 归同一兜底桶。★ **主链优先**：`purpose='background'` 的后台任务**不得越过排队中的主链**（主链延迟是用户可感知的转圈，后台只是为下一轮备料），但槽位空闲时后台不必等。★ **`release` 必须幂等**（重复调用不能把容量还两次导致超额）；**排队期间被停止**立即拒绝并**从队列移除**（不占位子等一个永远不会来的放行）。★ **调用点只有 2 个是后台**（`learning/terms.ts` 的 `extractTerms`、`chat/compact.ts` 的 `compactIfNeeded`）——`ChatRequest.purpose?` 默认 `'main'`，故全仓 14 个适配器调用点中其余 12 个零改动。★ **`acquire` 只在 `chat()` 顶部调用一次**，`chatStream`／`chatOnce` 都不再自己获取（`chat→chatOnce` 是内部转调，重复获取会在容量 1 时**自己把自己锁死**）。★★ **与等待兜底必须同批**：闸门把「无限并发」变成「排队」，而排队安全性**完全依赖「持有者一定会释放」**——上游挂起时持有者永不释放会让容量被永久占满，**反而比不限并发更糟**（从「各自卡」变成「互相拖死」）。★★ **M2c 两层闸门（2026-09-18 v0.2.61 已落码）**：契约 §8.1.3.1 的两层已实现——内层 `(baseUrl, 请求者)` 限**每用户 2**（`UPSTREAM_MAX_CONCURRENT`）、外层 `baseUrl` 限**全站封顶 N**（`SB_UPSTREAM_SITE_MAX_CONCURRENT`，**缺省 8 为占位值，待老板给业务值**）+ **队列上限**（`SB_UPSTREAM_SITE_QUEUE_MAX` 缺省 20，超出**直接拒**、文案 `UPSTREAM_BUSY_MESSAGE`）。★ **`acquireUpstream` 现为四参** `(baseUrl, purpose, signal, quota?)`，`quota: { ownerId, platform }` 由 `routeRole` 经 `bindQuota()` **绑在返回的 adapter 上** ⇒ 调用点零改动、结构上不可能漏传（这与否掉 ALS 的理由不矛盾：ALS 的危险在于**有"缺省"这个状态**，绑在返回值上**没有缺省态**）。★ **三条"写反了也不报错"的口径**：① `quota.ownerId` 是**请求者**、**不是 provider 的 owner**（免费通道 provider 的 `owner_id` 恒为 `NULL`，写反 ⇒ 所有免费用户挤进同一个容量 2 的桶）；② **外层只约束 `platform === true`**——契约原话「BYOK 不同 baseUrl ⇒ 天然另一个桶」**只在两家服务商不同时成立**（自带 OpenAI key 时 baseUrl 与平台完全相同），故按标志判断而不是靠 baseUrl；③ **内层先、外层后**，外层失败**必须归还内层槽**（否则该用户内层槽**永久泄漏**）。★ **为什么外层不可省**：免费通道**额度不限**，若只限每人 2，100 个用户就是 200 并发 ⇒ **等于没限**。★ **外层队列上限不可省**——不配的话排队者无限堆积，表现为「请求不报错但**永远不返回**」，比直接拒绝更难查。⚠️ **已知边界**：实现是**进程内 `Map`** ⇒ 多实例部署容量 × 实例数（本批不做，需外部存储） |
| `packages/server/src/llm/upstream-timeout.ts` | **上游等待兜底（2026-09-17 新建）**：`createUpstreamGuard({controller, external, idleMs?, totalMs?})` + `touch`／`dispose`／`timedOut`／`timeoutMessage` + `asUpstreamError`。★★ **存在理由＝本仓一次真事故**：两个适配器的兜底原写成 `if (req.signal) {桥接外部信号} else {setTimeout(abort, 120s)}` 的**二选一**，而生产路径（`/chat/send`、`/regenerate`、`/resend`）**总是**传 signal（那个 `AbortController` 只在用户点「停止」时才 abort）⇒ 那句 120s **从未生效**；上游「收下请求但不回数据也不断连」时 fetch 永久 pending → `runTurn` 不 settle → `aborters` 不摘除 → 侧栏「回复中」永不消失 → 前端 `busy` 永久 true。**修法是并存而非二选一**。★ **按形态分流**：流式＝**空闲超时** `UPSTREAM_IDLE_MS=120s`（每读到一段数据即 `touch()` 重置——**长回答不受影响**，只掐真静默的）；一次性＝**总时长** `UPSTREAM_TOTAL_MS=180s`（该模式没有空闲概念，`touch` 不重置）。★ **超时抛可读错误**（「上游 120 秒无响应，已中止本轮（服务商可能过载或限流，可稍后重试）」）而非裸 `AbortError`；`asUpstreamError` **只改写超时、HTTP 报文原样透传**（否则会把 429/500 也说成超时，把用户引向错误的排查方向）。★ **三条边界**：外部先中止时 `timedOut()` 为**假**（不许把「用户已停止」谎报成上游故障）、`dispose()` 必须**摘掉外部监听**（不摘会随会话数累积泄漏）、外部已中止则立即 abort 不等计时器 |
| `packages/web/src/features/quiz/weak-report.ts` | **薄弱点分析的展示文案纯函数**（2026-09-15 新建，照 `mix-report.ts` 先例，契约 `docs/QUIZ-WEAK-SPEC.md` §6）：`weakView` 把一次分析结果翻成渲染模型（多主题列表 + 口径行 + 空态/降级提示），`indexText` 做 0 基→1 基题号。★ **两条「写错不报错、只是骗用户」的锁**：① **降级必须如实标注**——三条真因文案两两不同、每条都含「本地规则版」，**不得把降级结果渲染成 AI 分析**（用户为此去设置页绑模型才是正解）；② **空态与降级分开**——`analyzed===0` 说「先做题」，**有错题却没分析出结果时不许说「先做题」**（那是误导）。★ 判定逻辑一律下沉到本文件：本仓 `.tsx` 无测试环境，留在组件里就测不到 |
| `packages/web/src/features/settings/QuizMixCard.tsx` | 设置页「出题题型配比」卡：4 预设 chip + 四题型步进器（0..10）+ 总题数提示，读写 `GET/PUT /api/settings/quiz-mix`；配比全局一份，对话页「出题」与题库页「一键出题」共用 |
| `packages/web/src/features/settings/QuizImageCard.tsx` | 设置页「出题配图」总开关（契约 `docs/QUIZ-IMAGE-SPEC.md`）：**默认关**，读写 `GET/PUT /api/settings/quiz-image`；点选即存。关＝服务端硬门剥掉 `svg`（模型给了也不渲染）；开＝提示词正面强制「涉及示意图的题目必须给 `svg`」，不必配图的题给 `""`，每组最多 3 张。题内渲染走 `SvgPreviewCard`（唯一注入点）；没图/丢图/被截断由 `mix-report.ts` 的 `imageNote()` 如实补一句（ADR-5 不静默） |
| `packages/web/src/features/settings/AnswerStyleCard.tsx` | 设置页「回答方式偏好」卡（**L0**）：四维 chip 行 + 当前摘要 + 「恢复默认」（＝调 DELETE 删键，回到没配过的态，下次出题会重新问）。点选即存，屏上永远回显服务端归一后的值（不能选 A 存 B） |
| `packages/shared/src/speech.ts` + `packages/server/src/storage/speech.ts` + `packages/server/src/routes/settings.ts` + `packages/web/src/lib/api-settings.ts` + `packages/web/src/features/settings/SpeechCard.tsx` + `packages/web/src/features/settings/RoleRow.tsx`（＋ 既有 `packages/web/src/lib/speech.ts`） | **AI 英文发音的语音设置（2026-09-20 v0.2.83 新建，零迁移零新依赖，复用 `app_settings(owner_id,key)` 表）**：设置页新增「英语发音」卡——**音色**（下拉，候选来自浏览器运行时 `speechSynthesis.getVoices()` 的英文音色）＋ **语速**（滑块 0.5×~2.0×／步长 0.1）＋ **试听** ＋ **恢复默认**。★ 一句话：**发音此前是「一刀切」**——`speakEnglish` 把 `SpeechSynthesisUtterance` 的 `lang` 写死 `en-US`、`rate` 用浏览器默认、`voice` 从不指定，用户既换不了音色也调不了快慢。★★ **本组最要紧的一条口径：服务端不白名单 `voiceName`**——音色候选是**浏览器运行时**才知道的（Chrome／Edge／系统语音包因机器而异），服务端若按自己知道的清单校验，用户换台机器后选择会被**静默清空**（把运行时事实写进服务端常量表的典型病）；故服务端只做**结构校验**（trim／**超 120 字符回落空串而不是截断**——截出的半截名字在任何机器上都匹配不到／非字符串 → 空串），前端 `pickVoice()` 找不到该音色时**回落系统默认**（合理降级，不是失败）。★ **语速钳位写两处**：滑块是体验、`normalizeSpeechSettings` 是契约（`NaN`／`Infinity`／字符串数字／越界一律回落，**逐字段回落而非整份作废**——一个坏 `rate` 不该把用户选的音色一起洗掉；保留 1 位小数以对齐 `step=0.1`）。★ **设置拉不到不该让喇叭哑掉**：`lib/speech.ts` 的 `resolveSpeechSettings()` 先读内存缓存再打 `/api/settings/speech`，**任何失败回落 `DEFAULT_SPEECH_SETTINGS`**、朗读照旧可用。★ **不做假控件**：环境不支持朗读时**连试听按钮一起不渲染**（摆一个点了没反应的按钮比不摆更糟，同「标记已掌握」按契约砍掉）；「恢复默认」走 `DELETE` **真删键**（写一份默认值会让将来改默认值时老用户永远拿不到）。★ **行数红线逼出三处拆分**（**拆分而非压注释**，仓里既有先例）：`routes.ts` 407→`routes/settings.ts`、`api.ts` 407→`lib/api-settings.ts`（两者都留 re-export ⇒ `index.ts` 接线与全部调用方**零改动**）、`SettingsView.tsx` 302→`features/settings/RoleRow.tsx`。★★ **两条实测踩到的坑**：① `speakEnglish` 由同步变**异步**（要 `await` 设置）⇒ `TermText.test.tsx` 那条「朗读失败时给提示」在断言处 `spoken` 仍是 `null`、报「找不到朗读请求」——**不是组件坏**，加 `await waitFor(...)` 即绿（该文件例数不变 18）；② 本仓 `vitest.config.ts` **未开 `globals`** ⇒ `@testing-library/react` 的**自动 cleanup 不生效**，残留 DOM 跨用例累积（`getByText` 报 "found multiple elements"），新 `.test.tsx` 必须**显式 `afterEach(cleanup)`**——`SpeechCard.test.tsx` 首轮 5/6 红即此因。★ `speechSummary` 的空音色文案是「系统默认英文音色」而**不是空串**。★ **未验**：真机听感（各音色的实际音色／两端点语速／试听发声）未听过——沙箱无音频输出、`speechSynthesis` 在 jsdom 里是桩；单测锁的是**取值与注入**（`u.rate`／`u.voice` 是否真挂上、本机没有该音色时是否回落默认而**不失败**）。⚠️ **同批未含**：词条卡「向 AI 追问」→ fork 会话 → 知识图自动连边（老板预告的第二件事）**尚未开工**——★ **该预告已于同日 v0.2.84 落地**（见下方「知识图『向 AI 追问』fork 批」行）：本行这句记的是 **v0.2.83 时点**的状态，两批合计 **+8 文件 / +105 例**。 |
| `packages/shared/src/follow-up.ts` + `packages/server/src/chat/follow-up.ts`（★ 原并列的 `learning/follow-up-links.ts`＝追问自动连知识边，2026-09-25 随知识图下线删除，批次 K ⇒ **追问只剩 fork，不再长图**） + `packages/server/src/chat/post-turn.ts` + `packages/web/src/app/SessionList.tsx`（＋ 既有 `packages/web/src/features/chat/TermCard.tsx`／`term-index.tsx`／`TermText.tsx`／`App.tsx`／`api.ts`／`routes.ts`／`routes/chat.ts`／`auth/ownership.ts`／`chat/flow.ts`／`storage/migrations-list-v31.ts`） | **知识图「向 AI 追问」fork 批（2026-09-20 v0.2.84 新建，契约 `docs/KNOWLEDGE-FOLLOWUP-SPEC.md`，迁移 v38 `sessions.forked_term`，零新依赖）**：词条卡新增「向 AI 追问」——点一下开一个**独立 fork 会话**，该会话回复里抽出的**每个词条自动与源词条连一条边**，一个回复含多条词条即**星型拓扑**，孩子会话还能再追问 ⇒ 长成树/图（老板点名「类似 Obsidian Graph View」，按**实际 2D** 做，故**不引 three.js**）。★★ **本批的价值有一半在「接上两条暗线」**：`sessions.forked_from_id` 自 **v1 迁移**起就声明了列、`domain.ts` 有类型、**全仓零读写**；`knowledge_edge.origin='ai'` 这个取值**定义着却没有任何生产者**——本批把两条同时接上（**不是新建两套结构**，这也证明「fork」这个产品概念本就在数据模型里预留过）。★★ **四条刻意设计**：① **边的 `origin` 写 `'ai'` 而不是 `'user'`**——老板拍板「**追问是 action，线是知识点关系**」：追问是用户手动确认（「点了才连线」的**开关**），而边断言的是「AI 说这两个词条相关」这个**命题**、命题来源是 AI；把「谁点的按钮」写成边的来源，图上的边就退化成**操作日志**；② ★ **只在用户确认的追问上连边**，**不做**「同一回复里共同出现就连边」（否则图瞬间变毛球且全是噪声）；③ ★ **不额外调一次 LLM 生成摘要**——上下文直接复用既有 `sessions.summary`（compact 产物）为第 ① 档、最近消息摘录（**排除 tool 消息**、取 8 条）为第 ② 档、`none` 为第 ③ 档；多打一次模型会让「点追问」**同步阻塞在首个 token 之前**（同源：本仓「降级不冒充 AI 分析」= 可用性优先、但要能解释）；④ **抗删快照（第 5 次复用）**：`forked_term` 存**词条名**而非 `term_library.id`（同 `evolution_event.term_text`／`knowledge_node.ref_text`）——词条被删/被合并后**星心仍立得住**；源词条不在库里则**返回 0 且一条边不建**（不留围绕幽灵星心的孤边）。★ **文件分工**：`shared/follow-up.ts`（契约与归一，**前后端共用的唯一一份**：`term` 不合格**拒绝而不截断**——截断会连到图上**错误的节点**、不可自愈；`question` 不合格**截断即可**——坏问题只是这次问得不完整）；`learning/follow-up-links.ts`（**连边**，全批唯一验「图真的长出来了」的地方）；`chat/follow-up.ts`（**三档上下文**与首问装配，**刻意不调模型**）；`chat/post-turn.ts`（**回复后处理**：抽词 → 落库 → 连边 → 压缩，**fire-and-forget**）。★ **行数红线逼出两处拆分**（**拆分而非压注释**）：`App.tsx` → 抽出纯展示的 `app/SessionList.tsx`（App 回落 **291** 行）；`chat/flow.ts` → 收尾段拆到 `chat/post-turn.ts`（flow 回落 **386** 行）。★ **本批记账的「已发现 Bug」为 1**（见 `auth/ownership.ts` 行与 `routes.ts` 行：`sessionExists` 那一处）。★ **新锁非空转**：`follow-up-links.test.ts` 的「非追问会话不建边」与「源词条不在库里不建边」两条都是**反向锁**——只断「追问会话建了边」的话，把判据整个删掉仍会全绿。★ **未验（诚实记账）**：真机端到端未跑（单测锁的是这条链的**每一段**，不等于**整条链通**）；星型在真图页上的观感未目检；`terms`／`study-flow` 页的追问入口已由同日 **v0.2.85** 补上（见下方 `features/study-flow/` 行）、`terms` 页仍未做。★ 交付**零依赖单文件预览** `Desktop/studentbuddy-追问fork改版预览.html` 供目检 |
| `packages/web/src/features/chat/AskStyleCard.tsx` | **L1「出题前先问一次」**：`useAskStyle(run)` 返 `{ tap, card, summary, hint }`——没配过时点「出题」只就地展开内联选项卡（**不新增 modal、不加遮罩**：打断感是这套功能最大的风险），勾「记住」则顺手 PUT。三条实测决定的行为：`configured` **初值 true**（读回来前按已配过处理，宁少问一次也不在出题路上插队）、**落库失败也照常出题**（本次覆盖照样生效）、「别问了，直接出」＝ `PUT 默认值` 后按归一值出题（屏上与库里同源）。聊天页与题库页**共用同一 hook**（两页各写一遍必然漂成两种行为），对话发送永不弹卡、只在未配过时于 composer 上方常驻一句 `hint` |
| `packages/web/src/components/StyleChips.tsx` | 四维 chip 行共用件（设置卡与出题前弹卡同一套渲染，题面/选项全取自 `ANSWER_STYLE_FIELDS`，前端不复制一份文案）；样式自卡在 `style-chips.css`，不依赖调用方的 css |
| `packages/server/src/auth/user-row.ts` + `packages/server/src/auth/github.ts` + `packages/server/src/auth/users.ts` + `packages/server/src/routes/auth-github.ts` + `packages/server/src/storage/migrations-list-v40.ts`（＋ `packages/web/src/components/GithubLoginButton.tsx`、`Landing.tsx` 文案、`shared/src/auth.ts` 错误码） | **GitHub OAuth 登录（契约 `docs/AUTH-SPEC.md` **v0.8.0** §2.8；2026-09-20 v0.2.74 新建，**2026-09-21 v0.2.105 口径反转**；迁移 v36 `users.github_id` ＋ **v40 `users.github_email`**；★ **本行的登记缺口如实记**：v0.2.74 那批当时未在 AGENTS.md 成行，本次随口径反转变更一并补上）**。★ **第三个登录通道**（密码／验证码之外），产出与前两条**完全相同**的会话（同一 `createSession` ＋ `sb_sid` cookie，30 天）⇒ 后续归属逻辑（TENANCY-SPEC）零分支。★ **三端点全是 GET**——OAuth 重定向流必须浏览器整页跳转，**没有 fetch 版本**：`/api/auth/providers`（**前端画不画按钮的唯一依据**，ADR-5 关断）→ `/api/auth/github`（发一次性 `sb_gst` state cookie，302 到 GitHub）→ `/api/auth/github/callback`（`timingSafeEqual` 逐字节验 state → 换 token → 拉身份 → 查号/建号 → 发会话 → 302 回 `/`）。★★ **本批反转的核心口径：严格独立建号（老板 2026-09-21 拍板，推翻 09-20 的「按已验证邮箱自动归并」）**——**查号键是 `github_id`**（退化成「每次登录都新建」＝用户第二次登录历史数据全丢，这是本口径最致命的一点）、**同邮箱不归并**（GitHub 邮箱即便命中现有 `users.email` 也照旧新建独立账号）⇒ 同一人在本站**可以有两个互不相通的账号**，**代价是数据永久分裂、老板明确接受**（⚠️ 将来若要「账号合并/绑定」那是**新增功能**，不是把口径改回归并）。★ **数据层为什么用占位串**：`users.email` 是 `TEXT NOT NULL UNIQUE`，而「同一真实邮箱要承载两个账号」直接冲突 ⇒ GitHub 账号的 `email` 落 **`gh-<github_id>@users.noreply.invalid`**（HTTP 客户端用 `users.noreply.invalid`，RFC 2606 保留 TLD、**永不可解析**），真实/展示邮箱落新列 `github_email`；**不整表重建**（ADR-2）。★ 拿不到邮箱（用户设为私密）时 `github_email` 记 **`{login}@users.noreply.github.com`**（GitHub 官方为不公开邮箱用户提供的地址格式，真实存在且 `login` 全局唯一）——**不能留 NULL**，否则 `AuthUser.email` 会回落成占位串、**照样暴露给用户**。★★ **`auth/user-row.ts#rowToAuthUser()` 是全仓唯一一份「库行 → AuthUser」的映射**（`email: row.github_email ?? row.email`）：★ 抽它的起因是一个**真实的双份实现 bug** —— `users.ts` 与 `github.ts` 曾各写一份 `toAuthUser`，只改一份导致 `/api/auth/me` 返回**占位邮箱**，而 `row.email` 类型仍是 `string` ⇒ **tsc／eslint 全绿、零编译错误**（跨文件重复实现正是没有编译期护栏的那类缺陷）。★ **scope 保留 `user:email`**（曾计划缩到最小，实施复核撤回：账号菜单要显示邮箱，而 `github_email` 的唯一来源是 `/user/emails`）——★ 「保留 scope」与「拿不到邮箱就拒绝登录」是**两件事**：前者保留、后者取消（`GITHUB_EMAIL_UNAVAILABLE` 已退化为罕见兜底，撞号分支**已删除**）。★ **部署依赖三个 env**：`SB_GITHUB_CLIENT_ID`／`SB_GITHUB_CLIENT_SECRET`／`SB_PUBLIC_ORIGIN` —— ★★ **不配 ⇒ 按钮静默不渲染、零报错**（用户看到的是「没有 GitHub 登录」，不是「服务端没配」；2026-09-21 线上被老板发现的正是这一现象，**纯配置、不需要发版**）；★ 唯一离线验不出的硬约束：回调地址必须与 GitHub OAuth App 里登记的 Callback URL 一致，否则真机报 `redirect_uri_mismatch` |
| `packages/shared/src/auth.ts`（`AuthProviders.demo` ＋ 4 常量）＋ `packages/server/src/auth/demo.ts` ＋ `packages/server/src/routes/auth.ts` ＋ `packages/web/src/components/DemoLoginButton.tsx`（＋ `routes/auth-github.ts` 的 `/providers` 加 `demo` 字段、`web/lib/api-auth.ts` 的 `demoLogin`） | **共享体验账号 demo-login（2026-09-22 v0.2.106 新建，契约 `docs/AUTH-SPEC.md` §2.10，零迁移零新依赖）**：线上访客**零凭证** `POST /api/auth/demo-login` 一键进入全站共用体验号。★ **三条刻意口径**：① 入口＝专用端点而非前端预填固定账密（口令不进 JS bundle、可按 IP 限流、可 env 一键关）；② `SB_DEMO_LOGIN` 未开 ⇒ 端点**等同不存在**（404 ＋ users 表零足迹，不是「返回一个 403」）；③ 会话下发与普通登录**逐字同构**（同一 `createSession` ＋ `sb_sid`，30 天）⇒ 归属逻辑零分支。★★ **幂等是硬要求**：固定 id `u-demo-shared` 惰性建号、并发汇流同一行——退化成「每次新建」＝**静默丢历史**（同 §2.8 的教训）；`password_hash` 用随机 scrypt ⇒ 密码登录对它永不可用。★ `DemoLoginButton.tsx` 里「内容全站共享、访客彼此可见，请勿输入个人信息」警示文案是**契约的一部分、不可删**（共享池方案的代价靠文案兜，不靠架构）。★ 防刷＝进程内 IP 桶 20/时（与发码 IP 桶同档）＋沿用 v39 平台配额。★ 测试：新 `src/routes/auth-demo.test.ts` **9**（2026-09-23 批次 F-1 起 **10**）＋ `Landing.test.tsx` 6→10。⚠️ **本行由 2026-09-22 发版会话补登**（该批交付时 AGENTS.md 目录速查漏行）。**＋ 2026-09-23 批次 F-1（渠道 C5）两份新件挂在本行下**：`packages/shared/src/demo-content.ts`（`DEMO_SEED_TERMS` 八条首屏种子，★ 词条名**与 `web/src/seo/term-corpus.ts` 的 `title` 逐字相同**、由 `term-corpus.test.ts` 锁，天数偏移只由 `REVIEW_INTERVALS_DAYS` 算出、本文件零常数）＋ `packages/server/src/auth/demo-seed.ts`（`ensureDemoSeed()` 挂在 `demoLogin` 上，幂等）。★★ 三条读代码时最容易误判的地方：① **只用真实领域函数写**（`saveOneTerm` ＋ `markReviewed`）——手写 `INSERT` 会少掉领域登记（v19 不变式）与别名索引，而体验号的承诺是「和真账号同一种数据」；② ★ **必须把种子领域纳入复习范围**（`term_domain.review_enabled` 是 `NOT NULL DEFAULT 0`，老板 09-18 拍「默认全不选」）⇒ 不打开则 `listDueQueue` **静默返回 `[]`**（八条全在库、状态全算对，实测踩过），且**打开必须在排进度之前**——`setDomainReviewScope` 的「关→开」边会跑 `resetProgress` 把刚排的进度抹平；③ ★ **种子不是用户产出**：走 `saveOneTerm` ⇒ **不发任何事件**（对比 `saveTerms` 会 publish `term_added`），所以 C4 诚实计数结构上数不到它，不靠「记得排除」。★ 测试：新 `src/demo-content.test.ts` **5** ＋ 新 `src/auth/demo-seed.test.ts` **10**（锁的是**真实读取路径下的样子**：`listDueQueue`／`computeReviewState`／`term_domain`，不是「灌进去了没」） |
| `packages/web/src/lib/svg-utils.ts` | SVG 净化（DOMParser 快路径 + 线性正则回退）+ L1 自愈（补闭合/钳宽/主题色）+ `openSvgDocument` 下载/新标签页（入参必须是**净化后**的 SVG，blob 文档继承本应用源），port from v1。**剥除白名单**：script/foreignObject/iframe/object/embed/**image**（`<image href>` 是外链信标，会让本地应用向外部发请求泄露 IP，2026-09-04 补） |
| `packages/web/src/features/quiz/mix-report.ts` | 配比/配图/联网展示文案纯函数（`mixSummary` 摘要 / `shortfallText` 缺题文案 / `imageNote` 配图提示 / `searchNote` 联网提示 / `refsList` 来源清单），组件只负责挂——判定逻辑留在组件里就测不到。`searchNote(report?)`（2026-09-13，契约 `docs/QUIZ-SEARCH-SPEC.md` §2.7）：没联网→`null`；有命中→报条数+来源；只命中缓存→说明；开了全失败→带真因；开了没搜到→也留一句（ADR-5 不静默）。`refsList(report?)`（**v1.1**，§2.9）：返 `report.refs ?? []`（没联网/无报告/老服务端缺键都返空数组，**前端不发明来源**）；与 `searchNote` **二选一**——有清单时由清单承担告知，`searchNote` 只兜「没取到」 |
| `packages/web/src/features/quiz/CollectPanel.tsx` + `collect-view.ts`（样式并入既有 `quiz.css`） | **题库页「搜集题目」预览确认面板**（契约 `docs/RESOURCE-SPEC.md` §3.5，2026-09-18 v0.2.57 新建，154 + 28 行）：输入主题 → `POST /api/quiz/collect/preview` → 逐题两态列表（通过＝可勾选；拒绝＝整行灰置并**显示逐题真因**如「疑似非原文题目」）、逐页抓取记账（title/url/失败原因）、每题带**来源链接可回源页**；勾选后才 `POST /commit` 入库并刷新题库列表。★★ **两条不能写反的口径**：① **前端不重算锚点判定**——通过/拒绝只信服务端的 `ok`（判定权在 verbatim 锁，前端复刻一份就是双事实源）；② **预览态永不落库**（ADR-5 两段式的存在理由：commit 前关窗＝零副作用）。★ 判定/展示逻辑抽 `collect-view.ts` 纯函数（`collect.test` 之外的 `collect-view.test.ts` 6 例）——本仓 `.tsx` 无 jsdom，留在组件里测不到（先例 `mix-report.ts`/`weak-report.ts`）。★ **渲染层目检挂 MT-01**（`docs/dev/manual-test.md` 首单）：三态齐备与逐题拒绝理由的真机观感尚无自动化测 |
| `packages/web/src/features/quiz/RefList.tsx` + `ref-list.css` | **套题级来源清单**（**v1.1 新建**，契约 `docs/QUIZ-SEARCH-SPEC.md` §2.9）：默认折叠的 `<details>`，标题「联网：本次参考了 N 条资料」，展开为 `[n]` 编号 + 可点击标题（`target="_blank" rel="noreferrer noopener"`）+ 来源名；**空清单返 `null`**（由 `searchNote` 兜底，不重复说）；URL 全部来自服务端映射的真实检索结果，本组件只渲染、不拼链接、不补全。题库页（**列表视图与练习视图两处都挂**）与对话页共用——两页各写一遍必然漂成两种样式 |
| `packages/server/src/routes/terms.ts` | 词条库路由：`/api/terms` CRUD + `POST /extract`（AI 抽取入库）+ 领域口（**2026-09-17 起与词条 CRUD 对等**）——`GET /domains`（统计，**含空领域**）、`POST /domains`（新建，**可零词条**；已存在回 200 且不覆盖 note）、`PUT /domains/:name`（带 `name`＝改名、只带 `note`＝改说明）、`DELETE /domains/:name`（词条**迁 general、一条不删**，回迁移条数）。★ 状态码由域层 `DomainError.status` **直通**（400/404/409，同 `routes/choice.ts` 手法）；★ `:name` 是**领域名本身**（可含中文/空格/`%`），前端 `encodeURIComponent`、服务端**不再二次解码**（Express 已解码，再解一次会让含 `%` 的名字炸） |
| `packages/server/src/learning/domains.ts` | **领域库（领域升为一等实体，迁移 v19，2026-09-17 新建）**：登记册 `term_domain`（`name` 主键 / `note` / 时间戳）的增删改查——`normalizeDomainName`（小写 + 截 30，与 `normalizeTerms`/`tidy.normDomain` **同口径**；不一致会让 `Math` 与 `math` 在库里存成两行）、`createDomain`（幂等、不覆盖既有 note）、`updateDomain`（只动 note）、`renameDomainEntry`、`removeDomain`（迁 general、**拒删 general**）、`domainStats`（**登记册为左表** ⇒ 空领域也出现；末段 `UNION ALL` 兜**孤儿域**，正常恒空集）。★★ **三条容易想反的语义**：① **改名的权威动作是改词条、不是改登记册**——顺序必须「先迁词条（事实）→ 再同步登记册（索引）」，反了会留下「词条指着一个登记册里已不存在的领域」的中间态；② 迁词条**一律复用 `tidy.renameDomain`**，因为目标域已有同名词条时会撞 `UNIQUE(term, domain)`，它内含「先并入再改名」的防御（本仓实测踩过该约束）；③ **不变式 `term_library.domain ⊆ term_domain.name`** 靠**写入侧**维持（`saveTerms`/`saveOneTerm` 落新域时 `INSERT OR IGNORE` 登记）——`extractTerms` 只做「优先复用」的软引导、不做白名单硬拦（硬拦会把没归好类的词条憋回去），模型随时吐新领域名，靠人工同步必然滞后 ⇒ Tab 漏项；`domainStats` 的孤儿兜底是第二道，**宁可在 Tab 上多一格，也不让词条"隐身"**。★ **依赖方向刻意单向（断环）**：`domains.ts → tidy.ts → terms.ts`，而 `terms.ts` **不反向依赖**本文件（其 `extractTerms` 的领域引导自带 SQL 直查 `term_domain`）；否则成 `terms → domains → tidy → terms` 环（先例：`web/src/lib/api-request.ts` 抽出时同样为断环）。★ 红线：本文件 **220** 行——`terms.ts` 381 行加领域统计会顶破 400 ⇒ 统计归位至此，`terms.ts` 回落 386 ★★ **M2d-2（v31）**：全部读写带 `owner_id`（`ownerForWrite` 口径）——`term_domain` 由 `name` 单列 PK 改 `PK(owner_id,name)` 后不再全局唯一，不带归属的 `UPDATE ... WHERE name = ?` 会一次改掉**所有人**的同名领域；`general` 改**每用户懒建 + 读路径补建**（`ensureDefaultDomain`）；`domainStats` 孤儿域兜底与 `renameDomainEntry` 并入判据都**按用户各自成立**。 |
| `packages/server/src/learning/mention.ts` | **词条提及流水（2026-09-18 v0.2.49 新建，契约 `docs/MEMORY-TREND-SPEC.md` §1，迁移 v26 `term_mention_log`）**：`recordMentions(rows, ownerId?, now?)` 落流水——★ **本函数不开事务**：调用方 `countUsage` 已把「UPDATE 计数」与「INSERT 流水」包在同一事务里（嵌套 `transaction()` 会抛 `cannot start a transaction within a transaction`）；`mentionTrend(days?, ownerId?, now?)` 按窗口聚合（`labels` 与 `values` 等长且**含今天**、无提及的日子补 0——缺一格会让折线整体左移一天）；`shortDayLabel(key)` 出横轴标签。★ **`domain` 存快照而非外键**：词条可改领域、可被删除，JOIN 取「当下领域」会让历史趋势整体漂移。★ `ownerId` 默认 `null` ＝ 未登录单人本地模式（不过滤） ★★ **M2d-2（v31）**：`ownerId` 由可选改**必填**，且聚合读一律 `ownerForWrite`（原 `ownerFilter` 的「null 就不加条件」会让未登录请求**跨用户求和**——正是契约 §8.2 点名的那条）。`term_mention_log` 的归属列口径从 `NULL` 对齐到**空串**（迁移 v31）。 |
| `packages/server/src/learning/term-recall.ts` | **词条检索（2026-09-18 v0.2.49 新建，从 `terms.ts` 拆出）**：`getRelevantTerms(query, limit)` 按关键词重叠 + 重要度 + 近期使用打分取 Top-K，供对话注入。★ **拆的判据**是「两块东西的修改频率或增长方向不同 → 拆」：检索**只读**（随打分策略变）与入库**写库**（随 AI 抽取协议变）几乎不会同时改，却一直挤同一份行数预算。**触发**：`terms.ts` 接上提及流水后涨到 **409 行**触「server .ts ≤400」红线。★ `terms.ts` 对它做 **re-export**，故 `chat/context-segments.ts` 与两处 `vi.mock("../learning/terms.js")` 零改动（改路径会同时打穿三处 mock） ★★ **M2d-2（v31）**：`getRelevantTerms(query, ownerId, limit)` 加**必填** `ownerId`（漏传 ⇒ 检索到别人的词条）。 |
| `packages/server/src/learning/term-extract.ts` | **词条抽取层（2026-09-18 v0.2.56 新建，从 `terms.ts` 拆出）**：`TERMS_PROTOCOL` 提示词 + `parseTermsBlock`（协议解析容错）+ `normalizeTerms`（入库闸门：字段校验与 importance 钳位）+ `extractTerms`（调模型抽取）+ `TermItem` 类型。★ **拆的判据同上一行**：**抽取随"AI 抽什么"变**（提示词、协议形状）而**入库随"怎么存怎么查"变**（SQL、领域登记、复习范围），两块几乎不会同时改，却一直挤同一份行数预算。**触发**：`terms.ts` 加复习范围列后涨到 **401 行**触「server .ts ≤400」红线。★ `terms.ts` 对它做 **re-export**（`export { TERMS_PROTOCOL, parseTermsBlock, normalizeTerms, extractTerms }` + `export type { TermItem }`）⇒ 调用方零改动，**两处 `vi.mock("../learning/terms.js")` 不会被打穿**。★ `TERMS_PROTOCOL` 由 `const` 改 `export const`——契约测试与将来换抽取角色时要复用 ★★ **M2d-2（v31）**：`extractTerms(..., ownerId)` 必填并穿透到 `saveTerms`。 |
| `packages/server/src/learning/terms.ts` + `term-usage.ts` + `tidy.ts` + `tidy-plan.ts` + `term-review.ts` + `term-review-scope.ts` | **词条库归主（M2d-2，2026-09-18 v31，契约 `docs/TENANCY-SPEC.md` §8.2）的六个落点**：核心 CRUD 在 `terms.ts`（357 行）；使用计数与提及流水拆到 `term-usage.ts`（`countUsage`，由「一轮回复完成」驱动，触发点与入库几乎不重叠）；整理计划解析拆到 `tidy-plan.ts`（`parseTidyBlock`/`normalizeTidyPlan`/`renameDomainTx`），应用侧留 `tidy.ts`；复习**范围**（开关/清零/打卡）拆到 `term-review-scope.ts`，**读**（概览/队列/连续天数）留 `term-review.ts`。★ 三处拆分都是被「加归属后触 server .ts ≤400 红线」逼出来的，照仓规**拆文件、不压注释**；`terms.ts` 对前两者做 **re-export** 保住既有导入面（多处 `vi.mock("../learning/terms.js")` 指着它）。★ `terms.ts` 的 `ownerId` 是**必填参数**（不给默认值——漏传的表现是**静默串台/静默丢写**，两种都不报错）。★ `term_review_log` **不加 owner 列**：它是流水历史事实，归属由 `term_id → term_library` 的连接带出来（加列会引入"流水说 A、词条说 B"的分叉）。 |
| `packages/server/src/pk/` + `packages/server/src/routes/pk.ts` | **PK 域（AI 出题微信双人对战，契约 `docs/PK-SPEC.md` v0.5.0）——P0-1 已完成**（2026-09-12）。① 登录：`pk_users` 表（v10 迁移；`openid` 是 `UNIQUE` 且**按真 openid 设计**，P0 只写模拟值 `mock_<userId>`）+ `pk/auth.ts` + `POST /auth/login`（非法昵称 400 且一字不落库）、`GET /auth/me`（未知账号 404），提交 `e02d67a`。② 房间：`pk/room.ts` **全内存状态机**（无落库、无 schema 改动，进程重启即丢局）——`createRoom`（幂等：重复建房返回原房，不制造第二间）／`joinRoom`（满员 409、重复入房幂等、**校验全过才摘旧房**，否则失败会把人踢出自己原来的房）／`startRoom`（仅 `players[0]` 房主可开、双方已进房才 active、`endsAt = now + PK_MATCH_MS`）／`sweepExpired`（**惰性** TTL 回收，不起定时器：waiting 30min／finished 10min／active 取「对局时钟 + 保留期」，P0-1 **不结算**、不擅自写 winner）＋端点 `POST /rooms`、`POST /rooms/join`、`POST /rooms/:id/start`、`GET /rooms/:id/state`、`GET /stream`。③ **身份闸门**：userId 必须命中库里账号（401），房内昵称从库取、**不信客户端自报**；**域错误码 → HTTP 状态与文案的映射只此一处**（`ERROR_STATUS`/`ERROR_TEXT`），域层不碰 HTTP。**P0-2 已落地（2026-09-13）**：`pk/match.ts` 计分引擎（§1 全表 +1/+2/−1/−1/−1；**1s ticker 统一时间驱动**——答题超时/怠慢/对局时钟结算/AI 出题 CD 全收口 `tickMatches` 一处，测试用假时钟直调不起真定时器）＋ `pk/ai-bot.ts` PVE 对手（AI 出题/答题与人**同一条计分路径**，`PK-SPEC` §8）＋ 计分两端点 `/rooms/:id/quiz`（**先落 CD 再生成，失败回滚 CD＝免费重试**）、`/rooms/:id/answer`（即时判分）。**PK 出题已接联网**（2026-09-13）：`match.ts`/`ai-bot.ts` 调 `generateQuiz(..., online=true)` **硬编码联网**，AI 出题与人对局同口径（契约 `docs/QUIZ-SEARCH-SPEC.md` §2.2；不传 report＝不面向用户就不记账）。P0-3 已落 a 批（见下行）。P1 换真微信网页授权只改 openid 写入方，`PkIdentity` 与前端零改动。**P0-4／P0-5／P0-6 契约已立、码未开**（2026-09-13 二次 grill，本文升 v0.3.0）：① **公共题**（一题双方都答；答对自己出的题 +1、答对手的题 +2、成功出题 +1、三条 −1 惩罚保留）；② **理解题**（裁判 AI 每 `COMP_INTERVAL_MS`=3 分钟按本局主题出 1 道，0~3 四档，计入同一总分）；③ **裁判 AI**（出题／判分给理由／盘中短评，短评只读已有状态不调模型）。★★ **本域最大的架构约束（决策⑫，改码前必读）**：`chat/sse-bus.ts` **只有整频道广播、无按订阅者定向能力**，而 PK 双方订阅同一个 `pk:<roomId>` ⇒ **凡进 SSE 的字段双方都看得到** ⇒ **私有判定结果只走 REST 响应体，SSE 只播公开事实**（契约 §1.2）。★ **P0-4 是破坏性改造不是加法**（`PkQuestion` 去 `toUserId`/`chosen`/`status` 换成 `attempts[]`），受影响 10 处落点与 3 个测试文件 38 例清单见契约 **§10**。**P0-7 已落地（2026-09-13 深夜，契约升 v0.4.0，细则 **§11**）**：① **主题轮转**：`PkPlayer.topic` + `PkRoomState` 的 `currentTopic`/`topicOwnerId`/`topicTurn`，两人各选一主题**按轮交替**，成功出题才切轮（失败不让轮）；选主题只在 waiting 期（`POST /rooms/:id/topic`），**双方都选完才能开局**（否则 409 `TOPIC_NOT_SET`），PVE 的 AI 座位兜底 `PK_DEFAULT_AI_TOPIC`。② **跑题校验**：出题时提示词塞「主题：X」约束 + 生成后由裁判 `judgeTopicFit` 判贴合度（新文件 `pk/judge.ts`，**失败返 null 绝不抛**，ADR-4）；不贴合 → 422 `TOPIC_MISMATCH` **不入题不占 CD**、只累计 `failStreak`，**满 3 次才 −1** 并随错误回传裁判建议（`extra.advice`）。③ **求助道具**：`PkPlayer.helpLeft`（每局 1 个），`POST /rooms/:id/help` 走裁判**当场联网**给 `advice/knowledge/refs`，**★ 硬规则：绝不含本题答案**（该规则写在提示词里检索素材之前）；用尽 409、裁判挂了 502 **且不扣次数**。④ **二次机会**：新文件 `pk/power.ts`——`POST /rooms/:id/retry` 只能挑**自己的错题**，给现场解析 + 同主题相似题（题上带 `isRetry`/`retryOf`，`fromUserId=JUDGE_USER_ID` 故不给任何人 +1），答对 **+2 且原 −1 保留**（净 +1），**3 分钟 CD 按玩家记**（`retryNextAt`），CD 在解析给出后即落（防刷解析）。⑤ `llm/router.ts` 的 `MODEL_ROLES` 新增 **`judge` 角色**（**无兜底默认模型**：没配就不配，不拿别的角色的模型顶上）；⑥ 常量 `TOPIC_MAX`/`QUIZ_FAIL_STRIKE`/`RETRY_CD_MS`/`HELP_PER_MATCH`/`JUDGE_USER_ID` 等登记在 `shared/src/pk.ts`，**`PK_QUIZ_MIX` 从 `match.ts` 下沉到 shared**（`judge.ts` 要用，留原地会与 `match.ts` 成环）。⚠️ **P0-4 公共题落地时必须回头改 `requestRetry`**（相似题现在是**单归属**，与 `attempts[]` 不兼容），待改点清单见契约 **§11.7**。**P0-8 ✅ 已落地（2026-09-14，契约 v0.5.0 细则 §12）**：① **投降** `POST /rooms/:id/forfeit`——认输即**对手胜、比分定格**（不额外扣分、不退回分），与时钟归零结算**共用 `finishRoom`** 一条收尾路径（新 `pk/settle.ts`：置 finished→定胜者→记 endReason→广播 `pk-end`→落历史）；`PkRoomState.endReason` **只在非时间到时写**（`timeup` 是老字段语义，不重复标注）；**服务端不加二次确认门**（幂等状态转换，重复调用撞 409——防误触是交互层的活）。② **对战历史**：**DB v15** 建 `pk_matches`（**P0 里第一处让对局落库**，**部分推翻决策记录⑤**——范围卡死：**房间是「过程」仍全内存，历史是「结果」才落库**）+ 新 **`pk/history.ts`**（**视角行**：PVP 一局两行、各存一份快照，查询走 `idx_pk_matches_user`；**只给真人写行**、AI 座位不写；幂等靠 `UNIQUE(room_id,user_id)`+`INSERT OR IGNORE`；**昵称存快照**——对手改名后仍显示当时对战名；**落库失败不阻断收尾**但必留痕 = ADR-4/ADR-5）+ `GET /matches`（列表，`limit` 归一 20／上限 100）与 `GET /matches/:id`（详情含末快照供题目回看；**「不存在」与「不是你的」故意合成同一个 404 码 `MATCH_NOT_FOUND`**，否则等于把「这个 id 存在」告诉没权限的人）。★ **常量硬约束（有不变式用例锁）**：`PK_HISTORY_KEEP ≥ PK_HISTORY_MAX`——保留量小于单页上限，后 100 条就永远拉不出来。★ **端点顺序坑**：`/matches/:id` 的 `:id` 会吞掉 `/stream`，故历史端点排在 `/stream` 之前。**★ B1 身份并入统一账号（2026-09-20，契约 PK-SPEC §14）**：`pk/auth.ts` 重写为 `toPkIdentity`/`pkIdentityOf` 薄适配（**会话优先 → local 兜底 `PK_LOCAL_IDENTITY` → 401**）；14 个端点全去 `{ userId }` 自证入参；`/auth/login` 删除、`/auth/me` 404→401；`pk_users` 保留（存量战绩指向它，老战绩归档不认领）；routes/pk.ts 377 行、pk/auth.ts 46 行、pk/match.ts 400 行。**★ B2 判断题（2026-09-20，PK-SPEC §15）**：`match.ts` 的 `submitQuiz` 加 `qKind` 尾参（默认 single），配比经 shared `pkQuizMixFor(kind)` 现算；judge 是 single 特例（options 恒 `[正确,错误]`），判分链路零特判。**★ B3 词条出题（2026-09-20，§15.6）**：新 `pk/quiz-terms.ts`（`resolvePkTerms` 校验＋`pkTermConstraint` 拼接，**不 import match 防循环依赖**）；`learning/terms.ts` 加 `listTermsByIds`（owner 隔离进 WHERE）；`submitQuiz` 加 `termIds` 尾参，**校验在 CD 落之前**（输入错误不吃冷却）；硬约束进提示词、释义走 material（PK 链路首次用 generateQuiz 第 2 参）；错误码 `TERM_NOT_FOUND`(404)／`TERM_LIMIT_EXCEEDED`(400)；match.ts 399 行 |
| `packages/server/src/pk/snapshot.ts` + `pk/scenario.ts` + `routes/pk-scenario.ts` | **PK 情景题（B4，契约 `docs/PK-SPEC.md` §15.4，2026-09-20）**。★ `snapshot.ts`（83 行）：把原先散在 `match.ts`（`snapshotQuestion`）与 `room.ts`（`snapshotRoom`）的两处快照映射**合并成唯一映射点**——「criteria 永不出服务端」由每处记得裁剪的纪律，升级成只此一处、白名单式放行的结构保证（`scenarioInternal`→`scenario` 只挑 demoId/title/tasks；情景题不映射 `answer`/`answerRevealed`/`chosen`）；`room.ts` 保留 `export { snapshotRoom }` re-export，旧导入面零改动。★ `scenario.ts`（202 行）：`generatePkScenario`（复用学习引擎 `streamScenarioDraft`，**不入 quiz_bank**——criteria+demo 只住房内存 `scenarioInternal`，房 TTL 即焚，与学习侧 `saveScenario` 落库路径刻意分叉）＋ `reportScenarioTask`（taskId 白名单防伪造评分点 → latest-wins → 全中提前结算）＋ `settleScenario`/`settleScenarioDue`（计分 delta/correct/answered 与既有题种同列，零新档）＋ `pkScenarioPage`（成员校验＋注入 `SCENARIO_BRIDGE_JS`）。★ `routes/pk-scenario.ts`（51 行）：`POST /rooms/:id/scenario-report` ＋ `GET /rooms/:id/scenario/:demoId`（CSP `sandbox allow-scripts allow-modals allow-forms` + `X-Frame-Options: SAMEORIGIN`）；ERROR/fail/unauthorized 从 `routes/pk.ts` 复用不另立。★ 三机制契约锁死：超时＝整页结算 −1；retry 排除情景题（`RETRY_NO_TARGET`）；AI 席位不玩 demo（超时 −1，无特权）。 |
| `packages/web/src/features/pk/`（`PkApp`/`PkLobby`/`PkRoom`/`pk-view.ts` + `pk.css`） | **PK 前端 P0-3a**（契约 `docs/PK-SPEC.md` §5，2026-09-12）：顶层 `#/pk` **零依赖 hash 路由**（`main.tsx` 按 hash 二选一渲染，不引 react-router，`@sb/web` 保持零运行时依赖）。大厅（昵称登录→建房→输码入房，建房幂等）→ 等待房（大房号＋双方座位＋房主开局）→ 对局视图（**SSE `pk-state` 活水**：实时比分＋对局时钟，时钟钳 0、只作展示）。**SSE 连续 3 次重连失败降级 2s 轮询**（契约 §2.2，Android 微信 X5 内核兜底），轮询 404＝房被 TTL 回收，自动回大厅并如实告知。★ 判定逻辑全在 `pk-view.ts` 纯函数（10 例回归锁），组件只挂 UI（先例 `doc-name.ts`）。★ `sse-client.ts` 已泛化（`connectSse(streamUrl)`），聊天与 PK 同一套重连门控。**未做（不放假按钮）**：出题/答题动作区与结算页属 P0-2 及其后；刷新后丢 roomId 回大厅（无「按 userId 查房」端点，建房幂等可找回原房）。**2026-09-16 响应式断点重构**：废弃 `@media (orientation: landscape)` 判据——**设备方向 ≠ 内容需要多宽**，它同时错判 iPad 竖屏 768（限死 480 白扔 288px）/ 手机横屏 667（强上三栏挤爆）/ 桌面窄窗三种情形；改**屏宽三档 + 屏高补充档**（移动 `<720`／平板 `720~1023`／桌面 `≥1024`／**矮屏横放** `600~1023 宽且 ≤520 高`——手机横屏的病根是屏矮不是屏宽，故判据换屏高、单独立档）。比例全收进 `.sb-pk` 上的 CSS 变量（`--sb-pk-max`／`--sb-pk-content-max`／`--sb-pk-cols`／`--sb-pk-score`），四个断点块**只覆盖变量、规则本身只写一次**；选项容器 `flex`→`grid` 以支持桌面两列。**改比例＝改一个数**。核验走新探针 `tools/probes/pk-breakpoint-cdp.mjs`（9 档视口全绿）。详见 `CHANGELOG.md` 2026-09-16 10:05 行。**★ B1（2026-09-20，§14.2/§14.3）**：`usePkIdentity` 改问服务端（删 localStorage 自证，`lib/auth.ts` 整文件删除）；大厅未登录换「去登录」引导；**邀请链接** `#/pk?code=`（hash 片段解析 + `buildPkInviteLink`/`pkInviteCodeFromHash`/`sanitizeReturnTo` 三个纯函数在 `pk-view.ts`）＋等待期「复制邀请链接」＋ returnTo（sessionStorage，登录后由 `main.tsx` 跳回）；`main.tsx` 的 `isPkHash` 补认 `#/pk?`（否则邀请链接落不进 PK 页）。**★ B3（2026-09-20，§15.6）**：出题区词条选择器＝新 `PkTermPicker.tsx`＋`PkTermPickerOverlay.tsx`（chips＋弹层；`PkTermOption {id,term,definition}` 最小视图形状——api 与 shared 两套 TermItem 类型分叉，展示层只认三字段）；`PkMatch` 挂载拉一次 `api.terms.list()`（失败=空=选择器不可用，不轮询）；`api.pk.submitQuiz` 空 termIds **不带字段**（请求体与 B3 前逐字一致）；PkMatch.tsx 298 行 |
| `packages/server/src/auth/`（`password`/`session`/`users`/`rate-limit`/`middleware`）+ `packages/server/src/routes/auth.ts` | **账号体系 MVP M1（2026-09-17 新建，契约 `docs/AUTH-SPEC.md` v1.0，迁移 v21）**——老板点单「studentbuddyv2 我准备上线了，把他做成那种 web 网页然后有用户以及微信登录的账号」。★ **先定资质再动代码**（多源核实）：微信网页登录需「开放平台·网站应用」＝**企业主体 + 认证 + 已备案域名，个人明确不可申请**；**短信签名同样卡企业**（工信部实名制报备）⇒ **个人主体唯一零资质路径是「邮箱+密码」**，故 M1 只做它，微信/短信留企业资质就绪后增强（且必须映射到**同一个 user**，不新建账号）。★ **五个文件各管一件事**：`password.ts` 用 Node 内置 `crypto.scrypt` 派生（**零新依赖**，不引 bcrypt/argon2 ⇒ 免原生编译、免供应链面），哈希**自描述**为 `scrypt$N$r$p$salt$hash`（**参数随哈希落库** ⇒ 将来把 N 从 2^14 提到 2^15 **不必洗库**），**脏哈希一律回 `false` 且绝不抛**（一个脏行不该把登录端点打成 500）；`session.ts` 签发 32 字节随机 token、**库里只存 `SHA-256(token)` 十六进制**（明文只存在于客户端 cookie 与服务端那一次返回 ⇒ **拖库也拿不到可用会话**），过期即**惰性删行**（省一个定时器），`last_seen_at` 只是**观察字段、不参与过期判定**（**刻意不做滑动过期**——那会让会话永不过期）；`users.ts` 建号/查号/鉴权（`UNIQUE(email)` 是库层兜底，约束错误翻译回域码 `EMAIL_TAKEN`）；`rate-limit.ts` 进程内失败计数（**连续 5 次 → 429**、成功即清零：防撞库，又不把正常用户攒着锁死）；`middleware.ts` 解析 cookie 与 `Authorization: Bearer` 注入 `req.user`。★ **路由四端点**：`POST /register`（建号**并直接下发会话 cookie**，`HttpOnly`；响应体**绝不含 `password_hash`/`scrypt$`**——不给「不小心下发凭据」留口子）、`POST /login`（**「邮箱不存在」与「密码错」回同一个 `CREDENTIALS_INVALID`**：分开等于告诉攻击者该邮箱是否已注册）、`POST /logout`（**幂等**，重复登出不当错误）、`GET /me`（未登录 401）。★ **鉴权默认关**：`SB_REQUIRE_AUTH=1` 才强制——**多租户数据隔离（M2）未做时强开会让全站既有接口一律 401**，属「把房子拆了再装修」。★★ **本批一条真 bug（测试当场逮到）**：`register` 与 `/me` 返回的 `createdAt` **格式不同**（内存 `toISOString()` vs 库 `datetime('now')`）——`createUser` 拿**内存对象**拼返回值 ⇒ 同一契约字段有了**两个事实源**；修法＝**建号后回读入库行再映射**（**库行是唯一事实源**），回归锁＝「注册拿到的 cookie 直接查 `/me` 且与 register 响应体**逐字段相等**」。⚠️ **M2a（2026-09-17 同日）已把会话域隔离做完**（契约 `docs/TENANCY-SPEC.md`，迁移 v22，`auth/ownership.ts`）：`sessions.user_id` + 会话/聊天域全端点归属断言 + `attachUser` 无条件挂载。但仍**不能上线**——`providers`（含 api_key 密文）／`quiz_bank`／`term_library`／`daily_activity`／`token_usage` 等**其余业务表仍无归属**（见 `test-plan.md` §6 挂账 M2c/M2d；`user_memory` 已于 **M2b v0.2.47** 收口），且生产必须开 `SB_REQUIRE_AUTH=1`。★ **真机冒烟入口**：`_probe/auth-smoke.mjs`（35 项断言）——起隔离实例 `SB_PORT=18799` + `SB_DATA_DIR` 指向仓内 `_probe/data-auth-smoke`（该目录已 gitignore；`resolveDataDir()` 里 `SB_DATA_DIR` 命中即 `return`，**不会碰生产库**），跑 `node _probe/auth-smoke.mjs`；**改 auth 后先跑它**。⚠️ 两个已踩过的环境坑：**Windows node 不认 MSYS 路径**（`/tmp/x` → `C:\tmp\x`，必须给绝对 Windows 路径）、**`kill npx` 不带走子进程**（残留实例会让「启动成功」造假象，故按端口反查 PID 杀、用「自有库文件出现」当就绪判据） |
| `packages/server/src/auth/codes.ts` + `auth/code-limit.ts` + `auth/code-flow.ts` + `packages/server/src/routes/auth.ts` 的 `send-code`/`login-by-code` | **邮箱验证码登录（M1.5，2026-09-18 新建，契约 `docs/AUTH-SPEC.md` v0.3.0 §1/§2.5/§4.5，迁移 v27）**——老板点单「不做微信登录了，搞完整的邮箱验证码登录」。★ **三个文件按「谁最容易改错」切**：`codes.ts` 只管表与 crypto（签发/核销/原子认领）、`code-limit.ts` 只管三道发送限流（**纯内存零 IO**）、`code-flow.ts` 管用例编排（限流准入 → §2.5 策略 → 发信）。★★ **两条最要紧的设计**：① **码只以明文存在一瞬间**——`issueCode` 是唯一出口（返回给调用方去发信），库里此后只有 `SHA-256`；⚠️ 但**哈希对在线爆破零作用**（6 位数字只有 10^6 空间，攻击者不需要还原哈希，猜一个数走一次校验即可）⇒ **真正的防线是 `attempts` 上限 + 发送侧限流**，哈希只是"拖库后不能直接拿来登录"的第二道；② **作废与消费都是「原子认领」**——`UPDATE … SET consumed_at = ? WHERE id = ? AND consumed_at IS NULL` 并以 `changes` 为唯一判据（先 SELECT 再 UPDATE 会在并发下让**同一个码被用两次**，而它是一次性的，双花就是双登）；该语句单独导出为 `claimCode` **就是为了能被直测**（内联在 `consumeCode` 里时那条分支在单进程同步流程中**永远走不到**，删掉 WHERE 条件不会有任何测试报红）。★★ **`send-code` 的响应与「邮箱是否已注册」按 purpose 分两套、且两个方向都是刻意的**：`login` 注册与否**一律回同一个 200**（分开就等于对外提供"这个邮箱注册了吗"的查询接口）；`register` 已注册**刻意回 409**（用户填错邮箱要当场知道，且该信息本就能从 `register` 的 409 拿到）。判据是「**这个信息会不会让攻击者拿到他本来拿不到的东西**」。★ 这张表抽成纯函数 `decideSend`，且与「本批接线范围 `isPurposeWired`」**刻意分成两件事**——混在一个函数里，`register`/`reset` 两格就永远走不到、成为改错了没人报红的死分支。★★ **本批只有 `login` 接线**（`register`/`reset` 的消费端点未做 ⇒ 400），第二层理由更硬：`register` 态要给**未注册邮箱**发信 ⇒ **任何人都能拿我们的发信通道给任意陌生邮箱发邮件**（垃圾邮件/钓鱼跳板、也是让发信域名被拉黑最快的方式），在完整注册链路落地前**不开这个口子**。★★ **实现时被测试逮到一个真错**（见 `test-plan.md` §4）：把「60 秒最小间隔」错用到 **IP 桶**上 ⇒ 同一出口 IP 的所有用户合计每分钟只能发 1 封（共享出口与**反代未配 `trust proxy` 时的全站单 IP** 都会被静默掐死，症状是"点了发送没反应"而服务端零错误日志）；修法是两个桶的判据分开写（`emailBlockedUntil` / `ipBlockedUntil`，后者**只判封数**）。★ 另两条**已知旁路，刻意不修**（写在 `code-flow.ts` 头注）：`login` 态**响应时间可区分注册与否**（该信息本就能从 `register` 的 409 拿到；要修得牺牲"发信失败能如实告知用户"）；**知道受害者邮箱的人可替他刷满每小时配额**（"按邮箱限流"的内生代价，而**密码登录不受影响**——这正是双通道并存不可退让的实例）。★ 新增错误码 `PURPOSE_INVALID`/`CODE_RATE_LIMITED`/`CODE_INVALID`/`CODE_EXPIRED`/**`MAIL_SEND_FAILED`(502)**；`CODE_EXPIRED` 与 `CODE_INVALID` **刻意分开**（用户动作不同：重发 vs 重输） |
| `packages/server/src/auth/code-limit.ts` + `auth/code-flow.ts` + `packages/server/src/routes/auth.ts`（`register` 用途）+ `packages/shared/src/auth.ts`（`AUTH_CODE_MAX_PER_IP_REGISTER_HOUR`）+ `packages/web/src/lib/api-auth.ts` + `packages/web/src/components/AccountTrigger.tsx` | **M1.6 注册即验证（2026-09-18 新建段，契约 `docs/AUTH-SPEC.md` §2.7，零迁移）**——把「注册必须验证邮箱」从契约落成**不可绕过**的代码。★★ **旧的无码注册路径是直接删掉的，不是留作"兼容入口"**：留着它就是**绕过邮箱验证的后门**，而 M1.6 的全部意义就是"没验证过的邮箱建不了号"（回归锁＝`routes/auth.test.ts` 的「不传 `code` → 400 且 `users` 一行不落」）。★★ **`registerByCode` 的三步顺序不可换**：**纯校验（免费、可逆）→ `consumeCode`（不可逆、一次性）→ `createUser`（复用既有两层查重）**——反过来先核销再校验，会让「弱口令被拒」白烧掉用户一个码；`createUser` 放最后是因为库层 `UNIQUE(email)` 是**最终兜底**，撞了必须翻译成 409 `EMAIL_TAKEN`、**不能漏成 500**。★★ **本批修掉一个真实产品缺陷**：「60 秒最小间隔」与「每小时封数」原先**从同一个数组推导**（`emailBlockedUntil(sends, now)`）⇒ 间隔的作用域被静默放大到「**跨用途**」。M1.5 时只有 `login` 一个已接线用途、该耦合**不可观测**；本批接上 `register` 后第一次变成真实行为，症状是「**拿已注册邮箱点注册 → 409（填错邮箱，当场告知）→ 切到登录页点发送验证码 → 429，等 60 秒**」——摩擦恰好落在用户刚受挫的那一刻。★ 修法＝拆成三个**具名函数、作用域两两不同**（判据是「这道闸门到底在挡什么」）：**最小间隔**按 `用途:邮箱`（挡同一个动作连点）／**每小时封数**按邮箱、**跨用途共用**（挡把某个邮箱炸了，与用途无关）／**每小时 IP 封数**按 `用途:IP`（`register` 是唯一给**未注册**地址发信的用途）。⚠️ **这不是放开防轰炸**：每小时封数仍跨用途共用（5 封/小时/邮箱）——若按用途拆开，对单个邮箱的骚扰上限会从 5 悄悄变成 15。★★ **这是同一缺陷类的第 2 次**：第 1 次（09-18 M1.5）把**邮箱间隔**错用到 **IP 桶**，第 2 次（本批）把**按动作的间隔**错当成**按邮箱的间隔**；共同特征是「**写错了不会有任何运行时错误，也不会有任何既有测试报红**」，只在真实用户路径上表现为"等 60 秒"或"校园网里所有人登不进来"。已按 §0.15 记**收敛计数 2/3**——若出现第 3 次，改为「新增闸门必须**先写一行作用域声明**（`{ 键维度, 阈值, 挡什么 }`）再由该声明生成实现」，让作用域从注释升级为数据结构（详见 `test-plan.md` §4）。★ **`register` 的 IP 桶单独收紧到 5/小时**（`AUTH_CODE_MAX_PER_IP_REGISTER_HOUR`），三条理由：① 它是**唯一**给未注册地址发信的用途 ⇒ 垃圾邮件/钓鱼的现成跳板；② 与 `login` 共用 20 会让同出口 IP 上所有人的**登录**码被注册流量挤掉；③ 真正的天花板是 Resend 的 100 封/天 ⇒ 要防的是**额度 DoS** 不是批量骚扰，故本批**不加 CAPTCHA**（记为后备）。⚠️ **诚实成本（写在该常量 docblock 里，不藏着）**：5 封/小时是**按出口 IP** 计的，而校园网/宿舍网/运营商 NAT 下成百上千人共用一个出口 IP ⇒ **同一间教室 20 个人一起注册，第 6 个人开始收 429**——而本项目的主要用户群体恰恰在校园网里；两条缓解（429 带 `retryAfterMs`、冷却期提示查垃圾箱）与两个升级触发条件也已写在该 docblock。★ **同批改了 5 个测试文件的注册夹具**：`code` 变必填后，全仓有 5 个文件拿 `POST /api/auth/register` 当"铺一个登录态"的夹具，而它们**走 HTTP ⇒ typecheck 完全看不见**（`tsc` 零报错，只在 vitest 运行时红）。处置是**夹具降级而非穿透新参数**——`routes/auth-code.test.ts`／`tenancy.test.ts`／`coach.test.ts` 三处改为**直接落库**（`createUser` + `createSession`；会话 cookie 的值就是裸 token，见 `middleware.ts`）。三条理由：① 省掉发信打桩；② **不吃注册用途的 5/小时名额**（测试全走同一出口 IP，借道注册会让「注册阈值一改，一堆无关文件跟着红」）；③ 更快。**前提**是注册端点本身已由 `routes/auth.test.ts` 端到端覆盖。★ **前端两处抽取（都是行数门禁逼出来的，非主动重构）**：`api.ts` 加完 auth 组即 **413/400** ⇒ 照仓里 `studyFlowApi`/`api-terms-*` 既有惯例抽出 `lib/api-auth.ts`（`api.ts` 回落 **382**），以 `auth: authApi` 挂引用 ⇒ **调用方零改动**，且把 M1.6 的破坏性变更（`register` 多一个 `code`）**集中在一处**；`AccountBox.tsx` 加完注册态即 **309/300** ⇒ 抽出 `components/AccountTrigger.tsx`（**纯展示层**：头像 + 收起态的名称/邮箱与"账号"标签，`AccountBox` 回落 **291**，auth 逻辑一行未动）。⚠️ **渲染层无自动化测**（本仓无 jsdom）：三条通道（登录/注册切换、验证码行、切换链接）与 `AccountTrigger` 的收起态**须真机目检**，已挂 `test-plan.md` §6。★ **另补一处非本批代码**：`routes/term-review.test.ts` 的 `scope` helper 把 `enabled` 改**可选**——该 helper 的职责之一就是发"故意缺字段"的请求体，写成必填会让「不传 `enabled` → 400」那条用例**编译不过**，而它正是要测的东西；本仓 `tsc -p packages/server` 由此**首次归零**。★ **v0.2.59（2026-09-18）补掉一条「只有真机才看得见」的漂移**：本行上方把「429 带 `retryAfterMs`」列为**校园网误伤的头号缓解措施**，但该字段**从未到达过响应体**——`admitSend` 一直在算、`code-limit.test.ts` 一直在断言，而 `sendCode` 只把 `admission.ok === false` 翻成一个**裸错误码**（域层约定「`message` 只放 `AuthError` 码」⇒ 结构化数字在"抛码"这条路径上无处可放）。**所有单测都是绿的**：`code-limit` 测"算得对"、`routes/auth-code` 测状态码与 `code`，中间那段**翻译**恰好落在所有断言的缝里 ⇒ 是 `_probe/auth-smoke.mjs` 真机跑到第 11 节揪出来的。修法＝`auth/code-flow.ts` 新增 `CodeRateLimitedError`（域层**唯一**带 payload 的错误类）＋ `routes/auth.ts` 单列 `failRateLimited`（**不给 `fail` 加可选参数**），契约 §2.5／§4.5 同批补响应形状；回归锁加在既有用例上（两条 429 断言带字段、一条 400 反向断言不带），**真机 PASS=62 FAIL=0**。★ 同批把该冒烟脚本 `git add -f` 纳入版本控制——此前本表把它当"真机冒烟入口"引用，而它**根本没被跟踪**，是一处悬空指针。⚠️ 前端 `AccountBox.tsx` **尚未读**这个字段（值到了浏览器、只是没人用），挂 `test-plan.md` §6 |
| `packages/server/src/mail/` | **发信通道（M1.5，2026-09-18 新建，契约 `docs/AUTH-SPEC.md` §4.6）**——本仓**第一次引入第三方服务**（Resend），故做成两层：`MailSender` 接口 + 两个实现（Resend / 控制台兜底）。★ 接口存在的理由很实在：**单测与本地开发不该依赖外部服务**，否则"跑个用例要联网、要额度、还可能给真人发信"。★★ **为什么只能用海外服务商**：服务器在海外不备案 ⇒ 国内发信厂商（阿里云 ¥2.00/千封、腾讯云 ¥1.90/千封）**全部要求域名备案、出局**；代价是**送达率**——国内厂商的看家本领恰恰是对 QQ / 163 做白名单优化，而目标用户是国内学生、QQ 邮箱占绝大多数 ⇒ **进不进收件箱是本批最大的未验风险**（缓解五条：独立发信子域 / SPF+DKIM+DMARC / 三档实测 / 文案避特征 / 密码兜底）。★ **邮件拼装的三条送达率写法**：正文**不含任何链接**（正文出现 URL 是最典型的垃圾邮件特征，而验证码信本不需要链接）、给**纯文本**不用 HTML、码**单独成行且在开头**；主题**不带验证码**（主题会出现在锁屏预览里，泄露面更大）。★ **超时必须显式设**（`AbortSignal.timeout(15s)`）——`fetch` 没有默认超时，上游 hang 住会**无限静默等待**（本仓在 LLM 上游上为这条付过学费）。★ **非 2xx 必须抛**且只记状态码**不记响应体**（出错时它可能回显请求内容，而日志比库更容易被翻）。⚠️ **兜底通道只在没配 key 时生效并打显眼警告**——生产误配的症状是「点了发送、界面说成功、邮箱里永远没有」，服务端一切正常、日志里连错误都没有，属最难查的一类故障 ⇒ M3 部署清单必须验 `RESEND_API_KEY` + `SB_MAIL_FROM` **两个都在位**（只配一个也算没配） |
| `packages/server/src/auth/ownership.ts` | **多租户归属解析（2026-09-17 新建，契约 `docs/TENANCY-SPEC.md`，迁移 v22）**——「谁拥有这条数据」的唯一落点。★ **归属模型：会话是唯一锚点**——只给 `sessions` 加 `user_id`，`messages`/`ask_choices`/`flow_run` **都不加列**、随父会话继承（两个事实源迟早漂移，本仓已在 register/me 的 `createdAt` 上付过学费）；读子表前先 `canAccessSession`。★ `ownerIdOf(req)` 返回 `null` 的语义是「**未登录的单人本地模式**」不是「无主」——此时**不过滤**，维持本地单用户既有行为；安全边界由部署形态保证（生产必开 `SB_REQUIRE_AUTH=1` ⇒ 每条请求必有 user ⇒ 必过滤）。★ **跨用户一律 404、不回 403**（403 等于告诉对方「这个 id 存在，只是不是你的」）。★ `insertSession` 建会话的**唯一落点**：立项时是因为 `POST /api/sessions` 与学习流 `createRun` **两处**各写各的 INSERT 迟早漏一处（★ 学习流那处 2026-09-25 随功能下线删除 ⇒ 今天只剩一处，但 **helper 与这条规矩都留着**，因为下一个建会话的地方一定会再来）——漏写的后果是**那条会话变孤儿、主人自己永远看不到它**（比泄露更隐蔽）。★ **老数据策略**：迁移时 `user_id` 留 `NULL`（那一刻无人知道老会话属于谁，强行回填＝把别人的历史判给别人），`WHERE user_id = ?` 天然不匹配 NULL ⇒ **不泄露**；认领走显式脚本，★ **刻意不做「首个注册者自动认领」**（并发注册归属不确定且无法撤销）。★★ **2026-09-18 M2b 批补：`user_memory` 也走 `ownerFilter`，但「孤儿」取值是 `''` 而非 `NULL`**——这不是疏漏：`user_memory` 有复合唯一键 `UNIQUE(user_id, kind, content)`，而 `ON CONFLICT` 的冲突目标**必须匹配唯一索引的列**，用 `COALESCE(user_id,'')` 表达式索引会让它退化成「无冲突目标」直接报错；两处对外行为一致（登录用户都查不到）。★★ **`providers` 刻意没进 M2b，整片挂成 M2c**：它连着 `role_bindings`（全局表、`PUT /roles/:role` 人人可调）与 `routeRole`（**14 个调用文件**、全仓无 `AsyncLocalStorage`/上下文传递机制）；只给 `providers` 加归属而不管其余，会把「能改别人的」升级成**「能改所有人的」**——**比不做更糟**。⇒ 归属收口必须**按「一整片数据的所有消费路径」切，而不是按表切**。★★ **2026-09-18 M2c 批补：新增 `ownerOfSession(sessionId)`**——「这个 run / 这条会话归谁」从 `sessions.user_id` 读**持久事实**，而不是给推进函数加参再指望每个调用点记得传（漏传的表现是「这一步的模型调用记到平台头上」，**无声无息**）；原学习流 `advanceRun` 走过它（2026-09-25 批次 K 随功能下线删除）。★★ **M2c 归属改造已落码（v0.2.60）**：`providers`／`role_bindings` 加 `owner_id`（**`NULL` = 平台通道**）、`routeRole` 第三参、**14 个消费点全量穿透**（含 3 处此前根本没传）。★ **平台行去重靠部分唯一索引**：复合 PK 在 SQLite 下**不拦 `NULL`**（每个 NULL 互不相等）⇒ `seedIfEmpty` 的 `INSERT OR REPLACE` 会退化成普通 INSERT、**每调一次多一条平台绑定**，而 `routeRole` 按行序取第一条 ⇒ **绑定变抽签**；实测取证与「两个约束分工不同、都要有」见 `docs/TENANCY-SPEC.md` §8.1.2★★ **2026-09-18 M2d-1 批补：本文件新增 `ownerForWrite(ownerId)`，并把「读侧用哪个」定成一条判据**——★ **成批行读用 `ownerFilter`（`sessions`/`user_memory`），单值或聚合读用 `ownerForWrite`（M2d 系列的表）**。理由（开工实测踩到、写错**不报错**）：`ownerFilter` 的「`ownerId === null` 就不加条件」用在 `SELECT value … .get()` 或 `SUM(count)` 上时，库里**多行**会返回**任意一行**／把所有人加在一起 ⇒ 未登录请求读到**某个用户**的设置/总结/XP。★ `ownerForWrite` = `ownerId ?? ''`，**读写两侧同口径**：`''` = 无主行（**谁都看不见**）；这与 `providers`/`role_bindings` 的 `NULL` = **平台通道（所有人可见）** 是**刻意相反**的两套语义，同一个「没有主人」的处境在两组表里可见性相反 ⇒ **取值必须不同，不要统一**（统一会把「孤儿不可见」静默变成「全站可见」）。★ 「鉴权关闭时旧行为不变」没有被违反：未登录模式下写入的行本来就是无主行（同一把 helper），看到的就是自己的全部历史。★★ 另补 `ownerOfSession(sessionId)` 的同批用途未变（M2c 建）；本批把 `learning/activity.ts` 三张表（`daily_activity`/`daily_summaries`/`user_stats`）与 `storage/answer-style.ts`、`learning/quiz.ts`、`learning/quiz-image.ts`、`search/index.ts` 的 `app_settings` 读写全部改为按 `owner_id` 过滤。★ **v0.2.84**：`insertSession(id, ownerId, title?, fork?)` 加**可选第四参**（写 `forked_from_id` / `forked_term` 两列，**列清单动态拼**——不传就一个字节都不写，既有 3 参调用点**零改动**）；同批新增 **`sessionExists(sessionId)`**（查 `sessions` 且 `deleted_at IS NULL`）。★★ **它是补的一个真洞**：`canAccessSession` 在**未登录模式**（`ownerId === null`）下**不查库直接返回 `true`**——那是「本机单人模式」的既有语义、本身没错，但结果是 `POST /sessions/:id/fork` 成了全仓**唯一**「父行可以不存在」的写口 ⇒ 曾回 **201** 并留下一行指向**幽灵父会话**的 `forked_from_id`。★ 通则：**「能不能访问」与「存不存在」是两件事**——未登录模式把前者恒真之后，后者必须有人单独兜（本批的教训，已进 `bug-ledger` 记法与 test-plan §4）。 |
| `packages/shared/src/auth.ts` | **账号契约的单一事实源**（2026-09-17 新建，契约 `docs/AUTH-SPEC.md` v1.0，**前后端共用一份**）：邮箱归一化（trim + 小写 ⇒ `Alice@Example.COM` 与 `alice@example.com` 是同一个账号）、口令长度（<8 拒、**>100 也拒**——上界不是装饰，超长口令会让 scrypt 变成 DoS 面）、昵称归一（trim + 截 20）、从邮箱派生昵称。★ **为什么必须是纯函数且放 shared**：前端「提交前先拦」与服务端「写入前再拦」**必须同一份**——否则边界两侧不一致会表现为「明明能提交却报错」（前端放行 101 字、服务端拒掉）；这里定的边界是**唯一权威**，两端都只是调用方。★★ **2026-09-18 M1.5 批补**：新增验证码常量（`AUTH_CODE_LEN` 位数／`AUTH_CODE_TTL_MS` 有效期／`AUTH_CODE_MAX_ATTEMPTS` 尝试上限／三道限流窗口）与 `normalizePurpose`／`normalizeCode`／`formatCode`。★ `normalizePurpose` **刻意不给缺省值**——默认成 `login` 看着方便，但「忘传 purpose」会被静默当成登录请求，而它本该是 400：**默认值要落在能被发现的那一侧**。★ `normalizeCode` **先 trim 再判长**（用户从邮件复制常带首尾空白/换行，为此回 400 属"把实现细节当用户错误"），但**刻意不做**"去掉中间空格 / 连字符"——那是在猜用户意图，猜错就是把 `123456` 与 `123 456` 当成同一个码、白送一次尝试机会。★ `formatCode` **补零**（不补的话 `7` 与用户的 `000007` 对不上，且长度校验会把它拒掉）。★ 两条**常量不变式**由 `auth.test.ts` 锁死：尝试上限必须 **< 10^6 空间**（否则这道唯一防线等于不存在）、同邮箱上限必须**严于**同 IP 上限（否则"防定向刷"形同虚设） |
| `packages/shared/src/pk.ts` | PK 契约类型与常量（P0-1：`PkIdentity`／`PkPlayer`／`PkQuestion`／`PkRoomState`／`PkRoomError` + `PK_MATCH_MS`/`PK_MAX_PLAYERS`/`PK_ROOM_CODE_LEN`/`PK_ROOM_TTL_MS`/`PK_FINISHED_KEEP_MS` + `pkChannel()` 频道键）。★ `QUIZ_CD_MS`/`ANSWER_TIME_MS`/`IDLE_PENALTY_MS` **刻意未登记**（P0-1 用不到，不提前造常量），随 P0-2 同批落地。★ **`PkQuestion` 类型层面就没有 `answer` 字段**——「正确答案永不下发」是硬约束，不给「不小心下发」留口子 |
| `packages/web/src/components/AccountBox.tsx` + `AccountTrigger.tsx` | 侧栏底部用户框（**统一账号**，AUTH-SPEC §2）：登录/注册/资料/退出；PK 的身份入口已并入此处——★ **2026-09-20 B1 批**：PK 昵称模拟登录三件套（`UserAuthBox.tsx`、`lib/auth.ts` 的 localStorage 身份、`/api/pk/auth/login`）**全部删除**，PK 大厅未登录只给「去登录」引导（存 returnTo 后跳主壳登录，成功后跳回，契约 PK-SPEC §14.1/§14.3）；身份由 httpOnly cookie 会话裁定，前端只读不存 |
| `packages/server/src/security.ts` | 安全三件之 Origin 校验（**不放行 `'null'`**：sandbox 预览页的源就是字符串 null，放行等于让模型写的网页能调写接口） |
| `api-request.ts` + `api-terms-domain.ts` | （★ 原并列的第三件 `api-study-flow.ts` 于 2026-09-25 随学习流整族删除，批次 K）——**抽出来的最低层**（`ApiError` + `request`）与**领域组**（统计 + 4 个写口 + 4 个类型）。★ **领域组于 2026-09-17 抽出（v0.2.33 领域一等实体批）**：`api.ts` 加完领域组即 **402/400** 撞 gates 红线 ⇒ 按同一手法再抽一次（`api.ts` → **353**），以 `...termsDomainApi` 挂引用 + 顶部 `export type {...}` 转出 ⇒ **调用方零改动**（`api.terms.domainAdd` 这层路径不变）。★ 抽出理由：`api.ts` 已 357/400 行，而放新文件会形成 `api.ts ↔ api-study-flow.ts` **环**（本仓明令避让，见 `QUIZ-WEAK-SPEC` §9 那次「刻意不做 re-export」）⇒ 把共用层抽出来，双方单向依赖本文件、**环断在这里**；`api.ts` 仍 `export { ApiError }` 转出，既有三处调用方零改动。★ **加超时是 SPEC §13 点名的必做项，但处置与原判断不同**：本仓 `request` 用原生 `fetch`——**它根本没有默认超时**，所以缺口不是「放宽」而是「**无限静默等待**」。故给长任务（`advanceRun`）显式 `LONG_TASK_TIMEOUT_MS = 180_000` + 可中断 `signal`，并加 `NO_RESPONSE = 0` 把「等太久/被取消」与真实 HTTP 错误分开——超时文案必须写明「**这一步在服务端可能仍在执行**」 |
| `packages/web/src/app/App.tsx` | 应用壳：180px 侧栏（功能列表 = 题库／**对战**／笔记／词条／今日总结／设置 + 可折叠历史对话 + 底部用户框）。★ 2026-09-17 曾新增「学习流」「知识图」两项并排在最前，**2026-09-25 整族下线删除（批次 K，老板判决：与主链路联动性太低）**⇒ 侧栏现为六项；新功能成组放最前，而不是塞在末尾当"附加功能"。另加「知识图 → 词条库」带词跳转（`TermsPage` 接 `initialKeyword`，调用方用 `key` 强制重挂）。**「对战」是例外项**（2026-09-13 加，老板实测「找不到入口」）：PK 页是 `#/pk` 上的独立移动优先页（契约 `docs/PK-SPEC.md` §5），这一项只把 hash 改过去、不进 `View` 联合也不 `setView`，根切换交给 `main.tsx`；返回靠 PkApp 头部的「← 学习助手」把 hash 置回 `#/` |
| `packages/web/src/styles/tokens.css` | 设计 token 唯一事实源（改它必须同步设计系统 demo） |
| `packages/web/src/components/icons.tsx` | SVG line-icon 基座（禁 emoji） |
| `packages/web/src/lib/markdown.ts` | 零依赖正文解析：块级切分 + 行内标记；围栏白名单只有 `svg` / `chart` / `html`，其余语言一律代码块（禁裸注入）。★ **2026-09-20 补行内图片语法并拆分**：新增 `![alt](url)`——规则**必须排在链接规则之前**（排后面会被链接规则先吃掉、留下孤立的 `!`），图源白名单**刻意比链接更严**（链接允许站内相对路径，图片**只认 `http/https`**——相对路径的图指向本应用自己的资源，等于给模型开一条读内部接口的洞）；`remend.ts` 流式产出的内部占位 `sb:incomplete-image` 由该白名单挡下、回落成 alt 文字。同批因加规则后本文件 **401 行**触 `.ts ≤400` 红线 ⇒ 把行内解析（`Inline` 类型／`parseInline`／`INLINE_RULES`／`safeHref`／`safeImgSrc`／`countOf`）整体搬到 **`packages/web/src/lib/markdown-inline.ts`**（**拆文件、不压注释**，同 `terms.ts→term-extract.ts` 先例），本文件只 re-export `parseInline` 与 `Inline` 类型保住既有导入面 |
| `packages/web/src/lib/svg-utils.ts` | SVG 净化（DOMParser 快路径 + 线性正则回退）+ L1 自愈（补闭合/钳宽/主题色）+ `openSvgDocument` 下载/新标签页（入参必须是**净化后**的 SVG，blob 文档继承本应用源），port from v1 |
| `packages/web/src/lib/chart-utils.ts` | 图表 DSL：```chart 围栏 JSON 容错（port from v1 fixEcharts）+ bar/line/pie 零依赖自绘 SVG，渲染卡在 features/chat/ChartCard.tsx |
| `packages/server/src/routes/preview.ts` | ```html 预览出页：内存暂存（上限 20 条 / 512KB）+ `CSP: sandbox`（无 allow-same-origin ⇒ 源为 null）；本页 `X-Frame-Options` 由全局 DENY 放宽到 SAMEORIGIN（侧栏面板要嵌它，外站仍嵌不到） |
| `packages/server/src/routes/images.ts` + `storage/image-cache.ts` | **`fetch_image` 的出图侧（2026-09-20 新增，契约 `docs/IMAGE-FETCH-SPEC.md`）**：工具取图落盘 `DATA_DIR/images/`（字节魔数判型六种白名单、**刻意不收 SVG**（同源打开执行源站脚本）——图床把图片声明成 octet-stream 是常态，**判型在字节上不在声明上**；sha256 内容命名 ⇒ 同内容去重幂等），路由 `GET /api/images/:name` 出图：强正则白名单挡路径穿越、按扩展名映射 Content-Type（全局 `nosniff` 要求准确）、内容哈希命名故 `immutable` 长缓存 |
| `packages/web/src/features/chat/HtmlCard.tsx` | html 卡片：永不内联，点「侧栏预览」送右侧面板、「新标签页」走同一上传路径，三态齐备（进行中/失败/成功） |
| `packages/web/src/features/preview/PreviewPanel.tsx` | 应用右侧内置浏览器面板：只挂 `/api/preview/:id` 沙箱文档，iframe 再叠 `sandbox` 双保险；**无地址栏**（不承诺打开任意 URL） |
| `packages/web/src/lib/preview-store.ts` + `preview-api.ts` | 面板状态（`useSyncExternalStore` 微型 store，跨层不套 provider）与 `uploadPreview()`/`pickTitle()` |
| `packages/server/src/learning/` | 练+忆+学域：quiz 出题引擎（+ **`quiz-json-repair.ts`**：两道无损前置修复——补模型漏写的 `]`（`repairJsonBrackets`）+ 把字符串内漏根的 LaTeX 单反斜杠补成两根（`repairJsonEscapes`，**必须先跑它再补括号**，否则非法转义会让括号修复白做）；两道在**合法 JSON 上永不触发**，故可当所有解析尝试的基底）/ **terms AI 词条库**（`[TERMS]` 协议抽取 / `UNIQUE(term,domain)` 入库 / 相关性检索注入 / usage 计数）/ **tidy 词条整理**（v2.1：`[TIDY]` 协议同义词归一+领域归一、别名感知入库防再分裂；契约 `docs/TERM-TIDY-SPEC.md`）/ **document 文档模式资料**（会话绑定；≤ 60k 整篇直塞、> 60k 走 BM25 切块检索，契约 `docs/DOC-RAG-SPEC.md`；**仍不做 embedding、不做跨会话资料库**）/ activity 打卡（srs+memorize 已废弃，2026-09-01） |
| `packages/server/src/storage/answer-style.ts` | 回答方式偏好读写（46 行，套路照 `loadQuizMix`/`saveQuizMix`）：`loadAnswerStyle`（未配过/坏 JSON/非法值→默认）/ `isAnswerStyleConfigured`（**只查 `app_settings` 键在不在**，`SELECT 1`）/ `saveAnswerStyle`（归一后 upsert，返回归一值）/ `resetAnswerStyle`（DELETE 删键）。★ 「配过没有」只能靠键存在性判断——存进去的恰好是默认值时 style 长得一模一样 |
| `packages/server/src/storage/migrations.ts` | **迁移执行器**：`migrate(db)`（逐版本幂等，≤ current 跳过，每版一个事务失败整体回滚）。★ **2026-09-16 二次拆分**：清单数据（v1~v17）搬到 **`storage/migrations-list.ts`**，本文件只留执行器。★ **2026-09-16 三次拆分（学习流批）**：v18 的七张表让清单再涨 ⇒ 按**版本区间**切成 **`storage/migrations-list-v1-9.ts`** + **`storage/migrations-list-v10.ts`**，`migrations-list.ts` 降为**聚合出口**（`export const MIGRATIONS = [...MIGRATIONS_V1_9, ...MIGRATIONS_V10]`）。**零行为改动**（迁移语句与注释一字未改）；这正是上一档预留的「按版本区间再切」预案，**不是压注释换行数**。理由与 2026-09-14 从 `db.ts` 搬出时完全一致——清单**只会单向增长**（v17 看图加列后本文件到 404 行、再次触「.ts ≤400 行」红线），而执行器自那次拆分后一行未改。**零行为改动**：迁移语句与注释一字未改。⚠️ 若清单再涨到 400 行：**按版本区间再切**，不要用「压注释」换行数（那些注释记的是每张表为什么这么建）。★ **2026-09-17 四次拆分（账号批）**：v21（`users` + `auth_sessions`，契约 `docs/AUTH-SPEC.md`）加完后 `-v10.ts` 涨到 **426 行**、触本仓红线 ⇒ 把 **v18~v21**（学习流 / 领域 / 情景题 / 账号会话）切到新 **`storage/migrations-list-v18.ts`**，`migrations-list.ts` 的导入与头注同步更新（`MIGRATIONS = [...V1_9, ...V10, ...V18]`）；切分用**脚本精确切片**（不手抄 260 行），切完复核 **v1~v21 连续、无缺号无重号**。★ 加新迁移＝在 **`storage/migrations-list-v18.ts`**（**v18 及以后**）数组尾部追加一项（v 号顺延），**不要动既有项**——已应用的版本号是历史锚点。⚠️ **v18 是「就地修正过一次」的特例**：`flow_step` 主键由 `id` 单列改为 `(def_id, id)` 复合（克隆流会撞唯一约束，实测踩到），因**该版本尚未应用于任何真实库**故就地改；**若已发布则绝不可如此**，必须新增版本号。★ **v18（2026-09-16，学习流，契约 `docs/STUDY-FLOW-SPEC.md`）**：**七张表、分三层、纯加法**（只 CREATE TABLE/INDEX，不动既有表）——控制流定义 `flow_def`/`flow_step`/`flow_edge`、控制流运行 `flow_run`（**带 `def_snapshot` 定义快照，不可省**）/`flow_run_step`、知识数据图 `knowledge_node`/`knowledge_edge`。三个库层幂等锚：`flow_run_step UNIQUE(run_id, seq)`、`knowledge_node UNIQUE(kind, ref_id)`、`knowledge_edge UNIQUE(from_node_id,to_node_id,kind)`（后者让重复抽取走 `INSERT OR IGNORE`）。⇒ **回放迁移链的测试本批无需 DROP 新列**（纯加法），但断言表清单/版本号处须补 18。★ **v17（2026-09-16，看图）**：`messages` 加 `images` 列（base64 dataURL 的 JSON 数组），**只用于历史回显缩略图**；主模型看到的是 `chat/vision.ts` 蒸馏后的文字（拼进同一行的 `content`），故**历史回放/重新生成都不再二次调视觉模型**。⚠️ **回放迁移链的测试必须把新列也 DROP 掉**（`ALTER TABLE ADD COLUMN` **不幂等**，实测踩到 `duplicate column name: summary`／`: images` 两次，见 `storage/db.test.ts`） |★ **v15（2026-09-14，PK P0-8）**：建 `pk_matches`（对战历史，契约 `docs/PK-SPEC.md` §12.2）——**P0 里第一处让对局落库**，房间状态机仍全内存；`UNIQUE(room_id,user_id)` 供幂等 + `idx_pk_matches_user(user_id,ended_at)` 供按人倒序查。★ **v16（2026-09-15，长期记忆，契约 `docs/MEMORY-SPEC.md` §3.1）**：`sessions` 加四列（`summary`/`summary_upto_rowid`/`summary_tokens`/`summary_updated_at`）——一个会话一份摘要、滚动覆盖、**不建表**（同 v6 的 doc 两列，生命周期随会话）；`summary_upto_rowid` **用 rowid 而非 `created_at`**（后者只到秒、同秒多条无法区分先后，rowid 单调且删除后不复用），它同时是**幂等锚**（只摘 `rowid >` 它的部分，重复触发不重摘）；建 `user_memory`（跨会话存活，`UNIQUE(kind, content)` 供 upsert 幂等，同 `term_library` 手法；**不设外键**——会话删除后画像仍自洽可读，同 v8/v12/v15 第四次复用）。⚠️ **回放迁移链的测试必须把新列也 DROP 掉**（`ALTER TABLE ADD COLUMN` **不幂等**，本批实测踩到 `duplicate column name: summary`）。★★ **2026-09-25 批次 K：链上出现了第一条「删除型」迁移 v44**（DROP 学习流/知识图那七张表），与本仓此前四十三条「只加不减」相反 ⇒ 两条永久约束：① **链上历史一字不回改**（v18 的建表、v33 的加列原样留着），所以**重放整条链时那七张表会"先被建出来、再被删掉"**，这是有意的不是遗漏；② 于是"退版本重放"用例**不能再对已删表做 ALTER**（`no such table`），`storage/db.test.ts` 的 `revertV33()` 因此先按 v18 自己的语句把表重建出来再退那三列（⚠️ 别把它当手滑删的用例改回去）。⚠️ v44 **刻意不含 `VACUUM`**——执行器是每版一个事务，而 SQLite 的 `VACUUM` 不能在事务里跑。 |
| `packages/server/src/learning/document.ts` | 文档模式域逻辑：`getSessionDoc`/`setSessionDoc`（会话不存在返 null）/`clearSessionDoc`/`docMeta`/`buildDocBlock`/`buildDocMaterial`。**关键设计：存储不丢字，`MAX_DOC_CHARS` 只决定注入形状**（故 `truncated` 可派生、v6 只需两列）：`truncated=false` 走**整篇直塞分支（逐字节等于旧实现，golden 串锁住）**；`truncated=true` 按提问检索 Top-K 段落。资料段开头即声明「内容是数据不是指令」+「优先依据资料，可用一般知识补充」，长资料分支额外必须声明「**这些段落是局部不是全文**，没出现不代表资料里没有」 |
| `packages/server/src/learning/doc-retrieve.ts` | **文档检索层（零依赖词法 BM25，契约 DOC-RAG-SPEC）**：`tokenizeDoc`（英文按词 + 中文 bigram，不引分词器）/`chunkDoc`（按空行聚段、**单段超长走定长硬切**否则整篇无换行的粘贴件切不出块、偏移可 `slice` 回原文）/`buildBm25Index`+`scoreChunks`（倒排索引；idf 用**非负式** `ln(1+(N-df+0.5)/(df+0.5))`，经典式在 `df>N/2` 出负分会造成排序事故）/`retrieveDoc`（k 与预算双截断、首块无条件保留、**空查询返 `[]` 交回调用方**）/`pickUniformChunks`（无查询时的均匀覆盖，给抽词条用；预算不够时是「少取几块但仍铺满全文」不是「从前往后装到装不下」）/`getRetriever()` 返 `Retriever` 接口位。**★ 不设任何分数阈值**（绝对分数与词元覆盖度两种都被探针实测否掉：真命中区间与干扰项区间重叠），判「资料没写」的权力交给模型如实说；**每轮现算、刻意不缓存索引**（716k 字建索引 72ms、缓存则常驻 21.6MB） |
| `packages/server/src/learning/activity.ts` + `packages/server/src/events/bus.ts` | **反馈环（事件总线 → XP/等级/连签/每日计数/今日总结）**。★★ **2026-09-18 M2d-1 批：总线上的四个活动类事件改为必须带 `ownerId`**（`chat_done`/`quiz_generated`/`quiz_answered`/`term_added`）。**为什么必填而不是可选**：订阅者要按人记 XP 与每日计数，可选字段的漏传是**静默**的（记进无主行、用户自己的 XP 少算，测试不覆盖就不红）⇒ 改成必填后，**6 个发布点在 `tsc` 阶段就被要求回答「这笔算在谁头上」**（同 `routeRole` 第三参、`bindQuota` 绑在返回值上，是同一条思路：**把「记得传」变成「不传编译不过」**）。★ 未登录（本地单人模式）传 `null` ⇒ `ownerForWrite` 落 `''`（无主行）。★★ 三张表随**迁移 v30** 归主：`daily_activity`（`PK(owner_id,day,type)`）、`daily_summaries`（`PK(owner_id,day)`）、`user_stats`（`PK(owner_id,key)`）。改前它们**全是全局表** ⇒ 「**A 和 B 的 XP 是同一个数**」「**B 直接读到 A 的今日总结**」（改前注释里已把这条记为「已知缺口」）「A、B 同一天聊天**直接撞主键**」。★ `recordActivity(type, ownerId, n)` 的参数顺序刻意把「这笔算在谁头上」放在数量**之前**且**必填**。★ `todaySummary` 的**缓存键含 owner**（`WHERE owner_id = ? AND day = ?`）；`todayStats`/`computeStreak`/`last7Days` 三处读**全部带 `owner_id`**（★ 这四处都是**单值/聚合**读，**不能**用 `ownerFilter` 的「`null` 就不加条件」——见 `auth/ownership.ts` 的判据）。★ `wireActivityEvents()` 只在**直接运行 `index.ts`** 时自动接线（`process.argv[1]` 判断）⇒ 测试里要自己调一次（幂等，模块单例）。★ 端点 `GET /api/activity/today`、`/week`、`/summary`（`routes/activity.ts` 只把 `ownerIdOf(req)` 的**原样值**传下去，**转换留给 `ownerForWrite`**——路由里写 `?? ''` 就是把转换抄了第二份）。★ 跨用户可见性由 `routes/settings-tenancy.test.ts` **11** 例在守。 |
| `packages/server/src/storage/migrations-list.ts` + `-v1-9.ts` + `-v10.ts` + `-v18.ts` + `-v22.ts` + `-v30.ts` + `-v31.ts` + `-v40.ts` + `-v41.ts` + `-v43.ts` + `-v44.ts` | 迁移清单（**在册形态：聚合出口 + 十个版本区间分片**——`-v1-9.ts`（v1~v9 建库地基）/ `-v10.ts`（v10~v17）/ `-v18.ts`（v18~v21）/ `-v22.ts`（v22~v29）/ `-v30.ts`（v30）/ `-v31.ts`（v31~v39）/ `-v40.ts`（v40）/ `-v41.ts`（v41~v42）/ `-v43.ts`（v43）/ **`-v44.ts`（v44 及以后）**）。⚠️ **本行原停在「七拆后／五个文件」**，与在册文件数差三片 ⇒ 2026-09-25 批次 K 按 `ls` 现数改齐（那三片各是别的批次加的、当时没回写本行）。★ **每次都是被门禁逼出来的**：2026-09-17 账号批切出 `-v18.ts`（v21 加完后 `-v10.ts` 426 行）；2026-09-18 多租户 M2b 批再切出 `-v22.ts`（v24 加完后 `-v18.ts` 413 行）。两次都照该文件自身写明的规矩「**按版本区间再切、不压注释换行数**」办理——分片注释记的是每张表**为什么这么建**，价值远高于行数。★★ **新迁移的落点是 `-v44.ts` 片尾（尾部追加，v 号顺延）**，不要再往更早的分片加（★ 2026-09-25 批次 K 起；本行此前停在 `-v31.ts` 未跟上，各次切片的真实理由写在**那片文件自己的头注**里）。★ 第五次拆分（2026-09-18 M2d-1 批）：`-v22.ts` 加到 v29 后 **346 行**、距 400 红线只剩 54 行，而 M2d 这批**多张表重建**光注释就装不下 ⇒ 照同一规矩切出 **`-v30.ts`**。★★ **v30（2026-09-18，M2d-1，契约 `docs/TENANCY-SPEC.md` §8.2）**：`app_settings`（`key` 单列 PK → `PK(owner_id,key)`）、`daily_activity`（`PK(day,type)` → `PK(owner_id,day,type)`）、`daily_summaries`（`day` 单列 PK → `PK(owner_id,day)`）、`user_stats`（`key` 单列 PK → `PK(owner_id,key)`）**四张全部重建**（照 v24/v29 的六步先例：建新表 → `INSERT … SELECT` 回填 → DROP → RENAME），老行**一律回填 `''`（无主）**。★★ **归属值取 `''` 而不是 `NULL`，与 v29 的 `providers` 刻意相反**（那里 `NULL` = 平台通道 = **所有人可见**；这里 `''` = 无主 = **谁都看不见**）——迁移头注写了完整对照，**改它之前先回答「这两组表的可见性是不是真的一样」**。★ 四张表都是**重建**（无加列）⇒ 退版本重放要「DROP 新表 + 按旧结构建回」，**不能只 DROP**（`migrate()` 只跑 `version > current`，被跳过的 v1 `CREATE TABLE IF NOT EXISTS` 不会重跑 ⇒ 整条链后面全报 `no such table`）。★ 连带必改（**只改迁移不改代码 = 运行时 500**）：4 处 `ON CONFLICT(key)`／`ON CONFLICT(day)` 的冲突目标必须同步改成复合主键。见上方 `storage/migrations.ts` 行。★ **v27（2026-09-18 M1.5 邮箱验证码）已按此规矩追加在 `-v22.ts` 尾部**：`auth_codes` 八列 + `(email, purpose)` 索引。★★ **主键刻意用自增 `id` 而不是 `email`**——同邮箱会**多次**请求验证码，以 email 为主键就只能覆盖（旧码无法保留以做重放审计），且并发下 `INSERT OR REPLACE` 会**静默吞掉正在校验的那一条**（症状是"码明明是对的却总说不对"且不可复现）。★ `email` **不设外键**：验证码可以**先于注册**存在（`register` 态就是给还没有的账号发码）。★ 本迁移是**纯建表**型（`CREATE TABLE IF NOT EXISTS`）⇒ 回放迁移链**无需 DROP 列**（与 v22/v23 的 `ADD COLUMN` 刻意不同，那两次踩过 `duplicate column name`） ★★ **v31（2026-09-18，M2d-2，契约 §8.2）**：`term_library`（`UNIQUE(term,domain)` → `UNIQUE(owner_id,term,domain)`，**`id` 仍是全局唯一 PK**——`knowledge_node.ref_id`（★ 该表 2026-09-25 批次 K 随知识图下线被 v44 DROP）与 `term_review_log.term_id` 都指向它）、`term_domain`（`name` 单列 PK → `PK(owner_id,name)`）、`term_mention_log`（口径 `NULL` → 空串）**三张重建**，老行一律回填空串。★ 归属值取空串而非 `NULL`，与 v29 的 `providers` **刻意相反**（理由同 v30）。★ 连带必改：`terms.ts` 两处 `ON CONFLICT(term,domain)` 的冲突目标要同步改 `ON CONFLICT(owner_id,term,domain)`（只改迁移不改代码 = 运行时 500）。 |
| `packages/server/src/routes/document.ts` | `/api/doc` 薄路由（GET/POST/DELETE）：只回元信息、**永不回显正文**；不落盘、不校验扩展名（约束在 UI）；缺参 400 / 会话不存在 404 |
| `packages/web/src/features/quiz·terms·summary/` | 题库 / 词条库（TermsPage 列表管理页）/ 今日总结三屏 |
| `packages/web/src/features/chat/DocModeControl.tsx` | 文档模式的**状态 pill 与载入面板**（挂在 composer 上方）：已载入 pill（名·字数·截断标·清除）/ 展开面板可粘贴或选 `.txt,.md,.markdown` 文件；刷新后只凭 `GET /api/doc` 的元信息复原。**→ 2026-09-13 起「触发器」不再在本文件**——「文档模式 / 换资料」按钮搬进输入框的「+」菜单（见 `components/ComposerMenu.tsx`），本文件改消费 `useDocMode` 的状态。理由：pill 属于**会话状态**，藏进折叠菜单等于把状态藏了（与「联网开关」同一处理：**动作可折叠、状态不藏**）。切换会话时 `useEffect([sessionId])` 重取（`alive` 标志防串台）在 hook 里。**→ 2026-09-07 文案修正三处**：按钮 `title` 按「有无资料」分两态（有资料时直说会替换哪一份）、pill 里扩展名单独成 span（截断时 `.txt` 不丢）、面板顶部补一行机制说明（阈值与会替换原先只在 tooltip 里）——详见 `docs/DOC-RAG-SPEC.md` §4.1。**→ 2026-09-07 打磨两项**：清除按钮 chip 化（可点性）、名与扩展名贴回一体（gap 不再插进文件名中间），after 图证 `shots/after/` |
| `packages/web/src/features/chat/doc-name.ts` | `splitDocName()` 纯函数（pill 文件名拆扩展名，`.gitignore` 类点在首字符的不算扩展名）。从组件里抽出来是为了能测（先例 `features/quiz/mix-report.ts`），配套 `doc-name.test.ts` 6 例——本仓 `.tsx` 无测试环境，纯函数放组件里就进了不了测链路 |
| `packages/web/src/features/chat/ChatComposer.tsx` | **对话页输入区**（2026-09-13 新建）：textarea + 发送/停止 + 「+」折叠菜单 + 文档模式 pill/面板 + 本轮出题提示（配比摘要 / 选项卡 / 连接提示 / 「回到最新」）。**从 `ChatView` 整段搬出**——ChatView 已贴 300 行门禁（gates 第 1 条），输入区自带二十多个受控 props，留在原处必然超线。★ **触发器只在 `sessionId === null` 时整体禁用**：生成中只禁被真正影响的菜单项，**「导出对话」与「联网开关」照旧可用**（ADR-5：能做的别藏着）。给触发器写 `disabled={blocked}` 是最省事也最错的写法——它会把这两条既有行为静默收掉。★ **图片入队判定 2026-09-20 抽成 `packages/web/src/lib/image-intake.ts`**（`judgeLoadedImage` / `remainingSlots` / `intakeRejectHint`）：上限从 `@sb/shared` 的 `chat-limits.ts` 读（本文件不再写 `MAX_IMAGES = 4` 这种字面量），**过大的图当场被拒并当场给提示**——治的是「点了发送没反应」那个静默症状（413 发生在**路由之前**，服务端那一侧根本没有可断言的代码路径，**这类症状只能在入队侧拦住**）。★ **判定顺序刻意「先尺寸后名额」**：反过来的话一张超大图会被告知「已满」，用户去删图仍然传不上——**指错方向比不指更糟**。★ `remainingSlots` 把**在途**（`inflight`）一并扣掉，这是**同一 tick 连贴两次不会双双放行**的前提（v0.2.69 实测曾落进 6 张） |
| `packages/web/src/features/chat/ChoiceCard.tsx` + `choice.css` | **方案选择框卡片**（2026-09-14 新建，契约 `docs/ASK-CHOICE-SPEC.md`）：渲染在**输入框上方浮层**（与 `AskStyleCard` 同处置，**不动 `messages` 表、零 schema 风险**）。三态（待选 / 已选 / 作废）＋自由输入出口；props `{request, onReply, onDismiss}`，**组件不自己调接口**（答复走 `useChoiceQueue` 的 `replyChoice`）。★ 自绘 SVG line-icon、禁 emoji（`components/icons.tsx` 基座）。★ 不做乐观切态（见 `useChoiceQueue`） |
| `packages/web/src/features/chat/useChoiceQueue.ts` | 方案选择框前端状态 hook（2026-09-14 新建）：`applyEvent(ev)` 消化 `choice-asked`/`choice-replied`/`choice-cancelled` 三帧（**幂等覆盖、切态不出队**——卡要留在屏上显示结果，作废也要看得见）、`replyChoice`（**刻意不做乐观切态**：并发双端点击的结果由服务端裁决，前端先切会让 409 那次的用户看到假成功）、`dismissChoice`、`reset`；**切会话时调 `api.choices.pending` 把挂起卡捞回来**（SSE 缓冲 60s 无订阅即回收，光靠流会漏）。从 `useChatStream` 抽出（该文件触 400 行红线）。★ **v18 加 `skipChoice(requestId)`**：**真去后端作废**那条挂起提问（`POST /api/choices/:id/cancel`）——原来的 `dismissChoice` 只动本地 state，后端 `askChoice` 还在等，而 grill-me 开场那张卡是**阻塞整轮**的，不真取消工具就永久悬挂、「可以跳过」成空话 |
| `packages/web/src/features/chat/useGrillChoice.tsx` | **grill-me 模式的前端侧（v18，2026-09-16 新建，71 行）**：模式开关 + 点选编排 + 消息流卡片接线。★ **hook 直接吐 `grillNode`**：`ChatView.tsx` 贴 300 行门禁，这一套三十余行塞进去必破线；与本仓「从 ChatView/useChatStream 往外抽」的既有手法同源（`useChoiceQueue`／`useSendActions`／`composer-status`），只是进一步把卡片也一并托管，调用方只落 4 行。★ **模式是会话级前端状态（不落库）**：与文档模式、联网 pill 同口径，切会话靠 ChatView 重新挂载而重置，零 schema 改动。★ **两段语义相反**：`pre` 段答复由服务端回灌，前端什么都不做；`post` 段本轮已结束、答复不回灌，故**前端必须把选中项作为新一轮提问发出**——否则点了选项毫无反应（这是最容易写错的一处）。★ **状态仍由 `useChoiceQueue` 单点持有**：本 hook 只做筛选（`pendingChoice?.grillPhase` 才算 grill 卡）与编排，**不造第二份队列** |
| `packages/web/src/features/chat/GrillPill.tsx` | grill-me 的**状态 pill**（v18，2026-09-16 新建）：模式收在「+」折叠菜单里，**开着必须在框外说一句**并给一键关闭。理由同 `composer-status.ts` 的「联网开着必说」——动作可以折叠，**状态不能藏**；不提示的话用户会以为这个模式没生效（这恰恰是它上一版失败的方式） |
| `packages/web/src/features/chat/AttachmentTray.tsx` | 输入框上方的**待发送图片缩略图托盘**（v17 看图，2026-09-16 新建）：每张带移除按钮。从 `ChatComposer` 抽出——该文件已贴 300 行门禁，本批 grill-me 与看图两处增量叠加后必须腾地方（AGENTS「贴线前先开新文件」） |
| `packages/web/src/lib/image-intake.ts` + `packages/web/src/lib/markdown-inline.ts` | **看图链路收口批（2026-09-20）新增的两件，都是「拆出来才测得到」的产物**：① `image-intake.ts`（44 行）——**贴图入队判定的纯函数**（`judgeLoadedImage(dataUrl, currentCount) → 'accept' \| 'too-large' \| 'full'`、`remainingSlots(currentCount, inflight)`、`intakeRejectHint(reason, count)`），从 `ChatComposer.tsx` 抽出以便**可测**：`.tsx` 在本仓一旦没有 jsdom 就是测试盲区，而这条判据决定「用户贴的图进不进得去、进不去有没有被告知」；② `markdown-inline.ts`（146 行）——**行内解析层**（`Inline` 类型／`parseInline`／`INLINE_RULES`／`safeHref`／`safeImgSrc`／`countOf`），因 `markdown.ts` 加图片规则后 **401 行**触红线而整体拆出（**拆文件、不压注释**）。两件都零依赖零 IO，分别由 `image-intake.test.ts`（11 例）与 `markdown.test.ts`（35 例）覆盖 |
| `packages/web/src/features/chat/useSendActions.ts` | 发送侧动作 hook（2026-09-14 新建）：`send`/`rerun`/`regenerate`/`resend`/`stop` 五个动作。同样为 `useChatStream` 腾 400 行门禁空间抽出，type-only 依赖 `StreamMessage`，**不含任何状态**（纯动作，状态仍归 `useChatStream`） |
| `packages/web/src/features/chat/useRoundBegin.ts` | **切轮八步重置**（2026-09-19 P0.5 新建，44 行，`bug-ledger` B-009）：`beginRound` 从 `useChatStream` 抽出（该文件 401/400 触线，**拆文件不压注释**）。★ 它持有本批新增的**本地兜底计时基线**——`startedAtRef`+state 双写（ref 给 SSE 回调读真值、state 给 `Thinking` 渲染），未收到 `round-start` 帧的渠道（coach/PK）就以此为基线，**退回是设计不是缺陷**。`depsRef` 取最新依赖保 `begin` 引用恒定稳定，调用方不必 memo |
| `packages/web/src/features/chat/step-fold.ts` | SSE `step` 帧 → 过程卡片列表的**折叠纯函数**（2026-09-19 P1 新建，配对锁见 `step-fold.test.ts` 7 例；从 `useChatStream` 抽出——加 toolCallId 配对后放不下，行数红线）。★ **配对键 = `toolCallId`（bug-ledger B-010）**：旧实现按「name + 倒扫最近一条 running」配对，**同名并行调用会串卡**（A 卡的终态盖到 B 卡上，两卡各错一半）；现在终态只认 id 原位替换，**配不上（断线只回放终态／缺 id 的老帧）一律另起一条**——宁可多一张也不盖别人的，绝不回退倒扫。★ `startedAtMs` 是**客户端时钟**、只驱动 running 徽标的本地 tick（assistant-ui 口径：timing 只在流结束时定稿，live badge 自己起 timer）；终态一到即被服务端的 `durationMs` 取代——这个值不落库、回放不带，就是「本地掐表只作过渡显示」的边界 |
| `packages/web/src/components/ComposerMenu.tsx` + `composer-menu.css` | 输入框「+」展开菜单（2026-09-13 新建）：**通用件，不认识任何具体功能**，只消费 `items` 数组——以后加功能＝往数组里加一项，本文件与 CSS 都不动（老板明确要求的「预留」）。两种条目：`action`（点完收起）/ `toggle`（点完**保持展开**并在右侧显示「已开/已关」——收起会把反馈一起收掉）。`status` 摘要挂在触发器上一直可见（**动作可折叠、状态不能藏**）；支持 ESC 与点外部关闭（缺这两条，菜单只能靠再点一次「+」关）。「+」展开时纯 CSS 转 45° 变「×」。目前只有对话页用它：题库页那行只有「联网」一个次级控件、主 CTA 必须常驻可见 |
| `packages/web/src/features/chat/composer-status.ts` | **菜单触发器的状态摘要 `menuStatus({online, docBase})`**（2026-09-13 新建，纯函数 `composer-status.test.ts` 6 例）：联网开着**必说**（这是收进折叠菜单后唯一会失去可见性的状态，写错不报错、只是少一句提示，用户会以为联网没开）、联网关着不说（主动选择不是异常）、已载资料报「资料 名字」（名超 10 字截断）。判定抽到这里才进得了测链路（先例 `features/quiz/mix-report.ts`、`features/chat/doc-name.ts`） |
| `packages/web/src/features/chat/useDocMode.ts` | 文档模式的状态与动作（2026-09-13 从 `DocModeControl` 抽出）：`meta/dn/name/text/busy/hint/overCap` + `submit/onPickFile/clear`。**抽出来的是状态不是判定**，故没有可直接上仪器的纯函数（缺口登记在 test-plan §6）。刻意**不接 `blocked` 参数**：动作禁用是调用方的事，状态层再设一道闸只会有两个真相源 |
| `packages/web/src/features/chat/useChatStream.ts` | SSE 生命周期 + 流式文本/step/block 累积编排（前端加一种 SSE 事件渲染从这里接）。**2026-09-14 拆出两个 hook**（`useChoiceQueue` 方案选择框状态 / `useSendActions` 发送侧五个动作）——加方案选择框后本文件 485 行触 400 红线，抽出两个内聚单元后回落 **392 行**；事件入口改为 `if (applyChoiceEvent(ev)) return;` 先把 choice 三帧转发给 `useChoiceQueue`。★ **切轮时 `beginRound` 要调 `resetChoices()`**，否则上一轮的卡会串进下一轮。**2026-09-19 P0.5（B-009）**：SSE 处理器首分支消费 `round-start` 帧（双写 `startedAtRef`+state，见 `useRoundBegin`），**切会话效果里必须 `startedAtRef.current = 0`**——不清零会把上一个会话的轮起点串进新会话；`beginRound` 本体已移出到 `useRoundBegin.ts`（401/400 触线） |
| `packages/web/src/features/chat/Markdown.tsx` | 助手正文渲染器（注入点只有 SvgPreviewCard / ChartCard 里净化后的 SVG，其余走 React 转义） |
| `packages/web/src/features/chat/Welcome.tsx` + `Mascot.tsx` | 空会话欢迎页（「未选会话」与「新会话无消息」共用一套；建议卡只填输入框不自动发送）；吉祥物是 **16×16 像素点阵**（`SPRITE` + 眨眼合帧 `LID` 由 `toRuns()` 并成 `<rect>`，`crispEdges` 且只按 4× 整数倍放大）；配色档位与 `steps()` 动效时序在 `chat.css`（组件内不写内联 style），`Mascot.test.ts` 用 `spriteErrors()` 钉住点阵自洽（行宽／字母登记／合帧必须正压眼位） |
| `tools/gates/check.mjs` | 行数/内联样式/any 门禁 |
| `tools/dev.mjs` | **一条命令并行拉起前后端 dev**（`npm run dev` 的载体，2026-09-12 新增）。替代原先 `npm run dev:server & npm run dev:web`——`&` 在 Windows 的 cmd 里是顺序而非后台，`tsx watch` 不退出 ⇒ 前端永远起不来。这里用 `spawn` 真并行、`stdio: inherit`、任一退出即收掉另一个并透传退出码（防空壳半跑）。零依赖 |
| `tools/probes/doc-rag-bm25.mjs` + `.result.txt` | **首个入库探针**（与同目录 `doc-rag-bm25.result.txt` 一起）：量切块/BM25 在 72k~716k 字资料上的召回、耗时、内存与 k 拐点，`docs/DOC-RAG-SPEC.md` §8 的每个数字都出自它。**纯 JS 不碰 DB 也不联网，Node 20/22 都可跑**；改 `doc-rag.ts` 里任何常量前重跑它。★ 它**不 import 仓内实现**（复刻同参数算法），两边的关联靠 `doc-retrieve.test.ts` 的召回锁；计时与内存行天然浮动，可复现的是召回与块数 |
| `tools/probes/quiz-e2e-cdp.mjs` | **出题页真机渲染探针**（2026-09-13 来源标注批入库）：零依赖（Node 22 内置 `fetch` + `WebSocket` 直驱 CDP）驱动无头 Chrome，真点「题库 → 填主题 → 一键出题」并抓屏上文案（联网提示、题内「来源：」行、`RefList` 的折叠清单与链接 href），截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**）。**存在理由：本仓 web 侧无渲染测试基建**（无 jsdom/testing-library，见 test-plan §1）——「屏幕上一行文案到底有没有出来」纯函数测不到，这是当前唯一能自动核验渲染层的手段。用法与前置（隔离实例 + 5174/18799）写在文件头；★ **必须「点选项 → 等一拍 → 点提交」**（同 tick 会因 picked 还是空而直接返回、题目不揭晓，看着像功能 bug 实为探针太快） |
| `tools/probes/chat-composer-cdp.mjs` | **输入区改版真机渲染探针**（2026-09-13 入库，零依赖同上一行）：11 条断言覆盖「按钮真的从输入框那行消失（只剩「+」与发送）」「来源清单不在常驻区」「侧栏含「对战」且点得进 PK 页 / 回得来主壳」「「+」点得开且 5 项俱在（`aria-expanded` 同步、联网是 `menuitemcheckbox`）」「ESC 收得起」「触发器挂「联网已开」摘要」「文档模式面板开在 composer 上方而非菜单里」。**默认不碰 LLM**；`SB_PROBE_QUIZ=1` 时**真出一次题**（主题「二重积分」）以验「从菜单触发」这条新接线——原为行内按钮直连、现经菜单条目转发，**纯函数测不到**（菜单点不着或转发丢了，单测与 tsc 全绿而用户按「出题」没反应）。★ **自带数据自清**：为点开菜单必须真建一个会话，跑完用页内 `fetch` 删掉会话**连同出题顺带写进题库的条目**，并复核两侧条数回到跑之前——不给用户真实库留垃圾（test-plan §7）。前置同上一行：前端 `vite` 在 **5174**（探针把端口写死；5173 上跑的是另一个项目）、后端 18791 |
| `tools/probes/weak-analysis-cdp.mjs` | **「薄弱点分析」真机渲染探针**（2026-09-15 薄弱点分析批入库，零依赖同上一行）：**三个分支都验**（五次连跑全 EXIT=0）——**AI 主路径**（该套题有错题）**17 条断言**、**空态分支**（`analyzed:0`，服务端不调模型）**15 条**、**降级分支**（`SB_PROBE_DEGRADE=no-model|call-failed|parse`，用 CDP **Fetch 域拦下 analyze 请求并伪造响应** ⇒ **零副作用**：不去设置页解绑模型、不碰真实库、不花模型额度）**17 条**。覆盖「**三态三头**」——点前无残留结果块／**在途**按钮转「分析中…」且 `disabled=true`／**落屏后**按钮复位为「薄弱点分析」且解禁（可重复分析，漏验这一头＝用户只能分析一次）；结果层「顶部口径行 `AI 分析（基于 N 道错题）`」「多主题卡片四要素（主题／题号／理由／建议）齐全」「题号是 1 基（无「第 0 题」）」「**非降级时 `.quiz-weak-fallback` 节点不存在**」（「降级不得冒充 AI 分析」ADR-5 的反向锁）；空态层「文案是『先做题』」「**不挂口径行**」「**不挂降级提示**」（空态与降级必须分得开，契约 §2.1）。★ **只读**：只调 `GET /api/quiz/analyze/:id`、服务端不写库 ⇒ **可对真库跑**（与 `quiz-e2e-cdp.mjs` 会真出题落库不同），但**真花模型额度**，别放进 CI 循环。前置：后端 18791 起着 + 前端 `vite` 在 **5174**（探针写死）+ 设置页已给「薄弱点分析」绑过模型。★ 三条坑：**「分析中…」窗口只有几秒 ⇒ 必须 80ms 密轮询**（间隔 1s 会跳过、把三态已实现误判成没实现）；**截图前必须把按钮滚进视口**（`captureScreenshot` 只拍视口，否则白截一张）；★ **不变量要判「空真」而非「失败」**——零延迟分支（空态不调模型）的在途窗口可能比 80ms 采样还短，**同一套代码两次跑一次采到一次采不到**，判红即**假红灯**（会逼人去改本来正确的代码）⇒ 首次采样前已出结果就记 **SKIP**，用 `skipped` 单独计数、不计入失败；**④★ 端口随机 + 起 Chrome 前先探测端口**——固定端口连跑时上一次的 Chrome 未必已释放端口，新实例**绑不上**、`/json/version` 反而连到**残留的旧浏览器**，探针跑到「别人家的实例」上去了（症状＝请求暂停后永不 fulfil、按钮永远停在「分析中…」、吃满超时整片红，**看着像产品挂了**；本批 `parse` 首跑 11/18 红、单独复跑 17/17 才定位到）⇒ 端口取 `9300+rand(200)`（可 `SB_CDP_PORT` 覆盖），起前探到有人就 `exit(2)` **拒绝跑**——宁可拒绝跑，也不给假结论。用法 `node tools/probes/weak-analysis-cdp.mjs [套题标题]`，截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**） |
| `tools/probes/pk-breakpoint-cdp.mjs` | **PK 响应式断点真机核验探针**（2026-09-16 PK 断点重构批入库，零依赖同上一行）：**9 档视口矩阵**——移动 375/414、**矮屏横放** 667/844、平板 768/834、桌面 1024/1280/1440，每档断容器宽／对阵方向／选项列数／侧栏几何／旋转提示显隐／无横向溢出，另加两条**全档底线**（题干 ≥14px、选项触摸目标 ≥44px）与一条专治「`var` 解析失败 ⇒ 整条 `grid-template-columns` 声明作废 ⇒ **悄悄退化成 1 列**」的 `colsRaw !== 'none'` 锁；另有本次重构的目标锁「真源不得再有 `@media … orientation`」。★ **与同目录其它探针最大的差别：它不需要后端、也不需要 vite**——媒体查询行为是**纯 CSS 事实**，故探针自带 http 服务，只把 `tokens.css` + `pk.css` **两个真源文件**（每次运行从源复制，不测副本）与一份照 `PkArena`/`PkMatch`/`PkAnswerBlock`/`PkTopicBar` 手搭的 DOM 喂给无头 Chrome ⇒ **零副作用**（不碰 DB、不联网、不调 LLM、不写用户数据），**可反复跑**（同目录其它探针因花模型额度或写库而不能）。用法 `node tools/probes/pk-breakpoint-cdp.mjs`；CDP 端口随机（`9300+rand(200)`，可 `SB_CDP_PORT` 覆盖）+ 起 Chrome 前先探端口，已占用即 `exit(2)` **拒绝跑**；截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**）。★ **改 `pk.css` 任何断点或 `--sb-pk-*` 变量后必须重跑**——tsc／eslint／gates 都看不见 CSS（不在门禁扫描范围），这是当前唯一能自动核验断点层的手段 |
| `tools/probes/coach-cdp.mjs` | **督促小窗真机渲染探针**（2026-09-18 督促小窗验收批 v0.2.50 入库，零依赖同上一行；**默认档 38 断言 / 三档可选**）：清偿 v0.2.48 挂的「`.tsx` 渲染层无自动化测」。★ **三档**：默认档**零 LLM**（胶囊文案与色调、有督促时脉冲点 → 打开抽屉 → 标题副标题 → **队列区在流区之上**的上下次序 → 队列卡四字段 → 翻转揭晓 → 快捷动作 → 输入框禁用/解禁 → **再开一次抽屉时督促卡不重复**）；`SB_PROBE_COACH_LLM=1`（点「今天怎么排」→ **以服务端 `/api/coach/messages` 的真值为基线**、再回 DOM 找那张新卡；**真调模型、真烧额度**）；`SB_PROBE_COACH_REVIEW=1`（点「记住了」→ 断言卡离开队列 + 打卡卡出现 + `due` 递减）。★ **写档自带生产保护**：`APP` 指向 5173/5174 时**拒绝跑**——防止在老板的生产实例上写数据。★ **两个必知前置**：① **拷库隔离时必须连 `.mk` 一起拷**（`storage/crypto.ts` 的主密钥就在 `DATA_DIR` 内、DPAPI 包裹）——只拷 `.db` 会让 `initCrypto()` 生成全新随机主密钥、副本里 `enc:v1:` 的 key 解不开、`decryptSecret` 返回空串 ⇒ 出站变成 `Authorization: Bearer `（空）⇒ provider **401「无效的令牌」**，**看着像 key 过期而其实是拷库漏了文件**（诊断法：直连 provider 的 `/models` 与 `/chat/completions` **双双 200** 即证 key 有效）；② 首版选择器取 DOM 里第一个 `.coach-card-ai`，抓到的是上一轮 401 失败卡 ⇒ 报「该回复没渲染」——**「探针红」的第一反应该是「谁的真值更可信」，而不是「改探针让它变绿」**（改由服务端真值做基线后才露出 `apiAi:8 / domAi:7 / busyStop:true` 这条决定性对照，**那条对照就是 `bug-ledger` B-007**）。用法 `node tools/probes/coach-cdp.mjs`（前置：`npm run dev` 已起；CDP 端口随机、被占即 `exit(2)` 拒绝跑；截图落 `SB_SHOT_DIR`，**不入仓**）★ 已知未覆盖：**督促频率阈值仍未验**（`NUDGE_MIN_OVERDUE_DAYS` 与 `NUDGE_MIN_BACKLOG` 是拍的数，**无真实语料支撑**，见 `test-plan.md` §6） |
| `tools/probes/terms-layout-cdp.mjs` | **词条页布局真机核验探针**（2026-09-18 v0.2.53 布局回归修复批入库，零依赖同上一行；**三态 × 两视口 = 6 条不变量 / 56 断言**）：清偿「`bug-ledger` B-008 词条库本体被挤出可视区」——本仓 `.tsx` 无 jsdom、**tsc/eslint/vitest/gates 对布局全绿也证明不了任何事**，故只能量真机像素。★ **与同目录其它 `-cdp` 探针最大的差别：它不需要后端、不需要 vite、不碰 DB、不联网、不烧额度**（同 `pk-breakpoint-cdp.mjs` 那一档）——从源读真 `tokens.css`+`app.css`+`terms.css`，按 `TermsPage`/`ReviewPanel` 的 JSX **手搭 DOM** 喂无头 Chrome，自带 http 服务。★ **三态**＝`none`（无面板）/`collapsed`（收起，默认态）/`expanded`（展开）× **两视口**＝800 / 1000 高。★ **六条不变量（刻意断言不变量而非像素快照，改样式不假红）**：① 页面不被裁（`.term-page` 无溢出裁剪）；② 面板有界（≤ 视口 45%）；③ `.term-list` ≥ 150px **且在视区内**；④ 搜索/添加/编辑/删除按钮**全在视区内**；⑤ 列表自身可滚（`overflow-y:auto` + `scrollHeight > clientHeight`）；⑥ 展开态队列内滚。★★ **已知局限（写在文件头）**：DOM 是照组件 JSX **手搭的骨架**——`TermsPage`/`ReviewPanel` 的**结构变更时须同步更新骨架**，否则测的是过期结构；探针**只验两种静态结构、不模拟点击**（收起/展开的点击手感须目检）。用法 `node tools/probes/terms-layout-cdp.mjs`（CDP 端口随机、`SB_CDP_PORT` 可覆盖、被占即 `exit(2)` 拒绝跑） |
| `tools/probes/landing-demo-cdp.mjs` | **落地页 hero 演示窗真机渲染探针**（2026-09-20 hero 知识图演示批 v0.2.86 入库，零依赖同上一行；**65 条断言**）：清偿「落地页演示窗里到底长什么样」——本仓 `.tsx` 有 jsdom，但**那一层没有真 CSS、没有真 SVG 布局**。★ **它必须先解决一个前置**：老板本机后端对 `/api/auth/providers` 返回 `{"form":"local"}` ⇒ `entryFor(null,'local')='app'` ⇒ **落地页根本渲染不出来**（直接进应用壳）；探针用 `Page.addScriptToEvaluateOnNewDocument` 在导航前**注入 fetch 覆盖把 form 改成 cloud** 才进得去（**这不是绕过测试，是复现线上 cloud 形态**）。★★ **5 帧逐帧断言**（F0 起点／F1 点追问／F2 星型／F3 二跳／F4 出处分层）：每帧的节点数、边数、「向 AI 追问」按钮、底部词条表、legend 项数（**恰 2 项**）与 user 边数（F0=1 → F4=2）；外加 hero 双栏布局（≤880px 堆叠）、Tab 数与选中唯一性、**「知识图层不溢出演示窗」**（`.ld-g` 的盒不越过 `.ld-stage`——上一版探针曾误断 `.ld-stage` 自身不许溢出，那是**设计如此**的裁剪，已改判）。★ 5 帧截图落 `SB_SHOT_DIR`（缺省系统临时目录，**不入仓**），**每帧截图前等 700ms**（CSS 交叉淡入的时序窗，等不足会拍到两串文案重叠的中间态——本批真踩过）。★ 需 5173 起 vite（`SB_URL` 可覆盖）；**零写入、不烧模型额度**（演示内容前端写死、不连后端）。★ 已知未覆盖：**Tab 真机点击切换未验**（只断言了 Tab 数与选中唯一性，没真点过） |
| `tools/probes/og-card-cdp.mts` | **分享卡截图器 ＋ 排版越界检测**（2026-09-23 v0.2.116 批次 F-3 入库；`npm run og:shots` 的载体，走 tsx 因为它要 import `packages/web/src/seo/og-card.ts`）。零依赖直驱无头 Edge（★ **`.mts` 不是笔误**：根 `package.json` 无 `"type": "module"` ⇒ tsx 把 `.ts` 编成 CJS、顶层 await 必炸），CDP `setDocumentContent` 逐张喂 HTML → 量排版（**遍历 `.frame *` 找越界盒并报最低内容底边**）→ `captureScreenshot` 写 **21** 张 1200×630 PNG 进 `packages/web/public/og/`（09-24 批次 H-1 由 14→21：＋英文目录页与六条英文词条页各一张）。★ **字号预算两档**（`OgCard.script`）：`cjk`（默认，中文五档）／`latin`（英文另一套，因为 `Retrieval` 这种长词按中文预算必溢出）——★ 缺省是 `cjk`，所以那 14 张旧卡**逐字节没变**，这一点是被 sha256 实测核过的不是推定。★ **它是「提交资源」这条口径的另一半**：产物签进仓、不进构建流程，跑它是**改完语料后的人为动作**（有越界即 `exit(1)`，不会静默截断）。⚠️ 与真库探针不同：**不连后端、不碰库**，纯渲染 |
| `tools/probes/settings-platform-cdp.mjs` | **「一键默认设置 ＋ 模型下拉」真机渲染探针**（2026-09-21 一键默认设置批收口 v0.2.101 入库，零依赖同上一行；**22 条断言**（v0.2.101 时 17 条，v0.2.102「二次确认」批 **17→22**：按钮改两段式后，探针**必须跟着走完整确认流程**——`一键默认设置` 单击**不再**触发配置；若发现 A9b/A9d 红而 A9-0 绿，先怀疑**探针自己**还在按旧的单击语义写））：清偿「设置页那个按钮点下去，绑定表到底跟不跟着变」——本仓 `.tsx` 虽有 jsdom，但那一层**没有真 fetch、没有真后端、没有真 CSS**，而本批要证的恰恰是「点一下 → 落库 → 重新拉取 → 行上真的变了」这条**跨进程链路**。★★ **它是本批唯一有判别力的手段**：本仓此前已有 **8 条** `RoleRow` 渲染锁、全绿，却没有一条能拦住那个真 bug（点「一键默认设置」后提示说「已配好 8 个角色」，而行上仍显示旧服务商 + 旧模型）——**根因是 `useState(initialProvider)` 只在挂载时取一次值，jsdom 在「只渲染一次」的写法下永远看不见**。★ **自带三件套**（全随机端口）：① 一个**只实现 `/v1/models` 的本地假上游**（证明「服务商有候选时那一列真会变成下拉」——免费通道没配 key 时它恒为空，这条只能靠假上游造出来）；② 一个**隔离后端**（`SB_DATA_DIR` = `mkdtemp`，**绝不碰真实库**）；③ 一个 vite 开发服。★ **副作用已在文件头显式声明**：本探针会**真写库**（建 1 个 provider、改 1 条 role_binding、点一次一键默认设置），故只对隔离实例跑；**不调任何真实模型**（假上游只回模型名列表、不接 chat）。★ **断言口径**：A1 应用壳渲染出来 ／ A2-A4 卡片与额度行在场且按钮可点 ／ **A5 有候选的行渲染真 `<select>` 且候选里有假上游返回的模型名** ／ A6 「（用默认模型）」选项在场 ／ **A7 绑平台通道的行是输入框**（＝未配 key 时的真实观感）／ **A8 反向：没有任何「自定义…」手填框**（空模型不许被判成自定义）／ **A9a 在途态**（按钮转「配置中…」且禁用）／ A9b 提示文案报出角色数 ／ **A9c 按钮复位解禁**（不是只能点一次）／ **A9d 8 行全部改绑平台通道**（下拉消失 ⇒ 全变输入框）／ **A10 覆盖语义实证**（某角色从「假上游 + 指定模型」被重置为「平台 + 留空」）／ A10b 真落库（8 行 `model` 全为空串）／ **A11 反向：卡上没有任何「显示/复制密钥」入口**。★★ **首轮 14/17，三条红里只有一条是真 bug**：A9d/A10 的 `null` 与 `? 行` 是**探针自己**把 `GET /roles` 的 `{roles, bindings}` 当成数组读、字段还是 snake_case `provider_id`——**探针的错要自己认，不能记到产品头上**（本仓 `coach-cdp.mjs` 那条「探针红的第一反应该是『谁的真值更可信』」的又一例）。修后 **17/17 全过、连跑两遍**。★ **两个环境坑已写进文件头**：① `npm run dev:web -- --port N` 会把 `--port N` 当**位置参数**传给 vite（`vite 5199`）⇒ 端口被忽略、vite 抢 5173；改为在 `packages/web` 里直接 `npx vite --port N --strictPort --host 127.0.0.1`（`--host 127.0.0.1` 顺带避开「只绑 IPv6 的 localhost」那个陷阱）。② 写请求**必须带 `Origin`**（`security.ts#originCheck`），Node 的 `fetch` 默认不带 ⇒ 播种时撞 403 `Forbidden: missing or disallowed origin`。★ **一条「现状事实」必须带走**：`GET /api/providers/:id/models` 要拿 **key** 问上游，而**免费通道的 key 只在服务端 env** ⇒ **没配 `SB_PLATFORM_*` 时平台服务商恒返回空候选**，绑到平台通道的那几行**渲染的是输入框、不是下拉**（A7 锁这条）——即「直接用选项选」在**配好 key 之前界面上看不见**；配好后同一行会自动变成下拉。用法 `node tools/probes/settings-platform-cdp.mjs`（前置：本机有 Chrome/Edge ＋ 仓内 `node_modules` 已装；CDP 端口随机、被占即 `exit(2)` 拒绝跑；截图落 `SB_SHOT_DIR`，**不入仓**） |
| `packages/shared/src/review-goal.ts` + `packages/server/src/learning/review-goal.ts` + `packages/server/src/learning/review-queue.ts` + `packages/web/src/features/terms/ReviewGoalCard.tsx` | **自定义复习目标（2026-09-21 v0.2.94 新建，契约 `docs/EBBINGHAUS-SPEC.md` v1.2 §10，**零迁移零新依赖**）**：用户自己定「今天复习多少张」＋优先领域多选，队列优先出指定领域、没凑够就把复习过的再复习一遍。★ 一句话：**把「今天复习多少」的决定权从遗忘曲线手里交回用户**——代价是必须先堵上「内置作弊器」。★ **四件分工**：`shared/review-goal.ts` 是**前后端唯一一份**目标归一（`normalizeReviewGoal`／`reviewGoalActive`／`reviewGoalProgress`；★★ `count = 0` 是**合法「关闭」值**而非「没设」，且**不做 `Number()` 宽松转换**——`'30'` 也回 0，猜一半比不猜更危险；返回新数组**不污染默认常量**）；`learning/review-goal.ts` 只做 KV 读写（`app_settings` 键 `review_goal`，`ON CONFLICT(owner_id,key) DO UPDATE`）；`learning/review-queue.ts` 是队列构建（**从 `term-review.ts` 拆出**——加完三段补位后后者会顶破 400 行红线）；`ReviewGoalCard.tsx` 是设置卡（★ **以服务端回写值刷新本地**——输 9999 显示 200，服务端钳位的结果才是真相源；**设 0 ＝ 关闭目标**）。★★ **三段补位顺序不可换**：① 真账（due/overdue）→ ② 提前背（upcoming）→ ③ 重复巩固（今日已打卡），拼接后截断到目标数。★ **把「提前背」排在「重复巩固」之前是比需求原话多出的一层**：未到期的收益高于已背过的，这才是「曲线太慢」的正面解法。★★ **领域优先只作用 ②③ 段**——真账段排序**逐字不变**，否则 §5「先还旧账」会被稀释成「勾个领域就能把别人的老账挤到后面」；且**优先是排序不是过滤**（做成过滤会让目标数永远凑不满，而凑不满正是本功能要解决的病）。★ **两个出口而非一个**：`listDueQueue`（只真账，给督促小窗——**用户的日目标不该让教练去催「提前背」**）与 `reviewQueue`（带补位），保证督促行为逐字不变。★★ **P0：同日只推进一次闸门**（`term-review.ts` 的 `markReviewed`）——原实现每次都推进 `stage`，配上目标数后就是**内置作弊器**（一天刷 7 遍从 stage 0 直接毕业，60 天计划 20 分钟刷完）；闸门**只挡推进**，`remembered = false` 照旧归零。★ **破坏性变更**：`GET /api/terms/review/queue` 由数组改对象 `{items,goal,doneCards,doneTerms,poolSize}`（进度与队列必须同源），调用方只有 `ReviewPanel` 一处、无外部消费者；新增 `GET/PUT /api/terms/review/goal`。★ 测试 **43 例**（`review-goal.test.ts` 17 ＋ `review-queue.test.ts` 16 ＋ `ReviewGoalCard.test.tsx` 10；另 `routes/term-review.test.ts` 17→26） |
| `packages/server/src/chat/vision-error.ts` | **「看图失败」上游报文翻译器（2026-09-20 v0.2.91 新建，纯函数零依赖零网络零 DB）**：`explainVisionFailure(err, model)` 把上游英文 JSON 400 翻成用户能懂、**能据此动手**的中文指引。★ **存在理由＝老板真机报告**「我使用视觉模型它提示 400」：根因是**运行态配置**（`vision` 角色绑成了 `agnes-image-2.5-flash`——那是**生成图片**的模型、只收 `/v1/images/generations`），但**代码有第二条洞**——`chat/vision.ts` 的流式循环**自 v17 落码起裸奔无 `catch`**，异常直冒到 `flow.ts` 的 `chat-error` 分支，而那里刻意「原样外发上游 message」（本意「别吞真信息」，效果「把内部事实当用户文案」），违反 ADR-5「失败必须可读、可重试」。★ **八分支按「最具体的排前面」定序**：生成模型误绑／不支持图片输入／404／401·403（失效·欠费·无权限——症状与「模型不可用」极像，**必须分开报**）／429（给「稍等十几秒再发」而非「改设置」，**给的是可执行下一步**）／5xx／超时／**兜底不猜病因**。★ **每条末行必附 `上游原文：`**（压平空白、截 240 字）——**翻译不是掩埋**，排查仍要有现场。★ 接线仅一处 `try/catch`，**不改错误类型、不改 SSE 帧形**。★★ 含**反向锁**：遮掉「是 image 模型」后**不得**再判成生成模型——否则该分支会因正则里别的东西常绿，而它声称在判的那件事没人验过 |
| `Dockerfile` + `docker-compose.yml` + `.dockerignore` + `tools/docker/` | **M3 部署容器化批（2026-09-20 在途，未实机验证）**：单镜像跑 api + 静态 web，SQLite 数据卷与 `DATA_DIR` 外挂。★ `tools/docker/部署切换手册.md` **自述 v0.1「全部步骤未实机验证」**；`tools/docker/daemon.json` 为宿主 docker 配置样本。⚠️ **本批未随附任何自动化测试**（容器层不在 vitest／gates 扫描范围），「跑不跑得起来」只能真机验——见 `docs/dev/launch-plan.md` §3 部署闸门 |
| `packages/web/public/`（`favicon.svg` / `robots.txt` / `.well-known/security.txt` / **`og/*.png` ×21**） | **静态公开资源目录（2026-09-20 在途新建）**：站点图标、爬虫协议、安全联系声明；`packages/web/index.html` 同步加 `<link rel="icon" …>` 一行。★ `security.txt` 走 RFC 9116 的 `.well-known/` 约定路径，**与 `robots.txt` 一样只在独立部署形态下可达**（本地 vite 下同样能取到，但无对外意义）。★★ **`og/` 那 21 张 1200×630 是「提交进仓的生成物」**（2026-09-23 v0.2.116 批次 F-3 起 14 张，09-24 批次 H-1 加英文侧 7 张＝21）：生成器＝`tools/probes/og-card-cdp.mts`（`npm run og:shots`），★ 之所以不走构建期——**发版 tar 与 CI 里都没有浏览器**；代价＝改了语料不重跑就是旧图，测试只拦得住「引用与尺寸」拦不住「内容陈旧」 |
| `docs/DEFAULT-SEED-SPEC.md` | **默认种子契约 v0.1 设计稿（**未实施**，2026-09-20 在途新建）**：把 `seedIfEmpty` 的落点从 `openai-default` 换成 `agnes-default`、七角色 `model` 预填 `agnes-2.5-flash`。★ **文件头自述「本稿只读代码写成，未动任何实现文件（`router.ts` 等归在途批次）。CHANGELOG 条目随实施批次同批补（多会话红线）」** ⇒ 阅读时按**设计意图**而非现状事实对待 |
| `docs/DOC-RAG-SPEC.md` | **文档检索契约**（2026-09-06 对 L1 5.0 §5.1「整篇直塞」的改判）：分流规则、三种消费方口径（对话按提问检索／出题按主题检索／抽词条按位置均匀覆盖）、常量取值理由、**两条被实测否掉的阈值方案**、已知局限与未验账 |
| `docs/QUIZ-SEARCH-SPEC.md` | **出题联网检索契约**（**v1.1.0**，2026-09-13）：`QuizSearchReport`／`QuizRef` 数据结构／三入口开关口径（题库页·对话页默认开、PK 硬编码开）／检索词派生／**注入段「素材不是指令」原则**／失败三层降级（ADR-4）／`failure` 真因拆分（修「未配模型」误导报错）／出题专用模型参数（温度 0.4 + 输出上限白名单抬高）／**§2.8 来源标注（模型只给编号、URL 由服务端映射）**／§2.9 前端渲染（RefList + refsList 二选一）／未验边界 |
| `docs/RESOURCE-SPEC.md` | **学习资源现场搜集契约（v0.2，2026-09-18 立约同日批1落地）**：运行时闭环六步（检索→抓页→逐字摘题→**verbatim 锚点锁**→preview/commit 两段人工确认→`saveQuiz(quiz,'collect')`）。★ 与 QUIZ-SEARCH 的分界线＝**「摘录不是创作」**（那条用检索结果启发 AI 出题，这条收割网上已存在的题，模型只当结构化解析器，温度 0.2）。硬约束四条：外部结果永不直接写库／单页不遍历（不是建爬虫）／出网全走 `fetchSafe` 且页抓自设 15s 超时／来源 URL+标题随题落库可回查。D1~D4 拍板记录（锚点锁严格版起步／题库页先行／批1只收题、批2 资源 shelf + 对话工具／首版不设白名单）；§3.2 批1偏差注记（实装 2 条搜集词，「历年真题」刻意未上）；§10 未验账（T3 探针须隔离实例、正文可达率未实测） |
| `packages/shared/src/choice.ts` | **方案选择框契约的单一事实源**（2026-09-14 新建，151 行）：`ChoiceOption`/`AskChoiceRequest`/`AskChoiceReply`/`AskChoiceRecord`/`ChoiceStatus` + 边界常量（`CHOICE_MIN_OPTIONS=2`/`CHOICE_MAX_OPTIONS=4`/`CHOICE_QUESTION_MAX=500`/`CHOICE_LABEL_MAX=120`/`CHOICE_DESC_MAX=300`/`CHOICE_CUSTOM_MAX=1000`）+ `choiceOptionId(index)`（**id 恒为 `o1..oN`，由下标派生不信模型**）+ `describeChoiceReply` + 两个归一纯函数。★★ `normalizeChoiceInput`/`normalizeChoiceReply` **返回 `{ok:false,error}` 而不抛异常**——错误文本要能回灌给模型自纠（抛异常会变成「工具执行失败」） |
| `packages/shared/src/quiz-weak.ts` | **薄弱点分析契约的单一事实源**（2026-09-15 新建，契约 `docs/QUIZ-WEAK-SPEC.md` v1.1）：`WeakPoint`/`WeakFailure`/`WeakAnalysis` + 边界常量（`WEAK_MAX_POINTS=4`／`WEAK_TOPIC_MAX=40`／`WEAK_REASON_MAX=200`／`WEAK_SUGGESTION_MAX=300`）+ **`normalizeWeakPoints(raw, questionCount)` 归一闸门**。★ **为什么收进 shared**：`WeakPoint` 此前只在 server 的 `learning/quiz.ts` 定义、前端 `QuizBankPage.tsx` 手抄了一份结构等价的内联字面量——加一个字段要改两处，漏一处就是「后端发了新字段、前端不认」的静默降级（与 `TaskItem` 曾有三份定义同源的老病）。★ **三道丢弃规则**（模型输出一律不可信）：三要素缺一即丢／越界题号过滤后一条不剩即丢（**题号是分析唯一能落地的锚点**）／跨条目去重（聚类是对错题的**划分**）。★ `WeakAnalysis.analyzed` 与 `fallback` **是两件事**——0 是「还没做题」（正常空态），`fallback` 才是降级 |
| `docs/QUIZ-WEAK-SPEC.md` | **薄弱点分析契约 v1.1**（2026-09-15 新建，同日升 v1.1）：改判动机（旧实现是纯本地规则 + `analyzer` 角色空转）／数据契约与归一闸门／**数据来源三合流**（统计 + 题干 + 错选快照，及「错选只在带 `answer` 提交时才有」的边界）／提示词口径三条硬约束／失败三层降级与真因／前端渲染口径（三态 + 降级标注 + 多主题列表）／常量取值理由／**§8 本版未做**（不缓存、不走 SSE、GET 带 LLM 副作用的取舍、不落库、不做跨题库）／**§9 影响面**（★ 已更正：`quiz.ts` **刻意不做 re-export**，会构成循环依赖）／**§10 验收状态**（三分支探针证据齐 + 明确「**算不算顺眼**未验收、判定权在老板」） |
| `docs/ASK-CHOICE-SPEC.md` | **方案选择框契约 v1.0**（2026-09-14）：形态（输入框上方浮层）／数据契约／三个 SSE 事件（`choice-asked`/`choice-replied`/`choice-cancelled`）／数据流图／**逃生口三路**（停止生成·删会话·进程重启，事后可恢复而非事前拦截）／边界与**本版未做**（答复留痕不入消息流——会破坏 OpenAI 消息配对约束） |
| `docs/MEMORY-SPEC.md` | **长期记忆契约 v1.0**（2026-09-15 新建，**机制照搬 Pi 的 Compaction + AGENTS.md，不引向量库/不引新依赖**）：§1 现状取证（根因＝`truncateHistoryToBudget` 是**纯截断**，数据在 `messages` 表里没丢、丢在**组装时**）／§2 形态（**与 Pi 的三处刻意分歧**：异步不阻塞、学习版摘要模板、画像搭车压缩同一次 LLM 调用省一次配额）／§3 数据契约（v16 迁移 DDL + 类型 + 常量取值理由）／§4 第一层会话内压缩（切点算法／分级降级／`alignToolRoundBoundary` 复用）／§5 第二层跨会话画像（upsert 幂等／恒注入不检索／真删淘汰）／§6 数据流／§7 逃生口与降级／§8 安全风险（P0 无／P1 提示注入·画像污染／P2 本地存储）／§9 测试计划／§10 落点清单（**含 `flow.ts` 触线的预案**）／§11 本版未做（前端页面·压缩提示条）／§12 未验账 |
| `docs/STUDY-FLOW-SPEC.md` | ⚰️ **墓碑**——原 **学习流契约 v1.0**（2026-09-16 新建，v0.2.27）：§1 三层架构（步骤注册表／控制流／知识数据图）+ **§1.1「图静态、流动态」**（读 LangGraph `StateGraph` 源码得出：`addNode`/`addEdge` 必须 `compile()` 前写完 ⇒ 用户编排的流**当数据**由固定运行器解释执行，**不建成图结构**）+ §1.2 与 n8n `INode` / Dify `WorkflowRunHistory` 的逐条对应 + §1.3 范围红线（用户可自定义「选哪种+填什么参数」，不可造新交互）／§2 定义层（含**复合主键就地修正**的理由）／§3 节点与邻域（**只存引用+抗删快照**、`truncated` 不冒充全图）／§4 边与出处分层（**三值不平权**）／§5 执行器（`awaitsUser` = LangGraph interrupt；**委派既有 `handleMessage`，零新增 LLM 调用路径**）／§6 运行器（**步进式**、`def_snapshot` 不可省、上限与失败口径）／§7 待接清单／§8 安全（**手工边 origin 强制 user**）／§9 路由与状态码／§10 影响面／**§11 验收状态**（58 例 + 全仓回归全绿；**真实模型端到端与前端均未做**，如实标注）/§12 本版未做/§13 未验账（时间窗定位法的秒级边界、`deriveDomainEdges` 的 O(n²)、前端超时未定）。★ **附「本文件与代码的章节号对应表」**——代码注释里写死了 `SPEC §n`，改本文件前必须核对该表 |
| `docs/dev/test-plan.md` | **§0.8 的强制载体**：测试策略/基线用例数/逐文件不变量/已发现 bug 登记。改代码不同步本表即违规 |
| `docs/dev/bug-ledger.md` | **§0.15 第 0 步的数据源**：只记反复/修不动的 bug，首位字段是**收敛计数**（1=正常修／2=必须换根因假设／**3=禁止第 4 次同向尝试**） |
| `packages/web/src/seo/`（`paths.ts` 四个常量 `CATALOG_PATH`／`CATALOG_PATH_EN`／`CHANGELOG_PATH`／`FEED_PATH` / `term-corpus.ts` 唯一事实源＋URL 拼装 / `term-entries-a.ts`＋`term-entries-b.ts` 语料各 6 条 / ★ **英文侧另起三件**（`term-corpus-en.ts` 自己的类型＋hreflang 簇 / `term-entries-en.ts` 六条，★ 每条挂一条 `zhSlug`＝**配对关系的唯一事实源**，中文侧凭它反查 / `term-page-en.ts` 渲英文词条页与英文目录页）——★★ 类型**故意不与中文共用**（中文那套字段锁的是中文口径：`productHint` 必出现「本产品」、description 预算 105 字……英文一条都不成立，共用＝两边各写一半合法字段），但**外壳 `page-shell.ts` 与卡生成器 `og-card.ts` 仍共用**（样式抄两遍就会漂） / **`page-shell.ts` 三份独立文档共用的一层壳：head＋样式＋顶栏＋页脚（09-24 从 `term-page.ts` 拆出，★ 样式只在这里一份）** / `term-page.ts` 纯函数出词条页与目录页 HTML / **`og-card.ts` 分享卡文案＋`og:image` 那四行 meta** / ★ **`changelog-public.ts` 对外更新口径唯一的手写清洗表**（渠道 C8；⚠️ **不是**从内部 `CHANGELOG.md` 脱敏来的，发版要记得回来加一行；★ **当前 10 条**＝v0.2.109～v0.2.118，最后那一行是 09-24 发版当批补的 ⇒ **「仓内比线上多一条」从此是正常状态**，别当回归失败）/ `changelog-page.ts` 把同一份数据渲两遍＝公开更新页＋`atom.xml` / ★ **`public-hygiene.ts` 「公开字节不许出现的字样」词表单点** / `ssg.ts` 落盘（**当前 23 件**＝09-24 批次 H-1 由 16 件 ＋英文目录页 ＋6 条英文词条页）/ 六份 `.test.ts`） | **词条长尾 SEO 静态页（2026-09-22 v0.2.111 新建，契约 `docs/SEO-SPEC.md`，零迁移零新依赖，server/shared 零改动）**：构建期由 `vite.config.ts` 的 `sb-seo-static-pages` 插件（`closeBundle: { order: 'post' }`，★ 必须是 post——vite 自己会清并写 `dist`，早跑等于白写）把 12 条学习科学词条渲成 12 个独立 HTML ＋ 目录页 ＋ `sitemap.xml`。★★ **三条必须知道的口径**：① URL **必须带 `.html`**——线上 Caddy 对无扩展名路径一律 `try_files → /index.html`（实测 `/terms/1234` 与 `/` 返回**同一份 SPA 壳、md5 相同**），不带扩展名就永远拿不到静态页——**目录页同理**：`/terms/` 线上兜成 SPA 壳（1487 字节、正文全空），真目录是 `/terms/index.html`（5705 字节），所以指向目录的链接（页内导航／`sitemap`／canonical／SPA 侧两处入口）**一律从 `paths.ts` 的 `CATALOG_PATH` 拼**，SPA 与构建两侧共用同一常量、不许各写一份；② 页面**零 `<script>`、整页只允许一个 `<link>`（canonical）**，不引 SPA 产物 ⇒ 关 JS 也读得到正文，加脚本就是破坏这个契约（有锁守着）；③ 语料**不许出现使用量数字／效果承诺／第三方背书**，`term-corpus.test.ts` 三条红线机器守——真实计数（批次 B）尚未上线，写数字就是造假，而这些页正是全站唯一对外宣称事实的地方。★ 词条 `slug` 用拼音、`related` 只能指本表内 slug（悬空即红）。★ 改这里必须同批改 `sitemap.xml` 的条数（当前 **21**＝首页＋中英两个目录页＋12 中文词条页＋6 英文词条页；★ 公式 `PUBLIC_TERMS.length + PUBLIC_TERMS_EN.length + 3`（首页＋两目录页），`ssg.ts` 每次构建现算；★ 守它的是 `og-card.test.ts` 那条「每张卡 ↔ sitemap 里恰好一条 `<loc>`、双向都成立」——英文侧一进来，原先「14 张＝14 条」那个巧合就断了）与 SEO-SPEC §3 计数；⚠️ **`sitemap` 的 `<loc>` 数与落盘件数不是一回事**——09-24 起落盘 16 件（多的是更新页与 `atom.xml`，★ 两个都刻意不进 sitemap：页会天天变、且没有分享卡），`changelog.test.ts` 与 `term-page.test.ts` 各锁着「`CHANGELOG_PATH` 不在 sitemap 里」。④ ★ **分享卡（2026-09-23 v0.2.116 批次 F-3）不另写一份文案**：`og-card.ts` 的卡面字段全部从 `term-corpus` 取（`title`／`alias`／`oneLine` ＋ 底部那行由 `pitfalls.length`／`actions.length` **实算**），`term-page.ts` 的 `head()` 与首页壳 `index.html` 的 og 块都对它同源；★★ **21 张 PNG 在 `packages/web/public/og/` 是提交资源、不是构建产物**（09-24 批次 H-1 由 14→21：英文目录页＋六条英文词条页各一张；★ 那 14 张旧卡**逐字节没变**，是 sha256 实测核过的不是推定——卡面字号预算按文种分了两档 `cjk`／`latin`，缺省 `cjk`）（发版 tar 与 CI 都没有浏览器），改了语料要重跑 `npm run og:shots` 才不会是旧图——`og-card.test.ts` 守「文件在不在＋IHDR 尺寸对不对＋引用对不对得上」，**守不住「图内容新不新」**；⑤ ★ **公开字节是一条红线，不是一句风格**（2026-09-24 批次 G-3，渠道 C8，★ **v0.2.118 已发版上线**＝这一条红线现在跑在公网那侧而不是只在仓内）：这一族文件（含手写的 `packages/web/index.html` 与 `public/robots.txt`）**逐字就是公网字节**——vite 构建**不剥 HTML 注释**，所以施工叙述写进去就是发出去，服务器路径／端口／systemd 单元名／内部挂账词汇一律不许出现在这一层（★ `page-shell.ts` 的文件头注释里因此只放一句指针，不放施工理由）。锁＝`public-hygiene.test.ts`（词表在 `public-hygiene.ts` 单点，扫手写壳＋`public/` 静态件＋全部构建期渲染产物，★ 它第一次跑就红在 `robots.txt` 一处既有注释泄漏上）；⚠️ **两条明说的缝**：不含 JS bundle，且**「发版要记得往清洗表加一行」机器拦不住**（★ 为什么不补一把反向锁：加了「内部 `CHANGELOG.md` 有而清洗表没有＝红」，它会把**先写进内部账本的拟号**天天判红，而拟号晚于发版进表是常态 ⇒ 天天红的锁等于没有锁；这一头只有人守）⇒ 详见 `SEO-SPEC.md` §5 第 13 条与 §6.3 末段（★ 09-24 发版当批这条纪律**执行了一次**：清洗表补了 v0.2.118 自己那一行）。⑥ ★ **多语言不是加一份文件，是三条代价**（09-24 批次 H-1）：① **`zhSlug` 是配对关系的唯一事实源**，只写在英文侧、中文侧凭它反查——两边各写一份就会漂，而漂了的 `hreflang` 不是「少了个标签」，检索侧判**整簇作废**（＝两边各白做一次）；② **写了就得「自指＋互指」**，只指对方不指自己等于没写；③ ★ **没配英文的中文页一条 `hreflang` 都不写**（此刻 12 条中文里 6 条有英文版）——「只声明自己是中文」合法但什么都不解决，留着只会让下一个读这页的人以为有对应版本；⚠️ 这条「不写」目前**只有单元层一把锁**守着（09-24 §7 改坏取证实测：改成「无配对也写一条自指」⇒ 恰好 1 红，渲染层那 17 例全绿＝没有第二把）；★ `x-default` **刻意不写**（两语侧各 1 条，写了就把「默认给谁」这个我们并没有决定的事说成决定了）。英文侧另有一条**必须用英文重写一遍**的诚实红线（`guarantee`／`improve your memory`／`never forget`／百分比——中文词表一条都盖不到它，09-24 实测注入一句即红） |
| `packages/web/public/robots.txt` | **抓取面闸门（2026-09-22 v0.2.111 由「全封」转「半开放」，拍板人＝老板）**：`Disallow: /` ＋ 只用 `Allow` 精确开门（`/$` 首页、`/terms/`、`robots.txt`/`favicon.svg`/`sitemap.xml`、★ **`/og/` 分享卡**（2026-09-23 v0.2.116 加：社交平台抓图不看 robots，**Google 图片抓它看**，不放行就是自己挡自己的图）、★ **`/changelog/` 与 `/atom.xml`**（2026-09-24 v0.2.118 批次 G-3 加：公开更新页与订阅；`public-hygiene.test.ts` 最后一例就是查这两扇门有没有开——**忘了加不报错，只是静默不公开**））。★★ **口径是「默认全封」而不是「默认全放」**——好处：应用里新增客户端路由不会被顺手放给爬虫；代价（★ 必须记住）：**以后任何新公开面要记得回来加 `Allow` 行**，忘了就是静默不被收录。`Sitemap:` 行与 `sitemap.xml` 互相指认。★ `public/` 下的文件由 vite 原样拷进 `dist`，随 `deploy.sh` 的 tar 上线，无需服务器改动 |
| `docs/SEO-SPEC.md` | **词条长尾 SEO 契约 v0.1.12**（2026-09-22 新建，09-23～09-24 九批改锁；★ v0.1.9＝09-24 批次 H-1 英文侧词条页落码（拟 v0.2.119、⚠️ **未发版** ⇒ §6 第 11 条那五条线上判据此刻为空）：公开面从 16 件变 **23 件落盘**、**`sitemap.xml` 的 `<loc>` 从 14 变 21**——本册 §3 与 §6 第 5 条那两处字面「14」作废，判据改成「读自语料条数」；★ hreflang **不是全局开关，是一簇一簇的**（英文只有 6 条 ⇒ 没配英文的那 6 条中文页**一条都不写**，见 §5 第 14 条）；★ 分享卡排版从此分两档 `OgCard.script: 'cjk' \| 'latin'`，默认 `cjk` ⇒ 已提交的 14 张中文卡逐字节未变）｜前值 v0.1.8＝09-24 08:48 发版 v0.2.118 后加的 **§6.3 发版实录**——§6 十条判据第一次全在线上跑过）：三处老板拍板口径（robots 半开放／学习科学概念 12 页／`.html` 扩展名）连同**各自被否掉的候选与代价**、构建期机制图、§4 三条红线各配哪把锁、§5 已知代价十四条（✅ 目录页地址已线上实测并改指 `/terms/index.html`；无 JSON-LD；🔵 产品内两处入口批次 E 已落码未发版；`lastmod` 取构建日；★ 词条页零 `<script>` ⇒ **GoatCounter 数不到它们**，只能看 Caddy 日志；★ 中文长尾卡在「百度从没来过」，Googlebot 每 1～2 天来；★ **站长验证文件会被本站「默认全封」的 robots 挡住**，必须同批加精确 `Allow`；★ **不存在的 `/terms/*.html` 返 200 SPA 壳而不是 404** ⇒ 验收只看状态码等于没验；★ **分享卡 PNG 是提交资源** ⇒ 改语料不重跑 `npm run og:shots` 就是旧图，机器拦得住引用与尺寸、拦不住内容陈旧；★ **公开页带施工注释**＝第 12 条，✅ 已手工清空并由锁守着；★ **第 13 条＝更新页那张清洗表要每次发版手工加一行、机器拦不住**；★ **第 14 条＝英文只有 6 条而中文 12 条 ⇒ hreflang 按簇写、孤立一侧写了反而判整簇作废**）、§6 上线后十一条 curl 判据（含反向判据、「按 UA 剔掉我们自己」的 grep 写法、★ 第 8 条分享卡要核 `content-type: image/png` ＋ 字节数、★ 第 10 条更新页与订阅四条**且都不许只看状态码**、★ 第 11 条英文页五条**未发版故此刻为空**）、§7 维护口径。★ v0.1.12（09-25）＝§5 第 12 条把「公开字节」补到 `dist` 的 JS／产物那一路（逐处点名豁免＋豁免当场有效锁），CI 步序改 build 先于 check。★ 加/改/删词条页前先读 §4 与 §7 |
| `docs/GROWTH-CHANNELS.md` | **免费增长渠道台账 v0.1.12**（2026-09-23 新建，拍板原话「目前这些渠道都要做下去」；★★ 09-23 23:5x 五批发版后 C2／C4／C5 三行已回标「已上线」，本行旧说法「C4＝后端已落码未发版」就此作废，见 `docs/GROWTH-SPEC.md`；★★ 09-24 08:48 v0.2.118 发版后 **C8 转 🟢「已上线」**（上一格还写着 🟡「已落码未上线」，作废见本表 §4 v0.1.10 那一行），做法与建表那条不同且是刻意的——内容源＝手写清洗表而非内部 `CHANGELOG.md` 脱敏；★ 该批同时执行了一次「发版要回清洗表加一行」这条人守纪律 ⇒ **仓内 10 条／线上 9 条**是正常状态；★ **v0.1.12＝批次 H-2：C6 从 ⚪ 转 🟡（素材与口径齐、等第一条真贴文），并把两条「我们一直在用的说法」打折**——① 仓里那六张真机截图**是旧的且当场看得出**（`landing-hero.png` 图上 149 个 REST 接口 vs 现读代码 150），② §3 那条「AI 探针只在本地栈跑」的约定**挡不住污染**（beacon 是绝对地址，localhost 也打到 `/stats/count`））：C1–C10 十个渠道各写「产品侧还要做什么／老板本人动作／**可证伪的验收判据**」三格，§0 三条凌驾口径（**「做能力」＝做进产品而不是 AI 代做动作**：发帖／提交／站长平台都是老板账号动作；数字只许放宽口径不许改标签；度量可信是渠道前提）、§2 批次 E/F/G/H 顺序（按依赖不按编号，★ 不预授权发版）、§3 **排除清单**（哪些量是我们自己造的：`u-demo-shared` 全部行、远端探针测试号、UptimeRobot、伪百度扫描器、`curl/` 与 `studentbuddy-probe` UA，★ 以及剔不掉的「AI 真 Chrome 打线上」只能用约定挡——⚠️ v0.1.12 起这条约定**已不完整**，见上一格）。★ 开任何新渠道前先在这张表成行 |
| `packages/server/src/growth/`（`counters.ts` 写侧与读侧同文件）＋ `packages/server/src/routes/growth.ts` ＋ `packages/server/src/storage/migrations-list-v41.ts` ＋ 三处采集点（`routes/auth-github.ts` 的 `/providers`、`routes/auth.ts` 的 `demo-login`／`register` **成功分支**） | **诚实增长计数（渠道台账 C4）后端（2026-09-23 批次 F-2 新建，拟 v0.2.114，契约 `docs/GROWTH-SPEC.md`；★ 唯一一处迁移＝v41，零新依赖，★ **本批页面什么都不画**——老板 09-23 定「只做后端、暂不显示」）**：`app_open`／`demo_enter`／`register_done` 三个动作按**「同一来源 IP 当天只算一次」**记进 `growth_action_day`，`GET /api/growth/counters` 公开可读。★★ 五处读代码时最容易误判的地方：① **计数只写在 HTTP 入口层、且写在成功之后**——这是「这数敢不敢对外报」的承重墙：种子内容走 `saveOneTerm`（不发事件、无入口）⇒ **C4 结构性数不到官方灌的东西**；一旦有人把 `recordGrowthAction` 挪进领域层或挪到闸门之前，它立刻开始把**系统自己造的行数成访客**；② **探针在写入前就被丢掉**（`X-SB-Probe` 头 **或** `studentbuddy-probe/` UA，两通道各自成立），★ 另有**反向锁**钉住 `curl/`、`node`、`Go-http-client`、**空 UA**、`UptimeRobot` 一律照记——剔的是「认得出自己」不是「白名单外的人」，写反了会把真实访客剔光而测试全绿；③ `bucket`＝`sha256(盐, ip)` 前 16 位、去重键 `PRIMARY KEY (kind, bucket, day)` ＋ `INSERT OR IGNORE`，盐由 **v41 迁移以 `randomblob` 现生成**存进 `growth_secret` 一行——★ 不取 env（本地/线上零配置）、★ **不缓存模块态**（测试里 `openIsolated` 会换库，缓存即跨库串盐）；库里**永不落裸 IP**；④ `recordGrowthAction` **永不抛**（它挂在登录路径上，抛出去＝把一次成功的登录标成失败），失败只 `console.warn` 并回 `false`，代价是「少个数」；⑤ 读端点是**公开 GET**（`index.ts` 的 `isAuthProtected` 白名单为此加了一行＝宣布那三个整数可对外，见 `AUTH-SPEC` §3）＋按 IP 限流 20/时（第 21 次 429 `GROWTH_RATE_LIMITED`）；响应带 `unit`／`unitLabel`（「次（同一来源 IP 当天只算一次）」）**随数字一起出门**，★ 显示批**不许自己编单位**——「次」≠「人」是全站口径红线。★ 测试：新 `src/growth/counters.test.ts` **15** ＋ 新 `src/routes/growth.test.ts` **8**。⚠️ ~~**未发版** ⇒ 线上此刻没有 `growth_*` 表，v41 首跑在线上未验~~（★★ **09-23 23:5x 已发版**：v41 预演水位 40→41、端点现读 200／三数全 0／`firstDay:null`；★ 而**「线上 `growth_*` 表存在不存在」这条地雷读已经排掉了，剩下那条没有**：且本机永远验不到「反代后 `req.ip` 是不是访客真 IP」（`SB_TRUST_PROXY` 未开会全站塌成每天一次）⇒ MT-18 未回收，仍待真人） |
| `docs/GROWTH-SPEC.md` | **诚实计数契约 v0.1.0**（2026-09-23 批次 F-2 新建）：§1 说清**为何现有设施数不出「有多少人来过」**（`daily_activity` 命不到入口、GoatCounter 的 139 与 86 差 38% 未核、我们自己的探针是全站最大流量源）、§2 三动作表 ＋ ★「计数只写在 HTTP 入口层」的三条推论 ＋ 去重键 ＋ 探针剔除 ＋ 隐私形状、§3 公开 GET ＋ 限流是**决策不是配置细节**、§4 **六条已知代价**（`SB_TRUST_PROXY`／IP·天是「人」的近似／按本地时区切日／记账永不抛／只数服务端可见的动作／~~未发版~~→**v0.1.1 已回标：09-23 23:5x 发版、端点 200**）、§5 六条可证伪验收（含「真人手机访问一次后 `app_open` +1」这条**只能真人跑**的）。★ 要把任何计数显示到页面上之前，先读 §4 与 §5 |
| `docs/GROWTH-COMMUNITY-PACK.md` | **中文社区贴文素材包 v0.1.0（渠道 C6，2026-09-24 批次 H-2 新建，★ 纯文档／零代码／不占版本号／不需要发版）**：与上一格那份 C7 包的分工要说清——**那一包填表单**（一个产品对一个清单站），**本包发帖**（一个人对一群读者）。§1 四条 30 秒主张**逐条标出处**（`landing-copy.ts` 的 `HERO`／`INTRO`、`term-entries-a.ts` 里那条 `pitfalls[0]` 原句、C7 包 §2）＝**没有一句新造事实**；§2 四平台卡（知乎／少数派／即刻／小红书，★ 四家的算法与外链政策**明写「本会话零实测」**）；§3 12 条中文讲解页＝12 篇贴文骨架（slug 与钩子**全部现读**语料文件，⚠️「动作」那一列只有第一行真取、其余 11 行标着「同上」＝半成品不冒充成品）；§4 **本包最有价值的一条：仓里那六张真机截图不能直接拿去发**——`landing-hero.png` 图上统计条 **149 个 REST 接口** vs 现读 `landing-copy.ts` **150**、hero 副句与第二节标题都不是 09-22 09:33 重排（`a2c49ec`）后的样子，而那次重排搬上前排的正是 C6 要讲的「**词条是主体**」⇒ ⚠️ **「缺图」会让人去补，「过期的图」会被当成证据直接发出去**；§5 只补 C6 特有的四条「不许说」（报用户量／暗示已有社区／许诺效果／代拟使用者口吻），前十三条**引用 C7 包 §5 不重抄**；★ §6 发帖登记表＝**C6 的验收判据**（此刻空着 ⇒ C6 只到 🟡 不到 🟢）。⚠️ 三条补拍硬约束在 §4 末（不许自动化打 `11wand.com`／不碰生产端口／**本机那份带埋点的壳会把一针打进线上 GoatCounter 且事后剔不掉**）。★ 改 `landing-copy.ts` 或落地页组件后要回来对账 §1 与 §4 |
| `docs/GITHUB-OPS-SPEC.md` | **GitHub 对外面维护规范 v0.1.1（2026-09-24 新建、09-25 升 v0.1.1，★ 纯文档批）**：issue／PR／tag／Release／模板这块表面的五条硬规则唯一出处——R-1 真实工作项先立 issue（现状／影响／建议／验收判据四段）／R-2 代码批走分支→PR 带 `Closes #N`（纯文档台账批可直进 main）／R-3 发版＝附注 tag（挂线上实际构建提交，不是之后的记账提交）＋ GitHub Release ＋ 本表台账行＋**对外更新表补行**四件套同批（第 4 件 09-25 起由发版脚本第 ①c 步硬核对）／R-4 对外文字零内部字样（禁批次号／渠道代号／内部台账称呼／端口／运维路径）／R-5 v0.2.71~v0.2.117 的 tag 真空刻意保留、不回填。★ `.github/` 模板四件（issue×2＋PR＋config）与本册同口径 |
| `docs/GROWTH-SUBMISSION-PACK.md` | **对外提交资产包 v0.1.0（渠道 C7，2026-09-24 批次 G-2 新建，★ 零代码／页面什么都不画）**：把「提交到导航站／清单站」这件**老板本人的动作**的前置成本降到复制粘贴——§1 身份与链接（每格带复验命令）／§2 一句话简介中·英各三候选（全部由 `index.html` og 块＋`landing-copy.ts`＋README 既有公开文案**重组**，没有一句新写）／§3 长简介 240·600·1200 三档（★ 英文档末明写**刻意没写**的四件事：不是 Anki 替代、没有原生 app、不是无限免费 AI、不是语义检索）／§4 标签池含**两条划线不贴**／**§5 十条隐私口径每条可当场复验 ＋ 一条必须主动说（内容要发给模型服务商）＋ 三条「不许说」**（端到端加密／GDPR 合规／数据 100% 不出设备）／§6 六张现成截图（★ 拍的是本地形态、零演示数据、2026-09-21）＋ 四张缺口／§7 逐平台卡（⚠️ **平台表单字段未实测**，只映射「资产↔字段类型」）／**§8 提交登记表＝C7 的验收判据，只有老板填得动**。★★ 三条对外不许回避的真实缺口就在这份文件里成文：无隐私政策页（★ 实测 `/privacy` 兜回**首页壳**且返 200——判据是「没有那一页的正文」，不是状态码也不是字节数，09-24 现读 2548 B 而该数会随壳改动而变）、**无自助注销路由**、`Dockerfile` 从未实机跑通（compose 挂着仓内不存在的 `Caddyfile`）。★ 改 README／`landing-copy.ts`／`landing-data.ts`／og 块任一处公开文案后**必须回来对账 §2／§3／§5**，否则「站内说 A、提交页说 B」 |

## 里程碑

| 阶段 | 状态 |
|------|------|
| M0 地基（脚手架/门禁/token/图标/文档骨架） | ✅ 2026-08-23 |
| M1 对话核（SSE/单轨工具/模型路由/内容块流） | 🔶 2026-08-28：SSE+单轨工具循环+角色路由真机通过；正文 Markdown + SVG 卡片 + chart 数据图内联渲染、```html 走右侧内置浏览器面板（沙箱 iframe，也可新标签页）；卡片带下载/放大，零依赖；SSE `block` 事件通道仍只服务 quiz |
| M2 练+析（出题/题库/golden dataset） | ✅ 2026-08-23：出题引擎/题库/QuizCard/逐题统计/薄弱点；**2026-09-02 加题型配比**——用户可配每题型题数（0..10，总上限 20），设置页存 `app_settings`，两处出题共用；模型没出齐时裁剪多余 + 如实告知缺哪类（不静默补题、不重试）；**2026-09-04 加出题配图**（契约 `docs/QUIZ-IMAGE-SPEC.md`）——`QuizQuestion` 可选 `svg` 字段（纯加法，老题不迁移）、设置页总开关**默认关**+模型自决、**丢图保题**三道防线（校验丢弃／整字段删除／parse 失败剥图重试救回整组题）；**同日 v1.1/v1.1.1 修「真机出图率 0」**：根因在提示词层（`svg` 没进 `[QUIZ]` 格式示例 + 劝说式负面措辞），改为字段进示例 + 正面强制口径；总开关改成服务端硬门；新增截断逐题回退与 `repairJsonBrackets` 补括号（撞 `max_tokens` 与模型漏写 `]` 都不再整组 502）；`QuizImageReport` 全链上报没图/丢图/被截断。**已真机端到端复验**（接口→落库→题库回读→浏览器渲染→清理）：4 题 3 图、实拍为切题真矢量图、控制台零 error；**同日再加「回答方式偏好」**（契约 `docs/ANSWER-STYLE-SPEC.md` v1.0）——四维偏好存 `app_settings` 键 `answer_style`，**L0** 设置页常驻卡 + **L1** 没配过时点「出题」先就地问一次（勾「记住」才落库），**默认档逐字等价现状口径**（老用户零感知）；真机量化：简短+列点 51 字 / 默认 136 字 / 详细+列点 312 字且 30 行里 26 行是列点（0.87），两维相乘生效。顺带修掉真机暴露的 LaTeX 非法转义导致整组题 502（新增 `repairJsonEscapes`：该组合 7 次里 3 次挂 → 修复后复验 4/4 成功）。**→ 2026-09-15 薄弱点分析改 AI 实时生成**（契约 `docs/QUIZ-WEAK-SPEC.md` v1.1）：旧实现是两句硬编码文案（`reason: '正确率低于 60% 的题目'`）、`fallback` 恒为 `true`，而 `analyzer` 角色虽早在 `llm/router.ts` 注册却**全仓无一行代码调用**；现改为走 `analyzer` 实时聚类出多主题薄弱点（喂统计 + 题干 + 用户错选快照），本地规则退为降级（ADR-4），`WeakPoint` 收进 shared。契约先行、单开 `learning/quiz-weak.ts`（`quiz.ts` 383/400 触线） |
| M3 忆（AI 词条库） | 🔶 2026-08-23：SM-2 调度 + 背背背翻卡；**2026-09-01 重构忆域 v2**：旧 SRS 翻卡废弃（源码删除、数据清空），改 AI 术语库——对话/手动双通道抽词入库、回复前软性注入优先使用、回复后自动计数；**2026-09-03 v2.1 词条库 AI 整理**：`tidy_terms` 对话工具（auto 全量整理 / merge 点名合并 / rename_domain 领域改名），DB 迁移 v7 加 `aliases` 列，别名感知入库防再分裂（契约 `docs/TERM-TIDY-SPEC.md`） |
| M4 反馈+迁移+定稿 | 🔶 2026-08-23：反馈环（事件总线/XP连签/今日总结/近7天趋势）+ v1 全量数据迁移工具已落；定稿未做 |
| M5 工具生态（S1 内核 / S2 内循环工具 / S3 MCP 接入） | 🔶 2026-09-06：**契约 v1.2 已立（`docs/TOOL-ECOSYSTEM-SPEC.md`）；截至 2026-09-13 已落 S1 内核「调度器」部分（并行/超时/取消，提交 `0c16251`/`54e2737`）+ `manage_terms` 直写工具（`67d2bf9`）；S1 注册表拆目录与契约字段、S2 确认门与删除撤销、S3 MCP 接入仍未开工**。**→ 2026-09-19 P2 批（test-plan v0.2.68）订正：S1 注册表拆目录与契约字段已落码**（`chat/tools/` 四件套＋三工具各自注册、kind 分档超时、schema 预闸、同轮去重、network 静默重试、tools JSON 计入预算＋16 工具上限；四工具回灌语义逐字等价，纯重构），未开工项剩 S2 确认门与删除撤销、S3 MCP 接入。拍板四项——先做内核不先堆工具、**MCP 首任客户＝自研 server**（故 stdio + Streamable HTTP 双做、`trusted` 免确认档、§6.6 自研最小实现规范）、~~确认门只对外部工具（内建 write 免确认）~~、里程碑单开本行。**→ 2026-09-06 深夜 v1.2 改判**（老板「让 term 变成 ai 自动增删改查，但改动多要向用户确认」）：确认门**扩至内建 write 并按影响条数分档**（read 永不问／≤3 条直接做／>3 条弹卡／删除一律弹／阈值可调），词条升为 **AI 全权 CRUD**，删除走 `term_delete_log` 快照**按批可撤销**，确认门机制由 S3a **提前到 S2**，S2 工具数 6→9。**S1 前置已满足**（doc-rag 已提交 `a399d5f`，深度理解批不碰 flow）**唯一残留前置＝深度理解的 v8 迁移尚未提交**（`migrate()` 按 `MAX(version)` 跳号，低号晚于高号提交会被静默跳过；建表号一律开工时 grep 取，不照契约预写的号）。现役 **7 个工具**（`search_web`/`fetch_page`/`tidy_terms`/`lookup_terms`/`upsert_term`/`delete_terms`/`ask_choice`）；`update_tasks` 走 flow exec 注入、不在 registry。★ **2026-09-20 订正（§0.11 防漂移）**：原文记「现役 **4 个工具**（…**`manage_terms`**…）」已过期——`manage_terms` 于 **P3 批（2026-09-19，契约 v1.4.1 拍板⑭）退役**，由 `lookup_terms`/`upsert_term`/`delete_terms` 接替；`fetch_page` 于 2026-09-20 新增（契约 v1.4.3 §5.3）。同批订正：本行前段「未开工项剩 S2 确认门与删除撤销、S3 MCP 接入」中的 **S2 已随 P3 批落码**，现只剩 **S3 MCP 接入** |
| M6 PK 微信答题（AI 出题双人对战，契约 `docs/PK-SPEC.md` v0.5.0） | 🔶 2026-09-09 契约定稿（与老板 grill 钉死六项——8 分钟固定时长局／P0 纯选择题／出题成功 +1、答对 +2、答错 −1、超时 −1、怠慢 −1 可累计且**分数可为负**／**全部计时在服务端**防作弊／**正确答案永不下发**给答题方／P0 局域网双人不做公网）。**P0-1 ✅ 2026-09-12 完成**：登录（`pk_users` v10 + `/auth/login`、`/auth/me` + `UserAuthBox`，提交 `e02d67a`）＋房间（`pk/room.ts` 状态机 + `/rooms` 四端点）＋SSE `pk:` 频道与 `pk-state` 广播；测试 `pk-auth.test.ts` 11 例 + `pk-room.test.ts` 19 例，全量 44 文件 586 例全绿。**P0-3 已落 a 批（2026-09-12）**：`#/pk` 路由＋大厅/等待/对局实时视图（比分、房号、时钟均为真实数据；SSE 3 次重连失败降级 2s 轮询）；**出题/答题动作区与结算页待 P0-2**（不放假按钮）。**P0-2+PVE ✅ 2026-09-13 完成**：计分引擎 `pk/match.ts`（1s ticker 时间驱动收口）＋ PVE 对手 `pk/ai-bot.ts`（AI 与人完全同口径：独立 LLM 调用只喂题干+选项、超时不占便宜、解析不出不乱猜；人机对称出题，建房可填主题方向，`PK-SPEC` §8）＋ 出题/答题两端点 ＋ `PkMatch` 动作区与结算回看。测试 `pk-match.test.ts` 15 + `pk-pve.test.ts` 7 + `pk-view.test.ts` +6，**全量 47 文件 624 例全绿**。**P0-3 收尾仍欠真机验收**（两台手机同 WiFi 微信内完整打完一局，契约 §6）。★ 本行 **2026-09-12 补登**（此前 PK 整条线在本文档无索引，属 §0.11 漂移），**「M6」编号亦为本次新增，待老板确认**。瓶颈不在代码：P1 真微信卡在**资质**（个人主体申请不到已认证服务号）＋公网 HTTPS/域名前置。**2026-09-13 二次 grill 追加（契约 v0.3.0）**：**P0-4（公共题）在途**、**P0-5（理解题＋裁判 AI）／P0-6（前端补齐）契约已立、码未开**；本行「P0 纯选择题」的旧口径**部分被推翻**——新增**理解题**＝主观判分（0~3 档），选择题仍是唯一「秒判零争议」题种；**P0-3 的真机验收欠账仍未还**。**P0-7 ✅ 2026-09-13 深夜完成（契约 v0.4.0 §11，老板当轮点单）**：主题轮转（两人各一主题按轮交替、未选主题不许开局）／出题跑题校验（裁判判贴合度，跑题 422 不占 CD、连败 3 次 −1 且给建议）／求助道具（每局 1 次，裁判当场联网、**不给答案**）／错题二次机会（解析 + 同主题相似题，答对 +2 而原 −1 保留，3 分钟 CD）。新增 `pk/judge.ts`（裁判四能力，失败返 null 不抛）与 `pk/power.ts`（道具/二次机会，因 `match.ts` 已贴 400 行门禁而拆出），新增端点 `/topic`、`/help`、`/retry`，测试 `routes/pk-power.test.ts` 11 例 + `pk-view.test.ts` +4，**全量 62 文件 / 757 passed + 1 skipped 全绿**；**真机冒烟已跑**（18791 上建双主题房→开局 `currentTopic=世界历史`/`topicOwnerId=房主`/`topicTurn=0`，三个新端点均返回域内错误码而非未知路由；**冒烟账号已自清并复核**）。**★ 同批修 23 例既有红灯**：三个 PK 测试的建房 helper 没带主题导致 `start` 全 409，修法是补主题而非放宽规则。**P0-8 ✅ 2026-09-14 完成（契约 v0.5.0 §12，老板当轮点单）**：**投降**（认输即对手胜、比分定格，与时钟归零**共用 `finishRoom`** 一条收尾路径；服务端不加确认门）+ **对战历史**（**DB v15** 建 `pk_matches`——P0 里第一处让对局落库，**视角行**／只给真人写／昵称存快照／幂等靠 `UNIQUE(room_id,user_id)`；`GET /matches` 列表 + `/matches/:id` 详情含末快照供题目回看）。新增 `pk/settle.ts`、`pk/history.ts` 与前端 `PkForfeit`/`PkHistory`/`PkReviewList`/`usePkIdentity`；测试 `routes/pk-history.test.ts` **22** 例 + `pk-view.test.ts` 20→**28**，**全量 67 文件／839 例全绿**；**真机冒烟 25/25 通过**，其中**重启后端后历史仍在**是本批唯一新增的能力（此前进程一重启对局即全丢）；**冒烟数据已自清并复核**。**★ B4 ✅ 2026-09-20 完成（契约 v0.5.x §15.4，老板拍板「整页一题」）**：情景题进对战——`PkQuizKind` 加 `scenario`，全部评分点命中 +2／任一错或未报 −1（计分同表不加档）；**criteria 结构性不出服务端**（新 `pk/snapshot.ts` 唯一快照映射点 + 序列化防泄漏锁测试）；生成复用学习引擎 `streamScenarioDraft` 但**不入 quiz_bank**（房 TTL 即焚）；新 `pk/scenario.ts` + `routes/pk-scenario.ts`（上报 taskId 白名单 + demo 页 CSP sandbox）+ 前端 `PkScenarioHost.tsx`；AI 席位不玩 demo（超时 −1 无特权）、retry 排除情景题、求助道具复用；测试 `pk-scenario.test.ts` **13** 例 + `pk-view.test.ts` +5，全量 **182 文件 / 2512 passed + 1 skipped 全绿**；未验（test-plan §6 T7）：两设备真机整局 |
| 5.0 §5 学环·**文档模式**（简易 RAG） | ✅ 2026-09-02：DB 迁移 v6 给 `sessions` 加 `doc_name`/`doc_text`（不建表，资料生命周期随会话）；`/api/doc` 三端点 + 前端 DocModeControl；`flow.ts` 注入资料段并计入预算；quiz/terms 缺材料时回退用会话资料。**同批修掉 B-001**（anthropic 丢弃第二条 system，忆域 v2 的词条注入因此静默失效）。**→ 2026-09-06 改判并落地**（契约 `docs/DOC-RAG-SPEC.md`）：旧实现不是真 RAG（>60k 直接砍尾，700k 字资料内容覆盖率实测 **0/13**），现改为≤ 60k 逐字等价直塞（golden 串锁住、零行为漂移）／> 60k 走**零依赖词法 BM25** 切块检索 Top-12 段落并声明非全文；新 `doc-retrieve.ts` + `shared/doc-rag.ts`，**零新依赖、零 DB 迁移、零新 SSE 事件**。T6/T7 已在 Node 22 实测全绿（19/18 例，全量 `npm run check` 亦绿）；**T9 真机端到端已于 2026-09-07 跑过**：合成 77,291 字／492 段讲义、冷事实埋第 423 段（86% 处），`truncated:true` 当场证明走的是检索分支，Q1 关键词型四项事实全中且逐字引用、Q2 改写型三项中，跑完已净库——★ **但模型标的「资料段 N」是本轮注入块内的相对序号、不是原文位置**（needle 实在原文件第 423 段而应答写「资料段 1」），所以上线口径只能说“可溯源到注入的第几段”，**不能说“可定位到原文”**（契约 §6.1 末行已登记此边界）；**仍欠**：T9 的老板主观确认（属 AI 忠实度类，不自行验收）、**pill 与文案的肉眼视觉**（可测部分已 DOM 实测，并已用 headless Chrome + CDP 出 6 张真页图（本批打磨后另拍 6 张 after 存 `shots/after/`；`shots/`＝AI 会话工作区目录、**不在本仓库内**，仓里 grep 不到 png 不等于没拍到，1280 与 643 两档视口，含一张专测中文长文件名被截断的）——内嵌 Browser 视图在 hidden 态被工具层拒出图，故自开无头实例；★ **图已拍到，但顺不顺眼仍归老板判**）、embedding 端点可用性（见 test-plan §6）；另有一处口径**已由老板 2026-09-07 拍定**（保持现状，不再按 `truncated` 拆态）——短资料会话的 `title` 里也带「超长资料按提问检索段落」这句，我判它是机制陈述而非状态断言，故未再按 `truncated` 分态（`DOC-RAG-SPEC` §4.1 末段已登记） |

## 已知约束

- **Node 版本必须「装依赖」与「运行时」一致**（**现役 Node 22.22.2 / ABI 127**，仓库带 `.nvmrc`）：`better-sqlite3` 原生模块产物按**安装那一刻**的 Node ABI 编译，换 Node 大版本**必须重装依赖**（删 `node_modules` 后 `npm install`），只换运行时无效——否则涉库代码全线 `ERR_DLOPEN_FAILED: was compiled against a different Node.js version`，症状是所有 HTTP 接口 500、测试大面积红，极易误判成新代码写错。历史：2026-09-02 踩到（Node 20 装的产物用 PATH 默认 Node 22 跑）；**2026-09-05 已正式迁到 Node 22**——删 node_modules 用 22 重装（prebuilt-install 直接下 v127 官方二进制，未本地编译），`npm run check` 26 文件/302 例全绿 + 隔离实例（`SB_PORT=18799` + 临时 `SB_DATA_DIR`）端到端验过建会话落库、配比 PUT 后 GET 回读一致、WAL 正常生成。**→ 2026-09-06 实测补登三处漂移**（都是当场误判的直接成因，别重踩）：① 本机确实有 Node 22，但**它在 `C:\nodejs22\node-v22.23.2-win-x64`，而 PATH 默认 node 是 `C:\nodejs20\node-v20.18.3-win-x64`（ABI 115）**——跑任何测试/服务前先 `$env:Path = 'C:\nodejs22\node-v22.23.2-win-x64;' + $env:Path`，否则涉库测试直接 `ERR_DLOPEN_FAILED: NODE_MODULE_VERSION 127 vs 115`，症状与本条开头那个坑完全一样，极易被误读成「环境不具备、只能交给用户」；② `.nvmrc` 写 `22.22.2`、实装 `v22.23.2`（同 ABI 127，不致病，但别把 `.nvmrc` 当装机清单）；③ 测试基线已到 **28 文件 / 343 例**（逐文件对账见 `docs/dev/test-plan.md` §3）
- dev 端口与 v1 冲突时用 `SB_PORT` 切换，**不杀 v1 进程**
- **文档模式：短文档直塞、长文档 BM25 检索**（2026-09-06 改判，契约 `docs/DOC-RAG-SPEC.md`）：≤ `MAX_DOC_CHARS=60_000` **逐字等价整篇直塞**；> 60k 才切块 + 按提问取 Top-12 段落（带段号、必须声明非全文）。**仍没有的：embedding 向量、跨会话资料库、持久化索引、pdf/docx 解析、可点击溯源**——要加这些需**先改契约（L1 5.0 §5.1 + L3 DOC-RAG-SPEC）再改码**，不得先写实现再补文档。词法路线的已知天花板：改写型提问（用户不用资料里的词）在 700k 字规模召回收敛在 **8/13 ≈ 62%**，多给块救不回来
- **免 key 兜底已从 DDG 换血为 Bing**（2026-09-17）：原 DDG 通道（`lite.duckduckgo.com` + `api.duckduckgo.com`）自 2026-08-27 实测起在本机网络直连超时、彻底不可用（三路全挂约 20s 且零结果）。现换为 **Bing 免费通道**：~~`cn.bing.com/search`（实测直连 200 无重定向）~~ → ★★ **`www.bing.com/search`（2026-09-25 换，`bug-ledger` B-019）**——**上面那句 2026-09-17 的实测读数今天正好反过来**：`cn.bing.com/search` 的 `301` 开始**丢掉 `/search` 路径**落到 `https://www.bing.com/?q=…`（首页），而旧码只判 `res.ok` ⇒ RSS 与 HTML 两路各拿回 0 条、**线上整条搜索静默哑掉至少一天**（12 条 `search_empty`，一句错都不报）。★★ **且可达性按出口 IP 而异**（本机出口 `www` → 302 到 `cn/search`、生产机 `www` 直 200），所以「换域名」只是让默认路径在当前出口上通，**不等于 Bing 免 key 通道变成了可靠依赖**——真网络复验走 `tools/probes/bing-channel-live.mjs`，**不进 CI**（老板 2026-09-25 拍板）——**RSS 主**（带 `&format=rss` 返回标准 RSS2.0，`<item>` 内 title/link/description 齐全，`<link>` 已是干净真实 URL）+ **HTML 兜底**（`b_algo` 块，标题在 `h2>a`、摘要在 `.b_caption>p`，链接同为真实直链、**无 `bing.com/ck/a` 跳转链**）。单路软降级不报失败，双路全挂才逐路冒泡原因。⚠️ RSS 端点自带 Microsoft 版权声明（限个人非商业用途的 RSS 聚合器呈现）——本仓为本地个人学习工具，用途相符；将来若商用需另评估。配了任一 key 时仍走托管三路，Bing 只在三家全无 key 时兜底
- vite 监听 IPv6 `::1`：本机验证用 `http://localhost:5173`（127.0.0.1 不通）
- **`npm run dev` 已能一条命令拉起两端**（2026-09-12 修，载体 `tools/dev.mjs`）：原脚本 `dev -w @sb/server & dev -w @sb/web` 里的 `&`，在 Windows 下 npm 把脚本交给 cmd 执行、而 cmd 的 `&` 是**命令分隔符、不是 shell 后台运算符**，前半个 `tsx watch` 又不退出 ⇒ 不能指望这一条同时把两端拉起（前端永远起不来）。现改为 `node tools/dev.mjs`：`spawn` 真并行 + `stdio: inherit` + 任一退出即收掉另一个并透传退出码。单端调试仍可分别跑 `npm run dev:server`（:18791）/ `npm run dev:web`（:5173）。历史坑留存：改前若误用 `npm run dev`，本机 5173/18791 已被占用时会先触发 vite `strictPort` 报错、干扰判读
- **行内公式不渲染**：`$a^2+b^2=c^2$` 按原文显示（未引 katex，保持 @sb/web 零运行时依赖）
- **转义修复只补「漏根」，不修「错命令」**：`repairJsonEscapes` 能把 LaTeX 单根反斜杠构成的非法 JSON 转义补成两根（真机 502 的主因，统计与复验见 `docs/ANSWER-STYLE-SPEC.md` §8.3）；但 `\theta` / `\nu` 这类恰好等于合法转义（`\t` / `\n`）的写法**无法与真制表符/换行区分，故意不修**——命中时该题仍走逐题回退，最坏丢那一题不连坐整组
- **重型图库仍未实现**：`mermaid` / `echarts` 围栏照旧降级代码块（刻意不引库）；数据图走自绘 ```chart，交互动画走 ```html 沙箱预览（2026-08-28 已上，真机验证预览文档源为 `null`、应用侧读不到其 DOM、读写接口均被拒）
- **预览页只活内存不落盘**：上限 20 条、服务重启即失效，页内提示回对话重新点「侧栏预览」；不做分享链接（本地单用户形态无场景）
- 面板宽 `min(--sb-browser-w, 46vw)` 且 `flex-shrink: 0`：窗口很窄时优先保面板、对话区被挤。未做可拖拽分隔条（内联 style 被门禁禁，需走 CSS 变量 + `documentElement.style.setProperty`，等真需要再加）
- **★ 本文件「目录速查」里的版本号与条数是手抄快照，全站没有任何一把锁守它**（2026-09-24 批次 H-2 现读抓到，当场只订正自己动过的那一行）：`metrics.mjs --check` 对的是 **README ↔ `docs/metrics.md`** 那一组数，**不读 AGENTS.md**；`gates/check.mjs` 四项是行数／内联样式／`any`／测试登记，**也不读 AGENTS.md** ⇒ 这里写的「契约 v0.1.x」「§5 已知代价 N 条」「测试基线 N 文件／N 例」全部靠人手抄，抄慢了就是错的。已实测到一处：目录速查把 `docs/SEO-SPEC.md` 抄成 v0.1.8，而该文件首行早已是 v0.1.9（同类的还有本条上方《已知约束》第一条（Node ABI 那条）末尾的「28 文件 / 343 例」——现役基线已是 **207 文件 / 2864 例**，那句属历史叙述，按「回标不回改」留着不动）。**口径：谁改契约文档，同一批把 AGENTS 目录速查里那一行的版本号一起带走；只订正自己本轮动过的行，不做全表清扫**（全表清扫会把别人在途的账一起改掉，违反 §多 AI 并行施工红线）。⚠️ 这仍是**人守纪律不是锁**：真要它变红，得加一条「grep 本文件里所有 `docs/X-SPEC.md` ＋ 那个文件首行 `> 版本：`」的比对门禁——本批**没做**，判据与代价先登记在这条里。
